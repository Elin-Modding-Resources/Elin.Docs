---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 1 file modified.
version: +EA 23.282 Stable Patch 2 - Plugin.BaseCore
changes: EInput
---

# +EA 23.282 Stable Patch 2 - Plugin.BaseCore

March 2, 2026

1 file modified.

## Important Changes

**None.**
## EInput

[`public class KeyMapManager`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6f73282fdb5dce264ca90058fa52bdfe6676fe33/Elin/Plugins.basecore/EInput.cs#L187-L199)
```cs:line-numbers=187
			action = EAction.Search
		};

		public KeyMap chat = new KeyMap // [!code ++]
		{ // [!code ++]
			action = EAction.Chat // [!code ++]
		}; // [!code ++]
 // [!code ++]
		public List<KeyMap> List()
		{
			return new List<KeyMap>
			{
				axisUp, axisDown, axisLeft, axisRight, axisUpLeft, axisUpRight, axisDownLeft, axisDownRight, journal, chara,
				inventory, ability, log, fire, wait, mouseLeft, mouseMiddle, mouseRight, report, quickSave, // [!code --]
				quickLoad, autoCombat, emptyHand, switchHotbar, examine, getAll, dump, mute, meditate, search // [!code --]
				inventory, ability, log, fire, chat, wait, mouseLeft, mouseMiddle, mouseRight, report, // [!code ++]
				quickSave, quickLoad, autoCombat, emptyHand, switchHotbar, examine, getAll, dump, mute, meditate, // [!code ++]
				search // [!code ++]
			};
		}
	}
```

[`private static EAction GetAction()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6f73282fdb5dce264ca90058fa52bdfe6676fe33/Elin/Plugins.basecore/EInput.cs#L1057-L1062)
```cs:line-numbers=1057
	{
		return EAction.Search;
	}
	if (Input.GetKeyDown(keys.chat.key)) // [!code ++]
	{ // [!code ++]
		return EAction.Chat; // [!code ++]
	} // [!code ++]
	if (keyFire.Update())
	{
		return keyFire.Action;
```
