---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 4 files modified. 1 new file created.
version: EA 23.343.2 Nightly
changes: DNA/+InvOwnerChoose/LayerInventory/TraitGodStatue/TraitPowerStatue
---

# EA 23.343.2 Nightly

September 7, 2026

4 files modified. 1 new file created.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [DNA (1)](#dna)
```cs:no-line-numbers
public static Thing GenerateManiGene(Card owner) // [!code --]
public static void GenerateManiGene(Card owner) // [!code ++]
```
## DNA

[`public static Thing GenerateRandomGene(int lv = -1, int seed = -1)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c03c906331960207e2e21ff8f0369da3385ec7ed/Elin/DNA.cs#L159-L179)
```cs:line-numbers=159
	return GenerateGene(r, Type.Superior, lv, seed);
}

public static Thing GenerateManiGene(Card owner) // [!code --]
public static void GenerateManiGene(Card owner) // [!code ++]
{
	owner.things.DestroyAll();
	Debug.Log("Mani:" + owner.c_seed); // [!code --]
	Rand.SetSeed(owner.c_seed); // [!code --]
	CardRow r = SpawnList.Get("chara").Select(100); // [!code --]
	Rand.SetSeed(owner.c_seed); // [!code --]
	Thing thing = GenerateGene(r, Type.Superior, owner.LV, owner.c_seed); // [!code --]
	thing.c_DNA.cost = 0; // [!code --]
	thing.c_DNA.isManiGene = true; // [!code --]
	thing.ChangeMaterial(36); // [!code --]
	thing.MakeRefFrom("mani"); // [!code --]
	Rand.SetSeed(); // [!code --]
	owner.c_seed++; // [!code --]
	return thing; // [!code --]
	for (int i = 0; i < 3; i++) // [!code ++]
	{ // [!code ++]
		Debug.Log("Mani:" + owner.c_seed); // [!code ++]
		Rand.SetSeed(owner.c_seed); // [!code ++]
		CardRow r = SpawnList.Get("chara").Select(100); // [!code ++]
		Rand.SetSeed(owner.c_seed); // [!code ++]
		Thing thing = GenerateGene(r, Type.Superior, owner.LV, owner.c_seed); // [!code ++]
		thing.c_DNA.cost = thing.c_DNA.cost / 2; // [!code ++]
		thing.c_DNA.isManiGene = true; // [!code ++]
		thing.ChangeMaterial(36); // [!code ++]
		thing.MakeRefFrom("mani"); // [!code ++]
		Rand.SetSeed(); // [!code ++]
		owner.c_seed++; // [!code ++]
		owner.AddCard(thing); // [!code ++]
	} // [!code ++]
}

public void Apply(Chara c)
```

## +InvOwnerChoose

::: details File Created
```cs
public class InvOwnerChoose : InvOwner
{
	public override bool UseGuide => true;

	public InvOwnerChoose(Card owner, Card container = null, CurrencyType _currency = CurrencyType.None)
		: base(owner, container, _currency)
	{
	}

	public override bool ShouldShowGuide(Thing t)
	{
		return t.parent == owner;
	}

	public override bool AllowMoved(Thing t)
	{
		return false;
	}

	public override bool OnCancelDrag(DragItemCard.DragInfo from)
	{
		return false;
	}

	public override void OnClick(ButtonGrid button)
	{
		Process(button);
	}

	public override void OnRightClick(ButtonGrid button)
	{
		Process(button);
	}

	public override void OnRightPressed(ButtonGrid button)
	{
	}

	public bool Process(ButtonGrid button)
	{
		if (!button || button.card == null || button.card.parent != owner)
		{
			return false;
		}
		owner.ChangeMaterial("onyx");
		owner.trait.OnUse(EClass.pc);
		EClass.pc.PickOrDrop(EClass.pc.pos, button.card.Thing);
		owner.things.DestroyAll();
		EClass.ui.CloseLayers();
		return true;
	}
}
```

:::
## LayerInventory

[`public static LayerInventory CreateContainer(Card owner, Card container)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c03c906331960207e2e21ff8f0369da3385ec7ed/Elin/LayerInventory.cs#L595-L599)
```cs:line-numbers=595
public static LayerInventory CreateContainer<T>(Card c, Card container, CurrencyType currency = CurrencyType.None) where T : InvOwner
{
	TooltipManager.Instance.disableTimer = 0.1f; // [!code ++]
	LayerInventory layerInventory = _Create();
	UIInventory uIInventory = layerInventory.SetInv();
	T owner = Activator.CreateInstance(typeof(T), c, container, currency) as T;
```

## TraitGodStatue

[`public class TraitGodStatue : TraitPowerStatue`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c03c906331960207e2e21ff8f0369da3385ec7ed/Elin/TraitGodStatue.cs#L1-L6)
```cs:line-numbers=1
public class TraitGodStatue : TraitPowerStatue
{
	public Religion Religion => EClass.game.religions.dictAll[GetParam(1)]; // [!code --]
	public override Religion Religion => EClass.game.religions.dictAll[GetParam(1)]; // [!code ++]

	public override bool IsImplemented()
	{
```

[`public void OnChangeMaterial()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c03c906331960207e2e21ff8f0369da3385ec7ed/Elin/TraitGodStatue.cs#L19-L25)
```cs:line-numbers=19
owner.rarity = (owner.isOn ? Rarity.Artifact : Rarity.Normal);
if (Religion.id == "machine")
{
	owner.AddCard(DNA.GenerateManiGene(owner)); // [!code --]
	DNA.GenerateManiGene(owner); // [!code ++]
}
if (owner.placeState == PlaceState.installed)
{
```

[`public override void _OnUse(Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c03c906331960207e2e21ff8f0369da3385ec7ed/Elin/TraitGodStatue.cs#L32-L49)
```cs:line-numbers=32
Religion.Talk("shrine");
switch (Religion.id)
{
case "machine": // [!code ++]
	break; // [!code ++]
case "harvest":
{
	Thing t = ThingGen.Create("book_kumiromi");
	EClass.pc.Pick(t);
	break;
}
case "machine": // [!code --]
{ // [!code --]
	Thing t2 = owner.things.Find("gene") ?? DNA.GenerateManiGene(owner); // [!code --]
	EClass.pc.Pick(t2); // [!code --]
	break; // [!code --]
} // [!code --]
case "healing":
{
	Msg.Say("jure_hug");
```

## TraitPowerStatue

[`public class TraitPowerStatue : TraitItem`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c03c906331960207e2e21ff8f0369da3385ec7ed/Elin/TraitPowerStatue.cs#L1-L5)
```cs:line-numbers=1
public class TraitPowerStatue : TraitItem
{
	public virtual Religion Religion => null; // [!code ++]
 // [!code ++]
	public override bool CanBeDestroyed
	{
		get
```

[`public override bool CanBeDestroyed`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c03c906331960207e2e21ff8f0369da3385ec7ed/Elin/TraitPowerStatue.cs#L21-L25)
```cs:line-numbers=21
public override bool UseExtra => owner.isOn;

public override string IDInvStyle => "jewelbox"; // [!code ++]
 // [!code ++]
public override int CostRerollShop => 0; // [!code ++]
 // [!code ++]
public override bool CanStack => false;

public override void OnCreate(int lv)
```

[`public override bool OnUse(Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c03c906331960207e2e21ff8f0369da3385ec7ed/Elin/TraitPowerStatue.cs#L47-L52)
```cs:line-numbers=47
	Msg.SayNothingHappen();
	return true;
}
if (Religion?.id == "machine" && owner.material.id == 12) // [!code ++]
{ // [!code ++]
	owner.things.SetSize(5, 2); // [!code ++]
	if (owner.things.Count < 3) // [!code ++]
	{ // [!code ++]
		DNA.GenerateManiGene(owner); // [!code ++]
	} // [!code ++]
	LayerInventory.CreateContainer<InvOwnerChoose>(owner, owner); // [!code ++]
	return false; // [!code ++]
} // [!code ++]
Msg.Say("shrine_power", owner);
if (flag)
{
```
