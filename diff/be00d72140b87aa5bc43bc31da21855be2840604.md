---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 17 files modified.
version: EA 23.282 Stable Patch 2
changes: AIAct/AI_Churyu/AI_Fish/AM_Adv/ActEffect/ActRide/CardActor/Chara/ContentConfigInput/CoreConfig/DNA/Dialog/DramaCustomSequence/ELEMENT/ID_Achievement/Msg/Player
---

# EA 23.282 Stable Patch 2

March 2, 2026

17 files modified.

## Important Changes

**None.**
## AIAct

[`public override bool IsToolValid()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/AIAct.cs#L172-L177)
```cs:line-numbers=172
		return false;
	}

	public T GetChild<T>() where T : AIAct // [!code ++]
	{ // [!code ++]
		if (this is T) // [!code ++]
		{ // [!code ++]
			return this as T; // [!code ++]
		} // [!code ++]
		AIAct aIAct = child; // [!code ++]
		return ((aIAct != null) ? aIAct.GetChild<T>() : null) ?? null; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public virtual void OnStart()
	{
	}
```

## AI_Churyu

[`public class AI_Churyu : AIWork`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/AI_Churyu.cs#L10-L15)
```cs:line-numbers=10

	public override IEnumerable<Status> Run()
	{
		if (restartCount == 0) // [!code ++]
		{ // [!code ++]
			EClass.player.UpdateNyan(); // [!code ++]
		} // [!code ++]
		if (!slave.ExistsOnMap || churyu.GetRootCard() != slave)
		{
			yield return Success();
```

## AI_Fish

[`public override void OnProgressComplete()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/AI_Fish.cs#L115-L124)
```cs:line-numbers=115
		}
		if (EClass.rnd(EClass.debug.enable ? 2 : 1000) == 0 && thing.category.IsChildOf("fish"))
		{
			owner.PlaySound("escape"); // [!code --]
			owner.PlayEffect("fizzle");
			if (owner.IsPC)
			{
				owner.PlaySound("escape"); // [!code ++]
				owner.Say("fish_ehe");
				owner.Say("fish_ehe2");
				owner.Say("fish_ehe3");
```

## AM_Adv

[`using System;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/AM_Adv.cs#L1-L5)
```cs:line-numbers=1
using System;
using System.Collections.Generic;
using System.Text; // [!code ++]
using UnityEngine;

public class AM_Adv : AM_BaseGameMode
```

[`public override void _OnUpdateInput()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/AM_Adv.cs#L823-L828)
```cs:line-numbers=823
	}
	switch (EInput.action)
	{
	case EAction.Chat: // [!code ++]
		Dialog.InputName("", "", delegate(bool cancel, string text) // [!code ++]
		{ // [!code ++]
			if (!cancel && !text.IsEmpty()) // [!code ++]
			{ // [!code ++]
				Chara chara = EClass.pc; // [!code ++]
				if ((text[0] == '@' || text[0] == '＠') && text.Length > 1 && int.TryParse(text[1].ToString().Normalize(NormalizationForm.FormKC), out var result)) // [!code ++]
				{ // [!code ++]
					text = text.Substring(2); // [!code ++]
					chara = EClass.pc.party.members.TryGet(result); // [!code ++]
				} // [!code ++]
				if (text.StartsWith('@')) // [!code ++]
				{ // [!code ++]
					text = text.Substring(1); // [!code ++]
				} // [!code ++]
				chara.SayRaw(text); // [!code ++]
				Msg.SetColor("ono"); // [!code ++]
				string text2 = text; // [!code ++]
				if (!text.StartsWith('*') && !text.StartsWith('(') && !text.StartsWith('（')) // [!code ++]
				{ // [!code ++]
					text = text.Bracket(); // [!code ++]
				} // [!code ++]
				Msg.Say(text, chara); // [!code ++]
				if (text2 == "nyan") // [!code ++]
				{ // [!code ++]
					Msg.SetColor("save"); // [!code ++]
					Msg.Say("*" + EClass.player.stats.lastChuryu + " nyan*"); // [!code ++]
				} // [!code ++]
			} // [!code ++]
		}, Dialog.InputType.None); // [!code ++]
		break; // [!code ++]
	case EAction.Search:
		EClass.ui.widgets.Toggle("Search")?.SoundActivate();
		break;
```

## ActEffect

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/ActEffect.cs#L1090-L1102)
```cs:line-numbers=1090
	{
		CC.PlaySound("clean_floor");
		Msg.Say("exterminate");
		List<Chara> list3 = EClass._map.charas.Where((Chara c) => c.isCopy && !c.IsPCFaction).ToList(); // [!code --]
		if (list3.Count == 0) // [!code --]
		List<Chara> list2 = EClass._map.charas.Where((Chara c) => c.isCopy && !c.IsPCFaction).ToList(); // [!code ++]
		if (list2.Count == 0) // [!code ++]
		{
			Msg.SayNothingHappen();
			return;
		}
		foreach (Chara item in list3) // [!code --]
		foreach (Chara item in list2) // [!code ++]
		{
			item.Say("split_fail", item);
			item.PlayEffect("vanish");
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/ActEffect.cs#L1110-L1119)
```cs:line-numbers=1110
		{
			return;
		}
		Thing thing = ThingGen.Create("mine"); // [!code --]
		thing.c_idRefCard = "dog_mine"; // [!code --]
		Thing thing2 = ThingGen.Create("mine"); // [!code ++]
		thing2.c_idRefCard = "dog_mine"; // [!code ++]
		Zone.ignoreSpawnAnime = true;
		EClass._zone.AddCard(thing, CC.pos).Install(); // [!code --]
		EClass._zone.AddCard(thing2, CC.pos).Install(); // [!code ++]
		break;
	}
	case EffectId.LittleSisterMigration:
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/ActEffect.cs#L1123-L1129)
```cs:line-numbers=1123
			Msg.SayNothingHappen();
			return;
		}
		List<Chara> list2 = new List<Chara>(); // [!code --]
		List<Chara> list3 = new List<Chara>(); // [!code ++]
		bool flag3 = false;
		foreach (Chara chara2 in EClass._map.charas)
		{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/ActEffect.cs#L1134-L1157)
```cs:line-numbers=1134
					flag3 = false;
					continue;
				}
				list2.Add(chara2); // [!code --]
				list3.Add(chara2); // [!code ++]
				flag3 = true;
			}
		}
		if (list2.Count == 0) // [!code --]
		if (list3.Count == 0) // [!code ++]
		{
			Msg.SayNothingHappen();
			return;
		}
		EClass.pc.PlaySound("chime_angel");
		foreach (Chara item2 in list2) // [!code --]
		foreach (Chara item2 in list3) // [!code ++]
		{
			item2.PlayEffect("revive");
			item2.Destroy();
		}
		Msg.Say("little_migration", list2.Count.ToString() ?? ""); // [!code --]
		EClass._zone.ModInfluence(list2.Count); // [!code --]
		EClass.player.stats.sistersDepart += list2.Count; // [!code --]
		Msg.Say("little_migration", list3.Count.ToString() ?? ""); // [!code ++]
		EClass._zone.ModInfluence(list3.Count); // [!code ++]
		EClass.player.stats.sistersDepart += list3.Count; // [!code ++]
		break;
	}
	case EffectId.MagicMap:
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/ActEffect.cs#L1207-L1215)
```cs:line-numbers=1207
		TC.PlaySound("curse3");
		TC.PlayEffect("curse");
		TC.Say("forgetItems", TC);
		int num3 = power / 50 + 1 + EClass.rnd(3); // [!code --]
		int num4 = power / 50 + 1 + EClass.rnd(3); // [!code ++]
		List<Thing> source = TC.things.List((Thing t) => t.c_IDTState == 0);
		for (int j = 0; j < num3; j++) // [!code --]
		for (int j = 0; j < num4; j++) // [!code ++]
		{
			source.RandomItem().c_IDTState = 5;
		}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/ActEffect.cs#L1221-L1230)
```cs:line-numbers=1221
	case EffectId.EnchantArmorGreat:
	{
		bool armor = id == EffectId.EnchantArmor || id == EffectId.EnchantArmorGreat;
		bool flag4 = id == EffectId.EnchantWeaponGreat || id == EffectId.EnchantArmorGreat; // [!code --]
		bool flag6 = id == EffectId.EnchantWeaponGreat || id == EffectId.EnchantArmorGreat; // [!code ++]
		if (!tc.isThing)
		{
			LayerDragGrid.CreateEnchant(CC, armor, flag4, state); // [!code --]
			LayerDragGrid.CreateEnchant(CC, armor, flag6, state); // [!code ++]
			return;
		}
		cc.PlaySound("identify");
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/ActEffect.cs#L1235-L1242)
```cs:line-numbers=1235
			tc.ModEncLv(-1);
			break;
		}
		int num4 = (flag4 ? 4 : 2) + (blessed ? 1 : 0); // [!code --]
		if (tc.encLV >= num4) // [!code --]
		int num6 = (flag6 ? 4 : 2) + (blessed ? 1 : 0); // [!code ++]
		if (tc.encLV >= num6) // [!code ++]
		{
			cc.Say("enc_resist", tc);
			break;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/ActEffect.cs#L1272-L1288)
```cs:line-numbers=1272
			LayerDragGrid.CreateUncurse(CC, state);
			return;
		}
		Thing thing3 = tc.Thing; // [!code --]
		if (thing3.blessedState == BlessedState.Cursed) // [!code --]
		Thing thing = tc.Thing; // [!code ++]
		if (thing.blessedState == BlessedState.Cursed) // [!code ++]
		{
			thing3.SetBlessedState(BlessedState.Normal); // [!code --]
			thing.SetBlessedState(BlessedState.Normal); // [!code ++]
		}
		else if (thing3.blessedState == BlessedState.Doomed) // [!code --]
		else if (thing.blessedState == BlessedState.Doomed) // [!code ++]
		{
			thing3.SetBlessedState(BlessedState.Normal); // [!code --]
			thing.SetBlessedState(BlessedState.Normal); // [!code ++]
		}
		thing3.GetRootCard()?.TryStack(thing3); // [!code --]
		LayerInventory.SetDirty(thing3); // [!code --]
		thing.GetRootCard()?.TryStack(thing); // [!code ++]
		LayerInventory.SetDirty(thing); // [!code ++]
		break;
	}
	case EffectId.Lighten:
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/ActEffect.cs#L1298-L1319)
```cs:line-numbers=1298
		}
		cc.PlaySound("offering");
		cc.PlayEffect("buff");
		int num6 = (tc.isWeightChanged ? tc.c_weight : tc.Thing.source.weight); // [!code --]
		int num3 = (tc.isWeightChanged ? tc.c_weight : tc.Thing.source.weight); // [!code ++]
		tc.isWeightChanged = true;
		Element orCreateElement = tc.elements.GetOrCreateElement(64);
		Element orCreateElement2 = tc.elements.GetOrCreateElement(65);
		Element orCreateElement3 = tc.elements.GetOrCreateElement(67);
		Element orCreateElement4 = tc.elements.GetOrCreateElement(66);
		bool flag6 = tc.IsEquipmentOrRangedOrAmmo || tc.IsThrownWeapon; // [!code --]
		bool flag4 = tc.IsEquipmentOrRangedOrAmmo || tc.IsThrownWeapon; // [!code ++]
		if (flag)
		{
			num6 = (int)(0.01f * (float)num6 * (float)power * 0.75f + 500f); // [!code --]
			if (num6 < 0 || num6 > 10000000) // [!code --]
			num3 = (int)(0.01f * (float)num3 * (float)power * 0.75f + 500f); // [!code ++]
			if (num3 < 0 || num3 > 10000000) // [!code ++]
			{
				num6 = 10000000; // [!code --]
				flag6 = false; // [!code --]
				num3 = 10000000; // [!code ++]
				flag4 = false; // [!code ++]
			}
			if (flag6) // [!code --]
			if (flag4) // [!code ++]
			{
				if (tc.IsWeapon || tc.IsThrownWeapon || tc.IsAmmo)
				{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/ActEffect.cs#L1330-L1341)
```cs:line-numbers=1330
		}
		else
		{
			num6 = num6 * (100 - power / 10) / 100; // [!code --]
			num3 = num3 * (100 - power / 10) / 100; // [!code ++]
			if (blessed)
			{
				power /= 4;
			}
			if (flag6) // [!code --]
			if (flag4) // [!code ++]
			{
				if (tc.IsWeapon || tc.IsThrownWeapon || tc.IsAmmo)
				{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/ActEffect.cs#L1350-L1356)
```cs:line-numbers=1350
			}
			cc.Say("lighten", tc);
		}
		tc.c_weight = num6; // [!code --]
		tc.c_weight = num3; // [!code ++]
		tc.SetDirtyWeight();
		if (tc.parent == null)
		{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/ActEffect.cs#L1379-L1399)
```cs:line-numbers=1379
		{
			blesstedState = state
		});
		Thing thing2 = ThingGen.Create(tc.id, -1, (int)((long)num5 * (long)power / 400)); // [!code --]
		thing2.genLv = num5; // [!code --]
		Thing thing3 = ThingGen.Create(tc.id, -1, (int)((long)num5 * (long)power / 400)); // [!code ++]
		thing3.genLv = num5; // [!code ++]
		if (tc.c_uidAttune != 0)
		{
			thing2.c_uidAttune = tc.c_uidAttune; // [!code --]
			if ((thing2.id == "amulet_engagement" || thing2.id == "ring_engagement") && tc.c_uidAttune != EClass.pc.uid) // [!code --]
			thing3.c_uidAttune = tc.c_uidAttune; // [!code ++]
			if (thing3.id == "amulet_engagement" || thing3.id == "ring_engagement") // [!code ++]
			{
				thing2.elements.ModBase(484, 3); // [!code --]
				if (tc.c_uidAttune != EClass.pc.uid) // [!code ++]
				{ // [!code ++]
					thing3.elements.ModBase(484, 3); // [!code ++]
				} // [!code ++]
				if (thing3.rarity < Rarity.Mythical) // [!code ++]
				{ // [!code ++]
					thing3.rarity = Rarity.Mythical; // [!code ++]
				} // [!code ++]
			}
		}
		tc.Destroy();
		CC.Pick(thing2, msg: false); // [!code --]
		CC.Pick(thing3, msg: false); // [!code ++]
		if (!CC.IsPC)
		{
			CC.TryEquip(thing2); // [!code --]
			CC.TryEquip(thing3); // [!code ++]
		}
		break;
	}
```

## ActRide

[`public class ActRide : Ability`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/ActRide.cs#L6-L11)
```cs:line-numbers=6

	public override bool Perform()
	{
		if (EClass._zone is Zone_Wedding) // [!code ++]
		{ // [!code ++]
			Msg.SayInvalidAction(); // [!code ++]
			return false; // [!code ++]
		} // [!code ++]
		List<Chara> list = Act.TP.ListCharas();
		list.Reverse();
		bool flag = false;
```

## CardActor

[`public virtual void OnRender(RenderParam p)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/CardActor.cs#L182-L188)
```cs:line-numbers=182
	}
	base.transform.position = tempV;
	base.transform.localScale = tempV2;
	destLiquid = Mathf.Lerp(destLiquid, p.liquidLv, Core.gameDelta * (float)((destLiquid > (float)p.liquidLv) ? 90 : 20)); // [!code --]
	destLiquid = Mathf.Lerp(destLiquid, p.liquidLv, Core.gameDeltaNoPause * (float)((destLiquid > (float)p.liquidLv) ? 90 : 20)); // [!code ++]
	mpb.SetFloat("_MatColor", p.matColor);
	mpb.SetFloat("_Color", p.color);
	mpb.SetFloat("_Liquid", destLiquid);
```

## Chara

[`public void TickConditions()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/Chara.cs#L3781-L3786)
```cs:line-numbers=3781
		}
		break;
	}
	case 4: // [!code ++]
		if (IsPC) // [!code ++]
		{ // [!code ++]
			EClass.player.UpdateNyan(); // [!code ++]
		} // [!code ++]
		break; // [!code ++]
	case 5:
	case 30:
		if (isWeakToSunlight && pos.IsSunLit)
```

[`public void ShowDialog()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/Chara.cs#L7223-L7228)
```cs:line-numbers=7223
		{
			return;
		}
		if (id == "nino") // [!code ++]
		{ // [!code ++]
			Steam.GetAchievement(ID_Achievement.NINO); // [!code ++]
		} // [!code ++]
		switch (id)
		{
		case "loytel":
```

## ContentConfigInput

[`public override void OnInstantiate()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/ContentConfigInput.cs#L305-L310)
```cs:line-numbers=305
			uIList.Add(keys.dump);
			uIList.Add(keys.mute);
			uIList.Add(keys.meditate);
			uIList.Add(keys.chat); // [!code ++]
		}
	};
	_refreshList();
```

## CoreConfig

[`public static CoreConfig TryLoadConfig()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/CoreConfig.cs#L697-L702)
```cs:line-numbers=697
		{
			coreConfig.ui.balloonBG = true;
		}
		if (coreConfig.version.IsBelow(0, 23, 282)) // [!code ++]
		{ // [!code ++]
			coreConfig.input.keys.chat.key = KeyCode.Return; // [!code ++]
		} // [!code ++]
		return coreConfig;
	}

```

## DNA

[`void AddAction()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/DNA.cs#L360-L366)
```cs:line-numbers=360
			ActList.Item a = model.ability.list.items.RandomItem();
			if (!(a.act.source.category != "ability") && a.act.source.geneSlot >= 0)
			{
				AddVal(a.act.source.id, a.chance * ((!a.pt) ? 1 : (-1)), allowStack: false, (int v) => 8 + a.act.source.cost[0] / 10 * 2); // [!code --]
				AddVal(a.act.source.id, a.chance * ((!a.pt) ? 1 : (-1)), allowStack: false, (int v) => 8 + a.act.source.cost[0] / 10 * 2, allowNegative: true); // [!code ++]
				action++;
			}
		}
```

[`void AddAction()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/DNA.cs#L368-L374)
```cs:line-numbers=368
	void AddAttribute()
	{
		Element element = listAttb[EClass.rnd(3)];
		AddVal(element.id, EClass.rndHalf(element.ValueWithoutLink / 2), allowStack: true, (int v) => v / 5 + 1); // [!code --]
		AddVal(element.id, EClass.rndHalf(element.ValueWithoutLink / 2), allowStack: true, (int v) => v / 5 + 1, allowNegative: false); // [!code ++]
	}
	void AddBody()
	{
```

[`void AddBody()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/DNA.cs#L386-L392)
```cs:line-numbers=386
			}
			if (bodySlot != null)
			{
				AddVal(bodySlot.elementId, 1, allowStack: false, (int v) => 20); // [!code --]
				AddVal(bodySlot.elementId, 1, allowStack: false, (int v) => 20, allowNegative: false); // [!code ++]
				body++;
			}
		}
```

[`void AddFeat()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/DNA.cs#L403-L409)
```cs:line-numbers=403
				{
					maxSlot = e.source.geneSlot;
				}
				AddVal(e.id, 1, allowStack: false, (int v) => e.source.cost[0] * 5); // [!code --]
				AddVal(e.id, 1, allowStack: false, (int v) => e.source.cost[0] * 5, allowNegative: false); // [!code ++]
			}
		}
	}
```

[`void AddRandom(int n)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/DNA.cs#L428-L434)
```cs:line-numbers=428
	void AddSkill()
	{
		Element element2 = listSkill[Mathf.Clamp(EClass.rnd(6), 0, listSkill.Count - 1)];
		AddVal(element2.id, EClass.rndHalf(element2.ValueWithoutLink / 2), allowStack: true, (int v) => v / 5 + 1); // [!code --]
		AddVal(element2.id, EClass.rndHalf(element2.ValueWithoutLink / 2), allowStack: true, (int v) => v / 5 + 1, allowNegative: false); // [!code ++]
	}
	void AddSpecial()
	{
```

[`void AddSpecial()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/DNA.cs#L453-L461)
```cs:line-numbers=453
			AddAttribute();
		}
	}
	void AddVal(int id, int v, bool allowStack, Func<int, int> funcCost) // [!code --]
	void AddVal(int id, int v, bool allowStack, Func<int, int> funcCost, bool allowNegative) // [!code ++]
	{
		if (v >= 0) // [!code --]
		if (v >= 0 || allowNegative) // [!code ++]
		{
			bool flag = false;
			int num = EClass.curve(v, 20, 10, 90);
```

## Dialog

[`private void Update()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/Dialog.cs#L122-L128)
```cs:line-numbers=122

	public override void OnUpdateInput()
	{
		if ((bool)input && option.canClose && (Input.GetKeyDown(KeyCode.Return) || Input.GetKeyDown(KeyCode.KeypadEnter))) // [!code --]
		if ((bool)input && option.canClose && (Input.GetKeyDown(KeyCode.Return) || Input.GetKeyDown(KeyCode.KeypadEnter)) && keymap == null) // [!code ++]
		{
			isInputEnter = true;
			Close();
```

[`public override void OnUpdateInput()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/Dialog.cs#L150-L159)
```cs:line-numbers=150
			KeyCode.Mouse3,
			KeyCode.Mouse4,
			KeyCode.Escape,
			KeyCode.Return, // [!code --]
			KeyCode.LeftShift,
			KeyCode.Delete, // [!code --]
			KeyCode.Backspace // [!code --]
			KeyCode.Delete // [!code ++]
		};
		if (Input.GetKeyDown(KeyCode.Delete))
		{
```

## DramaCustomSequence

[`public void Build(Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/DramaCustomSequence.cs#L274-L279)
```cs:line-numbers=274
				Choice2("daBlessing", "_blessing");
			}
		}
		else if (c.isDrunk || c.HasElement(1275) || EClass.debug.enable) // [!code ++]
		{ // [!code ++]
			Choice2(flag2 ? "daBird" : "daTail", "_tail"); // [!code ++]
		} // [!code ++]
	}
	if (c.IsHomeMember())
	{
```

[`public void Build(Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/DramaCustomSequence.cs#L603-L608)
```cs:line-numbers=603
		{
			destThing.Attune(c);
			destThing.elements.ModBase(484, 3);
			if (destThing.rarity < Rarity.Mythical) // [!code ++]
			{ // [!code ++]
				destThing.rarity = Rarity.Mythical; // [!code ++]
			} // [!code ++]
			c.AddCard(destThing);
			c.TryEquip(destThing);
			GameLang.refDrama1 = EClass.world.date.year.ToString() ?? "";
```

## ELEMENT

[`public void _WriteNote(UINote n, Chara c, Act act)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/ELEMENT.cs#L685-L691)
```cs:line-numbers=685
		if (c.IsPC && c.HasCondition<StanceManaCost>())
		{
			int num2 = c.Evalue(1657);
			if (num2 > 0 && vPotential >= 2) // [!code --]
			if (num2 > 0 && (vPotential >= 2 || c.ability.Has(act.id))) // [!code ++]
			{
				num = num * (100 - num2 * 20) / 100;
			}
```

## ID_Achievement

[`public enum ID_Achievement`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/ID_Achievement.cs#L13-L17)
```cs:line-numbers=13
	CHURYU,
	MELILITH,
	LARNNEIRE,
	LARNNEIRE2 // [!code --]
	LARNNEIRE2, // [!code ++]
	CHURYU2, // [!code ++]
	NINO, // [!code ++]
	CHURYU3 // [!code ++]
}
```

## Msg

[`public static string SayCannotUseHere()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/Msg.cs#L103-L108)
```cs:line-numbers=103
		return Say("cannot_use_here");
	}

	public static string SayInvalidAction() // [!code ++]
	{ // [!code ++]
		return Say("invalidAction"); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static void SayGod(string s, Card owner = null)
	{
		SetColor(colors.TalkGod);
```

## Player

[`public class Stats : EClass`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/Player.cs#L141-L146)
```cs:line-numbers=141

		public int lastShippingExpMax;

		public int lastChuryu; // [!code ++]
 // [!code ++]
		public long nefiaBeaten
		{
			get
```

[`public void OnAdvanceDay()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/be00d72140b87aa5bc43bc31da21855be2840604/Elin/Player.cs#L2430-L2435)
```cs:line-numbers=2430
		}
	}

	public void UpdateNyan() // [!code ++]
	{ // [!code ++]
		int num = 0; // [!code ++]
		foreach (Chara chara in EClass._map.charas) // [!code ++]
		{ // [!code ++]
			AI_Churyu child = chara.ai.GetChild<AI_Churyu>(); // [!code ++]
			if (child != null && child.slave == EClass.pc) // [!code ++]
			{ // [!code ++]
				num++; // [!code ++]
			} // [!code ++]
		} // [!code ++]
		EClass.player.stats.lastChuryu = num; // [!code ++]
		if (num >= 20) // [!code ++]
		{ // [!code ++]
			Steam.GetAchievement(ID_Achievement.CHURYU2); // [!code ++]
		} // [!code ++]
		if (num >= 50) // [!code ++]
		{ // [!code ++]
			Steam.GetAchievement(ID_Achievement.CHURYU3); // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public bool TryAbortAutoCombat(bool immediate = true)
	{
		if (!(EClass.pc.ai is GoalAutoCombat))
```
