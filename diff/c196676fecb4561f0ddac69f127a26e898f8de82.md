---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 8 files modified.
version: EA 23.283 Nightly Patch 2
changes: ActEffect/Chara/ConPeaky/ID_Achievement/ModManager/ModUtil/SourceImporter/Trait
---

# EA 23.283 Nightly Patch 2

March 6, 2026

8 files modified.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [ModManager (1)](#modmanager)
```cs:no-line-numbers
public void ImportModDialogs() // [!code --]
public void ImportAllModDialogs() // [!code ++]
```
## ActEffect

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c196676fecb4561f0ddac69f127a26e898f8de82/Elin/ActEffect.cs#L2058-L2067)
```cs:line-numbers=2058
			tc.Say("faith_bless", tc, faith.Name);
		}
		tc.ModExp(306, power * 10);
		tc.ModExp(85, power * 10); // [!code --]
		if (tc.elements.Base(85) >= tc.elements.Value(306)) // [!code --]
		if (tc.elements.Base(85) < tc.elements.Value(306)) // [!code ++]
		{
			tc.elements.SetBase(85, tc.elements.Value(306)); // [!code --]
			tc.ModExp(85, power * 10); // [!code ++]
		}
		break;
	}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c196676fecb4561f0ddac69f127a26e898f8de82/Elin/ActEffect.cs#L2483-L2489)
```cs:line-numbers=2483
		{
			Redirect(EffectId.BuffStats, BlessedState.Cursed, new ActRef
			{
				aliasEle = "STR" // [!code --]
				n1 = "STR" // [!code ++]
			});
		}
		break;
```

## Chara

[`public void ApplyRace(bool remove = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c196676fecb4561f0ddac69f127a26e898f8de82/Elin/Chara.cs#L2301-L2309)
```cs:line-numbers=2301
		}
		body.RefreshBodyParts();
		elements.ApplyElementMap(base.uid, SourceValueType.Chara, race.elementMap, base.DefaultLV, remove, applyFeat: true);
		if (!remove && race.id == "bike" && id != "bike_cub") // [!code --]
		if (!remove) // [!code ++]
		{
			SetFeat(1423, (id == "chara" || id == "player") ? 10 : (1 + EClass.rnd(10))); // [!code --]
			if (race.id == "bike" && id != "bike_cub") // [!code ++]
			{ // [!code ++]
				SetFeat(1423, (id == "chara" || id == "player") ? 10 : (1 + EClass.rnd(10))); // [!code ++]
			} // [!code ++]
			if (race.id == "horse" && EClass.rnd(5) == 0) // [!code ++]
			{ // [!code ++]
				SetFeat(1423, 1 + EClass.rnd(5)); // [!code ++]
			} // [!code ++]
		}
	}

```

## ConPeaky

[`public override void Tick()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c196676fecb4561f0ddac69f127a26e898f8de82/Elin/ConPeaky.cs#L24-L35)
```cs:line-numbers=24
	}
	int num = ec.Value(79);
	int num2 = owner.Evalue(1423) * 10;
	if (num < num2) // [!code --]
	if (num >= num2) // [!code ++]
	{
		ec.ModBase(79, Mathf.Clamp(num2 / 10, 1, num2 - num)); // [!code --]
		if (ec.Value(79) == num2) // [!code --]
		return; // [!code ++]
	} // [!code ++]
	ec.ModBase(79, Mathf.Clamp(num2 / 10, 1, num2 - num)); // [!code ++]
	if (ec.Value(79) == num2) // [!code ++]
	{ // [!code ++]
		owner.PlaySound("bike_kick"); // [!code ++]
		if (owner.host != null) // [!code ++]
		{
			owner.PlaySound("bike_kick"); // [!code --]
			owner.host.ModExp((owner.host.ride == owner) ? 226 : 227, owner.Evalue(1423) * 20); // [!code ++]
		}
	}
}
```

## ID_Achievement

[`public enum ID_Achievement`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c196676fecb4561f0ddac69f127a26e898f8de82/Elin/ID_Achievement.cs#L18-L22)
```cs:line-numbers=18
	NINO,
	CHURYU3,
	DEMITAS,
	KETTLE // [!code --]
	KETTLE, // [!code ++]
	FAITH_HEALING, // [!code ++]
	ONEV // [!code ++]
}
```

## ModManager

[`public override void Init(string path, string defaultPackage = "_Elona")`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c196676fecb4561f0ddac69f127a26e898f8de82/Elin/ModManager.cs#L67-L73)
```cs:line-numbers=67
			{
				ImportSourceLocalizations(lang as string);
			}
			ImportModDialogs(); // [!code --]
			ImportAllModDialogs(); // [!code ++]
		});
	}

```

[`public void ImportSourceLocalizations(string lang)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c196676fecb4561f0ddac69f127a26e898f8de82/Elin/ModManager.cs#L161-L167)
```cs:line-numbers=161
		{
			continue;
		}
		if (Lang.IsBuiltin(modPackage.Mapping.SourceLangMod) && Lang.IsBuiltin(lang)) // [!code --]
		if (Lang.IsBuiltin(lang)) // [!code ++]
		{
			break;
		}
```

[`public void ImportAllModSourceSheets()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c196676fecb4561f0ddac69f127a26e898f8de82/Elin/ModManager.cs#L194-L199)
```cs:line-numbers=194
{
	try
	{
		SourceCache.InvalidateCacheVersion(); // [!code ++]
		SourceImporter.HotInit(new SourceData[2]
		{
			EClass.sources.elements,
```

[`public void ImportAllModSourceSheets()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c196676fecb4561f0ddac69f127a26e898f8de82/Elin/ModManager.cs#L216-L221)
```cs:line-numbers=216
			}
		}
		ModUtil.sourceImporter.ImportFilesCached(list);
		SourceCache.FinalizeCache(); // [!code ++]
		SourceCache.InvalidateCacheBlobs(); // [!code ++]
		SourceCache.ClearDetail(); // [!code ++]
	}
	catch (Exception message)
	{
```

[`public void ImportAllModSourceSheets()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c196676fecb4561f0ddac69f127a26e898f8de82/Elin/ModManager.cs#L224-L230)
```cs:line-numbers=224
		Debug.Log("#source finished importing workbooks");
	}

	public void ImportModDialogs() // [!code --]
	public void ImportAllModDialogs() // [!code ++]
	{
		Lang.extraExcelDialogs = new HashSet<string>(PathComparer.Default);
		Lang.excelDialog = null;
```

[`public IEnumerator RefreshMods(Action onComplete, bool syncMods)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c196676fecb4561f0ddac69f127a26e898f8de82/Elin/ModManager.cs#L307-L320)
```cs:line-numbers=307
		if (mappedPackages.TryGetValue(item.id, out var value) && value.isInPackages)
		{
			value.hasPublishedPackage = true;
			mappedPackages[item.id] = item as ModPackage; // [!code --]
		}
	}
	packages.RemoveAll((BaseModPackage p) => p.hasPublishedPackage); // [!code --]
	_loading.Log($"Total number of mods:{packages.Count}");
	_loading.Log("Activating Mods...");
	yield return awaiter;
	ActivatePackages();
	foreach (BaseModPackage package in packages) // [!code ++]
	{ // [!code ++]
		if (package.activated) // [!code ++]
		{ // [!code ++]
			mappedPackages[package.id] = package as ModPackage; // [!code ++]
		} // [!code ++]
	} // [!code ++]
	BaseModManager.isInitialized = true;
	yield return awaiter;
	onComplete?.Invoke();
```

## ModUtil

[`public static SoundData LoadSoundData(FileInfo soundFile)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c196676fecb4561f0ddac69f127a26e898f8de82/Elin/ModUtil.cs#L151-L157)
```cs:line-numbers=151
	string fileNameWithoutExtension = Path.GetFileNameWithoutExtension(soundFile.FullName);
	string fullName = soundFile.FullName;
	AudioType audioType = GetAudioType(soundFile.Extension);
	bool stream = fileNameWithoutExtension.StartsWith("BGM/") && audioType == AudioType.OGGVORBIS; // [!code --]
	bool stream = fullName.NormalizePath().Contains("/BGM/") && audioType == AudioType.OGGVORBIS; // [!code ++]
	using UnityWebRequest unityWebRequest = AudioClipStream.GetAudioClip("file://" + fullName, audioType, compressed: false, stream);
	unityWebRequest.SendWebRequest();
	Stopwatch stopwatch = Stopwatch.StartNew();
```

## SourceImporter

[`where c.IsDirtyOrEmpty`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c196676fecb4561f0ddac69f127a26e898f8de82/Elin/SourceImporter.cs#L176-L183)
```cs:line-numbers=176
			hashSet.Add(sourceData2);
		}
	}
	SourceCache.FinalizeCache(); // [!code --]
	SourceCache.ClearDetail(); // [!code --]
	if (resetData)
	{
		HotInit(hashSet);
```

## Trait

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c196676fecb4561f0ddac69f127a26e898f8de82/Elin/Trait.cs#L1651-L1656)
```cs:line-numbers=1651
		Add("deed_move", 2 + EClass.rnd(5), 0);
		Add("deed_wedding", 1, 0);
		Add("deed_divorce", 1, 0);
		Add("deed_lostring", 1, 0); // [!code ++]
		Add("license_illumination", 1, 0);
		Add("license_void", 1, 0);
		Add("license_adv", 1, 0);
```
