---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 36 files modified.
version: EA 23.318 Nightly
changes: AI_
---

# EA 23.318 Nightly

June 18, 2026

36 files modified.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [FoodEffect (1)](#foodeffect)
```cs:no-line-numbers
public static void ProcTrait(Chara c, Card t) // [!code --]
public static void ProcTrait(Chara c, Card food) // [!code ++]
```
### [TaskDig (1)](#taskdig)
```cs:no-line-numbers
public Thing GetTreasureMap() // [!code --]
public Thing GetTreasureMap(Point p) // [!code ++]
```
## AI_Haul

[`public static Thing GetThingToClean(Chara c = null)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/AI_Haul.cs#L36-L42)
```cs:line-numbers=36
	_list.Clear();
	foreach (Thing thing in EClass._map.things)
	{
		if (thing.placeState == PlaceState.roaming && !thing.isMasked && (thing.id == "731" || thing.id == "_egg" || thing.id == "egg_fertilized" || thing.id == "milk")) // [!code --]
		if (thing.placeState == PlaceState.roaming && !thing.isMasked && (thing.id == "731" || thing.id == "_egg" || thing.id == "egg_fertilized" || thing.id == "_milk")) // [!code ++]
		{
			_list.Add(thing);
		}
```

## ActPlan

[`public void _Update(PointTarget target)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/ActPlan.cs#L784-L799)
```cs:line-numbers=784
							}
							if (!EClass._zone.IsUserZone || !t.isNPCProperty)
							{
								if (t.trait.CanEat(EClass.pc)) // [!code --]
								if (t.trait.CanDrink(EClass.pc)) // [!code ++]
								{
									TrySetAct(new AI_Eat // [!code --]
									TrySetAct(new AI_Drink // [!code ++]
									{
										target = t
									}, t);
								}
								if (t.trait.CanDrink(EClass.pc)) // [!code --]
								else if (t.trait.CanEat(EClass.pc)) // [!code ++]
								{
									TrySetAct(new AI_Drink // [!code --]
									TrySetAct(new AI_Eat // [!code ++]
									{
										target = t
									}, t);
```

## ActThrow

[`public static EffectIRenderer Throw(Card c, Point p, Card target, Thing t, Throw`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/ActThrow.cs#L182-L188)
```cs:line-numbers=182
		{
			if (t.trait.CanDrink(Act.TC.Chara))
			{
				t.trait.OnDrink(Act.TC.Chara); // [!code --]
				FoodEffect.ProcDrink(Act.TC.Chara, t); // [!code ++]
			}
			flag2 = t.IsNegativeGift;
			Act.TC.Chara.AddCondition<ConWet>();
```

## ActWater

[`public override bool Perform()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/ActWater.cs#L21-L27)
```cs:line-numbers=21
	num = ((num <= 0) ? 1 : Mathf.Min(waterCan.owner.c_charges, 2 + num / 10));
	if (num > 1)
	{
		List<Point> list = EClass._map.ListPointsInCircle(Act.TP, num); // [!code --]
		List<Point> list = EClass._map.ListPointsInSquare(Act.TP, num - 1); // [!code ++]
		list.Sort((Point a, Point b) => a.Distance(Act.TP) - b.Distance(Act.TP));
		foreach (Point item in list)
		{
```

## BaseArea

[`public void SetRandomName(int seed = -1)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/BaseArea.cs#L121-L132)
```cs:line-numbers=121

	public string GetRandomName(int seed = -1)
	{
		if (seed != -1) // [!code --]
		bool flag = seed != -1; // [!code ++]
		if (flag) // [!code ++]
		{
			Rand.SetSeed(seed);
		}
		string combinedName = WordGen.GetCombinedName(EClass.Branch?.GetRandomName(), ListRoomNames().RandomItem(), room: true); // [!code --]
		if (seed != -1) // [!code --]
		string combinedName = WordGen.GetCombinedName(EClass.Branch?.GetRandomName(), ListRoomNames().RandomItem(), room: true, flag); // [!code ++]
		if (flag) // [!code ++]
		{
			Rand.SetSeed();
		}
```

## BaseTaskHarvest

[`public bool IsTooHard`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/BaseTaskHarvest.cs#L50-L57)
```cs:line-numbers=50

	public bool IsHarvest => pos.cell.CanHarvest();

	public virtual string IdRecipe => ""; // [!code --]
 // [!code --]
	public virtual bool CanReapSeed => pos.cell.CanReapSeed();

	public virtual HarvestType harvestType => HarvestType.Block;
```

[`public bool IsTooHard`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/BaseTaskHarvest.cs#L64-L69)
```cs:line-numbers=64

	public override CursorInfo CursorIcon => CursorSystem.Cut;

	public virtual string GetIdRecipe(Point p) // [!code ++]
	{ // [!code ++]
		return ""; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public virtual string GetBaseText(string str)
	{
		return base.GetText(str);
```

[`public override string GetText(string str = "")`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/BaseTaskHarvest.cs#L84-L90)
```cs:line-numbers=84
	string text = "tHarvest".lang(GetBaseText(str), (EClass.pc.Tool == null) ? "hand".lang() : owner.Tool.NameSimple, toolLv.ToString() ?? "");
	if (EClass.pc.HasElement(1661))
	{
		RecipeManager.LearnState recipeLearnState = EClass.player.recipes.GetRecipeLearnState(IdRecipe); // [!code --]
		RecipeManager.LearnState recipeLearnState = EClass.player.recipes.GetRecipeLearnState(GetIdRecipe(pos)); // [!code ++]
		switch (recipeLearnState)
		{
		case RecipeManager.LearnState.Learnable:
```

## Card

[`public void SpawnLoot(Card origin)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Card.cs#L5339-L5345)
```cs:line-numbers=5339
			list.Add(thing2);
		}
	}
	bool flag2 = Chara.race.corpse[1].ToInt() > EClass.rnd(1500) || (Chara.IsPowerful && !IsPCFaction) || EClass.debug.godFood || HasTag(CTAG.alwaysDropCorpse); // [!code --]
	bool flag2 = Chara.race.corpse[1].ToInt() > EClass.rnd(1500) || (Chara.IsPowerful && !IsPCFaction) || EClass.debug.godFood || (HasTag(CTAG.alwaysDropCorpse) && !IsPCFaction); // [!code ++]
	int num = 1;
	if (!IsMinion && Chara.IsAnimal && EClass.rnd(EClass._zone.IsPCFaction ? 3 : 5) == 0)
	{
```

[`public void SpawnLoot(Card origin)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Card.cs#L5410-L5416)
```cs:line-numbers=5410
		}
		else
		{
			list.Add(ThingGen.Create("517").SetNum(num).MakeFoodFrom(this, makeRef: false)); // [!code --]
			list.Add(ThingGen.Create("milk_custard").SetNum(num).MakeFoodFrom(this, makeRef: false)); // [!code ++]
		}
	}
	if (!IsPCFaction && (!isUserZone || !EClass.game.principal.disableUsermapBenefit) && chance(200))
```

[`public Thing TryMakeRandomItem(int lv = -1, TryMakeRandomItemSource itemSource =`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Card.cs#L5708-L5714)
```cs:line-numbers=5708
	case "ore":
		ChangeMaterial(MATERIAL.GetRandomMaterialFromCategory(lv, "ore", material));
		break;
	case "milk": // [!code --]
	case "_milk": // [!code ++]
	case "_egg":
	case "egg_fertilized":
	case "_meat":
```

[`public Thing TryMakeRandomItem(int lv = -1, TryMakeRandomItemSource itemSource =`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Card.cs#L5727-L5733)
```cs:line-numbers=5727
		for (int i = 0; i < 20; i++)
		{
			CardRow cardRow = SpawnList.Get(text).Select(lv + i);
			if (cardRow.model.Chara.race.corpse[0] != "_meat" && id != "milk" && id != "_egg" && id != "egg_fertilized") // [!code --]
			if (cardRow.model.Chara.race.corpse[0] != "_meat" && id != "_milk" && id != "_egg" && id != "egg_fertilized") // [!code ++]
			{
				continue;
			}
```

[`public Thing TryMakeRandomItem(int lv = -1, TryMakeRandomItemSource itemSource =`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Card.cs#L5742-L5748)
```cs:line-numbers=5742
					cardRow = crafter.source;
				}
			}
			if (id == "milk") // [!code --]
			if (id == "_milk") // [!code ++]
			{
				if (c_idRefCard.IsEmpty())
				{
```

[`public Card MakeFoodFrom(Card c, bool makeRef = true)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Card.cs#L5783-L5789)
```cs:line-numbers=5783
	bool flag = id == "meat_marble";
	int num2 = 1;
	bool flag2 = category.IsChildOf("meat");
	bool flag3 = id == "517" || id == "marshmallow_nama" || category.IsChildOf("egg") || trait is TraitGene; // [!code --]
	bool flag3 = id == "marshmallow_nama" || category.IsChildOf("egg") || trait is TraitGene; // [!code ++]
	bool flag4 = category.IsChildOf("milk"); // [!code ++]
	if (flag)
	{
		num += 100;
```

[`public Card MakeFoodFrom(Card c, bool makeRef = true)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Card.cs#L5801-L5814)
```cs:line-numbers=5801
		}
		elements.SetBase(71, (int)Mathf.Clamp((float)(num / 10) + Mathf.Sqrt(race.height) - 10f, 1f, 60f));
	}
	else if (flag3) // [!code --]
	else if (!flag3) // [!code ++]
	{
		elements.SetBase(444, race.LER * race.LER / 5 * num / 100 - 10 + num / 10); // [!code --]
		num2 = 2; // [!code --]
		num2 = ((!flag4) ? 3 : 2); // [!code ++]
	}
	else
	{
		num2 = 3; // [!code --]
		elements.SetBase(444, race.LER * race.LER / 5 * num / 100 - 10 + num / 10); // [!code ++]
		num2 = 2; // [!code ++]
	}
	if (flag2)
	{
```

[`public Thing MakeEgg(bool effect = true, int num = 1, bool addToZone = true, int`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Card.cs#L5999-L6005)
```cs:line-numbers=5999

	public Thing MakeMilk(bool effect = true, int num = 1, bool addToZone = true, BlessedState? state = null)
	{
		Thing thing = ThingGen.Create("milk").SetNum(num); // [!code --]
		Thing thing = ThingGen.Create("_milk").SetNum(num); // [!code ++]
		if (!EClass.debug.enable && HasElement(1290) && Evalue(418) >= 0)
		{
			thing.MakeRefFrom(EClass.sources.charas.map["caladrius"].model);
```

## Chara

[`public void Drink(Card t)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Chara.cs#L5293-L5301)
```cs:line-numbers=5293
		Say("drink", this, t.Duplicate(1));
		Say("quaff");
		PlaySound("drink");
		hunger.Mod(-2); // [!code --]
		t.ModNum(-1);
		t.trait.OnDrink(this); // [!code --]
		FoodEffect.ProcDrink(this, t.Thing); // [!code ++]
		_ = IsPC;
	}

```

[`public bool TryUse(Thing t)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Chara.cs#L8195-L8200)
```cs:line-numbers=8195
		(t.trait as TraitItemProc).OnUse(this);
		return true;
	}
	if (t.trait.CanDrink(this)) // [!code ++]
	{ // [!code ++]
		Drink(t); // [!code ++]
		return true; // [!code ++]
	} // [!code ++]
	if (t.trait.CanEat(this) && hunger.GetPhase() > ((IsPCFaction || IsPCFactionMinion) ? 2 : 0))
	{
		SetAIImmediate(new AI_Eat
```

[`public bool TryUse(Thing t)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Chara.cs#L8203-L8213)
```cs:line-numbers=8203
		});
		return true;
	}
	if (t.trait.CanDrink(this)) // [!code --]
	{ // [!code --]
		Drink(t); // [!code --]
		return true; // [!code --]
	} // [!code --]
	if (t.trait.CanRead(this))
	{
		SetAIImmediate(new AI_Read
```

## CoreDebug

[`public void QuickStart()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/CoreDebug.cs#L469-L478)
```cs:line-numbers=469
		}
		EClass.pc.AddCard(thing);
		thing = ThingGen.Create("pouch");
		for (int k = 1; k < 5; k++) // [!code --]
		for (int k = 0; k < 5; k++) // [!code ++]
		{
			Thing thing3 = ThingGen.Create("scrubber");
			thing3.elements.SetBase(770, k * 10); // [!code --]
			thing3.elements.SetBase(770, k * 10 + 1); // [!code ++]
			thing.AddCard(thing3);
		}
		for (int l = 0; l < 30; l++)
```

[`public void UpdateInput()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/CoreDebug.cs#L979-L985)
```cs:line-numbers=979
		Chara targetChara = EClass.scene.mouseTarget.TargetChara;
		if (targetChara != null)
		{
			EClass.pc.Pick(CraftUtil.MakeLoveLunch(targetChara)); // [!code --]
			EClass.pc.Pick(targetChara.MakeMilk()); // [!code ++]
			EClass.pc.Pick(targetChara.MakeEgg(effect: true, 10));
			EClass.pc.Pick(targetChara.MakeGene());
			TraitFoodEggFertilized.Incubate(targetChara.MakeEgg(effect: false, 1, addToZone: false, 100), targetChara.pos.GetNearestPoint(allowBlock: false, allowChara: false));
```

[`public static string RegenerateNames()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/CoreDebug.cs#L2388-L2393)
```cs:line-numbers=2388
	EClass.core.mods.InitLang();
	NameGen.list = null;
	AliasGen.list = null;
	AliasGen.listMix.Clear(); // [!code ++]
	AliasGen.listBuiltin.Clear(); // [!code ++]
	WordGen.listMix.Clear(); // [!code ++]
	WordGen.listBuiltin.Clear(); // [!code ++]
	NameGen.Init();
	AliasGen.Init();
	foreach (Chara chara in EClass._map.charas)
```

## CraftUtil

[`public enum MixType`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/CraftUtil.cs#L8-L14)
```cs:line-numbers=8
	{
		General,
		Food,
		NoMix // [!code --]
		NoMix, // [!code ++]
		Drink // [!code ++]
	}

	public enum WrapType
```

[`public static Card MixIngredients(Card product, List<Thing> ings, MixType type,`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/CraftUtil.cs#L375-L380)
```cs:line-numbers=375
{
	bool noMix = type == MixType.NoMix || product.HasTag(CTAG.noMix);
	bool isFood = type == MixType.Food;
	bool isDrink = type == MixType.Drink; // [!code ++]
	bool isFoodOrDrink = isFood || isDrink; // [!code ++]
	int nutFactor = 100 - (ings.Count - 1) * 5;
	Thing thing = ((ings.Count > 0) ? ings[0] : null);
	bool creative = crafter?.HasElement(487) ?? false;
```

[`public static Card MixIngredients(Card product, List<Thing> ings, MixType type,`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/CraftUtil.cs#L382-L387)
```cs:line-numbers=382
	{
		nutFactor -= 10;
	}
	if (isDrink) // [!code ++]
	{ // [!code ++]
		nutFactor = 100 / ings.Count; // [!code ++]
	} // [!code ++]
	if (!noMix)
	{
		foreach (Element value2 in product.elements.dict.Values)
```

[`public static Card MixIngredients(Card product, List<Thing> ings, MixType type,`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/CraftUtil.cs#L400-L405)
```cs:line-numbers=400
			product.elements.ModBase(item.id, item.Value);
		}
	}
	if (isDrink) // [!code ++]
	{ // [!code ++]
		product.elements.SetTo(10, 0); // [!code ++]
	} // [!code ++]
	if (isFood)
	{
		product.elements.SetTo(10, 5);
```

[`bool IsValidTrait(Element e)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/CraftUtil.cs#L515-L520)
```cs:line-numbers=515
			}
			break;
		case MixType.Food:
		case MixType.Drink: // [!code ++]
			if (e.IsFoodTrait || e.IsTrait || e.id == 2)
			{
				return true;
```

[`void MixElements(Card t)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/CraftUtil.cs#L531-L537)
```cs:line-numbers=531
			{
				if (IsValidTrait(value3) && (!noMix || value3.id == 2))
				{
					if (isFood && value3.IsFoodTraitMain) // [!code --]
					if (isFoodOrDrink && value3.IsFoodTraitMain) // [!code ++]
					{
						int num5 = value3.Value;
						if (product.id == "lunch_dystopia")
```

[`void MixElements(Card t)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/CraftUtil.cs#L558-L563)
```cs:line-numbers=558
			{
				product.elements.ModBase(10, t.Evalue(10) * nutFactor / 100);
			}
			if (isDrink) // [!code ++]
			{ // [!code ++]
				product.elements.ModBase(10, Mathf.CeilToInt((float)t.Evalue(10) * (float)nutFactor / 100f)); // [!code ++]
			} // [!code ++]
		}
	}
}
```

## CustomReligionContent

[`public class CustomReligionContent : CustomSourceContent`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/CustomReligionContent.cs#L37-L57)
```cs:line-numbers=37

	public static CustomReligionContent CreateFromRow(SourceReligion.Row r, ModPackage owner = null)
	{
		EClass.sources.religions.map.Remove(r.id); // [!code --]
		r.id = r.id.Split('#')[0]; // [!code --]
		EClass.sources.religions.map[r.id] = r; // [!code --]
		string text = r.id.Split('#')[0]; // [!code ++]
		if (owner == null)
		{
			owner = ModUtil.FindSourceRowPackage(r);
		}
		CustomReligionContent obj = new CustomReligionContent
		{
			ContentId = "Religion/" + r.id, // [!code --]
			SourceId = r.id, // [!code --]
			ContentId = "Religion/" + text, // [!code ++]
			SourceId = text, // [!code ++]
			Owner = owner,
			canJoin = (!r.id.Contains("#noJoin") && !r.id.Contains("#cannot")),
			isMinorGod = r.id.Contains("#minor")
		};
		EClass.sources.religions.map[text] = r; // [!code ++]
		if (!Portrait.modPortraits.dict.ContainsKey("UN_" + r.id) && SpriteReplacer.dictModItems.TryGetValue(r.id, out var value))
		{
			Portrait.modPortraits.Add("UN_" + r.id, new FileInfo(value + ".png"));
```

[`public static void Init()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/CustomReligionContent.cs#L82-L91)
```cs:line-numbers=82
			{
				if (managed.TryGetValue(key, out var value))
				{
					bool flag = customReligionContent2.canJoin; // [!code --]
					value.content.canJoin = flag; // [!code --]
					bool flag2 = customReligionContent2.isMinorGod; // [!code --]
					value.content.isMinorGod = flag2; // [!code --]
					value.content.canJoin = customReligionContent2.canJoin; // [!code ++]
					value.content.isMinorGod = customReligionContent2.isMinorGod; // [!code ++]
					value.content.artifacts = customReligionContent2.artifacts;
					value.content.offeringMtp = customReligionContent2.offeringMtp;
					value.content.offeringValue = customReligionContent2.offeringValue;
```

## FactionBranch

[`void GetOutcome(Hobby h)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/FactionBranch.cs#L843-L849)
```cs:line-numbers=843
				{
					if (!(text == "_egg"))
					{
						if (text == "milk") // [!code --]
						if (text == "_milk") // [!code ++]
						{
							i.MakeMilk(date.IsRealTime, num6);
						}
```

## FoodEffect

[`public class FoodEffect : EClass`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/FoodEffect.cs#L6-L24)
```cs:line-numbers=6
{
	public static bool IsLeftoverable(Thing food)
	{
		if (food.HasElement(758) || food.HasElement(1229)) // [!code ++]
		{ // [!code ++]
			return false; // [!code ++]
		} // [!code ++]
		return food.trait is TraitLunch;
	}

	public static void Proc(Chara c, Thing food, bool consume = true)
	{
		Chara c2 = c; // [!code --]
		if (food.id == "bloodsample")
		{
			food.ModNum(-1);
			return;
		}
		food.CheckJustCooked();
		bool flag = EClass._zone.IsPCFactionOrTent && c2.IsInSpot<TraitSpotDining>(); // [!code --]
		bool flag = EClass._zone.IsPCFactionOrTent && c.IsInSpot<TraitSpotDining>(); // [!code ++]
		int num = (food.isCrafted ? ((EClass.pc.Evalue(1650) >= 3) ? 5 : 0) : 0);
		float num2 = (float)(100 + (food.HasElement(757) ? 10 : 0) + (flag ? 10 : 0) + num + Mathf.Min(food.QualityLv * 10, 100)) / 200f;
		if (num2 < 0.1f)
```

[`public static void Proc(Chara c, Thing food, bool consume = true)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/FoodEffect.cs#L31-L55)
```cs:line-numbers=31
	string idTaste = "";
	bool flag2 = food.HasElement(708);
	bool flag3 = food.HasElement(709);
	bool flag4 = c2.HasElement(1205); // [!code --]
	bool flag4 = c.HasElement(1205); // [!code ++]
	bool flag5 = food.IsDecayed || flag3;
	bool flag6 = IsLeftoverable(food);
	bool flag7 = EClass._zone.HasField(10001) && food.GetBool(128);
	c2.AddFoodHistory(food); // [!code --]
	if (c2.IsPCFaction && !c2.IsPC) // [!code --]
	c.AddFoodHistory(food); // [!code ++]
	if (c.IsPCFaction && !c.IsPC) // [!code ++]
	{
		int num6 = c2.CountNumEaten(food); // [!code --]
		bool flag8 = c2.GetFavFood().id == food.id; // [!code --]
		int num6 = c.CountNumEaten(food); // [!code ++]
		bool flag8 = c.GetFavFood().id == food.id; // [!code ++]
		if (num6 < 2 || flag8)
		{
			if (num6 == 1 || flag8 || EClass.rnd(4) == 0)
			{
				c2.Talk("foodNice"); // [!code --]
				c.Talk("foodNice"); // [!code ++]
			}
		}
		else if (num6 > 3 && EClass.rnd(num6) >= 3)
		{
			c2.Talk("foodBored"); // [!code --]
			c.Talk("foodBored"); // [!code ++]
		}
	}
	if (food.IsBlessed)
```

[`public static void Proc(Chara c, Thing food, bool consume = true)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/FoodEffect.cs#L79-L85)
```cs:line-numbers=79
		num5 = 0f;
		num2 *= 0.5f;
	}
	if (c2.HasElement(1250)) // [!code --]
	if (c.HasElement(1250)) // [!code ++]
	{
		if (food.HasElement(710))
		{
```

[`public static void Proc(Chara c, Thing food, bool consume = true)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/FoodEffect.cs#L90-L112)
```cs:line-numbers=90
			num3 /= 10;
		}
	}
	if (c2.HasElement(1200)) // [!code --]
	if (c.HasElement(1200)) // [!code ++]
	{
		num2 *= 1f + (float)c2.Evalue(1200) * 0.3f; // [!code --]
		num2 *= 1f + (float)c.Evalue(1200) * 0.3f; // [!code ++]
	}
	if (!c2.IsPC) // [!code --]
	if (!c.IsPC) // [!code ++]
	{
		num2 *= 3f;
	}
	if (flag5 && !c2.HasElement(480)) // [!code --]
	if (flag5 && !c.HasElement(480)) // [!code ++]
	{
		if (c2.IsPC) // [!code --]
		if (c.IsPC) // [!code ++]
		{
			if (flag3)
			{
				c2.Say("food_undead"); // [!code --]
				c.Say("food_undead"); // [!code ++]
			}
			c2.Say("food_rot"); // [!code --]
			c.Say("food_rot"); // [!code ++]
		}
		num5 = 0f;
		num3 /= 2;
```

[`public static void Proc(Chara c, Thing food, bool consume = true)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/FoodEffect.cs#L116-L389)
```cs:line-numbers=116
		switch (food.source._origin)
		{
		case "meat":
			if (c2.IsPC) // [!code --]
			if (c.IsPC) // [!code ++]
			{
				c2.Say("food_raw_meat"); // [!code --]
				c.Say("food_raw_meat"); // [!code ++]
			}
			num2 *= 0.7f;
			num5 = 0.5f;
			break;
		case "fish":
			if (c2.IsHuman) // [!code --]
			if (c.IsHuman) // [!code ++]
			{
				if (c2.IsPC) // [!code --]
				if (c.IsPC) // [!code ++]
				{
					c2.Say("food_raw_fish"); // [!code --]
					c.Say("food_raw_fish"); // [!code ++]
				}
				num2 *= 0.9f;
				num5 = 0.5f;
			}
			break;
		case "dough":
			if (c2.IsPC) // [!code --]
			if (c.IsPC) // [!code ++]
			{
				c2.Say("food_raw_powder"); // [!code --]
				c.Say("food_raw_powder"); // [!code ++]
			}
			num2 *= 0.9f;
			num5 = 0.5f;
			break;
		}
	}
	float num7 = (flag7 ? num3 : Mathf.Min(c2.hunger.value, num3)); // [!code --]
	if (c2.hunger.GetPhase() >= 3) // [!code --]
	float num7 = (flag7 ? num3 : Mathf.Min(c.hunger.value, num3)); // [!code ++]
	if (c.hunger.GetPhase() >= 3) // [!code ++]
	{
		num7 *= 1.1f;
	}
	if (flag5 && !c2.HasElement(480)) // [!code --]
	{ // [!code --]
		c2.ModExp(70, -300); // [!code --]
		c2.ModExp(71, -300); // [!code --]
		c2.ModExp(72, -200); // [!code --]
		c2.ModExp(73, -200); // [!code --]
		c2.ModExp(74, -200); // [!code --]
		c2.ModExp(75, 500); // [!code --]
		c2.ModExp(76, -200); // [!code --]
		c2.ModExp(77, -300); // [!code --]
	} // [!code --]
	else // [!code --]
	ProcNutrition(c, food, num2, num7); // [!code ++]
	if (!c.isDead) // [!code ++]
	{
		num2 = num2 * num7 / 10f; // [!code --]
		if (c2.HasCondition<ConAnorexia>()) // [!code --]
		{ // [!code --]
			num2 = 0.01f; // [!code --]
		} // [!code --]
		List<Element> list = food.ListValidTraits(isCraft: true, limit: false); // [!code --]
		foreach (Element value in food.elements.dict.Values) // [!code --]
		ProcTrait(c, food); // [!code ++]
		if (!c.isDead) // [!code ++]
		{
			if (value.source.foodEffect.IsEmpty() || !list.Contains(value)) // [!code --]
			{ // [!code --]
				continue; // [!code --]
			} // [!code --]
			string[] foodEffect = value.source.foodEffect; // [!code --]
			int id = value.id; // [!code --]
			float num8 = num2 * (float)value.Value; // [!code --]
			if (value.source.category == "food" && c2.IsPC) // [!code --]
			num4 += (float)food.Evalue(70); // [!code ++]
			num4 += (float)(food.Evalue(72) / 2); // [!code ++]
			num4 += (float)(food.Evalue(73) / 2); // [!code ++]
			num4 += (float)(food.Evalue(75) / 2); // [!code ++]
			num4 += (float)(food.Evalue(76) * 3 / 2); // [!code ++]
			num4 += (float)food.Evalue(440); // [!code ++]
			num4 += (float)(food.Evalue(445) / 2); // [!code ++]
			num4 -= (float)food.Evalue(71); // [!code ++]
			num4 += (float)food.Evalue(18); // [!code ++]
			num4 += (float)(num3 / 2); // [!code ++]
			num4 *= num5; // [!code ++]
			if (idTaste.IsEmpty()) // [!code ++]
			{
				bool flag9 = num8 >= 0f; // [!code --]
				string text = value.source.GetText(flag9 ? "textInc" : "textDec", returnNull: true); // [!code --]
				if (text != null) // [!code --]
				if (num4 > 100f) // [!code ++]
				{
					Msg.SetColor(flag9 ? "positive" : "negative"); // [!code --]
					c2.Say(text); // [!code --]
					idTaste = "food_great"; // [!code ++]
				}
			} // [!code --]
			switch (foodEffect[0]) // [!code --]
			{ // [!code --]
			case "god": // [!code --]
			{ // [!code --]
				int int2 = c2.GetInt(117); // [!code --]
				if (int2 < 10) // [!code --]
				else if (num4 > 70f) // [!code ++]
				{ // [!code ++]
					idTaste = "food_good"; // [!code ++]
				} // [!code ++]
				else if (num4 > 50f) // [!code ++]
				{
					foreach (Element value2 in c2.elements.dict.Values) // [!code --]
					idTaste = "food_soso"; // [!code ++]
				} // [!code ++]
				else if (num4 > 30f) // [!code ++]
				{ // [!code ++]
					idTaste = "food_average"; // [!code ++]
				} // [!code ++]
				else // [!code ++]
				{ // [!code ++]
					idTaste = "food_bad"; // [!code ++]
				} // [!code ++]
				if (c.IsPC) // [!code ++]
				{ // [!code ++]
					c.Say(idTaste); // [!code ++]
					if (flag2) // [!code ++]
					{ // [!code ++]
						c.Say(flag4 ? "food_human_pos" : "food_human_neg"); // [!code ++]
					} // [!code ++]
					else if (flag4) // [!code ++]
					{
						if (value2.IsMainAttribute) // [!code --]
						{ // [!code --]
							c2.elements.ModPotential(value2.id, 2); // [!code --]
						} // [!code --]
						c.Say("food_human_whine"); // [!code ++]
					}
				}
				c2.Say("little_eat", c2); // [!code --]
				c2.PlaySound("ding_potential"); // [!code --]
				c2.elements.ModExp(306, -1000f); // [!code --]
				c2.SetInt(117, int2 + 1); // [!code --]
				flag6 = false; // [!code --]
				break; // [!code --]
			}
			case "exp": // [!code --]
			if (LangGame.Has(idTaste + "2")) // [!code ++]
			{
				id = ((foodEffect.Length > 1) ? EClass.sources.elements.alias[foodEffect[1]].id : value.id); // [!code --]
				int a = (int)(num8 * (float)((foodEffect.Length > 2) ? foodEffect[2].ToInt() : 4)) * 2 / 3; // [!code --]
				c2.ModExp(id, a); // [!code --]
				break; // [!code --]
				c.Say(idTaste + "2", c, food); // [!code ++]
			}
			case "pot": // [!code --]
			if (!c.IsPCParty) // [!code ++]
			{
				id = ((foodEffect.Length > 1) ? EClass.sources.elements.alias[foodEffect[1]].id : value.id); // [!code --]
				int vTempPotential = c2.elements.GetElement(id).vTempPotential; // [!code --]
				int num9 = EClass.rndHalf((int)(num8 / 5f) + 1); // [!code --]
				num9 = num9 * 100 / Mathf.Max(100, vTempPotential * 2 / 3); // [!code --]
				c2.elements.ModTempPotential(id, num9, 8); // [!code --]
				break; // [!code --]
				num3 *= 2; // [!code ++]
			}
			case "karma": // [!code --]
				if (c2.IsPCParty) // [!code --]
				{ // [!code --]
					EClass.player.ModKarma(-5); // [!code --]
				} // [!code --]
				break; // [!code --]
			case "poison": // [!code --]
				ActEffect.Poison(c2, EClass.pc, value.Value * 10); // [!code --]
				if (!c2.isDead) // [!code --]
				{ // [!code --]
					break; // [!code --]
				} // [!code --]
				goto IL_0fad; // [!code --]
			case "love": // [!code --]
				ActEffect.LoveMiracle(c2, EClass.pc, value.Value * 10); // [!code --]
				break; // [!code --]
			case "loseWeight": // [!code --]
				c2.ModWeight(-EClass.rndHalf(value.Value), ignoreLimit: true); // [!code --]
				break; // [!code --]
			case "gainWeight": // [!code --]
				c2.ModWeight(EClass.rndHalf(value.Value), ignoreLimit: true); // [!code --]
				break; // [!code --]
			case "little": // [!code --]
			num3 = num3 * (100 + c.Evalue(1235) * 10) / (100 + c.Evalue(1234) * 10 + c.Evalue(1236) * 15); // [!code ++]
			c.hunger.Mod(-num3); // [!code ++]
			if (flag2) // [!code ++]
			{
				int @int = c2.GetInt(112); // [!code --]
				if (@int < 30) // [!code --]
				if (!flag4) // [!code ++]
				{
					c2.Say("little_eat", c2); // [!code --]
					c2.PlaySound("ding_potential"); // [!code --]
					int v = Mathf.Max(5 - @int / 2, 1); // [!code --]
					Debug.Log("sister eaten:" + @int + "/" + v); // [!code --]
					foreach (Element value3 in c2.elements.dict.Values) // [!code --]
					if (c.IsHuman) // [!code ++]
					{
						if (value3.IsMainAttribute) // [!code --]
						{ // [!code --]
							c2.elements.ModPotential(value3.id, v); // [!code --]
						} // [!code --]
						c.AddCondition<ConInsane>(200); // [!code ++]
						c.SAN.Mod(15); // [!code ++]
					} // [!code ++]
					if (EClass.rnd(c.IsHuman ? 5 : 20) == 0) // [!code ++]
					{ // [!code ++]
						c.SetFeat(1205, 1, msg: true); // [!code ++]
						flag4 = true; // [!code ++]
					}
				}
				if (c2.race.id == "mutant" && c2.elements.Base(1230) < 10) // [!code --]
				if (flag4) // [!code ++]
				{
					c2.Say("little_adam", c2); // [!code --]
					c2.SetFeat(1230, c2.elements.Base(1230) + 1); // [!code --]
					c.SetInt(31, EClass.world.date.GetRaw() + 10080); // [!code ++]
				}
				c2.SetInt(112, @int + 1); // [!code --]
				break; // [!code --]
			} // [!code --]
			}
		} // [!code --]
	} // [!code --]
	ProcTrait(c2, food); // [!code --]
	num4 += (float)food.Evalue(70); // [!code --]
	num4 += (float)(food.Evalue(72) / 2); // [!code --]
	num4 += (float)(food.Evalue(73) / 2); // [!code --]
	num4 += (float)(food.Evalue(75) / 2); // [!code --]
	num4 += (float)(food.Evalue(76) * 3 / 2); // [!code --]
	num4 += (float)food.Evalue(440); // [!code --]
	num4 += (float)(food.Evalue(445) / 2); // [!code --]
	num4 -= (float)food.Evalue(71); // [!code --]
	num4 += (float)food.Evalue(18); // [!code --]
	num4 += (float)(num3 / 2); // [!code --]
	num4 *= num5; // [!code --]
	if (idTaste.IsEmpty()) // [!code --]
	{ // [!code --]
		if (num4 > 100f) // [!code --]
		{ // [!code --]
			idTaste = "food_great"; // [!code --]
		} // [!code --]
		else if (num4 > 70f) // [!code --]
		{ // [!code --]
			idTaste = "food_good"; // [!code --]
		} // [!code --]
		else if (num4 > 50f) // [!code --]
		{ // [!code --]
			idTaste = "food_soso"; // [!code --]
		} // [!code --]
		else if (num4 > 30f) // [!code --]
		{ // [!code --]
			idTaste = "food_average"; // [!code --]
		} // [!code --]
		else // [!code --]
		{ // [!code --]
			idTaste = "food_bad"; // [!code --]
		} // [!code --]
		if (c2.IsPC) // [!code --]
		{ // [!code --]
			c2.Say(idTaste); // [!code --]
			if (flag2) // [!code --]
			else if (flag4 && c.GetInt(31) < EClass.world.date.GetRaw()) // [!code ++]
			{
				c2.Say(flag4 ? "food_human_pos" : "food_human_neg"); // [!code --]
				c.SetFeat(1205, 0, msg: true); // [!code ++]
			}
			else if (flag4) // [!code --]
			if (flag5 && !c.HasElement(480)) // [!code ++]
			{
				c2.Say("food_human_whine"); // [!code --]
				c.AddCondition<ConParalyze>(); // [!code ++]
				c.AddCondition<ConConfuse>(200); // [!code ++]
			}
		} // [!code --]
	} // [!code --]
	if (LangGame.Has(idTaste + "2")) // [!code --]
	{ // [!code --]
		c2.Say(idTaste + "2", c2, food); // [!code --]
	} // [!code --]
	if (!c2.IsPCParty) // [!code --]
	{ // [!code --]
		num3 *= 2; // [!code --]
	} // [!code --]
	num3 = num3 * (100 + c2.Evalue(1235) * 10) / (100 + c2.Evalue(1234) * 10 + c2.Evalue(1236) * 15); // [!code --]
	c2.hunger.Mod(-num3); // [!code --]
	if (flag2) // [!code --]
	{ // [!code --]
		if (!flag4) // [!code --]
		{ // [!code --]
			if (c2.IsHuman) // [!code --]
			if (c.HasCondition<ConAnorexia>()) // [!code ++]
			{
				c2.AddCondition<ConInsane>(200); // [!code --]
				c2.SAN.Mod(15); // [!code --]
				c.Vomit(); // [!code ++]
			}
			if (EClass.rnd(c2.IsHuman ? 5 : 20) == 0) // [!code --]
			if (num3 > 20 && c.HasElement(1413)) // [!code ++]
			{
				c2.SetFeat(1205, 1, msg: true); // [!code --]
				flag4 = true; // [!code --]
				Thing thing = ThingGen.Create("seed"); // [!code ++]
				if (EClass.rnd(EClass.debug.enable ? 2 : 10) == 0) // [!code ++]
				{ // [!code ++]
					TraitSeed.ApplySeed(thing, (EClass.rnd(4) == 0) ? 118 : ((EClass.rnd(3) == 0) ? 119 : 90)); // [!code ++]
				} // [!code ++]
				thing.SetNum(2 + EClass.rnd(3)); // [!code ++]
				c.Talk("vomit"); // [!code ++]
				c.Say("fairy_vomit", c, thing); // [!code ++]
				c.PickOrDrop(c.pos, thing); // [!code ++]
			}
			food.trait.OnEat(c); // [!code ++]
		}
		if (flag4) // [!code --]
		{ // [!code --]
			c2.SetInt(31, EClass.world.date.GetRaw() + 10080); // [!code --]
		} // [!code --]
	} // [!code --]
	else if (flag4 && c2.GetInt(31) < EClass.world.date.GetRaw()) // [!code --]
	{ // [!code --]
		c2.SetFeat(1205, 0, msg: true); // [!code --]
	} // [!code --]
	if (flag5 && !c2.HasElement(480)) // [!code --]
	{ // [!code --]
		c2.AddCondition<ConParalyze>(); // [!code --]
		c2.AddCondition<ConConfuse>(200); // [!code --]
	} // [!code --]
	if (c2.HasCondition<ConAnorexia>()) // [!code --]
	{ // [!code --]
		c2.Vomit(); // [!code --]
	} // [!code --]
	if (num3 > 20 && c2.HasElement(1413)) // [!code --]
	{ // [!code --]
		Thing thing = ThingGen.Create("seed"); // [!code --]
		if (EClass.rnd(EClass.debug.enable ? 2 : 10) == 0) // [!code --]
		{ // [!code --]
			TraitSeed.ApplySeed(thing, (EClass.rnd(4) == 0) ? 118 : ((EClass.rnd(3) == 0) ? 119 : 90)); // [!code --]
		} // [!code --]
		thing.SetNum(2 + EClass.rnd(3)); // [!code --]
		c2.Talk("vomit"); // [!code --]
		c2.Say("fairy_vomit", c2, thing); // [!code --]
		c2.PickOrDrop(c2.pos, thing); // [!code --]
	} // [!code --]
	food.trait.OnEat(c2); // [!code --]
	if (food.trait is TraitDrink) // [!code --]
	{ // [!code --]
		food.trait.OnDrink(c2); // [!code --]
	}
	goto IL_0fad; // [!code --]
	IL_0fad: // [!code --]
	if (consume)
	{
		num7 += 5f;
```

[`public static void Proc(Chara c, Thing food, bool consume = true)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/FoodEffect.cs#L401-L428)
```cs:line-numbers=401
			food.ModNum(-1);
		}
	}
	if (!c2.IsCat && food.trait is TraitFoodChuryu) // [!code --]
	if (c.isDead) // [!code ++]
	{
		int num10 = 0; // [!code --]
		foreach (Chara item in c2.pos.ListCharasInRadius(c2, 5, (Chara c) => c.IsCat)) // [!code --]
		return; // [!code ++]
	} // [!code ++]
	if (!c.IsCat && food.trait is TraitFoodChuryu) // [!code ++]
	{ // [!code ++]
		int num8 = 0; // [!code ++]
		foreach (Chara item in c.pos.ListCharasInRadius(c, 5, (Chara c) => c.IsCat)) // [!code ++]
		{
			item.Say("angry", item);
			item.ShowEmo(Emo.angry);
			item.PlaySound("Animal/Cat/cat_angry");
			if (c2.IsPC) // [!code --]
			if (c.IsPC) // [!code ++]
			{
				EClass.player.ModKarma(-3);
			}
			num10++; // [!code --]
			num8++; // [!code ++]
		}
		EClass.player.stats.angryCats += num10; // [!code --]
		Debug.Log(num10 + "/" + EClass.player.stats.angryCats); // [!code --]
		if (num10 >= 10) // [!code --]
		EClass.player.stats.angryCats += num8; // [!code ++]
		Debug.Log(num8 + "/" + EClass.player.stats.angryCats); // [!code ++]
		if (num8 >= 10) // [!code ++]
		{
			Steam.GetAchievement(ID_Achievement.CHURYU);
		}
	}
	if (c2.IsPC && EClass._zone is Zone_Lothria) // [!code --]
	if (c.IsPC && EClass._zone is Zone_Lothria) // [!code ++]
	{
		switch (food.id)
		{
```

[`public static void Proc(Chara c, Thing food, bool consume = true)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/FoodEffect.cs#L433-L446)
```cs:line-numbers=433
			break;
		}
	}
	if (!(food.trait is TraitGene) || !c2.IsPC || !c2.HasElement(1274)) // [!code --]
	if (!(food.trait is TraitGene) || !c.IsPC || !c.HasElement(1274)) // [!code ++]
	{
		return;
	}
	DNA c_DNA = food.c_DNA;
	int slot = c_DNA.slot;
	CharaGenes genes = c2.c_genes; // [!code --]
	int excess = c2.CurrentGeneSlot + slot - c2.MaxGeneSlot; // [!code --]
	CharaGenes genes = c.c_genes; // [!code ++]
	int excess = c.CurrentGeneSlot + slot - c.MaxGeneSlot; // [!code ++]
	switch (c_DNA.type)
	{
	case DNA.Type.Inferior:
```

[`public static void Proc(Chara c, Thing food, bool consume = true)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/FoodEffect.cs#L453-L461)
```cs:line-numbers=453
		if (genes != null)
		{
			genes.items.Shuffle();
			Msg.Say("reconstruct", c2); // [!code --]
			c2.Say("food_mind", c2); // [!code --]
			c2.AddCondition<ConHallucination>(); // [!code --]
			Msg.Say("reconstruct", c); // [!code ++]
			c.Say("food_mind", c); // [!code ++]
			c.AddCondition<ConHallucination>(); // [!code ++]
		}
		return;
	}
```

[`public static void Proc(Chara c, Thing food, bool consume = true)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/FoodEffect.cs#L466-L488)
```cs:line-numbers=466
				RemoveDNA(fromOldest: true);
			}
		}
		c_DNA.Apply(c2); // [!code --]
		c2.Say("little_eat", c2); // [!code --]
		c2.PlaySound("ding_potential"); // [!code --]
		c_DNA.Apply(c); // [!code ++]
		c.Say("little_eat", c); // [!code ++]
		c.PlaySound("ding_potential"); // [!code ++]
		SE.Play("mutation");
		c2.PlayEffect("identify"); // [!code --]
		c.PlayEffect("identify"); // [!code ++]
		void RemoveDNA(bool fromOldest)
		{
			DNA dNA = (fromOldest ? genes.items[0] : genes.items.Last());
			CharaGenes.Remove(c2, dNA); // [!code --]
			CharaGenes.Remove(c, dNA); // [!code ++]
			excess -= dNA.slot;
		}
	}

	public static void ProcTrait(Chara c, Card t) // [!code --]
	public static void ProcDrink(Chara c, Thing food) // [!code ++]
	{ // [!code ++]
		int num = Mathf.Max(1, food.Evalue(10)); // [!code ++]
		float num2 = (float)(100 + Mathf.Min(food.QualityLv * 10, 100)) / 200f; // [!code ++]
		if (num2 < 0.1f) // [!code ++]
		{ // [!code ++]
			num2 = 0.1f; // [!code ++]
		} // [!code ++]
		if (c.hunger.GetPhase() <= 0) // [!code ++]
		{ // [!code ++]
			num = 0; // [!code ++]
		} // [!code ++]
		c.hunger.Mod(-num); // [!code ++]
		ProcNutrition(c, food, num2, num); // [!code ++]
		if (!c.isDead) // [!code ++]
		{ // [!code ++]
			ProcTrait(c, food); // [!code ++]
		} // [!code ++]
		if (!c.isDead) // [!code ++]
		{ // [!code ++]
			food.trait.OnDrink(c); // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static void ProcNutrition(Chara c, Thing food, float effP, float validFill) // [!code ++]
	{ // [!code ++]
		bool flag = food.HasElement(709); // [!code ++]
		if ((food.IsDecayed || flag) && !c.HasElement(480)) // [!code ++]
		{ // [!code ++]
			c.ModExp(70, -300); // [!code ++]
			c.ModExp(71, -300); // [!code ++]
			c.ModExp(72, -200); // [!code ++]
			c.ModExp(73, -200); // [!code ++]
			c.ModExp(74, -200); // [!code ++]
			c.ModExp(75, 500); // [!code ++]
			c.ModExp(76, -200); // [!code ++]
			c.ModExp(77, -300); // [!code ++]
			return; // [!code ++]
		} // [!code ++]
		effP = effP * validFill / 10f; // [!code ++]
		if (c.HasCondition<ConAnorexia>()) // [!code ++]
		{ // [!code ++]
			effP = 0.01f; // [!code ++]
		} // [!code ++]
		List<Element> list = food.ListValidTraits(isCraft: true, limit: false); // [!code ++]
		foreach (Element value in food.elements.dict.Values) // [!code ++]
		{ // [!code ++]
			if (value.source.foodEffect.IsEmpty() || !list.Contains(value)) // [!code ++]
			{ // [!code ++]
				continue; // [!code ++]
			} // [!code ++]
			string[] foodEffect = value.source.foodEffect; // [!code ++]
			int id = value.id; // [!code ++]
			float num = effP * (float)value.Value; // [!code ++]
			if (value.source.category == "food" && c.IsPC) // [!code ++]
			{ // [!code ++]
				bool flag2 = num >= 0f; // [!code ++]
				string text = value.source.GetText(flag2 ? "textInc" : "textDec", returnNull: true); // [!code ++]
				if (text != null) // [!code ++]
				{ // [!code ++]
					Msg.SetColor(flag2 ? "positive" : "negative"); // [!code ++]
					c.Say(text); // [!code ++]
				} // [!code ++]
			} // [!code ++]
			switch (foodEffect[0]) // [!code ++]
			{ // [!code ++]
			case "god": // [!code ++]
			{ // [!code ++]
				int int2 = c.GetInt(117); // [!code ++]
				if (int2 < 10) // [!code ++]
				{ // [!code ++]
					foreach (Element value2 in c.elements.dict.Values) // [!code ++]
					{ // [!code ++]
						if (value2.IsMainAttribute) // [!code ++]
						{ // [!code ++]
							c.elements.ModPotential(value2.id, 2); // [!code ++]
						} // [!code ++]
					} // [!code ++]
				} // [!code ++]
				c.Say("little_eat", c); // [!code ++]
				c.PlaySound("ding_potential"); // [!code ++]
				c.elements.ModExp(306, -1000f); // [!code ++]
				c.SetInt(117, int2 + 1); // [!code ++]
				break; // [!code ++]
			} // [!code ++]
			case "exp": // [!code ++]
			{ // [!code ++]
				id = ((foodEffect.Length > 1) ? EClass.sources.elements.alias[foodEffect[1]].id : value.id); // [!code ++]
				int a = (int)(num * (float)((foodEffect.Length > 2) ? foodEffect[2].ToInt() : 4)) * 2 / 3; // [!code ++]
				c.ModExp(id, a); // [!code ++]
				break; // [!code ++]
			} // [!code ++]
			case "pot": // [!code ++]
			{ // [!code ++]
				id = ((foodEffect.Length > 1) ? EClass.sources.elements.alias[foodEffect[1]].id : value.id); // [!code ++]
				int vTempPotential = c.elements.GetElement(id).vTempPotential; // [!code ++]
				int num2 = EClass.rndHalf((int)(num / 5f) + 1); // [!code ++]
				num2 = num2 * 100 / Mathf.Max(100, vTempPotential * 2 / 3); // [!code ++]
				c.elements.ModTempPotential(id, num2, 8); // [!code ++]
				break; // [!code ++]
			} // [!code ++]
			case "karma": // [!code ++]
				if (c.IsPCParty) // [!code ++]
				{ // [!code ++]
					EClass.player.ModKarma(-5); // [!code ++]
				} // [!code ++]
				break; // [!code ++]
			case "poison": // [!code ++]
				ActEffect.Poison(c, EClass.pc, value.Value * 10); // [!code ++]
				break; // [!code ++]
			case "love": // [!code ++]
				ActEffect.LoveMiracle(c, EClass.pc, value.Value * 10); // [!code ++]
				break; // [!code ++]
			case "loseWeight": // [!code ++]
				c.ModWeight(-EClass.rndHalf(value.Value), ignoreLimit: true); // [!code ++]
				break; // [!code ++]
			case "gainWeight": // [!code ++]
				c.ModWeight(EClass.rndHalf(value.Value), ignoreLimit: true); // [!code ++]
				break; // [!code ++]
			case "little": // [!code ++]
			{ // [!code ++]
				int @int = c.GetInt(112); // [!code ++]
				if (@int < 30) // [!code ++]
				{ // [!code ++]
					c.Say("little_eat", c); // [!code ++]
					c.PlaySound("ding_potential"); // [!code ++]
					int v = Mathf.Max(5 - @int / 2, 1); // [!code ++]
					Debug.Log("sister eaten:" + @int + "/" + v); // [!code ++]
					foreach (Element value3 in c.elements.dict.Values) // [!code ++]
					{ // [!code ++]
						if (value3.IsMainAttribute) // [!code ++]
						{ // [!code ++]
							c.elements.ModPotential(value3.id, v); // [!code ++]
						} // [!code ++]
					} // [!code ++]
				} // [!code ++]
				if (c.race.id == "mutant" && c.elements.Base(1230) < 10) // [!code ++]
				{ // [!code ++]
					c.Say("little_adam", c); // [!code ++]
					c.SetFeat(1230, c.elements.Base(1230) + 1); // [!code ++]
				} // [!code ++]
				c.SetInt(112, @int + 1); // [!code ++]
				break; // [!code ++]
			} // [!code ++]
			} // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static void ProcTrait(Chara c, Card food) // [!code ++]
	{
		bool flag = false;
		foreach (Element value in t.elements.dict.Values) // [!code --]
		foreach (Element value in food.elements.dict.Values) // [!code ++]
		{
			if (!value.IsTrait)
			{
```

## FortuneRollData

[`public List<List<string>> GetPrizeList()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/FortuneRollData.cs#L178-L190)
```cs:line-numbers=178
		new List<string>
		{
			"microchip", "1089", "150", "855", "medal", "water", "goods_charm", "electronicsS", "electronics", "plat",
			"plat", "ration", "backpack2", "sister", "rp_food", "rp_block", "157", "sleepingbag" // [!code --]
			"plat", "ration", "backpack2", "sister", "rp_food", "rp_block", "157", "sleepingbag", "scrubber" // [!code ++]
		},
		new List<string>
		{
			"computer", "834", "1090", "goods_figure", "goods_canvas", "mb_1", "mb_2", "mb_3", "mb_4", "mb_5",
			"1174", "1085", "toilet", "714", "nobility", "plat", "1165", "mathammer", "medal", "bbq",
			"panty", "beehive", "ticket_resident", "lovepotion", "crystal_sun", "goods_crim", "ride_boat" // [!code --]
			"panty", "beehive", "ticket_resident", "lovepotion", "crystal_sun", "goods_crim", "ride_boat", "scrubber" // [!code ++]
		},
		new List<string>
		{
```

[`public void GetPrize(int grade, int seed)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/FortuneRollData.cs#L249-L258)
```cs:line-numbers=249
				card.rarity = Rarity.Legendary;
				break;
			case "scrubber":
				if (grade == 3) // [!code --]
				if (grade >= 1) // [!code ++]
				{
					card.rarity = Rarity.Legendary;
					card.elements.SetBase(770, 1 + EClass.rnd(25)); // [!code --]
					card.elements.SetBase(770, (grade == 1) ? 1 : ((grade - 1) * 10)); // [!code ++]
				}
				break;
			}
```

## GoalCombat

[`public virtual bool TryUseAbility(int dist, bool beforeMove = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/GoalCombat.cs#L569-L584)
```cs:line-numbers=569
			break;
		case "taunt":
		{
			bool flag10 = owner.HasCondition<StanceTaunt>(); // [!code --]
			bool flag11 = tactics.source.taunt != -1 && 100 * owner.hp / owner.MaxHP >= tactics.source.taunt; // [!code --]
			num = ((flag10 && !flag11) ? 100 : ((!flag10 && flag11) ? 100 : 0)); // [!code --]
			bool flag6 = owner.HasCondition<StanceTaunt>(); // [!code ++]
			bool flag7 = tactics.source.taunt != -1 && 100 * owner.hp / owner.MaxHP >= tactics.source.taunt; // [!code ++]
			num = ((flag6 && !flag7) ? 100 : ((!flag6 && flag7) ? 100 : 0)); // [!code ++]
			break;
		}
		case "song":
		{
			bool flag8 = owner.HasCondition<BaseSong>(); // [!code --]
			bool flag9 = owner.mana.value > owner.mana.max / 3; // [!code --]
			num = ((flag8 && !flag9) ? 100 : ((!flag8 && flag9) ? 100 : 0)); // [!code --]
			bool flag10 = owner.HasCondition<BaseSong>(); // [!code ++]
			bool flag11 = owner.mana.value > owner.mana.max / 3; // [!code ++]
			num = ((flag10 && !flag11) ? 100 : ((!flag10 && flag11) ? 100 : 0)); // [!code ++]
			break;
		}
		case "melee":
```

[`public virtual bool TryUseAbility(int dist, bool beforeMove = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/GoalCombat.cs#L654-L666)
```cs:line-numbers=654
			{
				continue;
			}
			bool flag12 = text == "dot"; // [!code --]
			if (flag12 && (owner.isRestrained || (tc != null && tc.IsRestrainedResident))) // [!code --]
			bool flag9 = text == "dot"; // [!code ++]
			if (flag9 && (owner.isRestrained || (tc != null && tc.IsRestrainedResident))) // [!code ++]
			{
				continue;
			}
			num = ((text == "attackMelee") ? tactics.P_Melee : tactics.P_Spell) + GetAttackMod(act);
			if (num > 0 && flag12) // [!code --]
			if (num > 0 && flag9) // [!code ++]
			{
				num += 10;
			}
```

[`public virtual bool TryUseAbility(int dist, bool beforeMove = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/GoalCombat.cs#L676-L689)
```cs:line-numbers=676
			{
				continue;
			}
			bool flag6 = act is ActBolt; // [!code --]
			bool flag7 = act is ActBall; // [!code --]
			if ((owner.pos.IsBlocked && (flag6 || flag7)) || !flag || (owner.IsPCParty && (EClass._zone.IsTown || EClass._zone.IsPCFaction)) || (act.id == 9150 && EClass._zone.IsPCFaction && owner.IsNeutralOrAbove())) // [!code --]
			bool flag8 = act is ActBolt; // [!code ++]
			if ((owner.pos.IsBlocked && (flag8 || act is ActBall || act is ActBreathe)) || !flag || (owner.IsPCParty && (EClass._zone.IsTown || EClass._zone.IsPCFaction)) || (act.id == 9150 && EClass._zone.IsPCFaction && owner.IsNeutralOrAbove())) // [!code ++]
			{
				continue;
			}
			GetNumEnemy(flag6 ? 6 : 5); // [!code --]
			if (numEnemy == 0 || (owner.IsPCFactionOrMinion && GetNumNeutral(flag6 ? 6 : 5) > 0)) // [!code --]
			GetNumEnemy(flag8 ? 6 : 5); // [!code ++]
			if (numEnemy == 0 || (owner.IsPCFactionOrMinion && GetNumNeutral(flag8 ? 6 : 5) > 0)) // [!code ++]
			{
				continue;
			}
```

[`int HealFactor(Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/GoalCombat.cs#L962-L997)
```cs:line-numbers=962
			{
				return 0;
			}
			float num5 = (float)c.hp / (float)c.MaxHP; // [!code --]
			if (num5 > (isHOT ? 0.85f : 0.75f)) // [!code --]
			float num7 = (float)c.hp / (float)c.MaxHP; // [!code ++]
			if (num7 > (isHOT ? 0.85f : 0.75f)) // [!code ++]
			{
				return 0;
			}
			int num6 = tactics.P_Heal - (int)((float)tactics.P_Heal * num5) + (isHOT ? 50 : 25); // [!code --]
			int num8 = tactics.P_Heal - (int)((float)tactics.P_Heal * num7) + (isHOT ? 50 : 25); // [!code ++]
			foreach (Condition condition2 in c.conditions)
			{
				if (condition2 is ConFear)
				{
					num6 += 10; // [!code --]
					num8 += 10; // [!code ++]
				}
				else if (condition2 is ConPoison)
				{
					num6 += 2; // [!code --]
					num8 += 2; // [!code ++]
				}
				else if (condition2 is ConConfuse)
				{
					num6 += 4; // [!code --]
					num8 += 4; // [!code ++]
				}
				else if (condition2 is ConDim)
				{
					num6 += 6; // [!code --]
					num8 += 6; // [!code ++]
				}
				else if (condition2 is ConBleed)
				{
					num6 += 8; // [!code --]
					num8 += 8; // [!code ++]
				}
			}
			return num6; // [!code --]
			return num8; // [!code ++]
		}
	}
	abilities.Sort((ItemAbility a, ItemAbility b) => b.priority - a.priority);
```

[`int ForeachChara(ItemAbility a, Func<Chara, int> func, bool isFriendlyAbility)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/GoalCombat.cs#L1094-L1104)
```cs:line-numbers=1094
			return func(owner);
		}
		BuildCharaList();
		int num7 = 0; // [!code --]
		int num5 = 0; // [!code ++]
		foreach (Chara chara3 in charas)
		{
			int num8 = func(chara3); // [!code --]
			if (num8 > 0) // [!code --]
			int num6 = func(chara3); // [!code ++]
			if (num6 > 0) // [!code ++]
			{
				if (isFriendlyAbility)
				{
```

[`int ForeachChara(ItemAbility a, Func<Chara, int> func, bool isFriendlyAbility)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/GoalCombat.cs#L1115-L1135)
```cs:line-numbers=1115
					}
					if (chara3 != owner)
					{
						num8 += tactics.P_Party; // [!code --]
						num6 += tactics.P_Party; // [!code ++]
					}
				}
				else if (!owner.IsHostile(chara3))
				{
					continue;
				}
				if (num8 >= num7) // [!code --]
				if (num6 >= num5) // [!code ++]
				{
					a.tg = chara3;
					num7 = num8; // [!code --]
					num5 = num6; // [!code ++]
				}
			}
		}
		return num7; // [!code --]
		return num5; // [!code ++]
	}
	int GetAttackMod(Act a)
	{
```

## Map

[`public void TrySmoothPick(Point p, Thing t, Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Map.cs#L1857-L1863)
```cs:line-numbers=1857
	{
		EClass.pc.PickOrDrop(p, t);
	}
	else if (c != null && c.IsPC && (c.pos.Equals(p) || (EClass.core.config.game.smoothPick && c.Dist(p) <= 1) || EClass._zone.IsRegion)) // [!code --]
	else if (c != null && c.IsPC && (c.pos.Equals(p) || EClass.core.config.game.smoothPick || EClass._zone.IsRegion)) // [!code ++]
	{
		c.PickOrDrop(p, t);
	}
```

[`public List<Point> ListPointsInCircle(Point center, float radius, bool mustBeWal`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Map.cs#L2333-L2338)
```cs:line-numbers=2333
		return list;
	}

	public List<Point> ListPointsInSquare(Point center, int radius, bool mustBeWalkable = true, bool los = true) // [!code ++]
	{ // [!code ++]
		List<Point> list = new List<Point>(); // [!code ++]
		ForeachSquare(center.x, center.z, radius, delegate(Point p) // [!code ++]
		{ // [!code ++]
			if ((!mustBeWalkable || !p.cell.blocked) && (!los || Los.IsVisible(center, p))) // [!code ++]
			{ // [!code ++]
				list.Add(p.Copy()); // [!code ++]
			} // [!code ++]
		}); // [!code ++]
		return list; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public List<Chara> ListCharasInCircle(Point center, float radius, bool los = true)
	{
		List<Chara> list = new List<Chara>();
```

[`public void ForeachSphere(int _x, int _z, float r, Action<Point> action)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Map.cs#L2443-L2448)
```cs:line-numbers=2443
		}
	}

	public void ForeachSquare(int _x, int _z, int r, Action<Point> action) // [!code ++]
	{ // [!code ++]
		Point point = new Point(); // [!code ++]
		for (int i = _x - r; i < _x + r + 1; i++) // [!code ++]
		{ // [!code ++]
			if (i < 0 || i >= Size) // [!code ++]
			{ // [!code ++]
				continue; // [!code ++]
			} // [!code ++]
			for (int j = _z - r; j < _z + r + 1; j++) // [!code ++]
			{ // [!code ++]
				if (j >= 0 && j < Size) // [!code ++]
				{ // [!code ++]
					point.Set(i, j); // [!code ++]
					action(point); // [!code ++]
				} // [!code ++]
			} // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void ForeachNeighbor(Point center, Action<Point> action)
	{
		int num = center.x;
```

## ModManager

[`public IEnumerator RefreshMods(Action onComplete, bool syncMods)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/ModManager.cs#L109-L122)
```cs:line-numbers=109
	disableMod |= Application.isEditor && EClass.debug.skipMod;
	LoadLocalPackages();
	LoadCustomPackage();
	if (!disableMod) // [!code --]
	if (!disableMod && dirWorkshop != null) // [!code ++]
	{
		_loading.Log("Loading workshop contents...");
		if (flag)
		{
			yield return LoadWorkshopPackages();
		}
		else if (dirWorkshop != null) // [!code --]
		else // [!code ++]
		{
			DirectoryInfo[] directories = dirWorkshop.GetDirectories();
			foreach (DirectoryInfo dir in directories)
```

[`public ModPackage AddPackage(DirectoryInfo dir, bool isInPackages = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/ModManager.cs#L173-L191)
```cs:line-numbers=173

	public ModPackage AddWorkshopPackage(WorkshopItem item, bool isInPackages = false)
	{
		ulong sizeOnDisk; // [!code --]
		string folderPath; // [!code --]
		DateTime timeStamp; // [!code --]
		bool itemInstallInfo = UserGeneratedContent.Client.GetItemInstallInfo(item.FileId, out sizeOnDisk, out folderPath, out timeStamp); // [!code --]
		DirectoryInfo directoryInfo = new DirectoryInfo(folderPath); // [!code --]
		if (!directoryInfo.Exists) // [!code --]
		bool itemInstallInfo = UserGeneratedContent.Client.GetItemInstallInfo(item.FileId, out var _, out var folderPath, out var _); // [!code ++]
		if (folderPath == null) // [!code ++]
		{
			return null; // [!code --]
			folderPath = Path.Combine(DirWorkshop.FullName, item.FileId.ToString()); // [!code ++]
		}
		DirectoryInfo directoryInfo = new DirectoryInfo(folderPath); // [!code ++]
		itemInstallInfo &= directoryInfo.Exists; // [!code ++]
		ModPackage modPackage = AddPackage(directoryInfo, isInPackages);
		modPackage.installed = itemInstallInfo;
		modPackage.banned = item.IsBanned;
		modPackage.workshopId = item.FileId.ToString();
		modPackage.item = item; // [!code ++]
		return modPackage;
	}

```

[`public void ActivatePackages()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/ModManager.cs#L354-L392)
```cs:line-numbers=354
public override void ParseExtra(DirectoryInfo dir, BaseModPackage package)
{
	ModPackage modPackage = (ModPackage)package;
	switch (dir.Name) // [!code --]
	switch (dir.Name.ToLower()) // [!code ++]
	{
	case "TalkText": // [!code --]
	case "talktext": // [!code ++]
		modPackage.ParseTalkText(dir);
		break;
	case "Map": // [!code --]
	case "map": // [!code ++]
		if (!package.builtin)
		{
			modPackage.ParseMap(dir);
		}
		break;
	case "Map Piece": // [!code --]
	case "map piece": // [!code ++]
		if (!package.builtin)
		{
			modPackage.ParseMapPiece(dir);
		}
		break;
	case "Texture Replace": // [!code --]
	case "texture replace": // [!code ++]
		replaceFiles.AddRange(modPackage.ParseTextureReplace(dir));
		break;
	case "Texture": // [!code --]
	case "texture": // [!code ++]
		modPackage.ParseTexture(dir);
		break;
	case "Portrait": // [!code --]
	case "portrait": // [!code ++]
		modPackage.ParsePortrait(dir);
		break;
	case "LangMod": // [!code --]
	case "langmod": // [!code ++]
		modPackage.ParseLangMod(dir);
		break;
	case "Sound": // [!code --]
	case "sound": // [!code ++]
		modPackage.ParseSound(dir);
		break;
	case "Lang": // [!code --]
	case "lang": // [!code ++]
		modPackage.AddOrUpdateLang(dir);
		break;
	}
```

## ModPackage

[`public override void GenerateCustomContentProfiles()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/ModPackage.cs#L225-L230)
```cs:line-numbers=225
								}
								else
								{
									ModUtil.FixDefaultThingRowPref(r6); // [!code ++]
									customContent.Add(CustomThingContent.CreateFromRow(r6, this));
								}
							}
```

## ModUtil

[`public static Zone FindZoneByFullName(string zoneFullName = "ntyris/0", bool use`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/ModUtil.cs#L127-L153)
```cs:line-numbers=127
	{
		return null;
	}
	zoneFullName = zoneFullName.Replace('/', '@'); // [!code --]
	Zone zone = EClass.game.spatials.Find((Zone z) => z.ZoneFullName == zoneFullName); // [!code --]
	if (zone != null) // [!code --]
	var (zoneType, zoneId, destLv) = Zone.ParseZoneFullName(zoneFullName); // [!code ++]
	if (zoneId.IsEmpty()) // [!code ++]
	{
		return zone; // [!code --]
	} // [!code --]
	int num = zoneFullName.LastIndexOf('@'); // [!code --]
	int destLv = 0; // [!code --]
	string zoneType; // [!code --]
	if (num > 0 && num < zoneFullName.Length - 1) // [!code --]
	{ // [!code --]
		zoneType = zoneFullName[..num]; // [!code --]
		destLv = zoneFullName[(num + 1)..].ToInt(); // [!code --]
	} // [!code --]
	else // [!code --]
	{ // [!code --]
		zoneType = zoneFullName.Replace("@", ""); // [!code --]
		return null; // [!code ++]
	}
	string zoneId = zoneType.Replace("Zone_", ""); // [!code --]
	zoneType = "Zone_" + zoneId; // [!code --]
	zone = EClass.game.spatials.Find((Zone z) => z.GetType().Name == zoneType || z.id == zoneId)?.FindOrCreateLevel(destLv); // [!code --]
	Zone zone = EClass.game.spatials.Find((Zone z) => z.GetType().Name == zoneType || z.id == zoneId)?.FindOrCreateLevel(destLv); // [!code ++]
	if (zone == null && (zoneId == "*" || useRandomFallback))
	{
		zone = (from z in EClass.game.spatials.map.Values.OfType<Zone>()
```

[`private static void OnPreLoadInit(GameIOContext context)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/ModUtil.cs#L228-L233)
```cs:line-numbers=228
private static void OnPostLoadInit(GameIOContext context)
{
	EClass.player.knownBGMs.RemoveWhere((int id) => !EClass.core.refs.dictBGM.ContainsKey(id));
	KeyValuePair<int, Spatial>[] array = EClass.game.spatials.map.ToArray(); // [!code ++]
	for (int i = 0; i < array.Length; i++) // [!code ++]
	{ // [!code ++]
		KeyValuePair<int, Spatial> keyValuePair = array[i]; // [!code ++]
		var (num2, spatial2) = (KeyValuePair<int, Spatial>)(ref keyValuePair); // [!code ++]
		if (spatial2.source == null) // [!code ++]
		{ // [!code ++]
			spatial2.Destroy(); // [!code ++]
			UnityEngine.Debug.LogWarning($"#mod-content removed invalid zone '{num2}', '{spatial2.id}', '{(spatial2 as Zone)?.ZoneFullName}'"); // [!code ++]
		} // [!code ++]
	} // [!code ++]
	List<ICustomContent> contents = new List<ICustomContent>();
	CustomReligionContent.LoadReligionData(context);
	LoadCustomContent<CustomZoneContent>();
```

[`private static void OnSetLang(string lang)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/ModUtil.cs#L338-L361)
```cs:line-numbers=338
				activatedUserMod.UpdateSourceLocalizationFile(lang);
			}
		}
		foreach (string item in LoadGodTalk()) // [!code --]
		AliasGen.list = null; // [!code ++]
		AliasGen.listMix.Clear(); // [!code ++]
		AliasGen.listBuiltin.Clear(); // [!code ++]
		WordGen.listMix.Clear(); // [!code ++]
		WordGen.listBuiltin.Clear(); // [!code ++]
		foreach (string item in LoadAlias()) // [!code ++]
		{ // [!code ++]
			MOD.listAlias.Add(new ExcelData(item)); // [!code ++]
		} // [!code ++]
		NameGen.list = null; // [!code ++]
		foreach (string item2 in LoadName()) // [!code ++]
		{
			MOD.listGodTalk.Add(new ExcelData(item)); // [!code --]
			MOD.listName.Add(new ExcelData(item2)); // [!code ++]
		}
		foreach (string item2 in LoadCharaTalk()) // [!code --]
		foreach (string item3 in LoadGodTalk()) // [!code ++]
		{
			MOD.listTalk.Add(new ExcelData(item2)); // [!code --]
			MOD.listGodTalk.Add(new ExcelData(item3)); // [!code ++]
		}
		foreach (string item3 in LoadCharaTone()) // [!code --]
		foreach (string item4 in LoadCharaTalk()) // [!code ++]
		{
			MOD.tones.Add(new ExcelData(item3)); // [!code --]
			MOD.listTalk.Add(new ExcelData(item4)); // [!code ++]
		} // [!code ++]
		foreach (string item5 in LoadCharaTone()) // [!code ++]
		{ // [!code ++]
			MOD.tones.Add(new ExcelData(item5)); // [!code ++]
		}
		BookList.dict = null;
		BottleMessageList.list = null;
		Lang.excelDialog = null;
		foreach (CustomFileContent item4 in _customContent.Values.OfType<CustomFileContent>()) // [!code --]
		foreach (CustomFileContent item6 in _customContent.Values.OfType<CustomFileContent>()) // [!code ++]
		{
			item4.OnSetLang(lang); // [!code --]
			item6.OnSetLang(lang); // [!code ++]
		}
	}

```

[`public static void AddContent(ICustomContent content)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/ModUtil.cs#L382-L393)
```cs:line-numbers=382
		_customContent[content.ContentId] = content;
	}

	public static bool FixDefaultRenderRowPref(RenderRow r, string id) // [!code ++]
	{ // [!code ++]
		FileInfo fileInfo = PackageIterator.GetFiles("Texture/" + id + ".pref").LastOrDefault(); // [!code ++]
		if (fileInfo == null) // [!code ++]
		{ // [!code ++]
			return false; // [!code ++]
		} // [!code ++]
		r.pref = SourcePref.ReadFromIni(fileInfo.FullName); // [!code ++]
		return true; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static void FixDefaultCharaRowPref(SourceChara.Row r)
	{
		r.pref = new SourcePref // [!code --]
		if (!r.tag.Contains("usePref") || !FixDefaultRenderRowPref(r, r.id)) // [!code ++]
		{
			pivotY = -10 // [!code --]
		}; // [!code --]
			r.pref = new SourcePref // [!code ++]
			{ // [!code ++]
				pivotY = -10 // [!code ++]
			}; // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static void FixDefaultThingRowPref(SourceThing.Row r) // [!code ++]
	{ // [!code ++]
		if (!r.tag.Contains("usePref") || !FixDefaultRenderRowPref(r, r.id)) // [!code ++]
		{ // [!code ++]
			r.pref = new SourcePref // [!code ++]
			{ // [!code ++]
				shadow = 1 // [!code ++]
			}; // [!code ++]
		} // [!code ++]
	}

	public static List<Thing> GenerateMerchantStock(Card owner, string stockId = null, bool forceRestock = false)
```

[`public static List<string> LoadExcelDialog()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/ModUtil.cs#L496-L501)
```cs:line-numbers=496
		return list;
	}

	public static List<string> LoadAlias() // [!code ++]
	{ // [!code ++]
		List<string> list = new List<string>(); // [!code ++]
		FileInfo[] files = PackageIterator.GetFiles(Lang.langCode + "/Data/Alias.xlsx"); // [!code ++]
		foreach (FileInfo fileInfo in files) // [!code ++]
		{ // [!code ++]
			list.Add(fileInfo.FullName); // [!code ++]
			UnityEngine.Debug.Log("#mod-content loaded Alias " + fileInfo.ShortPath()); // [!code ++]
		} // [!code ++]
		return list; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static List<string> LoadName() // [!code ++]
	{ // [!code ++]
		List<string> list = new List<string>(); // [!code ++]
		FileInfo[] files = PackageIterator.GetFiles(Lang.langCode + "/Data/Name.xlsx"); // [!code ++]
		foreach (FileInfo fileInfo in files) // [!code ++]
		{ // [!code ++]
			list.Add(fileInfo.FullName); // [!code ++]
			UnityEngine.Debug.Log("#mod-content loaded Name " + fileInfo.ShortPath()); // [!code ++]
		} // [!code ++]
		return list; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static List<string> LoadCharaTalk()
	{
		List<string> list = new List<string>();
```

[`public static string ExportSourceDataJson(string sourceData, Formatting format =`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/ModUtil.cs#L1090-L1096)
```cs:line-numbers=1090
		{
			return "";
		}
		return JsonConvert.SerializeObject(sourceData2.ExportRows(), format); // [!code --]
		return JsonConvert.SerializeObject(sourceData2.ExportRows(), format, GameIOContext.Settings); // [!code ++]
	}

	public static void ExportAllSourceDataJson(string dir)
```

## RecipeCard

[`public void MixIngredients(Thing t)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/RecipeCard.cs#L373-L379)
```cs:line-numbers=373
		{
			list.Add(ingredient.thing);
		}
		CraftUtil.MixIngredients(t, list, CraftUtil.MixType.General, GetQualityBonus()); // [!code --]
		CraftUtil.MixIngredients(t, list, (t.trait is TraitDrink) ? CraftUtil.MixType.Drink : CraftUtil.MixType.General, GetQualityBonus()); // [!code ++]
		Rand.SetSeed();
	}

```

## Spatial

[`public Faction mainFaction`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Spatial.cs#L450-L456)
```cs:line-numbers=450
		}
	}

	public SourceZone.Row source => _source ?? (_source = EClass.sources.zones.map[id]); // [!code --]
	public SourceZone.Row source => _source ?? (_source = EClass.sources.zones.map.TryGetValue(id)); // [!code ++]

	public bool IsPlayerFaction => mainFaction == EClass.pc.faction;

```

## TaskDig

[`using System.Collections.Generic;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskDig.cs#L1-L3)
```cs:line-numbers=1
using System.Collections.Generic; // [!code ++]
 // [!code ++]
public class TaskDig : BaseTaskHarvest
{
	public enum Mode
```

[`public enum Mode`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskDig.cs#L13-L30)
```cs:line-numbers=13

	public override HarvestType harvestType => HarvestType.Floor;

	public override string IdRecipe // [!code --]
	{ // [!code --]
		get // [!code --]
		{ // [!code --]
			if (!pos.HasBridge) // [!code --]
			{ // [!code --]
				return pos.sourceFloor.RecipeID; // [!code --]
			} // [!code --]
			return pos.sourceBridge.RecipeID; // [!code --]
		} // [!code --]
	} // [!code --]
 // [!code --]
	public override int RightHand => 1101;

	public override bool IsHostileAct => true;
```

[`public override int destDist`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskDig.cs#L51-L56)
```cs:line-numbers=51

	public override CursorInfo CursorIcon => CursorSystem.Dig;

	public override string GetIdRecipe(Point p) // [!code ++]
	{ // [!code ++]
		if (!p.HasBridge) // [!code ++]
		{ // [!code ++]
			return p.sourceFloor.RecipeID; // [!code ++]
		} // [!code ++]
		return p.sourceBridge.RecipeID; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public override string GetTextSmall(Card c)
	{
		if (!pos.cell.HasBridge)
```

[`public override void OnCreateProgress(Progress_Custom p)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskDig.cs#L94-L100)
```cs:line-numbers=94

	public override HitResult GetHitResult()
	{
		if (EClass._zone.IsRegion && GetTreasureMap() != null) // [!code --]
		if (EClass._zone.IsRegion && GetTreasureMap(pos) != null) // [!code ++]
		{
			return HitResult.Valid;
		}
```

[`public override HitResult GetHitResult()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskDig.cs#L133-L144)
```cs:line-numbers=133
		return HitResult.Default;
	}

	public Thing GetTreasureMap() // [!code --]
	public Thing GetTreasureMap(Point p) // [!code ++]
	{
		foreach (Thing item in EClass.pc.things.List((Thing t) => t.trait is TraitScrollMapTreasure))
		{
			TraitScrollMapTreasure traitScrollMapTreasure = item.trait as TraitScrollMapTreasure;
			if (pos.Equals(traitScrollMapTreasure.GetDest(fix: true))) // [!code --]
			if (p.Equals(traitScrollMapTreasure.GetDest(fix: true))) // [!code ++]
			{
				return item;
			}
```

[`public Thing GetTreasureMap()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskDig.cs#L148-L211)
```cs:line-numbers=148

	public override void OnProgressComplete()
	{
		string idRecipe = IdRecipe; // [!code --]
		int num = (pos.HasBridge ? pos.matBridge.hardness : pos.matFloor.hardness); // [!code --]
		if (EClass._zone.IsRegion) // [!code --]
		int num = owner.Tool?.Evalue(770) ?? 0; // [!code ++]
		num = ((num <= 0) ? 1 : (2 + num / 10)); // [!code ++]
		if (num > 1) // [!code ++]
		{
			Thing map = GetTreasureMap(); // [!code --]
			if (map != null || EClass.debug.enable) // [!code --]
			List<Point> list = EClass._map.ListPointsInSquare(pos, num - 1); // [!code ++]
			list.Sort((Point a, Point b) => a.Distance(pos) - b.Distance(pos)); // [!code ++]
			{
				if (map == null) // [!code --]
				foreach (Point item in list) // [!code ++]
				{
					map = ThingGen.Create("map_treasure", -1, EClass.pc.LV); // [!code --]
					if (owner == null || owner.isDead) // [!code ++]
					{ // [!code ++]
						break; // [!code ++]
					} // [!code ++]
					Dig(item); // [!code ++]
				}
				SE.Play("ding_skill"); // [!code --]
				Msg.Say("digTreasure"); // [!code --]
				Rand.UseSeed(map.refVal, delegate // [!code --]
				{ // [!code --]
					Thing thing = ThingGen.CreateTreasure("chest_treasure", map.LV); // [!code --]
					EClass._zone.AddCard(thing, EClass.pc.pos); // [!code --]
					ThingGen.TryLickChest(thing); // [!code --]
				}); // [!code --]
				map.Destroy(); // [!code --]
				EClass.player.willAutoSave = true; // [!code --]
				return;
			}
		}
		switch (mode) // [!code --]
		Dig(pos); // [!code ++]
		void Dig(Point p) // [!code ++]
		{
		case Mode.Default: // [!code --]
			EClass._map.SetBridge(pos.x, pos.z, 0, 0, 0, 0, 0); // [!code --]
			break; // [!code --]
		case Mode.RemoveFloor: // [!code --]
			EClass._map.MineFloor(pos, owner); // [!code --]
			pos.Animate(AnimeID.Dig, animeBlock: true); // [!code --]
			if (!owner.IsAgent) // [!code --]
			if (GetHitResult(p) != HitResult.Invalid) // [!code ++]
			{
				owner.elements.ModExp(230, 20 + num / 2); // [!code --]
			} // [!code --]
			break; // [!code --]
		case Mode.Ramp: // [!code --]
			EClass._map.MineFloor(pos, owner); // [!code --]
			break; // [!code --]
		} // [!code --]
		if (EClass._zone.IsCrime(owner, this)) // [!code --]
		{ // [!code --]
			EClass.player.ModKarma(-1); // [!code --]
		} // [!code --]
		if (EClass.rnd(2) == 0) // [!code --]
		{ // [!code --]
			owner.stamina.Mod(-1); // [!code --]
		} // [!code --]
		if (owner != null) // [!code --]
		{ // [!code --]
			if (owner.IsPC) // [!code --]
			{ // [!code --]
				EClass.player.recipes.ComeUpWithRecipe(idRecipe, 30); // [!code --]
			} // [!code --]
			if (owner.IsPC && owner.IsAliveInCurrentZone && EClass._zone.IsSkyLevel && !EClass.game.IsSurvival && owner.pos.IsSky) // [!code --]
			{ // [!code --]
				EClass.pc.FallFromZone(); // [!code --]
				string idRecipe = GetIdRecipe(p); // [!code ++]
				int num2 = (p.HasBridge ? p.matBridge.hardness : p.matFloor.hardness); // [!code ++]
				if (EClass._zone.IsRegion) // [!code ++]
				{ // [!code ++]
					Thing map = GetTreasureMap(p); // [!code ++]
					if (map != null || EClass.debug.enable) // [!code ++]
					{ // [!code ++]
						if (map == null) // [!code ++]
						{ // [!code ++]
							map = ThingGen.Create("map_treasure", -1, EClass.pc.LV); // [!code ++]
						} // [!code ++]
						SE.Play("ding_skill"); // [!code ++]
						Msg.Say("digTreasure"); // [!code ++]
						Rand.UseSeed(map.refVal, delegate // [!code ++]
						{ // [!code ++]
							Thing thing = ThingGen.CreateTreasure("chest_treasure", map.LV); // [!code ++]
							EClass._zone.AddCard(thing, p); // [!code ++]
							ThingGen.TryLickChest(thing); // [!code ++]
						}); // [!code ++]
						map.Destroy(); // [!code ++]
						EClass.player.willAutoSave = true; // [!code ++]
						return; // [!code ++]
					} // [!code ++]
				} // [!code ++]
				switch (mode) // [!code ++]
				{ // [!code ++]
				case Mode.Default: // [!code ++]
					EClass._map.SetBridge(p.x, p.z, 0, 0, 0, 0, 0); // [!code ++]
					break; // [!code ++]
				case Mode.RemoveFloor: // [!code ++]
					EClass._map.MineFloor(p, owner); // [!code ++]
					p.Animate(AnimeID.Dig, animeBlock: true); // [!code ++]
					if (!owner.IsAgent) // [!code ++]
					{ // [!code ++]
						owner.elements.ModExp(230, 20 + num2 / 2); // [!code ++]
					} // [!code ++]
					break; // [!code ++]
				case Mode.Ramp: // [!code ++]
					EClass._map.MineFloor(p, owner); // [!code ++]
					break; // [!code ++]
				} // [!code ++]
				if (EClass._zone.IsCrime(owner, this)) // [!code ++]
				{ // [!code ++]
					EClass.player.ModKarma(-1); // [!code ++]
				} // [!code ++]
				if (EClass.rnd(2) == 0) // [!code ++]
				{ // [!code ++]
					owner.stamina.Mod(-1); // [!code ++]
				} // [!code ++]
				if (owner != null) // [!code ++]
				{ // [!code ++]
					if (owner.IsPC) // [!code ++]
					{ // [!code ++]
						EClass.player.recipes.ComeUpWithRecipe(idRecipe, 30); // [!code ++]
					} // [!code ++]
					if (owner.IsPC && owner.IsAliveInCurrentZone && EClass._zone.IsSkyLevel && !EClass.game.IsSurvival && owner.pos.IsSky) // [!code ++]
					{ // [!code ++]
						EClass.pc.FallFromZone(); // [!code ++]
					} // [!code ++]
				} // [!code ++]
			}
		}
	}
```

## TaskDrawWater

[`using System.Collections.Generic;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskDrawWater.cs#L1-L3)
```cs:line-numbers=1
using System.Collections.Generic; // [!code ++]
 // [!code ++]
public class TaskDrawWater : TaskDesignation
{
	public TraitToolWaterPot pot;
```

[`public override void OnCreateProgress(Progress_Custom p)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskDrawWater.cs#L53-L106)
```cs:line-numbers=53
		};
		p.onProgressComplete = delegate
		{
			Effect.Get("mine").Play(pos).SetParticleColor(pos.cell.HasBridge ? pos.matBridge.GetColor() : pos.matFloor.GetColor()) // [!code --]
				.Emit(10 + EClass.rnd(10)); // [!code --]
			pos.Animate(AnimeID.Dig, animeBlock: true); // [!code --]
			owner.PlaySound("water");
			pot.owner.Dye(pos.HasBridge ? pos.matBridge : pos.matFloor); // [!code --]
			switch ((pos.HasBridge ? pos.sourceBridge : pos.sourceFloor).alias) // [!code --]
			{ // [!code --]
			case "floor_water_shallow": // [!code --]
				ChangeFloor("floor_water_shallow2"); // [!code --]
				break; // [!code --]
			case "floor_water": // [!code --]
				ChangeFloor("floor_water_shallow"); // [!code --]
				break; // [!code --]
			case "floor_water_deep": // [!code --]
				ChangeFloor("floor_water"); // [!code --]
				break; // [!code --]
			default: // [!code --]
				ChangeFloor("floor_raw3"); // [!code --]
				break; // [!code --]
			} // [!code --]
			pot.owner.ModCharge(1); // [!code --]
			owner.elements.ModExp(286, 5f); // [!code --]
			if (EClass.rnd(3) == 0) // [!code --]
			int num = owner.Tool?.Evalue(770) ?? 0; // [!code ++]
			num = ((num <= 0) ? 1 : (2 + num / 10)); // [!code ++]
			if (num > 1) // [!code ++]
			{
				owner.stamina.Mod(-1); // [!code --]
				List<Point> list = EClass._map.ListPointsInSquare(pos, num - 1); // [!code ++]
				list.Sort((Point a, Point b) => a.Distance(pos) - b.Distance(pos)); // [!code ++]
				{ // [!code ++]
					foreach (Point item in list) // [!code ++]
					{ // [!code ++]
						if (owner == null || owner.isDead) // [!code ++]
						{ // [!code ++]
							break; // [!code ++]
						} // [!code ++]
						Draw(item); // [!code ++]
					} // [!code ++]
					return; // [!code ++]
				} // [!code ++]
			}
			Draw(pos); // [!code ++]
		};
		void ChangeFloor(string id) // [!code --]
		void Draw(Point p) // [!code ++]
		{
			SourceFloor.Row row = EClass.sources.floors.alias[id]; // [!code --]
			if (pos.HasBridge) // [!code --]
			if (pot.owner.c_charges < pot.MaxCharge && GetHitResult(p) != HitResult.Invalid) // [!code ++]
			{
				pos.cell._bridge = (byte)row.id; // [!code --]
				if (id == "floor_raw3") // [!code --]
				Effect.Get("mine").Play(p).SetParticleColor(p.cell.HasBridge ? p.matBridge.GetColor() : p.matFloor.GetColor()) // [!code ++]
					.Emit(10 + EClass.rnd(10)); // [!code ++]
				p.Animate(AnimeID.Dig, animeBlock: true); // [!code ++]
				pot.owner.Dye(p.HasBridge ? p.matBridge : p.matFloor); // [!code ++]
				switch ((p.HasBridge ? p.sourceBridge : p.sourceFloor).alias) // [!code ++]
				{
					pos.cell._bridgeMat = 45; // [!code --]
				case "floor_water_shallow": // [!code ++]
					ChangeFloor("floor_water_shallow2"); // [!code ++]
					break; // [!code ++]
				case "floor_water": // [!code ++]
					ChangeFloor("floor_water_shallow"); // [!code ++]
					break; // [!code ++]
				case "floor_water_deep": // [!code ++]
					ChangeFloor("floor_water"); // [!code ++]
					break; // [!code ++]
				default: // [!code ++]
					ChangeFloor("floor_raw3"); // [!code ++]
					break; // [!code ++]
				} // [!code ++]
				pot.owner.ModCharge(1); // [!code ++]
				owner.elements.ModExp(286, 5f); // [!code ++]
				if (EClass.rnd(3) == 0) // [!code ++]
				{ // [!code ++]
					owner.stamina.Mod(-1); // [!code ++]
				}
			}
			else // [!code --]
			void ChangeFloor(string id) // [!code ++]
			{
				pos.cell._floor = (byte)row.id; // [!code --]
				if (id == "floor_raw3") // [!code --]
				SourceFloor.Row row = EClass.sources.floors.alias[id]; // [!code ++]
				if (p.HasBridge) // [!code ++]
				{ // [!code ++]
					p.cell._bridge = (byte)row.id; // [!code ++]
					if (id == "floor_raw3") // [!code ++]
					{ // [!code ++]
						p.cell._bridgeMat = 45; // [!code ++]
					} // [!code ++]
				} // [!code ++]
				else // [!code ++]
				{
					pos.cell._floorMat = 45; // [!code --]
					p.cell._floor = (byte)row.id; // [!code ++]
					if (id == "floor_raw3") // [!code ++]
					{ // [!code ++]
						p.cell._floorMat = 45; // [!code ++]
					} // [!code ++]
				}
				EClass._map.SetLiquid(p.x, p.z); // [!code ++]
				p.RefreshNeighborTiles(); // [!code ++]
			}
			EClass._map.SetLiquid(pos.x, pos.z); // [!code --]
			pos.RefreshNeighborTiles(); // [!code --]
		}
	}

```

## TaskHarvest

[`public bool IsReapSeed`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskHarvest.cs#L30-L51)
```cs:line-numbers=30

	public override HarvestType harvestType => mode;

	public override string IdRecipe // [!code --]
	{ // [!code --]
		get // [!code --]
		{ // [!code --]
			if (!IsObj) // [!code --]
			{ // [!code --]
				if (target == null) // [!code --]
				{ // [!code --]
					return ""; // [!code --]
				} // [!code --]
				return target.source.RecipeID; // [!code --]
			} // [!code --]
			return pos.sourceObj.RecipeID; // [!code --]
		} // [!code --]
	} // [!code --]
 // [!code --]
	public override int RightHand => 1005;

	public override int destDist => 1;
```

[`public override bool Loop`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskHarvest.cs#L98-L103)
```cs:line-numbers=98
		}
	}

	public override string GetIdRecipe(Point p) // [!code ++]
	{ // [!code ++]
		if (!IsObj) // [!code ++]
		{ // [!code ++]
			if (target == null) // [!code ++]
			{ // [!code ++]
				return ""; // [!code ++]
			} // [!code ++]
			return target.source.RecipeID; // [!code ++]
		} // [!code ++]
		return p.sourceObj.RecipeID; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public override bool CanManualCancel()
	{
		return true;
```

[`public override void OnCreateProgress(Progress_Custom p)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskHarvest.cs#L346-L352)
```cs:line-numbers=346
	};
	p.onProgressComplete = delegate
	{
		string idRecipe = IdRecipe; // [!code --]
		string idRecipe = GetIdRecipe(pos); // [!code ++]
		SourceBacker.Row backerObj = EClass._map.GetBackerObj(pos);
		int num2 = ((EClass.rnd(3) != 0) ? 1 : 0);
		if (IsObj)
```

## TaskMine

[`public enum Mode`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskMine.cs#L14-L21)
```cs:line-numbers=14

	public override HarvestType harvestType => HarvestType.Block;

	public override string IdRecipe => pos.sourceBlock.RecipeID; // [!code --]
 // [!code --]
	public override int destDist => 1;

	public override bool isBlock => true;
```

[`public enum Mode`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskMine.cs#L30-L35)
```cs:line-numbers=30

	public override bool IsHostileAct => true;

	public override string GetIdRecipe(Point p) // [!code ++]
	{ // [!code ++]
		return p.sourceBlock.RecipeID; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static bool CanMine(Point pos, Card t)
	{
		if (t != null && pos.HasBlock && (!pos.HasObj || !pos.sourceObj.tileType.IsBlockMount))
```

[`public override HitResult GetHitResult()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskMine.cs#L135-L141)
```cs:line-numbers=135

	public override void OnProgressComplete()
	{
		string idRecipe = IdRecipe; // [!code --]
		string idRecipe = GetIdRecipe(pos); // [!code ++]
		int hardness = pos.matBlock.hardness;
		switch (mode)
		{
```

## TaskPlow

[`using System.Collections.Generic;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskPlow.cs#L1-L3)
```cs:line-numbers=1
using System.Collections.Generic; // [!code ++]
using UnityEngine;

public class TaskPlow : TaskDesignation
```

[`public override void OnCreateProgress(Progress_Custom p)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskPlow.cs#L56-L79)
```cs:line-numbers=56
		p.onProgressComplete = delegate
		{
			owner.PlaySound(pos.cell.HasBridge ? pos.cell.matBridge.GetSoundDead(pos.sourceBridge) : pos.cell.matFloor.GetSoundDead(pos.sourceFloor));
			Effect.Get("mine").Play(pos).SetParticleColor(pos.cell.HasBridge ? pos.matBridge.GetColor() : pos.matFloor.GetColor()) // [!code --]
				.Emit(10 + EClass.rnd(10)); // [!code --]
			pos.Animate(AnimeID.Dig, animeBlock: true); // [!code --]
			if (pos.HasBridge) // [!code --]
			int num = owner.Tool?.Evalue(770) ?? 0; // [!code ++]
			num = ((num <= 0) ? 1 : (2 + num / 10)); // [!code ++]
			if (num > 1) // [!code ++]
			{
				pos.cell._bridge = 4; // [!code --]
				List<Point> list = EClass._map.ListPointsInSquare(pos, num - 1); // [!code ++]
				list.Sort((Point a, Point b) => a.Distance(pos) - b.Distance(pos)); // [!code ++]
				{ // [!code ++]
					foreach (Point item in list) // [!code ++]
					{ // [!code ++]
						if (owner == null || owner.isDead) // [!code ++]
						{ // [!code ++]
							break; // [!code ++]
						} // [!code ++]
						Plow(item); // [!code ++]
					} // [!code ++]
					return; // [!code ++]
				} // [!code ++]
			}
			else // [!code --]
			{ // [!code --]
				pos.SetFloor(pos.matFloor.id, 4); // [!code --]
			} // [!code --]
			owner.elements.ModExp(286, 30f); // [!code --]
			if (owner.IsPC) // [!code --]
			Plow(pos); // [!code ++]
		}; // [!code ++]
		void Plow(Point p) // [!code ++]
		{ // [!code ++]
			if ((p.HasBridge ? p.sourceBridge : p.sourceFloor).tag.Contains("soil") && GetHitResult(p) != HitResult.Invalid) // [!code ++]
			{
				EClass.player.stats.plow++; // [!code --]
				Effect.Get("mine").Play(p).SetParticleColor(p.cell.HasBridge ? p.matBridge.GetColor() : p.matFloor.GetColor()) // [!code ++]
					.Emit(10 + EClass.rnd(10)); // [!code ++]
				p.Animate(AnimeID.Dig, animeBlock: true); // [!code ++]
				if (p.HasBridge) // [!code ++]
				{ // [!code ++]
					p.cell._bridge = 4; // [!code ++]
				} // [!code ++]
				else // [!code ++]
				{ // [!code ++]
					p.SetFloor(p.matFloor.id, 4); // [!code ++]
				} // [!code ++]
				owner.elements.ModExp(286, 30f); // [!code ++]
				if (owner.IsPC) // [!code ++]
				{ // [!code ++]
					EClass.player.stats.plow++; // [!code ++]
				} // [!code ++]
				owner.stamina.Mod(-1); // [!code ++]
			}
			owner.stamina.Mod(-1); // [!code --]
		}; // [!code --]
		} // [!code ++]
	}

	public override HitResult GetHitResult()
```

## TaskPoint

[`public override bool CanManualCancel()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskPoint.cs#L39-L44)
```cs:line-numbers=39
		return false;
	}

	public HitResult GetHitResult(Point p) // [!code ++]
	{ // [!code ++]
		Point point = pos.Copy(); // [!code ++]
		pos = p; // [!code ++]
		HitResult hitResult = GetHitResult(); // [!code ++]
		pos = point; // [!code ++]
		return hitResult; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public override bool CanPerform()
	{
		if (owner == null)
```

## TaskPourWater

[`using System.Collections.Generic;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskPourWater.cs#L1-L3)
```cs:line-numbers=1
using System.Collections.Generic; // [!code ++]
 // [!code ++]
public class TaskPourWater : TaskDesignation
{
	public TraitToolWaterPot pot;
```

[`public override void OnCreateProgress(Progress_Custom p)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskPourWater.cs#L40-L100)
```cs:line-numbers=40
		};
		p.onProgressComplete = delegate
		{
			if (pot.owner.DyeMat == null) // [!code --]
			{ // [!code --]
				pot.owner.Dye(MATERIAL.sourceWaterSea); // [!code --]
			} // [!code --]
			switch ((pos.HasBridge ? pos.sourceBridge : pos.sourceFloor).alias) // [!code --]
			{ // [!code --]
			case "floor_water_shallow2": // [!code --]
				ChangeFloor("floor_water_shallow"); // [!code --]
				break; // [!code --]
			case "floor_water_shallow": // [!code --]
				ChangeFloor("floor_water"); // [!code --]
				break; // [!code --]
			case "floor_water": // [!code --]
				ChangeFloor("floor_water_deep"); // [!code --]
				break; // [!code --]
			default: // [!code --]
				ChangeFloor("floor_water_shallow2"); // [!code --]
				break; // [!code --]
			} // [!code --]
			Effect.Get("mine").Play(pos).SetParticleColor(pos.cell.HasBridge ? pos.matBridge.GetColor() : pos.matFloor.GetColor()) // [!code --]
				.Emit(10 + EClass.rnd(10)); // [!code --]
			pos.Animate(AnimeID.Dig, animeBlock: true); // [!code --]
			owner.PlaySound("water_farm");
			pot.owner.ModCharge(-1); // [!code --]
			if (pot.owner.c_charges <= 0) // [!code --]
			{ // [!code --]
				pot.owner.Dye(EClass.sources.materials.alias["void"]); // [!code --]
			} // [!code --]
			owner.elements.ModExp(286, 5f); // [!code --]
			if (EClass.rnd(3) == 0) // [!code --]
			int num = owner.Tool?.Evalue(770) ?? 0; // [!code ++]
			num = ((num <= 0) ? 1 : (2 + num / 10)); // [!code ++]
			if (num > 1) // [!code ++]
			{
				owner.stamina.Mod(-1); // [!code --]
				List<Point> list = EClass._map.ListPointsInSquare(pos, num - 1); // [!code ++]
				list.Sort((Point a, Point b) => a.Distance(pos) - b.Distance(pos)); // [!code ++]
				{ // [!code ++]
					foreach (Point item in list) // [!code ++]
					{ // [!code ++]
						if (owner == null || owner.isDead) // [!code ++]
						{ // [!code ++]
							break; // [!code ++]
						} // [!code ++]
						Pour(item); // [!code ++]
					} // [!code ++]
					return; // [!code ++]
				} // [!code ++]
			}
			Pour(pos); // [!code ++]
		};
		void ChangeFloor(string id) // [!code --]
		void Pour(Point p) // [!code ++]
		{
			SourceFloor.Row row = EClass.sources.floors.alias[id]; // [!code --]
			if (pos.HasBridge) // [!code --]
			if (pot.owner.c_charges > 0 && GetHitResult(p) != HitResult.Invalid) // [!code ++]
			{
				pos.cell._bridge = (byte)row.id; // [!code --]
				pos.cell._bridgeMat = (byte)pot.owner.DyeMat.id; // [!code --]
				if (pot.owner.DyeMat == null) // [!code ++]
				{ // [!code ++]
					pot.owner.Dye(MATERIAL.sourceWaterSea); // [!code ++]
				} // [!code ++]
				switch ((p.HasBridge ? p.sourceBridge : p.sourceFloor).alias) // [!code ++]
				{ // [!code ++]
				case "floor_water_shallow2": // [!code ++]
					ChangeFloor("floor_water_shallow"); // [!code ++]
					break; // [!code ++]
				case "floor_water_shallow": // [!code ++]
					ChangeFloor("floor_water"); // [!code ++]
					break; // [!code ++]
				case "floor_water": // [!code ++]
					ChangeFloor("floor_water_deep"); // [!code ++]
					break; // [!code ++]
				default: // [!code ++]
					ChangeFloor("floor_water_shallow2"); // [!code ++]
					break; // [!code ++]
				} // [!code ++]
				Effect.Get("mine").Play(p).SetParticleColor(p.cell.HasBridge ? pos.matBridge.GetColor() : p.matFloor.GetColor()) // [!code ++]
					.Emit(10 + EClass.rnd(10)); // [!code ++]
				p.Animate(AnimeID.Dig, animeBlock: true); // [!code ++]
				pot.owner.ModCharge(-1); // [!code ++]
				if (pot.owner.c_charges <= 0) // [!code ++]
				{ // [!code ++]
					pot.owner.Dye(EClass.sources.materials.alias["void"]); // [!code ++]
				} // [!code ++]
				owner.elements.ModExp(286, 5f); // [!code ++]
				if (EClass.rnd(3) == 0) // [!code ++]
				{ // [!code ++]
					owner.stamina.Mod(-1); // [!code ++]
				} // [!code ++]
			}
			else // [!code --]
			void ChangeFloor(string id) // [!code ++]
			{
				pos.cell._floor = (byte)row.id; // [!code --]
				pos.cell._floorMat = (byte)pot.owner.DyeMat.id; // [!code --]
				SourceFloor.Row row = EClass.sources.floors.alias[id]; // [!code ++]
				if (p.HasBridge) // [!code ++]
				{ // [!code ++]
					p.cell._bridge = (byte)row.id; // [!code ++]
					p.cell._bridgeMat = (byte)pot.owner.DyeMat.id; // [!code ++]
				} // [!code ++]
				else // [!code ++]
				{ // [!code ++]
					p.cell._floor = (byte)row.id; // [!code ++]
					p.cell._floorMat = (byte)pot.owner.DyeMat.id; // [!code ++]
				} // [!code ++]
				EClass._map.SetLiquid(p.x, p.z); // [!code ++]
				p.RefreshNeighborTiles(); // [!code ++]
			}
			EClass._map.SetLiquid(pos.x, pos.z); // [!code --]
			pos.RefreshNeighborTiles(); // [!code --]
		}
	}

	public override HitResult GetHitResult()
	{
		if (pos.HasBridge || pos.HasObj || pos.cell.HasFullBlock) // [!code --]
		if (pos.HasBridge || pos.HasObj || pos.cell.HasFullBlock || (!pos.cell.sourceSurface.tag.Contains("soil") && !pos.cell.IsTopWater) || pos.cell.sourceSurface.alias == "floor_water_deep") // [!code ++]
		{
			return HitResult.Invalid;
		}
```

## TaskWater

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TaskWater.cs#L76-L82)
```cs:line-numbers=76
		num = ((num <= 0) ? 1 : Mathf.Min(waterCan.owner.c_charges, 2 + num / 10));
		if (num > 1)
		{
			List<Point> list2 = EClass._map.ListPointsInCircle(p2, num); // [!code --]
			List<Point> list2 = EClass._map.ListPointsInSquare(p2, num - 1); // [!code ++]
			list2.Sort((Point a, Point b) => a.Distance(p2) - b.Distance(p2));
			foreach (Point item in list2)
			{
```

## Thing

[`public override string GetName(NameStyle style, int _num = -1)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Thing.cs#L689-L695)
```cs:line-numbers=689
				{
					Rand.UseSeed(base.uid + EClass.game.seed, delegate
					{
						sig = AliasGen.GetRandomAlias().Bracket((base.rarity == Rarity.Mythical) ? 3 : 2); // [!code --]
						sig = AliasGen.GetRandomAlias(builtin: true).Bracket((base.rarity == Rarity.Mythical) ? 3 : 2); // [!code ++]
					});
					sig = Lang.space + sig;
				}
```

[`public override void WriteNote(UINote n, Action<UINote> onWriteNote = null, IIns`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Thing.cs#L1266-L1272)
```cs:line-numbers=1266
	{
		AddText("isNoProcessIng", FontColor.Default);
	}
	if (HasElement(10)) // [!code --]
	if (trait.CanDrink(EClass.pc)) // [!code ++]
	{ // [!code ++]
		AddText("isDrinkable", FontColor.Default); // [!code ++]
	} // [!code ++]
	else if (trait.CanEat(EClass.pc)) // [!code ++]
	{
		AddText("isEdible", FontColor.Default);
	}
```

## Trait

[`public virtual void OnListInteraction(InvOwner.ListInteraction list, ButtonGrid`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Trait.cs#L789-L795)
```cs:line-numbers=789
				});
			}, "remove");
		}
		if (CanEat(EClass.pc)) // [!code --]
		else if (CanEat(EClass.pc)) // [!code ++]
		{
			list.Add("invFood", 140, delegate
			{
```

[`public Thing CreateStock()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Trait.cs#L2544-L2555)
```cs:line-numbers=2544
			case ShopType.Milk:
				if (EClass._zone is Zone_Nefu && EClass.rnd(2) == 0)
				{
					Thing thing4 = ThingGen.Create("milk"); // [!code --]
					Thing thing4 = ThingGen.Create("_milk"); // [!code ++]
					thing4.MakeRefFrom(EClass.sources.charas.rows.Where((SourceChara.Row r) => r.race == "mifu" || r.race == "nefu").RandomItem().model);
					Debug.Log(thing4);
					return thing4;
				}
				return Create("milk"); // [!code --]
				return Create("_milk"); // [!code ++]
			case ShopType.Map:
				return ThingGen.CreateMap();
			case ShopType.Plan:
```

## TraitBrewery

[`public override bool OnChildDecay(Card c, bool firstDecay)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TraitBrewery.cs#L49-L55)
```cs:line-numbers=49
		{
			return true;
		}
		if (!c.IsFood && !(c.id == "508") && !(c.id == "778") && !(c.id == "milk")) // [!code --]
		if (!c.IsFood && !(c.id == "508") && !(c.id == "778") && !(c.id == "_milk")) // [!code ++]
		{
			return true;
		}
```

## TraitDrink

[`public override void OnDrink(Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TraitDrink.cs#L180-L186)
```cs:line-numbers=180
			refThing = owner.Thing,
			act = ((source != null && source.id != 0) ? ACT.Create(source) : null)
		});
		FoodEffect.ProcTrait(c, owner); // [!code --]
	}

	public override void OnThrowGround(Chara c, Point p)
```

## TraitDrinkMilkMother

[``](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TraitDrinkMilkMother.cs#L3-L9)
```cs:line-numbers=3

public class TraitDrinkMilkMother : TraitDrinkMilk
{
	public override bool HoldAsDefaultInteraction => true; // [!code --]
	public override bool HoldAsDefaultInteraction => owner.id == "_milk"; // [!code ++]

	public override Emo2 GetHeldEmo(Chara c)
	{
```

## TraitLumiestCrimAle

[`public override bool CanDrink(Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/TraitLumiestCrimAle.cs#L10-L15)
```cs:line-numbers=10
	public override void OnDrink(Chara c)
	{
		ActEffect.Proc(EffectId.Booze, 500, owner.blessedState, c);
		FoodEffect.ProcTrait(c, owner); // [!code --]
	}
}
```

## Zone

[`public Zone FindOrCreateLevel(int destLv, string subId = "")`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Zone.cs#L545-L550)
```cs:line-numbers=545
		return zone2;
	}

	public static (string zoneType, string zoneId, int zoneLv) ParseZoneFullName(string zoneFullName) // [!code ++]
	{ // [!code ++]
		string item = ""; // [!code ++]
		string item2 = ""; // [!code ++]
		int item3 = 0; // [!code ++]
		zoneFullName = zoneFullName.Replace('/', '@'); // [!code ++]
		if (EClass.game.spatials.Find((Zone z) => z.ZoneFullName == zoneFullName) != null) // [!code ++]
		{ // [!code ++]
			return (zoneType: item, zoneId: item2, zoneLv: item3); // [!code ++]
		} // [!code ++]
		int num = zoneFullName.LastIndexOf('@'); // [!code ++]
		if (num > 0 && num < zoneFullName.Length - 1) // [!code ++]
		{ // [!code ++]
			item = zoneFullName[..num]; // [!code ++]
			item3 = zoneFullName[(num + 1)..].ToInt(); // [!code ++]
		} // [!code ++]
		else // [!code ++]
		{ // [!code ++]
			item = zoneFullName.Replace("@", ""); // [!code ++]
		} // [!code ++]
		item2 = item.Replace("Zone_", ""); // [!code ++]
		item = "Zone_" + item2; // [!code ++]
		return (zoneType: item, zoneId: item2, zoneLv: item3); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public int Evalue(int ele)
	{
		return elements.Value(ele);
```

[`public Chara SpawnMob(Point pos = null, SpawnSetting setting = null)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/20158c55ab0f5aaf0f06b16ef3b9a0d74422232f/Elin/Zone.cs#L2894-L2913)
```cs:line-numbers=2894
		biome = ((EClass.rnd(4) != 0) ? EClass.core.refs.biomes.Water : EClass.core.refs.biomes.Sand);
	}
	SpawnList spawnList = null;
	spawnList = ((setting.idSpawnList != null) ? SpawnList.Get(setting.idSpawnList) : ((EClass._zone is Zone_DungeonYeek && EClass.rnd(5) != 0) ? SpawnListChara.Get("dungeon_yeek", (SourceChara.Row r) => r.race == "yeek" && r.quality == 0) : ((EClass._zone is Zone_DungeonDragon && EClass.rnd(5) != 0) ? SpawnListChara.Get("dungeon_dragon", (SourceChara.Row r) => (r.race == "dragon" || r.race == "drake" || r.race == "wyvern" || r.race == "lizardman" || r.race == "dinosaur") && r.quality == 0) : ((EClass._zone is Zone_DungeonMino && EClass.rnd(5) != 0) ? SpawnListChara.Get("dungeon_mino", (SourceChara.Row r) => r.race == "minotaur" && r.quality == 0) : ((setting.hostility == SpawnHostility.Neutral || (setting.hostility != SpawnHostility.Enemy && Rand.Range(0f, 1f) < ChanceSpawnNeutral)) ? SpawnList.Get(IsInstance ? "c_neutral_war" : "c_neutral") : ((biome.spawn.chara.Count <= 0) ? SpawnList.Get(biome.name, "chara", new CharaFilter // [!code --]
	if (setting.idSpawnList != null) // [!code ++]
	{ // [!code ++]
		spawnList = SpawnList.Get(setting.idSpawnList); // [!code ++]
	} // [!code ++]
	else if (EClass._zone is Zone_DungeonYeek && EClass.rnd(5) != 0) // [!code ++]
	{ // [!code ++]
		spawnList = SpawnListChara.Get("dungeon_yeek", (SourceChara.Row r) => r.race == "yeek" && r.quality == 0); // [!code ++]
	} // [!code ++]
	else if (EClass._zone is Zone_DungeonDragon && EClass.rnd(5) != 0) // [!code ++]
	{ // [!code ++]
		spawnList = SpawnListChara.Get("dungeon_dragon", (SourceChara.Row r) => (r.race == "dragon" || r.race == "drake" || r.race == "wyvern" || r.race == "lizardman" || r.race == "dinosaur") && r.quality == 0); // [!code ++]
	} // [!code ++]
	else if (EClass._zone is Zone_DungeonMino && EClass.rnd(5) != 0) // [!code ++]
	{
		ShouldPass = delegate(SourceChara.Row s) // [!code --]
		spawnList = SpawnListChara.Get("dungeon_mino", (SourceChara.Row r) => r.race == "minotaur" && r.quality == 0); // [!code ++]
	} // [!code ++]
	else if (setting.hostility == SpawnHostility.Neutral || (setting.hostility != SpawnHostility.Enemy && Rand.Range(0f, 1f) < ChanceSpawnNeutral)) // [!code ++]
	{ // [!code ++]
		spawnList = SpawnList.Get(IsInstance ? "c_neutral_war" : "c_neutral"); // [!code ++]
	} // [!code ++]
	else if (biome.spawn.chara.Count > 0) // [!code ++]
	{ // [!code ++]
		string randomCharaId = biome.spawn.GetRandomCharaId(); // [!code ++]
		spawnList = ((!IsInstance) ? SpawnList.Get(randomCharaId) : SpawnList.Get("instance_" + randomCharaId, randomCharaId, new CharaFilter // [!code ++]
		{ // [!code ++]
			ShouldPass = (SourceChara.Row s) => (!(s.hostility != "") || (!s.tag.Contains("cat") && !s.race_row.tag.Contains("cat"))) ? true : false // [!code ++]
		})); // [!code ++]
	} // [!code ++]
	else // [!code ++]
	{ // [!code ++]
		spawnList = SpawnList.Get(biome.name, "chara", new CharaFilter // [!code ++]
		{
			if (s.hostility != "") // [!code --]
			ShouldPass = delegate(SourceChara.Row s) // [!code ++]
			{
				return false; // [!code --]
				if (s.hostility != "") // [!code ++]
				{ // [!code ++]
					return false; // [!code ++]
				} // [!code ++]
				return s.biome == biome.name || s.biome.IsEmpty(); // [!code ++]
			}
			return s.biome == biome.name || s.biome.IsEmpty(); // [!code --]
		} // [!code --]
	}) : SpawnList.Get(biome.spawn.GetRandomCharaId(), "chara", new CharaFilter // [!code --]
	{ // [!code --]
		ShouldPass = (SourceChara.Row s) => (!IsInstance || !(s.hostility != "") || !s.tag.Contains("cat")) ? true : false // [!code --]
	}))))))); // [!code --]
		}); // [!code ++]
	} // [!code ++]
	int num = ((setting.dangerLv == -1) ? DangerLv : setting.dangerLv);
	CardBlueprint cardBlueprint = new CardBlueprint
	{
```
