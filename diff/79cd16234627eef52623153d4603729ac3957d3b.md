---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 9 files modified.
version: EA 23.307 Stable
changes: ActTelekinesis/Cell/Chara/GameUpdater/GoalCombat/InvOwnerGene/Map/TaskBuild/TraitGeneMachine
---

# EA 23.307 Stable

May 18, 2026

9 files modified.

## Important Changes

**None.**
## ActTelekinesis

[`public override bool CanPerform()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/79cd16234627eef52623153d4603729ac3957d3b/Elin/ActTelekinesis.cs#L50-L56)
```cs:line-numbers=50
	{
		return false;
	}
	if (!Act.CC.IsPC && Act.CC.Dist(Act.TC) != 1) // [!code --]
	if (!Act.CC.IsPC && (Act.CC.Dist(Act.TC) != 1 || Act.TC.isThing)) // [!code ++]
	{
		return false;
	}
```

## Cell

[`public void Refresh()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/79cd16234627eef52623153d4603729ac3957d3b/Elin/Cell.cs#L1428-L1434)
```cs:line-numbers=1428
	}
	bool IsAutoTileEdge(Cell cell)
	{
		if ((!sourceFloor.isBeach || !cell.sourceFloor.isBeach) && (cell._floor != _floor || cell._floorMat != _floorMat)) // [!code --]
		if (!sourceFloor.isBeach && !cell.sourceFloor.isBeach && (cell._floor != _floor || cell._floorMat != _floorMat)) // [!code ++]
		{
			return true;
		}
```

[`bool IsBridgeAutoTileEdge(Cell cell)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/79cd16234627eef52623153d4603729ac3957d3b/Elin/Cell.cs#L1448-L1454)
```cs:line-numbers=1448
	}
	bool IsDecoAutoTileEdge(Cell cell)
	{
		if (cell._deco != _deco) // [!code --]
		if (cell._deco != _deco || cell._decoMat != _decoMat) // [!code ++]
		{
			return true;
		}
```

## Chara

[`public override void Tick()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/79cd16234627eef52623153d4603729ac3957d3b/Elin/Chara.cs#L4268-L4274)
```cs:line-numbers=4268
			hygiene.Mod(2);
		}
	}
	if (cell.sourceFloor.id == 20) // [!code --]
	if (cell.sourceSurface.id == 20) // [!code ++]
	{
		AddCondition<ConBurning>();
	}
```

## GameUpdater

[`public override void FixedUpdate()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/79cd16234627eef52623153d4603729ac3957d3b/Elin/GameUpdater.cs#L160-L165)
```cs:line-numbers=160
					EClass._map.SetEffect(pos.x, pos.z);
				}
			}
			if (cell.sourceSurface.id == 20 && EClass.rnd(10) == 0) // [!code ++]
			{ // [!code ++]
				EClass._map.TryShatter(pos, 910, 100); // [!code ++]
			} // [!code ++]
			x++;
			if (x >= base.Size)
			{
```

## GoalCombat

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/79cd16234627eef52623153d4603729ac3957d3b/Elin/GoalCombat.cs#L149-L160)
```cs:line-numbers=149
			}
			owner.calmCheckTurn--;
		}
		string text = owner.id; // [!code --]
		if ((text == "big_daddy" || text == "big_daddy2") && !owner.IsPCFactionOrMinion) // [!code --]
		if (owner.trait is TraitBigDaddy && !owner.IsPCFactionOrMinion) // [!code ++]
		{
			foreach (Chara chara in EClass._map.charas)
			{
				if (!chara.IsPCFaction && !chara.IsInCombat && chara != owner) // [!code --]
				if (!chara.IsPCFactionOrMinion && !chara.IsInCombat && chara != owner && chara.trait is TraitBigDaddy) // [!code ++]
				{
					chara.GoHostile(tc);
				}
```

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/79cd16234627eef52623153d4603729ac3957d3b/Elin/GoalCombat.cs#L331-L338)
```cs:line-numbers=331
				yield return Success();
			}
			idleCount = 0;
			text = owner.source.aiIdle; // [!code --]
			if (text == "stand" || text == "root") // [!code --]
			string aiIdle = owner.source.aiIdle; // [!code ++]
			if (aiIdle == "stand" || aiIdle == "root") // [!code ++]
			{
				yield return Success();
			}
```

## InvOwnerGene

[`public override bool ShouldShowGuide(Thing t)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/79cd16234627eef52623153d4603729ac3957d3b/Elin/InvOwnerGene.cs#L25-L31)
```cs:line-numbers=25
public override void _OnProcess(Thing t)
{
	DNA.Type type = t.c_DNA.type;
	if (type != 0 && tg.c_genes != null) // [!code --]
	if (type != 0) // [!code ++]
	{
		int num = t.c_DNA.slot;
		if (num > 1 && tg.HasElement(1237))
```

## Map

[`public void TryShatter(Point pos, int ele, int power)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/79cd16234627eef52623153d4603729ac3957d3b/Elin/Map.cs#L1289-L1294)
```cs:line-numbers=1289
	}
	list.Shuffle();
	int num = 0;
	bool flag = false; // [!code ++]
	foreach (Card item3 in list)
	{
		if (!item3.trait.CanBeDestroyed || item3.category.IsChildOf("currency") || item3.trait is TraitDoor || item3.trait is TraitFigure || item3.trait is TraitTrainingDummy || item3.rarity >= Rarity.Legendary)
```

[`public void TryShatter(Point pos, int ele, int power)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/79cd16234627eef52623153d4603729ac3957d3b/Elin/Map.cs#L1319-L1327)
```cs:line-numbers=1319
		{
			continue;
		}
		bool flag = CanCook(item3); // [!code --]
		bool flag2 = CanCook(item3); // [!code ++]
		string text = "";
		if (flag) // [!code --]
		if (flag2) // [!code ++]
		{
			if (fire)
			{
```

[`public void TryShatter(Point pos, int ele, int power)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/79cd16234627eef52623153d4603729ac3957d3b/Elin/Map.cs#L1358-L1366)
```cs:line-numbers=1358
				text = "711";
			}
		}
		if (flag && !text.IsEmpty()) // [!code --]
		if (flag2 && !text.IsEmpty()) // [!code ++]
		{
			item3.GetRoot(); // [!code --]
			Thing thing2 = item3.Split(1);
			List<Thing> list4 = new List<Thing>();
			list4.Add(thing2);
```

[`public void TryShatter(Point pos, int ele, int power)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/79cd16234627eef52623153d4603729ac3957d3b/Elin/Map.cs#L1414-L1419)
```cs:line-numbers=1414
		{
			ActionMode.Adv.itemLost++;
		}
		if (!flag) // [!code ++]
		{ // [!code ++]
			pos.PlayEffect(fire ? "Element/eleFire" : "Element/eleCold"); // [!code ++]
			flag = true; // [!code ++]
		} // [!code ++]
		num++;
	}
	_ValidateInstalled(pos.x, pos.z);
```

## TaskBuild

[`public HitResult _GetHitResult(Point p)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/79cd16234627eef52623153d4603729ac3957d3b/Elin/TaskBuild.cs#L259-L265)
```cs:line-numbers=259
		}
		if (p.Installed != null)
		{
			if (p.Installed.trait is TraitSeed && !(EClass.pc.held.trait is TraitFertilizer)) // [!code --]
			if (p.Installed.trait is TraitSeed && !(EClass.pc.held.trait is TraitFertilizer) && !(EClass.pc.held.trait is TraitFloor) && !(EClass.pc.held.trait is TraitFloorDeco)) // [!code ++]
			{
				return HitResult.Invalid;
			}
```

## TraitGeneMachine

[`public override bool OnUse(Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/79cd16234627eef52623153d4603729ac3957d3b/Elin/TraitGeneMachine.cs#L138-L143)
```cs:line-numbers=138
		{
			LayerDragGrid.Create(new InvOwnerGene(owner, target));
		}
		return true; // [!code --]
		return false; // [!code ++]
	}
}
```
