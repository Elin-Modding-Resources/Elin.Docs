---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 17 files modified. 1 new file created.
version: EA 23.332 Nightly Patch 1
changes: AI_Idle/ActEffect/Card/Chara/ContentConfigTest/CoreConfig/DuplicateCondition/ELEMENT/EffectMeteor/FactionBranch/GameDate/GoalCombat/GrowSystem/Religion/Trait/TraitLightSun/+TraitSpotGuest/Zone
---

# EA 23.332 Nightly Patch 1

August 1, 2026

17 files modified. 1 new file created.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [Chara (1)](#chara)
```cs:no-line-numbers
public Chara TryDuplicate(DuplicateCondition con = DuplicateCondition.Default, Point dest = null) // [!code --]
public Chara TryDuplicate(DuplicateCondition con = DuplicateCondition.Default, Point dest = null, bool inheritHostility = false) // [!code ++]
```
### [Trait (1)](#trait)
```cs:no-line-numbers
public virtual List<Point> ListPoints(Point center = null, bool onlyPassable = true) // [!code --]
public virtual List<Point> ListPoints(Point center = null, bool onlyPassable = true, bool allowChara = true) // [!code ++]
```
### [TraitLightSun (1)](#traitlightsun)
```cs:no-line-numbers
public override List<Point> ListPoints(Point center = null, bool onlyPassable = true) // [!code --]
public override List<Point> ListPoints(Point center = null, bool onlyPassable = true, bool allowChara = true) // [!code ++]
```
## AI_Idle

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/AI_Idle.cs#L566-L571)
```cs:line-numbers=566
			}
			break;
		}
		case "imotoroid": // [!code ++]
		case "marshmallow_monster":
			if (owner.TryFuse(0.001f))
			{
```

## ActEffect

[`public static bool DamageEle(Card CC, EffectId id, int power, Element e, List<Po`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L266-L274)
```cs:line-numbers=266
					num4 /= 5;
					break;
				}
				if (id == EffectId.Ball || id == EffectId.BallBubble || id == EffectId.Explosive || id == EffectId.Rocket || id == EffectId.GravityGun) // [!code --]
				int num5 = 0; // [!code ++]
				switch (id) // [!code ++]
				{ // [!code ++]
				case EffectId.Earthquake: // [!code ++]
				case EffectId.Comet: // [!code ++]
					num5 = 5; // [!code ++]
					break; // [!code ++]
				case EffectId.Ball: // [!code ++]
				case EffectId.Explosive: // [!code ++]
				case EffectId.BallBubble: // [!code ++]
				case EffectId.Rocket: // [!code ++]
				case EffectId.GravityGun: // [!code ++]
					num5 = 10; // [!code ++]
					break; // [!code ++]
				} // [!code ++]
				if (num5 != 0) // [!code ++]
				{
					num4 = num4 * 100 / (90 + center.Distance(p) * 10); // [!code --]
					num4 = num4 * 100 / (90 + center.Distance(p) * num5); // [!code ++]
				}
			}
			if (id == EffectId.Sword)
```

[`public static bool DamageEle(Card CC, EffectId id, int power, Element e, List<Po`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L282-L299)
```cs:line-numbers=282
			}
			if (isChara && points.Count > 1 && c != null && c.isChara && CC.isChara && CC.Chara.IsFriendOrAbove(c.Chara))
			{
				int num5 = CC.Evalue(302); // [!code --]
				int num6 = CC.Evalue(302); // [!code ++]
				if (!CC.IsPC && CC.IsPCFactionOrMinion)
				{
					num5 += EClass.pc.Evalue(302); // [!code --]
					num6 += EClass.pc.Evalue(302); // [!code ++]
				}
				if (CC.HasElement(1214))
				{
					num5 *= 2; // [!code --]
					num6 *= 2; // [!code ++]
				}
				if (num5 > 0) // [!code --]
				if (num6 > 0) // [!code ++]
				{
					if (num5 * 10 > EClass.rnd(num4 + 1)) // [!code --]
					if (num6 * 10 > EClass.rnd(num4 + 1)) // [!code ++]
					{
						if (c == c.pos.FirstChara)
						{
```

[`public static bool DamageEle(Card CC, EffectId id, int power, Element e, List<Po`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L301-L307)
```cs:line-numbers=301
						}
						continue;
					}
					num4 = EClass.rnd(num4 * 100 / (100 + num5 * 10 + 1)); // [!code --]
					num4 = EClass.rnd(num4 * 100 / (100 + num6 * 10 + 1)); // [!code ++]
					if (c == c.pos.FirstChara)
					{
						CC.ModExp(302, CC.IsPC ? 20 : 100);
```

[`public static bool DamageEle(Card CC, EffectId id, int power, Element e, List<Po`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L381-L394)
```cs:line-numbers=381
				case EffectId.DrainMana:
					if (CC.IsAliveInCurrentZone && c.isChara && CC.isChara && c.Chara.mana.value > 0)
					{
						long num6 = num4 * num / 100; // [!code --]
						Debug.Log(num4 + " v:" + num6 + " evalue:" + e.Value + " power:" + power + " elepMod:" + num); // [!code --]
						if (num6 > c.Chara.mana.value) // [!code --]
						long num7 = num4 * num / 100; // [!code ++]
						Debug.Log(num4 + " v:" + num7 + " evalue:" + e.Value + " power:" + power + " elepMod:" + num); // [!code ++]
						if (num7 > c.Chara.mana.value) // [!code ++]
						{
							num6 = c.Chara.mana.value; // [!code --]
							num7 = c.Chara.mana.value; // [!code ++]
						}
						c.Chara.mana.Mod((int)(-num6)); // [!code --]
						CC.Chara.mana.Mod((int)num6); // [!code --]
						c.Chara.mana.Mod((int)(-num7)); // [!code ++]
						CC.Chara.mana.Mod((int)num7); // [!code ++]
					}
					break;
				}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L412-L434)
```cs:line-numbers=412
		}
		if ((id == EffectId.Explosive || id == EffectId.Suicide || id == EffectId.Rocket) && (!EClass._zone.IsPCFaction || !EClass.Branch.HasItemProtection))
		{
			int num7; // [!code --]
			int num8; // [!code ++]
			switch (id)
			{
			default:
				num7 = ((actref.refThing != null) ? actref.refThing.material.hardness : (30 + power / 20)); // [!code --]
				num8 = ((actref.refThing != null) ? actref.refThing.material.hardness : (30 + power / 20)); // [!code ++]
				break;
			case EffectId.Suicide:
				num7 = CC.LV / 3 + 40; // [!code --]
				num8 = CC.LV / 3 + 40; // [!code ++]
				break;
			case EffectId.Meteor:
			case EffectId.Comet:
				num7 = 50 + power / 20; // [!code --]
				num8 = 50 + power / 20; // [!code ++]
				break;
			}
			int num8 = num7; // [!code --]
			int num9 = num8; // [!code ++]
			bool flag3 = EClass._zone.HasLaw && !EClass._zone.IsPCFaction && (CC.IsPC || (id == EffectId.Explosive && actref.refThing == null)) && !(EClass._zone is Zone_Vernis);
			if (p.HasObj && p.cell.matObj.hardness <= num8) // [!code --]
			if (p.HasObj && p.cell.matObj.hardness <= num9) // [!code ++]
			{
				EClass._map.MineObj(p);
				if (flag3)
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L436-L442)
```cs:line-numbers=436
					EClass.player.ModKarma(-1);
				}
			}
			if (!p.HasObj && p.HasBlock && p.matBlock.hardness <= num8) // [!code --]
			if (!p.HasObj && p.HasBlock && p.matBlock.hardness <= num9) // [!code ++]
			{
				EClass._map.MineBlock(p);
				if (flag3)
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L447-L462)
```cs:line-numbers=447
		}
		if (e.id == 910)
		{
			int num9 = 0; // [!code --]
			int num10 = 0; // [!code ++]
			if (id == EffectId.Meteor)
			{
				num9 = 2; // [!code --]
				num10 = 2; // [!code ++]
			}
			if (EClass._zone.IsPCFaction && EClass._zone.branch.HasItemProtection)
			{
				num9 = 0; // [!code --]
				num10 = 0; // [!code ++]
			}
			if (num9 > EClass.rnd(10)) // [!code --]
			if (num10 > EClass.rnd(10)) // [!code ++]
			{
				p.ModFire(4 + EClass.rnd(10));
			}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L500-L521)
```cs:line-numbers=500
	{
	case EffectId.Earthquake:
	{
		List<Point> list6 = EClass._map.ListPointsInCircle(CC.pos, 12f, mustBeWalkable: false); // [!code --]
		if (list6.Count == 0) // [!code --]
		List<Point> list = EClass._map.ListPointsInCircle(CC.pos, 12f, mustBeWalkable: false); // [!code ++]
		if (list.Count == 0) // [!code ++]
		{
			list6.Add(CC.pos.Copy()); // [!code --]
			list.Add(CC.pos.Copy()); // [!code ++]
		}
		CC.Say("spell_earthquake", CC, element.Name.ToLower());
		TryDelay(delegate
		{
			CC.PlaySound("spell_earthquake");
		});
		if (CC.IsInMutterDistance()) // [!code --]
		if (CC.IsInMutterDistance() && !EClass.core.config.test.disableShake2) // [!code ++]
		{
			Shaker.ShakeCam("ball");
		}
		EClass.Wait(1f, CC);
		DamageEle(CC, id, power, element, list6, actRef, "spell_earthquake"); // [!code --]
		DamageEle(CC, id, power, element, list, actRef, "spell_earthquake"); // [!code ++]
		break;
	}
	case EffectId.Meteor:
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L523-L544)
```cs:line-numbers=523
		EffectMeteor.Create(cc.pos, 10, 10, delegate
		{
		});
		List<Point> list = EClass._map.ListPointsInCircle(CC.pos, 10f); // [!code --]
		if (list.Count == 0) // [!code --]
		List<Point> list5 = EClass._map.ListPointsInCircle(CC.pos, 10f); // [!code ++]
		if (list5.Count == 0) // [!code ++]
		{
			list.Add(CC.pos.Copy()); // [!code --]
			list5.Add(CC.pos.Copy()); // [!code ++]
		}
		CC.Say("spell_ball", CC, element.Name.ToLower());
		TryDelay(delegate
		{
			CC.PlaySound("spell_ball");
		});
		if (CC.IsInMutterDistance()) // [!code --]
		if (CC.IsInMutterDistance() && !EClass.core.config.test.disableShake2) // [!code ++]
		{
			Shaker.ShakeCam("ball");
		}
		EClass.Wait(1f, CC);
		DamageEle(CC, id, power, element, list, actRef, "spell_ball"); // [!code --]
		DamageEle(CC, id, power, element, list5, actRef, "spell_ball"); // [!code ++]
		return;
	}
	case EffectId.Comet:
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L546-L567)
```cs:line-numbers=546
		EffectMeteor.CreateComet(tp, delegate
		{
		}, matColor * 2f);
		List<Point> list7 = EClass._map.ListPointsInCircle(tp, 7f); // [!code --]
		if (list7.Count == 0) // [!code --]
		List<Point> list6 = EClass._map.ListPointsInCircle(tp, 7f); // [!code ++]
		if (list6.Count == 0) // [!code ++]
		{
			list7.Add(tp.Copy()); // [!code --]
			list6.Add(tp.Copy()); // [!code ++]
		}
		CC.Say("spell_ball", CC, element.Name.ToLower());
		TryDelay(delegate
		{
			CC.PlaySound("spell_ball");
		});
		if (CC.IsInMutterDistance()) // [!code --]
		if (CC.IsInMutterDistance() && !EClass.core.config.test.disableShake2) // [!code ++]
		{
			Shaker.ShakeCam("ball");
		}
		EClass.Wait(1f, CC);
		DamageEle(CC, id, power, element, list7, actRef, "spell_ball", tp); // [!code --]
		DamageEle(CC, id, power, element, list6, actRef, "spell_ball", tp); // [!code ++]
		return;
	}
	case EffectId.Hand:
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L569-L582)
```cs:line-numbers=569
	case EffectId.DrainMana:
	case EffectId.Sword:
	{
		List<Point> list2 = new List<Point>(); // [!code --]
		list2.Add(tp.Copy()); // [!code --]
		List<Point> list7 = new List<Point>(); // [!code ++]
		list7.Add(tp.Copy()); // [!code ++]
		EClass.Wait(0.3f, CC);
		TryDelay(delegate
		{
			CC.PlaySound("spell_hand");
		});
		if (!DamageEle(CC, id, power, element, list2, actRef, (id == EffectId.DrainBlood || id == EffectId.DrainMana) ? "" : ((id == EffectId.Sword) ? "spell_sword" : "spell_hand"))) // [!code --]
		if (!DamageEle(CC, id, power, element, list7, actRef, (id == EffectId.DrainBlood || id == EffectId.DrainMana) ? "" : ((id == EffectId.Sword) ? "spell_sword" : "spell_hand"))) // [!code ++]
		{
			CC.Say("spell_hand_miss", CC, element.Name.ToLower());
		}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L640-L648)
```cs:line-numbers=640
		string id3 = actRef.n1;
		int num3 = 1;
		int num4 = -1;
		int radius = 3; // [!code --]
		bool flag3 = false; // [!code --]
		bool flag4 = actRef.n1 == "special"; // [!code --]
		int radius2 = 3; // [!code ++]
		bool flag4 = false; // [!code ++]
		bool flag5 = actRef.n1 == "special"; // [!code ++]
		int num5 = -1;
		string text = "";
		switch (actRef.n1)
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L663-L669)
```cs:line-numbers=663
			break;
		case "tentacle":
			num4 = 20 + EClass.rnd(10);
			radius = 1; // [!code --]
			radius2 = 1; // [!code ++]
			break;
		case "special":
			CC.SetInt(70, EClass.world.date.GetRaw() + 1440);
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L683-L694)
```cs:line-numbers=683
				break;
			}
			Point point = null;
			point = ((!(actRef.n1 == "special2")) ? tp.GetRandomPoint(radius)?.GetNearestPoint(allowBlock: false, allowChara: false) : EClass._map.GetRandomSurface(centered: false, walkable: true, allowWater: true)?.GetNearestPoint(allowBlock: false, allowChara: false)); // [!code --]
			point = ((!(actRef.n1 == "special2")) ? tp.GetRandomPoint(radius2)?.GetNearestPoint(allowBlock: false, allowChara: false) : EClass._map.GetRandomSurface(centered: false, walkable: true, allowWater: true)?.GetNearestPoint(allowBlock: false, allowChara: false)); // [!code ++]
			if (point == null || !point.IsValid)
			{
				continue;
			}
			Chara chara = null; // [!code --]
			Chara chara2 = null; // [!code ++]
			CardBlueprint.Set(new CardBlueprint());
			if (num5 != -1)
			{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L703-L762)
```cs:line-numbers=703
			case "special":
				if (j == 0 && !CC.HasMinion("imolonac") && !CC.HasMinion("ygolonac"))
				{
					chara = CharaGen.Create((EClass.rnd(20) == 0) ? "imolonac" : "ygolonac"); // [!code --]
					chara2 = CharaGen.Create((EClass.rnd(20) == 0) ? "imolonac" : "ygolonac"); // [!code ++]
					break;
				}
				chara = CharaGen.Create("hound", CC.LV); // [!code --]
				chara2 = CharaGen.Create("hound", CC.LV); // [!code ++]
				if (text.IsEmpty())
				{
					text = chara.MainElement.source.alias; // [!code --]
					text = chara2.MainElement.source.alias; // [!code ++]
				}
				break;
			case "special2":
				chara = CharaGen.Create("cocoon"); // [!code --]
				chara2 = CharaGen.Create("cocoon"); // [!code ++]
				break;
			case "yeek":
				chara = CharaGen.CreateFromFilter(SpawnListChara.Get("summon_yeek", (SourceChara.Row r) => r.race == "yeek"), power / 10); // [!code --]
				chara2 = CharaGen.CreateFromFilter(SpawnListChara.Get("summon_yeek", (SourceChara.Row r) => r.race == "yeek"), power / 10); // [!code ++]
				break;
			case "orc":
				chara = CharaGen.CreateFromFilter(SpawnListChara.Get("summon_orc", (SourceChara.Row r) => r.race == "orc"), power / 10); // [!code --]
				chara2 = CharaGen.CreateFromFilter(SpawnListChara.Get("summon_orc", (SourceChara.Row r) => r.race == "orc"), power / 10); // [!code ++]
				break;
			case "dragon":
				chara = CharaGen.CreateFromFilter(SpawnListChara.Get("summon_dragon", (SourceChara.Row r) => r.race == "dragon" || r.race == "drake" || r.race == "wyvern"), power / 5); // [!code --]
				chara2 = CharaGen.CreateFromFilter(SpawnListChara.Get("summon_dragon", (SourceChara.Row r) => r.race == "dragon" || r.race == "drake" || r.race == "wyvern"), power / 5); // [!code ++]
				break;
			case "undead":
				chara = CharaGen.CreateFromFilter(SpawnListChara.Get("summon_undead", (SourceChara.Row r) => r.HasTag(CTAG.undead) || (EClass.sources.races.map.TryGetValue(r.race)?.IsUndead ?? false)), power / 5); // [!code --]
				chara2 = CharaGen.CreateFromFilter(SpawnListChara.Get("summon_undead", (SourceChara.Row r) => r.HasTag(CTAG.undead) || (EClass.sources.races.map.TryGetValue(r.race)?.IsUndead ?? false)), power / 5); // [!code ++]
				break;
			case "pawn":
				chara = CharaGen.CreateFromFilter("c_pawn", power / 10); // [!code --]
				chara2 = CharaGen.CreateFromFilter("c_pawn", power / 10); // [!code ++]
				break;
			case "machine":
				chara = CharaGen.CreateFromFilter("c_machine", power / 10); // [!code --]
				chara2 = CharaGen.CreateFromFilter("c_machine", power / 10); // [!code ++]
				break;
			case "monster":
				chara = CharaGen.CreateFromFilter("c_dungeon", power / 10); // [!code --]
				chara2 = CharaGen.CreateFromFilter("c_dungeon", power / 10); // [!code ++]
				break;
			case "animal":
				chara = CharaGen.CreateFromFilter("c_animal", power / 15); // [!code --]
				chara2 = CharaGen.CreateFromFilter("c_animal", power / 15); // [!code ++]
				break;
			case "fire":
				chara = CharaGen.CreateFromElement("Fire", power / 10); // [!code --]
				chara2 = CharaGen.CreateFromElement("Fire", power / 10); // [!code ++]
				break;
			case "fish":
				chara = CharaGen.CreateFromFilter(SpawnListChara.Get("summon_fish", (SourceChara.Row r) => r.ContainsTag("water") || r.model.Chara.race.tag.Contains("water")), power / 10); // [!code --]
				chara2 = CharaGen.CreateFromFilter(SpawnListChara.Get("summon_fish", (SourceChara.Row r) => r.ContainsTag("water") || r.model.Chara.race.tag.Contains("water")), power / 10); // [!code ++]
				break;
			case "octopus":
				chara = CharaGen.CreateFromFilter(SpawnListChara.Get("summon_octopus", (SourceChara.Row r) => r.race == "octopus"), power / 10); // [!code --]
				chara2 = CharaGen.CreateFromFilter(SpawnListChara.Get("summon_octopus", (SourceChara.Row r) => r.race == "octopus"), power / 10); // [!code ++]
				break;
			default:
				chara = CharaGen.Create(id3, power / 10); // [!code --]
				chara2 = CharaGen.Create(id3, power / 10); // [!code ++]
				break;
			}
			if (chara == null) // [!code --]
			if (chara2 == null) // [!code ++]
			{
				continue;
			}
			if (chara.rarity >= Rarity.Legendary && !flag4) // [!code --]
			if (chara2.rarity >= Rarity.Legendary && !flag5) // [!code ++]
			{
				j--;
				continue;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L778-L784)
```cs:line-numbers=778
					}
					if (num5 == -1)
					{
						num6 = chara.LV * (100 + power / 10) / 100 + power / 30; // [!code --]
						num6 = chara2.LV * (100 + power / 10) / 100 + power / 30; // [!code ++]
					}
					if (num6 < num7)
					{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L794-L838)
```cs:line-numbers=794
			{
				num6 = power / 10 + 1;
			}
			if (chara.LV < num6) // [!code --]
			if (chara2.LV < num6) // [!code ++]
			{
				chara.SetLv((int)num6); // [!code --]
				chara2.SetLv((int)num6); // [!code ++]
			}
			chara.interest = 0; // [!code --]
			if (chara.HaveFur()) // [!code --]
			chara2.interest = 0; // [!code ++]
			if (chara2.HaveFur()) // [!code ++]
			{
				chara.c_fur = -1; // [!code --]
				chara2.c_fur = -1; // [!code ++]
			}
			n = actRef.n1;
			if (!(n == "shadow"))
			{
				if (n == "special_force")
				{
					chara.homeZone = EClass._zone; // [!code --]
					chara2.homeZone = EClass._zone; // [!code ++]
				}
			}
			else
			{
				chara.hp = chara.MaxHP / 2; // [!code --]
				chara2.hp = chara2.MaxHP / 2; // [!code ++]
			}
			EClass._zone.AddCard(chara, point); // [!code --]
			EClass._zone.AddCard(chara2, point); // [!code ++]
			if (flag)
			{
				Chara chara2 = chara; // [!code --]
				Hostility hostility2 = (chara.c_originalHostility = Hostility.Enemy); // [!code --]
				chara2.hostility = hostility2; // [!code --]
				Chara chara3 = chara2; // [!code ++]
				Hostility hostility2 = (chara2.c_originalHostility = Hostility.Enemy); // [!code ++]
				chara3.hostility = hostility2; // [!code ++]
			}
			else if (!(chara.id == "cocoon") && (!(actRef.n1 == "monster") || actRef.refThing == null)) // [!code --]
			else if (!(chara2.id == "cocoon") && (!(actRef.n1 == "monster") || actRef.refThing == null)) // [!code ++]
			{
				chara.MakeMinion(CC); // [!code --]
				chara2.MakeMinion(CC); // [!code ++]
			}
			if (num4 != -1)
			{
				chara.SetSummon(num4); // [!code --]
				chara2.SetSummon(num4); // [!code ++]
			}
			flag3 = true; // [!code --]
			flag4 = true; // [!code ++]
		}
		if (!flag3) // [!code --]
		if (!flag4) // [!code ++]
		{
			CC.Say("summon_ally_fail", CC);
		}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L848-L869)
```cs:line-numbers=848
		}
		CC.Say("spell_funnel", CC, element.Name.ToLower());
		CC.PlaySound("spell_funnel");
		Chara chara3 = CharaGen.Create((id == EffectId.Bit) ? "bit2" : "bit"); // [!code --]
		chara3.SetMainElement(element.source.alias, element.Value, elemental: true); // [!code --]
		chara3.SetSummon(20 + power / 20 + EClass.rnd(10)); // [!code --]
		chara3.SetLv(Mathf.Abs(power) / 15); // [!code --]
		chara3.interest = 0; // [!code --]
		EClass._zone.AddCard(chara3, tp.GetNearestPoint(allowBlock: false, allowChara: false)); // [!code --]
		chara3.PlayEffect("teleport"); // [!code --]
		chara3.MakeMinion(CC); // [!code --]
		Chara chara = CharaGen.Create((id == EffectId.Bit) ? "bit2" : "bit"); // [!code ++]
		chara.SetMainElement(element.source.alias, element.Value, elemental: true); // [!code ++]
		chara.SetSummon(20 + power / 20 + EClass.rnd(10)); // [!code ++]
		chara.SetLv(Mathf.Abs(power) / 15); // [!code ++]
		chara.interest = 0; // [!code ++]
		EClass._zone.AddCard(chara, tp.GetNearestPoint(allowBlock: false, allowChara: false)); // [!code ++]
		chara.PlayEffect("teleport"); // [!code ++]
		chara.MakeMinion(CC); // [!code ++]
		return;
	}
	case EffectId.Breathe:
	{
		List<Point> list5 = EClass._map.ListPointsInArc(CC.pos, tp, 7, 35f); // [!code --]
		if (list5.Count == 0) // [!code --]
		List<Point> list8 = EClass._map.ListPointsInArc(CC.pos, tp, 7, 35f); // [!code ++]
		if (list8.Count == 0) // [!code ++]
		{
			list5.Add(CC.pos.Copy()); // [!code --]
			list8.Add(CC.pos.Copy()); // [!code ++]
		}
		CC.Say("spell_breathe", CC, element.Name.ToLower());
		EClass.Wait(0.8f, CC);
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L871-L881)
```cs:line-numbers=871
		{
			CC.PlaySound("spell_breathe");
		});
		if (CC.IsInMutterDistance() && !EClass.core.config.graphic.disableShake) // [!code --]
		if (CC.IsInMutterDistance() && !EClass.core.config.graphic.disableShake && !EClass.core.config.test.disableShake2) // [!code ++]
		{
			Shaker.ShakeCam("breathe");
		}
		DamageEle(CC, id, power, element, list5, actRef, "spell_breathe"); // [!code --]
		DamageEle(CC, id, power, element, list8, actRef, "spell_breathe"); // [!code ++]
		return;
	}
	case EffectId.Scream:
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L902-L911)
```cs:line-numbers=902
	case EffectId.Flare:
	case EffectId.GravityGun:
	{
		float radius2 = ((id == EffectId.GravityGun) ? 4f : ((id == EffectId.Rocket) ? 2.8f : ((id == EffectId.Suicide) ? 3.5f : ((id == EffectId.Flare) ? 2.1f : ((float)((id == EffectId.BallBubble) ? 2 : 5)))))); // [!code --]
		float radius = ((id == EffectId.GravityGun) ? 4f : ((id == EffectId.Rocket) ? 2.8f : ((id == EffectId.Suicide) ? 3.5f : ((id == EffectId.Flare) ? 2.1f : ((float)((id == EffectId.BallBubble) ? 2 : 5)))))); // [!code ++]
		if ((id == EffectId.Explosive || id == EffectId.Rocket) && actRef.refThing != null)
		{
			radius2 = 2 + actRef.refThing.Evalue(666); // [!code --]
			radius = 2 + actRef.refThing.Evalue(666); // [!code ++]
		}
		if (id == EffectId.Suicide)
		{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L919-L932)
```cs:line-numbers=919
			}
			if (CC.HasTag(CTAG.kamikaze))
			{
				radius2 = 1.5f; // [!code --]
				radius = 1.5f; // [!code ++]
			}
		}
		bool flag5 = id == EffectId.Explosive || id == EffectId.Suicide || id == EffectId.Rocket; // [!code --]
		List<Point> list8 = EClass._map.ListPointsInCircle((id == EffectId.GravityGun || id == EffectId.Rocket || id == EffectId.Flare) ? tp : cc.pos, radius2, !flag5, !flag5); // [!code --]
		if (list8.Count == 0) // [!code --]
		bool flag3 = id == EffectId.Explosive || id == EffectId.Suicide || id == EffectId.Rocket; // [!code ++]
		List<Point> list2 = EClass._map.ListPointsInCircle((id == EffectId.GravityGun || id == EffectId.Rocket || id == EffectId.Flare) ? tp : cc.pos, radius, !flag3, !flag3); // [!code ++]
		if (list2.Count == 0) // [!code ++]
		{
			list8.Add(cc.pos.Copy()); // [!code --]
			list2.Add(cc.pos.Copy()); // [!code ++]
		}
		cc.Say((id == EffectId.Suicide) ? "abSuicide" : "spell_ball", cc, element.Name.ToLower());
		EClass.Wait(0.8f, cc);
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L941-L951)
```cs:line-numbers=941
				cc.PlaySound((id == EffectId.Flare) ? "spell_flare" : "spell_ball");
			}
		});
		if (id != EffectId.GravityGun && cc.IsInMutterDistance() && !EClass.core.config.graphic.disableShake) // [!code --]
		if (id != EffectId.GravityGun && cc.IsInMutterDistance() && !EClass.core.config.graphic.disableShake && !EClass.core.config.test.disableShake2) // [!code ++]
		{
			Shaker.ShakeCam("ball");
		}
		DamageEle(actRef.origin ?? cc, id, power, element, list8, actRef, (id == EffectId.Suicide) ? "suicide" : "spell_ball"); // [!code --]
		DamageEle(actRef.origin ?? cc, id, power, element, list2, actRef, (id == EffectId.Suicide) ? "suicide" : "spell_ball"); // [!code ++]
		if (id == EffectId.Suicide && CC.IsAliveInCurrentZone)
		{
			CC.Die();
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L965-L971)
```cs:line-numbers=965
		{
			CC.PlaySound("spell_bolt");
		});
		if (CC.IsInMutterDistance() && !EClass.core.config.graphic.disableShake) // [!code --]
		if (CC.IsInMutterDistance() && !EClass.core.config.graphic.disableShake && !EClass.core.config.test.disableShake2) // [!code ++]
		{
			Shaker.ShakeCam("bolt");
		}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L1234-L1242)
```cs:line-numbers=1234
		TC.PlaySound("curse3");
		TC.PlayEffect("curse");
		TC.Say("forgetItems", TC);
		int num3 = power / 50 + 1 + EClass.rnd(3); // [!code --]
		int num4 = power / 50 + 1 + EClass.rnd(3); // [!code ++]
		List<Thing> source = TC.things.List((Thing t) => t.c_IDTState == 0);
		for (int j = 0; j < num3; j++) // [!code --]
		for (int j = 0; j < num4; j++) // [!code ++]
		{
			source.RandomItem().c_IDTState = 5;
		}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L1248-L1257)
```cs:line-numbers=1248
	case EffectId.EnchantArmorGreat:
	{
		bool armor = id == EffectId.EnchantArmor || id == EffectId.EnchantArmorGreat;
		bool flag6 = id == EffectId.EnchantWeaponGreat || id == EffectId.EnchantArmorGreat; // [!code --]
		bool flag5 = id == EffectId.EnchantWeaponGreat || id == EffectId.EnchantArmorGreat; // [!code ++]
		if (!tc.isThing)
		{
			LayerDragGrid.CreateEnchant(CC, armor, flag6, state); // [!code --]
			LayerDragGrid.CreateEnchant(CC, armor, flag5, state); // [!code ++]
			return;
		}
		cc.PlaySound("identify");
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L1262-L1269)
```cs:line-numbers=1262
			tc.ModEncLv(-1);
			break;
		}
		int num4 = (flag6 ? 4 : 2) + (blessed ? 1 : 0); // [!code --]
		if (tc.encLV >= num4) // [!code --]
		int num3 = (flag5 ? 4 : 2) + (blessed ? 1 : 0); // [!code ++]
		if (tc.encLV >= num3) // [!code ++]
		{
			cc.Say("enc_resist", tc);
			break;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L1275-L1295)
```cs:line-numbers=1275
	case EffectId.Identify:
	case EffectId.GreaterIdentify:
	{
		bool flag5 = id == EffectId.GreaterIdentify; // [!code --]
		bool flag6 = id == EffectId.GreaterIdentify; // [!code ++]
		if (flag)
		{
			Redirect(EffectId.ForgetItems, flag5 ? BlessedState.Cursed : BlessedState.Normal, default(ActRef)); // [!code --]
			Redirect(EffectId.ForgetItems, flag6 ? BlessedState.Cursed : BlessedState.Normal, default(ActRef)); // [!code ++]
			break;
		}
		if (!tc.isThing)
		{
			int count = ((!blessed) ? 1 : (flag5 ? (2 + EClass.rnd(2)) : (3 + EClass.rnd(3)))); // [!code --]
			LayerDragGrid.CreateIdentify(CC, flag5, state, 0, count); // [!code --]
			int count = ((!blessed) ? 1 : (flag6 ? (2 + EClass.rnd(2)) : (3 + EClass.rnd(3)))); // [!code ++]
			LayerDragGrid.CreateIdentify(CC, flag6, state, 0, count); // [!code ++]
			return;
		}
		cc.PlaySound("identify");
		cc.PlayEffect("identify");
		tc.Thing.Identify(cc.IsPCParty, (!flag5) ? IDTSource.Identify : IDTSource.SuperiorIdentify); // [!code --]
		tc.Thing.Identify(cc.IsPCParty, (!flag6) ? IDTSource.Identify : IDTSource.SuperiorIdentify); // [!code ++]
		break;
	}
	case EffectId.Uncurse:
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L1704-L1714)
```cs:line-numbers=1704
				}
			}
		}
		if (tc.ExistsOnMap && tc.c_bossType == BossType.none && tc.id == "mush_quantum" && !tc.HasElement(400) && !tc.HasCondition<ConGravity>()) // [!code --]
		if (tc.ExistsOnMap && tc.id == "mush_quantum" && !tc.HasElement(400) && !tc.HasCondition<ConGravity>()) // [!code ++]
		{
			if (EClass.rnd(2) == 0)
			{
				if (tc.IsGlobal || EClass._zone.IsInstance) // [!code --]
				if (tc.IsGlobal || EClass._zone.IsInstance || tc.c_bossType != 0) // [!code ++]
				{
					tc.Die();
				}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ActEffect.cs#L1718-L1724)
```cs:line-numbers=1718
				}
				break;
			}
			tc.Chara.TryDuplicate(); // [!code --]
			tc.Chara.TryDuplicate(DuplicateCondition.Quantum, null, inheritHostility: true); // [!code ++]
		}
		if (flag)
		{
```

## Card

[`public void SpawnLoot(Card origin)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/Card.cs#L5444-L5449)
```cs:line-numbers=5444
			{
				thing3.ChangeMaterial(Chara.material);
			}
			if (thing3.HasElement(758)) // [!code ++]
			{ // [!code ++]
				thing3.SetNum(1); // [!code ++]
			} // [!code ++]
			list.Add(thing3);
			break;
		}
```

## Chara

[`public bool TryFuse(float chance = 1f)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/Chara.cs#L1839-L1845)
```cs:line-numbers=1839
		return false;
	}
	Say("fuse", this);
	Chara chara = EClass._zone.SpawnMob("marshmallow_king", pos); // [!code --]
	string text = "marshmallow_king"; // [!code ++]
	if (id == "imotoroid") // [!code ++]
	{ // [!code ++]
		text = "imotomaton"; // [!code ++]
	} // [!code ++]
	Chara chara = EClass._zone.SpawnMob(text, pos); // [!code ++]
	chara.SetHostility(hostility); // [!code ++]
	foreach (Chara item in list)
	{
		item.Talk("fusion");
```

[`public bool CanDuplicate(DuplicateCondition con = DuplicateCondition.Default)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/Chara.cs#L1897-L1903)
```cs:line-numbers=1897
	{
		return false;
	}
	if (con != DuplicateCondition.Water && (HasCondition<ConPoison>() || HasCondition<ConConfuse>() || HasCondition<ConDim>() || HasCondition<ConParalyze>() || HasCondition<ConSleep>() || HasCondition<ConBurning>() || HasCondition<ConFreeze>() || HasCondition<ConMiasma>())) // [!code --]
	if (con != DuplicateCondition.Water && con != DuplicateCondition.Quantum && (HasCondition<ConPoison>() || HasCondition<ConConfuse>() || HasCondition<ConDim>() || HasCondition<ConParalyze>() || HasCondition<ConSleep>() || HasCondition<ConBurning>() || HasCondition<ConFreeze>() || HasCondition<ConMiasma>())) // [!code ++]
	{
		return false;
	}
```

[`public bool CanDuplicate(DuplicateCondition con = DuplicateCondition.Default)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/Chara.cs#L1919-L1925)
```cs:line-numbers=1919
		return true;
	}

	public Chara TryDuplicate(DuplicateCondition con = DuplicateCondition.Default, Point dest = null) // [!code --]
	public Chara TryDuplicate(DuplicateCondition con = DuplicateCondition.Default, Point dest = null, bool inheritHostility = false) // [!code ++]
	{
		if (dest == null)
		{
```

[`public Chara TryDuplicate(DuplicateCondition con = DuplicateCondition.Default, P`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/Chara.cs#L1933-L1938)
```cs:line-numbers=1933
	}
	Chara chara = Duplicate();
	EClass._zone.AddCard(chara, dest);
	if (inheritHostility) // [!code ++]
	{ // [!code ++]
		chara.SetHostility(hostility); // [!code ++]
	} // [!code ++]
	Say("split", this);
	return chara;
}
```

[`public void RestockEquip(bool onCreate)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/Chara.cs#L5164-L5169)
```cs:line-numbers=5164
		case "mech_scarab":
			AddThing("gun_laser");
			break;
		case "imotomaton": // [!code ++]
		case "rocketman":
		{
			Thing thing2 = AddThing("panzerfaust");
```

[`public bool CanAcceptGift(Chara c, Card t)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/Chara.cs#L8698-L8707)
```cs:line-numbers=8698
	{
		return false;
	}
	if (t.category.id == "currency" && !IsPCFaction) // [!code --]
	{ // [!code --]
		return false; // [!code --]
	} // [!code --]
	if (base.IsPowerful && t.trait is TraitScroll { source: not null } traitScroll && traitScroll.source.abilityType.Contains("summon"))
	{
		return false;
```

## ContentConfigTest

[`public class ContentConfigTest : ContentConfig`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ContentConfigTest.cs#L31-L36)
```cs:line-numbers=31

	public UIButton toggleBlockOnItem;

	public UIButton toggleDisableShake2; // [!code ++]
 // [!code ++]
	public UIButton toggleAlwaysFixCamera;

	public UIButton toggleIgnoreBackerFlag;
```

[`public override void OnInstantiate()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ContentConfigTest.cs#L57-L62)
```cs:line-numbers=57
		base.config.test.idSkin = a;
		base.config.ApplySkin();
	});
	toggleDisableShake2.SetToggle(base.config.test.disableShake2, delegate(bool on) // [!code ++]
	{ // [!code ++]
		base.config.test.disableShake2 = on; // [!code ++]
	}); // [!code ++]
	List<string> langs = new List<string> { "ani0", "ani1", "ani2", "ani3", "ani4" };
	List<string> langs2 = new List<string> { "0x", "1x", "2x", "5x", "10x" };
	toggleRefIcon.SetToggle(base.config.test.showRefIcon, delegate(bool on)
```

## CoreConfig

[`public class Test`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/CoreConfig.cs#L563-L568)
```cs:line-numbers=563

		public bool dontCancelIfZeroDamage;

		public bool disableShake2; // [!code ++]
 // [!code ++]
		public bool alwaysFixCamera;

		public bool ignoreBackerDestoryFlag;
```

## DuplicateCondition

[`public enum DuplicateCondition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/DuplicateCondition.cs#L3-L7)
```cs:line-numbers=3
	Default,
	Scarab,
	SpiderEgg,
	Water // [!code --]
	Water, // [!code ++]
	Quantum // [!code ++]
}
```

## ELEMENT

[`public void _WriteNote(UINote n, ElementContainer owner, Action<UINote> onWriteN`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/ELEMENT.cs#L801-L806)
```cs:line-numbers=801
		{
			uIItem2.text2.SetText(GetPower(EClass.pc).ToString() ?? "");
		}
		bool flag5 = source.eleP != 50 && this is Act; // [!code ++]
		uIItem2.text4.SetActive(flag5); // [!code ++]
		uIItem2.Find("text5")?.SetActive(flag5); // [!code ++]
		if (flag5) // [!code ++]
		{ // [!code ++]
			uIItem2.text4.SetText(source.eleP.ToString() ?? ""); // [!code ++]
		} // [!code ++]
	}
	OnWriteNote(n, owner);
	if (EClass.debug.showExtra)
```

## EffectMeteor

[`public override void OnPlay()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/EffectMeteor.cs#L30-L36)
```cs:line-numbers=30
				destPos.Animate(AnimeID.Dig, animeBlock: true);
				onComplete?.Invoke();
				EMono.Sound.Play(soundExplode, destV);
				Shaker.ShakeCam("meteor"); // [!code --]
				if (!EMono.core.config.test.disableShake2) // [!code ++]
				{ // [!code ++]
					Shaker.ShakeCam("meteor"); // [!code ++]
				} // [!code ++]
			});
	}

```

## FactionBranch

[`public void OnSimulateHour(VirtualDate date)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/FactionBranch.cs#L379-L385)
```cs:line-numbers=379
	}
	if ((date.IsRealTime || num2 == 0) && num6 != 0 && EClass.rnd(num6) == 0 && EClass._map.CountWildAnimal() < num4)
	{
		EClass._zone.SpawnMob(null, SpawnSetting.HomeWild(5)); // [!code --]
		EClass._zone.SpawnMob(EClass._zone.GetSpawnPos(SpawnPosition.Guest), SpawnSetting.HomeWild(5)); // [!code ++]
	}
	if (EClass.rnd(5) == 0 && policies.IsActive(2810))
	{
```

[`public void OnSimulateHour(VirtualDate date)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/FactionBranch.cs#L400-L406)
```cs:line-numbers=400
			}
			else
			{
				chara2 = EClass._zone.SpawnMob(null, SpawnSetting.HomeGuest(ContentLV)); // [!code --]
				chara2 = EClass._zone.SpawnMob(EClass._zone.GetSpawnPos(SpawnPosition.Guest), SpawnSetting.HomeGuest(ContentLV)); // [!code ++]
			}
			if (chara2 != null && (chara2.id == "nun_mother" || chara2.id == "prostitute") && policies.IsActive(2710))
			{
```

## GameDate

[`public void AdvanceHour()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/GameDate.cs#L103-L109)
```cs:line-numbers=103
	if (!shaken && EClass.rnd(24) == 0 && !EClass._zone.IsRegion)
	{
		Msg.Say("earthquake");
		if (!EClass.core.config.graphic.disableShake) // [!code --]
		if (!EClass.core.config.graphic.disableShake && !EClass.core.config.test.disableShake2) // [!code ++]
		{
			Shaker.ShakeCam("earthquake");
		}
```

## GoalCombat

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/GoalCombat.cs#L179-L184)
```cs:line-numbers=179
		{
			tc.TrySetEnemy(owner);
		}
		if (TryFuse()) // [!code ++]
		{ // [!code ++]
			yield return Success(); // [!code ++]
		} // [!code ++]
		if (!tc.IsMinion && EClass.rnd(10) == 0 && EClass.rnd(tc.DEX + 10) > owner.LV && tc.HasElement(1315) && !owner.HasElement(1315) && owner.IsMachine && owner.CanBeTempAlly(tc) && owner.id != "mech_scarab")
		{
			owner.Say("dominate_machine", tc, owner);
```

[`public virtual bool TryThrow(int dist)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/GoalCombat.cs#L507-L512)
```cs:line-numbers=507
		return true;
	}

	public virtual bool TryFuse() // [!code ++]
	{ // [!code ++]
		string text = owner.id; // [!code ++]
		if ((text == "imotoroid" || text == "marshmallow_monster") && owner.TryFuse(0.05f)) // [!code ++]
		{ // [!code ++]
			return true; // [!code ++]
		} // [!code ++]
		return false; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public virtual bool TryUseAbility(int dist, bool beforeMove = false)
	{
		if (abilities.Count == 0)
```

## GrowSystem

[`public void PopHarvest(Chara c, Thing t = null, int num = -1)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/GrowSystem.cs#L650-L668)
```cs:line-numbers=650
{
	if (t == null)
	{
		if (idHarvestThing.StartsWith('#')) // [!code --]
		{ // [!code --]
			t = ThingGen.CreateFromCategory(idHarvestThing.Replace("#", "")); // [!code --]
		} // [!code --]
		else // [!code --]
		{ // [!code --]
			string text = idHarvestThing; // [!code --]
			if (text == "potato" && EClass.rnd(3) == 0) // [!code --]
			{ // [!code --]
				text = "784"; // [!code --]
			} // [!code --]
			t = ThingGen.Create(text.IsEmpty("apple")); // [!code --]
		} // [!code --]
		t = ((!idHarvestThing.StartsWith('#')) ? ThingGen.Create(idHarvestThing.IsEmpty("apple")) : ThingGen.CreateFromCategory(idHarvestThing.Replace("#", ""))); // [!code ++]
	}
	ApplySeed(t);
	if (cell.isObjDyed && source.ContainsTag("dyeable"))
```

## Religion

[`public void DoPunish(Chara c, PunishType type)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/Religion.cs#L452-L458)
```cs:line-numbers=452
		c.mimicry.Kill();
	}
	Talk((type == PunishType.Takeover) ? "takeoverFail" : "wrath");
	if (type == PunishType.Wrath && c.Evalue(1228) > 0) // [!code --]
	if ((type == PunishType.Wrath || type == PunishType.Takeover) && c.Evalue(1228) > 0) // [!code ++]
	{
		c.SayNothingHappans();
		return;
```

## Trait

[`public Point GetRandomPoint(Func<Point, bool> func = null, Chara accessChara = n`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/Trait.cs#L861-L867)
```cs:line-numbers=861
		return list[0];
	}

	public virtual List<Point> ListPoints(Point center = null, bool onlyPassable = true) // [!code --]
	public virtual List<Point> ListPoints(Point center = null, bool onlyPassable = true, bool allowChara = true) // [!code ++]
	{
		listRadiusPoints.Clear();
		if (center == null)
```

[`public virtual List<Point> ListPoints(Point center = null, bool onlyPassable = t`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/Trait.cs#L878-L890)
```cs:line-numbers=878
	{
		foreach (Point point in room.points)
		{
			if (radiusType == TraitRadiusType.Farm) // [!code --]
			if (allowChara || !point.HasChara) // [!code ++]
			{
				listRadiusPoints.Add(point.Copy()); // [!code --]
			} // [!code --]
			else if ((!onlyPassable || !point.cell.blocked) && !point.cell.HasBlock && point.cell.HasFloor) // [!code --]
			{ // [!code --]
				listRadiusPoints.Add(point.Copy()); // [!code --]
				if (radiusType == TraitRadiusType.Farm) // [!code ++]
				{ // [!code ++]
					listRadiusPoints.Add(point.Copy()); // [!code ++]
				} // [!code ++]
				else if ((!onlyPassable || !point.cell.blocked) && !point.cell.HasBlock && point.cell.HasFloor) // [!code ++]
				{ // [!code ++]
					listRadiusPoints.Add(point.Copy()); // [!code ++]
				} // [!code ++]
			}
		}
	}
```

[`public virtual List<Point> ListPoints(Point center = null, bool onlyPassable = t`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/Trait.cs#L892-L908)
```cs:line-numbers=892
	{
		EClass._map.ForeachSphere(center.x, center.z, radius + 1, delegate(Point p)
		{
			if (radiusType == TraitRadiusType.Farm) // [!code --]
			if (allowChara || !p.HasChara) // [!code ++]
			{
				if (!p.cell.HasBlock || p.cell.HasFence) // [!code --]
				if (radiusType == TraitRadiusType.Farm) // [!code ++]
				{ // [!code ++]
					if (!p.cell.HasBlock || p.cell.HasFence) // [!code ++]
					{ // [!code ++]
						listRadiusPoints.Add(p.Copy()); // [!code ++]
					} // [!code ++]
				} // [!code ++]
				else if ((!onlyPassable || !p.cell.blocked) && !p.cell.HasBlock && p.cell.HasFloor && (!onlyPassable || Los.IsVisible(center, p))) // [!code ++]
				{
					listRadiusPoints.Add(p.Copy());
				}
			}
			else if ((!onlyPassable || !p.cell.blocked) && !p.cell.HasBlock && p.cell.HasFloor && (!onlyPassable || Los.IsVisible(center, p))) // [!code --]
			{ // [!code --]
				listRadiusPoints.Add(p.Copy()); // [!code --]
			} // [!code --]
		});
	}
	if (listRadiusPoints.Count == 0)
```

## TraitLightSun

[`public override int radius`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/TraitLightSun.cs#L16-L22)
```cs:line-numbers=16

	public override bool CanUseRoomRadius => false;

	public override List<Point> ListPoints(Point center = null, bool onlyPassable = true) // [!code --]
	public override List<Point> ListPoints(Point center = null, bool onlyPassable = true, bool allowChara = true) // [!code ++]
	{
		Trait.listRadiusPoints.Clear();
		if (center == null)
```

## +TraitSpotGuest

::: details File Created
```cs
public class TraitSpotGuest : TraitSpot
{
}
```

:::
## Zone

[`public Point GetSpawnPos(SpawnPosition type, int tries = 100)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/09d7ad6881081e05988ce98a865417a615290cfc/Elin/Zone.cs#L2867-L2872)
```cs:line-numbers=2867
		{
		case SpawnPosition.Guest:
		{
			List<TraitSpotGuest> list = EClass._map.props.installed.traits.List<TraitSpotGuest>(); // [!code ++]
			if (list.Count > 0) // [!code ++]
			{ // [!code ++]
				list.Shuffle(); // [!code ++]
				foreach (TraitSpotGuest item in list) // [!code ++]
				{ // [!code ++]
					List<Point> list2 = item.ListPoints(null, onlyPassable: true, allowChara: false); // [!code ++]
					if (list2.Count > 0) // [!code ++]
					{ // [!code ++]
						return list2.RandomItem(); // [!code ++]
					} // [!code ++]
				} // [!code ++]
			} // [!code ++]
			Room room = point.cell.room;
			if (room != null && room.data.accessType != 0)
			{
```
