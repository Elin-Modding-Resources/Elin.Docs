---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 1 file modified.
version: +EA 23.299 Nightly - Plugin.UI
changes: PixelPaint
---

# +EA 23.299 Nightly - Plugin.UI

April 27, 2026

1 file modified.

## Important Changes

**None.**
## PixelPaint

[`using UnityEngine;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/ab2ba5e6cc63cf21703b5b322452ef80703f0360/Elin/Plugins.UI/PixelPaint.cs#L2-L8)
```cs:line-numbers=2
using UnityEngine;
using UnityEngine.UI;

public class PixelPaint : MonoBehaviour // [!code --]
public class PixelPaint : MonoBehaviour, IChangeResolution // [!code ++]
{
	public RawImage imageRect;

```

[`public class PixelPaint : MonoBehaviour`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/ab2ba5e6cc63cf21703b5b322452ef80703f0360/Elin/Plugins.UI/PixelPaint.cs#L30-L35)
```cs:line-numbers=30

	public Color bgColor;

	private Vector2 lastSize; // [!code ++]
 // [!code ++]
	private bool first = true;

	private void Update()
```

[`private void Update()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/ab2ba5e6cc63cf21703b5b322452ef80703f0360/Elin/Plugins.UI/PixelPaint.cs#L91-L111)
```cs:line-numbers=91

	public void Init()
	{
		Vector2 sizeDelta = new Vector2(size.x * scale, size.y * scale); // [!code --]
		imageRect.rectTransform.sizeDelta = sizeDelta; // [!code --]
		imageGrid.rectTransform.sizeDelta = sizeDelta; // [!code --]
		imageGrid.uvRect = new Rect(0f, 0f, size.x, size.y); // [!code --]
		tex = new Texture2D(size.x, size.y, TextureFormat.ARGB32, mipChain: false);
		tex.filterMode = FilterMode.Point;
		imagePreview.texture = tex;
		imagePreview.rectTransform.sizeDelta = new Vector2(size.x * 2, size.y * 2);
		imageRect.texture = tex;
		Fill(bgColor);
		RefreshSize(); // [!code ++]
		picker.Init();
		picker.SelectColor(startColor);
		picker.SelectColor(startColor);
	}

	public void OnChangeResolution() // [!code ++]
	{ // [!code ++]
		RefreshSize(); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void RefreshSize() // [!code ++]
	{ // [!code ++]
		scale = (int)((float)(Screen.height - 200) / BaseCore.Instance.uiScale / (float)size.y); // [!code ++]
		Vector2 vector = new Vector2(size.x * scale, size.y * scale); // [!code ++]
		if (!(lastSize == vector)) // [!code ++]
		{ // [!code ++]
			imageRect.rectTransform.sizeDelta = vector; // [!code ++]
			imageGrid.rectTransform.sizeDelta = vector; // [!code ++]
			imageGrid.uvRect = new Rect(0f, 0f, size.x, size.y); // [!code ++]
			lastSize = vector; // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void Fill(Color color)
	{
		int num = tex.GetPixels().Length;
```
