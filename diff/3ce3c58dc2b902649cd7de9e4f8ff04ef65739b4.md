---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 1 file modified.
version: +EA 23.298 Nightly - Plugin.UI
changes: Dropper
---

# +EA 23.298 Nightly - Plugin.UI

April 24, 2026

1 file modified.

## Important Changes

**None.**
## Dropper

[`private void Awake()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/3ce3c58dc2b902649cd7de9e4f8ff04ef65739b4/Elin/Plugins.UI/Empyrean/ColorPicker/Dropper.cs#L47-L52)
```cs:line-numbers=47
		}
	}

	private void OnDestroy() // [!code ++]
	{ // [!code ++]
		if ((bool)renderTexture) // [!code ++]
		{ // [!code ++]
			UnityEngine.Object.Destroy(renderTexture); // [!code ++]
		} // [!code ++]
		if ((bool)texture2D) // [!code ++]
		{ // [!code ++]
			UnityEngine.Object.Destroy(texture2D); // [!code ++]
		} // [!code ++]
		if ((bool)pixelBlock) // [!code ++]
		{ // [!code ++]
			UnityEngine.Object.Destroy(pixelBlock); // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	private void Start()
	{
	}
```

[`private void UpdateRenderTextureIfNecessary()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/3ce3c58dc2b902649cd7de9e4f8ff04ef65739b4/Elin/Plugins.UI/Empyrean/ColorPicker/Dropper.cs#L60-L65)
```cs:line-numbers=60
{
	if (renderTexture.width != Screen.width || renderTexture.height != Screen.height)
	{
		UnityEngine.Object.Destroy(renderTexture); // [!code ++]
		renderTexture = new RenderTexture(Screen.width, Screen.height, 24);
	}
}
```

[`private void UpdateTexture()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/3ce3c58dc2b902649cd7de9e4f8ff04ef65739b4/Elin/Plugins.UI/Empyrean/ColorPicker/Dropper.cs#L96-L102)
```cs:line-numbers=96

	private void UpdateColorByMousePosition()
	{
		color = texture2D.GetPixel((int)EInput.uiMousePosition.x, (int)EInput.uiMousePosition.y); // [!code --]
		int num = (int)Mathf.Clamp(Input.mousePosition.x, 0f, texture2D.width); // [!code ++]
		int num2 = (int)Mathf.Clamp(Input.mousePosition.y, 0f, texture2D.height); // [!code ++]
		Vector2 vector = new Vector2(num - 8, num2 - 8); // [!code ++]
		Vector2 vector2 = new Vector2(num + 8, num2 + 8); // [!code ++]
		Vector2 vector3 = new Vector2(Mathf.Clamp(vector.x, 0f, texture2D.width - 8), Mathf.Clamp(vector.y, 0f, texture2D.height - 8)); // [!code ++]
		Vector2 vector4 = new Vector2(Mathf.Clamp(vector2.x, 8f, texture2D.width), Mathf.Clamp(vector2.y, 8f, texture2D.height)); // [!code ++]
		int num3 = (int)vector4.x - (int)vector3.x; // [!code ++]
		int num4 = (int)vector4.y - (int)vector3.y; // [!code ++]
		color = texture2D.GetPixel((int)vector3.x + num3 / 2, (int)vector3.y + num4 / 2); // [!code ++]
	}

	public void PickColors(Action<Color> onColorPicked, Action onDropCanceled)
```

[`public void PickColors(Action<Color> onColorPicked, Action onDropCanceled)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/3ce3c58dc2b902649cd7de9e4f8ff04ef65739b4/Elin/Plugins.UI/Empyrean/ColorPicker/Dropper.cs#L111-L116)
```cs:line-numbers=111

	private void OnBlockerClicked()
	{
		UpdateColorByMousePosition(); // [!code ++]
		Stop();
		onColorPicked(color);
	}
```
