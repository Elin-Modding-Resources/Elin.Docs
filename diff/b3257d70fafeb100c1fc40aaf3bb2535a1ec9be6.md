---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 16 files modified. 2 new files created.
version: EA 23.289 Nightly
changes: AI_Idle/Card/Chara/FoodEffect/Region/ShopType/SourceMaterial/SpawnSetting/Trait/TraitBuildBoard/TraitCrafter/+TraitDoorCurtain3/TraitIncubator/TraitMapBoard/+TraitMerchantTravel/TraitPaintBoard/Zone/ZonePreEnterEncounter
---

# EA 23.289 Nightly

March 30, 2026

16 files modified. 2 new files created.

## Important Changes

**None.**
## AI_Idle

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/AI_Idle.cs#L458-L464)
```cs:line-numbers=458
		{
		case "azzrasizzle":
		case "geist":
		case "machine_speller": // [!code --]
		{
			if (EClass.rnd(20) != 0)
			{
```

## Card

[`public void ApplyTrait()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/Card.cs#L2958-L2968)
```cs:line-numbers=2958
	string str = c_idTrait;
	if (str.IsEmpty())
	{
		if (sourceCard.trait.IsEmpty()) // [!code --]
		{ // [!code --]
			trait = (isChara ? new TraitChara() : new Trait()); // [!code --]
		} // [!code --]
		else // [!code --]
		if (!sourceCard.trait.IsEmpty()) // [!code ++]
		{
			trait = ClassCache.Create<Trait>("Trait" + sourceCard.trait[0], "Elin");
		}
```

[`public void ApplyTrait()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/Card.cs#L2971-L2976)
```cs:line-numbers=2971
		{
			trait = ClassCache.Create<Trait>(str, "Elin");
		}
		if (trait == null) // [!code ++]
		{ // [!code ++]
			trait = (isChara ? new TraitChara() : new Trait()); // [!code ++]
		} // [!code ++]
		trait.SetOwner(this);
	}

```

## Chara

[`public bool UseAbility(Act a, Card tc = null, Point pos = null, bool pt = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/Chara.cs#L5993-L6001)
```cs:line-numbers=5993
	{
		if (!Dialog.warned && cost.type == Act.CostType.MP && cost.cost > 0 && mana.value < num6 && !EClass.debug.godMode)
		{
			Card cardBk = EClass.scene.mouseTarget.card; // [!code ++]
			ActPlan.warning = true;
			Dialog.TryWarnMana(delegate
			{
				EClass.scene.mouseTarget.card = cardBk; // [!code ++]
				if (UseAbility(a, tc, pos, pt))
				{
					EClass.player.EndTurn();
```

## FoodEffect

[`public static void Proc(Chara c, Thing food, bool consume = true)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/FoodEffect.cs#L18-L24)
```cs:line-numbers=18
		return;
	}
	food.CheckJustCooked();
	bool flag = EClass._zone.IsPCFaction && c2.IsInSpot<TraitSpotDining>(); // [!code --]
	bool flag = EClass._zone.IsPCFactionOrTent && c2.IsInSpot<TraitSpotDining>(); // [!code ++]
	int num = (food.isCrafted ? ((EClass.pc.Evalue(1650) >= 3) ? 5 : 0) : 0);
	float num2 = (float)(100 + (food.HasElement(757) ? 10 : 0) + (flag ? 10 : 0) + num + Mathf.Min(food.QualityLv * 10, 100)) / 200f;
	if (num2 < 0.1f)
```

## Region

[`public override void OnAdvanceHour()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/Region.cs#L361-L367)
```cs:line-numbers=361
		{
			ShouldPass = (SourceChara.Row s) => s.biome == biome.name || s.biome.IsEmpty()
		}) : SpawnList.Get(biome.spawn.GetRandomCharaId()));
		Chara chara = CharaGen.CreateFromFilter(list, regionPoint.dangerLv); // [!code --]
		Chara chara = null; // [!code ++]
		chara = ((EClass.rnd(EClass.debug.enable ? 2 : 50) != 0) ? CharaGen.CreateFromFilter(list, regionPoint.dangerLv) : CharaGen.Create("merchant_travel")); // [!code ++]
		if (chara != null && !(chara.trait is TraitUniqueMonster))
		{
			AddCard(chara, randomPoint);
```

## ShopType

[`public enum ShopType`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/ShopType.cs#L51-L55)
```cs:line-numbers=51
	Sweet,
	Moyer,
	Curry,
	KeeperOfGarden // [!code --]
	KeeperOfGarden, // [!code ++]
	TravelMerchant // [!code ++]
}
```

## SourceMaterial

[`public void Init()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/SourceMaterial.cs#L131-L136)
```cs:line-numbers=131
					}
				}
			}
			Core.Instance.Colors.matColors[alias] = matColors; // [!code ++]
		}
		matColor = matColors.main;
		altColor = matColors.alt;
```

## SpawnSetting

[`public static SpawnSetting Mob(string id, string idEle = null, int fixedLv = -1)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/SpawnSetting.cs#L90-L95)
```cs:line-numbers=90
		};
	}

	public static SpawnSetting TravelMerchant(string id, int fixedLv = -1) // [!code ++]
	{ // [!code ++]
		return new SpawnSetting // [!code ++]
		{ // [!code ++]
			id = id, // [!code ++]
			rarity = Rarity.Legendary, // [!code ++]
			fixedLv = fixedLv // [!code ++]
		}; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static SpawnSetting HomeWild(int lv)
	{
		return new SpawnSetting
```

## Trait

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/Trait.cs#L1674-L1683)
```cs:line-numbers=1674
		};
		foreach (string id2 in array)
		{
			AddThing(ThingGen.Create(id2, MATERIAL.GetRandomMaterialFromCategory(50, "rock", EClass.sources.materials.alias["granite"]).id).SetNum(10)); // [!code --]
			AddThing(ThingGen.Create(id2, MATERIAL.GetRandomMaterialFromCategory(50, "rock", EClass.sources.materials.alias["granite"]).id).SetNum(99)); // [!code ++]
		}
		Add("scroll_alias", 10, 0); // [!code --]
		Add("scroll_biography", 10, 0); // [!code --]
		Add("cloud", 99, 0); // [!code ++]
		Add("scroll_alias", 99, 0); // [!code ++]
		Add("scroll_biography", 99, 0); // [!code ++]
		Add("1329", 1, 0);
		break;
	}
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/Trait.cs#L1915-L1920)
```cs:line-numbers=1915
				case ShopType.StrangeGirl:
					num3 = 50;
					break;
				case ShopType.TravelMerchant: // [!code ++]
					num2 /= 3f; // [!code ++]
					num3 = 30; // [!code ++]
					break; // [!code ++]
				}
				num2 = num2 * (float)(100 + EClass.pc.Evalue(1406) * 5) / 100f;
				num2 = Mathf.Min(num2, num3);
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/Trait.cs#L2152-L2157)
```cs:line-numbers=2152
				{
					thing11.c_priceFix = -70;
				}
				if (ShopType == ShopType.TravelMerchant) // [!code ++]
				{ // [!code ++]
					thing11.c_priceFix = 200; // [!code ++]
				} // [!code ++]
				if (thing11.trait is TraitErohon)
				{
					thing11.c_IDTState = 5;
```

[`Thing Add(string id, int a, int idSkin)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/Trait.cs#L2185-L2196)
```cs:line-numbers=2185
			}
			void AddAdvWeek(int i)
			{
				if (i != 18) // [!code --]
				{ // [!code --]
					Thing thing7 = ThingGen.CreateRedBook("advweek_" + i); // [!code --]
					thing7.c_priceFix = -90; // [!code --]
					AddThing(thing7); // [!code --]
				} // [!code --]
				Thing thing7 = ThingGen.CreateRedBook("advweek_" + i); // [!code ++]
				thing7.c_priceFix = -90; // [!code ++]
				AddThing(thing7); // [!code ++]
			}
			void AddCassette(int idCas, string idQuest, int phase)
			{
```

[`public Thing CreateStock()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/Trait.cs#L2312-L2319)
```cs:line-numbers=2312
				return FromFilter("shop_gun");
			case ShopType.Blackmarket:
			case ShopType.Exotic:
			case ShopType.TravelMerchant: // [!code ++]
			{
				int num = 30;
				if (ShopType == ShopType.TravelMerchant) // [!code ++]
				{ // [!code ++]
					num = 5; // [!code ++]
				} // [!code ++]
				if (Guild.Thief.IsCurrentZone)
				{
					num = 25;
```

[`public Thing CreateStock()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/Trait.cs#L2322-L2331)
```cs:line-numbers=2322
				{
					num = 15;
				}
				if (EClass.debug.enable) // [!code --]
				{ // [!code --]
					num = 1; // [!code --]
				} // [!code --]
				CardBlueprint.SetRarity((EClass.rnd(num * 5) == 0) ? Rarity.Mythical : ((EClass.rnd(num) == 0) ? Rarity.Legendary : ((EClass.rnd(5) == 0) ? Rarity.Superior : Rarity.Normal)));
				return FromFilter("shop_blackmarket");
			}
```

## TraitBuildBoard

[`public class TraitBuildBoard : TraitBoard`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/TraitBuildBoard.cs#L4-L10)
```cs:line-numbers=4

	public override void TrySetAct(ActPlan p)
	{
		if (EClass.debug.godBuild || EClass._zone.IsPCFaction || EClass._zone is Zone_Tent) // [!code --]
		if (EClass.debug.godBuild || EClass._zone.IsPCFactionOrTent) // [!code ++]
		{
			p.TrySetAct("actBuildMode", delegate
			{
```

## TraitCrafter

[`public override void TrySetAct(ActPlan p)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/TraitCrafter.cs#L563-L569)
```cs:line-numbers=563
			return false;
		}, _t, CursorSystem.Craft);
	}
	else // [!code --]
	else if (!(this is TraitIncubator) || EClass._zone.IsPCFactionOrTent) // [!code ++]
	{
		p.TrySetAct(CrafterTitle, delegate
		{
```

## +TraitDoorCurtain3

::: details File Created
```cs
public class TraitDoorCurtain3 : TraitDoorAuto
{
	public override string idSound => "Material/meat_drop";
}
```

:::
## TraitIncubator

[`public class TraitIncubator : TraitCrafter`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/TraitIncubator.cs#L17-L20)
```cs:line-numbers=17
	public override bool AutoToggle => false;

	public override bool CanUseFromInventory => false;
 // [!code ++]
	public override bool IsHomeItem => true; // [!code ++]
}
```

## TraitMapBoard

[`public class TraitMapBoard : TraitBoard`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/TraitMapBoard.cs#L9-L15)
```cs:line-numbers=9

	public override void TrySetAct(ActPlan p)
	{
		if (!EClass.debug.enable && !EClass._zone.IsPCFaction && !(EClass._zone is Zone_Tent)) // [!code --]
		if (!EClass.debug.enable && !EClass._zone.IsPCFactionOrTent) // [!code ++]
		{
			return;
		}
```

## +TraitMerchantTravel

::: details File Created
```cs
public class TraitMerchantTravel : TraitMerchant
{
	public override int ShopLv
	{
		get
		{
			if (!base.owner.IsPCFactionOrMinion)
			{
				return base.owner.LV;
			}
			return base.ShopLv;
		}
	}

	public override ShopType ShopType => ShopType.TravelMerchant;

	public override bool AllowCriminal => true;

	public override int CostRerollShop => 10;

	public override int RestockDay => 360;
}
```

:::
## TraitPaintBoard

[`public override bool RenderExtra`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/TraitPaintBoard.cs#L26-L32)
```cs:line-numbers=26

	public override void TrySetAct(ActPlan p)
	{
		if (!EClass.debug.enable && (!EClass._zone.IsPCFaction || !p.altAction)) // [!code --]
		if (!EClass.debug.enable && (!EClass._zone.IsPCFactionOrTent || !p.altAction)) // [!code ++]
		{
			return;
		}
```

## Zone

[`public void Deactivate()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/Zone.cs#L1695-L1701)
```cs:line-numbers=1695
			}
		}
	}
	base.isPeace = false; // [!code --]
	if (!EClass.player.simulatingZone) // [!code ++]
	{ // [!code ++]
		base.isPeace = false; // [!code ++]
	} // [!code ++]
	OnBeforeDeactivate();
	if (IsPCFaction)
	{
```

## ZonePreEnterEncounter

[`public override void Execute()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/b3257d70fafeb100c1fc40aaf3bb2535a1ec9be6/Elin/ZonePreEnterEncounter.cs#L18-L103)
```cs:line-numbers=18
	{
		List<Chara> list = new List<Chara>();
		Chara leader = null;
		float num = Mathf.Clamp(EClass.pc.FameLv + 8, 8f, 24f + Mathf.Sqrt(EClass.pc.FameLv)); // [!code --]
		for (int i = 0; i < EClass.rndHalf((int)num); i++) // [!code --]
		if (mob.id == "merchant_travel") // [!code ++]
		{
			lv = Mathf.Max(20, EClass.pc.FameLv * 5); // [!code ++]
			Point randomPointInRadius = EClass.pc.pos.GetRandomPointInRadius(2, 5, requireLos: false, allowChara: false);
			if (randomPointInRadius != null) // [!code --]
			for (int i = 0; i < EClass.rndHalf(12); i++) // [!code ++]
			{
				Chara chara = EClass._zone.SpawnMob(randomPointInRadius, SpawnSetting.Mob(mob.id, (mob.MainElement == Element.Void) ? null : mob.MainElement.source.alias.Substring(3))); // [!code --]
				Hostility hostility2 = (chara.c_originalHostility = Hostility.Enemy); // [!code --]
				chara.hostility = hostility2; // [!code --]
				chara.enemy = EClass.pc.party.members.RandomItem(); // [!code --]
				leader = chara; // [!code --]
				if (EClass.rnd(5) == 0) // [!code --]
				Point randomPointInRadius2 = randomPointInRadius.GetRandomPointInRadius(1, 4, requireLos: false, allowChara: false); // [!code ++]
				if (randomPointInRadius2 != null) // [!code ++]
				{
					TraitFoodEggFertilized.MakeBaby(chara, 1); // [!code --]
					Chara chara = null; // [!code ++]
					if (i == 0) // [!code ++]
					{ // [!code ++]
						chara = (leader = EClass._zone.SpawnMob(randomPointInRadius2, SpawnSetting.TravelMerchant(mob.id, lv))); // [!code ++]
					} // [!code ++]
					else // [!code ++]
					{ // [!code ++]
						string[] source = new string[5] { "merc", "merc_archer", "merc_mage", "merc_warrior", "dog_hound" }; // [!code ++]
						chara = EClass._zone.SpawnMob(randomPointInRadius2, SpawnSetting.Mob(source.RandomItem(), null, lv)); // [!code ++]
						Chara chara2 = chara; // [!code ++]
						Hostility hostility2 = (chara.c_originalHostility = Hostility.Neutral); // [!code ++]
						chara2.hostility = hostility2; // [!code ++]
						chara.MakeMinion(leader); // [!code ++]
					} // [!code ++]
					list.Add(chara); // [!code ++]
				}
				list.Add(chara); // [!code --]
			}
			leader.ShowDialog(); // [!code ++]
		}
		if (leader != null) // [!code --]
		else // [!code ++]
		{
			List<Thing> list2 = EClass.pc.things.List(delegate(Thing t) // [!code --]
			float num = Mathf.Clamp(EClass.pc.FameLv + 8, 8f, 24f + Mathf.Sqrt(EClass.pc.FameLv)); // [!code ++]
			for (int j = 0; j < EClass.rndHalf((int)num); j++) // [!code ++]
			{
				if (t.Num >= 10) // [!code --]
				Point randomPointInRadius3 = EClass.pc.pos.GetRandomPointInRadius(2, 5, requireLos: false, allowChara: false); // [!code ++]
				if (randomPointInRadius3 != null) // [!code ++]
				{
					return false; // [!code --]
					Chara chara3 = EClass._zone.SpawnMob(randomPointInRadius3, SpawnSetting.Mob(mob.id, (mob.MainElement == Element.Void) ? null : mob.MainElement.source.alias.Substring(3))); // [!code ++]
					Hostility hostility2 = (chara3.c_originalHostility = Hostility.Enemy); // [!code ++]
					chara3.hostility = hostility2; // [!code ++]
					chara3.enemy = EClass.pc.party.members.RandomItem(); // [!code ++]
					leader = chara3; // [!code ++]
					if (EClass.rnd(5) == 0) // [!code ++]
					{ // [!code ++]
						TraitFoodEggFertilized.MakeBaby(chara3, 1); // [!code ++]
					} // [!code ++]
					list.Add(chara3); // [!code ++]
				}
				return t.trait.CanBeDestroyed && t.things.Count == 0 && t.invY != 1 && t.trait.CanBeStolen && !t.trait.CanOnlyCarry && !t.IsUnique && !t.isEquipped; // [!code --]
			}, onlyAccessible: true); // [!code --]
			Thing t2 = ((list2.Count > 0) ? list2.RandomItem() : null); // [!code --]
			if (t2 == null) // [!code --]
			{ // [!code --]
				GameLang.refDrama1 = (GameLang.refDrama2 = "mobPity".lang()); // [!code --]
			} // [!code --]
			else // [!code --]
			{ // [!code --]
				GameLang.refDrama1 = t2.NameSimple; // [!code --]
				GameLang.refDrama2 = t2.Name; // [!code --]
			}
			LayerDrama.refAction1 = delegate // [!code --]
			if (leader != null) // [!code ++]
			{
				foreach (Chara item in list) // [!code --]
				List<Thing> list2 = EClass.pc.things.List(delegate(Thing t) // [!code ++]
				{
					item.ShowEmo(Emo.angry); // [!code --]
					if (EClass.rnd(6) == 0) // [!code --]
					if (t.Num >= 10) // [!code ++]
					{
						item.Talk((EClass.rnd(5) == 0) ? "rumor_bad" : ((EClass.rnd(5) == 0) ? "callGuards" : "disgust")); // [!code --]
						return false; // [!code ++]
					}
					return t.trait.CanBeDestroyed && t.things.Count == 0 && t.invY != 1 && t.trait.CanBeStolen && !t.trait.CanOnlyCarry && !t.IsUnique && !t.isEquipped; // [!code ++]
				}, onlyAccessible: true); // [!code ++]
				Thing t2 = ((list2.Count > 0) ? list2.RandomItem() : null); // [!code ++]
				if (t2 == null) // [!code ++]
				{ // [!code ++]
					GameLang.refDrama1 = (GameLang.refDrama2 = "mobPity".lang()); // [!code ++]
				}
			}; // [!code --]
			LayerDrama.refAction2 = delegate // [!code --]
			{ // [!code --]
				if (t2 != null) // [!code --]
				else // [!code ++]
				{
					leader.AddCard(t2); // [!code --]
					GameLang.refDrama1 = t2.NameSimple; // [!code ++]
					GameLang.refDrama2 = t2.Name; // [!code ++]
				}
				foreach (Chara item2 in list) // [!code --]
				LayerDrama.refAction1 = delegate // [!code ++]
				{
					if (EClass.rnd(6) == 0) // [!code --]
					foreach (Chara item in list) // [!code ++]
					{
						item2.Talk((EClass.rnd(5) == 0) ? "rumor_good" : ((EClass.rnd(3) == 0) ? "thanks3" : "thanks")); // [!code --]
						item.ShowEmo(Emo.angry); // [!code ++]
						if (EClass.rnd(6) == 0) // [!code ++]
						{ // [!code ++]
							item.Talk((EClass.rnd(5) == 0) ? "rumor_bad" : ((EClass.rnd(5) == 0) ? "callGuards" : "disgust")); // [!code ++]
						} // [!code ++]
					}
					item2.ShowEmo(Emo.happy); // [!code --]
					Hostility hostility5 = (item2.c_originalHostility = Hostility.Neutral); // [!code --]
					item2.hostility = hostility5; // [!code --]
					item2.enemy = null; // [!code --]
				} // [!code --]
				EClass.player.ModKarma(1); // [!code --]
			}; // [!code --]
			leader.ShowDialog("_chara", "encounter_mob"); // [!code --]
				}; // [!code ++]
				LayerDrama.refAction2 = delegate // [!code ++]
				{ // [!code ++]
					if (t2 != null) // [!code ++]
					{ // [!code ++]
						leader.AddCard(t2); // [!code ++]
					} // [!code ++]
					foreach (Chara item2 in list) // [!code ++]
					{ // [!code ++]
						if (EClass.rnd(6) == 0) // [!code ++]
						{ // [!code ++]
							item2.Talk((EClass.rnd(5) == 0) ? "rumor_good" : ((EClass.rnd(3) == 0) ? "thanks3" : "thanks")); // [!code ++]
						} // [!code ++]
						item2.ShowEmo(Emo.happy); // [!code ++]
						Hostility hostility6 = (item2.c_originalHostility = Hostility.Neutral); // [!code ++]
						item2.hostility = hostility6; // [!code ++]
						item2.enemy = null; // [!code ++]
					} // [!code ++]
					EClass.player.ModKarma(1); // [!code ++]
				}; // [!code ++]
				leader.ShowDialog("_chara", "encounter_mob"); // [!code ++]
			} // [!code ++]
		}
	}
	else
	{
		for (int j = 0; j < enemies; j++) // [!code --]
		for (int k = 0; k < enemies; k++) // [!code ++]
		{
			Point nearestPoint = (EClass.pc.pos.GetRandomPoint(4) ?? EClass.pc.pos).GetNearestPoint(allowBlock: false, allowChara: false);
			Chara chara2 = EClass._zone.SpawnMob(nearestPoint, SpawnSetting.Encounter(lv)); // [!code --]
			Hostility hostility2 = (chara2.c_originalHostility = Hostility.Enemy); // [!code --]
			chara2.hostility = hostility2; // [!code --]
			chara2.enemy = EClass.pc.party.members.RandomItem(); // [!code --]
			Chara chara4 = EClass._zone.SpawnMob(nearestPoint, SpawnSetting.Encounter(lv)); // [!code ++]
			Hostility hostility2 = (chara4.c_originalHostility = Hostility.Enemy); // [!code ++]
			chara4.hostility = hostility2; // [!code ++]
			chara4.enemy = EClass.pc.party.members.RandomItem(); // [!code ++]
		}
	}
	if (flag && EClass.rnd(EClass.debug.enable ? 1 : 3) == 0)
```
