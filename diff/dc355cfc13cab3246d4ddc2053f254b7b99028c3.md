---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 25 files modified. 2 new files created.
version: EA 23.284 Nightly Patch 2
changes: AM_Adv/ActEffect/ActKiss/BaseListPeople/Card/Chara/ConPeaky/CoreDebug/DramaManager/ELEMENT/GameDate/GoalCombat/GrowSystem/ID_Achievement/LayerAbility/LayerPeople/ListOwner/+ListPeopleParty/LoveData/Party/Player/Religion/Trait/TraitGene/+TraitPartyBoard/UIMultiList/WindowChara
---

# EA 23.284 Nightly Patch 2

March 9, 2026

25 files modified. 2 new files created.

## Important Changes

**None.**
## AM_Adv

[`public override void _OnUpdateInput()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/AM_Adv.cs#L546-L552)
```cs:line-numbers=546
		if (AI_PlayMusic.keepPlaying)
		{
			Thing playingTool = AI_PlayMusic.playingTool;
			if (EInput.IsAnyKeyDown() || (playingTool.GetRootCard() != EClass.pc && (!playingTool.ExistsOnMap || playingTool.Dist(EClass.pc) > 1))) // [!code --]
			ConSuffocation condition = EClass.pc.GetCondition<ConSuffocation>(); // [!code ++]
			if (EInput.IsAnyKeyDown() || (playingTool.GetRootCard() != EClass.pc && (!playingTool.ExistsOnMap || playingTool.Dist(EClass.pc) > 1)) || (condition != null && condition.GetPhase() > 0)) // [!code ++]
			{
				AI_PlayMusic.CancelKeepPlaying();
				return;
```

## ActEffect

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/ActEffect.cs#L1078-L1083)
```cs:line-numbers=1078
		{
			CC.TryDropBossLoot();
		}
		if (CC.id == "bell_silver") // [!code ++]
		{ // [!code ++]
			EClass.player.stats.escapeSilverBell++; // [!code ++]
			if (EClass.player.stats.escapeSilverBell >= 10) // [!code ++]
			{ // [!code ++]
				Steam.GetAchievement(ID_Achievement.BELL); // [!code ++]
			} // [!code ++]
		} // [!code ++]
		CC.Destroy();
		break;
	case EffectId.BurnMana:
```

## ActKiss

[`public override bool CanPerform()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/ActKiss.cs#L8-L18)
```cs:line-numbers=8
		{
			return false;
		}
		if (Act.TC.IsPC && Act.TC.Chara.parasite == null && Act.TC.Chara.ride == null) // [!code ++]
		{ // [!code ++]
			return false; // [!code ++]
		} // [!code ++]
		return true;
	}

	public override bool Perform()
	{
		if (Act.CC.IsPC && Act.TC.IsPC) // [!code ++]
		{ // [!code ++]
			Act.TC = ((EClass.rnd(2) == 0 || EClass.pc.parasite == null) ? EClass.pc.ride : EClass.pc.parasite); // [!code ++]
			if (Act.TC == null) // [!code ++]
			{ // [!code ++]
				Act.TC = EClass.pc; // [!code ++]
			} // [!code ++]
		} // [!code ++]
		Act.CC.Kiss(Act.TC.Chara);
		return true;
	}
```

## BaseListPeople

[`public override void OnRefreshMenu()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/BaseListPeople.cs#L483-L487)
```cs:line-numbers=483
				List();
			});
		}
		if (!(this is ListPeopleParty)) // [!code ++]
		{ // [!code ++]
			return; // [!code ++]
		} // [!code ++]
		menuLeft.AddSpace(); // [!code ++]
		menuLeft.AddHeader("party"); // [!code ++]
		if (EClass.pc.party.members.Count > 1) // [!code ++]
		{ // [!code ++]
			menuLeft.AddButton("disband", delegate // [!code ++]
			{ // [!code ++]
				EClass.pc.party.Disband(); // [!code ++]
				SE.Trash(); // [!code ++]
				RefreshAll(); // [!code ++]
			}); // [!code ++]
		} // [!code ++]
	}
}
```

## Card

[`public bool isScaled`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/Card.cs#L923-L928)
```cs:line-numbers=923
		}
	}

	public bool isFav // [!code ++]
	{ // [!code ++]
		get // [!code ++]
		{ // [!code ++]
			return _bits2[10]; // [!code ++]
		} // [!code ++]
		set // [!code ++]
		{ // [!code ++]
			_bits2[10] = value; // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public bool isBackerContent => c_idBacker != 0;

	public SourceBacker.Row sourceBacker
```

## Chara

[`public bool UseAbility(Act a, Card tc = null, Point pos = null, bool pt = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/Chara.cs#L6048-L6054)
```cs:line-numbers=6048
	}
	else
	{
		if (IsPC && HasElement(1274) && a.vPotential < 0 && !flag2) // [!code --]
		if (IsPC && HasElement(1274) && a.vPotential < 0 && !flag2 && !a.HasTag("dontForget")) // [!code ++]
		{
			Msg.Say("noSpellStock");
			EInput.Consume();
```

[`public bool FindNewEnemy()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/Chara.cs#L6611-L6617)
```cs:line-numbers=6611
			}
			stealthSeen++;
		}
		else if (CanSeeLos(chara2) && (!flag2 || EClass.pc.isBlind || EClass.pc.CanSeeLos(chara2)) && (!IsPCFaction || EClass.pc.ai.ShouldAllyAttack(chara2))) // [!code --]
		else if (CanSeeLos(chara2) && (!flag2 || EClass.pc.isBlind || EClass.pc.CanSeeLos(chara2)) && (!base.IsPCFactionOrMinion || EClass.pc.ai.ShouldAllyAttack(chara2))) // [!code ++]
		{
			if (!IsPCParty)
			{
```

[`public override void SetSortVal(UIList.SortMode m, CurrencyType currency = Curre`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/Chara.cs#L7450-L7455)
```cs:line-numbers=7450
		case UIList.SortMode.ByWorkk:
			break;
		}
		if (base.isFav) // [!code ++]
		{ // [!code ++]
			sortVal -= 100000000; // [!code ++]
		} // [!code ++]
	}

	public void ClearBed(Map map = null)
```

## ConPeaky

[`public override void Tick()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/ConPeaky.cs#L34-L40)
```cs:line-numbers=34
		owner.PlaySound("bike_kick");
		if (owner.host != null)
		{
			owner.host.ModExp((owner.host.ride == owner) ? 226 : 227, owner.Evalue(1423) * 20); // [!code --]
			owner.host.ModExp((owner.host.ride == owner) ? 226 : 227, owner.Evalue(1423) * 50); // [!code ++]
		}
	}
}
```

## CoreDebug

[`public void UpdateInput()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/CoreDebug.cs#L951-L956)
```cs:line-numbers=951
	}
	if (Input.GetKeyDown(KeyCode.F2))
	{
		EClass.pc.body.AddBodyPart(35); // [!code ++]
		EClass._zone.development += 10;
		EClass._zone.development *= EClass._zone.development;
		Chara targetChara = EClass.scene.mouseTarget.TargetChara;
```

## DramaManager

[`public void ParseLine(Dictionary<string, string> item)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/DramaManager.cs#L813-L818)
```cs:line-numbers=813
			EMono.BranchOrHomeBranch.resources.Get(p2).Mod(p3.ToInt());
		});
		break;
	case "getAchievement": // [!code ++]
		AddEvent(delegate // [!code ++]
		{ // [!code ++]
			if (new string[1] { "ONEV" }.Contains(p2)) // [!code ++]
			{ // [!code ++]
				Steam.GetAchievement(p2.ToEnum<ID_Achievement>()); // [!code ++]
			} // [!code ++]
		}); // [!code ++]
		break; // [!code ++]
	case "bossText":
		AddEvent(delegate
		{
```

## ELEMENT

[`public void _WriteNote(UINote n, Chara c, Act act)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/ELEMENT.cs#L656-L661)
```cs:line-numbers=656
	{
		n.AddText("_bullet".lang() + "hintSyncRide".lang());
	}
	if (EClass.pc.HasElement(1274) && source.tag.Contains("dontForget")) // [!code ++]
	{ // [!code ++]
		n.AddText("_bullet".lang() + "hintDontForget".lang()); // [!code ++]
	} // [!code ++]
	if (act.HaveLongPressAction)
	{
		int i = id;
```

## GameDate

[`public void AdvanceDay()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/GameDate.cs#L223-L228)
```cs:line-numbers=223
		{
			EClass.world.SendPackage(ThingGen.Create("box_xmas"));
		}
		foreach (Chara value2 in EClass.game.cards.globalCharas.Values) // [!code ++]
		{ // [!code ++]
			if (value2.IsPCFaction && value2.IsMarried && !value2.c_love.gotMusicBox) // [!code ++]
			{ // [!code ++]
				Date date = Date.ToDate(value2.c_love.dateMarriage); // [!code ++]
				if (date.month == base.month && date.day == base.day) // [!code ++]
				{ // [!code ++]
					value2.c_love.gotMusicBox = true; // [!code ++]
					Thing thing3 = ThingGen.Create("musicbox_memory2"); // [!code ++]
					thing3.MakeRefFrom(value2, EClass.pc, simple: true); // [!code ++]
					Thing thing4 = ThingGen.Create("parchment"); // [!code ++]
					thing4.MakeRefFrom(value2, null, simple: true); // [!code ++]
					thing4.SetStr(53, "letter_love"); // [!code ++]
					Thing p3 = ThingGen.CreateParcel(null, thing3, thing4); // [!code ++]
					EClass.world.SendPackage(p3); // [!code ++]
				} // [!code ++]
			} // [!code ++]
		} // [!code ++]
	}

	public void AdvanceMonth()
```

## GoalCombat

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/GoalCombat.cs#L89-L95)
```cs:line-numbers=89
			}
		}
		tc = owner.enemy;
		if (tc != null && owner.IsPCFaction) // [!code --]
		if (tc != null && owner.IsPCFactionOrMinion) // [!code ++]
		{
			if (EClass.pc.isHidden && owner.isHidden)
			{
```

## GrowSystem

[`public void PopHarvest(Chara c, Thing t = null, int num = -1)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/GrowSystem.cs#L688-L697)
```cs:line-numbers=688
		{
			t.SetNum(num);
		}
		int num3 = t.Num; // [!code ++]
		TryPick(cell, t, c);
		if (flag)
		{
			c.ModExpParty(207, Mathf.Min(t.c_weight / 100, 500)); // [!code --]
			c.ModExpParty(207, Mathf.Min(t.c_weight * num3 / 125, 500)); // [!code ++]
		}
	}

```

## ID_Achievement

[`public enum ID_Achievement`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/ID_Achievement.cs#L20-L24)
```cs:line-numbers=20
	DEMITAS,
	KETTLE,
	FAITH_HEALING,
	ONEV // [!code --]
	ONEV, // [!code ++]
	JURE, // [!code ++]
	BELL // [!code ++]
}
```

## LayerAbility

[`public void SelectGroup(string id)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/LayerAbility.cs#L210-L216)
```cs:line-numbers=210
			foreach (Element value in ELayer.pc.elements.dict.Values)
			{
				string categorySub = value.source.categorySub;
				if (value.Value != 0 && (!config.hideDepletedSpell || ((!value.PotentialAsStock || value.vPotential > 0) && value.vPotential >= 0)) && (value.id != 6020 || ELayer.pc.HasElement(1216))) // [!code --]
				if (value.Value != 0 && (!config.hideDepletedSpell || ((!value.PotentialAsStock || value.vPotential > 0 || chara.ability.Has(value.id)) && (value.vPotential >= 0 || value.HasTag("dontForget")))) && (value.id != 6020 || ELayer.pc.HasElement(1216))) // [!code ++]
				{
					if (id == "favAbility" && ELayer.player.favAbility.Contains(value.id))
					{
```

## LayerPeople

[`public static LayerPeople CreateReserve()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/LayerPeople.cs#L125-L130)
```cs:line-numbers=125
		return layerPeople;
	}

	public static LayerPeople CreateParty() // [!code ++]
	{ // [!code ++]
		LayerPeople layerPeople = Create(Mode.Double); // [!code ++]
		layerPeople.multi.AddOwner(0, new ListPeopleParty // [!code ++]
		{ // [!code ++]
			textHeader = "candidates" // [!code ++]
		}); // [!code ++]
		layerPeople.multi.AddOwner(1, new ListPeopleParty // [!code ++]
		{ // [!code ++]
			textHeader = "faction_Member" // [!code ++]
		}); // [!code ++]
		ELayer.ui.AddLayer(layerPeople); // [!code ++]
		return layerPeople; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static LayerPeople CreateBed(TraitBed bed)
	{
		LayerPeople layerPeople = Create(Mode.Double);
```

## ListOwner

[`public void MoveToOther(object c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/ListOwner.cs#L121-L124)
```cs:line-numbers=121
		RefreshCaption();
		other.RefreshCaption();
	}
 // [!code ++]
	public void RefreshAll(bool freeze = true) // [!code ++]
	{ // [!code ++]
		if (freeze) // [!code ++]
		{ // [!code ++]
			EClass.ui.FreezeScreen(0.1f); // [!code ++]
		} // [!code ++]
		List(); // [!code ++]
		other.List(); // [!code ++]
		RefreshCaption(); // [!code ++]
		other.RefreshCaption(); // [!code ++]
		Main.OnRefreshMenu(); // [!code ++]
	} // [!code ++]
}
```

## +ListPeopleParty

::: details File Created
```cs
using System.Linq;

public class ListPeopleParty : BaseListPeople
{
	public override void OnCreate()
	{
		list.sorts = new UIList.SortMode[3]
		{
			UIList.SortMode.ByFeat,
			UIList.SortMode.ByJob,
			UIList.SortMode.ByRace
		};
		list.sortMode = UIList.SortMode.ByFeat;
	}

	public override void OnInstantiate(Chara c, ItemGeneral i)
	{
		Zone zone = (main ? c.currentZone : c.homeZone);
		i.SetSubText((zone == null) ? "???" : zone.Name, 240);
		if (!c.IsPC)
		{
			UIButton uIButton = i.AddSubButton(EClass.core.refs.icons.fav, delegate
			{
				SE.ClickGeneral();
				c.isFav = !c.isFav;
				RefreshAll();
			});
			uIButton.icon.SetAlpha(c.isFav ? 1f : 0.3f);
			uIButton.icon.SetNativeSize();
		}
	}

	public override void OnClick(Chara c, ItemGeneral i)
	{
		if (main)
		{
			if (c.isDead || c.HasCondition<ConSuspend>() || c.currentZone == null)
			{
				SE.Beep();
				return;
			}
			if (c.currentZone != EClass._zone)
			{
				c.MoveZone(EClass._zone);
				c.MoveImmediate(EClass.pc.pos.GetNearestPoint(allowBlock: false, allowChara: false) ?? EClass.pc.pos);
			}
			EClass.pc.party.AddMemeber(c);
		}
		else
		{
			if (c.IsPC)
			{
				SE.Beep();
				return;
			}
			EClass.pc.party.RemoveMember(c);
			if (c.homeZone != EClass._zone)
			{
				c.MoveZone(c.homeZone);
			}
		}
		MoveToOther(c);
		base.Main.OnRefreshMenu();
	}

	public override void OnList()
	{
		if (main)
		{
			foreach (Chara value in EClass.game.cards.globalCharas.Values)
			{
				if (value.IsPCFaction && !value.IsPCParty && value.memberType == FactionMemberType.Default)
				{
					list.Add(value);
				}
			}
			return;
		}
		foreach (Chara member in EClass.pc.party.members)
		{
			list.Add(member);
		}
		list.sortMode = UIList.SortMode.ByNone;
	}

	public HireInfo GetInfo(Chara c)
	{
		return EClass.Home.listReserve.First((HireInfo a) => a.chara == c);
	}
}
```

:::
## LoveData

[`public class LoveData : EClass`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/LoveData.cs#L17-L21)
```cs:line-numbers=17
	[JsonProperty]
	public bool gaveCutter;

	[JsonProperty] // [!code ++]
	public bool gotMusicBox; // [!code ++]
 // [!code ++]
	public bool IsWed => dateWedding != 0;
}
```

## Party

[`public int Count()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/Party.cs#L219-L222)
```cs:line-numbers=219
		}
		return num;
	}
 // [!code ++]
	public void Disband() // [!code ++]
	{ // [!code ++]
		foreach (Chara item in members.Copy()) // [!code ++]
		{ // [!code ++]
			if (!item.IsPC) // [!code ++]
			{ // [!code ++]
				RemoveMember(item); // [!code ++]
				if (item.homeZone != EClass._zone) // [!code ++]
				{ // [!code ++]
					item.MoveZone(item.homeZone); // [!code ++]
				} // [!code ++]
			} // [!code ++]
		} // [!code ++]
	} // [!code ++]
}
```

## Player

[`public class Stats : EClass`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/Player.cs#L134-L139)
```cs:line-numbers=134
		[JsonProperty]
		public int angryCats;

		[JsonProperty] // [!code ++]
		public int escapeSilverBell; // [!code ++]
 // [!code ++]
		[JsonProperty]
		public long shipMoney;

```

## Religion

[`public int GetOfferingValue(Thing t, int num = -1)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/Religion.cs#L213-L219)
```cs:line-numbers=213
		void SetValue(SourceCategory.Row cat, int mtp)
		{
			v = Mathf.Clamp(t.SelfWeight / 10, 50, 1000);
			v *= cat.offer * mtp / 100; // [!code --]
			v = v * cat.offer * mtp / 100; // [!code ++]
		}
	}

```

[`public void JoinFaith(Chara c, ConvertType type = ConvertType.Default)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/Religion.cs#L465-L470)
```cs:line-numbers=465
			}
		}
		EClass.pc.PurgeEythArtifact();
		if (this is ReligionHealing) // [!code ++]
		{ // [!code ++]
			Steam.GetAchievement(ID_Achievement.FAITH_HEALING); // [!code ++]
		} // [!code ++]
	}

	public void LeaveFaith(Chara c, Religion newFaith, ConvertType type)
```

## Trait

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/Trait.cs#L1609-L1614)
```cs:line-numbers=1609
				break;
			}
		}
		Steam.GetAchievement((owner.trait is TraitKettle) ? ID_Achievement.KETTLE : ID_Achievement.DEMITAS); // [!code ++]
		break;
	}
	case ShopType.Specific:
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/Trait.cs#L1685-L1690)
```cs:line-numbers=1685
		Add("board_map", 1, 0);
		Add("board_build", 1, 0);
		Add("book_resident", 1, 0);
		Add("board_party", 1, 0); // [!code ++]
		Add("3", 1, 0);
		Add("4", 1, 0);
		Add("5", 1, 0);
```

## TraitGene

[`public override void WriteNote(UINote n, bool identified)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/TraitGene.cs#L40-L46)
```cs:line-numbers=40

	public override void TrySetHeldAct(ActPlan p)
	{
		if (!p.pos.Equals(EClass.pc.pos) || !EClass.pc.HasElement(1274)) // [!code --]
		if (owner.c_DNA == null || !p.pos.Equals(EClass.pc.pos) || !EClass.pc.HasElement(1274)) // [!code ++]
		{
			return;
		}
```

## +TraitPartyBoard

::: details File Created
```cs
public class TraitPartyBoard : TraitBoard
{
	public override bool IsHomeItem => true;

	public override void TrySetAct(ActPlan p)
	{
		if (EClass._zone.IsPCFaction)
		{
			p.TrySetAct("LayerPeople", () => LayerPeople.CreateParty(), owner);
		}
	}
}
```

:::
## UIMultiList

[`public void Build(UIList.SortMode m = UIList.SortMode.ByNone)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/UIMultiList.cs#L42-L48)
```cs:line-numbers=42
{
	foreach (ListOwner owner in owners)
	{
		owner.list.sortMode = m; // [!code --]
		owner.list.sortMode = ((owner is ListPeopleParty) ? UIList.SortMode.ByFeat : m); // [!code ++]
	}
	if (Double)
	{
```

## WindowChara

[`public static void RefreshNote(Chara chara, UINote n, bool shortMode = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/dc355cfc13cab3246d4ddc2053f254b7b99028c3/Elin/WindowChara.cs#L989-L995)
```cs:line-numbers=989
			foreach (Chara item in list)
			{
				Date date = Date.ToDate(item.c_love.dateMarriage);
				AddText("item_marriage".lang(item.NameBraced, date.year.ToString() ?? "", date.month.ToString() ?? "", item.c_love.nameZoneMarriage) + (item.c_love.IsWed ? "already_wed".lang() : "")); // [!code --]
				AddText("item_marriage".lang(item.NameBraced, date.year.ToString() ?? "", date.month.ToString() ?? "", item.c_love.nameZoneMarriage, date.day.ToString() ?? "") + (item.c_love.IsWed ? "already_wed".lang() : "")); // [!code ++]
			}
		}
	}
```
