---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 12 files modified. 1 new file created.
version: EA 23.329 Nightly
changes: ActEffect/BaseCondition/BaseSong/CINT/Chara/ConDim/+ConSongValor/DNA/FortuneRollData/GoalCombat/StanceSongValor/Trait/TraitTrolley
---

# EA 23.329 Nightly

July 21, 2026

12 files modified. 1 new file created.

## Important Changes

**None.**
## ActEffect

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L1548-L1554)
```cs:line-numbers=1548
		EClass.ui.AddLayer<LayerList>().SetStringList(delegate
		{
			List<string> list12 = new List<string>();
			for (int num20 = 0; num20 < 10; num20++) // [!code --]
			for (int num21 = 0; num21 < 10; num21++) // [!code ++]
			{
				list12.Add(AliasGen.GetRandomAlias());
			}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L1693-L1707)
```cs:line-numbers=1693
		break;
	case EffectId.StripBlessing:
	{
		List<Condition> list4 = new List<Condition>(); // [!code --]
		List<Condition> list6 = new List<Condition>(); // [!code ++]
		foreach (Condition condition4 in TC.conditions)
		{
			if (GetBlessingDifficulty(condition4) > 0 && EClass.rnd(GetBlessingDifficulty(condition4)) == 0)
			{
				list4.Add(condition4); // [!code --]
				list6.Add(condition4); // [!code ++]
			}
		}
		if (list4.Count == 0) // [!code --]
		if (list6.Count == 0) // [!code ++]
		{
			CC.SayNothingHappans();
			break;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L1709-L1717)
```cs:line-numbers=1709
		TC.pos.PlayEffect("holyveil");
		TC.pos.PlaySound("holyveil");
		TC.Say("unpolluted", TC);
		list4.Shuffle(); // [!code --]
		list6.Shuffle(); // [!code ++]
		{
			foreach (Condition item5 in list4) // [!code --]
			foreach (Condition item5 in list6) // [!code ++]
			{
				item5.Kill();
				if (CC.IsHostile(TC))
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L1728-L1742)
```cs:line-numbers=1728
		{
			break;
		}
		int num12 = 0; // [!code --]
		int num15 = 0; // [!code ++]
		foreach (Condition condition5 in TC.conditions)
		{
			if (condition5.Type == ConditionType.Debuff)
			{
				num12++; // [!code --]
				num15++; // [!code ++]
			}
		}
		if (num12 == 0) // [!code --]
		if (num15 == 0) // [!code ++]
		{
			CC.SayNothingHappans();
			break;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L1753-L1766)
```cs:line-numbers=1753
		});
		TC.Say("abShutterHex", TC);
		Point center = CC.pos.Copy();
		List<Chara> list5 = TC.pos.ListCharasInRadius(TC, 4, (Chara c) => c == TC || c.IsHostile(CC)); // [!code --]
		for (int l = 0; l < num12; l++) // [!code --]
		List<Chara> list9 = TC.pos.ListCharasInRadius(TC, 4, (Chara c) => c == TC || c.IsHostile(CC)); // [!code ++]
		for (int m = 0; m < num15; m++) // [!code ++]
		{
			TweenUtil.Delay((float)l * 0.1f, delegate // [!code --]
			TweenUtil.Delay((float)m * 0.1f, delegate // [!code ++]
			{
				center.PlaySound("shutterhex");
			});
			foreach (Chara item6 in list5) // [!code --]
			foreach (Chara item6 in list9) // [!code ++]
			{
				if (item6.ExistsOnMap)
				{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L1769-L1780)
```cs:line-numbers=1769
					Color startColor = (componentInChildren.endColor = EClass.Colors.elementColors["eleHoly"]);
					componentInChildren.startColor = startColor;
					Point pos = item6.pos.Copy();
					TweenUtil.Delay((float)l * 0.1f, delegate // [!code --]
					TweenUtil.Delay((float)m * 0.1f, delegate // [!code ++]
					{
						effect.Play(center, 0f, pos);
					});
					int num13 = Dice.Create("SpShutterHex", power, CC, (actRef.refThing != null) ? null : actRef.act).Roll(); // [!code --]
					item6.DamageHP(num13, 919, power, AttackSource.None, CC, showEffect: false); // [!code --]
					int num16 = Dice.Create("SpShutterHex", power, CC, (actRef.refThing != null) ? null : actRef.act).Roll(); // [!code ++]
					item6.DamageHP(num16, 919, power, AttackSource.None, CC, showEffect: false); // [!code ++]
				}
			}
		}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L1829-L1854)
```cs:line-numbers=1829
			TC.Say((actRef.n1 == "money") ? "abStealNegateMoney" : "abStealNegate", TC);
			break;
		}
		Thing thing7 = null; // [!code --]
		bool flag12 = actRef.n1 == "food"; // [!code --]
		Thing thing5 = null; // [!code ++]
		bool flag11 = actRef.n1 == "food"; // [!code ++]
		if (actRef.n1 == "money")
		{
			int currency = TC.GetCurrency();
			if (currency > 0)
			{
				currency = Mathf.Clamp(EClass.rnd(currency / 10), 1, 100 + EClass.rndHalf(Mathf.Min(CC.LV, 1000000) * 200));
				thing7 = ThingGen.Create("money").SetNum(currency); // [!code --]
				thing5 = ThingGen.Create("money").SetNum(currency); // [!code ++]
				TC.ModCurrency(-currency);
			}
		}
		else
		{
			Func<Thing, bool> func = (Thing t) => true;
			if (flag12) // [!code --]
			if (flag11) // [!code ++]
			{
				func = (Thing t) => t.IsFood;
			}
			List<Thing> list9 = TC.things.List(delegate(Thing t) // [!code --]
			List<Thing> list7 = TC.things.List(delegate(Thing t) // [!code ++]
			{
				if (t.parentCard?.trait is TraitChestMerchant || t.trait is TraitTool || t.IsThrownWeapon)
				{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L1856-L1885)
```cs:line-numbers=1856
				}
				return t.trait.CanBeDestroyed && t.things.Count == 0 && t.invY != 1 && t.trait.CanBeStolen && !t.trait.CanOnlyCarry && !t.IsUnique && !t.isEquipped && t.blessedState == BlessedState.Normal && func(t);
			}, onlyAccessible: true);
			if (list9.Count > 0) // [!code --]
			if (list7.Count > 0) // [!code ++]
			{
				thing7 = list9.RandomItem(); // [!code --]
				if (thing7.Num > 1) // [!code --]
				thing5 = list7.RandomItem(); // [!code ++]
				if (thing5.Num > 1) // [!code ++]
				{
					thing7 = thing7.Split(1); // [!code --]
					thing5 = thing5.Split(1); // [!code ++]
				}
			}
			CC.AddCooldown(6640, 200);
		}
		if (thing7 == null) // [!code --]
		if (thing5 == null) // [!code ++]
		{
			CC.Say("abStealNothing", CC, TC);
			break;
		}
		thing7.SetInt(116, 1); // [!code --]
		TC.PlaySound(thing7.material.GetSoundDrop(thing7.sourceCard)); // [!code --]
		CC.Pick(thing7, msg: false); // [!code --]
		CC.Say("abSteal", CC, TC, thing7.Name); // [!code --]
		thing5.SetInt(116, 1); // [!code ++]
		TC.PlaySound(thing5.material.GetSoundDrop(thing5.sourceCard)); // [!code ++]
		CC.Pick(thing5, msg: false); // [!code ++]
		CC.Say("abSteal", CC, TC, thing5.Name); // [!code ++]
		if (actRef.n1 == "food")
		{
			if (CC.hunger.value != 0)
			{
				CC.InstantEat(thing7); // [!code --]
				CC.InstantEat(thing5); // [!code ++]
			}
		}
		else
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L1904-L1910)
```cs:line-numbers=1904
		{
			break;
		}
		List<Thing> list7 = TC.things.List(delegate(Thing t) // [!code --]
		List<Thing> list11 = TC.things.List(delegate(Thing t) // [!code ++]
		{
			if (!t.isEquipped || t.blessedState == BlessedState.Doomed || t.IsToolbelt)
			{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L1912-L1926)
```cs:line-numbers=1912
			}
			return (t.blessedState < BlessedState.Blessed || EClass.rnd(10) == 0) ? true : false;
		});
		if (list7.Count == 0) // [!code --]
		if (list11.Count == 0) // [!code ++]
		{
			CC.SayNothingHappans();
			break;
		}
		Thing thing5 = list7.RandomItem(); // [!code --]
		TC.Say("curse_hit", TC, thing5); // [!code --]
		thing5.SetBlessedState((thing5.blessedState == BlessedState.Cursed) ? BlessedState.Doomed : BlessedState.Cursed); // [!code --]
		LayerInventory.SetDirty(thing5); // [!code --]
		Thing thing7 = list11.RandomItem(); // [!code ++]
		TC.Say("curse_hit", TC, thing7); // [!code ++]
		thing7.SetBlessedState((thing7.blessedState == BlessedState.Cursed) ? BlessedState.Doomed : BlessedState.Cursed); // [!code ++]
		LayerInventory.SetDirty(thing7); // [!code ++]
		break;
	}
	case EffectId.UncurseEQ:
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L1939-L1964)
```cs:line-numbers=1939
		List<Thing> list = new List<Thing>();
		TC.things.Foreach(delegate(Thing t)
		{
			int num19 = 0; // [!code --]
			int num20 = 0; // [!code ++]
			if ((t.isEquipped || t.IsRangedWeapon || blessed) && t.blessedState < BlessedState.Normal)
			{
				if (t.blessedState == BlessedState.Cursed)
				{
					num19 = EClass.rnd(200); // [!code --]
					num20 = EClass.rnd(200); // [!code ++]
				}
				if (t.blessedState == BlessedState.Doomed)
				{
					num19 = EClass.rnd(1000); // [!code --]
					num20 = EClass.rnd(1000); // [!code ++]
				}
				if (blessed)
				{
					num19 /= 2; // [!code --]
					num20 /= 2; // [!code ++]
				}
				if (id == EffectId.UncurseEQGreater)
				{
					num19 /= 10; // [!code --]
					num20 /= 10; // [!code ++]
				}
				if (power >= num19) // [!code --]
				if (power >= num20) // [!code ++]
				{
					TC.Say("uncurseEQ_success", t);
					t.SetBlessedState(BlessedState.Normal);
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L2026-L2037)
```cs:line-numbers=2026
	{
		EClass.game.religions.Trickery.Talk("ability");
		bool hex = CC.IsHostile(TC);
		List<SourceStat.Row> list6 = EClass.sources.stats.rows.Where((SourceStat.Row con) => con.tag.Contains("random") && con.group == (hex ? "Debuff" : "Buff")).ToList(); // [!code --]
		List<SourceStat.Row> list8 = EClass.sources.stats.rows.Where((SourceStat.Row con) => con.tag.Contains("random") && con.group == (hex ? "Debuff" : "Buff")).ToList(); // [!code ++]
		int power2 = power;
		for (int m = 0; m < 4 + EClass.rnd(2); m++) // [!code --]
		for (int l = 0; l < 4 + EClass.rnd(2); l++) // [!code ++]
		{
			SourceStat.Row row2 = list6.RandomItem(); // [!code --]
			list6.Remove(row2); // [!code --]
			SourceStat.Row row2 = list8.RandomItem(); // [!code ++]
			list8.Remove(row2); // [!code ++]
			Proc(hex ? EffectId.DebuffKizuami : EffectId.Buff, CC, TC, power2, new ActRef
			{
				n1 = row2.alias
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L2053-L2086)
```cs:line-numbers=2053
		{
			power = power * 2 / 3;
		}
		int num14 = TC.WIL * (isPowerful ? 20 : 5); // [!code --]
		int num12 = TC.WIL * (isPowerful ? 20 : 5); // [!code ++]
		ConHolyVeil condition = TC.GetCondition<ConHolyVeil>();
		if (condition != null)
		{
			num14 += condition.power * 5; // [!code --]
			num12 += condition.power * 5; // [!code ++]
		}
		num14 /= EClass.sources.stats.alias[n].hexPower; // [!code --]
		if (id != EffectId.DebuffKizuami && EClass.rnd(power) < num14) // [!code --]
		num12 /= EClass.sources.stats.alias[n].hexPower; // [!code ++]
		if (id != EffectId.DebuffKizuami && EClass.rnd(power) < num12) // [!code ++]
		{
			int num15 = 10; // [!code --]
			int num16 = (TC.IsPowerful ? 40 : 90); // [!code --]
			int num13 = 10; // [!code ++]
			int num14 = (TC.IsPowerful ? 40 : 90); // [!code ++]
			if (CC.IsPCFaction)
			{
				num15 += EClass.curve(CC.LER, 100, 50) / 10; // [!code --]
				num13 += EClass.curve(CC.LER, 100, 50) / 10; // [!code ++]
			}
			if (TC.IsPowerful)
			{
				num15 /= 2; // [!code --]
				num13 /= 2; // [!code ++]
			}
			if (condition != null)
			{
				num15 /= 5; // [!code --]
				num13 /= 5; // [!code ++]
			}
			if (num15 >= num16) // [!code --]
			if (num13 >= num14) // [!code ++]
			{
				num15 = num16; // [!code --]
				num13 = num14; // [!code ++]
			}
			if (EClass.rnd(100) > num15) // [!code --]
			if (EClass.rnd(100) > num13) // [!code ++]
			{
				TC.Say("debuff_resist", TC);
				CC.DoHostileAction(TC);
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L2310-L2325)
```cs:line-numbers=2310
		break;
	case EffectId.Revive:
	{
		List<KeyValuePair<int, Chara>> list11 = EClass.game.cards.globalCharas.Where((KeyValuePair<int, Chara> a) => a.Value.isDead && a.Value.faction == EClass.pc.faction && !a.Value.isSummon && a.Value.c_wasInPcParty).ToList(); // [!code --]
		List<KeyValuePair<int, Chara>> list4 = EClass.game.cards.globalCharas.Where((KeyValuePair<int, Chara> a) => a.Value.isDead && a.Value.faction == EClass.pc.faction && !a.Value.isSummon && a.Value.c_wasInPcParty).ToList(); // [!code ++]
		if (TC.IsPCFaction || TC.IsPCFactionMinion)
		{
			if (TC.IsPC && list11.Count == 0) // [!code --]
			if (TC.IsPC && list4.Count == 0) // [!code ++]
			{
				list11 = EClass.game.cards.globalCharas.Where((KeyValuePair<int, Chara> a) => a.Value.CanRevive() && a.Value.isDead && a.Value.faction == EClass.pc.faction && !a.Value.isSummon).ToList(); // [!code --]
				list4 = EClass.game.cards.globalCharas.Where((KeyValuePair<int, Chara> a) => a.Value.CanRevive() && a.Value.isDead && a.Value.faction == EClass.pc.faction && !a.Value.isSummon).ToList(); // [!code ++]
			}
			if (list11.Count > 0) // [!code --]
			if (list4.Count > 0) // [!code ++]
			{
				list11.RandomItem().Value.Chara.GetRevived(); // [!code --]
				list4.RandomItem().Value.Chara.GetRevived(); // [!code ++]
				break;
			}
		}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L2332-L2344)
```cs:line-numbers=2332
	case EffectId.DamageMindGreat:
	case EffectId.Weaken:
	{
		bool flag9 = id == EffectId.DamageBody || id == EffectId.DamageBodyGreat; // [!code --]
		bool flag10 = id == EffectId.DamageBody || id == EffectId.DamageBodyGreat; // [!code ++]
		bool mind = id == EffectId.DamageMind || id == EffectId.DamageMindGreat;
		int num9 = ((id == EffectId.DamageBody || id == EffectId.DamageMind) ? 1 : (4 + EClass.rnd(4)));
		if (id == EffectId.Weaken)
		{
			flag9 = EClass.rnd(2) == 0; // [!code --]
			mind = !flag9; // [!code --]
			flag10 = EClass.rnd(2) == 0; // [!code ++]
			mind = !flag10; // [!code ++]
			num9 = 1;
		}
		else
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L2346-L2355)
```cs:line-numbers=2346
			TC.PlayEffect("debuff");
			TC.PlaySound("debuff");
		}
		TC.Say(flag9 ? "damageBody" : "damageMind", TC); // [!code --]
		TC.Say(flag10 ? "damageBody" : "damageMind", TC); // [!code ++]
		for (int k = 0; k < num9; k++)
		{
			TC.DamageTempElements(power, flag9, mind, id != EffectId.Weaken); // [!code --]
			TC.DamageTempElements(power, flag10, mind, id != EffectId.Weaken); // [!code ++]
		}
		if (TC.IsPC)
		{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L2362-L2395)
```cs:line-numbers=2362
	case EffectId.EnhanceBodyGreat:
	case EffectId.EnhanceMindGreat:
	{
		bool flag11 = id == EffectId.EnhanceBody || id == EffectId.EnhanceBodyGreat; // [!code --]
		bool flag12 = id == EffectId.EnhanceBody || id == EffectId.EnhanceBodyGreat; // [!code ++]
		bool mind2 = id == EffectId.EnhanceMind || id == EffectId.EnhanceMindGreat;
		if (id != EffectId.EnhanceBody && id != EffectId.EnhanceMind)
		{
			EClass.rnd(4);
		}
		TC.Say(flag11 ? "enhanceBody" : "enhanceMind", TC); // [!code --]
		TC.Say(flag12 ? "enhanceBody" : "enhanceMind", TC); // [!code ++]
		TC.PlayEffect("buff");
		TC.PlaySound("buff");
		TC.EnhanceTempElements(power, flag11, mind2, onlyRenew: true); // [!code --]
		TC.EnhanceTempElements(power, flag12, mind2, onlyRenew: true); // [!code ++]
		break;
	}
	case EffectId.RestoreBody:
	case EffectId.RestoreMind:
	{
		bool flag10 = id == EffectId.RestoreBody; // [!code --]
		bool flag9 = id == EffectId.RestoreBody; // [!code ++]
		if (flag)
		{
			Redirect(flag10 ? EffectId.DamageBodyGreat : EffectId.DamageMindGreat, BlessedState.Normal, default(ActRef)); // [!code --]
			Redirect(flag9 ? EffectId.DamageBodyGreat : EffectId.DamageMindGreat, BlessedState.Normal, default(ActRef)); // [!code ++]
			break;
		}
		TC.Say(flag10 ? "restoreBody" : "restoreMind", TC); // [!code --]
		TC.Say(flag9 ? "restoreBody" : "restoreMind", TC); // [!code ++]
		TC.PlaySound("heal");
		TC.PlayEffect("heal");
		TC.CureHost(flag10 ? CureType.CureBody : CureType.CureMind, power, state); // [!code --]
		TC.CureHost(flag9 ? CureType.CureBody : CureType.CureMind, power, state); // [!code ++]
		if (blessed)
		{
			Redirect(flag10 ? EffectId.EnhanceBodyGreat : EffectId.EnhanceMindGreat, BlessedState.Normal, default(ActRef)); // [!code --]
			Redirect(flag9 ? EffectId.EnhanceBodyGreat : EffectId.EnhanceMindGreat, BlessedState.Normal, default(ActRef)); // [!code ++]
		}
		break;
	}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L2444-L2454)
```cs:line-numbers=2444
			Redirect(EffectId.CurseEQ, BlessedState.Normal, default(ActRef));
			break;
		}
		IList<Condition> list10 = TC.conditions.Copy(); // [!code --]
		list10.Shuffle(); // [!code --]
		IList<Condition> list5 = TC.conditions.Copy(); // [!code ++]
		list5.Shuffle(); // [!code ++]
		CC.PlaySound("remove_hex");
		CC.PlayEffect("remove_hex");
		foreach (Condition item8 in list10) // [!code --]
		foreach (Condition item8 in list5) // [!code ++]
		{
			if (item8.Type != ConditionType.Debuff || item8.IsKilled)
			{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L2563-L2570)
```cs:line-numbers=2563
		if (TC.HasElement(1211))
		{
			TC.Say("drinkSaltWater_snail", TC);
			int num17 = ((TC.hp > 10) ? (TC.hp - EClass.rnd(10)) : 10000); // [!code --]
			TC.DamageHP(num17, AttackSource.None, CC); // [!code --]
			int num19 = ((TC.hp > 10) ? (TC.hp - EClass.rnd(10)) : 10000); // [!code ++]
			TC.DamageHP(num19, AttackSource.None, CC); // [!code ++]
		}
		else if (TC.IsPC)
		{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L2691-L2704)
```cs:line-numbers=2691
		{
			TC.PlayEffect("buff");
			TC.PlaySound("offering");
			int num18 = TC.Evalue(1423); // [!code --]
			if (flag && num18 > 1) // [!code --]
			{ // [!code --]
				TC.SetFeat(1423, num18 - 1, msg: true); // [!code --]
			} // [!code --]
			else if (num18 < 15) // [!code --]
			int num17 = TC.Evalue(1423); // [!code ++]
			if (num17 > 0) // [!code ++]
			{
				TC.SetFeat(1423, Mathf.Min(num18 + ((!blessed) ? 1 : 2), 15), msg: true); // [!code --]
				int num18 = 0; // [!code ++]
				if (flag && num17 > 1) // [!code ++]
				{ // [!code ++]
					num18 = -1; // [!code ++]
				} // [!code ++]
				else if (num17 < 15) // [!code ++]
				{ // [!code ++]
					num18 = ((!blessed) ? 1 : 2); // [!code ++]
					if (num18 + num17 > 15) // [!code ++]
					{ // [!code ++]
						num18 = 1; // [!code ++]
					} // [!code ++]
				} // [!code ++]
				if (num18 != 0) // [!code ++]
				{ // [!code ++]
					TC.SetFeat(1423, num17 + num18, msg: true); // [!code ++]
					TC.SetInt(134, TC.GetInt(134) + num18); // [!code ++]
				} // [!code ++]
			}
		}
		if (flag)
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ActEffect.cs#L2745-L2754)
```cs:line-numbers=2745
		{
			power /= 4;
		}
		List<Thing> list8 = TC.things.List((Thing t) => (t.Num <= 1 && t.IsEquipmentOrRanged && !t.IsToolbelt && !t.IsLightsource && t.isEquipped) ? true : false); // [!code --]
		if (list8.Count != 0) // [!code --]
		List<Thing> list10 = TC.things.List((Thing t) => (t.Num <= 1 && t.IsEquipmentOrRanged && !t.IsToolbelt && !t.IsLightsource && t.isEquipped) ? true : false); // [!code ++]
		if (list10.Count != 0) // [!code ++]
		{
			Thing thing6 = list8.RandomItem(); // [!code --]
			Thing thing6 = list10.RandomItem(); // [!code ++]
			TC.Say("acid_hit", TC);
			if (thing6.isAcidproof)
			{
```

## BaseCondition

[`public void SetElement(int id)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/BaseCondition.cs#L166-L171)
```cs:line-numbers=166
		refVal = id;
	}

	public void SetValue(int a) // [!code ++]
	{ // [!code ++]
		value = a; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void SetRefVal(int a, int b)
	{
		refVal = a;
```

## BaseSong

[`public override void OnStart()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/BaseSong.cs#L10-L34)
```cs:line-numbers=10

	public override void Tick()
	{
		if (EClass._zone.IsRegion) // [!code --]
		if (!EClass._zone.IsRegion && !owner.HasCondition<ConSilence>()) // [!code ++]
		{
			return; // [!code --]
			Element element = owner.elements.GetElement(IdAbility); // [!code ++]
			if (element != null) // [!code ++]
			{ // [!code ++]
				owner.elements.ModExp(element.id, 20f); // [!code ++]
			} // [!code ++]
			if (EClass.rnd(100) == 0) // [!code ++]
			{ // [!code ++]
				owner.elements.ModExp(241, owner.IsPC ? 10 : 100); // [!code ++]
			} // [!code ++]
			TickSong(); // [!code ++]
		}
		if (owner.HasCondition<ConSilence>()) // [!code --]
		{ // [!code --]
			Kill(); // [!code --]
			return; // [!code --]
		} // [!code --]
		Element element = owner.elements.GetElement(IdAbility); // [!code --]
		if (element != null) // [!code --]
		{ // [!code --]
			owner.elements.ModExp(element.id, 20f); // [!code --]
		} // [!code --]
		if (EClass.rnd(10) == 0) // [!code --]
		{ // [!code --]
			owner.elements.ModExp(241, owner.IsPC ? 10 : 100); // [!code --]
		} // [!code --]
		TickSong(); // [!code --]
	}

	public virtual void TickSong()
```

## CINT

[`public class CINT`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/CINT.cs#L183-L186)
```cs:line-numbers=183
	public const int isAlreadyCaptured = 132;

	public const int uidAttune = 133;
 // [!code ++]
	public const int peakyAdded = 134; // [!code ++]
}
```

## Chara

[`public void CureCondition<T>(int v = 99999) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/Chara.cs#L9986-L9991)
```cs:line-numbers=9986
		}
	}

	public BaseCondition GetCondition(string idCon) // [!code ++]
	{ // [!code ++]
		for (int i = 0; i < conditions.Count; i++) // [!code ++]
		{ // [!code ++]
			if (conditions[i].source.alias == idCon) // [!code ++]
			{ // [!code ++]
				return conditions[i]; // [!code ++]
			} // [!code ++]
		} // [!code ++]
		return null; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public override T GetCondition<T>()
	{
		for (int i = 0; i < conditions.Count; i++)
```

## ConDim

[`public class ConDim : BadCondition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/ConDim.cs#L1-L4)
```cs:line-numbers=1
public class ConDim : BadCondition
{
	public override bool ConsumeTurn => GetPhase() >= 1; // [!code --]
	public override bool ConsumeTurn => GetPhase() >= 2; // [!code ++]
}
```

## +ConSongValor

::: details File Created
```cs
public class ConSongValor : BaseBuff
{
}
```

:::
## DNA

[`public void Apply(Chara c, bool reverse)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/DNA.cs#L222-L229)
```cs:line-numbers=222
			}
			break;
		case "feat":
			c.SetFeat(num, c.elements.ValueWithoutLink(num) + ((!reverse) ? 1 : (-1)), !reverse); // [!code --]
		{ // [!code ++]
			int num3 = c.elements.ValueWithoutLink(num) + ((!reverse) ? 1 : (-1)); // [!code ++]
			if (num3 < 0) // [!code ++]
			{ // [!code ++]
				num3 = 0; // [!code ++]
			} // [!code ++]
			c.SetFeat(num, num3, !reverse); // [!code ++]
			if (reverse && num == 1423) // [!code ++]
			{ // [!code ++]
				int @int = c.GetInt(134); // [!code ++]
				if (@int > 0) // [!code ++]
				{ // [!code ++]
					c.SetFeat(num, -@int); // [!code ++]
				} // [!code ++]
				c.SetInt(134); // [!code ++]
			} // [!code ++]
			break;
		} // [!code ++]
		case "ability":
			if (reverse)
			{
```

## FortuneRollData

[`public List<List<string>> GetPrizeList()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/FortuneRollData.cs#L184-L190)
```cs:line-numbers=184
		{
			"computer", "834", "1090", "goods_figure", "goods_canvas", "mb_1", "mb_2", "mb_3", "mb_4", "mb_5",
			"1174", "1085", "toilet", "714", "nobility", "plat", "1165", "mathammer", "medal", "bbq",
			"panty", "beehive", "ticket_resident", "lovepotion", "crystal_sun", "goods_crim", "ride_boat" // [!code --]
			"panty", "beehive", "ticket_resident", "lovepotion", "crystal_sun", "goods_crim", "ride_boat", "ride_surf" // [!code ++]
		},
		new List<string>
		{
```

## GoalCombat

[`public virtual bool TryUseAbility(int dist, bool beforeMove = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/GoalCombat.cs#L569-L584)
```cs:line-numbers=569
			break;
		case "taunt":
		{
			bool flag6 = owner.HasCondition<StanceTaunt>(); // [!code --]
			bool flag7 = tactics.source.taunt != -1 && 100 * owner.hp / owner.MaxHP >= tactics.source.taunt; // [!code --]
			num = ((flag6 && !flag7) ? 100 : ((!flag6 && flag7) ? 100 : 0)); // [!code --]
			bool flag9 = owner.HasCondition<StanceTaunt>(); // [!code ++]
			bool flag10 = tactics.source.taunt != -1 && 100 * owner.hp / owner.MaxHP >= tactics.source.taunt; // [!code ++]
			num = ((flag9 && !flag10) ? 100 : ((!flag9 && flag10) ? 100 : 0)); // [!code ++]
			break;
		}
		case "song":
		{
			bool flag10 = owner.HasCondition<BaseSong>(); // [!code --]
			bool flag11 = owner.mana.value > owner.mana.max / 3; // [!code --]
			num = ((flag10 && !flag11) ? 100 : ((!flag10 && flag11) ? 100 : 0)); // [!code --]
			BaseSong baseSong = owner.GetCondition(act.source.proc[1]) as BaseSong; // [!code ++]
			bool num2 = baseSong != null; // [!code ++]
			bool flag8 = num2 && act.GetPower(owner) > baseSong.power; // [!code ++]
			num = ((!num2 || flag8) ? 200 : 0); // [!code ++]
			break;
		}
		case "melee":
```

[`public virtual bool TryUseAbility(int dist, bool beforeMove = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/GoalCombat.cs#L654-L666)
```cs:line-numbers=654
			{
				continue;
			}
			bool flag9 = text == "dot"; // [!code --]
			if (flag9 && (owner.isRestrained || (tc != null && tc.IsRestrainedResident))) // [!code --]
			bool flag7 = text == "dot"; // [!code ++]
			if (flag7 && (owner.isRestrained || (tc != null && tc.IsRestrainedResident))) // [!code ++]
			{
				continue;
			}
			num = ((text == "attackMelee") ? tactics.P_Melee : tactics.P_Spell) + GetAttackMod(act);
			if (num > 0 && flag9) // [!code --]
			if (num > 0 && flag7) // [!code ++]
			{
				num += 10;
			}
```

[`public virtual bool TryUseAbility(int dist, bool beforeMove = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/GoalCombat.cs#L676-L688)
```cs:line-numbers=676
			{
				continue;
			}
			bool flag8 = act is ActBolt; // [!code --]
			if ((owner.pos.IsBlocked && (flag8 || act is ActBall || act is ActBreathe)) || !flag || (owner.IsPCParty && (EClass._zone.IsTown || EClass._zone.IsPCFaction)) || (act.id == 9150 && EClass._zone.IsPCFaction && owner.IsNeutralOrAbove())) // [!code --]
			bool flag6 = act is ActBolt; // [!code ++]
			if ((owner.pos.IsBlocked && (flag6 || act is ActBall || act is ActBreathe)) || !flag || (owner.IsPCParty && (EClass._zone.IsTown || EClass._zone.IsPCFaction)) || (act.id == 9150 && EClass._zone.IsPCFaction && owner.IsNeutralOrAbove())) // [!code ++]
			{
				continue;
			}
			GetNumEnemy(flag8 ? 6 : 5); // [!code --]
			if (numEnemy == 0 || (owner.IsPCFactionOrMinion && GetNumNeutral(flag8 ? 6 : 5) > 0)) // [!code --]
			GetNumEnemy(flag6 ? 6 : 5); // [!code ++]
			if (numEnemy == 0 || (owner.IsPCFactionOrMinion && GetNumNeutral(flag6 ? 6 : 5) > 0)) // [!code ++]
			{
				continue;
			}
```

[`public virtual bool TryUseAbility(int dist, bool beforeMove = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/GoalCombat.cs#L757-L768)
```cs:line-numbers=757
			{
				continue;
			}
			int num2 = EClass._zone.CountMinions(owner); // [!code --]
			if (num2 >= owner.MaxSummon) // [!code --]
			int num3 = EClass._zone.CountMinions(owner); // [!code ++]
			if (num3 >= owner.MaxSummon) // [!code ++]
			{
				continue;
			}
			num = tactics.P_Summon - 20 * num2 / owner.MaxSummon; // [!code --]
			num = tactics.P_Summon - 20 * num3 / owner.MaxSummon; // [!code ++]
			break;
		}
		case "summonAlly":
```

[`public virtual bool TryUseAbility(int dist, bool beforeMove = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/GoalCombat.cs#L882-L891)
```cs:line-numbers=882
			{
				foreach (Chara member2 in EClass.pc.party.members)
				{
					float num4 = 100f - (float)(member2.mana.value * 100) / MathF.Max(1f, member2.mana.max); // [!code --]
					if (num4 > (float)num) // [!code --]
					float num5 = 100f - (float)(member2.mana.value * 100) / MathF.Max(1f, member2.mana.max); // [!code ++]
					if (num5 > (float)num) // [!code ++]
					{
						num = (int)num4; // [!code --]
						num = (int)num5; // [!code ++]
					}
				}
			}
```

[`public virtual bool TryUseAbility(int dist, bool beforeMove = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/GoalCombat.cs#L903-L917)
```cs:line-numbers=903
			break;
		case 9200:
		{
			int num3 = 0; // [!code --]
			int num4 = 0; // [!code ++]
			foreach (Condition condition in tc.conditions)
			{
				if (condition.Type == ConditionType.Debuff)
				{
					num3++; // [!code --]
					num4++; // [!code ++]
				}
			}
			num = num3 * 15; // [!code --]
			num = num4 * 15; // [!code ++]
			break;
		}
		}
```

[`int HealFactor(Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/GoalCombat.cs#L967-L1002)
```cs:line-numbers=967
			{
				return 0;
			}
			float num7 = (float)c.hp / (float)c.MaxHP; // [!code --]
			if (num7 > (isHOT ? 0.85f : 0.75f)) // [!code --]
			float num8 = (float)c.hp / (float)c.MaxHP; // [!code ++]
			if (num8 > (isHOT ? 0.85f : 0.75f)) // [!code ++]
			{
				return 0;
			}
			int num8 = tactics.P_Heal - (int)((float)tactics.P_Heal * num7) + (isHOT ? 50 : 25); // [!code --]
			int num9 = tactics.P_Heal - (int)((float)tactics.P_Heal * num8) + (isHOT ? 50 : 25); // [!code ++]
			foreach (Condition condition2 in c.conditions)
			{
				if (condition2 is ConFear)
				{
					num8 += 10; // [!code --]
					num9 += 10; // [!code ++]
				}
				else if (condition2 is ConPoison)
				{
					num8 += 2; // [!code --]
					num9 += 2; // [!code ++]
				}
				else if (condition2 is ConConfuse)
				{
					num8 += 4; // [!code --]
					num9 += 4; // [!code ++]
				}
				else if (condition2 is ConDim)
				{
					num8 += 6; // [!code --]
					num9 += 6; // [!code ++]
				}
				else if (condition2 is ConBleed)
				{
					num8 += 8; // [!code --]
					num9 += 8; // [!code ++]
				}
			}
			return num8; // [!code --]
			return num9; // [!code ++]
		}
	}
	abilities.Sort((ItemAbility a, ItemAbility b) => b.priority - a.priority);
```

[`void BuildCharaList()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/GoalCombat.cs#L1093-L1100)
```cs:line-numbers=1093
		{
			if (chara2 != owner)
			{
				int num11 = owner.Dist(chara2); // [!code --]
				if (num11 > sightRadius || !owner.CanSeeLos(chara2, num11)) // [!code --]
				int num12 = owner.Dist(chara2); // [!code ++]
				if (num12 > sightRadius || !owner.CanSeeLos(chara2, num12)) // [!code ++]
				{
					continue;
				}
```

[`int ForeachChara(ItemAbility a, Func<Chara, int> func, bool isFriendlyAbility)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/GoalCombat.cs#L1110-L1120)
```cs:line-numbers=1110
			return func(owner);
		}
		BuildCharaList();
		int num5 = 0; // [!code --]
		int num6 = 0; // [!code ++]
		foreach (Chara chara3 in charas)
		{
			int num6 = func(chara3); // [!code --]
			if (num6 > 0) // [!code --]
			int num7 = func(chara3); // [!code ++]
			if (num7 > 0) // [!code ++]
			{
				if (isFriendlyAbility)
				{
```

[`int ForeachChara(ItemAbility a, Func<Chara, int> func, bool isFriendlyAbility)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/GoalCombat.cs#L1131-L1151)
```cs:line-numbers=1131
					}
					if (chara3 != owner)
					{
						num6 += tactics.P_Party; // [!code --]
						num7 += tactics.P_Party; // [!code ++]
					}
				}
				else if (!owner.IsHostile(chara3))
				{
					continue;
				}
				if (num6 >= num5) // [!code --]
				if (num7 >= num6) // [!code ++]
				{
					a.tg = chara3;
					num5 = num6; // [!code --]
					num6 = num7; // [!code ++]
				}
			}
		}
		return num5; // [!code --]
		return num6; // [!code ++]
	}
	int GetAttackMod(Act a)
	{
```

[`int GetAttackMod(Act a)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/GoalCombat.cs#L1153-L1190)
```cs:line-numbers=1153
		{
			return 0;
		}
		int num9 = ((a.source.aliasRef == "mold") ? owner.MainElement.id : EClass.sources.elements.alias[a.source.aliasRef].id); // [!code --]
		int num10 = -15 * tc.ResistLvFrom(num9); // [!code --]
		int num10 = ((a.source.aliasRef == "mold") ? owner.MainElement.id : EClass.sources.elements.alias[a.source.aliasRef].id); // [!code ++]
		int num11 = -15 * tc.ResistLvFrom(num10); // [!code ++]
		if (a is ActSword)
		{
			num10 = 0; // [!code --]
			num11 = 0; // [!code ++]
		}
		if (owner.HasElement(1238) && num10 < -15) // [!code --]
		if (owner.HasElement(1238) && num11 < -15) // [!code ++]
		{
			num10 = -15; // [!code --]
			num11 = -15; // [!code ++]
		}
		switch (num9) // [!code --]
		switch (num10) // [!code ++]
		{
		case 910:
			if (tc.isWet)
			{
				num10 -= 30; // [!code --]
				num11 -= 30; // [!code ++]
			}
			break;
		case 911:
			if (tc.HasCondition<ConBurning>())
			{
				num10 -= 30; // [!code --]
				num11 -= 30; // [!code ++]
			}
			break;
		case 912:
			if (tc.isWet)
			{
				num10 += 30; // [!code --]
				num11 += 30; // [!code ++]
			}
			break;
		}
		return num10; // [!code --]
		return num11; // [!code ++]
	}
	void GetNumEnemy(int radius)
	{
```

## StanceSongValor

[`public class StanceSongValor : BaseSong`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/StanceSongValor.cs#L4-L18)
```cs:line-numbers=4

	public override void TickSong()
	{
		foreach (Chara item in owner.pos.ListCharasInRadius(owner, 4, (Chara c) => (c.IsDeadOrSleeping || !owner.IsPCFactionOrMinion) ? (!c.IsHostile(owner)) : c.IsPCFactionOrMinion)) // [!code --]
		foreach (Chara item in owner.pos.ListCharasInRadius(owner, 4, (Chara c) => !c.IsDeadOrSleeping && ((!owner.IsPCFactionOrMinion) ? (!c.IsHostile(owner)) : c.IsPCFactionOrMinion))) // [!code ++]
		{
			if (!item.HasCondition<ConEuphoric>()) // [!code --]
			if (!item.HasCondition<ConSongValor>()) // [!code ++]
			{
				item.AddCondition<ConEuphoric>(base.power); // [!code --]
				item.AddCondition<ConSongValor>(base.power); // [!code ++]
			}
			if (!item.HasCondition<ConHero>() && (item.HasCondition<ConFear>() || item.HasCondition<ConConfuse>())) // [!code --]
			else // [!code ++]
			{
				item.AddCondition<ConHero>(base.power); // [!code --]
				item.GetCondition<ConSongValor>()?.SetValue(3); // [!code ++]
			}
		}
	}
```

## Trait

[`public virtual void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/Trait.cs#L2207-L2213)
```cs:line-numbers=2207
				break;
			case ShopType.Moyer:
			{
				for (int num14 = 1; num14 <= 24; num14++) // [!code --]
				for (int num14 = 1; num14 <= 25; num14++) // [!code ++]
				{
					AddAdvWeek(num14);
				}
```

## TraitTrolley

[`public virtual string GetIdSound()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8ce158cdbedef9c660447fd2876783633579946b/Elin/TraitTrolley.cs#L70-L75)
```cs:line-numbers=70
		return "ride_kin";
	case "ride_boat":
	case "ride_boat2":
	case "ride_surf": // [!code ++]
		return "ride_boat";
	case "wheel_chair":
		if (owner.idSkin != 1)
```
