---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 6 files modified.
version: EA 23.344.1 Nightly
changes: ActTelekinesis/Card/GrowSystemFlower/GrowSystemRose/TraitFigure/TraitStrangeStatue
---

# EA 23.344.1 Nightly

September 12, 2026

6 files modified.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [GrowSystemFlower (1)](#growsystemflower)
```cs:no-line-numbers
public override int GetStageTile() // [!code --]

```
## ActTelekinesis

[`public int GetWeightLimit()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/31dd3a2a5569e17be8f21a2cd23675dfdd70f97d/Elin/ActTelekinesis.cs#L94-L100)
```cs:line-numbers=94
	long num = GetPower(Act.CC);
	if (num * num <= int.MaxValue)
	{
		return (int)(num * num); // [!code --]
		return (int)Mathf.Max(num * num, 1f); // [!code ++]
	}
	return int.MaxValue;
}
```

## Card

[`public void SpawnLoot(Card origin)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/31dd3a2a5569e17be8f21a2cd23675dfdd70f97d/Elin/Card.cs#L5698-L5708)
```cs:line-numbers=5698
		}
	}
}
foreach (Thing thing7 in things) // [!code --]
if (!(trait is TraitPowerStatue)) // [!code ++]
{
	if (thing7.GetInt(116) != 0) // [!code --]
	foreach (Thing thing7 in things) // [!code ++]
	{
		list.Add(thing7); // [!code --]
		if (thing7.GetInt(116) != 0) // [!code ++]
		{ // [!code ++]
			list.Add(thing7); // [!code ++]
		} // [!code ++]
	}
}
Point nearestPoint = GetRootCard().pos;
```

## GrowSystemFlower

[`public override void OnSetObj()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/31dd3a2a5569e17be8f21a2cd23675dfdd70f97d/Elin/GrowSystemFlower.cs#L28-L36)
```cs:line-numbers=28
			GrowSystem.cell.objDir = EClass.rnd(4);
		}
	}
 // [!code --]
	public override int GetStageTile() // [!code --]
	{ // [!code --]
		return source._tiles[GrowSystem.cell.objDir % source._tiles.Length]; // [!code --]
	} // [!code --]
}
```

## GrowSystemRose

[`public class GrowSystemRose : GrowSystemFlower`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/31dd3a2a5569e17be8f21a2cd23675dfdd70f97d/Elin/GrowSystemRose.cs#L1-L4)
```cs:line-numbers=1
public class GrowSystemRose : GrowSystemFlower
{
	protected override bool UseGenericFirstStageTile => false;
 // [!code ++]
	public override int GetStageTile() // [!code ++]
	{ // [!code ++]
		if (base.stage.idx == HarvestStage && source.alias == "roseflower") // [!code ++]
		{ // [!code ++]
			return source._tiles[GrowSystem.cell.objDir % source._tiles.Length]; // [!code ++]
		} // [!code ++]
		return base.GetStageTile(); // [!code ++]
	} // [!code ++]
}
```

## TraitFigure

[`public override int GetValue()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/31dd3a2a5569e17be8f21a2cd23675dfdd70f97d/Elin/TraitFigure.cs#L39-L44)
```cs:line-numbers=39
public override void TrySetAct(ActPlan p)
{
	if (p.input != ActInput.AllAction || source.tiles.Length <= 1) // [!code --]
	if (p.input != ActInput.AllAction || source == null || source.tiles.Length <= 1) // [!code ++]
	{
		return;
	}
```

## TraitStrangeStatue

[`public override void OnDie()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/31dd3a2a5569e17be8f21a2cd23675dfdd70f97d/Elin/TraitStrangeStatue.cs#L9-L15)
```cs:line-numbers=9
			idSpawnList = "c_statue",
			filterLv = EClass._zone.DangerLv + 30
		});
		chara.SetHostility((EClass.rnd(3) != 0) ? Hostility.Enemy : Hostility.Neutral); // [!code --]
		chara.SetHostility((EClass.rnd(3) != 0 || chara.trait is TraitMerchant) ? Hostility.Enemy : Hostility.Neutral); // [!code ++]
		Msg.Say("statue_chara", chara, owner);
	}
}
```
