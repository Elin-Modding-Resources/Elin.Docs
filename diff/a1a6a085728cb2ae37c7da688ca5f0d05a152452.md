---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 14 files modified.
version: EA 23.286 Nightly Patch 1
changes: AI_Fuck/AI_Shear/AI_Slaughter/ActEffect/BaseTaskHarvest/Chara/InvOwnerChangeRarity/ModManager/Player/ShopTransaction/SourceImporter/TaskBuild/TaskPlow/TaskPoint
---

# EA 23.286 Nightly Patch 1

March 17, 2026

14 files modified.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [SourceImporter (1)](#sourceimporter)
```cs:no-line-numbers
private (string file, ISheet[] sheets, ISheet element) PrefetchWorkbook(string file) // [!code --]
private (string file, ISheet[] sheets, ISheet[] fetched) PrefetchWorkbook(string file, string[] prefetchNames) // [!code ++]
```
## AI_Fuck

[`static void SuccubusExp(Chara c, Chara tg)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/AI_Fuck.cs#L540-L543)
```cs:line-numbers=540
			}
		}
	}
 // [!code ++]
	public override void OnSetOwner() // [!code ++]
	{ // [!code ++]
		if (parent is AI_Goto aI_Goto) // [!code ++]
		{ // [!code ++]
			aI_Goto.ignoreConnection = true; // [!code ++]
		} // [!code ++]
	} // [!code ++]
}
```

## AI_Shear

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/AI_Shear.cs#L81-L86)
```cs:line-numbers=81
		yield return Do(seq);
	}

	public override void OnSetOwner() // [!code ++]
	{ // [!code ++]
		if (parent is AI_Goto aI_Goto) // [!code ++]
		{ // [!code ++]
			aI_Goto.ignoreConnection = true; // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static int GetFurLv(Chara c)
	{
		return Mathf.Clamp(c.c_fur / 10 + 1, 1, 5);
```

## AI_Slaughter

[`public override void OnCancelOrSuccess()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/AI_Slaughter.cs#L129-L132)
```cs:line-numbers=129
			target.SetCensored(enable: false);
		}
	}
 // [!code ++]
	public override void OnSetOwner() // [!code ++]
	{ // [!code ++]
		if (parent is AI_Goto aI_Goto) // [!code ++]
		{ // [!code ++]
			aI_Goto.ignoreConnection = true; // [!code ++]
		} // [!code ++]
	} // [!code ++]
}
```

## ActEffect

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1118-L1127)
```cs:line-numbers=1118
		{
			return;
		}
		Thing thing = ThingGen.Create("mine"); // [!code --]
		thing.c_idRefCard = "dog_mine"; // [!code --]
		Thing thing2 = ThingGen.Create("mine"); // [!code ++]
		thing2.c_idRefCard = "dog_mine"; // [!code ++]
		Zone.ignoreSpawnAnime = true;
		EClass._zone.AddCard(thing, CC.pos).Install(); // [!code --]
		EClass._zone.AddCard(thing2, CC.pos).Install(); // [!code ++]
		break;
	}
	case EffectId.LittleSisterMigration:
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1224-L1232)
```cs:line-numbers=1224
		TC.PlaySound("curse3");
		TC.PlayEffect("curse");
		TC.Say("forgetItems", TC);
		int num3 = power / 50 + 1 + EClass.rnd(3); // [!code --]
		int num2 = power / 50 + 1 + EClass.rnd(3); // [!code ++]
		List<Thing> source = TC.things.List((Thing t) => t.c_IDTState == 0);
		for (int j = 0; j < num3; j++) // [!code --]
		for (int i = 0; i < num2; i++) // [!code ++]
		{
			source.RandomItem().c_IDTState = 5;
		}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1238-L1247)
```cs:line-numbers=1238
	case EffectId.EnchantArmorGreat:
	{
		bool armor = id == EffectId.EnchantArmor || id == EffectId.EnchantArmorGreat;
		bool flag4 = id == EffectId.EnchantWeaponGreat || id == EffectId.EnchantArmorGreat; // [!code --]
		bool flag8 = id == EffectId.EnchantWeaponGreat || id == EffectId.EnchantArmorGreat; // [!code ++]
		if (!tc.isThing)
		{
			LayerDragGrid.CreateEnchant(CC, armor, flag4, state); // [!code --]
			LayerDragGrid.CreateEnchant(CC, armor, flag8, state); // [!code ++]
			return;
		}
		cc.PlaySound("identify");
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1252-L1259)
```cs:line-numbers=1252
			tc.ModEncLv(-1);
			break;
		}
		int num5 = (flag4 ? 4 : 2) + (blessed ? 1 : 0); // [!code --]
		if (tc.encLV >= num5) // [!code --]
		int num7 = (flag8 ? 4 : 2) + (blessed ? 1 : 0); // [!code ++]
		if (tc.encLV >= num7) // [!code ++]
		{
			cc.Say("enc_resist", tc);
			break;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1265-L1285)
```cs:line-numbers=1265
	case EffectId.Identify:
	case EffectId.GreaterIdentify:
	{
		bool flag2 = id == EffectId.GreaterIdentify; // [!code --]
		bool flag3 = id == EffectId.GreaterIdentify; // [!code ++]
		if (flag)
		{
			Redirect(EffectId.ForgetItems, flag2 ? BlessedState.Cursed : BlessedState.Normal, default(ActRef)); // [!code --]
			Redirect(EffectId.ForgetItems, flag3 ? BlessedState.Cursed : BlessedState.Normal, default(ActRef)); // [!code ++]
			break;
		}
		if (!tc.isThing)
		{
			int count = ((!blessed) ? 1 : (flag2 ? (2 + EClass.rnd(2)) : (3 + EClass.rnd(3)))); // [!code --]
			LayerDragGrid.CreateIdentify(CC, flag2, state, 0, count); // [!code --]
			int count = ((!blessed) ? 1 : (flag3 ? (2 + EClass.rnd(2)) : (3 + EClass.rnd(3)))); // [!code ++]
			LayerDragGrid.CreateIdentify(CC, flag3, state, 0, count); // [!code ++]
			return;
		}
		cc.PlaySound("identify");
		cc.PlayEffect("identify");
		tc.Thing.Identify(cc.IsPCParty, (!flag2) ? IDTSource.Identify : IDTSource.SuperiorIdentify); // [!code --]
		tc.Thing.Identify(cc.IsPCParty, (!flag3) ? IDTSource.Identify : IDTSource.SuperiorIdentify); // [!code ++]
		break;
	}
	case EffectId.Uncurse:
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1289-L1305)
```cs:line-numbers=1289
			LayerDragGrid.CreateUncurse(CC, state);
			return;
		}
		Thing thing3 = tc.Thing; // [!code --]
		if (thing3.blessedState == BlessedState.Cursed) // [!code --]
		Thing thing = tc.Thing; // [!code ++]
		if (thing.blessedState == BlessedState.Cursed) // [!code ++]
		{
			thing3.SetBlessedState(BlessedState.Normal); // [!code --]
			thing.SetBlessedState(BlessedState.Normal); // [!code ++]
		}
		else if (thing3.blessedState == BlessedState.Doomed) // [!code --]
		else if (thing.blessedState == BlessedState.Doomed) // [!code ++]
		{
			thing3.SetBlessedState(BlessedState.Normal); // [!code --]
			thing.SetBlessedState(BlessedState.Normal); // [!code ++]
		}
		thing3.GetRootCard()?.TryStack(thing3); // [!code --]
		LayerInventory.SetDirty(thing3); // [!code --]
		thing.GetRootCard()?.TryStack(thing); // [!code ++]
		LayerInventory.SetDirty(thing); // [!code ++]
		break;
	}
	case EffectId.Lighten:
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1315-L1336)
```cs:line-numbers=1315
		}
		cc.PlaySound("offering");
		cc.PlayEffect("buff");
		int num6 = (tc.isWeightChanged ? tc.c_weight : tc.Thing.source.weight); // [!code --]
		int num = (tc.isWeightChanged ? tc.c_weight : tc.Thing.source.weight); // [!code ++]
		tc.isWeightChanged = true;
		Element orCreateElement = tc.elements.GetOrCreateElement(64);
		Element orCreateElement2 = tc.elements.GetOrCreateElement(65);
		Element orCreateElement3 = tc.elements.GetOrCreateElement(67);
		Element orCreateElement4 = tc.elements.GetOrCreateElement(66);
		bool flag5 = tc.IsEquipmentOrRangedOrAmmo || tc.IsThrownWeapon; // [!code --]
		bool flag2 = tc.IsEquipmentOrRangedOrAmmo || tc.IsThrownWeapon; // [!code ++]
		if (flag)
		{
			num6 = (int)(0.01f * (float)num6 * (float)power * 0.75f + 500f); // [!code --]
			if (num6 < 0 || num6 > 10000000) // [!code --]
			num = (int)(0.01f * (float)num * (float)power * 0.75f + 500f); // [!code ++]
			if (num < 0 || num > 10000000) // [!code ++]
			{
				num6 = 10000000; // [!code --]
				flag5 = false; // [!code --]
				num = 10000000; // [!code ++]
				flag2 = false; // [!code ++]
			}
			if (flag5) // [!code --]
			if (flag2) // [!code ++]
			{
				if (tc.IsWeapon || tc.IsThrownWeapon || tc.IsAmmo)
				{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1347-L1358)
```cs:line-numbers=1347
		}
		else
		{
			num6 = num6 * (100 - power / 10) / 100; // [!code --]
			num = num * (100 - power / 10) / 100; // [!code ++]
			if (blessed)
			{
				power /= 4;
			}
			if (flag5) // [!code --]
			if (flag2) // [!code ++]
			{
				if (tc.IsWeapon || tc.IsThrownWeapon || tc.IsAmmo)
				{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1367-L1373)
```cs:line-numbers=1367
			}
			cc.Say("lighten", tc);
		}
		tc.c_weight = num6; // [!code --]
		tc.c_weight = num; // [!code ++]
		tc.SetDirtyWeight();
		if (tc.parent == null)
		{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1391-L1423)
```cs:line-numbers=1391
		cc.PlayEffect("identify");
		cc.Say("reconstruct", tc);
		EClass.game.cards.uidNext += EClass.rnd(30);
		int num4 = Mathf.Max(tc.genLv, tc.LV, EClass.player.stats.deepest); // [!code --]
		int num5 = Mathf.Max(tc.genLv, tc.LV, EClass.player.stats.deepest); // [!code ++]
		CardBlueprint.Set(new CardBlueprint
		{
			blesstedState = state
		});
		Thing thing2 = ThingGen.Create(tc.id, -1, (int)((long)num4 * (long)power / 400)); // [!code --]
		thing2.genLv = num4; // [!code --]
		Thing thing3 = ThingGen.Create(tc.id, -1, (int)((long)num5 * (long)power / 400)); // [!code ++]
		thing3.genLv = num5; // [!code ++]
		if (tc.c_uidAttune != 0)
		{
			thing2.c_uidAttune = tc.c_uidAttune; // [!code --]
			if (thing2.id == "amulet_engagement" || thing2.id == "ring_engagement") // [!code --]
			thing3.c_uidAttune = tc.c_uidAttune; // [!code ++]
			if (thing3.id == "amulet_engagement" || thing3.id == "ring_engagement") // [!code ++]
			{
				if (tc.c_uidAttune != EClass.pc.uid)
				{
					thing2.elements.ModBase(484, 3); // [!code --]
					thing3.elements.ModBase(484, 3); // [!code ++]
				}
				if (thing2.rarity < Rarity.Mythical) // [!code --]
				if (thing3.rarity < Rarity.Mythical) // [!code ++]
				{
					thing2.rarity = Rarity.Mythical; // [!code --]
					thing3.rarity = Rarity.Mythical; // [!code ++]
				}
			}
		}
		tc.Destroy();
		CC.Pick(thing2, msg: false); // [!code --]
		CC.Pick(thing3, msg: false); // [!code ++]
		if (!CC.IsPC)
		{
			CC.TryEquip(thing2); // [!code --]
			CC.TryEquip(thing3); // [!code ++]
		}
		break;
	}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1438-L1473)
```cs:line-numbers=1438
		string name = tc.Name;
		if (row == null)
		{
			bool num = id == EffectId.ChangeMaterialGreater; // [!code --]
			bool flag3 = id == EffectId.ChangeMaterialLesser; // [!code --]
			bool num3 = id == EffectId.ChangeMaterialGreater; // [!code ++]
			bool flag4 = id == EffectId.ChangeMaterialLesser; // [!code ++]
			string text2 = tc.Thing.source.tierGroup;
			Dictionary<string, SourceMaterial.TierList> tierMap = SourceMaterial.tierMap;
			int num2 = 1; // [!code --]
			int num4 = 1; // [!code ++]
			if (flag)
			{
				num2 -= 2; // [!code --]
				num4 -= 2; // [!code ++]
			}
			if (blessed)
			{
				num2++; // [!code --]
				num4++; // [!code ++]
			}
			if (num) // [!code --]
			if (num3) // [!code ++]
			{
				num2++; // [!code --]
				num4++; // [!code ++]
			}
			if (flag3) // [!code --]
			if (flag4) // [!code ++]
			{
				num2 -= 2; // [!code --]
				num4 -= 2; // [!code ++]
			}
			num2 = Mathf.Clamp(num2 + EClass.rnd(2), 0, 4); // [!code --]
			num4 = Mathf.Clamp(num4 + EClass.rnd(2), 0, 4); // [!code ++]
			if (EClass.rnd(10) == 0)
			{
				text2 = ((text2 == "metal") ? "leather" : "metal");
			}
			SourceMaterial.TierList tierList = (text2.IsEmpty() ? tierMap.RandomItem() : tierMap[text2]);
			for (int i = 0; i < 1000; i++) // [!code --]
			for (int j = 0; j < 1000; j++) // [!code ++]
			{
				row = tierList.tiers[num2].Select(); // [!code --]
				row = tierList.tiers[num4].Select(); // [!code ++]
				if (row != tc.material)
				{
					break;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1518-L1563)
```cs:line-numbers=1518
		string name2 = tc.Name;
		cc.PlaySound("offering");
		cc.PlayEffect("buff");
		bool flag5 = tc.rarity == Rarity.Legendary; // [!code ++]
		CardBlueprint.Set(new CardBlueprint
		{
			rarity = ((tc.rarity < Rarity.Legendary) ? Rarity.Legendary : Rarity.Mythical), // [!code --]
			rarity = (flag5 ? Rarity.Mythical : Rarity.Legendary), // [!code ++]
			generation = CardBlueprint.Generation.GarokkHammer
		});
		tc.Destroy(); // [!code --]
		Rand.SetBaseSeed(tc.uid); // [!code --]
		Rand.SetSeed(tc.uid); // [!code --]
		tc = ThingGen.Create(tc.id, tc.idMaterial, tc.genLv); // [!code --]
		Rand.SetBaseSeed(tc.uid + 1); // [!code ++]
		Rand.SetSeed(tc.uid + 2); // [!code ++]
		Thing thing4 = ThingGen.Create(tc.id, tc.idMaterial, tc.genLv); // [!code ++]
		Rand.SetSeed();
		Rand.SetBaseSeed();
		EClass._zone.AddCard(tc, cc.pos); // [!code --]
		int num6 = 0; // [!code ++]
		foreach (Element item4 in thing4.elements.dict.Values.ToList().Shuffle()) // [!code ++]
		{ // [!code ++]
			if (tc.elements.Has(item4.id) && tc.elements.GetElement(item4.id).vBase == item4.vBase) // [!code ++]
			{ // [!code ++]
				continue; // [!code ++]
			} // [!code ++]
			tc.elements.SetBase(item4.id, item4.vBase); // [!code ++]
			Debug.Log(item4.Name + "/" + item4.vBase); // [!code ++]
			int id2 = item4.id; // [!code ++]
			if ((uint)(id2 - 64) > 3u) // [!code ++]
			{ // [!code ++]
				num6++; // [!code ++]
				if (flag5) // [!code ++]
				{ // [!code ++]
					_ = 2; // [!code ++]
				} // [!code ++]
			} // [!code ++]
		} // [!code ++]
		tc.rarity = (flag5 ? Rarity.Mythical : Rarity.Legendary); // [!code ++]
		EClass.pc.Say("reconstruct", name2);
		EClass.pc.Pick(tc.Thing, msg: false); // [!code --]
		break;
	}
	case EffectId.ChangeAlias:
	{ // [!code ++]
		Chara c2 = CC; // [!code ++]
		EClass.ui.AddLayer<LayerList>().SetStringList(delegate
		{
			List<string> list11 = new List<string>();
			for (int num15 = 0; num15 < 10; num15++) // [!code --]
			for (int num16 = 0; num16 < 10; num16++) // [!code ++]
			{
				list11.Add(AliasGen.GetRandomAlias());
			}
			return list11;
		}, delegate(int a, string b)
		{
			EClass.pc._alias = b; // [!code --]
			EClass.pc.Say("reconstruct", EClass.pc); // [!code --]
			c2._alias = b; // [!code ++]
			c2.Say("reconstruct", c2); // [!code ++]
			SE.Change();
			EClass.pc.PlayEffect("mutation"); // [!code --]
			c2.PlayEffect("mutation"); // [!code ++]
		}).SetSize()
			.EnableReroll()
			.SetTitles("wAlias");
		break;
	} // [!code ++]
	case EffectId.ChangeBiography:
		EClass.pc.bio.RerollParents(); // [!code --]
		EClass.pc.Say("reconstruct", EClass.pc); // [!code --]
		CC.bio.RerollParents(); // [!code ++]
		CC.Say("reconstruct", CC); // [!code ++]
		SE.Change();
		EClass.pc.PlayEffect("mutation"); // [!code --]
		CC.PlayEffect("mutation"); // [!code ++]
		break;
	case EffectId.ReturnVoid:
	{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1700-L1708)
```cs:line-numbers=1700
		TC.Say("unpolluted", TC);
		list4.Shuffle();
		{
			foreach (Condition item4 in list4) // [!code --]
			foreach (Condition item5 in list4) // [!code ++]
			{
				item4.Kill(); // [!code --]
				item5.Kill(); // [!code ++]
				if (CC.IsHostile(TC))
				{
					break;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1717-L1731)
```cs:line-numbers=1717
		{
			break;
		}
		int num12 = 0; // [!code --]
		int num13 = 0; // [!code ++]
		foreach (Condition condition5 in TC.conditions)
		{
			if (condition5.Type == ConditionType.Debuff)
			{
				num12++; // [!code --]
				num13++; // [!code ++]
			}
		}
		if (num12 == 0) // [!code --]
		if (num13 == 0) // [!code ++]
		{
			CC.SayNothingHappans();
			break;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1743-L1769)
```cs:line-numbers=1743
		TC.Say("abShutterHex", TC);
		Point center = CC.pos.Copy();
		List<Chara> list10 = TC.pos.ListCharasInRadius(TC, 4, (Chara c) => c == TC || c.IsHostile(CC));
		for (int m = 0; m < num12; m++) // [!code --]
		for (int m = 0; m < num13; m++) // [!code ++]
		{
			TweenUtil.Delay((float)m * 0.1f, delegate
			{
				center.PlaySound("shutterhex");
			});
			foreach (Chara item5 in list10) // [!code --]
			foreach (Chara item6 in list10) // [!code ++]
			{
				if (item5.ExistsOnMap) // [!code --]
				if (item6.ExistsOnMap) // [!code ++]
				{
					Effect effect = Effect.Get("spell_moonspear");
					TrailRenderer componentInChildren = effect.GetComponentInChildren<TrailRenderer>();
					Color startColor = (componentInChildren.endColor = EClass.Colors.elementColors["eleHoly"]);
					componentInChildren.startColor = startColor;
					Point pos = item5.pos.Copy(); // [!code --]
					Point pos = item6.pos.Copy(); // [!code ++]
					TweenUtil.Delay((float)m * 0.1f, delegate
					{
						effect.Play(center, 0f, pos);
					});
					int num13 = Dice.Create("SpShutterHex", power, CC, (actRef.refThing != null) ? null : actRef.act).Roll(); // [!code --]
					item5.DamageHP(num13, 919, power, AttackSource.None, CC, showEffect: false); // [!code --]
					int num14 = Dice.Create("SpShutterHex", power, CC, (actRef.refThing != null) ? null : actRef.act).Roll(); // [!code ++]
					item6.DamageHP(num14, 919, power, AttackSource.None, CC, showEffect: false); // [!code ++]
				}
			}
		}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1818-L1839)
```cs:line-numbers=1818
			TC.Say((actRef.n1 == "money") ? "abStealNegateMoney" : "abStealNegate", TC);
			break;
		}
		Thing thing6 = null; // [!code --]
		bool flag10 = actRef.n1 == "food"; // [!code --]
		Thing thing7 = null; // [!code ++]
		bool flag11 = actRef.n1 == "food"; // [!code ++]
		if (actRef.n1 == "money")
		{
			int currency = TC.GetCurrency();
			if (currency > 0)
			{
				currency = Mathf.Clamp(EClass.rnd(currency / 10), 1, 100 + EClass.rndHalf(Mathf.Min(CC.LV, 1000000) * 200));
				thing6 = ThingGen.Create("money").SetNum(currency); // [!code --]
				thing7 = ThingGen.Create("money").SetNum(currency); // [!code ++]
				TC.ModCurrency(-currency);
			}
		}
		else
		{
			Func<Thing, bool> func = (Thing t) => true;
			if (flag10) // [!code --]
			if (flag11) // [!code ++]
			{
				func = (Thing t) => t.IsFood;
			}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1847-L1874)
```cs:line-numbers=1847
			}, onlyAccessible: true);
			if (list9.Count > 0)
			{
				thing6 = list9.RandomItem(); // [!code --]
				if (thing6.Num > 1) // [!code --]
				thing7 = list9.RandomItem(); // [!code ++]
				if (thing7.Num > 1) // [!code ++]
				{
					thing6 = thing6.Split(1); // [!code --]
					thing7 = thing7.Split(1); // [!code ++]
				}
			}
			CC.AddCooldown(6640, 200);
		}
		if (thing6 == null) // [!code --]
		if (thing7 == null) // [!code ++]
		{
			CC.Say("abStealNothing", CC, TC);
			break;
		}
		thing6.SetInt(116, 1); // [!code --]
		TC.PlaySound(thing6.material.GetSoundDrop(thing6.sourceCard)); // [!code --]
		CC.Pick(thing6, msg: false); // [!code --]
		CC.Say("abSteal", CC, TC, thing6.Name); // [!code --]
		thing7.SetInt(116, 1); // [!code ++]
		TC.PlaySound(thing7.material.GetSoundDrop(thing7.sourceCard)); // [!code ++]
		CC.Pick(thing7, msg: false); // [!code ++]
		CC.Say("abSteal", CC, TC, thing7.Name); // [!code ++]
		if (actRef.n1 == "food")
		{
			if (CC.hunger.value != 0)
			{
				CC.InstantEat(thing6); // [!code --]
				CC.InstantEat(thing7); // [!code ++]
			}
		}
		else
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1906-L1915)
```cs:line-numbers=1906
			CC.SayNothingHappans();
			break;
		}
		Thing thing4 = list5.RandomItem(); // [!code --]
		TC.Say("curse_hit", TC, thing4); // [!code --]
		thing4.SetBlessedState((thing4.blessedState == BlessedState.Cursed) ? BlessedState.Doomed : BlessedState.Cursed); // [!code --]
		LayerInventory.SetDirty(thing4); // [!code --]
		Thing thing5 = list5.RandomItem(); // [!code ++]
		TC.Say("curse_hit", TC, thing5); // [!code ++]
		thing5.SetBlessedState((thing5.blessedState == BlessedState.Cursed) ? BlessedState.Doomed : BlessedState.Cursed); // [!code ++]
		LayerInventory.SetDirty(thing5); // [!code ++]
		break;
	}
	case EffectId.UncurseEQ:
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1928-L1953)
```cs:line-numbers=1928
		List<Thing> list = new List<Thing>();
		TC.things.Foreach(delegate(Thing t)
		{
			int num14 = 0; // [!code --]
			int num15 = 0; // [!code ++]
			if ((t.isEquipped || t.IsRangedWeapon || blessed) && t.blessedState < BlessedState.Normal)
			{
				if (t.blessedState == BlessedState.Cursed)
				{
					num14 = EClass.rnd(200); // [!code --]
					num15 = EClass.rnd(200); // [!code ++]
				}
				if (t.blessedState == BlessedState.Doomed)
				{
					num14 = EClass.rnd(1000); // [!code --]
					num15 = EClass.rnd(1000); // [!code ++]
				}
				if (blessed)
				{
					num14 /= 2; // [!code --]
					num15 /= 2; // [!code ++]
				}
				if (id == EffectId.UncurseEQGreater)
				{
					num14 /= 10; // [!code --]
					num15 /= 10; // [!code ++]
				}
				if (power >= num14) // [!code --]
				if (power >= num15) // [!code ++]
				{
					TC.Say("uncurseEQ_success", t);
					t.SetBlessedState(BlessedState.Normal);
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L1965-L1973)
```cs:line-numbers=1965
				}
			}
		});
		foreach (Thing item6 in list) // [!code --]
		foreach (Thing item7 in list) // [!code ++]
		{
			item6.GetRootCard()?.TryStack(item6); // [!code --]
			item7.GetRootCard()?.TryStack(item7); // [!code ++]
		}
		if (success == 0 && fail == 0)
		{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L2043-L2055)
```cs:line-numbers=2043
			power = power * 2 / 3;
		}
		int a2 = power;
		int num10 = TC.WIL * (isPowerful ? 20 : 5); // [!code --]
		int num11 = TC.WIL * (isPowerful ? 20 : 5); // [!code ++]
		ConHolyVeil condition3 = TC.GetCondition<ConHolyVeil>();
		if (condition3 != null)
		{
			num10 += condition3.power * 5; // [!code --]
			num11 += condition3.power * 5; // [!code ++]
		}
		if (id != EffectId.DebuffKizuami && EClass.rnd(a2) < num10 / EClass.sources.stats.alias[n].hexPower && EClass.rnd(10) != 0) // [!code --]
		if (id != EffectId.DebuffKizuami && EClass.rnd(a2) < num11 / EClass.sources.stats.alias[n].hexPower && EClass.rnd(10) != 0) // [!code ++]
		{
			TC.Say("debuff_resist", TC);
			CC.DoHostileAction(TC);
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L2173-L2192)
```cs:line-numbers=2173
	{
		tc.PlaySound("mutation");
		tc.PlayEffect("mutation");
		int num11 = ((actRef.refThing != null) ? actRef.refThing.GetInt(118) : actRef.refVal); // [!code --]
		if (num11 == 0) // [!code --]
		int num12 = ((actRef.refThing != null) ? actRef.refThing.GetInt(118) : actRef.refVal); // [!code ++]
		if (num12 == 0) // [!code ++]
		{
			num11 = tc.GetInt(118); // [!code --]
			if (num11 == 0) // [!code --]
			num12 = tc.GetInt(118); // [!code ++]
			if (num12 == 0) // [!code ++]
			{
				num11 = EClass.game.seed + tc.uid; // [!code --]
				num12 = EClass.game.seed + tc.uid; // [!code ++]
			}
			num11++; // [!code --]
			num12++; // [!code ++]
		}
		tc.Say("transBlood", tc);
		tc.Talk("tail");
		tc.c_bloodData = null;
		tc.SetInt(118, num11); // [!code --]
		tc.SetInt(118, num12); // [!code ++]
		break;
	}
	case EffectId.Youth:
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L2299-L2322)
```cs:line-numbers=2299
	case EffectId.DamageMindGreat:
	case EffectId.Weaken:
	{
		bool flag11 = id == EffectId.DamageBody || id == EffectId.DamageBodyGreat; // [!code --]
		bool flag12 = id == EffectId.DamageBody || id == EffectId.DamageBodyGreat; // [!code ++]
		bool mind2 = id == EffectId.DamageMind || id == EffectId.DamageMindGreat;
		int num9 = ((id == EffectId.DamageBody || id == EffectId.DamageMind) ? 1 : (4 + EClass.rnd(4))); // [!code --]
		int num10 = ((id == EffectId.DamageBody || id == EffectId.DamageMind) ? 1 : (4 + EClass.rnd(4))); // [!code ++]
		if (id == EffectId.Weaken)
		{
			flag11 = EClass.rnd(2) == 0; // [!code --]
			mind2 = !flag11; // [!code --]
			num9 = 1; // [!code --]
			flag12 = EClass.rnd(2) == 0; // [!code ++]
			mind2 = !flag12; // [!code ++]
			num10 = 1; // [!code ++]
		}
		else
		{
			TC.PlayEffect("debuff");
			TC.PlaySound("debuff");
		}
		TC.Say(flag11 ? "damageBody" : "damageMind", TC); // [!code --]
		for (int l = 0; l < num9; l++) // [!code --]
		TC.Say(flag12 ? "damageBody" : "damageMind", TC); // [!code ++]
		for (int l = 0; l < num10; l++) // [!code ++]
		{
			TC.DamageTempElements(power, flag11, mind2, id != EffectId.Weaken); // [!code --]
			TC.DamageTempElements(power, flag12, mind2, id != EffectId.Weaken); // [!code ++]
		}
		if (TC.IsPC)
		{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L2329-L2362)
```cs:line-numbers=2329
	case EffectId.EnhanceBodyGreat:
	case EffectId.EnhanceMindGreat:
	{
		bool flag8 = id == EffectId.EnhanceBody || id == EffectId.EnhanceBodyGreat; // [!code --]
		bool flag9 = id == EffectId.EnhanceBody || id == EffectId.EnhanceBodyGreat; // [!code ++]
		bool mind = id == EffectId.EnhanceMind || id == EffectId.EnhanceMindGreat;
		if (id != EffectId.EnhanceBody && id != EffectId.EnhanceMind)
		{
			EClass.rnd(4);
		}
		TC.Say(flag8 ? "enhanceBody" : "enhanceMind", TC); // [!code --]
		TC.Say(flag9 ? "enhanceBody" : "enhanceMind", TC); // [!code ++]
		TC.PlayEffect("buff");
		TC.PlaySound("buff");
		TC.EnhanceTempElements(power, flag8, mind, onlyRenew: true); // [!code --]
		TC.EnhanceTempElements(power, flag9, mind, onlyRenew: true); // [!code ++]
		break;
	}
	case EffectId.RestoreBody:
	case EffectId.RestoreMind:
	{
		bool flag9 = id == EffectId.RestoreBody; // [!code --]
		bool flag10 = id == EffectId.RestoreBody; // [!code ++]
		if (flag)
		{
			Redirect(flag9 ? EffectId.DamageBodyGreat : EffectId.DamageMindGreat, BlessedState.Normal, default(ActRef)); // [!code --]
			Redirect(flag10 ? EffectId.DamageBodyGreat : EffectId.DamageMindGreat, BlessedState.Normal, default(ActRef)); // [!code ++]
			break;
		}
		TC.Say(flag9 ? "restoreBody" : "restoreMind", TC); // [!code --]
		TC.Say(flag10 ? "restoreBody" : "restoreMind", TC); // [!code ++]
		TC.PlaySound("heal");
		TC.PlayEffect("heal");
		TC.CureHost(flag9 ? CureType.CureBody : CureType.CureMind, power, state); // [!code --]
		TC.CureHost(flag10 ? CureType.CureBody : CureType.CureMind, power, state); // [!code ++]
		if (blessed)
		{
			Redirect(flag9 ? EffectId.EnhanceBodyGreat : EffectId.EnhanceMindGreat, BlessedState.Normal, default(ActRef)); // [!code --]
			Redirect(flag10 ? EffectId.EnhanceBodyGreat : EffectId.EnhanceMindGreat, BlessedState.Normal, default(ActRef)); // [!code ++]
		}
		break;
	}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L2372-L2388)
```cs:line-numbers=2372
		{
			EClass.game.religions.Healing.Talk("ability");
		}
		int num8 = Dice.Create((actRef.act != null && EClass.sources.calc.map.ContainsKey(actRef.act.ID)) ? actRef.act.ID : "SpHealLight", power, CC, (actRef.refThing != null) ? null : actRef.act).Roll(); // [!code --]
		int num9 = Dice.Create((actRef.act != null && EClass.sources.calc.map.ContainsKey(actRef.act.ID)) ? actRef.act.ID : "SpHealLight", power, CC, (actRef.refThing != null) ? null : actRef.act).Roll(); // [!code ++]
		if (actRef.refThing != null)
		{
			num8 = num8 * (100 + actRef.refThing.Evalue(750) * 10) / 100; // [!code --]
			num9 = num9 * (100 + actRef.refThing.Evalue(750) * 10) / 100; // [!code ++]
		}
		if (flag)
		{
			TC.DamageHP(num8 / 2, 919, power); // [!code --]
			TC.DamageHP(num9 / 2, 919, power); // [!code ++]
			break;
		}
		TC.HealHPHost(num8, (actRef.refThing == null && id != EffectId.JureHeal) ? HealSource.Magic : HealSource.Item); // [!code --]
		TC.HealHPHost(num9, (actRef.refThing == null && id != EffectId.JureHeal) ? HealSource.Magic : HealSource.Item); // [!code ++]
		TC.CureHost(CureType.Heal, power, state);
		TC.Say((power >= 300) ? "heal_heavy" : "heal_light", TC);
		break;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L2403-L2414)
```cs:line-numbers=2403
			Redirect(EffectId.CurseEQ, BlessedState.Normal, default(ActRef));
			break;
		}
		foreach (Condition item7 in TC.conditions.Copy()) // [!code --]
		foreach (Condition item8 in TC.conditions.Copy()) // [!code ++]
		{
			if (item7.Type == ConditionType.Debuff && !item7.IsKilled && EClass.rnd(power * (CC.IsPowerful ? 5 : 2)) > EClass.rnd(item7.power)) // [!code --]
			if (item8.Type == ConditionType.Debuff && !item8.IsKilled && EClass.rnd(power * (CC.IsPowerful ? 5 : 2)) > EClass.rnd(item8.power)) // [!code ++]
			{
				CC.Say("removeHex", TC, item7.Name.ToLower()); // [!code --]
				item7.Kill(); // [!code --]
				CC.Say("removeHex", TC, item8.Name.ToLower()); // [!code ++]
				item8.Kill(); // [!code ++]
				if (id == EffectId.RemoveHex)
				{
					break;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L2487-L2494)
```cs:line-numbers=2487
		if (TC.HasElement(1211))
		{
			TC.Say("drinkSaltWater_snail", TC);
			int num7 = ((TC.hp > 10) ? (TC.hp - EClass.rnd(10)) : 10000); // [!code --]
			TC.DamageHP(num7, AttackSource.None, CC); // [!code --]
			int num8 = ((TC.hp > 10) ? (TC.hp - EClass.rnd(10)) : 10000); // [!code ++]
			TC.DamageHP(num8, AttackSource.None, CC); // [!code ++]
		}
		else if (TC.IsPC)
		{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ActEffect.cs#L2657-L2673)
```cs:line-numbers=2657
		List<Thing> list6 = TC.things.List((Thing t) => (t.Num <= 1 && t.IsEquipmentOrRanged && !t.IsToolbelt && !t.IsLightsource && t.isEquipped) ? true : false);
		if (list6.Count != 0)
		{
			Thing thing5 = list6.RandomItem(); // [!code --]
			Thing thing6 = list6.RandomItem(); // [!code ++]
			TC.Say("acid_hit", TC);
			if (thing5.isAcidproof) // [!code --]
			if (thing6.isAcidproof) // [!code ++]
			{
				TC.Say("acid_nullify", thing5); // [!code --]
				TC.Say("acid_nullify", thing6); // [!code ++]
			}
			else if (thing5.encLV > -5) // [!code --]
			else if (thing6.encLV > -5) // [!code ++]
			{
				TC.Say("acid_rust", TC, thing5); // [!code --]
				thing5.ModEncLv(-1); // [!code --]
				LayerInventory.SetDirty(thing5); // [!code --]
				TC.Say("acid_rust", TC, thing6); // [!code ++]
				thing6.ModEncLv(-1); // [!code ++]
				LayerInventory.SetDirty(thing6); // [!code ++]
			}
			if (TC.IsPCParty)
			{
```

## BaseTaskHarvest

[`public override bool CanProgress()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/BaseTaskHarvest.cs#L291-L296)
```cs:line-numbers=291
		return base.CanProgress();
	}

	public override void OnStart() // [!code ++]
	{ // [!code ++]
		if (tool != owner.Tool) // [!code ++]
		{ // [!code ++]
			SetTarget(owner); // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public int[] GetToolEfficiency(SourceMaterial.Row mat)
	{
		switch (mat.category)
```

## Chara

[`public override void OnCreate(int genLv)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/Chara.cs#L1607-L1613)
```cs:line-numbers=1607
				list.Add(new Tuple<string, int, int>(array[0], (array.Length > 1) ? int.Parse(array[1]) : 0, num2));
			}
		}
		Tuple<string, int, int> tuple = list.RandomItemWeighted((Tuple<string, int, int> a) => 10000 / (100 + (_genLv - a.Item3) * 25)); // [!code --]
		Tuple<string, int, int> tuple = list.RandomItemWeighted((Tuple<string, int, int> a) => 10000 / Mathf.Max(100 + (_genLv - a.Item3) * 25, 1)); // [!code ++]
		if (!bp.idEle.IsEmpty())
		{
			tuple = list.Where((Tuple<string, int, int> a) => a.Item1 == bp.idEle || "ele" + a.Item1 == bp.idEle).FirstOrDefault() ?? tuple;
```

## InvOwnerChangeRarity

[`public override Thing CreateDefaultContainer()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/InvOwnerChangeRarity.cs#L15-L21)
```cs:line-numbers=15

	public override bool ShouldShowGuide(Thing t)
	{
		if (t.IsEquipment && !t.IsUnique && !t.HasTag(CTAG.godArtifact)) // [!code --]
		if (t.IsEquipment && t.rarity <= Rarity.Legendary && !t.HasTag(CTAG.godArtifact)) // [!code ++]
		{
			return !t.IsLightsource;
		}
```

## ModManager

[`public IEnumerator RefreshMods(Action onComplete, bool syncMods)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ModManager.cs#L306-L312)
```cs:line-numbers=306
	InitPackagesMeta();
	LoadLoadOrder();
	packages.Sort((BaseModPackage a, BaseModPackage b) => a.loadPriority - b.loadPriority);
	foreach (BaseModPackage item in packages.Where((BaseModPackage p) => !p.isInPackages && p.willActivate)) // [!code --]
	foreach (BaseModPackage item in packages.Where((BaseModPackage p) => !p.isInPackages && p.willActivate && !p.id.IsEmpty())) // [!code ++]
	{
		if (mappedPackages.TryGetValue(item.id, out var value) && value.isInPackages)
		{
```

## Player

[`public int fishStolen`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/Player.cs#L732-L737)
```cs:line-numbers=732
			}
		}

		public int garokkHammerBought // [!code ++]
		{ // [!code ++]
			get // [!code ++]
			{ // [!code ++]
				return ints[22]; // [!code ++]
			} // [!code ++]
			set // [!code ++]
			{ // [!code ++]
				ints[22] = value; // [!code ++]
			} // [!code ++]
		} // [!code ++]
 // [!code ++]
		[OnSerializing]
		private void _OnSerializing(StreamingContext context)
		{
```

## ShopTransaction

[`public void OnEndTransaction()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/ShopTransaction.cs#L165-L173)
```cs:line-numbers=165
		{
			num += (int)Mathf.Sqrt(Mathf.Abs(item5.price * 5));
		}
		if (item5.thing.trait is TraitDeed) // [!code --]
		string id = item5.thing.id; // [!code ++]
		if (!(id == "hammer_garokk")) // [!code ++]
		{
			EClass.player.flags.landDeedBought += item5.num; // [!code --]
			if (id == "deed") // [!code ++]
			{ // [!code ++]
				EClass.player.flags.landDeedBought += item5.num; // [!code ++]
			} // [!code ++]
		} // [!code ++]
		else // [!code ++]
		{ // [!code ++]
			EClass.player.flags.garokkHammerBought += item5.num; // [!code ++]
		}
	}
	if (num >= 10)
```

## SourceImporter

[`public SourceData FindSourceByName(string name)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/SourceImporter.cs#L84-L93)
```cs:line-numbers=84

	public IEnumerable<SourceData> ImportFilesCached(IEnumerable<string> imports, bool resetData = true)
	{
		string[] prefetchSheetNames = new string[2] { "Element", "Material" }; // [!code ++]
		SourceCache[] array = imports.Select(SourceCache.GetOrCreate).Distinct().ToArray();
		Dictionary<SourceCache, (string, ISheet[], ISheet)> dictionary = (from c in array // [!code --]
		Dictionary<SourceCache, (string, ISheet[], ISheet[])> dictionary = (from c in array // [!code ++]
			where c.IsDirtyOrEmpty
			select PrefetchWorkbook(c.SheetFile.FullName)).ToArray().ToDictionary(((string file, ISheet[] sheets, ISheet element) p) => SourceCache.GetOrCreate(p.file)); // [!code --]
			select PrefetchWorkbook(c.SheetFile.FullName, prefetchSheetNames)).ToArray().ToDictionary(((string file, ISheet[] sheets, ISheet[] fetched) p) => SourceCache.GetOrCreate(p.file)); // [!code ++]
		SourceElement elements = EClass.sources.elements;
		HashSet<SourceData> hashSet = new HashSet<SourceData> { elements };
		SourceCache[] array2 = array;
```

[`where c.IsDirtyOrEmpty`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/SourceImporter.cs#L98-L147)
```cs:line-numbers=98
		{
			sourceCache.SetMod(value);
		}
		SourceData.BaseRow[] rows; // [!code --]
		if (sourceCache.IsDirtyOrEmpty)
		{
			if (dictionary.TryGetValue(sourceCache, out var value2) && value2.Item3 != null) // [!code --]
			if (dictionary.TryGetValue(sourceCache, out var value2) && value2.Item3.Length != 0) // [!code ++]
			{
				SourceData.BaseRow[] item = LoadBySheetName(value2.Item3, value2.Item1).Item2; // [!code --]
				sourceCache.EmplaceCache("Element", item); // [!code --]
				value?.sourceRows.UnionWith(item); // [!code --]
				Debug.Log(string.Format("#source workbook {0}:{1}:{2}", arg, "Element", item.Length)); // [!code --]
				ISheet[] item = value2.Item3; // [!code ++]
				foreach (ISheet sheet in item) // [!code ++]
				{ // [!code ++]
					SourceData.BaseRow[] item2 = LoadBySheetName(sheet, value2.Item1).Item2; // [!code ++]
					sourceCache.EmplaceCache(sheet.SheetName, item2); // [!code ++]
					value?.sourceRows.UnionWith(item2); // [!code ++]
					Debug.Log($"#source workbook {arg}:{sheet.SheetName}:{item2.Length}"); // [!code ++]
				} // [!code ++]
			}
			continue; // [!code ++]
		}
		else if (sourceCache.TryGetCache("Element", out rows)) // [!code --]
		string[] array3 = prefetchSheetNames; // [!code ++]
		foreach (string text in array3) // [!code ++]
		{
			int num = elements.ImportRows(rows); // [!code --]
			value?.sourceRows.UnionWith(rows); // [!code --]
			Debug.Log(string.Format("#source workbook-cache {0}:{1}:{2}", arg, "Element", num)); // [!code --]
			if (sourceCache.TryGetCache(text, out var rows)) // [!code ++]
			{ // [!code ++]
				int num = elements.ImportRows(rows); // [!code ++]
				value?.sourceRows.UnionWith(rows); // [!code ++]
				Debug.Log($"#source workbook-cache {arg}:{text}:{num}"); // [!code ++]
			} // [!code ++]
		}
	}
	array2 = array;
	foreach (SourceCache sourceCache2 in array2)
	{
		string text = sourceCache2.SheetFile.ShortPath(); // [!code --]
		string text2 = sourceCache2.SheetFile.ShortPath(); // [!code ++]
		string key; // [!code ++]
		if (sourceCache2.IsDirtyOrEmpty)
		{
			if (!dictionary.TryGetValue(sourceCache2, out var value3) || value3.Item2.Length == 0)
			{
				continue;
			}
			Debug.Log("#source workbook " + text); // [!code --]
			ISheet[] item2 = value3.Item2; // [!code --]
			foreach (ISheet sheet in item2) // [!code --]
			Debug.Log("#source workbook " + text2); // [!code ++]
			ISheet[] item = value3.Item2; // [!code ++]
			foreach (ISheet sheet2 in item) // [!code ++]
			{
				if (sheet.SheetName == "Element") // [!code --]
				key = sheet2.SheetName; // [!code ++]
				if (key == "Element" || key == "Material") // [!code ++]
				{
					continue;
				}
				var (sourceData, array3) = LoadBySheetName(sheet, value3.Item1); // [!code --]
				var (sourceData, array4) = LoadBySheetName(sheet2, value3.Item1); // [!code ++]
				if ((object)sourceData != null)
				{
					int? num2 = array3?.Length; // [!code --]
					int? num2 = array4?.Length; // [!code ++]
					if (num2.HasValue && num2.GetValueOrDefault() > 0)
					{
						sourceCache2.EmplaceCache(sheet.SheetName, array3); // [!code --]
						sourceCache2.Mod?.sourceRows.UnionWith(array3); // [!code --]
						sourceCache2.EmplaceCache(sheet2.SheetName, array4); // [!code ++]
						sourceCache2.Mod?.sourceRows.UnionWith(array4); // [!code ++]
						hashSet.Add(sourceData);
					}
				}
```

[`where c.IsDirtyOrEmpty`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/SourceImporter.cs#L150-L159)
```cs:line-numbers=150
		}
		foreach (KeyValuePair<string, SourceData.BaseRow[]> item3 in sourceCache2.Source)
		{
			item3.Deconstruct(out var key, out var value4); // [!code --]
			string text2 = key; // [!code --]
			SourceData.BaseRow[] array4 = value4; // [!code --]
			SourceData sourceData2 = FindSourceByName(text2); // [!code --]
			item3.Deconstruct(out key, out var value4); // [!code ++]
			string text3 = key; // [!code ++]
			SourceData.BaseRow[] array5 = value4; // [!code ++]
			SourceData sourceData2 = FindSourceByName(text3); // [!code ++]
			if (!(sourceData2 is SourceThingV))
			{
				if (sourceData2 is SourceElement || (object)sourceData2 == null)
```

[`where c.IsDirtyOrEmpty`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/SourceImporter.cs#L165-L178)
```cs:line-numbers=165
			{
				sourceData2 = EClass.sources.things;
			}
			if (array4 == null) // [!code --]
			if (array5 == null) // [!code ++]
			{
				Debug.Log("#source cached rows are empty " + text + ":" + text2); // [!code --]
				Debug.Log("#source cached rows are empty " + text2 + ":" + text3); // [!code ++]
				continue;
			}
			int num3 = sourceData2.ImportRows(array4); // [!code --]
			sourceCache2.Mod?.sourceRows.UnionWith(array4); // [!code --]
			Debug.Log($"#source workbook-cache {text}:{text2}:{num3}"); // [!code --]
			int num3 = sourceData2.ImportRows(array5); // [!code ++]
			sourceCache2.Mod?.sourceRows.UnionWith(array5); // [!code ++]
			Debug.Log($"#source workbook-cache {text2}:{text3}:{num3}"); // [!code ++]
			hashSet.Add(sourceData2);
		}
	}
```

[`public static void HotInit(IEnumerable<SourceData> sourceData)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/SourceImporter.cs#L201-L229)
```cs:line-numbers=201
		Debug.Log("#source initialized data");
	}

	private (string file, ISheet[] sheets, ISheet element) PrefetchWorkbook(string file) // [!code --]
	private (string file, ISheet[] sheets, ISheet[] fetched) PrefetchWorkbook(string file, string[] prefetchNames) // [!code ++]
	{
		using FileStream @is = File.Open(file, FileMode.Open, FileAccess.Read, FileShare.ReadWrite);
		XSSFWorkbook xSSFWorkbook = new XSSFWorkbook((Stream)@is);
		List<ISheet> list = new List<ISheet>();
		ISheet item = null; // [!code --]
		List<ISheet> list2 = new List<ISheet>(); // [!code ++]
		for (int i = 0; i < xSSFWorkbook.NumberOfSheets; i++)
		{
			ISheet sheetAt = xSSFWorkbook.GetSheetAt(i);
			SourceData sourceData = FindSourceByName(sheetAt.SheetName); // [!code --]
			if (!(sourceData is SourceElement)) // [!code --]
			if (FindSourceByName(sheetAt.SheetName) != null && prefetchNames.Contains(sheetAt.SheetName)) // [!code ++]
			{
				if ((object)sourceData != null) // [!code --]
				{ // [!code --]
					list.Add(sheetAt); // [!code --]
				} // [!code --]
				list2.Add(sheetAt); // [!code ++]
			}
			else
			{
				item = sheetAt; // [!code --]
				list.Add(sheetAt); // [!code ++]
			}
		}
		Debug.Log("#source workbook-prefetch " + file.ShortPath());
		return (file: file, sheets: list.ToArray(), element: item); // [!code --]
		return (file: file, sheets: list.ToArray(), fetched: list2.ToArray()); // [!code ++]
	}
}
```

## TaskBuild

[`public override bool CanPressRepeat`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/TaskBuild.cs#L44-L52)
```cs:line-numbers=44
	{
		if (useHeld)
		{
			if (EClass.pc.held != null) // [!code --]
			if (owner.held != null) // [!code ++]
			{
				return EClass.pc.held.trait.CanExtendBuild; // [!code --]
				return owner.held.trait.CanExtendBuild; // [!code ++]
			}
			return false;
		}
```

[`public bool CanRotateBlock()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/TaskBuild.cs#L77-L97)
```cs:line-numbers=77
	{
		disableRotateBlock = false;
	}
	if (useHeld && EClass.pc.held != null) // [!code --]
	if (useHeld && owner.held != null) // [!code ++]
	{
		if (EClass._zone.IsRegion)
		{
			return false;
		}
		if (!(EClass._zone is Zone_Tent) && !EClass._zone.IsPCFactionOrTent && EClass.pc.held.trait.CanBeOnlyBuiltInHome) // [!code --]
		if (!(EClass._zone is Zone_Tent) && !EClass._zone.IsPCFactionOrTent && owner.held.trait.CanBeOnlyBuiltInHome) // [!code ++]
		{
			return false;
		}
		if (EClass._zone.RestrictBuild && !EClass.pc.held.trait.CanBuildInTown) // [!code --]
		if (EClass._zone.RestrictBuild && !owner.held.trait.CanBuildInTown) // [!code ++]
		{
			return false;
		}
		if (EClass.pc.held.trait is TraitBlock && pos.HasBlock && !EClass.pc.held.trait.IsDoor && !disableRotateBlock) // [!code --]
		if (owner.held.trait is TraitBlock && pos.HasBlock && !owner.held.trait.IsDoor && !disableRotateBlock) // [!code ++]
		{
			return true;
		}
```

[`public override string GetText(string str = "")`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/TaskBuild.cs#L105-L117)
```cs:line-numbers=105
	{
		return "actRotateWall".lang();
	}
	if (useHeld && EClass.pc.held != null) // [!code --]
	if (useHeld && owner.held != null) // [!code ++]
	{
		if (EClass.pc.held.category.id == "seed") // [!code --]
		if (owner.held.category.id == "seed") // [!code ++]
		{
			return "actInstallSeed".lang();
		}
		if (EClass.pc.held.id == "fertilizer") // [!code --]
		if (owner.held.id == "fertilizer") // [!code ++]
		{
			return "actInstallFertilizer".lang();
		}
```

[`public override void OnProgressComplete()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/TaskBuild.cs#L282-L288)
```cs:line-numbers=282
{
	if (useHeld)
	{
		if (EClass.pc.held == null || EClass.pc.held.GetRootCard() != EClass.pc || pos.Distance(EClass.pc.pos) > 1 || !pos.IsInBounds) // [!code --]
		if (owner.held == null || owner.held.GetRootCard() != owner || pos.Distance(owner.pos) > 1 || !pos.IsInBounds) // [!code ++]
		{
			return;
		}
```

[`public override void OnProgressComplete()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/TaskBuild.cs#L296-L312)
```cs:line-numbers=296
		disableRotateBlock = true;
		ActionMode.Build.FixBridge(pos, recipe);
		bridgeHeight = ActionMode.Build.bridgeHeight;
		target = (EClass.pc.held.category.installOne ? EClass.pc.held.Split(1) : EClass.pc.held); // [!code --]
		target = (owner.held.category.installOne ? owner.held.Split(1) : owner.held); // [!code ++]
		if (target.trait is TraitTile)
		{
			target.ModNum(-1);
		}
		dir = recipe._dir;
		EClass.pc.LookAt(pos); // [!code --]
		EClass.pc.renderer.PlayAnime(AnimeID.Attack_Place, pos); // [!code --]
		owner.LookAt(pos); // [!code ++]
		owner.renderer.PlayAnime(AnimeID.Attack_Place, pos); // [!code ++]
		if (target.id == "statue_weird")
		{
			EClass.pc.Say("statue_install"); // [!code --]
			owner.Say("statue_install"); // [!code ++]
		}
	}
	lastPos = pos.Copy();
```

[`public override void OnProgressComplete()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/TaskBuild.cs#L337-L350)
```cs:line-numbers=337
			if (pos.HasWallOrFence && pos.cell.blockDir != 2 && pos.cell.blockDir != recipe._dir)
			{
				pos.cell.blockDir = 2;
				EClass.pc.PlaySound(pos.matBlock.GetSoundImpact()); // [!code --]
				owner.PlaySound(pos.matBlock.GetSoundImpact()); // [!code ++]
				pos.RefreshTile();
				return;
			}
			if (pos.sourceRoofBlock.tileType.IsWallOrFence && pos.cell._roofBlockDir % 4 != 2 && pos.cell._roofBlockDir % 4 != recipe._dir)
			{
				pos.cell._roofBlockDir = (byte)(pos.cell._roofBlockDir / 4 * 4 + 2);
				EClass.pc.PlaySound(pos.matBlock.GetSoundImpact()); // [!code --]
				owner.PlaySound(pos.matBlock.GetSoundImpact()); // [!code ++]
				pos.RefreshTile();
				return;
			}
```

[`public override void OnProgressComplete()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/TaskBuild.cs#L360-L366)
```cs:line-numbers=360
	EClass._map.RefreshShadow(pos.x, pos.z);
	EClass._map.RefreshShadow(pos.x, pos.z - 1);
	EClass._map.RefreshFOV(pos.x, pos.z);
	EClass.pc.renderer.SetFirst(first: true); // [!code --]
	owner.renderer.SetFirst(first: true); // [!code ++]
	if (recipe.IsFloor)
	{
		foreach (Card item in pos.ListThings<TraitNewZone>())
```

[`public override void OnProgressComplete()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/TaskBuild.cs#L376-L382)
```cs:line-numbers=376
			{
				foreach (Chara item2 in p.ListCharas())
				{
					EClass.pc.Kick(item2, ignoreSelf: true, karmaLoss: false, show: false); // [!code --]
					owner.Kick(item2, ignoreSelf: true, karmaLoss: false, show: false); // [!code ++]
				}
			}
		});
```

## TaskPlow

[`public override HitResult GetHitResult()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/TaskPlow.cs#L82-L85)
```cs:line-numbers=82
		}
		return HitResult.Valid;
	}
 // [!code ++]
	public override void OnSetOwner() // [!code ++]
	{ // [!code ++]
	} // [!code ++]
}
```

## TaskPoint

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a1a6a085728cb2ae37c7da688ca5f0d05a152452/Elin/TaskPoint.cs#L125-L130)
```cs:line-numbers=125
		while (Loop);
	}

	public override void OnSetOwner() // [!code ++]
	{ // [!code ++]
		if (parent is AI_Goto aI_Goto) // [!code ++]
		{ // [!code ++]
			aI_Goto.ignoreConnection = true; // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void OnInspect()
	{
	}
```
