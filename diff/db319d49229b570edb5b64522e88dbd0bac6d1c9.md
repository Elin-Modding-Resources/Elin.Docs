---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 32 files modified.
version: EA 23.316 Nightly
changes: ABILITY/AI_Eat/AI_Fuck/AI_Idle/AI_Trolley/ActEffect/BaseTileMap/Chara/ConSmoking/CustomDramaExpansion/DOMAIN/DramaOutcome/ELEMENT/ENC/FACTION/FortuneRollData/ID_Achievement/InvOwner/InvOwnerEquip/Map/ModUtil/RecipeCard/Religion/SlaverData/SourceImporter/SourceMaterial/TCUI/Trait/TraitCard/TraitDiary/TraitLightSource/TraitTrolley
---

# EA 23.316 Nightly

June 13, 2026

32 files modified.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [CustomDramaExpansion (1)](#customdramaexpansion)
```cs:no-line-numbers
public static bool add_item(DramaManager dm, Dictionary<string, string> line, string itemId, string materialAlias = "wood", int lv = -1, int count = 1) // [!code --]
public static bool add_item(DramaManager dm, Dictionary<string, string> line, string itemId, string materialAlias = "", int lv = -1, int count = 1) // [!code ++]
```
### [Map (1)](#map)
```cs:no-line-numbers
public void TryShatter(Point pos, int ele, int power) // [!code --]
public void TryShatter(Point pos, int ele, int power, Card CC = null) // [!code ++]
```
## ABILITY

[``](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ABILITY.cs#L2-L8)
```cs:line-numbers=2

public class ABILITY
{
	public const int ActMoneyDump = 5058; // [!code --]
	public const int ActMoneyThrow = 5057; // [!code ++]

	public const int ActDreamBug = 6020;

```

[`public class ABILITY`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ABILITY.cs#L22-L28)
```cs:line-numbers=22

	public const int AI_PlayMusic = 6001;

	public const int ActMoneyThrow = 5057; // [!code --]
	public const int ActMoneyDump = 5058; // [!code ++]

	public const int ActMoneySlap = 5056;

```

[`public class ABILITY`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ABILITY.cs#L232-L237)
```cs:line-numbers=232

	public const int ActHeadpat = 6904;

	public const int ActKiss = 6628; // [!code ++]
 // [!code ++]
	public const int ActDeepKiss = 6629;

	public const int ActInsult = 6630;
```

[`public class ABILITY`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ABILITY.cs#L254-L261)
```cs:line-numbers=254

	public const int ActSwarm = 6662;

	public const int ActKiss = 6628; // [!code --]
 // [!code --]
	public const int ActBladeStorm = 6664;

	public const int ActBladeStorm2 = 6665;
```

[`public class ABILITY`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ABILITY.cs#L284-L303)
```cs:line-numbers=284

	public const int ActMissileBarrage = 6667;

	public const int StMama = 6701; // [!code ++]
 // [!code ++]
	public const int StManaCost = 6720;

	public const int ActWombDark = 6801;

	public const int StMama = 6701; // [!code --]
 // [!code --]
	public const int StTaunt = 6700;

	public const int ActDeathSentense = 6802;

	public static readonly int[] IDS = new int[146]
	{
		5058, 6020, 6019, 6018, 6015, 6013, 6012, 6011, 6003, 6001, // [!code --]
		5057, 5056, 5055, 5054, 5053, 5052, 6050, 6400, 6410, 6420, // [!code --]
		5057, 6020, 6019, 6018, 6015, 6013, 6012, 6011, 6003, 6001, // [!code ++]
		5058, 5056, 5055, 5054, 5053, 5052, 6050, 6400, 6410, 6420, // [!code ++]
		6621, 6620, 6613, 6612, 6611, 6610, 6608, 5051, 6606, 6604,
		6603, 6602, 6601, 6600, 6500, 6450, 6605, 6622, 5050, 5048,
		5004, 5005, 5006, 5007, 5008, 5009, 5010, 5011, 5012, 5013,
```

[`public class ABILITY`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ABILITY.cs#L307-L316)
```cs:line-numbers=307
		50210, 50209, 6626, 50207, 50206, 50212, 50205, 50203, 50202, 50201,
		50200, 8796, 8795, 50204, 8794, 50213, 50215, 5031, 5030, 5029,
		5028, 5027, 5026, 50214, 5025, 5023, 5022, 5021, 5020, 5019,
		50216, 5024, 8793, 50208, 6904, 6629, 6630, 6631, 6640, 6641, // [!code --]
		6642, 6650, 6660, 6661, 7000, 6662, 6628, 6664, 6665, 6666, // [!code --]
		50216, 5024, 8793, 50208, 6904, 6628, 6629, 6630, 6631, 6640, // [!code ++]
		6641, 6642, 6650, 6660, 6661, 7000, 6662, 6664, 6665, 6666, // [!code ++]
		6663, 6800, 6903, 6902, 6901, 6900, 6753, 6752, 6751, 6750,
		6667, 6720, 6801, 6701, 6700, 6802 // [!code --]
		6667, 6701, 6720, 6801, 6700, 6802 // [!code ++]
	};
}
public class Ability : Act
```

## AI_Eat

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/AI_Eat.cs#L130-L135)
```cs:line-numbers=130
				{
					EClass.player.forceTalk = true;
					owner.Talk("eatammoth");
					if (owner.IsPC) // [!code ++]
					{ // [!code ++]
						Steam.GetAchievement(ID_Achievement.MAMMOTH); // [!code ++]
					} // [!code ++]
				}
			}
			turn++;
```

## AI_Fuck

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/AI_Fuck.cs#L110-L120)
```cs:line-numbers=110
	}
	cc.Say((this.variation == Variation.Slime) ? "slime_start" : ((this.variation == Variation.Bloodsuck) ? "suck_start" : (Type.ToString() + "_start")), cc, tc);
	isFail = () => !tc.IsAliveInCurrentZone || tc.Dist(owner) > 3;
	maxProgress = ((this.variation == Variation.NTR || this.variation == Variation.Bloodsuck) ? 10 : 25); // [!code ++]
	if (Type == FuckType.tame)
	{
		cc.SetTempHand(1104, -1);
	}
	maxProgress = ((this.variation == Variation.NTR || this.variation == Variation.Bloodsuck) ? 10 : 25); // [!code --]
	Variation variation = this.variation;
	if ((uint)(variation - 3) <= 2u)
	{
```

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/AI_Fuck.cs#L133-L139)
```cs:line-numbers=133
		target.AddCondition<ConEntangle>(500, force: true);
		break;
	}
	for (int i = 0; i < maxProgress; i++) // [!code --]
	int i; // [!code ++]
	for (i = 0; i < maxProgress; i++) // [!code ++]
	{
		progress = i;
		if (owner.host != target)
```

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/AI_Fuck.cs#L175-L186)
```cs:line-numbers=175
				}
				break;
			}
			int num5 = ((cc.HasElement(1216) || tc.HasElement(1216)) ? 100 : 20); // [!code --]
			int num = ((cc.HasElement(1216) || tc.HasElement(1216)) ? 100 : 20); // [!code ++]
			if (this.variation == Variation.MotherMilk)
			{
				num5 *= 5; // [!code --]
				num *= 5; // [!code ++]
			}
			if (num5 > EClass.rnd(100)) // [!code --]
			if (num > EClass.rnd(100)) // [!code ++]
			{
				((EClass.rnd(2) == 0) ? cc : tc).PlayEffect("love2");
			}
```

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/AI_Fuck.cs#L206-L271)
```cs:line-numbers=206
				break;
			}
			case FuckType.tame:
			{ // [!code --]
				int num = 100; // [!code --]
				if (!tc.IsAnimal) // [!code --]
				if (i == 0 || i == 10) // [!code ++]
				{ // [!code ++]
					cc.Talk("goodBoy"); // [!code ++]
				} // [!code ++]
				if (i % 5 == 0) // [!code ++]
				{ // [!code ++]
					tc.PlaySound("brushing"); // [!code ++]
				} // [!code ++]
				if (cc.Tool != null && cc.Tool.Evalue(770) > 0) // [!code ++]
				{ // [!code ++]
					foreach (Chara item in cc.pos.ListCharasInRadius(cc, 2 + cc.Tool.Evalue(770) / 10, (Chara c) => true)) // [!code ++]
					{ // [!code ++]
						if (cc == tc || item != cc) // [!code ++]
						{ // [!code ++]
							Brush(item); // [!code ++]
							if (cc.isDead) // [!code ++]
							{ // [!code ++]
								break; // [!code ++]
							} // [!code ++]
						} // [!code ++]
					} // [!code ++]
				} // [!code ++]
				else // [!code ++]
				{
					num += 50; // [!code --]
					Brush(tc); // [!code ++]
				}
				if (tc.IsHuman) // [!code --]
				break; // [!code ++]
			} // [!code ++]
		} // [!code ++]
		Finish(); // [!code ++]
		void Brush(Chara c) // [!code ++]
		{ // [!code ++]
			if (c.interest >= 0) // [!code ++]
			{ // [!code ++]
				int num2 = 100; // [!code ++]
				if (!c.IsAnimal) // [!code ++]
				{
					num += 50; // [!code --]
					num2 += 50; // [!code ++]
				}
				if (tc.IsInCombat) // [!code --]
				if (c.IsHuman) // [!code ++]
				{
					num += 100; // [!code --]
					num2 += 50; // [!code ++]
				}
				if (tc == cc) // [!code --]
				if (c.IsInCombat) // [!code ++]
				{
					num = 50; // [!code --]
					num2 += 100; // [!code ++]
				}
				else if (tc.affinity.CurrentStage < Affinity.Stage.Respected && EClass.rnd((tc.IsPCFaction ? 30 : 10) * 100 / num) == 0) // [!code --]
				if (c == cc) // [!code ++]
				{
					tc.AddCondition<ConFear>(60); // [!code --]
					num2 = 50; // [!code ++]
				}
				tc.interest -= (tc.IsPCFaction ? 20 : (2 * num / 100)); // [!code --]
				if (i == 0 || i == 10) // [!code --]
				else if (c.affinity.CurrentStage < Affinity.Stage.Respected && EClass.rnd((c.IsPCFaction ? 30 : 10) * 100 / num2) == 0) // [!code ++]
				{
					cc.Talk("goodBoy"); // [!code --]
					tc.AddCondition<ConFear>(60); // [!code ++]
				}
				c.interest -= (c.IsPCFaction ? 20 : (2 * num2 / 100)); // [!code ++]
				if (i % 5 == 0)
				{
					tc.PlaySound("brushing"); // [!code --]
					int num2 = cc.CHA / 2 + cc.Evalue(237) - tc.CHA * 2; // [!code --]
					int num3; // [!code --]
					if (EClass.rnd(cc.CHA / 2 + cc.Evalue(237)) > EClass.rnd(tc.CHA * num / 100)) // [!code --]
					int num3 = 0; // [!code ++]
					int num4 = cc.CHA / 2 + cc.Evalue(237) - c.CHA * 2; // [!code ++]
					if (EClass.rnd(cc.CHA / 2 + cc.Evalue(237)) > EClass.rnd(c.CHA * num2 / 100)) // [!code ++]
					{
						num3 = 5 + Mathf.Clamp(num2 / 20, 0, 20); // [!code --]
						num3 = 5 + Mathf.Clamp(num4 / 20, 0, 20); // [!code ++]
					}
					else
					{
						num3 = -5 + ((!tc.IsPCFaction) ? Mathf.Clamp(num2 / 10, -30, 0) : 0); // [!code --]
						num3 = -5 + ((!tc.IsPCFaction) ? Mathf.Clamp(num4 / 10, -30, 0) : 0); // [!code ++]
						fails++;
					}
					int num4 = 20; // [!code --]
					if (tc.IsPCFactionOrMinion && tc.affinity.CurrentStage >= Affinity.Stage.Love) // [!code --]
					int num5 = 20; // [!code ++]
					if (c.IsPCFactionOrMinion && c.affinity.CurrentStage >= Affinity.Stage.Love) // [!code ++]
					{
						num3 = ((EClass.rnd(3) == 0) ? 4 : 0);
						num4 = 10; // [!code --]
						num5 = 10; // [!code ++]
					}
					totalAffinity += num3;
					tc.ModAffinity(EClass.pc, num3, show: true, showOnlyEmo: true); // [!code --]
					cc.elements.ModExp(237, num4); // [!code --]
					c.ModAffinity(EClass.pc, num3, show: true, showOnlyEmo: true); // [!code ++]
					cc.elements.ModExp(237, num5); // [!code ++]
					if (EClass.rnd(4) == 0)
					{
						cc.stamina.Mod(-1);
					}
				}
				break; // [!code --]
			} // [!code --]
				if (c.interest < 0 && c != tc) // [!code ++]
				{ // [!code ++]
					Msg.Say("tame_end", c); // [!code ++]
					c.PlaySound("groomed"); // [!code ++]
					c.PlayEffect("heal_tick"); // [!code ++]
					c.hygiene.Mod(15); // [!code ++]
					if (cc == EClass.pc) // [!code ++]
					{ // [!code ++]
						EClass.player.stats.brush++; // [!code ++]
					} // [!code ++]
				} // [!code ++]
			}
		}
		Finish(); // [!code --]
	}

	public void Finish()
```

## AI_Idle

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/AI_Idle.cs#L980-L985)
```cs:line-numbers=980
				owner.Say("use_whip3", owner, c);
				owner.PlaySound("whip");
				owner.Talk("insult");
				if (c.IsPC) // [!code ++]
				{ // [!code ++]
					Steam.GetAchievement(ID_Achievement.SNAIL); // [!code ++]
				} // [!code ++]
				c.PlayAnime(AnimeID.Shiver);
				c.OnInsulted();
			});
```

## AI_Trolley

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/AI_Trolley.cs#L79-L89)
```cs:line-numbers=79
		string idSound = trolley.GetIdSound();
		if (owner.IsPC)
		{
			owner.PlaySound(idSound); // [!code --]
			if (trolley.ShouldPlaySound(owner.pos)) // [!code ++]
			{ // [!code ++]
				owner.PlaySound(idSound); // [!code ++]
			} // [!code ++]
			else // [!code ++]
			{ // [!code ++]
				EClass.Sound.Stop(idSound, Mathf.Max(1f, trolley.FadeDuration)); // [!code ++]
			} // [!code ++]
		}
		else if (!(EClass.pc.ai is AI_Trolley))
		{
			owner.PlaySound(idSound); // [!code --]
			if (trolley.ShouldPlaySound(owner.pos)) // [!code ++]
			{ // [!code ++]
				owner.PlaySound(idSound); // [!code ++]
			} // [!code ++]
			EClass.Sound.Stop(idSound, Mathf.Max(1f, trolley.FadeDuration));
		}
		foreach (Chara item in point.ListCharas().ToList())
```

## ActEffect

[`public static bool DamageEle(Card CC, EffectId id, int power, Element e, List<Po`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ActEffect.cs#L177-L217)
```cs:line-numbers=177
				});
			}
		}
		bool flag3 = false; // [!code --]
		if (CC.IsPCFactionOrMinion && (CC.HasElement(1651) || EClass.pc.Evalue(1651) >= 2)) // [!code --]
		if (e.id == 910) // [!code ++]
		{
			bool flag4 = false; // [!code --]
			foreach (Card item in p.ListCards()) // [!code --]
			{ // [!code --]
				if (item.isChara) // [!code --]
				{ // [!code --]
					if (item.IsPCFactionOrMinion) // [!code --]
					{ // [!code --]
						flag4 = true; // [!code --]
					} // [!code --]
				} // [!code --]
				else if ((e.id != 910 && e.id != 911) || !item.IsFood || !item.category.IsChildOf("foodstuff")) // [!code --]
				{ // [!code --]
					flag4 = true; // [!code --]
				} // [!code --]
			} // [!code --]
			flag3 = flag4; // [!code --]
			EClass._map.TryShatter(p, 910, power, CC); // [!code ++]
		}
		if (!flag3) // [!code --]
		if (e.id == 911) // [!code ++]
		{
			if (e.id == 910) // [!code --]
			{ // [!code --]
				EClass._map.TryShatter(p, 910, power); // [!code --]
			} // [!code --]
			if (e.id == 911) // [!code --]
			{ // [!code --]
				EClass._map.TryShatter(p, 911, power); // [!code --]
			} // [!code --]
			EClass._map.TryShatter(p, 911, power, CC); // [!code ++]
		}
		foreach (Card item2 in p.ListCards().ToList()) // [!code --]
		foreach (Card item in p.ListCards().ToList()) // [!code ++]
		{
			Card c = item2; // [!code --]
			if ((!c.isChara && !c.trait.CanBeAttacked) || (c.IsMultisize && item2 == CC) || (c.isChara && (c.Chara.host == CC || c.Chara.parasite == CC || c.Chara.ride == CC))) // [!code --]
			Card c = item; // [!code ++]
			if ((!c.isChara && !c.trait.CanBeAttacked) || (c.IsMultisize && item == CC) || (c.isChara && (c.Chara.host == CC || c.Chara.parasite == CC || c.Chara.ride == CC))) // [!code ++]
			{
				continue;
			}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ActEffect.cs#L437-L447)
```cs:line-numbers=437
				EffectId.Meteor => 50 + power / 20, 
				_ => (actref.refThing != null) ? actref.refThing.material.hardness : (30 + power / 20), 
			};
			bool flag5 = EClass._zone.HasLaw && !EClass._zone.IsPCFaction && (CC.IsPC || (id == EffectId.Explosive && actref.refThing == null)) && !(EClass._zone is Zone_Vernis); // [!code --]
			bool flag3 = EClass._zone.HasLaw && !EClass._zone.IsPCFaction && (CC.IsPC || (id == EffectId.Explosive && actref.refThing == null)) && !(EClass._zone is Zone_Vernis); // [!code ++]
			if (p.HasObj && p.cell.matObj.hardness <= num7)
			{
				EClass._map.MineObj(p);
				if (flag5) // [!code --]
				if (flag3) // [!code ++]
				{
					EClass.player.ModKarma(-1);
				}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ActEffect.cs#L449-L455)
```cs:line-numbers=449
			if (!p.HasObj && p.HasBlock && p.matBlock.hardness <= num7)
			{
				EClass._map.MineBlock(p);
				if (flag5) // [!code --]
				if (flag3) // [!code ++]
				{
					EClass.player.ModKarma(-1);
				}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ActEffect.cs#L478-L489)
```cs:line-numbers=478
	}
	if (RapidCount == 0)
	{
		foreach (Card item3 in list) // [!code --]
		foreach (Card item2 in list) // [!code ++]
		{
			if (item3.ExistsOnMap) // [!code --]
			if (item2.ExistsOnMap) // [!code ++]
			{
				RapidCount += 2;
				ProcAt(id, power, BlessedState.Normal, item3, null, item3.pos, isNeg: true, actref); // [!code --]
				ProcAt(id, power, BlessedState.Normal, item2, null, item2.pos, isNeg: true, actref); // [!code ++]
			}
		}
	}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ActEffect.cs#L1159-L1169)
```cs:line-numbers=1159
		if (flag3)
		{
			EClass.player.stats.catDepart += list2.Count;
			break; // [!code ++]
		}
		else // [!code --]
		{ // [!code --]
			EClass.player.stats.sistersDepart += list2.Count; // [!code --]
		} // [!code --]
		EClass.player.stats.sistersDepart += list2.Count; // [!code ++]
		Steam.GetAchievement(ID_Achievement.LITTLEMIG); // [!code ++]
		break;
	}
	case EffectId.MagicMap:
```

## BaseTileMap

[`public virtual void DrawTile()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/BaseTileMap.cs#L1287-L3047)
```cs:line-numbers=1287
			fogged = true;
		}
	}
	goto IL_7dad; // [!code --]
	IL_7dad: // [!code --]
	if (detail.things.Count == 0 && detail.charas.Count == 0) // [!code --]
	goto IL_7d72; // [!code ++]
	IL_16c3: // [!code ++]
	if (this.cell.isSlopeEdge) // [!code ++]
	{
		return; // [!code --]
		float num3 = (float)height * _heightMod.y; // [!code ++]
		orgY = param.y; // [!code ++]
		orgZ = param.z; // [!code ++]
		param.dir = this.cell.blockDir; // [!code ++]
		if (snowed) // [!code ++]
		{ // [!code ++]
			param.color = floorLight; // [!code ++]
		} // [!code ++]
		SourceBlock.Row defBlock; // [!code ++]
		if (sourceBlock.tileType.IsFullBlock) // [!code ++]
		{ // [!code ++]
			defBlock = sourceBlock; // [!code ++]
			param.mat = matBlock; // [!code ++]
			param.tile = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length]; // [!code ++]
			param.matColor = ((sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, sourceBlock.colorMod)); // [!code ++]
		} // [!code ++]
		else // [!code ++]
		{ // [!code ++]
			defBlock = sourceFloor._defBlock; // [!code ++]
			param.mat = matFloor; // [!code ++]
			param.tile = defBlock._tiles[this.cell.blockDir % defBlock._tiles.Length]; // [!code ++]
			if (defBlock.id != 1) // [!code ++]
			{ // [!code ++]
				param.matColor = ((sourceFloor.colorMod == 0) ? 104025 : GetColorInt(ref matFloor.matColor, sourceFloor.colorMod)); // [!code ++]
			} // [!code ++]
			else // [!code ++]
			{ // [!code ++]
				param.matColor = 104025f; // [!code ++]
			} // [!code ++]
		} // [!code ++]
		for (int j = 0; (float)j < num3 / heightBlockSize; j++) // [!code ++]
		{ // [!code ++]
			param.y += ugFix.y; // [!code ++]
			param.z += ugFix.z + slopeFixZ * (float)j; // [!code ++]
			defBlock.renderData.Draw(param); // [!code ++]
			if (this.cell.pcSync && EMono.player.lightPower > 0f) // [!code ++]
			{ // [!code ++]
				float num4 = param.tile; // [!code ++]
				param.tile = 0f; // [!code ++]
				rendererFov.Draw(param); // [!code ++]
				param.tile = num4; // [!code ++]
			} // [!code ++]
		} // [!code ++]
		param.y = orgY; // [!code ++]
		param.z = orgZ; // [!code ++]
	}
	int num3 = 0; // [!code --]
	thingPos.x = 0f; // [!code --]
	thingPos.y = 0f; // [!code --]
	thingPos.z = 0f; // [!code --]
	freePos.x = (freePos.y = (freePos.z = 0f)); // [!code --]
	if (this.cell.HasRamp) // [!code --]
	param.color = floorLight; // [!code ++]
	if (!isWater && (this.cell.Front.sourceFloor.tileType.IsWater || this.cell.Right.sourceFloor.tileType.IsWater) && this.cell.sourceBlock.tileType.RenderWaterBlock && !flag) // [!code ++]
	{
		Vector3 rampFix = sourceBlock.tileType.GetRampFix(this.cell.blockDir); // [!code --]
		param.x += rampFix.x; // [!code --]
		param.y += rampFix.y; // [!code --]
		param.z += rampFix.z; // [!code --]
		freePos.x += rampFix.x; // [!code --]
		freePos.y += rampFix.y; // [!code --]
		freePos.z += rampFix.z; // [!code --]
		orgY = param.y; // [!code ++]
		orgZ = param.z; // [!code ++]
		int num5 = 0; // [!code ++]
		if (sourceBlock.tileType.IsFullBlock) // [!code ++]
		{ // [!code ++]
			SourceBlock.Row row3 = sourceBlock; // [!code ++]
			num5 = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length]; // [!code ++]
		} // [!code ++]
		else // [!code ++]
		{ // [!code ++]
			SourceBlock.Row row3 = sourceFloor._defBlock; // [!code ++]
			num5 = row3._tiles[this.cell.blockDir % row3._tiles.Length]; // [!code ++]
		} // [!code ++]
		if (((this.cell.Front.shore / 12) & 1) == 0 && this.cell.Front.sourceFloor.tileType.IsWater && this.cell.Front.height <= height && this.cell.Front.sourceBlock.tileType.RenderWaterBlock) // [!code ++]
		{ // [!code ++]
			param.y = (float)(cz - cx) * screen.tileAlign.y - (this.cell.Front.sourceFloor.tileType.IsDeepWater ? 0.6f : 0.4f) + (float)(int)this.cell.Front.height * _heightMod.y; // [!code ++]
			param.z = 1000f + param.x * screen.tileWeight.x + param.y * screen.tileWeight.z; // [!code ++]
			param.tile = num5 + ((!this.cell.Front.sourceFloor.tileType.IsDeepWater) ? 3000000 : 0); // [!code ++]
			rendererWaterBlock.Draw(param); // [!code ++]
		} // [!code ++]
		if (((this.cell.Right.shore / 12) & 8) == 0 && this.cell.Right.sourceFloor.tileType.IsWater && this.cell.Right.height <= height && this.cell.Right.sourceBlock.tileType.RenderWaterBlock) // [!code ++]
		{ // [!code ++]
			param.y = (float)(cz - cx) * screen.tileAlign.y - (this.cell.Right.sourceFloor.tileType.IsDeepWater ? 0.6f : 0.4f) + (float)(int)this.cell.Right.height * _heightMod.y; // [!code ++]
			param.z = 1000f + param.x * screen.tileWeight.x + param.y * screen.tileWeight.z; // [!code ++]
			param.tile = num5 + ((!this.cell.Right.sourceFloor.tileType.IsDeepWater) ? 3000000 : 0); // [!code ++]
			rendererWaterBlock.Draw(param); // [!code ++]
		} // [!code ++]
		param.y = orgY; // [!code ++]
		param.z = orgZ; // [!code ++]
	}
	param.y += (flag ? 0f : ((this.cell._bridge != 0) ? this.cell.sourceBridge.tileType.FloorHeight : sourceFloor.tileType.FloorHeight)); // [!code --]
	orgPos.x = (orgX = param.x); // [!code --]
	orgPos.y = (orgY = param.y); // [!code --]
	orgPos.z = (orgZ = param.z); // [!code --]
	if (flag && liquidLv > 0) // [!code --]
	if (showBorder && !this.cell.outOfBounds) // [!code ++]
	{
		if (liquidLv > 10) // [!code --]
		param.matColor = 104025f; // [!code ++]
		if (cx == EMono._map.bounds.x) // [!code ++]
		{
			liquidLv = TileType.FloorWaterShallow.LiquidLV * 10; // [!code --]
			renderBorder.Draw(param, 12 + (EMono.world.date.IsNight ? 4 : 0)); // [!code ++]
		} // [!code ++]
		else if (cx == EMono._map.bounds.maxX) // [!code ++]
		{ // [!code ++]
			renderBorder.Draw(param, 13 + (EMono.world.date.IsNight ? 4 : 0)); // [!code ++]
		} // [!code ++]
		if (cz == EMono._map.bounds.z) // [!code ++]
		{ // [!code ++]
			renderBorder.Draw(param, 14 + (EMono.world.date.IsNight ? 4 : 0)); // [!code ++]
		} // [!code ++]
		else if (cz == EMono._map.bounds.maxZ) // [!code ++]
		{ // [!code ++]
			renderBorder.Draw(param, 15 + (EMono.world.date.IsNight ? 4 : 0)); // [!code ++]
		}
		liquidLv -= (int)(floatY * 0.5f); // [!code --]
		param.liquidLv = liquidLv; // [!code --]
		param.y -= TileType.FloorWaterShallow.FloorHeight; // [!code --]
	}
	Thing thing = null; // [!code --]
	bool shadow = liquidLv == 0; // [!code --]
	float num4 = 0f; // [!code --]
	float num5 = 0f; // [!code --]
	float num6 = 0f; // [!code --]
	float num7 = 0f; // [!code --]
	bool flag6 = false; // [!code --]
	float num8 = 0f; // [!code --]
	bool flag7 = false; // [!code --]
	float num9 = 0f; // [!code --]
	if (detail.things.Count > 0 && isSeen) // [!code --]
	if (this.cell.isSkyFloor || (detail != null && detail.anime != null && detail.anime.drawBlock)) // [!code ++]
	{
		_ = zSetting.max1; // [!code --]
		float num10 = 0f; // [!code --]
		for (int j = 0; j < detail.things.Count; j++) // [!code --]
		orgY = param.y; // [!code ++]
		orgZ = param.z; // [!code ++]
		SourceBlock.Row defBlock2 = sourceFloor._defBlock; // [!code ++]
		param.mat = matFloor; // [!code ++]
		param.tile = defBlock2._tiles[this.cell.blockDir % defBlock2._tiles.Length]; // [!code ++]
		if (defBlock2.id != 1) // [!code ++]
		{
			Thing t = detail.things[j]; // [!code --]
			if ((fogged && !t.isRoofItem) || ((t.isHidden || t.trait.HideInAdv || t.isMasked) && !EMono.scene.actionMode.ShowMaskedThings) || (t.isRoofItem && ((this.room == null && !sourceBlock.tileType.IsFullBlock && !EMono._zone.IsPCFaction && !buildMode) || (lowBlock && !showFullWall && this.room != null) || (noRoofMode && currentRoom == null))) || (flag3 && !t.isRoofItem)) // [!code --]
			param.matColor = ((sourceFloor.colorMod == 0) ? 104025 : GetColorInt(ref matFloor.matColor, sourceFloor.colorMod)); // [!code ++]
		} // [!code ++]
		else // [!code ++]
		{ // [!code ++]
			param.matColor = 104025f; // [!code ++]
		} // [!code ++]
		for (int k = 0; k < ((!this.cell.isSkyFloor) ? 1 : EMono._map.config.skyBlockHeight); k++) // [!code ++]
		{ // [!code ++]
			param.y += ugFix.y; // [!code ++]
			param.z += ugFix.z + slopeFixZ * (float)k; // [!code ++]
			defBlock2.renderData.Draw(param); // [!code ++]
		} // [!code ++]
		param.y = orgY; // [!code ++]
		param.z = orgZ; // [!code ++]
	} // [!code ++]
	if (!sourceFloor.tileType.IsSkipFloor) // [!code ++]
	{ // [!code ++]
		if ((hasBridge && sourceBridge.tileType.CastShadowSelf) || this.cell.castFloorShadow) // [!code ++]
		{ // [!code ++]
			floorLight2 = _lightMod * light * 0.2f + _baseBrightness + _shadowStrength * floorShadowStrength * (isWater ? 0.7f : (hasBridge ? 1f : (0.6f * (1f - nightRatio)))); // [!code ++]
			if (snowed) // [!code ++]
			{
				continue; // [!code --]
				floorLight2 = (int)((double)floorLight2 * 0.85 * 50.0) * 262144 + snowColorToken; // [!code ++]
			}
			TileType tileType = t.trait.tileType; // [!code --]
			bool isInstalled = t.IsInstalled; // [!code --]
			SourcePref pref = t.Pref; // [!code --]
			if (!isInstalled && t.category.tileDummy != 0) // [!code --]
			else // [!code ++]
			{
				pref = rendererObjDummy.shadowPref; // [!code --]
				floorLight2 = (int)(floorLight2 * 50f) * 262144 + ((this.cell.lightR >= 64) ? 63 : this.cell.lightR) * 4096 + ((this.cell.lightG >= 64) ? 63 : this.cell.lightG) * 64 + ((this.cell.lightB >= 64) ? 63 : this.cell.lightB); // [!code ++]
			}
			float num11 = ((tileType.UseMountHeight && isInstalled) ? 0f : ((pref.height < 0f) ? 0f : ((pref.height == 0f) ? 0.1f : pref.height))); // [!code --]
			if (t.ignoreStackHeight) // [!code --]
			param.color = floorLight2; // [!code ++]
			if (this.cell.lotShade) // [!code ++]
			{
				thingPos.y -= num4; // [!code --]
				thingPos -= altitudeFix * num5; // [!code --]
				floorLight = floorLight2; // [!code ++]
			}
			shadow = thingPos.y < 0.16f && num9 < 0.16f; // [!code --]
			_ = pref.bypassShadow; // [!code --]
			param.shadowFix = 0f - thingPos.y; // [!code --]
			param.liquidLv = ((thingPos.y + (float)t.altitude < 0.1f) ? liquidLv : 0); // [!code --]
			if (t.isRoofItem) // [!code --]
		} // [!code ++]
		floorMatColor = ((sourceFloor.colorMod == 0) ? 104025 : GetColorInt(ref matFloor.matColor, sourceFloor.colorMod)); // [!code ++]
		if (isWater && flag) // [!code ++]
		{ // [!code ++]
			param.y -= 0.01f * floatY; // [!code ++]
		} // [!code ++]
		if (!sourceBlock.tileType.IsSkipFloor || sourceBlock.transparent || hasBridge || this.cell.hasDoor || this.cell.skipRender) // [!code ++]
		{ // [!code ++]
			param.mat = matFloor; // [!code ++]
			param.tile = sourceFloor._tiles[floorDir % sourceFloor._tiles.Length]; // [!code ++]
			param.matColor = floorMatColor; // [!code ++]
			param.snow = snowed; // [!code ++]
			if (sourceFloor.anime.Length != 0) // [!code ++]
			{
				param.snow = isSnowCovered && !this.cell.isClearSnow; // [!code --]
				SetRoofHeight(param, this.cell, cx, cz); // [!code --]
				_actorPos.x = param.x; // [!code --]
				_actorPos.y = param.y; // [!code --]
				_actorPos.z = param.z + num10; // [!code --]
				if (this.room != null) // [!code --]
				int num6 = (int)(Time.realtimeSinceStartup * 1000f / (float)sourceFloor.anime[1] % (float)sourceFloor.anime[0]); // [!code ++]
				param.tile += num6; // [!code ++]
			} // [!code ++]
			if (this.cell.isDeck) // [!code ++]
			{ // [!code ++]
				param.z += 1f; // [!code ++]
				if ((bool)sourceFloor.renderData.subData) // [!code ++]
				{
					param.color = GetRoofLight(this.room.lot); // [!code --]
					sourceFloor.renderData.subData.Draw(param); // [!code ++]
				}
				shadow = false; // [!code --]
				param.liquidLv = 0; // [!code --]
				sourceFloor.renderData.Draw(param); // [!code ++]
				param.z -= 1f; // [!code ++]
			}
			else
			{
				param.snow = snowed; // [!code --]
				_actorPos.x = orgX + num7; // [!code --]
				_actorPos.y = orgY; // [!code --]
				_actorPos.z = orgZ + num10 + thingPos.z; // [!code --]
				if (tileType.CanStack || !isInstalled) // [!code --]
				if ((bool)sourceFloor.renderData.subData) // [!code ++]
				{
					if (thing?.id != t.id) // [!code --]
					sourceFloor.renderData.subData.Draw(param); // [!code ++]
				} // [!code ++]
				sourceFloor.renderData.Draw(param); // [!code ++]
			} // [!code ++]
			int num7 = 0; // [!code ++]
			if (isSnowCovered && sourceFloor == FLOOR.sourceSnow && !this.cell.hasDoor) // [!code ++]
			{ // [!code ++]
				if (!this.cell.Right.IsSnowTile && this.cell.Right.topHeight == this.cell.topHeight) // [!code ++]
				{ // [!code ++]
					num7++; // [!code ++]
				} // [!code ++]
				if (!this.cell.Front.IsSnowTile && this.cell.Front.topHeight == this.cell.topHeight) // [!code ++]
				{ // [!code ++]
					num7 += 2; // [!code ++]
				} // [!code ++]
				if (num7 != 0) // [!code ++]
				{ // [!code ++]
					param.tile = 448 + num7 + 12; // [!code ++]
					param.z -= 0.1f; // [!code ++]
					sourceFloor.renderData.Draw(param); // [!code ++]
					param.z += 0.1f; // [!code ++]
				} // [!code ++]
			} // [!code ++]
			if (this.cell.shadow != 0 && !hasBridge && !this.cell.skipRender) // [!code ++]
			{ // [!code ++]
				if (snowed) // [!code ++]
				{ // [!code ++]
					if (sourceFloor == FLOOR.sourceSnow) // [!code ++]
					{
						_actorPos.x += thingPos.x; // [!code --]
						param.tile = 448 + this.cell.shadow + 8 + (this.cell.HasFence ? 4 : 0); // [!code ++]
						param.z -= 0.01f; // [!code ++]
						sourceFloor.renderData.Draw(param); // [!code ++]
					}
					_actorPos.y += thingPos.y; // [!code --]
					if (t.trait.IgnoreLastStackHeight && (thing == null || !thing.trait.IgnoreLastStackHeight)) // [!code --]
				} // [!code ++]
				else // [!code ++]
				{ // [!code ++]
					pass = passEdge; // [!code ++]
					batch = pass.batches[pass.batchIdx]; // [!code ++]
					batch.matrices[pass.idx].m03 = param.x + ambientShadowFix[this.cell.shadow].x; // [!code ++]
					batch.matrices[pass.idx].m13 = param.y + ambientShadowFix[this.cell.shadow].y; // [!code ++]
					batch.matrices[pass.idx].m23 = param.z + ambientShadowFix[this.cell.shadow].z; // [!code ++]
					batch.tiles[pass.idx] = 448 + this.cell.shadow; // [!code ++]
					batch.colors[pass.idx] = param.color; // [!code ++]
					batch.matColors[pass.idx] = 104025f; // [!code ++]
					pass.idx++; // [!code ++]
					if (pass.idx == pass.batchSize) // [!code ++]
					{
						thingPos.y -= num4; // [!code --]
						if (thing != null) // [!code --]
						{ // [!code --]
							_actorPos.z -= 0.2f; // [!code --]
							thingPos.z -= 0.2f; // [!code --]
						} // [!code --]
						_actorPos.y -= num4; // [!code --]
						pass.NextBatch(); // [!code ++]
					}
					_actorPos.z += renderSetting.thingZ + (float)j * -0.01f + zSetting.mod1 * thingPos.y; // [!code --]
				}
				if (isInstalled) // [!code --]
				if (!sourceFloor.ignoreTransition && !snowed) // [!code ++]
				{
					if (t.TileType.IsRamp) // [!code --]
					Cell back = this.cell.Back; // [!code ++]
					if (back.sourceBlock.transition[0] != -1 && back.isSeen && !back.hasDoor) // [!code ++]
					{
						Vector3 rampFix2 = t.TileType.GetRampFix(t.dir, pref); // [!code --]
						orgX += rampFix2.x; // [!code --]
						orgY += rampFix2.y; // [!code --]
						orgZ += rampFix2.z; // [!code --]
						freePos.x += rampFix2.x; // [!code --]
						freePos.y += rampFix2.y; // [!code --]
						freePos.z += rampFix2.z; // [!code --]
						if (!this.cell.IsTopWater || t.altitude > 0) // [!code --]
						{ // [!code --]
							num9 += rampFix2.y; // [!code --]
						} // [!code --]
						liquidLv -= (int)(rampFix2.y * 150f); // [!code --]
						if (liquidLv < 0) // [!code --]
						{ // [!code --]
							liquidLv = 0; // [!code --]
						} // [!code --]
					} // [!code --]
					else if (!flag7 && t.trait.IsChangeFloorHeight && !t.ignoreStackHeight) // [!code --]
					{ // [!code --]
						orgY += num11 + (float)t.altitude * altitudeFix.y; // [!code --]
						orgZ += (float)t.altitude * altitudeFix.z; // [!code --]
						freePos.y += num11 + (float)t.altitude * altitudeFix.y; // [!code --]
						if (!this.cell.IsTopWater || t.altitude > 0) // [!code --]
						{ // [!code --]
							num9 += num11 + (float)t.altitude * altitudeFix.y; // [!code --]
						} // [!code --]
						_actorPos.x += pref.x * (float)((!t.flipX) ? 1 : (-1)); // [!code --]
						_actorPos.z += pref.z; // [!code --]
						thingPos.z += pref.z; // [!code --]
						liquidLv -= (int)(num11 * 150f); // [!code --]
						if (liquidLv < 0) // [!code --]
						pass = passFloor; // [!code ++]
						batch = pass.batches[pass.batchIdx]; // [!code ++]
						batch.matrices[pass.idx].m03 = param.x + transitionFix[0].x; // [!code ++]
						batch.matrices[pass.idx].m13 = param.y + transitionFix[0].y; // [!code ++]
						batch.matrices[pass.idx].m23 = param.z + transitionFix[0].z; // [!code ++]
						batch.tiles[pass.idx] = 480 + back.sourceBlock.transition[0] + Rand.bytes[index % Rand.MaxBytes] % back.sourceBlock.transition[1]; // [!code ++]
						batch.colors[pass.idx] = param.color; // [!code ++]
						batch.matColors[pass.idx] = GetColorInt(ref back.matBlock.matColor, back.sourceBlock.colorMod); // [!code ++]
						pass.idx++; // [!code ++]
						if (pass.idx == pass.batchSize) // [!code ++]
						{
							liquidLv = 0; // [!code --]
							pass.NextBatch(); // [!code ++]
						}
					}
					else // [!code --]
					back = this.cell.Left; // [!code ++]
					if (back.sourceBlock.transition[0] != -1 && back.isSeen && !back.hasDoor) // [!code ++]
					{
						thingPos.y += num11; // [!code --]
						if (tileType.UseMountHeight) // [!code --]
						{ // [!code --]
							if ((tileType != TileType.Illumination || !this.cell.HasObj) && !tileType.AlwaysShow) // [!code --]
							{ // [!code --]
								if (noRoofMode && currentRoom == null && t.altitude >= lowWallObjAltitude && tileType != TileType.ObjFloat) // [!code --]
								{ // [!code --]
									continue; // [!code --]
								} // [!code --]
								if (hideHang && (this.cell.room?.lot != currentLot || (!this.cell.lotWall && this.cell.room != currentRoom))) // [!code --]
								{ // [!code --]
									Room room = ((t.dir == 0) ? this.cell.Back.room : this.cell.Left.room); // [!code --]
									if (t.trait.AlwaysHideOnLowWall) // [!code --]
									{ // [!code --]
										if (room == null || !room.data.showWallItem) // [!code --]
										{ // [!code --]
											continue; // [!code --]
										} // [!code --]
									} // [!code --]
									else if (t.altitude >= lowWallObjAltitude) // [!code --]
									{ // [!code --]
										continue; // [!code --]
									} // [!code --]
								} // [!code --]
							} // [!code --]
							if (tileType.UseHangZFix) // [!code --]
							{ // [!code --]
								flag6 = true; // [!code --]
							} // [!code --]
							tileType.GetMountHeight(ref _actorPos, Point.shared.Set(index), t.dir, t); // [!code --]
							shadow = false; // [!code --]
							param.liquidLv = 0; // [!code --]
							if (t.freePos) // [!code --]
							{ // [!code --]
								_actorPos.x += t.fx; // [!code --]
								_actorPos.y += t.fy; // [!code --]
							} // [!code --]
						} // [!code --]
						else // [!code --]
						{ // [!code --]
							thingPos.y += (float)t.altitude * altitudeFix.y; // [!code --]
							thingPos.z += (float)t.altitude * altitudeFix.z; // [!code --]
						} // [!code --]
						_actorPos.x += pref.x * (float)((!t.flipX) ? 1 : (-1)); // [!code --]
						_actorPos.z += pref.z; // [!code --]
						if (pref.height >= 0f) // [!code --]
						pass = passFloor; // [!code ++]
						batch = pass.batches[pass.batchIdx]; // [!code ++]
						batch.matrices[pass.idx].m03 = param.x + transitionFix[1].x; // [!code ++]
						batch.matrices[pass.idx].m13 = param.y + transitionFix[1].y; // [!code ++]
						batch.matrices[pass.idx].m23 = param.z + transitionFix[1].z; // [!code ++]
						batch.tiles[pass.idx] = 512 + back.sourceBlock.transition[0] + Rand.bytes[index % Rand.MaxBytes] % back.sourceBlock.transition[1]; // [!code ++]
						batch.colors[pass.idx] = param.color; // [!code ++]
						batch.matColors[pass.idx] = GetColorInt(ref back.matBlock.matColor, back.sourceBlock.colorMod); // [!code ++]
						pass.idx++; // [!code ++]
						if (pass.idx == pass.batchSize) // [!code ++]
						{
							thingPos.z += pref.z; // [!code --]
							pass.NextBatch(); // [!code ++]
						}
					}
					if (!tileType.UseMountHeight && j > 10) // [!code --]
					{ // [!code --]
						flag7 = true; // [!code --]
					} // [!code --]
				}
				else // [!code --]
			} // [!code ++]
			if (this.cell.autotile != 0 && sourceFloor.autotile != 0 && (!hasBridge || this.cell.bridgeHeight - this.cell.height > 3) && !this.cell.skipRender && num7 == 0) // [!code ++]
			{ // [!code ++]
				pass = (isWater ? passAutoTileWater : passAutoTile); // [!code ++]
				batch = pass.batches[pass.batchIdx]; // [!code ++]
				batch.matrices[pass.idx].m03 = param.x; // [!code ++]
				batch.matrices[pass.idx].m13 = param.y; // [!code ++]
				batch.matrices[pass.idx].m23 = param.z + ((hasBridge || this.cell._block != 0) ? 0.8f : 0f); // [!code ++]
				batch.tiles[pass.idx] = (26 + sourceFloor.autotile / 2) * 32 + sourceFloor.autotile % 2 * 16 + this.cell.autotile; // [!code ++]
				batch.colors[pass.idx] = param.color + (float)((int)(sourceFloor.autotileBrightness * 100f) * 262144); // [!code ++]
				batch.matColors[pass.idx] = param.matColor; // [!code ++]
				pass.idx++; // [!code ++]
				if (pass.idx == pass.batchSize) // [!code ++]
				{
					thingPos.y += num11; // [!code --]
					_actorPos.x += pref.x * (float)((!t.flipX) ? 1 : (-1)); // [!code --]
					_actorPos.z += pref.z; // [!code --]
					thingPos.z += pref.z; // [!code --]
					pass.NextBatch(); // [!code ++]
				}
				if (t.isFloating && isWater && !hasBridge && !flag) // [!code --]
			} // [!code ++]
		} // [!code ++]
		if (isWater) // [!code ++]
		{ // [!code ++]
			int num8 = 12; // [!code ++]
			int num9 = this.cell.shore / num8; // [!code ++]
			int num10 = this.cell.shore % num8; // [!code ++]
			bool isShoreSand = this.cell.isShoreSand; // [!code ++]
			if (this.cell.shore != 0) // [!code ++]
			{ // [!code ++]
				Cell cell = ((((uint)num9 & (true ? 1u : 0u)) != 0) ? this.cell.Back : ((((uint)num9 & 2u) != 0) ? this.cell.Right : ((((uint)num9 & 4u) != 0) ? this.cell.Front : this.cell.Left))); // [!code ++]
				if (isShoreSand && !cell.sourceFloor.isBeach) // [!code ++]
				{
					flag = true; // [!code --]
					float num12 = ((this.cell._bridge != 0) ? sourceBridge.tileType.FloorHeight : sourceFloor.tileType.FloorHeight); // [!code --]
					orgY += 0.01f * floatY - num12; // [!code --]
					if (!t.trait.IsChangeFloorHeight) // [!code --]
					{ // [!code --]
						num8 = num11; // [!code --]
					} // [!code --]
					_actorPos.y += 0.01f * floatY - num12; // [!code --]
					if (liquidLv > 10) // [!code --]
					cell = ((((uint)num9 & 8u) != 0) ? this.cell.Left : ((((uint)num9 & 4u) != 0) ? this.cell.Front : ((((uint)num9 & 2u) != 0) ? this.cell.Right : this.cell.Back))); // [!code ++]
				} // [!code ++]
				if (!cell.IsSnowTile) // [!code ++]
				{ // [!code ++]
					param.matColor = GetColorInt(ref cell.matFloor.matColor, cell.sourceFloor.colorMod); // [!code ++]
					if (isShoreSand) // [!code ++]
					{
						liquidLv = TileType.FloorWaterShallow.LiquidLV * 10; // [!code --]
						pass = passShore; // [!code ++]
						batch = pass.batches[pass.batchIdx]; // [!code ++]
						batch.matrices[pass.idx].m03 = param.x; // [!code ++]
						batch.matrices[pass.idx].m13 = param.y; // [!code ++]
						batch.matrices[pass.idx].m23 = param.z; // [!code ++]
						batch.tiles[pass.idx] = 768 + this.cell.shore / num8; // [!code ++]
						batch.colors[pass.idx] = param.color; // [!code ++]
						batch.matColors[pass.idx] = param.matColor; // [!code ++]
						pass.idx++; // [!code ++]
						if (pass.idx == pass.batchSize) // [!code ++]
						{ // [!code ++]
							pass.NextBatch(); // [!code ++]
						} // [!code ++]
						num10 = 2; // [!code ++]
					}
					liquidLv -= (int)(floatY * 0.5f); // [!code --]
					if (liquidLv < 0) // [!code --]
					else // [!code ++]
					{
						liquidLv = 0; // [!code --]
						num10 = cell.sourceFloor.edge; // [!code ++]
					}
					param.liquidLv = liquidLv; // [!code --]
					param.tile = (24 + num10 / 2) * 32 + num10 % 2 * 16 + num9; // [!code ++]
					rendererShore.Draw(param); // [!code ++]
				}
				num4 = num11; // [!code --]
				if (t.sourceCard.multisize && !t.trait.IsGround) // [!code --]
			} // [!code ++]
			if (this.cell.Back.isShoreSand && ((uint)(this.cell.Back.shore / num8) & 8u) != 0 && this.cell.Left.isShoreSand && ((uint)(this.cell.Left.shore / num8) & (true ? 1u : 0u)) != 0) // [!code ++]
			{ // [!code ++]
				param.tile = 785f; // [!code ++]
				param.matColor = GetColorInt(ref this.cell.BackLeft.matFloor.matColor, this.cell.BackLeft.sourceFloor.colorMod); // [!code ++]
				passShore.Add(param); // [!code ++]
				Draw(60); // [!code ++]
			} // [!code ++]
			if (this.cell.Back.isShoreSand && ((uint)(this.cell.Back.shore / num8) & 2u) != 0 && this.cell.Right.isShoreSand && ((uint)(this.cell.Right.shore / num8) & (true ? 1u : 0u)) != 0) // [!code ++]
			{ // [!code ++]
				param.tile = 786f; // [!code ++]
				param.matColor = GetColorInt(ref this.cell.BackRight.matFloor.matColor, this.cell.BackRight.sourceFloor.colorMod); // [!code ++]
				passShore.Add(param); // [!code ++]
				Draw(56); // [!code ++]
			} // [!code ++]
			if (this.cell.Front.isShoreSand && ((uint)(this.cell.Front.shore / num8) & 2u) != 0 && this.cell.Right.isShoreSand && ((uint)(this.cell.Right.shore / num8) & 4u) != 0) // [!code ++]
			{ // [!code ++]
				param.tile = 787f; // [!code ++]
				param.matColor = GetColorInt(ref this.cell.FrontRight.matFloor.matColor, this.cell.FrontRight.sourceFloor.colorMod); // [!code ++]
				passShore.Add(param); // [!code ++]
				Draw(48); // [!code ++]
			} // [!code ++]
			if (this.cell.Front.isShoreSand && ((uint)(this.cell.Front.shore / num8) & 8u) != 0 && this.cell.Left.isShoreSand && ((uint)(this.cell.Left.shore / num8) & 4u) != 0) // [!code ++]
			{ // [!code ++]
				param.tile = 788f; // [!code ++]
				param.matColor = GetColorInt(ref this.cell.FrontLeft.matFloor.matColor, this.cell.FrontLeft.sourceFloor.colorMod); // [!code ++]
				passShore.Add(param); // [!code ++]
				Draw(52); // [!code ++]
			} // [!code ++]
			if (this.cell._bridge != 0 && this.cell.isBridgeEdge && this.cell.bridgePillar != byte.MaxValue) // [!code ++]
			{ // [!code ++]
				pass = passEdge; // [!code ++]
				batch = pass.batches[pass.batchIdx]; // [!code ++]
				batch.matrices[pass.idx].m03 = param.x + waterEdgeBridgeFix.x; // [!code ++]
				batch.matrices[pass.idx].m13 = param.y + waterEdgeBridgeFix.y; // [!code ++]
				batch.matrices[pass.idx].m23 = param.z + waterEdgeBridgeFix.z; // [!code ++]
				batch.tiles[pass.idx] = 616 + waterAnimeIndex % 4; // [!code ++]
				batch.colors[pass.idx] = param.color; // [!code ++]
				batch.matColors[pass.idx] = 104025f; // [!code ++]
				pass.idx++; // [!code ++]
				if (pass.idx == pass.batchSize) // [!code ++]
				{
					num10 += zSetting.multiZ; // [!code --]
					pass.NextBatch(); // [!code ++]
				}
				orgZ += t.renderer.data.stackZ; // [!code --]
				if (param.liquidLv > 0) // [!code --]
			} // [!code ++]
			bool flag6 = false; // [!code ++]
			if (isShoreSand) // [!code ++]
			{ // [!code ++]
				if (((uint)num9 & (true ? 1u : 0u)) != 0) // [!code ++]
				{
					param.liquidLv += pref.liquidMod; // [!code --]
					if (param.liquidLv < 1) // [!code --]
					if (((uint)num9 & 8u) != 0) // [!code ++]
					{
						param.liquidLv = 1; // [!code --]
						if ((num9 & 2) == 0 && (num9 & 4) == 0) // [!code ++]
						{ // [!code ++]
							Draw(16); // [!code ++]
						} // [!code ++]
						flag6 = true; // [!code ++]
					}
					else if (param.liquidLv > 99 + pref.liquidModMax) // [!code --]
					if (((uint)num9 & 2u) != 0) // [!code ++]
					{
						param.liquidLv = 99 + pref.liquidModMax; // [!code --]
						if ((num9 & 8) == 0 && (num9 & 4) == 0) // [!code ++]
						{ // [!code ++]
							Draw(20); // [!code ++]
						} // [!code ++]
						flag6 = true; // [!code ++]
					}
				}
			} // [!code --]
			if (!isInstalled || !tileType.UseMountHeight) // [!code --]
			{ // [!code --]
				if (t.altitude != 0) // [!code --]
				if (((uint)num9 & 4u) != 0) // [!code ++]
				{
					_actorPos += altitudeFix * t.altitude; // [!code --]
					if (!t.isRoofItem) // [!code --]
					if (((uint)num9 & 8u) != 0) // [!code ++]
					{
						num6 += (float)t.altitude; // [!code --]
						num5 = t.altitude; // [!code --]
						if ((num9 & 2) == 0 && (num9 & 1) == 0) // [!code ++]
						{ // [!code ++]
							Draw(24); // [!code ++]
						} // [!code ++]
						flag6 = true; // [!code ++]
					} // [!code ++]
					if (((uint)num9 & 2u) != 0) // [!code ++]
					{ // [!code ++]
						if ((num9 & 8) == 0 && (num9 & 1) == 0) // [!code ++]
						{ // [!code ++]
							Draw(28); // [!code ++]
						} // [!code ++]
						flag6 = true; // [!code ++]
					}
				}
				if (num6 >= 2f && ((this.cell.Back.room != null && this.cell.Back.IsRoomEdge) || (this.cell.Left.room != null && this.cell.Left.IsRoomEdge)) && hideHang && (this.cell.room?.lot != currentLot || (!this.cell.lotWall && this.cell.room != currentRoom))) // [!code --]
				{ // [!code --]
					continue; // [!code --]
				} // [!code --]
				if (t.freePos) // [!code --]
				if (!flag6) // [!code ++]
				{
					if (t.isRoofItem) // [!code --]
					if (!this.cell.Front.sourceFloor.tileType.IsWater && !this.cell.Front.isDeck) // [!code ++]
					{
						_actorPos.x += t.fx; // [!code --]
						_actorPos.y += t.fy - (float)t.altitude * altitudeFix.y; // [!code --]
						Draw(8); // [!code ++]
					}
					else // [!code --]
					if (!this.cell.Right.sourceFloor.tileType.IsWater && !this.cell.Right.isDeck) // [!code ++]
					{
						_actorPos.x = orgX + t.fx - freePos.x; // [!code --]
						_actorPos.y = orgY + t.fy - freePos.y; // [!code --]
						Draw(12); // [!code ++]
					}
				}
				if (t.trait is TraitDoor && (t.trait as TraitDoor).IsOpen()) // [!code --]
				{ // [!code --]
					_actorPos.z += -0.5f; // [!code --]
				} // [!code --]
			}
			if (!t.sourceCard.multisize || (t.pos.x == cx && t.pos.z == cz)) // [!code --]
			if (!flag6) // [!code ++]
			{
				if (iconMode != 0) // [!code --]
				if (!this.cell.Back.sourceFloor.tileType.IsWater && !this.cell.Back.isDeck) // [!code ++]
				{
					int num13 = 0; // [!code --]
					switch (iconMode) // [!code --]
					pass = passEdge; // [!code ++]
					batch = pass.batches[pass.batchIdx]; // [!code ++]
					batch.tiles[pass.idx] = 608 + waterAnimeIndex % 4; // [!code ++]
					batch.matColors[pass.idx] = 104025f; // [!code ++]
					if (((uint)(this.cell.shore / num8) & (true ? 1u : 0u)) != 0) // [!code ++]
					{
					case CardIconMode.Visibility: // [!code --]
						if (t.isMasked) // [!code --]
						{ // [!code --]
							num13 = 17; // [!code --]
						} // [!code --]
						break; // [!code --]
					case CardIconMode.State: // [!code --]
						if (t.placeState == PlaceState.installed) // [!code --]
						if (isShoreSand) // [!code ++]
						{
							num13 = 18; // [!code --]
							param.matColor = GetColorInt(ref this.cell.Back.matFloor.matColor, this.cell.Back.sourceFloor.colorMod); // [!code ++]
							batch.matrices[pass.idx].m03 = param.x + waterEdgeFixShoreSand.x; // [!code ++]
							batch.matrices[pass.idx].m13 = param.y + waterEdgeFixShoreSand.y; // [!code ++]
							batch.matrices[pass.idx].m23 = param.z + waterEdgeFixShoreSand.z; // [!code ++]
							batch.tiles[pass.idx] = 640 + seaAnimeIndexes[waterAnimeIndex % seaAnimeIndexes.Length]; // [!code ++]
							batch.matColors[pass.idx] = param.matColor; // [!code ++]
						}
						break; // [!code --]
					case CardIconMode.Deconstruct: // [!code --]
						if (t.isDeconstructing) // [!code --]
						else // [!code ++]
						{
							num13 = 14; // [!code --]
							batch.matrices[pass.idx].m03 = param.x; // [!code ++]
							batch.matrices[pass.idx].m13 = param.y; // [!code ++]
							batch.matrices[pass.idx].m23 = param.z + waterEdgeFixShore.z; // [!code ++]
						}
						break; // [!code --]
					}
					if (t.isNPCProperty && !EMono.debug.godBuild) // [!code --]
					else // [!code ++]
					{
						num13 = 13; // [!code --]
						batch.matrices[pass.idx].m03 = param.x; // [!code ++]
						batch.matrices[pass.idx].m13 = param.y; // [!code ++]
						batch.matrices[pass.idx].m23 = param.z + waterEdgeFix.z; // [!code ++]
						batch.tiles[pass.idx] += 12f; // [!code ++]
					}
					if (num13 != 0) // [!code --]
					batch.colors[pass.idx] = param.color; // [!code ++]
					pass.idx++; // [!code ++]
					if (pass.idx == pass.batchSize) // [!code ++]
					{
						passGuideBlock.Add(_actorPos.x, _actorPos.y, _actorPos.z - 10f, num13); // [!code --]
						pass.NextBatch(); // [!code ++]
					}
				}
				t.SetRenderParam(param); // [!code --]
				if (_lowblock && t.trait.UseLowblock && !this.cell.HasFullBlock) // [!code --]
				{ // [!code --]
					param.tile += ((param.tile < 0f) ? (-64) : 64); // [!code --]
				} // [!code --]
				if (t.trait is TraitTrolley && EMono.pc.ai is AI_Trolley aI_Trolley && aI_Trolley.trolley.owner == t) // [!code --]
				if (!this.cell.Left.sourceFloor.tileType.IsWater && !this.cell.Left.isDeck) // [!code ++]
				{
					RenderParam _param = new RenderParam(param); // [!code --]
					EMono.core.actionsLateUpdate.Add(delegate // [!code --]
					pass = passEdge; // [!code ++]
					batch = pass.batches[pass.batchIdx]; // [!code ++]
					batch.tiles[pass.idx] = 612 + waterAnimeIndex % 4; // [!code ++]
					batch.matColors[pass.idx] = 104025f; // [!code ++]
					if (((uint)(this.cell.shore / num8) & 8u) != 0) // [!code ++]
					{
						t.SetRenderParam(_param); // [!code --]
						_actorPos.x = EMono.pc.renderer.position.x; // [!code --]
						_actorPos.y = EMono.pc.renderer.position.y - pref.height; // [!code --]
						_actorPos.z = EMono.pc.renderer.position.z + 0.02f; // [!code --]
						t.renderer.Draw(_param, ref _actorPos, !t.noShadow && (shadow || tileType.AlwaysShowShadow)); // [!code --]
					}); // [!code --]
				} // [!code --]
				else // [!code --]
				{ // [!code --]
					t.renderer.Draw(param, ref _actorPos, !t.noShadow && (shadow || tileType.AlwaysShowShadow)); // [!code --]
						if (isShoreSand) // [!code ++]
						{ // [!code ++]
							param.matColor = GetColorInt(ref this.cell.Left.matFloor.matColor, this.cell.Left.sourceFloor.colorMod); // [!code ++]
							batch.matrices[pass.idx].m03 = param.x + waterEdgeFixShoreSand.x; // [!code ++]
							batch.matrices[pass.idx].m13 = param.y + waterEdgeFixShoreSand.y; // [!code ++]
							batch.matrices[pass.idx].m23 = param.z + waterEdgeFixShoreSand.z; // [!code ++]
							batch.tiles[pass.idx] = 644 + seaAnimeIndexes[waterAnimeIndex % seaAnimeIndexes.Length]; // [!code ++]
							batch.matColors[pass.idx] = param.matColor; // [!code ++]
						} // [!code ++]
						else // [!code ++]
						{ // [!code ++]
							batch.matrices[pass.idx].m03 = param.x; // [!code ++]
							batch.matrices[pass.idx].m13 = param.y; // [!code ++]
							batch.matrices[pass.idx].m23 = param.z + waterEdgeFixShore.z; // [!code ++]
						} // [!code ++]
					} // [!code ++]
					else // [!code ++]
					{ // [!code ++]
						batch.matrices[pass.idx].m03 = param.x; // [!code ++]
						batch.matrices[pass.idx].m13 = param.y; // [!code ++]
						batch.matrices[pass.idx].m23 = param.z + waterEdgeFix.z; // [!code ++]
						batch.tiles[pass.idx] += 12f; // [!code ++]
					} // [!code ++]
					batch.colors[pass.idx] = param.color; // [!code ++]
					pass.idx++; // [!code ++]
					if (pass.idx == pass.batchSize) // [!code ++]
					{ // [!code ++]
						pass.NextBatch(); // [!code ++]
					} // [!code ++]
				}
			}
			if (isInstalled) // [!code --]
			{ // [!code --]
				num7 += pref.stackX * (float)((!t.flipX) ? 1 : (-1)); // [!code --]
			} // [!code --]
			param.x = orgX; // [!code --]
			param.y = orgY; // [!code --]
			param.z = orgZ; // [!code --]
			param.color = floorLight; // [!code --]
			thing = t; // [!code --]
			if (pref.Float) // [!code --]
			if (flag) // [!code ++]
			{
				liquidLv = 0; // [!code --]
				param.y += 0.01f * floatY; // [!code ++]
			}
		}
		if (flag) // [!code ++]
		{ // [!code ++]
			param.z -= 1f; // [!code ++]
		} // [!code ++]
	}
	orgY += num8; // [!code --]
	if (detail.charas.Count <= 0) // [!code --]
	if (this.cell.skipRender) // [!code ++]
	{
		if (this.cell.pcSync) // [!code ++]
		{ // [!code ++]
			param.tile = 0f; // [!code ++]
			rendererFov.Draw(param); // [!code ++]
		} // [!code ++]
		return;
	}
	param.shadowFix = 0f - num9; // [!code --]
	param.color += 1310720f; // [!code --]
	float max = zSetting.max2; // [!code --]
	for (int k = 0; k < detail.charas.Count; k++) // [!code --]
	if (hasBridge) // [!code ++]
	{
		Chara chara = detail.charas[k]; // [!code --]
		if (chara.host != null) // [!code --]
		param.y = (float)(cz - cx) * screen.tileAlign.y + (float)(int)this.cell.bridgeHeight * _heightMod.y; // [!code ++]
		param.z = 1000f + param.x * screen.tileWeight.x + param.y * screen.tileWeight.z + (float)(int)this.cell.bridgeHeight * _heightMod.z; // [!code ++]
		if (flag) // [!code ++]
		{
			continue; // [!code --]
			param.y += 0.01f * floatY; // [!code ++]
		}
		if (chara != EMono.pc && chara != LayerDrama.alwaysVisible) // [!code --]
		param.color = floorLight; // [!code ++]
		param.mat = matBridge; // [!code ++]
		floorMatColor = ((sourceBridge.colorMod == 0) ? 104025 : GetColorInt(ref matBridge.matColor, sourceBridge.colorMod)); // [!code ++]
		param.dir = this.cell.floorDir; // [!code ++]
		param.tile = sourceBridge._tiles[this.cell.floorDir % sourceBridge._tiles.Length]; // [!code ++]
		param.matColor = floorMatColor; // [!code ++]
		sourceBridge.renderData.Draw(param); // [!code ++]
		if (this.cell.autotileBridge != 0 && sourceBridge.autotile != 0) // [!code ++]
		{
			if (chara.mimicry != null && chara.mimicry.IsThing) // [!code --]
			{ // [!code --]
				if (flag3 || fogged || !this.cell.isSeen) // [!code --]
				{ // [!code --]
					continue; // [!code --]
				} // [!code --]
			} // [!code --]
			else if (flag3 || fogged || (!showAllCards && !EMono.player.CanSee(chara))) // [!code --]
			pass = passAutoTile; // [!code ++]
			batch = pass.batches[pass.batchIdx]; // [!code ++]
			batch.matrices[pass.idx].m03 = param.x; // [!code ++]
			batch.matrices[pass.idx].m13 = param.y; // [!code ++]
			batch.matrices[pass.idx].m23 = param.z + ((this.cell._block != 0) ? 0.8f : 0f); // [!code ++]
			batch.tiles[pass.idx] = (26 + sourceBridge.autotile / 2) * 32 + sourceBridge.autotile % 2 * 16 + this.cell.autotileBridge; // [!code ++]
			batch.colors[pass.idx] = param.color + (float)((int)(sourceBridge.autotileBrightness * 100f) * 262144); // [!code ++]
			batch.matColors[pass.idx] = param.matColor; // [!code ++]
			pass.idx++; // [!code ++]
			if (pass.idx == pass.batchSize) // [!code ++]
			{
				continue; // [!code --]
				pass.NextBatch(); // [!code ++]
			}
		}
		_actorPos.x = orgX; // [!code --]
		_actorPos.y = orgY; // [!code --]
		_actorPos.z = orgZ; // [!code --]
		if (this.cell.IsBlocked && chara.IsAstralBody) // [!code --]
		{ // [!code --]
			_actorPos.z += EMono.setting.render.astralBodyFixZ; // [!code --]
		} // [!code --]
		chara.SetRenderParam(param); // [!code --]
		_ = chara.IsAliveInCurrentZone; // [!code --]
		if (chara.isRestrained) // [!code --]
		if (this.cell.shadow != 0) // [!code ++]
		{
			TraitShackle restrainer = chara.GetRestrainer(); // [!code --]
			if (restrainer != null) // [!code --]
			if (sourceBridge == FLOOR.sourceSnow) // [!code ++]
			{
				Vector3 getRestrainPos = restrainer.GetRestrainPos; // [!code --]
				if (getRestrainPos != default(Vector3)) // [!code --]
				param.tile = 448 + this.cell.shadow + 8 + (this.cell.HasFence ? 4 : 0); // [!code ++]
				param.z -= 0.01f; // [!code ++]
				sourceBridge.renderData.Draw(param); // [!code ++]
			} // [!code ++]
			else // [!code ++]
			{ // [!code ++]
				pass = passEdge; // [!code ++]
				batch = pass.batches[pass.batchIdx]; // [!code ++]
				batch.matrices[pass.idx].m03 = param.x + ambientShadowFix[this.cell.shadow].x; // [!code ++]
				batch.matrices[pass.idx].m13 = param.y + ambientShadowFix[this.cell.shadow].y; // [!code ++]
				batch.matrices[pass.idx].m23 = param.z + ambientShadowFix[this.cell.shadow].z; // [!code ++]
				batch.tiles[pass.idx] = 448 + this.cell.shadow; // [!code ++]
				batch.colors[pass.idx] = blockLight; // [!code ++]
				batch.matColors[pass.idx] = 104025f; // [!code ++]
				pass.idx++; // [!code ++]
				if (pass.idx == pass.batchSize) // [!code ++]
				{
					Vector3 position = restrainer.owner.renderer.position; // [!code --]
					float defCharaHeight = EMono.setting.render.defCharaHeight; // [!code --]
					float num14 = getRestrainPos.y + defCharaHeight - ((chara.Pref.height == 0f) ? defCharaHeight : chara.source.pref.height); // [!code --]
					_actorPos.x = position.x + getRestrainPos.x * (float)((restrainer.owner.dir % 2 == 0) ? 1 : (-1)); // [!code --]
					_actorPos.y = position.y + num14; // [!code --]
					_actorPos.z = position.z + getRestrainPos.z; // [!code --]
					param.liquidLv = 0; // [!code --]
					param.shadowFix = orgY - _actorPos.y; // [!code --]
					chara.renderer.SetFirst(first: true); // [!code --]
					chara.renderer.Draw(param, ref _actorPos, drawShadow: true); // [!code --]
					param.shadowFix = 0f; // [!code --]
					continue; // [!code --]
					pass.NextBatch(); // [!code ++]
				}
			}
		}
		if (!chara.sourceCard.multisize || (chara.pos.x == cx && chara.pos.z == cz)) // [!code --]
		if (this.cell.isBridgeEdge && this.cell.bridgeHeight - this.cell.height >= 3 && this.cell.bridgePillar != byte.MaxValue && !noSlopMode) // [!code ++]
		{
			if (chara.IsDeadOrSleeping && chara.IsPCC) // [!code --]
			orgY = param.y; // [!code ++]
			orgZ = param.z; // [!code ++]
			param.y += bridgeFix.y; // [!code ++]
			param.z += bridgeFix.z; // [!code ++]
			param.dir = 0; // [!code ++]
			SourceBlock.Row row4 = sourceBridge._bridgeBlock; // [!code ++]
			float num11 = (float)(this.cell.bridgeHeight - this.cell.height) * _heightMod.y; // [!code ++]
			if (this.cell.sourceFloor.tileType == TileType.Sky) // [!code ++]
			{
				float num15 = chara.renderer.data.size.y * 0.3f; // [!code --]
				if (thingPos.y > max) // [!code --]
				{ // [!code --]
					thingPos.y = max; // [!code --]
				} // [!code --]
				float num16 = thingPos.y + num15; // [!code --]
				float num17 = (float)k * -0.01f; // [!code --]
				if (num16 > zSetting.thresh1) // [!code --]
				{ // [!code --]
					num17 = zSetting.mod1; // [!code --]
				} // [!code --]
				_actorPos.x += thingPos.x; // [!code --]
				_actorPos.y += thingPos.y; // [!code --]
				_actorPos.z += renderSetting.laydownZ + num17; // [!code --]
				param.liquidLv = ((thingPos.y == 0f && liquidLv > 0) ? 90 : 0); // [!code --]
				thingPos.y += num15 * 0.8f; // [!code --]
				chara.renderer.Draw(param, ref _actorPos, liquidLv == 0); // [!code --]
				num11 += (float)EMono._map.config.skyBlockHeight; // [!code ++]
			} // [!code ++]
			int num12 = (int)(num11 / heightBlockSize) + 2; // [!code ++]
			if (this.cell.bridgePillar != 0) // [!code ++]
			{ // [!code ++]
				row4 = EMono.sources.blocks.rows[this.cell.bridgePillar]; // [!code ++]
				param.tile = row4._tiles[0] + ((num12 == 2) ? 32 : 0); // [!code ++]
				param.mat = ((sourceBridge.DefaultMaterial == row4.DefaultMaterial) ? sourceBridge.DefaultMaterial : row4.DefaultMaterial); // [!code ++]
				param.matColor = ((row4.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.matColor, row4.colorMod)); // [!code ++]
			}
			else
			{
				param.liquidLv = liquidLv; // [!code --]
				if (isUnderwater) // [!code --]
				{ // [!code --]
					if (chara.Pref.FloatUnderwater) // [!code --]
					{ // [!code --]
						float num18 = ((this.cell._bridge != 0) ? sourceBridge.tileType.FloorHeight : sourceFloor.tileType.FloorHeight); // [!code --]
						float num19 = floatYs[chara.uid % 10] + 10f + (float)(chara.uid % 30); // [!code --]
						orgY += 0.01f * num19 - num18; // [!code --]
						_actorPos.y += 0.01f * num19 - num18; // [!code --]
						param.shadowFix -= 0.01f * num19 - num18; // [!code --]
					} // [!code --]
				} // [!code --]
				else if (liquidLv > 0) // [!code --]
				{ // [!code --]
					if (chara.Pref.Float && !flag && !hasBridge) // [!code --]
					{ // [!code --]
						if (liquidLv > 20) // [!code --]
						{ // [!code --]
							float num20 = ((this.cell._bridge != 0) ? sourceBridge.tileType.FloorHeight : sourceFloor.tileType.FloorHeight); // [!code --]
							orgY += 0.01f * floatY - num20; // [!code --]
							_actorPos.y += 0.01f * floatY - num20; // [!code --]
							int num21 = TileType.FloorWaterShallow.LiquidLV * 10; // [!code --]
							num21 -= (int)(floatY * 0.5f); // [!code --]
							param.liquidLv = num21; // [!code --]
						} // [!code --]
						else // [!code --]
						{ // [!code --]
							param.liquidLv -= 20; // [!code --]
						} // [!code --]
					} // [!code --]
					param.liquidLv += chara.Pref.liquidMod; // [!code --]
					if (param.liquidLv < 1) // [!code --]
					{ // [!code --]
						param.liquidLv = 1; // [!code --]
					} // [!code --]
					else if (param.liquidLv > 99 + chara.Pref.liquidModMax) // [!code --]
					{ // [!code --]
						param.liquidLv = 99 + chara.Pref.liquidModMax; // [!code --]
					} // [!code --]
				} // [!code --]
				if (!chara.IsPC && EMono.pc.host != chara && !chara.renderer.IsMoving && detail.charas.Count > 1 && (detail.charas.Count != 2 || !detail.charas[0].IsDeadOrSleeping || !detail.charas[0].IsPCC)) // [!code --]
				param.mat = matBlock; // [!code ++]
				param.tile = row4._tiles[0] + 32; // [!code ++]
				param.matColor = ((row4.colorMod == 0) ? 104025 : GetColorInt(ref matBridge.matColor, row4.colorMod)); // [!code ++]
			} // [!code ++]
			param.y += ugFixBridgeTop.y; // [!code ++]
			param.z += ugFixBridgeTop.z; // [!code ++]
			for (int l = 0; l < num12; l++) // [!code ++]
			{ // [!code ++]
				if (l == num12 - 1) // [!code ++]
				{
					_actorPos += renderSetting.charaPos[1 + ((num3 < 4) ? num3 : 3)]; // [!code --]
					param.y = (float)(cz - cx) * screen.tileAlign.y + (float)height * _heightMod.y + ugFixBridgeBottom.y; // [!code ++]
					param.z = 1000f + param.x * screen.tileWeight.x + param.y * screen.tileWeight.z + (float)height * _heightMod.z + ugFixBridgeBottom.z; // [!code ++]
				}
				_actorPos.z += 0.01f * (float)k + renderSetting.charaZ; // [!code --]
				num3++; // [!code --]
				if (flag6) // [!code --]
				else // [!code ++]
				{
					_actorPos.z += chara.renderer.data.hangedFixZ; // [!code --]
					param.y += ugFixBridge.y; // [!code ++]
					param.z += ugFixBridge.z; // [!code ++]
				}
				chara.renderer.Draw(param, ref _actorPos, liquidLv == 0); // [!code --]
				row4.renderData.Draw(param); // [!code ++]
			}
			param.y = orgY; // [!code ++]
			param.z = orgZ; // [!code ++]
		}
		param.x = orgX; // [!code --]
		param.y = orgY; // [!code --]
		param.z = orgZ; // [!code --]
	}
	return; // [!code --]
	IL_16c3: // [!code --]
	if (this.cell.isSlopeEdge) // [!code --]
	if (this.cell._deco != 0) // [!code ++]
	{
		float num22 = (float)height * _heightMod.y; // [!code --]
		orgY = param.y; // [!code --]
		orgZ = param.z; // [!code --]
		param.dir = this.cell.blockDir; // [!code --]
		if (snowed) // [!code --]
		param.mat = matFloor; // [!code ++]
		param.matColor = ((sourceDeco.colorMod == 0) ? 104025 : GetColorInt(ref matDeco.matColor, sourceDeco.colorMod)); // [!code ++]
		param.snow = snowed; // [!code ++]
		if (sourceDeco.autotile != 0) // [!code ++]
		{
			param.color = floorLight; // [!code --]
		} // [!code --]
		SourceBlock.Row defBlock; // [!code --]
		if (sourceBlock.tileType.IsFullBlock) // [!code --]
		{ // [!code --]
			defBlock = sourceBlock; // [!code --]
			param.mat = matBlock; // [!code --]
			param.tile = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length]; // [!code --]
			param.matColor = ((sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, sourceBlock.colorMod)); // [!code --]
		} // [!code --]
		else // [!code --]
		{ // [!code --]
			defBlock = sourceFloor._defBlock; // [!code --]
			param.mat = matFloor; // [!code --]
			param.tile = defBlock._tiles[this.cell.blockDir % defBlock._tiles.Length]; // [!code --]
			if (defBlock.id != 1) // [!code --]
			{ // [!code --]
				param.matColor = ((sourceFloor.colorMod == 0) ? 104025 : GetColorInt(ref matFloor.matColor, sourceFloor.colorMod)); // [!code --]
			} // [!code --]
			else // [!code --]
			{ // [!code --]
				param.matColor = 104025f; // [!code --]
			} // [!code --]
		} // [!code --]
		for (int l = 0; (float)l < num22 / heightBlockSize; l++) // [!code --]
		{ // [!code --]
			param.y += ugFix.y; // [!code --]
			param.z += ugFix.z + slopeFixZ * (float)l; // [!code --]
			defBlock.renderData.Draw(param); // [!code --]
			if (this.cell.pcSync && EMono.player.lightPower > 0f) // [!code --]
			if (this.cell.autotileDeco != 0) // [!code ++]
			{
				float num23 = param.tile; // [!code --]
				param.tile = 0f; // [!code --]
				rendererFov.Draw(param); // [!code --]
				param.tile = num23; // [!code --]
				param.tile = (26 + sourceDeco.autotile / 2) * 32 + sourceDeco.autotile % 2 * 16 + this.cell.autotileDeco; // [!code ++]
				sourceDeco.renderData.Draw(param); // [!code ++]
				param.z = orgZ; // [!code ++]
			}
		}
		param.y = orgY; // [!code --]
		param.z = orgZ; // [!code --]
	} // [!code --]
	param.color = floorLight; // [!code --]
	if (!isWater && (this.cell.Front.sourceFloor.tileType.IsWater || this.cell.Right.sourceFloor.tileType.IsWater) && this.cell.sourceBlock.tileType.RenderWaterBlock && !flag) // [!code --]
	{ // [!code --]
		orgY = param.y; // [!code --]
		orgZ = param.z; // [!code --]
		int num24 = 0; // [!code --]
		if (sourceBlock.tileType.IsFullBlock) // [!code --]
		{ // [!code --]
			SourceBlock.Row row3 = sourceBlock; // [!code --]
			num24 = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length]; // [!code --]
		} // [!code --]
		else
		{
			SourceBlock.Row row3 = sourceFloor._defBlock; // [!code --]
			num24 = row3._tiles[this.cell.blockDir % row3._tiles.Length]; // [!code --]
		} // [!code --]
		if (((this.cell.Front.shore / 12) & 1) == 0 && this.cell.Front.sourceFloor.tileType.IsWater && this.cell.Front.height <= height && this.cell.Front.sourceBlock.tileType.RenderWaterBlock) // [!code --]
		{ // [!code --]
			param.y = (float)(cz - cx) * screen.tileAlign.y - (this.cell.Front.sourceFloor.tileType.IsDeepWater ? 0.6f : 0.4f) + (float)(int)this.cell.Front.height * _heightMod.y; // [!code --]
			param.z = 1000f + param.x * screen.tileWeight.x + param.y * screen.tileWeight.z; // [!code --]
			param.tile = num24 + ((!this.cell.Front.sourceFloor.tileType.IsDeepWater) ? 3000000 : 0); // [!code --]
			rendererWaterBlock.Draw(param); // [!code --]
		} // [!code --]
		if (((this.cell.Right.shore / 12) & 8) == 0 && this.cell.Right.sourceFloor.tileType.IsWater && this.cell.Right.height <= height && this.cell.Right.sourceBlock.tileType.RenderWaterBlock) // [!code --]
		{ // [!code --]
			param.y = (float)(cz - cx) * screen.tileAlign.y - (this.cell.Right.sourceFloor.tileType.IsDeepWater ? 0.6f : 0.4f) + (float)(int)this.cell.Right.height * _heightMod.y; // [!code --]
			param.z = 1000f + param.x * screen.tileWeight.x + param.y * screen.tileWeight.z; // [!code --]
			param.tile = num24 + ((!this.cell.Right.sourceFloor.tileType.IsDeepWater) ? 3000000 : 0); // [!code --]
			rendererWaterBlock.Draw(param); // [!code --]
			param.tile = sourceDeco._tiles[floorDir % sourceDeco._tiles.Length]; // [!code ++]
			sourceDeco.renderData.Draw(param); // [!code ++]
		}
		param.y = orgY; // [!code --]
		param.z = orgZ; // [!code --]
	}
	if (showBorder && !this.cell.outOfBounds) // [!code --]
	if (!buildMode && this.cell.highlight != 0) // [!code ++]
	{
		param.matColor = 104025f; // [!code --]
		if (cx == EMono._map.bounds.x) // [!code --]
		{ // [!code --]
			renderBorder.Draw(param, 12 + (EMono.world.date.IsNight ? 4 : 0)); // [!code --]
		} // [!code --]
		else if (cx == EMono._map.bounds.maxX) // [!code --]
		{ // [!code --]
			renderBorder.Draw(param, 13 + (EMono.world.date.IsNight ? 4 : 0)); // [!code --]
		} // [!code --]
		if (cz == EMono._map.bounds.z) // [!code --]
		if (this.cell._block != 0 && !this.cell.hasDoor) // [!code ++]
		{
			renderBorder.Draw(param, 14 + (EMono.world.date.IsNight ? 4 : 0)); // [!code --]
			screen.guide.DrawWall(this.cell.GetPoint(), EMono.Colors.blockColors.MapHighlight, useMarkerPass: true); // [!code ++]
		}
		else if (cz == EMono._map.bounds.maxZ) // [!code --]
		else // [!code ++]
		{
			renderBorder.Draw(param, 15 + (EMono.world.date.IsNight ? 4 : 0)); // [!code --]
			passGuideFloor.Add(this.cell.GetPoint(), (int)this.cell.highlight); // [!code ++]
		}
	}
	if (this.cell.isSkyFloor || (detail != null && detail.anime != null && detail.anime.drawBlock)) // [!code --]
	param.color = blockLight; // [!code ++]
	if (isSnowCovered && (sourceBlock.id != 0 || this.cell.hasDoor) && (snowed || this.cell.isClearSnow) && (this.cell.Front.HasRoof || this.cell.Right.HasRoof)) // [!code ++]
	{
		orgY = param.y; // [!code --]
		orgZ = param.z; // [!code --]
		SourceBlock.Row defBlock2 = sourceFloor._defBlock; // [!code --]
		param.mat = matFloor; // [!code --]
		param.tile = defBlock2._tiles[this.cell.blockDir % defBlock2._tiles.Length]; // [!code --]
		if (defBlock2.id != 1) // [!code --]
		{ // [!code --]
			param.matColor = ((sourceFloor.colorMod == 0) ? 104025 : GetColorInt(ref matFloor.matColor, sourceFloor.colorMod)); // [!code --]
		} // [!code --]
		else // [!code --]
		{ // [!code --]
			param.matColor = 104025f; // [!code --]
		} // [!code --]
		for (int m = 0; m < ((!this.cell.isSkyFloor) ? 1 : EMono._map.config.skyBlockHeight); m++) // [!code --]
		{ // [!code --]
			param.y += ugFix.y; // [!code --]
			param.z += ugFix.z + slopeFixZ * (float)m; // [!code --]
			defBlock2.renderData.Draw(param); // [!code --]
		} // [!code --]
		param.y = orgY; // [!code --]
		param.z = orgZ; // [!code --]
		snowed = false; // [!code ++]
	}
	if (!sourceFloor.tileType.IsSkipFloor) // [!code --]
	int num13 = 0; // [!code ++]
	if (sourceBlock.id != 0) // [!code ++]
	{
		if ((hasBridge && sourceBridge.tileType.CastShadowSelf) || this.cell.castFloorShadow) // [!code --]
		{ // [!code --]
			floorLight2 = _lightMod * light * 0.2f + _baseBrightness + _shadowStrength * floorShadowStrength * (isWater ? 0.7f : (hasBridge ? 1f : (0.6f * (1f - nightRatio)))); // [!code --]
			if (snowed) // [!code --]
			{ // [!code --]
				floorLight2 = (int)((double)floorLight2 * 0.85 * 50.0) * 262144 + snowColorToken; // [!code --]
			} // [!code --]
			else // [!code --]
			{ // [!code --]
				floorLight2 = (int)(floorLight2 * 50f) * 262144 + ((this.cell.lightR >= 64) ? 63 : this.cell.lightR) * 4096 + ((this.cell.lightG >= 64) ? 63 : this.cell.lightG) * 64 + ((this.cell.lightB >= 64) ? 63 : this.cell.lightB); // [!code --]
			} // [!code --]
			param.color = floorLight2; // [!code --]
			if (this.cell.lotShade) // [!code --]
			{ // [!code --]
				floorLight = floorLight2; // [!code --]
			} // [!code --]
		} // [!code --]
		floorMatColor = ((sourceFloor.colorMod == 0) ? 104025 : GetColorInt(ref matFloor.matColor, sourceFloor.colorMod)); // [!code --]
		if (isWater && flag) // [!code --]
		{ // [!code --]
			param.y -= 0.01f * floatY; // [!code --]
		} // [!code --]
		if (!sourceBlock.tileType.IsSkipFloor || sourceBlock.transparent || hasBridge || this.cell.hasDoor || this.cell.skipRender) // [!code --]
		{ // [!code --]
			param.mat = matFloor; // [!code --]
			param.tile = sourceFloor._tiles[floorDir % sourceFloor._tiles.Length]; // [!code --]
			param.matColor = floorMatColor; // [!code --]
			param.snow = snowed; // [!code --]
			if (sourceFloor.anime.Length != 0) // [!code --]
			{ // [!code --]
				int num25 = (int)(Time.realtimeSinceStartup * 1000f / (float)sourceFloor.anime[1] % (float)sourceFloor.anime[0]); // [!code --]
				param.tile += num25; // [!code --]
			} // [!code --]
			if (this.cell.isDeck) // [!code --]
			{ // [!code --]
				param.z += 1f; // [!code --]
				if ((bool)sourceFloor.renderData.subData) // [!code --]
				{ // [!code --]
					sourceFloor.renderData.subData.Draw(param); // [!code --]
				} // [!code --]
				sourceFloor.renderData.Draw(param); // [!code --]
				param.z -= 1f; // [!code --]
			} // [!code --]
			else // [!code --]
			{ // [!code --]
				if ((bool)sourceFloor.renderData.subData) // [!code --]
				{ // [!code --]
					sourceFloor.renderData.subData.Draw(param); // [!code --]
				} // [!code --]
				sourceFloor.renderData.Draw(param); // [!code --]
			} // [!code --]
			int num26 = 0; // [!code --]
			if (isSnowCovered && sourceFloor == FLOOR.sourceSnow && !this.cell.hasDoor) // [!code --]
			{ // [!code --]
				if (!this.cell.Right.IsSnowTile && this.cell.Right.topHeight == this.cell.topHeight) // [!code --]
				{ // [!code --]
					num26++; // [!code --]
				} // [!code --]
				if (!this.cell.Front.IsSnowTile && this.cell.Front.topHeight == this.cell.topHeight) // [!code --]
				{ // [!code --]
					num26 += 2; // [!code --]
				} // [!code --]
				if (num26 != 0) // [!code --]
				{ // [!code --]
					param.tile = 448 + num26 + 12; // [!code --]
					param.z -= 0.1f; // [!code --]
					sourceFloor.renderData.Draw(param); // [!code --]
					param.z += 0.1f; // [!code --]
				} // [!code --]
			} // [!code --]
			if (this.cell.shadow != 0 && !hasBridge && !this.cell.skipRender) // [!code --]
			{ // [!code --]
				if (snowed) // [!code --]
				{ // [!code --]
					if (sourceFloor == FLOOR.sourceSnow) // [!code --]
					{ // [!code --]
						param.tile = 448 + this.cell.shadow + 8 + (this.cell.HasFence ? 4 : 0); // [!code --]
						param.z -= 0.01f; // [!code --]
						sourceFloor.renderData.Draw(param); // [!code --]
					} // [!code --]
				} // [!code --]
				else // [!code --]
				{ // [!code --]
					pass = passEdge; // [!code --]
					batch = pass.batches[pass.batchIdx]; // [!code --]
					batch.matrices[pass.idx].m03 = param.x + ambientShadowFix[this.cell.shadow].x; // [!code --]
					batch.matrices[pass.idx].m13 = param.y + ambientShadowFix[this.cell.shadow].y; // [!code --]
					batch.matrices[pass.idx].m23 = param.z + ambientShadowFix[this.cell.shadow].z; // [!code --]
					batch.tiles[pass.idx] = 448 + this.cell.shadow; // [!code --]
					batch.colors[pass.idx] = param.color; // [!code --]
					batch.matColors[pass.idx] = 104025f; // [!code --]
					pass.idx++; // [!code --]
					if (pass.idx == pass.batchSize) // [!code --]
					{ // [!code --]
						pass.NextBatch(); // [!code --]
					} // [!code --]
				} // [!code --]
				if (!sourceFloor.ignoreTransition && !snowed) // [!code --]
				{ // [!code --]
					Cell back = this.cell.Back; // [!code --]
					if (back.sourceBlock.transition[0] != -1 && back.isSeen && !back.hasDoor) // [!code --]
					{ // [!code --]
						pass = passFloor; // [!code --]
						batch = pass.batches[pass.batchIdx]; // [!code --]
						batch.matrices[pass.idx].m03 = param.x + transitionFix[0].x; // [!code --]
						batch.matrices[pass.idx].m13 = param.y + transitionFix[0].y; // [!code --]
						batch.matrices[pass.idx].m23 = param.z + transitionFix[0].z; // [!code --]
						batch.tiles[pass.idx] = 480 + back.sourceBlock.transition[0] + Rand.bytes[index % Rand.MaxBytes] % back.sourceBlock.transition[1]; // [!code --]
						batch.colors[pass.idx] = param.color; // [!code --]
						batch.matColors[pass.idx] = GetColorInt(ref back.matBlock.matColor, back.sourceBlock.colorMod); // [!code --]
						pass.idx++; // [!code --]
						if (pass.idx == pass.batchSize) // [!code --]
						{ // [!code --]
							pass.NextBatch(); // [!code --]
						} // [!code --]
					} // [!code --]
					back = this.cell.Left; // [!code --]
					if (back.sourceBlock.transition[0] != -1 && back.isSeen && !back.hasDoor) // [!code --]
					{ // [!code --]
						pass = passFloor; // [!code --]
						batch = pass.batches[pass.batchIdx]; // [!code --]
						batch.matrices[pass.idx].m03 = param.x + transitionFix[1].x; // [!code --]
						batch.matrices[pass.idx].m13 = param.y + transitionFix[1].y; // [!code --]
						batch.matrices[pass.idx].m23 = param.z + transitionFix[1].z; // [!code --]
						batch.tiles[pass.idx] = 512 + back.sourceBlock.transition[0] + Rand.bytes[index % Rand.MaxBytes] % back.sourceBlock.transition[1]; // [!code --]
						batch.colors[pass.idx] = param.color; // [!code --]
						batch.matColors[pass.idx] = GetColorInt(ref back.matBlock.matColor, back.sourceBlock.colorMod); // [!code --]
						pass.idx++; // [!code --]
						if (pass.idx == pass.batchSize) // [!code --]
						{ // [!code --]
							pass.NextBatch(); // [!code --]
						} // [!code --]
					} // [!code --]
				} // [!code --]
			} // [!code --]
			if (this.cell.autotile != 0 && sourceFloor.autotile != 0 && (!hasBridge || this.cell.bridgeHeight - this.cell.height > 3) && !this.cell.skipRender && num26 == 0) // [!code --]
			{ // [!code --]
				pass = (isWater ? passAutoTileWater : passAutoTile); // [!code --]
				batch = pass.batches[pass.batchIdx]; // [!code --]
				batch.matrices[pass.idx].m03 = param.x; // [!code --]
				batch.matrices[pass.idx].m13 = param.y; // [!code --]
				batch.matrices[pass.idx].m23 = param.z + ((hasBridge || this.cell._block != 0) ? 0.8f : 0f); // [!code --]
				batch.tiles[pass.idx] = (26 + sourceFloor.autotile / 2) * 32 + sourceFloor.autotile % 2 * 16 + this.cell.autotile; // [!code --]
				batch.colors[pass.idx] = param.color + (float)((int)(sourceFloor.autotileBrightness * 100f) * 262144); // [!code --]
				batch.matColors[pass.idx] = param.matColor; // [!code --]
				pass.idx++; // [!code --]
				if (pass.idx == pass.batchSize) // [!code --]
				{ // [!code --]
					pass.NextBatch(); // [!code --]
				} // [!code --]
			} // [!code --]
		} // [!code --]
		if (isWater) // [!code --]
		this.tileType = sourceBlock.tileType; // [!code ++]
		roomHeight = 0f; // [!code ++]
		int blockDir = this.cell.blockDir; // [!code ++]
		bool num14 = this.tileType.blockRenderMode == BlockRenderMode.Floor; // [!code ++]
		bool flag7 = false; // [!code ++]
		if (!num14) // [!code ++]
		{
			int num27 = 12; // [!code --]
			int num28 = this.cell.shore / num27; // [!code --]
			int num29 = this.cell.shore % num27; // [!code --]
			bool isShoreSand = this.cell.isShoreSand; // [!code --]
			if (this.cell.shore != 0) // [!code --]
			{ // [!code --]
				Cell cell = ((((uint)num28 & (true ? 1u : 0u)) != 0) ? this.cell.Back : ((((uint)num28 & 2u) != 0) ? this.cell.Right : ((((uint)num28 & 4u) != 0) ? this.cell.Front : this.cell.Left))); // [!code --]
				if (isShoreSand && !cell.sourceFloor.isBeach) // [!code --]
				{ // [!code --]
					cell = ((((uint)num28 & 8u) != 0) ? this.cell.Left : ((((uint)num28 & 4u) != 0) ? this.cell.Front : ((((uint)num28 & 2u) != 0) ? this.cell.Right : this.cell.Back))); // [!code --]
				} // [!code --]
				if (!cell.IsSnowTile) // [!code --]
				{ // [!code --]
					param.matColor = GetColorInt(ref cell.matFloor.matColor, cell.sourceFloor.colorMod); // [!code --]
					if (isShoreSand) // [!code --]
					{ // [!code --]
						pass = passShore; // [!code --]
						batch = pass.batches[pass.batchIdx]; // [!code --]
						batch.matrices[pass.idx].m03 = param.x; // [!code --]
						batch.matrices[pass.idx].m13 = param.y; // [!code --]
						batch.matrices[pass.idx].m23 = param.z; // [!code --]
						batch.tiles[pass.idx] = 768 + this.cell.shore / num27; // [!code --]
						batch.colors[pass.idx] = param.color; // [!code --]
						batch.matColors[pass.idx] = param.matColor; // [!code --]
						pass.idx++; // [!code --]
						if (pass.idx == pass.batchSize) // [!code --]
						{ // [!code --]
							pass.NextBatch(); // [!code --]
						} // [!code --]
						num29 = 2; // [!code --]
					} // [!code --]
					else // [!code --]
					{ // [!code --]
						num29 = cell.sourceFloor.edge; // [!code --]
					} // [!code --]
					param.tile = (24 + num29 / 2) * 32 + num29 % 2 * 16 + num28; // [!code --]
					rendererShore.Draw(param); // [!code --]
				} // [!code --]
			} // [!code --]
			if (this.cell.Back.isShoreSand && ((uint)(this.cell.Back.shore / num27) & 8u) != 0 && this.cell.Left.isShoreSand && ((uint)(this.cell.Left.shore / num27) & (true ? 1u : 0u)) != 0) // [!code --]
			{ // [!code --]
				param.tile = 785f; // [!code --]
				param.matColor = GetColorInt(ref this.cell.BackLeft.matFloor.matColor, this.cell.BackLeft.sourceFloor.colorMod); // [!code --]
				passShore.Add(param); // [!code --]
				Draw(60); // [!code --]
			} // [!code --]
			if (this.cell.Back.isShoreSand && ((uint)(this.cell.Back.shore / num27) & 2u) != 0 && this.cell.Right.isShoreSand && ((uint)(this.cell.Right.shore / num27) & (true ? 1u : 0u)) != 0) // [!code --]
			{ // [!code --]
				param.tile = 786f; // [!code --]
				param.matColor = GetColorInt(ref this.cell.BackRight.matFloor.matColor, this.cell.BackRight.sourceFloor.colorMod); // [!code --]
				passShore.Add(param); // [!code --]
				Draw(56); // [!code --]
			} // [!code --]
			if (this.cell.Front.isShoreSand && ((uint)(this.cell.Front.shore / num27) & 2u) != 0 && this.cell.Right.isShoreSand && ((uint)(this.cell.Right.shore / num27) & 4u) != 0) // [!code --]
			{ // [!code --]
				param.tile = 787f; // [!code --]
				param.matColor = GetColorInt(ref this.cell.FrontRight.matFloor.matColor, this.cell.FrontRight.sourceFloor.colorMod); // [!code --]
				passShore.Add(param); // [!code --]
				Draw(48); // [!code --]
			} // [!code --]
			if (this.cell.Front.isShoreSand && ((uint)(this.cell.Front.shore / num27) & 8u) != 0 && this.cell.Left.isShoreSand && ((uint)(this.cell.Left.shore / num27) & 4u) != 0) // [!code --]
			{ // [!code --]
				param.tile = 788f; // [!code --]
				param.matColor = GetColorInt(ref this.cell.FrontLeft.matFloor.matColor, this.cell.FrontLeft.sourceFloor.colorMod); // [!code --]
				passShore.Add(param); // [!code --]
				Draw(52); // [!code --]
			} // [!code --]
			if (this.cell._bridge != 0 && this.cell.isBridgeEdge && this.cell.bridgePillar != byte.MaxValue) // [!code --]
			{ // [!code --]
				pass = passEdge; // [!code --]
				batch = pass.batches[pass.batchIdx]; // [!code --]
				batch.matrices[pass.idx].m03 = param.x + waterEdgeBridgeFix.x; // [!code --]
				batch.matrices[pass.idx].m13 = param.y + waterEdgeBridgeFix.y; // [!code --]
				batch.matrices[pass.idx].m23 = param.z + waterEdgeBridgeFix.z; // [!code --]
				batch.tiles[pass.idx] = 616 + waterAnimeIndex % 4; // [!code --]
				batch.colors[pass.idx] = param.color; // [!code --]
				batch.matColors[pass.idx] = 104025f; // [!code --]
				pass.idx++; // [!code --]
				if (pass.idx == pass.batchSize) // [!code --]
			switch (wallClipMode) // [!code ++]
			{ // [!code ++]
			case WallClipMode.ByRoom: // [!code ++]
				if (!this.tileType.RepeatBlock) // [!code ++]
				{
					pass.NextBatch(); // [!code --]
					break; // [!code ++]
				}
			} // [!code --]
			bool flag8 = false; // [!code --]
			if (isShoreSand) // [!code --]
			{ // [!code --]
				if (((uint)num28 & (true ? 1u : 0u)) != 0) // [!code --]
				if (currentRoom == null || showFullWall) // [!code ++]
				{
					if (((uint)num28 & 8u) != 0) // [!code --]
					{ // [!code --]
						if ((num28 & 2) == 0 && (num28 & 4) == 0) // [!code --]
						{ // [!code --]
							Draw(16); // [!code --]
						} // [!code --]
						flag8 = true; // [!code --]
					} // [!code --]
					if (((uint)num28 & 2u) != 0) // [!code --]
					{ // [!code --]
						if ((num28 & 8) == 0 && (num28 & 4) == 0) // [!code --]
						{ // [!code --]
							Draw(20); // [!code --]
						} // [!code --]
						flag8 = true; // [!code --]
					} // [!code --]
					this.room = this.room ?? this.cell.Front.room ?? this.cell.Right.room ?? this.cell.FrontRight.room; // [!code ++]
					_lowblock = lowBlock; // [!code ++]
				} // [!code ++]
				else if (this.room != this.cell.Front.room && (this.cell.Front.room == currentRoom || (this.room?.lot != currentLot && this.cell.Front.room?.lot == currentLot))) // [!code ++]
				{ // [!code ++]
					this.room = this.cell.Front.room; // [!code ++]
					_lowblock = !this.cell.Front.lotWall && !this.cell.Front.fullWall; // [!code ++]
				} // [!code ++]
				else if (this.room != this.cell.Right.room && (this.cell.Right.room == currentRoom || (this.room?.lot != currentLot && this.cell.Right.room?.lot == currentLot))) // [!code ++]
				{ // [!code ++]
					this.room = this.cell.Right.room; // [!code ++]
					_lowblock = !this.cell.Right.lotWall && !this.cell.Right.fullWall; // [!code ++]
				} // [!code ++]
				else if (this.tileType.IsFullBlock && this.room != this.cell.FrontRight.room && (this.cell.FrontRight.room == currentRoom || (this.room?.lot != currentLot && this.cell.FrontRight.room?.lot == currentLot))) // [!code ++]
				{ // [!code ++]
					this.room = this.cell.FrontRight.room; // [!code ++]
					_lowblock = !this.cell.FrontRight.lotWall && !this.cell.FrontRight.fullWall; // [!code ++]
				}
				if (((uint)num28 & 4u) != 0) // [!code --]
				else // [!code ++]
				{
					if (((uint)num28 & 8u) != 0) // [!code --]
					this.room = this.room ?? this.cell.Front.room ?? this.cell.Right.room ?? this.cell.FrontRight.room; // [!code ++]
					_lowblock = true; // [!code ++]
					if (!this.tileType.IsFullBlock) // [!code ++]
					{
						if ((num28 & 2) == 0 && (num28 & 1) == 0) // [!code --]
						if (this.cell.lotWall) // [!code ++]
						{
							Draw(24); // [!code --]
							_lowblock = false; // [!code ++]
						}
						flag8 = true; // [!code --]
					} // [!code --]
					if (((uint)num28 & 2u) != 0) // [!code --]
					{ // [!code --]
						if ((num28 & 8) == 0 && (num28 & 1) == 0) // [!code --]
						else if (this.room == currentRoom) // [!code ++]
						{
							Draw(28); // [!code --]
							_lowblock = !this.cell.fullWall; // [!code ++]
						}
						flag8 = true; // [!code --]
					}
				}
				if (!flag8) // [!code --]
				flag7 = (this.room != null && this.room.data.atrium) || (this.cell.room != null && this.cell.room.data.atrium); // [!code ++]
				if (flag7) // [!code ++]
				{
					if (!this.cell.Front.sourceFloor.tileType.IsWater && !this.cell.Front.isDeck) // [!code --]
					{ // [!code --]
						Draw(8); // [!code --]
					} // [!code --]
					if (!this.cell.Right.sourceFloor.tileType.IsWater && !this.cell.Right.isDeck) // [!code --]
					{ // [!code --]
						Draw(12); // [!code --]
					} // [!code --]
					_lowblock = false; // [!code ++]
				}
			} // [!code --]
			if (!flag8) // [!code --]
			{ // [!code --]
				if (!this.cell.Back.sourceFloor.tileType.IsWater && !this.cell.Back.isDeck) // [!code --]
				if (this.room == null && alwaysLowblock) // [!code ++]
				{
					pass = passEdge; // [!code --]
					batch = pass.batches[pass.batchIdx]; // [!code --]
					batch.tiles[pass.idx] = 608 + waterAnimeIndex % 4; // [!code --]
					batch.matColors[pass.idx] = 104025f; // [!code --]
					if (((uint)(this.cell.shore / num27) & (true ? 1u : 0u)) != 0) // [!code --]
					_lowblock = true; // [!code ++]
					roomHeight = 0f; // [!code ++]
				} // [!code ++]
				if (this.room != null) // [!code ++]
				{ // [!code ++]
					maxHeight = (float)(cz - cx) * screen.tileAlign.y + (float)this.room.lot.mh * _heightMod.y; // [!code ++]
					if (showRoof) // [!code ++]
					{
						if (isShoreSand) // [!code --]
						{ // [!code --]
							param.matColor = GetColorInt(ref this.cell.Back.matFloor.matColor, this.cell.Back.sourceFloor.colorMod); // [!code --]
							batch.matrices[pass.idx].m03 = param.x + waterEdgeFixShoreSand.x; // [!code --]
							batch.matrices[pass.idx].m13 = param.y + waterEdgeFixShoreSand.y; // [!code --]
							batch.matrices[pass.idx].m23 = param.z + waterEdgeFixShoreSand.z; // [!code --]
							batch.tiles[pass.idx] = 640 + seaAnimeIndexes[waterAnimeIndex % seaAnimeIndexes.Length]; // [!code --]
							batch.matColors[pass.idx] = param.matColor; // [!code --]
						} // [!code --]
						else // [!code --]
						{ // [!code --]
							batch.matrices[pass.idx].m03 = param.x; // [!code --]
							batch.matrices[pass.idx].m13 = param.y; // [!code --]
							batch.matrices[pass.idx].m23 = param.z + waterEdgeFixShore.z; // [!code --]
						} // [!code --]
						roomHeight = this.room.lot.realHeight; // [!code ++]
						break; // [!code ++]
					}
					else // [!code --]
					if ((noRoofMode && currentRoom == null) || (_lowblock && !this.tileType.ForceRpeatBlock)) // [!code ++]
					{
						batch.matrices[pass.idx].m03 = param.x; // [!code --]
						batch.matrices[pass.idx].m13 = param.y; // [!code --]
						batch.matrices[pass.idx].m23 = param.z + waterEdgeFix.z; // [!code --]
						batch.tiles[pass.idx] += 12f; // [!code --]
						roomHeight = 0f; // [!code ++]
						break; // [!code ++]
					}
					batch.colors[pass.idx] = param.color; // [!code --]
					pass.idx++; // [!code --]
					if (pass.idx == pass.batchSize) // [!code --]
					int num15 = ((this.room.data.maxHeight == 0) ? 2 : this.room.data.maxHeight); // [!code ++]
					roomHeight = EMono.setting.render.roomHeightMod * (float)((this.room.lot.height < num15) ? this.room.lot.height : num15) + 0.01f * (float)this.room.lot.heightFix; // [!code ++]
				} // [!code ++]
				break; // [!code ++]
			case WallClipMode.ByLot: // [!code ++]
				if (defaultBlockHeight > 0f || isIndoor) // [!code ++]
				{ // [!code ++]
					_lowblock = cx != 0 && cz != Size - 1 && ((!this.cell.Back.HasBlock && !this.cell.Back.isWallEdge) || (!this.cell.Left.HasBlock && !this.cell.Left.isWallEdge) || !this.cell.Back.Left.HasBlock); // [!code ++]
					if (!_lowblock) // [!code ++]
					{
						pass.NextBatch(); // [!code --]
						roomHeight = defaultBlockHeight * EMono.setting.render.roomHeightMod; // [!code ++]
						maxHeight = (float)(cz - cx) * screen.tileAlign.y + (float)(int)this.cell.TopHeight * _heightMod.y; // [!code ++]
					}
					break; // [!code ++]
				}
				if (!this.cell.Left.sourceFloor.tileType.IsWater && !this.cell.Left.isDeck) // [!code --]
				if (showFullWall) // [!code ++]
				{
					pass = passEdge; // [!code --]
					batch = pass.batches[pass.batchIdx]; // [!code --]
					batch.tiles[pass.idx] = 612 + waterAnimeIndex % 4; // [!code --]
					batch.matColors[pass.idx] = 104025f; // [!code --]
					if (((uint)(this.cell.shore / num27) & 8u) != 0) // [!code --]
					_lowblock = this.room != null; // [!code ++]
					if (_lowblock) // [!code ++]
					{
						if (isShoreSand) // [!code --]
						{ // [!code --]
							param.matColor = GetColorInt(ref this.cell.Left.matFloor.matColor, this.cell.Left.sourceFloor.colorMod); // [!code --]
							batch.matrices[pass.idx].m03 = param.x + waterEdgeFixShoreSand.x; // [!code --]
							batch.matrices[pass.idx].m13 = param.y + waterEdgeFixShoreSand.y; // [!code --]
							batch.matrices[pass.idx].m23 = param.z + waterEdgeFixShoreSand.z; // [!code --]
							batch.tiles[pass.idx] = 644 + seaAnimeIndexes[waterAnimeIndex % seaAnimeIndexes.Length]; // [!code --]
							batch.matColors[pass.idx] = param.matColor; // [!code --]
						} // [!code --]
						else // [!code --]
						if (this.cell.Back.IsRoomEdge && this.cell.Right.IsRoomEdge && this.cell.Back.room == null && this.cell.Right.room == null && this.cell.Right.Front.room?.lot == this.room?.lot) // [!code ++]
						{
							batch.matrices[pass.idx].m03 = param.x; // [!code --]
							batch.matrices[pass.idx].m13 = param.y; // [!code --]
							batch.matrices[pass.idx].m23 = param.z + waterEdgeFixShore.z; // [!code --]
							_lowblock = false; // [!code ++]
						}
					}
					else // [!code --]
					else if (this.cell.Back.room != null && this.cell.Back.room.lot == (this.cell.Front.room ?? this.cell.Right.room)?.lot) // [!code ++]
					{
						batch.matrices[pass.idx].m03 = param.x; // [!code --]
						batch.matrices[pass.idx].m13 = param.y; // [!code --]
						batch.matrices[pass.idx].m23 = param.z + waterEdgeFix.z; // [!code --]
						batch.tiles[pass.idx] += 12f; // [!code --]
						_lowblock = true; // [!code ++]
					}
					batch.colors[pass.idx] = param.color; // [!code --]
					pass.idx++; // [!code --]
					if (pass.idx == pass.batchSize) // [!code --]
				} // [!code ++]
				else // [!code ++]
				{ // [!code ++]
					_lowblock = lowBlock; // [!code ++]
				} // [!code ++]
				if (this.tileType.RepeatBlock) // [!code ++]
				{ // [!code ++]
					this.room = this.room ?? this.cell.Front.room ?? this.cell.Right.room ?? this.cell.FrontRight.room; // [!code ++]
					if (this.room != null && (!noRoofMode || currentRoom != null) && (!showFullWall || currentRoom == null || this.room.lot == currentRoom.lot)) // [!code ++]
					{
						pass.NextBatch(); // [!code --]
						roomHeight = ((_lowblock && !this.tileType.ForceRpeatBlock) ? 0f : this.room.lot.realHeight); // [!code ++]
						maxHeight = (float)(cz - cx) * screen.tileAlign.y + (float)this.room.lot.mh * _heightMod.y; // [!code ++]
					}
				}
			} // [!code --]
			if (flag) // [!code --]
			{ // [!code --]
				param.y += 0.01f * floatY; // [!code --]
				break; // [!code ++]
			}
		}
		if (flag) // [!code --]
		if (!_lowblock && (double)roomHeight > 1.2 && this.tileType.RepeatBlock) // [!code ++]
		{
			param.z -= 1f; // [!code --]
			num13 = 1; // [!code ++]
		}
	} // [!code --]
	if (this.cell.skipRender) // [!code --]
	{ // [!code --]
		if (this.cell.pcSync) // [!code --]
		else if (lowBlock) // [!code ++]
		{
			param.tile = 0f; // [!code --]
			rendererFov.Draw(param); // [!code --]
			num13 = 2; // [!code ++]
		}
		return; // [!code --]
	} // [!code --]
	if (hasBridge) // [!code --]
	{ // [!code --]
		param.y = (float)(cz - cx) * screen.tileAlign.y + (float)(int)this.cell.bridgeHeight * _heightMod.y; // [!code --]
		param.z = 1000f + param.x * screen.tileWeight.x + param.y * screen.tileWeight.z + (float)(int)this.cell.bridgeHeight * _heightMod.z; // [!code --]
		if (flag) // [!code --]
		param.mat = matBlock; // [!code ++]
		param.dir = this.cell.blockDir; // [!code ++]
		param.snow = snowed; // [!code ++]
		switch (this.tileType.blockRenderMode) // [!code ++]
		{
			param.y += 0.01f * floatY; // [!code --]
		} // [!code --]
		param.color = floorLight; // [!code --]
		param.mat = matBridge; // [!code --]
		floorMatColor = ((sourceBridge.colorMod == 0) ? 104025 : GetColorInt(ref matBridge.matColor, sourceBridge.colorMod)); // [!code --]
		param.dir = this.cell.floorDir; // [!code --]
		param.tile = sourceBridge._tiles[this.cell.floorDir % sourceBridge._tiles.Length]; // [!code --]
		param.matColor = floorMatColor; // [!code --]
		sourceBridge.renderData.Draw(param); // [!code --]
		if (this.cell.autotileBridge != 0 && sourceBridge.autotile != 0) // [!code --]
		case BlockRenderMode.FullBlock: // [!code ++]
		{
			pass = passAutoTile; // [!code --]
			batch = pass.batches[pass.batchIdx]; // [!code --]
			batch.matrices[pass.idx].m03 = param.x; // [!code --]
			batch.matrices[pass.idx].m13 = param.y; // [!code --]
			batch.matrices[pass.idx].m23 = param.z + ((this.cell._block != 0) ? 0.8f : 0f); // [!code --]
			batch.tiles[pass.idx] = (26 + sourceBridge.autotile / 2) * 32 + sourceBridge.autotile % 2 * 16 + this.cell.autotileBridge; // [!code --]
			batch.colors[pass.idx] = param.color + (float)((int)(sourceBridge.autotileBrightness * 100f) * 262144); // [!code --]
			batch.matColors[pass.idx] = param.matColor; // [!code --]
			pass.idx++; // [!code --]
			if (pass.idx == pass.batchSize) // [!code --]
			bool invisible = sourceBlock.tileType.Invisible; // [!code ++]
			if (invisible && (!buildMode || ActionMode.Cinema.IsActive)) // [!code ++]
			{ // [!code ++]
				break; // [!code ++]
			} // [!code ++]
			if (this.cell.isSurrounded) // [!code ++]
			{ // [!code ++]
				switch (innerMode) // [!code ++]
				{ // [!code ++]
				case InnerMode.InnerBlock: // [!code ++]
				case InnerMode.BuildMode: // [!code ++]
					blockLight = _baseBrightness + fogBrightness; // [!code ++]
					param.color = (int)(50f * blockLight) * 262144; // [!code ++]
					param.matColor = 104025f; // [!code ++]
					param.tile = (buildMode ? 1 : 2) + ((_lowblock || defaultBlockHeight > 0f) ? 3000000 : 0); // [!code ++]
					rendererInnerBlock.Draw(param); // [!code ++]
					return; // [!code ++]
				case InnerMode.None: // [!code ++]
				case InnerMode.Height: // [!code ++]
					param.color = blockLight; // [!code ++]
					break; // [!code ++]
				} // [!code ++]
			} // [!code ++]
			if (snowed) // [!code ++]
			{ // [!code ++]
				param.color = floorLight; // [!code ++]
			} // [!code ++]
			param.color -= (int)(_shadowStrength * 0.8f * 50f) * 262144; // [!code ++]
			if (currentRoom != null && !showFullWall) // [!code ++]
			{ // [!code ++]
				_lowblock = true; // [!code ++]
				roomHeight = 0f; // [!code ++]
				if (this.cell.room != currentRoom && (this.cell.Front.room == currentRoom || this.cell.Right.room == currentRoom || this.cell.FrontRight.room == currentRoom) && (this.cell.Back.room != currentRoom || this.cell.Right.room != currentRoom) && (this.cell.Front.room != currentRoom || this.cell.Left.room != currentRoom)) // [!code ++]
				{ // [!code ++]
					_lowblock = false; // [!code ++]
				} // [!code ++]
				if (!_lowblock) // [!code ++]
				{ // [!code ++]
					int num17 = ((currentRoom.data.maxHeight == 0) ? 2 : currentRoom.data.maxHeight); // [!code ++]
					roomHeight = EMono.setting.render.roomHeightMod * (float)((currentRoom.lot.height < num17) ? currentRoom.lot.height : num17) + 0.01f * (float)currentRoom.lot.heightFix; // [!code ++]
				} // [!code ++]
			} // [!code ++]
			if (flag7) // [!code ++]
			{ // [!code ++]
				_lowblock = (!this.cell.Front.HasFullBlock || !this.cell.Right.HasFullBlock) && (!this.cell.Front.HasFullBlock || !this.cell.Left.HasFullBlock) && (!this.cell.Back.HasFullBlock || !this.cell.Right.HasFullBlock) && (!this.cell.Back.HasFullBlock || !this.cell.Left.HasFullBlock); // [!code ++]
				if (_lowblock) // [!code ++]
				{ // [!code ++]
					roomHeight = 0f; // [!code ++]
				} // [!code ++]
			} // [!code ++]
			if (invisible) // [!code ++]
			{
				pass.NextBatch(); // [!code --]
				roomHeight = 0f; // [!code ++]
				_lowblock = false; // [!code ++]
			}
		} // [!code --]
		if (this.cell.shadow != 0) // [!code --]
		{ // [!code --]
			if (sourceBridge == FLOOR.sourceSnow) // [!code --]
			if (this.cell.Things.Count > 0) // [!code ++]
			{
				param.tile = 448 + this.cell.shadow + 8 + (this.cell.HasFence ? 4 : 0); // [!code --]
				param.z -= 0.01f; // [!code --]
				sourceBridge.renderData.Draw(param); // [!code --]
				_lowblock = false; // [!code ++]
			}
			else // [!code --]
			param.tile = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length] + (_lowblock ? 3000000 : 0); // [!code ++]
			param.matColor = ((sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, sourceBlock.colorMod)); // [!code ++]
			if (roomHeight == 0f) // [!code ++]
			{
				pass = passEdge; // [!code --]
				batch = pass.batches[pass.batchIdx]; // [!code --]
				batch.matrices[pass.idx].m03 = param.x + ambientShadowFix[this.cell.shadow].x; // [!code --]
				batch.matrices[pass.idx].m13 = param.y + ambientShadowFix[this.cell.shadow].y; // [!code --]
				batch.matrices[pass.idx].m23 = param.z + ambientShadowFix[this.cell.shadow].z; // [!code --]
				batch.tiles[pass.idx] = 448 + this.cell.shadow; // [!code --]
				batch.colors[pass.idx] = blockLight; // [!code --]
				batch.matColors[pass.idx] = 104025f; // [!code --]
				pass.idx++; // [!code --]
				if (pass.idx == pass.batchSize) // [!code --]
				if (!this.cell.hasDoor) // [!code ++]
				{
					pass.NextBatch(); // [!code --]
					sourceBlock.renderData.Draw(param); // [!code ++]
				}
			}
		} // [!code --]
		if (this.cell.isBridgeEdge && this.cell.bridgeHeight - this.cell.height >= 3 && this.cell.bridgePillar != byte.MaxValue && !noSlopMode) // [!code --]
		{ // [!code --]
			orgY = param.y; // [!code --]
			orgZ = param.z; // [!code --]
			param.y += bridgeFix.y; // [!code --]
			param.z += bridgeFix.z; // [!code --]
			param.dir = 0; // [!code --]
			SourceBlock.Row row4 = sourceBridge._bridgeBlock; // [!code --]
			float num30 = (float)(this.cell.bridgeHeight - this.cell.height) * _heightMod.y; // [!code --]
			if (this.cell.sourceFloor.tileType == TileType.Sky) // [!code --]
			else // [!code ++]
			{
				num30 += (float)EMono._map.config.skyBlockHeight; // [!code --]
				sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFixBlock, this.cell.hasDoor, this.cell.effect?.FireAmount ?? 0, isBlock: true); // [!code ++]
			}
			int num31 = (int)(num30 / heightBlockSize) + 2; // [!code --]
			if (this.cell.bridgePillar != 0) // [!code --]
			Room room = this.cell.Front.room ?? this.cell.room; // [!code ++]
			if (room == null && this.cell.Right.room != null) // [!code ++]
			{
				row4 = EMono.sources.blocks.rows[this.cell.bridgePillar]; // [!code --]
				param.tile = row4._tiles[0] + ((num31 == 2) ? 32 : 0); // [!code --]
				param.mat = ((sourceBridge.DefaultMaterial == row4.DefaultMaterial) ? sourceBridge.DefaultMaterial : row4.DefaultMaterial); // [!code --]
				param.matColor = ((row4.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.matColor, row4.colorMod)); // [!code --]
				room = this.cell.Right.room; // [!code ++]
			}
			else // [!code --]
			if (!invisible && room != null) // [!code ++]
			{
				param.mat = matBlock; // [!code --]
				param.tile = row4._tiles[0] + 32; // [!code --]
				param.matColor = ((row4.colorMod == 0) ? 104025 : GetColorInt(ref matBridge.matColor, row4.colorMod)); // [!code --]
				if (room.lot.idDeco != 0 && !this.cell.hasDoor) // [!code ++]
				{ // [!code ++]
					param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room.lot.idDeco); // [!code ++]
					param.matColor = room.lot.colDeco; // [!code ++]
					float y = param.y; // [!code ++]
					param.y += (float)room.lot.decoFix * 0.01f; // [!code ++]
					rendererWallDeco.Draw(param); // [!code ++]
					param.y = y; // [!code ++]
				} // [!code ++]
				if (room.lot.idDeco2 != 0 && roomHeight != 0f && (float)room.lot.decoFix2 * 0.01f + heightLimitDeco < roomHeight + maxHeight - param.y) // [!code ++]
				{ // [!code ++]
					param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room.lot.idDeco2); // [!code ++]
					param.matColor = room.lot.colDeco2; // [!code ++]
					float y2 = param.y; // [!code ++]
					float num18 = param.z; // [!code ++]
					param.y += (float)room.lot.decoFix2 * 0.01f; // [!code ++]
					param.z += (float)room.lot.decoFix2 * 0.01f * heightModDeco; // [!code ++]
					rendererWallDeco.Draw(param); // [!code ++]
					param.y = y2; // [!code ++]
					param.z = num18; // [!code ++]
				} // [!code ++]
			}
			param.y += ugFixBridgeTop.y; // [!code --]
			param.z += ugFixBridgeTop.z; // [!code --]
			for (int n = 0; n < num31; n++) // [!code --]
			room = this.cell.Right.room ?? this.cell.room; // [!code ++]
			if (room == null && this.cell.Front.room != null) // [!code ++]
			{ // [!code ++]
				room = this.cell.Front.room; // [!code ++]
			} // [!code ++]
			if (!invisible && room != null) // [!code ++]
			{
				if (n == num31 - 1) // [!code --]
				if (room.lot.idDeco != 0 && !this.cell.hasDoor) // [!code ++]
				{
					param.y = (float)(cz - cx) * screen.tileAlign.y + (float)height * _heightMod.y + ugFixBridgeBottom.y; // [!code --]
					param.z = 1000f + param.x * screen.tileWeight.x + param.y * screen.tileWeight.z + (float)height * _heightMod.z + ugFixBridgeBottom.z; // [!code --]
					param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room.lot.idDeco) * -1; // [!code ++]
					param.matColor = room.lot.colDeco; // [!code ++]
					float y3 = param.y; // [!code ++]
					param.y += (float)room.lot.decoFix * 0.01f; // [!code ++]
					rendererWallDeco.Draw(param); // [!code ++]
					param.y = y3; // [!code ++]
				}
				else // [!code --]
				if (room.lot.idDeco2 != 0 && roomHeight != 0f && (float)room.lot.decoFix2 * 0.01f + heightLimitDeco < roomHeight + maxHeight - param.y) // [!code ++]
				{
					param.y += ugFixBridge.y; // [!code --]
					param.z += ugFixBridge.z; // [!code --]
					param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room.lot.idDeco2) * -1; // [!code ++]
					param.matColor = room.lot.colDeco2; // [!code ++]
					float y4 = param.y; // [!code ++]
					float num19 = param.z; // [!code ++]
					param.y += (float)room.lot.decoFix2 * 0.01f; // [!code ++]
					param.z += (float)room.lot.decoFix2 * 0.01f * heightModDeco; // [!code ++]
					rendererWallDeco.Draw(param); // [!code ++]
					param.y = y4; // [!code ++]
					param.z = num19; // [!code ++]
				}
				row4.renderData.Draw(param); // [!code --]
			}
			param.y = orgY; // [!code --]
			param.z = orgZ; // [!code --]
			break; // [!code ++]
		}
	} // [!code --]
	if (this.cell._deco != 0) // [!code --]
	{ // [!code --]
		param.mat = matFloor; // [!code --]
		param.matColor = ((sourceDeco.colorMod == 0) ? 104025 : GetColorInt(ref matDeco.matColor, sourceDeco.colorMod)); // [!code --]
		param.snow = snowed; // [!code --]
		if (sourceDeco.autotile != 0) // [!code --]
		case BlockRenderMode.WallOrFence: // [!code ++]
		{
			if (this.cell.autotileDeco != 0) // [!code --]
			if (map.config.fullWallHeight) // [!code ++]
			{
				param.tile = (26 + sourceDeco.autotile / 2) * 32 + sourceDeco.autotile % 2 * 16 + this.cell.autotileDeco; // [!code --]
				param.z += ((hasBridge || this.cell._block != 0) ? 0.8f : 0f); // [!code --]
				sourceDeco.renderData.Draw(param); // [!code --]
				param.z = orgZ; // [!code --]
				showFullWall = true; // [!code ++]
				_lowblock = false; // [!code ++]
			}
		} // [!code --]
		else // [!code --]
		{ // [!code --]
			param.tile = sourceDeco._tiles[floorDir % sourceDeco._tiles.Length]; // [!code --]
			sourceDeco.renderData.Draw(param); // [!code --]
		} // [!code --]
	} // [!code --]
	if (!buildMode && this.cell.highlight != 0) // [!code --]
	{ // [!code --]
		if (this.cell._block != 0 && !this.cell.hasDoor) // [!code --]
		{ // [!code --]
			screen.guide.DrawWall(this.cell.GetPoint(), EMono.Colors.blockColors.MapHighlight, useMarkerPass: true); // [!code --]
		} // [!code --]
		else // [!code --]
		{ // [!code --]
			passGuideFloor.Add(this.cell.GetPoint(), (int)this.cell.highlight); // [!code --]
		} // [!code --]
	} // [!code --]
	param.color = blockLight; // [!code --]
	if (isSnowCovered && (sourceBlock.id != 0 || this.cell.hasDoor) && (snowed || this.cell.isClearSnow) && (this.cell.Front.HasRoof || this.cell.Right.HasRoof)) // [!code --]
	{ // [!code --]
		snowed = false; // [!code --]
	} // [!code --]
	int num32 = 0; // [!code --]
	if (sourceBlock.id != 0) // [!code --]
	{ // [!code --]
		this.tileType = sourceBlock.tileType; // [!code --]
		roomHeight = 0f; // [!code --]
		int blockDir = this.cell.blockDir; // [!code --]
		bool num33 = this.tileType.blockRenderMode == BlockRenderMode.Floor; // [!code --]
		bool flag9 = false; // [!code --]
		if (!num33) // [!code --]
		{ // [!code --]
			switch (wallClipMode) // [!code --]
			orgY = param.y; // [!code ++]
			orgZ = param.z; // [!code ++]
			param.color = (this.tileType.IsFence ? (floorLight - (float)((int)(_shadowStrength * 0.8f * 50f) * 262144)) : blockLight); // [!code ++]
			bool flag8 = blockDir == 1 || _lowblock || flag7; // [!code ++]
			bool flag9 = blockDir == 0 || _lowblock || flag7; // [!code ++]
			if (!showFullWall && currentRoom != null) // [!code ++]
			{
			case WallClipMode.ByRoom: // [!code --]
				if (!this.tileType.RepeatBlock) // [!code --]
				if (!flag8) // [!code ++]
				{
					break; // [!code --]
					if (currentRoom == this.cell.room || (this.cell.lotWall && this.cell.room?.lot == currentLot && this.cell.Front.room != currentRoom)) // [!code ++]
					{ // [!code ++]
						if (!this.cell.IsRoomEdge || (this.cell.Front.room != this.cell.room && this.cell.FrontRight.room != this.cell.room)) // [!code ++]
						{ // [!code ++]
							flag8 = true; // [!code ++]
						} // [!code ++]
					} // [!code ++]
					else if ((!this.cell.Front.lotWall || this.cell.Front.room?.lot != currentLot) && this.cell.Front.room != currentRoom) // [!code ++]
					{ // [!code ++]
						flag8 = true; // [!code ++]
					} // [!code ++]
				}
				if (currentRoom == null || showFullWall) // [!code --]
				if (!flag9) // [!code ++]
				{
					this.room = this.room ?? this.cell.Front.room ?? this.cell.Right.room ?? this.cell.FrontRight.room; // [!code --]
					_lowblock = lowBlock; // [!code --]
					if (currentRoom == this.cell.room || (this.cell.lotWall && this.cell.room?.lot == currentLot && this.cell.Right.room != currentRoom)) // [!code ++]
					{ // [!code ++]
						if (!this.cell.IsRoomEdge || (this.cell.Right.room != this.cell.room && this.cell.FrontRight.room != this.cell.room)) // [!code ++]
						{ // [!code ++]
							flag9 = true; // [!code ++]
						} // [!code ++]
					} // [!code ++]
					else if ((!this.cell.Right.lotWall || this.cell.Right.room?.lot != currentLot) && this.cell.Right.room != currentRoom) // [!code ++]
					{ // [!code ++]
						flag9 = true; // [!code ++]
					} // [!code ++]
				}
				else if (this.room != this.cell.Front.room && (this.cell.Front.room == currentRoom || (this.room?.lot != currentLot && this.cell.Front.room?.lot == currentLot))) // [!code --]
			} // [!code ++]
			if (blockDir == 0 || blockDir == 2) // [!code ++]
			{ // [!code ++]
				param.dir = 0; // [!code ++]
				Room room2 = this.cell.Front.room ?? this.cell.room; // [!code ++]
				if (room2 != null && this.tileType.IsWall) // [!code ++]
				{
					this.room = this.cell.Front.room; // [!code --]
					_lowblock = !this.cell.Front.lotWall && !this.cell.Front.fullWall; // [!code --]
					if (room2.lot.idDeco != 0 && !this.cell.hasDoor) // [!code ++]
					{ // [!code ++]
						param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room2.lot.idDeco); // [!code ++]
						param.matColor = room2.lot.colDeco; // [!code ++]
						param.y += (float)room2.lot.decoFix * 0.01f; // [!code ++]
						rendererWallDeco.Draw(param); // [!code ++]
						param.y = orgY; // [!code ++]
					} // [!code ++]
					if (room2.lot.idDeco2 != 0 && roomHeight != 0f && !flag8 && (float)room2.lot.decoFix2 * 0.01f + heightLimitDeco < roomHeight + maxHeight - param.y) // [!code ++]
					{ // [!code ++]
						param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room2.lot.idDeco2); // [!code ++]
						param.matColor = room2.lot.colDeco2; // [!code ++]
						param.y += (float)room2.lot.decoFix2 * 0.01f; // [!code ++]
						param.z += (float)room2.lot.decoFix2 * 0.01f * heightModDeco; // [!code ++]
						rendererWallDeco.Draw(param); // [!code ++]
						param.y = orgY; // [!code ++]
						param.z = orgZ; // [!code ++]
					} // [!code ++]
				}
				else if (this.room != this.cell.Right.room && (this.cell.Right.room == currentRoom || (this.room?.lot != currentLot && this.cell.Right.room?.lot == currentLot))) // [!code --]
				Cell left = this.cell.Left; // [!code ++]
				if (blockDir == 2 && left.sourceBlock.tileType.IsWallOrFence && !this.cell.crossWall) // [!code ++]
				{
					this.room = this.cell.Right.room; // [!code --]
					_lowblock = !this.cell.Right.lotWall && !this.cell.Right.fullWall; // [!code --]
					_sourceBlock = left.sourceBlock; // [!code ++]
					param.mat = left.matBlock; // [!code ++]
				}
				else if (this.tileType.IsFullBlock && this.room != this.cell.FrontRight.room && (this.cell.FrontRight.room == currentRoom || (this.room?.lot != currentLot && this.cell.FrontRight.room?.lot == currentLot))) // [!code --]
				else // [!code ++]
				{
					this.room = this.cell.FrontRight.room; // [!code --]
					_lowblock = !this.cell.FrontRight.lotWall && !this.cell.FrontRight.fullWall; // [!code --]
					_sourceBlock = sourceBlock; // [!code ++]
					param.mat = matBlock; // [!code ++]
				} // [!code ++]
				this.tileType = _sourceBlock.tileType; // [!code ++]
				param.tile = (tile = _sourceBlock._tiles[0] + ((flag8 && this.tileType.UseLowBlock) ? 32 : 0)); // [!code ++]
				if (_sourceBlock.useAltColor) // [!code ++]
				{ // [!code ++]
					param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.altColor, _sourceBlock.colorMod)); // [!code ++]
				}
				else
				{
					this.room = this.room ?? this.cell.Front.room ?? this.cell.Right.room ?? this.cell.FrontRight.room; // [!code --]
					_lowblock = true; // [!code --]
					if (!this.tileType.IsFullBlock) // [!code --]
					param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.matColor, _sourceBlock.colorMod)); // [!code ++]
				} // [!code ++]
				if (roomHeight == 0f || flag8 || !this.tileType.RepeatBlock) // [!code ++]
				{ // [!code ++]
					if (!this.cell.hasDoor) // [!code ++]
					{
						if (this.cell.lotWall) // [!code --]
						{ // [!code --]
							_lowblock = false; // [!code --]
						} // [!code --]
						else if (this.room == currentRoom) // [!code --]
						{ // [!code --]
							_lowblock = !this.cell.fullWall; // [!code --]
						} // [!code --]
						_sourceBlock.renderData.Draw(param); // [!code ++]
					}
				}
				flag9 = (this.room != null && this.room.data.atrium) || (this.cell.room != null && this.cell.room.data.atrium); // [!code --]
				if (flag9) // [!code --]
				else // [!code ++]
				{
					_lowblock = false; // [!code --]
					_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFix, this.cell.hasDoor, this.cell.effect?.FireAmount ?? 0); // [!code ++]
				}
				if (this.room == null && alwaysLowblock) // [!code --]
				param.z += cornerWallFix2.z; // [!code ++]
				if ((blockDir == 2 || (this.cell.Front.HasWallOrFence && this.cell.Front.blockDir != 0)) != this.cell.isToggleWallPillar) // [!code ++]
				{
					_lowblock = true; // [!code --]
					roomHeight = 0f; // [!code --]
					if (this.cell.Back.IsSnowTile && this.cell.Right.IsSnowTile) // [!code ++]
					{ // [!code ++]
						param.snow = true; // [!code ++]
					} // [!code ++]
					param.tile = _sourceBlock._tiles[0] + ((flag8 && flag9 && this.tileType.UseLowBlock && !flag7) ? 32 : 0) + (this.tileType.IsFence ? 32 : 64); // [!code ++]
					if (roomHeight == 0f || !this.tileType.RepeatBlock || (flag8 && flag9 && !flag7)) // [!code ++]
					{ // [!code ++]
						_sourceBlock.renderData.Draw(param); // [!code ++]
					} // [!code ++]
					else // [!code ++]
					{ // [!code ++]
						_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFix); // [!code ++]
					} // [!code ++]
				}
				if (this.room != null) // [!code --]
				if (!flag8 && !showRoof && this.cell.Left.HasWallOrFence && this.cell.Left.blockDir != 0 && !this.cell.Left.isToggleWallPillar) // [!code ++]
				{ // [!code ++]
					orgX = param.x; // [!code ++]
					param.tile = _sourceBlock._tiles[0] + ((flag8 && this.tileType.UseLowBlock && !flag7) ? 32 : 0) + (this.tileType.IsFence ? 32 : 64); // [!code ++]
					param.x += cornerWallFix3.x; // [!code ++]
					param.y += cornerWallFix3.y; // [!code ++]
					param.z += cornerWallFix3.z; // [!code ++]
					if (!flag7 && (roomHeight == 0f || flag8)) // [!code ++]
					{ // [!code ++]
						_sourceBlock.renderData.Draw(param); // [!code ++]
					} // [!code ++]
					else // [!code ++]
					{ // [!code ++]
						_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight + cornerWallFix.y, ref renderSetting.peakFix); // [!code ++]
					} // [!code ++]
					param.x = orgX; // [!code ++]
				} // [!code ++]
				else if (this.cell.FrontLeft.HasWallOrFence && this.cell.FrontLeft.blockDir != 0 && (!flag8 || !this.cell.Left.HasWall) && !this.cell.isToggleWallPillar) // [!code ++]
				{ // [!code ++]
					orgX = param.x; // [!code ++]
					param.tile = _sourceBlock._tiles[0] + ((flag8 && this.tileType.UseLowBlock && !flag7) ? 32 : 0) + (this.tileType.IsFence ? 32 : 64); // [!code ++]
					param.x += cornerWallFix.x; // [!code ++]
					param.y += cornerWallFix.y; // [!code ++]
					param.z += cornerWallFix.z; // [!code ++]
					if (!flag7 && (roomHeight == 0f || flag8)) // [!code ++]
					{ // [!code ++]
						_sourceBlock.renderData.Draw(param); // [!code ++]
					} // [!code ++]
					else // [!code ++]
					{ // [!code ++]
						_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight + cornerWallFix.y, ref renderSetting.peakFix); // [!code ++]
					} // [!code ++]
					param.x = orgX; // [!code ++]
				} // [!code ++]
			} // [!code ++]
			if (blockDir == 1 || blockDir == 2) // [!code ++]
			{ // [!code ++]
				param.y = orgY; // [!code ++]
				param.z = orgZ; // [!code ++]
				param.dir = 1; // [!code ++]
				Room room3 = this.cell.Right.room ?? this.cell.room; // [!code ++]
				if (room3 != null && this.tileType.IsWall) // [!code ++]
				{
					maxHeight = (float)(cz - cx) * screen.tileAlign.y + (float)this.room.lot.mh * _heightMod.y; // [!code --]
					if (showRoof) // [!code --]
					if (room3.lot.idDeco != 0 && !this.cell.hasDoor) // [!code ++]
					{
						roomHeight = this.room.lot.realHeight; // [!code --]
						break; // [!code --]
						param.tile = -EMono.sources.blocks.rows[0].ConvertTile(1000 + room3.lot.idDeco); // [!code ++]
						param.matColor = room3.lot.colDeco; // [!code ++]
						param.y += (float)room3.lot.decoFix * 0.01f; // [!code ++]
						rendererWallDeco.Draw(param); // [!code ++]
						param.y = orgY; // [!code ++]
					}
					if ((noRoofMode && currentRoom == null) || (_lowblock && !this.tileType.ForceRpeatBlock)) // [!code --]
					if (room3.lot.idDeco2 != 0 && roomHeight != 0f && !flag9 && (float)room3.lot.decoFix2 * 0.01f + heightLimitDeco < roomHeight + maxHeight - param.y) // [!code ++]
					{
						roomHeight = 0f; // [!code --]
						break; // [!code --]
						param.tile = -EMono.sources.blocks.rows[0].ConvertTile(1000 + room3.lot.idDeco2); // [!code ++]
						param.matColor = room3.lot.colDeco2; // [!code ++]
						param.y += (float)room3.lot.decoFix2 * 0.01f; // [!code ++]
						param.z += (float)room3.lot.decoFix2 * 0.01f * heightModDeco; // [!code ++]
						rendererWallDeco.Draw(param); // [!code ++]
						param.y = orgY; // [!code ++]
						param.z = orgZ; // [!code ++]
					}
					int num34 = ((this.room.data.maxHeight == 0) ? 2 : this.room.data.maxHeight); // [!code --]
					roomHeight = EMono.setting.render.roomHeightMod * (float)((this.room.lot.height < num34) ? this.room.lot.height : num34) + 0.01f * (float)this.room.lot.heightFix; // [!code --]
				}
				break; // [!code --]
			case WallClipMode.ByLot: // [!code --]
				if (defaultBlockHeight > 0f || isIndoor) // [!code --]
				if (blockDir == 2 && this.cell.room == null && this.cell.Right.room != null) // [!code ++]
				{
					_lowblock = cx != 0 && cz != Size - 1 && ((!this.cell.Back.HasBlock && !this.cell.Back.isWallEdge) || (!this.cell.Left.HasBlock && !this.cell.Left.isWallEdge) || !this.cell.Back.Left.HasBlock); // [!code --]
					if (!_lowblock) // [!code --]
					Room room4 = this.cell.Right.room; // [!code ++]
					maxHeight = (float)(cz - cx) * screen.tileAlign.y + (float)room4.lot.mh * _heightMod.y; // [!code ++]
					if (showRoof) // [!code ++]
					{
						roomHeight = defaultBlockHeight * EMono.setting.render.roomHeightMod; // [!code --]
						maxHeight = (float)(cz - cx) * screen.tileAlign.y + (float)(int)this.cell.TopHeight * _heightMod.y; // [!code --]
						roomHeight = room4.lot.realHeight; // [!code ++]
					}
					break; // [!code --]
				} // [!code --]
				if (showFullWall) // [!code --]
				{ // [!code --]
					_lowblock = this.room != null; // [!code --]
					if (_lowblock) // [!code --]
					else if ((noRoofMode && currentRoom == null) || (_lowblock && !this.tileType.ForceRpeatBlock)) // [!code ++]
					{
						if (this.cell.Back.IsRoomEdge && this.cell.Right.IsRoomEdge && this.cell.Back.room == null && this.cell.Right.room == null && this.cell.Right.Front.room?.lot == this.room?.lot) // [!code --]
						{ // [!code --]
							_lowblock = false; // [!code --]
						} // [!code --]
						roomHeight = 0f; // [!code ++]
					}
					else if (this.cell.Back.room != null && this.cell.Back.room.lot == (this.cell.Front.room ?? this.cell.Right.room)?.lot) // [!code --]
					else // [!code ++]
					{
						_lowblock = true; // [!code --]
						int num20 = ((room4.data.maxHeight == 0) ? 2 : room4.data.maxHeight); // [!code ++]
						roomHeight = EMono.setting.render.roomHeightMod * (float)((room4.lot.height < num20) ? room4.lot.height : num20) + 0.01f * (float)room4.lot.heightFix; // [!code ++]
					}
				}
				Cell back2 = this.cell.Back; // [!code ++]
				if (blockDir == 2 && back2.sourceBlock.tileType.IsWallOrFence && !this.cell.crossWall) // [!code ++]
				{ // [!code ++]
					_sourceBlock = back2.sourceBlock; // [!code ++]
					param.mat = back2.matBlock; // [!code ++]
				} // [!code ++]
				else
				{
					_lowblock = lowBlock; // [!code --]
					_sourceBlock = sourceBlock; // [!code ++]
					param.mat = matBlock; // [!code ++]
				}
				if (this.tileType.RepeatBlock) // [!code --]
				this.tileType = _sourceBlock.tileType; // [!code ++]
				param.tile = (tile = -_sourceBlock._tiles[0] + ((flag9 && this.tileType.UseLowBlock) ? (-32) : 0)); // [!code ++]
				if (_sourceBlock.useAltColor) // [!code ++]
				{
					this.room = this.room ?? this.cell.Front.room ?? this.cell.Right.room ?? this.cell.FrontRight.room; // [!code --]
					if (this.room != null && (!noRoofMode || currentRoom != null) && (!showFullWall || currentRoom == null || this.room.lot == currentRoom.lot)) // [!code --]
					{ // [!code --]
						roomHeight = ((_lowblock && !this.tileType.ForceRpeatBlock) ? 0f : this.room.lot.realHeight); // [!code --]
						maxHeight = (float)(cz - cx) * screen.tileAlign.y + (float)this.room.lot.mh * _heightMod.y; // [!code --]
					} // [!code --]
					param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.altColor, _sourceBlock.colorMod)); // [!code ++]
				}
				break; // [!code --]
			} // [!code --]
		} // [!code --]
		if (!_lowblock && (double)roomHeight > 1.2 && this.tileType.RepeatBlock) // [!code --]
		{ // [!code --]
			num32 = 1; // [!code --]
		} // [!code --]
		else if (lowBlock) // [!code --]
		{ // [!code --]
			num32 = 2; // [!code --]
		} // [!code --]
		param.mat = matBlock; // [!code --]
		param.dir = this.cell.blockDir; // [!code --]
		param.snow = snowed; // [!code --]
		switch (this.tileType.blockRenderMode) // [!code --]
		{ // [!code --]
		case BlockRenderMode.FullBlock: // [!code --]
		{ // [!code --]
			bool invisible = sourceBlock.tileType.Invisible; // [!code --]
			if (invisible && (!buildMode || ActionMode.Cinema.IsActive)) // [!code --]
			{ // [!code --]
				break; // [!code --]
			} // [!code --]
			if (this.cell.isSurrounded) // [!code --]
			{ // [!code --]
				switch (innerMode) // [!code --]
				else // [!code ++]
				{
				case InnerMode.InnerBlock: // [!code --]
				case InnerMode.BuildMode: // [!code --]
					blockLight = _baseBrightness + fogBrightness; // [!code --]
					param.color = (int)(50f * blockLight) * 262144; // [!code --]
					param.matColor = 104025f; // [!code --]
					param.tile = (buildMode ? 1 : 2) + ((_lowblock || defaultBlockHeight > 0f) ? 3000000 : 0); // [!code --]
					rendererInnerBlock.Draw(param); // [!code --]
					return; // [!code --]
				case InnerMode.None: // [!code --]
				case InnerMode.Height: // [!code --]
					param.color = blockLight; // [!code --]
					break; // [!code --]
					param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.matColor, _sourceBlock.colorMod)); // [!code ++]
				}
			} // [!code --]
			if (snowed) // [!code --]
			{ // [!code --]
				param.color = floorLight; // [!code --]
			} // [!code --]
			param.color -= (int)(_shadowStrength * 0.8f * 50f) * 262144; // [!code --]
			if (currentRoom != null && !showFullWall) // [!code --]
			{ // [!code --]
				_lowblock = true; // [!code --]
				roomHeight = 0f; // [!code --]
				if (this.cell.room != currentRoom && (this.cell.Front.room == currentRoom || this.cell.Right.room == currentRoom || this.cell.FrontRight.room == currentRoom) && (this.cell.Back.room != currentRoom || this.cell.Right.room != currentRoom) && (this.cell.Front.room != currentRoom || this.cell.Left.room != currentRoom)) // [!code --]
				param.color += _rightWallShade; // [!code ++]
				if (roomHeight == 0f || flag9 || !this.tileType.RepeatBlock) // [!code ++]
				{
					_lowblock = false; // [!code --]
					if (!this.cell.hasDoor) // [!code ++]
					{ // [!code ++]
						_sourceBlock.renderData.Draw(param); // [!code ++]
					} // [!code ++]
				}
				if (!_lowblock) // [!code --]
				else // [!code ++]
				{
					int num36 = ((currentRoom.data.maxHeight == 0) ? 2 : currentRoom.data.maxHeight); // [!code --]
					roomHeight = EMono.setting.render.roomHeightMod * (float)((currentRoom.lot.height < num36) ? currentRoom.lot.height : num36) + 0.01f * (float)currentRoom.lot.heightFix; // [!code --]
					_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFix, this.cell.hasDoor, this.cell.effect?.FireAmount ?? 0); // [!code ++]
				}
			} // [!code --]
			if (flag9) // [!code --]
			{ // [!code --]
				_lowblock = (!this.cell.Front.HasFullBlock || !this.cell.Right.HasFullBlock) && (!this.cell.Front.HasFullBlock || !this.cell.Left.HasFullBlock) && (!this.cell.Back.HasFullBlock || !this.cell.Right.HasFullBlock) && (!this.cell.Back.HasFullBlock || !this.cell.Left.HasFullBlock); // [!code --]
				if (_lowblock) // [!code --]
				if ((this.cell.Right.HasWallOrFence && this.cell.Right.blockDir != 1) != this.cell.isToggleWallPillar && (blockDir != 2 || !this.cell.isToggleWallPillar)) // [!code ++]
				{
					roomHeight = 0f; // [!code --]
					if (this.cell.Left.IsSnowTile && this.cell.Front.IsSnowTile) // [!code ++]
					{ // [!code ++]
						param.snow = true; // [!code ++]
					} // [!code ++]
					orgX = param.x; // [!code ++]
					param.tile = _sourceBlock._tiles[0] + ((flag9 && this.tileType.UseLowBlock && !flag7) ? 32 : 0) + (this.tileType.IsFence ? 32 : 64); // [!code ++]
					if (!flag7 && (roomHeight == 0f || !this.tileType.RepeatBlock || flag9)) // [!code ++]
					{ // [!code ++]
						_sourceBlock.renderData.Draw(param); // [!code ++]
					} // [!code ++]
					else // [!code ++]
					{ // [!code ++]
						_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFix); // [!code ++]
					} // [!code ++]
					param.x = orgX; // [!code ++]
				}
			}
			if (invisible) // [!code --]
			param.y = orgY; // [!code ++]
			param.z = orgZ; // [!code ++]
			break; // [!code ++]
		} // [!code ++]
		case BlockRenderMode.HalfBlock: // [!code ++]
			param.color = floorLight; // [!code ++]
			_sourceBlock = ((sourceBlock.id == 5) ? EMono.sources.blocks.rows[matBlock.defBlock] : sourceBlock); // [!code ++]
			param.tile = _sourceBlock._tiles[0]; // [!code ++]
			param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, _sourceBlock.colorMod)); // [!code ++]
			param.tile2 = _sourceBlock.sourceAutoFloor._tiles[0]; // [!code ++]
			param.halfBlockColor = ((_sourceBlock.sourceAutoFloor.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, _sourceBlock.sourceAutoFloor.colorMod)); // [!code ++]
			sourceBlock.renderData.Draw(param); // [!code ++]
			break; // [!code ++]
		case BlockRenderMode.Pillar: // [!code ++]
		{ // [!code ++]
			RenderData renderData2 = sourceBlock.renderData; // [!code ++]
			param.tile = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length]; // [!code ++]
			param.matColor = ((sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, sourceBlock.colorMod)); // [!code ++]
			int num16 = this.cell.objDir + ((this.cell.objDir >= 7) ? this.cell.objDir : 0) + 1; // [!code ++]
			if (num16 == 0) // [!code ++]
			{
				roomHeight = 0f; // [!code --]
				_lowblock = false; // [!code --]
				renderData2.Draw(param); // [!code ++]
			}
			if (this.cell.Things.Count > 0) // [!code --]
			else // [!code ++]
			{
				_lowblock = false; // [!code --]
				renderData2.DrawRepeat(param, num16, sourceBlock.tileType.RepeatSize); // [!code ++]
			}
			param.tile = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length] + (_lowblock ? 3000000 : 0); // [!code --]
			param.tile = renderData2.idShadow; // [!code ++]
			SourcePref shadowPref2 = renderData2.shadowPref; // [!code ++]
			int shadow3 = shadowPref2.shadow; // [!code ++]
			passShadow.AddShadow(param.x + renderData2.offsetShadow.x, param.y + renderData2.offsetShadow.y, param.z + renderData2.offsetShadow.z, ShadowData.Instance.items[shadow3], shadowPref2, 0, param.snow); // [!code ++]
			break; // [!code ++]
		} // [!code ++]
		case BlockRenderMode.Floor: // [!code ++]
			param.color = floorLight; // [!code ++]
			param.tile = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length]; // [!code ++]
			param.matColor = ((sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, sourceBlock.colorMod)); // [!code ++]
			sourceBlock.renderData.Draw(param); // [!code ++]
			break; // [!code ++]
		default: // [!code ++]
			param.color = floorLight; // [!code ++]
			param.tile = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length] + ((_lowblock && this.tileType.UseLowBlock) ? 3000000 : 0); // [!code ++]
			param.matColor = ((sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, sourceBlock.colorMod));
			if (roomHeight == 0f)
			{
				if (!this.cell.hasDoor) // [!code --]
				{ // [!code --]
					sourceBlock.renderData.Draw(param); // [!code --]
				} // [!code --]
				sourceBlock.renderData.Draw(param); // [!code ++]
			}
			else
			{
				sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFixBlock, this.cell.hasDoor, this.cell.effect?.FireAmount ?? 0, isBlock: true); // [!code --]
				sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFixBlock); // [!code ++]
			}
			Room room2 = this.cell.Front.room ?? this.cell.room; // [!code --]
			if (room2 == null && this.cell.Right.room != null) // [!code --]
			break; // [!code ++]
		} // [!code ++]
	} // [!code ++]
	if (this.cell.pcSync && EMono.player.lightPower > 0f && !cinemaMode) // [!code ++]
	{ // [!code ++]
		if (this.cell.room != null || !this.cell.IsRoomEdge || !showRoof) // [!code ++]
		{ // [!code ++]
			goto IL_71e1; // [!code ++]
		} // [!code ++]
		if (this.cell._block == 0 || !this.cell.sourceBlock.tileType.RepeatBlock) // [!code ++]
		{ // [!code ++]
			Room obj = this.cell.FrontRight.room; // [!code ++]
			if (obj == null || !obj.HasRoof) // [!code ++]
			{
				room2 = this.cell.Right.room; // [!code --]
				goto IL_71e1; // [!code ++]
			}
			if (!invisible && room2 != null) // [!code --]
			{ // [!code --]
				if (room2.lot.idDeco != 0 && !this.cell.hasDoor) // [!code --]
				{ // [!code --]
					param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room2.lot.idDeco); // [!code --]
					param.matColor = room2.lot.colDeco; // [!code --]
					float y = param.y; // [!code --]
					param.y += (float)room2.lot.decoFix * 0.01f; // [!code --]
					rendererWallDeco.Draw(param); // [!code --]
					param.y = y; // [!code --]
				} // [!code --]
				if (room2.lot.idDeco2 != 0 && roomHeight != 0f && (float)room2.lot.decoFix2 * 0.01f + heightLimitDeco < roomHeight + maxHeight - param.y) // [!code --]
				{ // [!code --]
					param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room2.lot.idDeco2); // [!code --]
					param.matColor = room2.lot.colDeco2; // [!code --]
					float y2 = param.y; // [!code --]
					float num37 = param.z; // [!code --]
					param.y += (float)room2.lot.decoFix2 * 0.01f; // [!code --]
					param.z += (float)room2.lot.decoFix2 * 0.01f * heightModDeco; // [!code --]
					rendererWallDeco.Draw(param); // [!code --]
					param.y = y2; // [!code --]
					param.z = num37; // [!code --]
				} // [!code --]
		} // [!code ++]
	} // [!code ++]
	goto IL_7241; // [!code ++]
	IL_71e1: // [!code ++]
	if (!showRoof || !roof || this.cell.room == null || this.cell.Front.room == null || this.cell.Right.room == null) // [!code ++]
	{ // [!code ++]
		param.tile = num13; // [!code ++]
		rendererFov.Draw(param); // [!code ++]
	} // [!code ++]
	goto IL_7241; // [!code ++]
	IL_7d72: // [!code ++]
	if (detail.things.Count == 0 && detail.charas.Count == 0) // [!code ++]
	{ // [!code ++]
		return; // [!code ++]
	} // [!code ++]
	int num21 = 0; // [!code ++]
	thingPos.x = 0f; // [!code ++]
	thingPos.y = 0f; // [!code ++]
	thingPos.z = 0f; // [!code ++]
	freePos.x = (freePos.y = (freePos.z = 0f)); // [!code ++]
	if (this.cell.HasRamp) // [!code ++]
	{ // [!code ++]
		Vector3 rampFix = sourceBlock.tileType.GetRampFix(this.cell.blockDir); // [!code ++]
		param.x += rampFix.x; // [!code ++]
		param.y += rampFix.y; // [!code ++]
		param.z += rampFix.z; // [!code ++]
		freePos.x += rampFix.x; // [!code ++]
		freePos.y += rampFix.y; // [!code ++]
		freePos.z += rampFix.z; // [!code ++]
	} // [!code ++]
	param.y += (flag ? 0f : ((this.cell._bridge != 0) ? this.cell.sourceBridge.tileType.FloorHeight : sourceFloor.tileType.FloorHeight)); // [!code ++]
	orgPos.x = (orgX = param.x); // [!code ++]
	orgPos.y = (orgY = param.y); // [!code ++]
	orgPos.z = (orgZ = param.z); // [!code ++]
	if (flag && liquidLv > 0) // [!code ++]
	{ // [!code ++]
		if (liquidLv > 10) // [!code ++]
		{ // [!code ++]
			liquidLv = TileType.FloorWaterShallow.LiquidLV * 10; // [!code ++]
		} // [!code ++]
		liquidLv -= (int)(floatY * 0.5f); // [!code ++]
		param.liquidLv = liquidLv; // [!code ++]
		param.y -= TileType.FloorWaterShallow.FloorHeight; // [!code ++]
	} // [!code ++]
	Thing thing = null; // [!code ++]
	bool shadow = liquidLv == 0; // [!code ++]
	float num22 = 0f; // [!code ++]
	float num23 = 0f; // [!code ++]
	float num24 = 0f; // [!code ++]
	float num25 = 0f; // [!code ++]
	bool flag10 = false; // [!code ++]
	float num26 = 0f; // [!code ++]
	bool flag11 = false; // [!code ++]
	float num27 = 0f; // [!code ++]
	if (detail.things.Count > 0 && isSeen) // [!code ++]
	{ // [!code ++]
		_ = zSetting.max1; // [!code ++]
		float num28 = 0f; // [!code ++]
		for (int m = 0; m < detail.things.Count; m++) // [!code ++]
		{ // [!code ++]
			Thing t = detail.things[m]; // [!code ++]
			if ((fogged && !t.isRoofItem) || ((t.isHidden || t.trait.HideInAdv || t.isMasked) && !EMono.scene.actionMode.ShowMaskedThings) || (t.isRoofItem && ((this.room == null && !sourceBlock.tileType.IsFullBlock && !EMono._zone.IsPCFaction && !buildMode) || (lowBlock && !showFullWall && this.room != null) || (noRoofMode && currentRoom == null))) || (flag3 && !t.isRoofItem)) // [!code ++]
			{ // [!code ++]
				continue; // [!code ++]
			}
			room2 = this.cell.Right.room ?? this.cell.room; // [!code --]
			if (room2 == null && this.cell.Front.room != null) // [!code --]
			TileType tileType = t.trait.tileType; // [!code ++]
			bool isInstalled = t.IsInstalled; // [!code ++]
			SourcePref pref = t.Pref; // [!code ++]
			if (!isInstalled && t.category.tileDummy != 0) // [!code ++]
			{
				room2 = this.cell.Front.room; // [!code --]
				pref = rendererObjDummy.shadowPref; // [!code ++]
			}
			if (!invisible && room2 != null) // [!code --]
			float num29 = ((tileType.UseMountHeight && isInstalled) ? 0f : ((pref.height < 0f) ? 0f : ((pref.height == 0f) ? 0.1f : pref.height))); // [!code ++]
			if (t.ignoreStackHeight) // [!code ++]
			{
				if (room2.lot.idDeco != 0 && !this.cell.hasDoor) // [!code --]
				{ // [!code --]
					param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room2.lot.idDeco) * -1; // [!code --]
					param.matColor = room2.lot.colDeco; // [!code --]
					float y3 = param.y; // [!code --]
					param.y += (float)room2.lot.decoFix * 0.01f; // [!code --]
					rendererWallDeco.Draw(param); // [!code --]
					param.y = y3; // [!code --]
				} // [!code --]
				if (room2.lot.idDeco2 != 0 && roomHeight != 0f && (float)room2.lot.decoFix2 * 0.01f + heightLimitDeco < roomHeight + maxHeight - param.y) // [!code --]
				{ // [!code --]
					param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room2.lot.idDeco2) * -1; // [!code --]
					param.matColor = room2.lot.colDeco2; // [!code --]
					float y4 = param.y; // [!code --]
					float num38 = param.z; // [!code --]
					param.y += (float)room2.lot.decoFix2 * 0.01f; // [!code --]
					param.z += (float)room2.lot.decoFix2 * 0.01f * heightModDeco; // [!code --]
					rendererWallDeco.Draw(param); // [!code --]
					param.y = y4; // [!code --]
					param.z = num38; // [!code --]
				} // [!code --]
				thingPos.y -= num22; // [!code ++]
				thingPos -= altitudeFix * num23; // [!code ++]
			}
			break; // [!code --]
		} // [!code --]
		case BlockRenderMode.WallOrFence: // [!code --]
		{ // [!code --]
			if (map.config.fullWallHeight) // [!code --]
			shadow = thingPos.y < 0.16f && num27 < 0.16f; // [!code ++]
			_ = pref.bypassShadow; // [!code ++]
			param.shadowFix = 0f - thingPos.y; // [!code ++]
			param.liquidLv = ((thingPos.y + (float)t.altitude < 0.1f) ? liquidLv : 0); // [!code ++]
			if (t.isRoofItem) // [!code ++]
			{
				showFullWall = true; // [!code --]
				_lowblock = false; // [!code --]
				param.snow = isSnowCovered && !this.cell.isClearSnow; // [!code ++]
				SetRoofHeight(param, this.cell, cx, cz); // [!code ++]
				_actorPos.x = param.x; // [!code ++]
				_actorPos.y = param.y; // [!code ++]
				_actorPos.z = param.z + num28; // [!code ++]
				if (this.room != null) // [!code ++]
				{ // [!code ++]
					param.color = GetRoofLight(this.room.lot); // [!code ++]
				} // [!code ++]
				shadow = false; // [!code ++]
				param.liquidLv = 0; // [!code ++]
			}
			orgY = param.y; // [!code --]
			orgZ = param.z; // [!code --]
			param.color = (this.tileType.IsFence ? (floorLight - (float)((int)(_shadowStrength * 0.8f * 50f) * 262144)) : blockLight); // [!code --]
			bool flag10 = blockDir == 1 || _lowblock || flag9; // [!code --]
			bool flag11 = blockDir == 0 || _lowblock || flag9; // [!code --]
			if (!showFullWall && currentRoom != null) // [!code --]
			else // [!code ++]
			{
				if (!flag10) // [!code --]
				param.snow = snowed; // [!code ++]
				_actorPos.x = orgX + num25; // [!code ++]
				_actorPos.y = orgY; // [!code ++]
				_actorPos.z = orgZ + num28 + thingPos.z; // [!code ++]
				if (tileType.CanStack || !isInstalled) // [!code ++]
				{
					if (currentRoom == this.cell.room || (this.cell.lotWall && this.cell.room?.lot == currentLot && this.cell.Front.room != currentRoom)) // [!code --]
					if (thing?.id != t.id) // [!code ++]
					{
						if (!this.cell.IsRoomEdge || (this.cell.Front.room != this.cell.room && this.cell.FrontRight.room != this.cell.room)) // [!code --]
						{ // [!code --]
							flag10 = true; // [!code --]
						} // [!code --]
						_actorPos.x += thingPos.x; // [!code ++]
					}
					else if ((!this.cell.Front.lotWall || this.cell.Front.room?.lot != currentLot) && this.cell.Front.room != currentRoom) // [!code --]
					_actorPos.y += thingPos.y; // [!code ++]
					if (t.trait.IgnoreLastStackHeight && (thing == null || !thing.trait.IgnoreLastStackHeight)) // [!code ++]
					{
						flag10 = true; // [!code --]
						thingPos.y -= num22; // [!code ++]
						if (thing != null) // [!code ++]
						{ // [!code ++]
							_actorPos.z -= 0.2f; // [!code ++]
							thingPos.z -= 0.2f; // [!code ++]
						} // [!code ++]
						_actorPos.y -= num22; // [!code ++]
					}
					_actorPos.z += renderSetting.thingZ + (float)m * -0.01f + zSetting.mod1 * thingPos.y; // [!code ++]
				}
				if (!flag11) // [!code --]
				if (isInstalled) // [!code ++]
				{
					if (currentRoom == this.cell.room || (this.cell.lotWall && this.cell.room?.lot == currentLot && this.cell.Right.room != currentRoom)) // [!code --]
					if (t.TileType.IsRamp) // [!code ++]
					{
						if (!this.cell.IsRoomEdge || (this.cell.Right.room != this.cell.room && this.cell.FrontRight.room != this.cell.room)) // [!code --]
						Vector3 rampFix2 = t.TileType.GetRampFix(t.dir, pref); // [!code ++]
						orgX += rampFix2.x; // [!code ++]
						orgY += rampFix2.y; // [!code ++]
						orgZ += rampFix2.z; // [!code ++]
						freePos.x += rampFix2.x; // [!code ++]
						freePos.y += rampFix2.y; // [!code ++]
						freePos.z += rampFix2.z; // [!code ++]
						if (!this.cell.IsTopWater || t.altitude > 0) // [!code ++]
						{
							flag11 = true; // [!code --]
							num27 += rampFix2.y; // [!code ++]
						} // [!code ++]
						liquidLv -= (int)(rampFix2.y * 150f); // [!code ++]
						if (liquidLv < 0) // [!code ++]
						{ // [!code ++]
							liquidLv = 0; // [!code ++]
						}
					}
					else if ((!this.cell.Right.lotWall || this.cell.Right.room?.lot != currentLot) && this.cell.Right.room != currentRoom) // [!code --]
					{ // [!code --]
						flag11 = true; // [!code --]
					} // [!code --]
				} // [!code --]
			} // [!code --]
			if (blockDir == 0 || blockDir == 2) // [!code --]
			{ // [!code --]
				param.dir = 0; // [!code --]
				Room room3 = this.cell.Front.room ?? this.cell.room; // [!code --]
				if (room3 != null && this.tileType.IsWall) // [!code --]
				{ // [!code --]
					if (room3.lot.idDeco != 0 && !this.cell.hasDoor) // [!code --]
					else if (!flag11 && t.trait.IsChangeFloorHeight && !t.ignoreStackHeight) // [!code ++]
					{
						param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room3.lot.idDeco); // [!code --]
						param.matColor = room3.lot.colDeco; // [!code --]
						param.y += (float)room3.lot.decoFix * 0.01f; // [!code --]
						rendererWallDeco.Draw(param); // [!code --]
						param.y = orgY; // [!code --]
						orgY += num29 + (float)t.altitude * altitudeFix.y; // [!code ++]
						orgZ += (float)t.altitude * altitudeFix.z; // [!code ++]
						freePos.y += num29 + (float)t.altitude * altitudeFix.y; // [!code ++]
						if (!this.cell.IsTopWater || t.altitude > 0) // [!code ++]
						{ // [!code ++]
							num27 += num29 + (float)t.altitude * altitudeFix.y; // [!code ++]
						} // [!code ++]
						_actorPos.x += pref.x * (float)((!t.flipX) ? 1 : (-1)); // [!code ++]
						_actorPos.z += pref.z; // [!code ++]
						thingPos.z += pref.z; // [!code ++]
						liquidLv -= (int)(num29 * 150f); // [!code ++]
						if (liquidLv < 0) // [!code ++]
						{ // [!code ++]
							liquidLv = 0; // [!code ++]
						} // [!code ++]
					}
					if (room3.lot.idDeco2 != 0 && roomHeight != 0f && !flag10 && (float)room3.lot.decoFix2 * 0.01f + heightLimitDeco < roomHeight + maxHeight - param.y) // [!code --]
					else // [!code ++]
					{
						param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room3.lot.idDeco2); // [!code --]
						param.matColor = room3.lot.colDeco2; // [!code --]
						param.y += (float)room3.lot.decoFix2 * 0.01f; // [!code --]
						param.z += (float)room3.lot.decoFix2 * 0.01f * heightModDeco; // [!code --]
						rendererWallDeco.Draw(param); // [!code --]
						param.y = orgY; // [!code --]
						param.z = orgZ; // [!code --]
						thingPos.y += num29; // [!code ++]
						if (tileType.UseMountHeight) // [!code ++]
						{ // [!code ++]
							if ((tileType != TileType.Illumination || !this.cell.HasObj) && !tileType.AlwaysShow) // [!code ++]
							{ // [!code ++]
								if (noRoofMode && currentRoom == null && t.altitude >= lowWallObjAltitude && tileType != TileType.ObjFloat) // [!code ++]
								{ // [!code ++]
									continue; // [!code ++]
								} // [!code ++]
								if (hideHang && (this.cell.room?.lot != currentLot || (!this.cell.lotWall && this.cell.room != currentRoom))) // [!code ++]
								{ // [!code ++]
									Room room5 = ((t.dir == 0) ? this.cell.Back.room : this.cell.Left.room); // [!code ++]
									if (t.trait.AlwaysHideOnLowWall) // [!code ++]
									{ // [!code ++]
										if (room5 == null || !room5.data.showWallItem) // [!code ++]
										{ // [!code ++]
											continue; // [!code ++]
										} // [!code ++]
									} // [!code ++]
									else if (t.altitude >= lowWallObjAltitude) // [!code ++]
									{ // [!code ++]
										continue; // [!code ++]
									} // [!code ++]
								} // [!code ++]
							} // [!code ++]
							if (tileType.UseHangZFix) // [!code ++]
							{ // [!code ++]
								flag10 = true; // [!code ++]
							} // [!code ++]
							tileType.GetMountHeight(ref _actorPos, Point.shared.Set(index), t.dir, t); // [!code ++]
							shadow = false; // [!code ++]
							param.liquidLv = 0; // [!code ++]
							if (t.freePos) // [!code ++]
							{ // [!code ++]
								_actorPos.x += t.fx; // [!code ++]
								_actorPos.y += t.fy; // [!code ++]
							} // [!code ++]
						} // [!code ++]
						else // [!code ++]
						{ // [!code ++]
							thingPos.y += (float)t.altitude * altitudeFix.y; // [!code ++]
							thingPos.z += (float)t.altitude * altitudeFix.z; // [!code ++]
						} // [!code ++]
						_actorPos.x += pref.x * (float)((!t.flipX) ? 1 : (-1)); // [!code ++]
						_actorPos.z += pref.z; // [!code ++]
						if (pref.height >= 0f) // [!code ++]
						{ // [!code ++]
							thingPos.z += pref.z; // [!code ++]
						} // [!code ++]
					}
				} // [!code --]
				Cell left = this.cell.Left; // [!code --]
				if (blockDir == 2 && left.sourceBlock.tileType.IsWallOrFence && !this.cell.crossWall) // [!code --]
				{ // [!code --]
					_sourceBlock = left.sourceBlock; // [!code --]
					param.mat = left.matBlock; // [!code --]
				} // [!code --]
				else // [!code --]
				{ // [!code --]
					_sourceBlock = sourceBlock; // [!code --]
					param.mat = matBlock; // [!code --]
				} // [!code --]
				this.tileType = _sourceBlock.tileType; // [!code --]
				param.tile = (tile = _sourceBlock._tiles[0] + ((flag10 && this.tileType.UseLowBlock) ? 32 : 0)); // [!code --]
				if (_sourceBlock.useAltColor) // [!code --]
				{ // [!code --]
					param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.altColor, _sourceBlock.colorMod)); // [!code --]
				} // [!code --]
				else // [!code --]
				{ // [!code --]
					param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.matColor, _sourceBlock.colorMod)); // [!code --]
				} // [!code --]
				if (roomHeight == 0f || flag10 || !this.tileType.RepeatBlock) // [!code --]
				{ // [!code --]
					if (!this.cell.hasDoor) // [!code --]
					if (!tileType.UseMountHeight && m > 10) // [!code ++]
					{
						_sourceBlock.renderData.Draw(param); // [!code --]
						flag11 = true; // [!code ++]
					}
				}
				else
				{
					_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFix, this.cell.hasDoor, this.cell.effect?.FireAmount ?? 0); // [!code --]
				} // [!code --]
				param.z += cornerWallFix2.z; // [!code --]
				if ((blockDir == 2 || (this.cell.Front.HasWallOrFence && this.cell.Front.blockDir != 0)) != this.cell.isToggleWallPillar) // [!code --]
				{ // [!code --]
					if (this.cell.Back.IsSnowTile && this.cell.Right.IsSnowTile) // [!code --]
					{ // [!code --]
						param.snow = true; // [!code --]
					} // [!code --]
					param.tile = _sourceBlock._tiles[0] + ((flag10 && flag11 && this.tileType.UseLowBlock && !flag9) ? 32 : 0) + (this.tileType.IsFence ? 32 : 64); // [!code --]
					if (roomHeight == 0f || !this.tileType.RepeatBlock || (flag10 && flag11 && !flag9)) // [!code --]
					{ // [!code --]
						_sourceBlock.renderData.Draw(param); // [!code --]
					} // [!code --]
					else // [!code --]
					{ // [!code --]
						_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFix); // [!code --]
					} // [!code --]
					thingPos.y += num29; // [!code ++]
					_actorPos.x += pref.x * (float)((!t.flipX) ? 1 : (-1)); // [!code ++]
					_actorPos.z += pref.z; // [!code ++]
					thingPos.z += pref.z; // [!code ++]
				}
				if (!flag10 && !showRoof && this.cell.Left.HasWallOrFence && this.cell.Left.blockDir != 0 && !this.cell.Left.isToggleWallPillar) // [!code --]
				if (t.isFloating && isWater && !hasBridge && !flag) // [!code ++]
				{
					orgX = param.x; // [!code --]
					param.tile = _sourceBlock._tiles[0] + ((flag10 && this.tileType.UseLowBlock && !flag9) ? 32 : 0) + (this.tileType.IsFence ? 32 : 64); // [!code --]
					param.x += cornerWallFix3.x; // [!code --]
					param.y += cornerWallFix3.y; // [!code --]
					param.z += cornerWallFix3.z; // [!code --]
					if (!flag9 && (roomHeight == 0f || flag10)) // [!code --]
					{ // [!code --]
						_sourceBlock.renderData.Draw(param); // [!code --]
					} // [!code --]
					else // [!code --]
					flag = true; // [!code ++]
					float num30 = ((this.cell._bridge != 0) ? sourceBridge.tileType.FloorHeight : sourceFloor.tileType.FloorHeight); // [!code ++]
					orgY += 0.01f * floatY - num30; // [!code ++]
					if (!t.trait.IsChangeFloorHeight) // [!code ++]
					{
						_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight + cornerWallFix.y, ref renderSetting.peakFix); // [!code --]
						num26 = num29; // [!code ++]
					}
					param.x = orgX; // [!code --]
				} // [!code --]
				else if (this.cell.FrontLeft.HasWallOrFence && this.cell.FrontLeft.blockDir != 0 && (!flag10 || !this.cell.Left.HasWall) && !this.cell.isToggleWallPillar) // [!code --]
				{ // [!code --]
					orgX = param.x; // [!code --]
					param.tile = _sourceBlock._tiles[0] + ((flag10 && this.tileType.UseLowBlock && !flag9) ? 32 : 0) + (this.tileType.IsFence ? 32 : 64); // [!code --]
					param.x += cornerWallFix.x; // [!code --]
					param.y += cornerWallFix.y; // [!code --]
					param.z += cornerWallFix.z; // [!code --]
					if (!flag9 && (roomHeight == 0f || flag10)) // [!code --]
					_actorPos.y += 0.01f * floatY - num30; // [!code ++]
					if (liquidLv > 10) // [!code ++]
					{
						_sourceBlock.renderData.Draw(param); // [!code --]
						liquidLv = TileType.FloorWaterShallow.LiquidLV * 10; // [!code ++]
					}
					else // [!code --]
					liquidLv -= (int)(floatY * 0.5f); // [!code ++]
					if (liquidLv < 0) // [!code ++]
					{
						_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight + cornerWallFix.y, ref renderSetting.peakFix); // [!code --]
						liquidLv = 0; // [!code ++]
					}
					param.x = orgX; // [!code --]
					param.liquidLv = liquidLv; // [!code ++]
				}
			} // [!code --]
			if (blockDir == 1 || blockDir == 2) // [!code --]
			{ // [!code --]
				param.y = orgY; // [!code --]
				param.z = orgZ; // [!code --]
				param.dir = 1; // [!code --]
				Room room4 = this.cell.Right.room ?? this.cell.room; // [!code --]
				if (room4 != null && this.tileType.IsWall) // [!code --]
				num22 = num29; // [!code ++]
				if (t.sourceCard.multisize && !t.trait.IsGround) // [!code ++]
				{
					if (room4.lot.idDeco != 0 && !this.cell.hasDoor) // [!code --]
					{ // [!code --]
						param.tile = -EMono.sources.blocks.rows[0].ConvertTile(1000 + room4.lot.idDeco); // [!code --]
						param.matColor = room4.lot.colDeco; // [!code --]
						param.y += (float)room4.lot.decoFix * 0.01f; // [!code --]
						rendererWallDeco.Draw(param); // [!code --]
						param.y = orgY; // [!code --]
					} // [!code --]
					if (room4.lot.idDeco2 != 0 && roomHeight != 0f && !flag11 && (float)room4.lot.decoFix2 * 0.01f + heightLimitDeco < roomHeight + maxHeight - param.y) // [!code --]
					{ // [!code --]
						param.tile = -EMono.sources.blocks.rows[0].ConvertTile(1000 + room4.lot.idDeco2); // [!code --]
						param.matColor = room4.lot.colDeco2; // [!code --]
						param.y += (float)room4.lot.decoFix2 * 0.01f; // [!code --]
						param.z += (float)room4.lot.decoFix2 * 0.01f * heightModDeco; // [!code --]
						rendererWallDeco.Draw(param); // [!code --]
						param.y = orgY; // [!code --]
						param.z = orgZ; // [!code --]
					} // [!code --]
					num28 += zSetting.multiZ; // [!code ++]
				}
				if (blockDir == 2 && this.cell.room == null && this.cell.Right.room != null) // [!code --]
				orgZ += t.renderer.data.stackZ; // [!code ++]
				if (param.liquidLv > 0) // [!code ++]
				{
					Room room5 = this.cell.Right.room; // [!code --]
					maxHeight = (float)(cz - cx) * screen.tileAlign.y + (float)room5.lot.mh * _heightMod.y; // [!code --]
					if (showRoof) // [!code --]
					{ // [!code --]
						roomHeight = room5.lot.realHeight; // [!code --]
					} // [!code --]
					else if ((noRoofMode && currentRoom == null) || (_lowblock && !this.tileType.ForceRpeatBlock)) // [!code --]
					param.liquidLv += pref.liquidMod; // [!code ++]
					if (param.liquidLv < 1) // [!code ++]
					{
						roomHeight = 0f; // [!code --]
						param.liquidLv = 1; // [!code ++]
					}
					else // [!code --]
					else if (param.liquidLv > 99 + pref.liquidModMax) // [!code ++]
					{
						int num39 = ((room5.data.maxHeight == 0) ? 2 : room5.data.maxHeight); // [!code --]
						roomHeight = EMono.setting.render.roomHeightMod * (float)((room5.lot.height < num39) ? room5.lot.height : num39) + 0.01f * (float)room5.lot.heightFix; // [!code --]
						param.liquidLv = 99 + pref.liquidModMax; // [!code ++]
					}
				}
				Cell back2 = this.cell.Back; // [!code --]
				if (blockDir == 2 && back2.sourceBlock.tileType.IsWallOrFence && !this.cell.crossWall) // [!code --]
				{ // [!code --]
					_sourceBlock = back2.sourceBlock; // [!code --]
					param.mat = back2.matBlock; // [!code --]
				} // [!code --]
				else // [!code --]
				{ // [!code --]
					_sourceBlock = sourceBlock; // [!code --]
					param.mat = matBlock; // [!code --]
				} // [!code --]
				this.tileType = _sourceBlock.tileType; // [!code --]
				param.tile = (tile = -_sourceBlock._tiles[0] + ((flag11 && this.tileType.UseLowBlock) ? (-32) : 0)); // [!code --]
				if (_sourceBlock.useAltColor) // [!code --]
			} // [!code ++]
			if (!isInstalled || !tileType.UseMountHeight) // [!code ++]
			{ // [!code ++]
				if (t.altitude != 0) // [!code ++]
				{
					param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.altColor, _sourceBlock.colorMod)); // [!code --]
					_actorPos += altitudeFix * t.altitude; // [!code ++]
					if (!t.isRoofItem) // [!code ++]
					{ // [!code ++]
						num24 += (float)t.altitude; // [!code ++]
						num23 = t.altitude; // [!code ++]
					} // [!code ++]
				}
				else // [!code --]
				if (num24 >= 2f && ((this.cell.Back.room != null && this.cell.Back.IsRoomEdge) || (this.cell.Left.room != null && this.cell.Left.IsRoomEdge)) && hideHang && (this.cell.room?.lot != currentLot || (!this.cell.lotWall && this.cell.room != currentRoom))) // [!code ++]
				{
					param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.matColor, _sourceBlock.colorMod)); // [!code --]
					continue; // [!code ++]
				}
				param.color += _rightWallShade; // [!code --]
				if (roomHeight == 0f || flag11 || !this.tileType.RepeatBlock) // [!code --]
				if (t.freePos) // [!code ++]
				{
					if (!this.cell.hasDoor) // [!code --]
					if (t.isRoofItem) // [!code ++]
					{
						_sourceBlock.renderData.Draw(param); // [!code --]
						_actorPos.x += t.fx; // [!code ++]
						_actorPos.y += t.fy - (float)t.altitude * altitudeFix.y; // [!code ++]
					} // [!code ++]
					else // [!code ++]
					{ // [!code ++]
						_actorPos.x = orgX + t.fx - freePos.x; // [!code ++]
						_actorPos.y = orgY + t.fy - freePos.y; // [!code ++]
					}
				}
				else // [!code --]
				if (t.trait is TraitDoor && (t.trait as TraitDoor).IsOpen()) // [!code ++]
				{
					_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFix, this.cell.hasDoor, this.cell.effect?.FireAmount ?? 0); // [!code --]
					_actorPos.z += -0.5f; // [!code ++]
				}
				if ((this.cell.Right.HasWallOrFence && this.cell.Right.blockDir != 1) != this.cell.isToggleWallPillar && (blockDir != 2 || !this.cell.isToggleWallPillar)) // [!code --]
			} // [!code ++]
			if (!t.sourceCard.multisize || (t.pos.x == cx && t.pos.z == cz)) // [!code ++]
			{ // [!code ++]
				if (iconMode != 0) // [!code ++]
				{
					if (this.cell.Left.IsSnowTile && this.cell.Front.IsSnowTile) // [!code --]
					int num31 = 0; // [!code ++]
					switch (iconMode) // [!code ++]
					{
						param.snow = true; // [!code --]
					case CardIconMode.Visibility: // [!code ++]
						if (t.isMasked) // [!code ++]
						{ // [!code ++]
							num31 = 17; // [!code ++]
						} // [!code ++]
						break; // [!code ++]
					case CardIconMode.State: // [!code ++]
						if (t.placeState == PlaceState.installed) // [!code ++]
						{ // [!code ++]
							num31 = 18; // [!code ++]
						} // [!code ++]
						break; // [!code ++]
					case CardIconMode.Deconstruct: // [!code ++]
						if (t.isDeconstructing) // [!code ++]
						{ // [!code ++]
							num31 = 14; // [!code ++]
						} // [!code ++]
						break; // [!code ++]
					}
					orgX = param.x; // [!code --]
					param.tile = _sourceBlock._tiles[0] + ((flag11 && this.tileType.UseLowBlock && !flag9) ? 32 : 0) + (this.tileType.IsFence ? 32 : 64); // [!code --]
					if (!flag9 && (roomHeight == 0f || !this.tileType.RepeatBlock || flag11)) // [!code --]
					if (t.isNPCProperty && !EMono.debug.godBuild) // [!code ++]
					{
						_sourceBlock.renderData.Draw(param); // [!code --]
						num31 = 13; // [!code ++]
					}
					else // [!code --]
					if (num31 != 0) // [!code ++]
					{
						_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFix); // [!code --]
						passGuideBlock.Add(_actorPos.x, _actorPos.y, _actorPos.z - 10f, num31); // [!code ++]
					}
					param.x = orgX; // [!code --]
				}
				t.SetRenderParam(param); // [!code ++]
				if (_lowblock && t.trait.UseLowblock && !this.cell.HasFullBlock) // [!code ++]
				{ // [!code ++]
					param.tile += ((param.tile < 0f) ? (-64) : 64); // [!code ++]
				} // [!code ++]
				if (t.trait is TraitTrolley && EMono.pc.ai is AI_Trolley aI_Trolley && aI_Trolley.trolley.owner == t) // [!code ++]
				{ // [!code ++]
					RenderParam _param = new RenderParam(param); // [!code ++]
					EMono.core.actionsLateUpdate.Add(delegate // [!code ++]
					{ // [!code ++]
						t.SetRenderParam(_param); // [!code ++]
						_actorPos.x = EMono.pc.renderer.position.x; // [!code ++]
						_actorPos.y = EMono.pc.renderer.position.y - pref.height; // [!code ++]
						_actorPos.z = EMono.pc.renderer.position.z + 0.02f; // [!code ++]
						t.renderer.Draw(_param, ref _actorPos, !t.noShadow && (shadow || tileType.AlwaysShowShadow)); // [!code ++]
					}); // [!code ++]
				} // [!code ++]
				else // [!code ++]
				{ // [!code ++]
					t.renderer.Draw(param, ref _actorPos, !t.noShadow && (shadow || tileType.AlwaysShowShadow)); // [!code ++]
				} // [!code ++]
			} // [!code ++]
			if (isInstalled) // [!code ++]
			{ // [!code ++]
				num25 += pref.stackX * (float)((!t.flipX) ? 1 : (-1)); // [!code ++]
			}
			param.x = orgX; // [!code ++]
			param.y = orgY;
			param.z = orgZ;
			break; // [!code --]
		} // [!code --]
		case BlockRenderMode.HalfBlock: // [!code --]
			param.color = floorLight;
			_sourceBlock = ((sourceBlock.id == 5) ? EMono.sources.blocks.rows[matBlock.defBlock] : sourceBlock); // [!code --]
			param.tile = _sourceBlock._tiles[0]; // [!code --]
			param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, _sourceBlock.colorMod)); // [!code --]
			param.tile2 = _sourceBlock.sourceAutoFloor._tiles[0]; // [!code --]
			param.halfBlockColor = ((_sourceBlock.sourceAutoFloor.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, _sourceBlock.sourceAutoFloor.colorMod)); // [!code --]
			sourceBlock.renderData.Draw(param); // [!code --]
			break; // [!code --]
		case BlockRenderMode.Pillar: // [!code --]
		{ // [!code --]
			RenderData renderData2 = sourceBlock.renderData; // [!code --]
			param.tile = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length]; // [!code --]
			param.matColor = ((sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, sourceBlock.colorMod)); // [!code --]
			int num35 = this.cell.objDir + ((this.cell.objDir >= 7) ? this.cell.objDir : 0) + 1; // [!code --]
			if (num35 == 0) // [!code --]
			{ // [!code --]
				renderData2.Draw(param); // [!code --]
			} // [!code --]
			else // [!code --]
			thing = t; // [!code ++]
			if (pref.Float) // [!code ++]
			{
				renderData2.DrawRepeat(param, num35, sourceBlock.tileType.RepeatSize); // [!code --]
				liquidLv = 0; // [!code ++]
			}
			param.tile = renderData2.idShadow; // [!code --]
			SourcePref shadowPref2 = renderData2.shadowPref; // [!code --]
			int shadow3 = shadowPref2.shadow; // [!code --]
			passShadow.AddShadow(param.x + renderData2.offsetShadow.x, param.y + renderData2.offsetShadow.y, param.z + renderData2.offsetShadow.z, ShadowData.Instance.items[shadow3], shadowPref2, 0, param.snow); // [!code --]
			break; // [!code --]
		}
		case BlockRenderMode.Floor: // [!code --]
			param.color = floorLight; // [!code --]
			param.tile = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length]; // [!code --]
			param.matColor = ((sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, sourceBlock.colorMod)); // [!code --]
			sourceBlock.renderData.Draw(param); // [!code --]
			break; // [!code --]
		default: // [!code --]
			param.color = floorLight; // [!code --]
			param.tile = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length] + ((_lowblock && this.tileType.UseLowBlock) ? 3000000 : 0); // [!code --]
			param.matColor = ((sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, sourceBlock.colorMod)); // [!code --]
			if (roomHeight == 0f) // [!code --]
	} // [!code ++]
	orgY += num26; // [!code ++]
	if (detail.charas.Count <= 0) // [!code ++]
	{ // [!code ++]
		return; // [!code ++]
	} // [!code ++]
	param.shadowFix = 0f - num27; // [!code ++]
	param.color += 1310720f; // [!code ++]
	float max = zSetting.max2; // [!code ++]
	for (int n = 0; n < detail.charas.Count; n++) // [!code ++]
	{ // [!code ++]
		Chara chara = detail.charas[n]; // [!code ++]
		if (chara.host != null) // [!code ++]
		{ // [!code ++]
			continue; // [!code ++]
		} // [!code ++]
		if (chara != EMono.pc && chara != LayerDrama.alwaysVisible) // [!code ++]
		{ // [!code ++]
			if (chara.mimicry != null && chara.mimicry.IsThing) // [!code ++]
			{
				sourceBlock.renderData.Draw(param); // [!code --]
				if (flag3 || fogged || !this.cell.isSeen) // [!code ++]
				{ // [!code ++]
					continue; // [!code ++]
				} // [!code ++]
			}
			else // [!code --]
			else if (flag3 || fogged || (!showAllCards && !EMono.player.CanSee(chara))) // [!code ++]
			{
				sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFixBlock); // [!code --]
				continue; // [!code ++]
			}
			break; // [!code --]
		}
	} // [!code --]
	if (this.cell.pcSync && EMono.player.lightPower > 0f && !cinemaMode) // [!code --]
	{ // [!code --]
		if (this.cell.room != null || !this.cell.IsRoomEdge || !showRoof) // [!code --]
		_actorPos.x = orgX; // [!code ++]
		_actorPos.y = orgY; // [!code ++]
		_actorPos.z = orgZ; // [!code ++]
		if (this.cell.IsBlocked && chara.IsAstralBody) // [!code ++]
		{
			goto IL_721c; // [!code --]
			_actorPos.z += EMono.setting.render.astralBodyFixZ; // [!code ++]
		}
		if (this.cell._block == 0 || !this.cell.sourceBlock.tileType.RepeatBlock) // [!code --]
		chara.SetRenderParam(param); // [!code ++]
		_ = chara.IsAliveInCurrentZone; // [!code ++]
		if (chara.isRestrained) // [!code ++]
		{
			Room obj = this.cell.FrontRight.room; // [!code --]
			if (obj == null || !obj.HasRoof) // [!code --]
			TraitShackle restrainer = chara.GetRestrainer(); // [!code ++]
			if (restrainer != null) // [!code ++]
			{ // [!code ++]
				Vector3 getRestrainPos = restrainer.GetRestrainPos; // [!code ++]
				if (getRestrainPos != default(Vector3)) // [!code ++]
				{ // [!code ++]
					Vector3 position = restrainer.owner.renderer.position; // [!code ++]
					float defCharaHeight = EMono.setting.render.defCharaHeight; // [!code ++]
					float num32 = getRestrainPos.y + defCharaHeight - ((chara.Pref.height == 0f) ? defCharaHeight : chara.source.pref.height); // [!code ++]
					_actorPos.x = position.x + getRestrainPos.x * (float)((restrainer.owner.dir % 2 == 0) ? 1 : (-1)); // [!code ++]
					_actorPos.y = position.y + num32; // [!code ++]
					_actorPos.z = position.z + getRestrainPos.z; // [!code ++]
					param.liquidLv = 0; // [!code ++]
					param.shadowFix = orgY - _actorPos.y; // [!code ++]
					chara.renderer.SetFirst(first: true); // [!code ++]
					chara.renderer.Draw(param, ref _actorPos, drawShadow: true); // [!code ++]
					param.shadowFix = 0f; // [!code ++]
					continue; // [!code ++]
				} // [!code ++]
			} // [!code ++]
		} // [!code ++]
		if (!chara.sourceCard.multisize || (chara.pos.x == cx && chara.pos.z == cz)) // [!code ++]
		{ // [!code ++]
			if (chara.IsDeadOrSleeping && chara.IsPCC) // [!code ++]
			{ // [!code ++]
				float num33 = chara.renderer.data.size.y * 0.3f; // [!code ++]
				if (thingPos.y > max) // [!code ++]
				{ // [!code ++]
					thingPos.y = max; // [!code ++]
				} // [!code ++]
				float num34 = thingPos.y + num33; // [!code ++]
				float num35 = (float)n * -0.01f; // [!code ++]
				if (num34 > zSetting.thresh1) // [!code ++]
				{ // [!code ++]
					num35 = zSetting.mod1; // [!code ++]
				} // [!code ++]
				_actorPos.x += thingPos.x; // [!code ++]
				_actorPos.y += thingPos.y; // [!code ++]
				_actorPos.z += renderSetting.laydownZ + num35; // [!code ++]
				param.liquidLv = ((thingPos.y == 0f && liquidLv > 0) ? 90 : 0); // [!code ++]
				thingPos.y += num33 * 0.8f; // [!code ++]
				chara.renderer.Draw(param, ref _actorPos, liquidLv == 0); // [!code ++]
			} // [!code ++]
			else // [!code ++]
			{
				goto IL_721c; // [!code --]
				param.liquidLv = liquidLv; // [!code ++]
				if (isUnderwater) // [!code ++]
				{ // [!code ++]
					if (chara.Pref.FloatUnderwater) // [!code ++]
					{ // [!code ++]
						float num36 = ((this.cell._bridge != 0) ? sourceBridge.tileType.FloorHeight : sourceFloor.tileType.FloorHeight); // [!code ++]
						float num37 = floatYs[chara.uid % 10] + 10f + (float)(chara.uid % 30); // [!code ++]
						orgY += 0.01f * num37 - num36; // [!code ++]
						_actorPos.y += 0.01f * num37 - num36; // [!code ++]
						param.shadowFix -= 0.01f * num37 - num36; // [!code ++]
					} // [!code ++]
				} // [!code ++]
				else if (liquidLv > 0) // [!code ++]
				{ // [!code ++]
					if (chara.Pref.Float && !flag && !hasBridge) // [!code ++]
					{ // [!code ++]
						if (liquidLv > 20) // [!code ++]
						{ // [!code ++]
							float num38 = ((this.cell._bridge != 0) ? sourceBridge.tileType.FloorHeight : sourceFloor.tileType.FloorHeight); // [!code ++]
							orgY += 0.01f * floatY - num38; // [!code ++]
							_actorPos.y += 0.01f * floatY - num38; // [!code ++]
							int num39 = TileType.FloorWaterShallow.LiquidLV * 10; // [!code ++]
							num39 -= (int)(floatY * 0.5f); // [!code ++]
							param.liquidLv = num39; // [!code ++]
						} // [!code ++]
						else // [!code ++]
						{ // [!code ++]
							param.liquidLv -= 20; // [!code ++]
						} // [!code ++]
					} // [!code ++]
					param.liquidLv += chara.Pref.liquidMod; // [!code ++]
					if (param.liquidLv < 1) // [!code ++]
					{ // [!code ++]
						param.liquidLv = 1; // [!code ++]
					} // [!code ++]
					else if (param.liquidLv > 99 + chara.Pref.liquidModMax) // [!code ++]
					{ // [!code ++]
						param.liquidLv = 99 + chara.Pref.liquidModMax; // [!code ++]
					} // [!code ++]
				} // [!code ++]
				if (!chara.IsPC && EMono.pc.host != chara && !chara.renderer.IsMoving && detail.charas.Count > 1 && (detail.charas.Count != 2 || !detail.charas[0].IsDeadOrSleeping || !detail.charas[0].IsPCC)) // [!code ++]
				{ // [!code ++]
					_actorPos += renderSetting.charaPos[1 + ((num21 < 4) ? num21 : 3)]; // [!code ++]
				} // [!code ++]
				_actorPos.z += 0.01f * (float)n + renderSetting.charaZ; // [!code ++]
				num21++; // [!code ++]
				if (flag10) // [!code ++]
				{ // [!code ++]
					_actorPos.z += chara.renderer.data.hangedFixZ; // [!code ++]
				} // [!code ++]
				chara.renderer.Draw(param, ref _actorPos, liquidLv == 0); // [!code ++]
			}
		}
		param.x = orgX; // [!code ++]
		param.y = orgY; // [!code ++]
		param.z = orgZ; // [!code ++]
	}
	goto IL_727c; // [!code --]
	IL_721c: // [!code --]
	if (!showRoof || !roof || this.cell.room == null || this.cell.Front.room == null || this.cell.Right.room == null) // [!code --]
	{ // [!code --]
		param.tile = num32; // [!code --]
		rendererFov.Draw(param); // [!code --]
	} // [!code --]
	goto IL_727c; // [!code --]
	IL_727c: // [!code --]
	return; // [!code ++]
	IL_7241: // [!code ++]
	if (isSnowCovered && (sourceBlock.id != 0 || this.cell.hasDoor) && !snowed && !this.cell.isClearSnow && ((!this.cell.Front.HasRoof && !this.cell.Front.HasBlock) || (!this.cell.Right.HasRoof && !this.cell.Right.HasBlock)))
	{
		snowed = true;
```

[`public virtual void DrawTile()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/BaseTileMap.cs#L3259-L3265)
```cs:line-numbers=3259
		param.matColor = 104025f;
		renderFootmark.Draw(param);
	}
	goto IL_7dad; // [!code --]
	goto IL_7d72; // [!code ++]
	void Draw(int tile)
	{
		pass = passEdge;
```

## Chara

[`public Thing MakeBraineCell()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/Chara.cs#L8508-L8523)
```cs:line-numbers=8508

	public void OnInsulted()
	{
		if (!isDead) // [!code --]
		if (isDead) // [!code ++]
		{
			if (HasElement(1231)) // [!code --]
			{ // [!code --]
				Talk("insulted"); // [!code --]
				AddCondition<ConEuphoric>(100 * Evalue(1231)); // [!code --]
			} // [!code --]
			else if (EClass.rnd(20) == 0) // [!code --]
			return; // [!code ++]
		} // [!code ++]
		if (HasElement(1231)) // [!code ++]
		{ // [!code ++]
			Talk("insulted"); // [!code ++]
			AddCondition<ConEuphoric>(100 * Evalue(1231)); // [!code ++]
		} // [!code ++]
		else if (EClass.rnd(20) == 0) // [!code ++]
		{ // [!code ++]
			SetFeat(1231, 1, msg: true); // [!code ++]
			if (IsPC) // [!code ++]
			{
				SetFeat(1231, 1, msg: true); // [!code --]
				Steam.GetAchievement(ID_Achievement.NIRVANA); // [!code ++]
			}
		}
	}
```

## ConSmoking

[`public override int GetPhase()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ConSmoking.cs#L5-L10)
```cs:line-numbers=5
		return 0;
	}

	public override void OnStart() // [!code ++]
	{ // [!code ++]
		base.OnStart(); // [!code ++]
		if (owner.IsPC) // [!code ++]
		{ // [!code ++]
			Steam.GetAchievement(ID_Achievement.SMOKE); // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public override void Tick()
	{
		base.Tick();
```

## CustomDramaExpansion

[`public static bool console_cmd(DramaManager dm, Dictionary<string, string> line,`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/CustomDramaExpansion.cs#L322-L332)
```cs:line-numbers=322
	}

	[ElinDramaActionInvoke(null)]
	public static bool add_item(DramaManager dm, Dictionary<string, string> line, string itemId, string materialAlias = "wood", int lv = -1, int count = 1) // [!code --]
	public static bool add_item(DramaManager dm, Dictionary<string, string> line, string itemId, string materialAlias = "", int lv = -1, int count = 1) // [!code ++]
	{
		Chara chara = dm.GetChara(line["actor"]);
		SourceMaterial.Row row = EClass.sources.materials.alias.TryGetValue(materialAlias, "wood"); // [!code --]
		Thing t = ThingGen.Create(itemId, row.id, lv).SetNum(count); // [!code --]
		int idMat = EClass.sources.materials.alias.TryGetValue(materialAlias)?.id ?? (-1); // [!code ++]
		Thing t = ThingGen.Create(itemId, idMat, lv).SetNum(count); // [!code ++]
		chara.Pick(t);
		return true;
	}
```

## DOMAIN

[`public class DOMAIN`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/DOMAIN.cs#L4-L20)
```cs:line-numbers=4
{
	public const int domLuck = 810;

	public const int domEyth = 814; // [!code --]
	public const int domFaith = 802; // [!code ++]

	public const int domOblivion = 813;

	public const int domEarth = 812; // [!code ++]
 // [!code ++]
	public const int domMiracle = 803;

	public const int domArcane = 804;

	public const int domEyth = 814; // [!code ++]
 // [!code ++]
	public const int domComm = 805;

	public const int domEarth = 812; // [!code --]
	public const int domHealing = 811; // [!code ++]

	public const int domElement = 806;

```

[`public class DOMAIN`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/DOMAIN.cs#L22-L43)
```cs:line-numbers=22

	public const int domHarvest = 808;

	public const int domHealing = 811; // [!code --]
 // [!code --]
	public const int domMachine = 809;

	public const int domHarmony = 815;

	public const int domSurvival = 801; // [!code --]
 // [!code --]
	public const int domFaith = 802; // [!code --]
 // [!code --]
	public const int domTest = 800;

	public const int domSurvival = 801; // [!code ++]
 // [!code ++]
	public static readonly int[] IDS = new int[16]
	{
		810, 814, 813, 803, 804, 805, 812, 806, 807, 808, // [!code --]
		811, 809, 815, 801, 802, 800 // [!code --]
		810, 802, 813, 812, 803, 804, 814, 805, 811, 806, // [!code ++]
		807, 808, 809, 815, 800, 801 // [!code ++]
	};
}
public class Domain : EClass
```

## DramaOutcome

[`public void upgrade_miscreation()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/DramaOutcome.cs#L500-L505)
```cs:line-numbers=500
			}
		}
		Rand.SetSeed();
		Steam.GetAchievement(ID_Achievement.UPGRADE); // [!code ++]
	}

	public static bool If(DramaChoice item, Chara c)
```

## ELEMENT

[`public class ELEMENT`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ELEMENT.cs#L29-L34)
```cs:line-numbers=29

	public const int recharge = 761;

	public const int effectRange = 770; // [!code ++]
 // [!code ++]
	public const int comfort = 750;

	public const int _void = 0;
```

[`public class ELEMENT`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ELEMENT.cs#L77-L88)
```cs:line-numbers=77

	public const int fire = 21;

	public static readonly int[] IDS = new int[36] // [!code --]
	public static readonly int[] IDS = new int[37] // [!code ++]
	{
		765, 763, 762, 751, 752, 753, 754, 755, 756, 759,
		760, 761, 750, 0, 10, 1, 2, 3, 5, 11, // [!code --]
		12, 13, 14, 15, 17, 18, 20, 16, 22, 23, // [!code --]
		24, 25, 26, 29, 85, 21 // [!code --]
		760, 761, 770, 750, 0, 10, 1, 2, 3, 5, // [!code ++]
		11, 12, 13, 14, 15, 17, 18, 20, 16, 22, // [!code ++]
		23, 24, 25, 26, 29, 85, 21 // [!code ++]
	};
}
public class Element : EClass
```

## ENC

[`public class ENC`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ENC.cs#L48-L54)
```cs:line-numbers=48

	public const int mod_feint = 623;

	public const int sustain_END = 441; // [!code --]
	public const int sustain_DEX = 442; // [!code ++]

	public const int mod_flurry = 621;

```

[`public class ENC`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ENC.cs#L138-L151)
```cs:line-numbers=138

	public const int slowCorruption = 412;

	public const int sustain_DEX = 442; // [!code --]
	public const int mod_rapid = 602; // [!code ++]

	public const int creativeChef = 487;

	public const int deathagony = 488;

	public const int music_drunk = 489; // [!code --]
 // [!code --]
	public const int farsee = 490;

	public const int negateGravity = 491;
```

[`public class ENC`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ENC.cs#L174-L189)
```cs:line-numbers=174

	public const int mod_chaser = 620;

	public const int sustain_END = 441; // [!code ++]
 // [!code ++]
	public const int safetyMeasure = 486;

	public const int nonLethal = 485;

	public const int mod_rapid = 602; // [!code --]
	public const int music_drunk = 489; // [!code ++]

	public const int optimizeMana = 483;

	public const int slot_rune = 484; // [!code --]
 // [!code --]
	public const int sustain_PER = 443;

	public const int sustain_LER = 444;
```

[`public class ENC`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ENC.cs#L192-L198)
```cs:line-numbers=192

	public const int sustain_MAG = 446;

	public const int sustain_figure = 450; // [!code --]
	public const int sustain_CHA = 447; // [!code ++]
 // [!code ++]
	public const int slot_rune = 484; // [!code ++]

	public const int bane_dragon = 460;

```

[`public class ENC`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ENC.cs#L200-L206)
```cs:line-numbers=200

	public const int bane_fairy = 462;

	public const int sustain_CHA = 447; // [!code --]
	public const int sustain_figure = 450; // [!code ++]

	public const int bane_man = 464;

```

[`public class ENC`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ENC.cs#L224-L237)
```cs:line-numbers=224
	{
		656, 655, 654, 653, 651, 641, 650, 652, 660, 640,
		661, 851, 663, 664, 665, 666, 667, 850, 624, 865,
		864, 852, 662, 623, 441, 621, 419, 420, 421, 422, // [!code --]
		864, 852, 662, 623, 442, 621, 419, 420, 421, 422, // [!code ++]
		423, 424, 425, 426, 418, 427, 429, 430, 431, 432,
		435, 436, 437, 438, 428, 439, 417, 415, 380, 381,
		382, 383, 400, 401, 402, 403, 416, 404, 406, 407,
		408, 409, 410, 411, 622, 414, 405, 440, 412, 442, // [!code --]
		487, 488, 489, 490, 491, 492, 493, 600, 601, 603, // [!code --]
		604, 605, 606, 607, 608, 609, 620, 486, 485, 602, // [!code --]
		483, 484, 443, 444, 445, 446, 450, 460, 461, 462, // [!code --]
		447, 464, 465, 466, 467, 468, 480, 481, 463, 482 // [!code --]
		408, 409, 410, 411, 622, 414, 405, 440, 412, 602, // [!code ++]
		487, 488, 490, 491, 492, 493, 600, 601, 603, 604, // [!code ++]
		605, 606, 607, 608, 609, 620, 441, 486, 485, 489, // [!code ++]
		483, 443, 444, 445, 446, 447, 484, 460, 461, 462, // [!code ++]
		450, 464, 465, 466, 467, 468, 480, 481, 463, 482 // [!code ++]
	};
}
```

## FACTION

[``](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/FACTION.cs#L4-L34)
```cs:line-numbers=4

public class FACTION
{
	public const int bfRuin = 3702; // [!code --]
	public const int bfGeyser = 3701; // [!code ++]
 // [!code ++]
	public const int bfPlain = 3600; // [!code ++]

	public const int bfForest = 3601;

	public const int bfSnow = 3602;

	public const int bfHill = 3603; // [!code --]
	public const int bfFertile = 3700; // [!code ++]

	public const int bfBeach = 3604;

	public const int bfGeyser = 3701; // [!code --]
	public const int bfSea = 3605; // [!code ++]

	public const int bfUndersea = 3606;

	public const int bfFertile = 3700; // [!code --]
 // [!code --]
	public const int bfPlain = 3600; // [!code --]
 // [!code --]
	public const int bfSea = 3605; // [!code --]
 // [!code --]
	public const int bfCave = 3500;

	public const int fLuck = 2118; // [!code --]
	public const int bfHill = 3603; // [!code ++]

	public const int fSafety = 2205; // [!code --]
	public const int fRation = 2207; // [!code ++]
 // [!code ++]
	public const int fAdmin = 2115; // [!code ++]

	public const int fFood = 2204;

```

[`public class FACTION`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/FACTION.cs#L44-L62)
```cs:line-numbers=44

	public const int fTaxEvasion = 2119;

	public const int fLuck = 2118; // [!code ++]
 // [!code ++]
	public const int fLoyal = 2117;

	public const int fEducation = 2116;

	public const int bfTranquil = 3703; // [!code --]
	public const int bfRuin = 3702; // [!code ++]

	public const int fRation = 2207; // [!code --]
	public const int fAttraction = 2206; // [!code ++]

	public const int bfVolcano = 3704; // [!code --]
	public const int bfTranquil = 3703; // [!code ++]

	public const int actBuildMine = 4001;

	public const int bfFish = 3706; // [!code --]
	public const int bfHunt = 3705; // [!code ++]

	public const int actBuildInspect = 4006;

```

[`public class FACTION`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/FACTION.cs#L68-L75)
```cs:line-numbers=68

	public const int actBuildTerrain = 4002;

	public const int fAdmin = 2115; // [!code --]
 // [!code --]
	public const int actBuildCut = 4000;

	public const int bfStart = 3900;
```

[`public class FACTION`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/FACTION.cs#L104-L123)
```cs:line-numbers=104

	public const int bfMonster = 3707;

	public const int bfHunt = 3705; // [!code --]
	public const int bfFish = 3706; // [!code ++]
 // [!code ++]
	public const int bfVolcano = 3704; // [!code ++]

	public const int fConstruction = 2003;

	public const int fAttraction = 2206; // [!code --]
	public const int fSafety = 2205; // [!code ++]

	public static readonly int[] IDS = new int[53]
	{
		3702, 3601, 3602, 3603, 3604, 3701, 3606, 3700, 3600, 3605, // [!code --]
		3500, 2118, 2205, 2204, 2203, 2202, 2201, 2200, 2120, 2119, // [!code --]
		2117, 2116, 3703, 2207, 3704, 4001, 3706, 4006, 4005, 4004, // [!code --]
		4003, 4002, 2115, 4000, 3900, 3805, 3804, 3803, 3802, 3801, // [!code --]
		3800, 3784, 3783, 3782, 3781, 3780, 3710, 3709, 3708, 3707, // [!code --]
		3705, 2003, 2206 // [!code --]
		3701, 3600, 3601, 3602, 3700, 3604, 3605, 3606, 3500, 3603, // [!code ++]
		2207, 2115, 2204, 2203, 2202, 2201, 2200, 2120, 2119, 2118, // [!code ++]
		2117, 2116, 3702, 2206, 3703, 4001, 3705, 4006, 4005, 4004, // [!code ++]
		4003, 4002, 4000, 3900, 3805, 3804, 3803, 3802, 3801, 3800, // [!code ++]
		3784, 3783, 3782, 3781, 3780, 3710, 3709, 3708, 3707, 3706, // [!code ++]
		3704, 2003, 2205 // [!code ++]
	};
}
public class Faction : EClass
```

## FortuneRollData

[`public void Refresh()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/FortuneRollData.cs#L130-L136)
```cs:line-numbers=130
public void RefreshPrize()
{
	prizes.Clear();
	Rand.SetSeed(EClass.game.seed + seed + count); // [!code --]
	Rand.SetSeed(EClass.debug.enable ? (-1) : (EClass.game.seed + seed + count)); // [!code ++]
	List<List<string>> list = GetPrizeList();
	if ((EClass._zone.IsTown && EClass._zone.lv == 0) || (EClass.game.IsSurvival && EClass._zone is Zone_StartSiteSky))
	{
```

[`public List<List<string>> GetPrizeList()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/FortuneRollData.cs#L184-L190)
```cs:line-numbers=184
		{
			"computer", "834", "1090", "goods_figure", "goods_canvas", "mb_1", "mb_2", "mb_3", "mb_4", "mb_5",
			"1174", "1085", "toilet", "714", "nobility", "plat", "1165", "mathammer", "medal", "bbq",
			"panty", "beehive", "ticket_resident", "lovepotion", "crystal_sun", "goods_crim" // [!code --]
			"panty", "beehive", "ticket_resident", "lovepotion", "crystal_sun", "goods_crim", "ride_boat" // [!code ++]
		},
		new List<string>
		{
```

[`public List<List<string>> GetPrizeList()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/FortuneRollData.cs#L192-L198)
```cs:line-numbers=192
			"plat",
			"1165",
			"boat3",
			"medal" // [!code --]
			"medal", // [!code ++]
			"ride_boat2", // [!code ++]
			"play_ride_big", // [!code ++]
			"scrubber" // [!code ++]
		}
	};
	if (EClass.pc.faction.IsGlobalPolicyActive(2712))
```

[`public void GetPrize(int grade, int seed)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/FortuneRollData.cs#L245-L256)
```cs:line-numbers=245
				card.c_idRefCard = prize.idRef;
				card.rarity = Rarity.Legendary;
				break;
			case "scrubber": // [!code ++]
				if (grade == 3) // [!code ++]
				{ // [!code ++]
					card.rarity = Rarity.Legendary; // [!code ++]
					card.elements.SetBase(770, 1 + EClass.rnd(25)); // [!code ++]
				} // [!code ++]
				break; // [!code ++]
			}
		}
		if (grade != 3) // [!code --]
		{ // [!code --]
			prize.claimed = true; // [!code --]
		} // [!code --]
		prize.claimed = true; // [!code ++]
	}
	else
	{
```

## ID_Achievement

[`public enum ID_Achievement`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ID_Achievement.cs#L29-L33)
```cs:line-numbers=29
	PRIEST,
	PRIEST2,
	MAMA2,
	MAMA3 // [!code --]
	MAMA3, // [!code ++]
	DIARY, // [!code ++]
	LITTLEMIG, // [!code ++]
	MAMMOTH, // [!code ++]
	NIRVANA, // [!code ++]
	SMOKE, // [!code ++]
	SNAIL, // [!code ++]
	UPGRADE // [!code ++]
}
```

## InvOwner

[`public ListInteraction ListInteractions(ButtonGrid b, bool context)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/InvOwner.cs#L1383-L1401)
```cs:line-numbers=1383
		}
	}
	ListInteractions(listInteraction, t, trait, b, context);
	if (context && trait is TraitLightSource) // [!code --]
	{ // [!code --]
		listInteraction.Add("customBrightness", 300, delegate // [!code --]
		{ // [!code --]
			UIContextMenu uIContextMenu = EClass.ui.CreateContextMenuInteraction(); // [!code --]
			uIContextMenu.AddSlider("brightness", (float a) => a.ToString() ?? "", EClass.player.customLightMod, delegate(float b) // [!code --]
			{ // [!code --]
				EClass.player.customLightMod = (int)b; // [!code --]
				EClass.pc.RecalculateFOV(); // [!code --]
			}, 1f, 6f, isInt: true, hideOther: false); // [!code --]
			uIContextMenu.Show(); // [!code --]
		}); // [!code --]
	} // [!code --]
	if (AllowHold(t) && !t.isEquipped && !HasTrader)
	{
		Interaction item = listInteraction.Add((EClass.pc.held == t) ? "actPick" : "actHold", 60, delegate
```

[`public virtual void ListInteractions(ListInteraction list, Thing t, Trait trait,`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/InvOwner.cs#L1552-L1633)
```cs:line-numbers=1552
				});
			}
		}
		if (!LayerChara.Instance) // [!code --]
		{ // [!code --]
			if (trait.CanRead(EClass.pc)) // [!code --]
			{ // [!code --]
				list.Add("invRead", 110, delegate // [!code --]
				{ // [!code --]
					t.DoAct(new AI_Read // [!code --]
					{ // [!code --]
						target = t // [!code --]
					}); // [!code --]
				}, "remove"); // [!code --]
			} // [!code --]
			if (trait.CanUse(EClass.pc)) // [!code --]
			{ // [!code --]
				list.Add(trait.LangUse, 120, delegate // [!code --]
				{ // [!code --]
					if (trait.OnUse(EClass.pc)) // [!code --]
					{ // [!code --]
						EClass.player.EndTurn(); // [!code --]
					} // [!code --]
				}, "use"); // [!code --]
			} // [!code --]
			if (trait.CanDrink(EClass.pc)) // [!code --]
			{ // [!code --]
				list.Add("invDrink", 130, delegate // [!code --]
				{ // [!code --]
					t.DoAct(new AI_Drink // [!code --]
					{ // [!code --]
						target = t // [!code --]
					}); // [!code --]
				}, "remove"); // [!code --]
			} // [!code --]
			if (trait.CanEat(EClass.pc)) // [!code --]
			{ // [!code --]
				list.Add("invFood", 140, delegate // [!code --]
				{ // [!code --]
					t.DoAct(new AI_Eat // [!code --]
					{ // [!code --]
						cook = false, // [!code --]
						target = t // [!code --]
					}); // [!code --]
				}, "remove"); // [!code --]
			} // [!code --]
			if (trait.IsBlendBase) // [!code --]
			{ // [!code --]
				list.Add("invBlend", 150, delegate // [!code --]
				{ // [!code --]
					LayerDragGrid.Create(new InvOwnerBlend(t)); // [!code --]
				}, "blend"); // [!code --]
			} // [!code --]
		} // [!code --]
		if (!context) // [!code --]
		{ // [!code --]
			return; // [!code --]
		} // [!code --]
		if (trait is TraitCard) // [!code --]
		{ // [!code --]
			list.Add("invCollect", 150, delegate // [!code --]
			{ // [!code --]
				ContentCodex.Collect(t); // [!code --]
			}); // [!code --]
		} // [!code --]
		if (!trait.CanName) // [!code --]
		{ // [!code --]
			return; // [!code --]
		} // [!code --]
		list.Add("changeName", 200, delegate // [!code --]
		{ // [!code --]
			Dialog.InputName("dialogChangeName", t.c_refText.IsEmpty(""), delegate(bool cancel, string text) // [!code --]
			{ // [!code --]
				if (!cancel) // [!code --]
				{ // [!code --]
					t.c_refText = text; // [!code --]
				} // [!code --]
			}); // [!code --]
		}); // [!code --]
		trait.OnListInteraction(list, b, context); // [!code ++]
	}

	public virtual string GetAutoUseLang(ButtonGrid button)
```

## InvOwnerEquip

[`public override void ListInteractions(ListInteraction list, Thing t, Trait trait`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/InvOwnerEquip.cs#L35-L40)
```cs:line-numbers=35
				}
			}
		});
		trait.OnListInteraction(list, b, context); // [!code ++]
	}

	public override bool IsFailByCurse(Thing t)
```

## Map

[`public void ModFire(int x, int z, int amount)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/Map.cs#L1218-L1225)
```cs:line-numbers=1218
		}
	}

	public void TryShatter(Point pos, int ele, int power) // [!code --]
	public void TryShatter(Point pos, int ele, int power, Card CC = null) // [!code ++]
	{
		bool flag = false; // [!code ++]
		if (CC != null && CC.IsPCFactionOrMinion && (CC.HasElement(1651) || EClass.pc.Evalue(1651) >= 2)) // [!code ++]
		{ // [!code ++]
			flag = true; // [!code ++]
		} // [!code ++]
		Element element = Element.Create(ele);
		List<Card> list = new List<Card>();
		bool fire = ele == 910;
```

[`public void TryShatter(Point pos, int ele, int power)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/Map.cs#L1236-L1241)
```cs:line-numbers=1236
		{
			continue;
		}
		if (flag) // [!code ++]
		{ // [!code ++]
			if (item.isChara) // [!code ++]
			{ // [!code ++]
				if (item.IsPCFactionOrMinion) // [!code ++]
				{ // [!code ++]
					continue; // [!code ++]
				} // [!code ++]
			} // [!code ++]
			else if (!CanCook(item)) // [!code ++]
			{ // [!code ++]
				continue; // [!code ++]
			} // [!code ++]
		} // [!code ++]
		if (EClass._zone.IsPCFaction && EClass.Branch.HasItemProtection)
		{
			Card rootCard = item.GetRootCard();
```

[`public void TryShatter(Point pos, int ele, int power)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/Map.cs#L1289-L1295)
```cs:line-numbers=1289
	}
	list.Shuffle();
	int num = 0;
	bool flag = false; // [!code --]
	bool flag2 = false; // [!code ++]
	foreach (Card item3 in list)
	{
		if (!item3.trait.CanBeDestroyed || item3.category.IsChildOf("currency") || item3.trait is TraitDoor || item3.trait is TraitFigure || item3.trait is TraitTrainingDummy || item3.rarity >= Rarity.Legendary)
```

[`public void TryShatter(Point pos, int ele, int power)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/Map.cs#L1320-L1328)
```cs:line-numbers=1320
		{
			continue;
		}
		bool flag2 = CanCook(item3); // [!code --]
		bool flag3 = CanCook(item3); // [!code ++]
		string text = "";
		if (flag2) // [!code --]
		if (flag3) // [!code ++]
		{
			if (fire)
			{
```

[`public void TryShatter(Point pos, int ele, int power)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/Map.cs#L1359-L1365)
```cs:line-numbers=1359
				text = "711";
			}
		}
		if (flag2 && !text.IsEmpty()) // [!code --]
		if (flag3 && !text.IsEmpty()) // [!code ++]
		{
			Thing thing2 = item3.Split(1);
			List<Thing> list4 = new List<Thing>();
```

[`public void TryShatter(Point pos, int ele, int power)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/Map.cs#L1414-L1423)
```cs:line-numbers=1414
		{
			ActionMode.Adv.itemLost++;
		}
		if (!flag) // [!code --]
		if (!flag2) // [!code ++]
		{
			pos.PlayEffect(fire ? "Element/eleFire" : "Element/eleCold");
			flag = true; // [!code --]
			flag2 = true; // [!code ++]
		}
		num++;
	}
```

## ModUtil

[`public static void OnModsActivated()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ModUtil.cs#L177-L182)
```cs:line-numbers=177

	private static void OnSourceImporting()
	{
		ExcelParser.allowTrimming = true; // [!code ++]
		ImportAllModSourceSheets();
	}

```

[`private static void OnSourceImported()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ModUtil.cs#L209-L214)
```cs:line-numbers=209
	SourceCache.FinalizeCache();
	SourceCache.InvalidateCacheBlobs();
	SourceCache.ClearDetail();
	ExcelParser.ClearStaticRows(); // [!code ++]
	if (EClass.core.launchArgs.Contains("EXPORTSOURCE"))
	{
		string text = CorePath.rootExe + "/SourceExport/" + EClass.core.version.GetText();
```

[`public static void AddContextMenuEntry(Action onClick, string menuEntry, string`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/ModUtil.cs#L1115-L1121)
```cs:line-numbers=1115
		ContextMenuProxy contextMenuProxy = children.Find((ContextMenuProxy p) => p.MenuEntry == part);
		if (contextMenuProxy == null)
		{
			contextMenuProxy = new ContextMenuProxy(part, flag ? part : displayName) // [!code --]
			contextMenuProxy = new ContextMenuProxy(flag ? part : displayName, part) // [!code ++]
			{
				onClick = ((i == array.Length - 1 && flag) ? null : new Action(SafeInvoke)),
				isMenu = flag
```

## RecipeCard

[`public override void Build(Chara chara, Card t, Point pos, int mat, int dir, int`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/RecipeCard.cs#L464-L474)
```cs:line-numbers=464
	{
		mat = 2;
	}
	SourceMaterial.Row row = EClass.sources.materials.map.TryGetValue(mat) ?? EClass.sources.materials.map[2]; // [!code --]
	EClass.pc.PlaySound(row.GetSoundImpact()); // [!code --]
	t.SetDir(dir);
	t.idSkin = idSkin;
	EClass._zone.AddCard(t, pos);
	t.PlaySoundImpact(); // [!code ++]
	if (t.trait is TraitHouseBoard && ActionMode.Build.houseBoard != null)
	{
		(t.trait as TraitHouseBoard).data = IO.DeepCopy(ActionMode.Build.houseBoard.data);
```

## Religion

[`public virtual void OnReforge(Thing t)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/Religion.cs#L296-L302)
```cs:line-numbers=296
	foreach (Element value in t.elements.dict.Values)
	{
		int num = value.id;
		if ((uint)(num - 64) > 3u && IsFaithElement(value)) // [!code --]
		if ((uint)(num - 64) > 3u && num != 92 && IsFaithElement(value)) // [!code ++]
		{
			value.vExp = -1;
		}
```

## SlaverData

[`public void TryRefresh(Chara owner)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/SlaverData.cs#L19-L25)
```cs:line-numbers=19
	int num = 4 + EClass.rnd(4);
	for (int i = 0; i < num; i++)
	{
		Chara item = ((owner.trait.SlaverType != SlaverType.Animal) ? CharaGen.Create(SpawnListChara.Get("slaver_human", (SourceChara.Row s) => EClass.sources.races.map[s.race].tag.Contains("human") && s.quality == 0).Select().id) : ((!(EClass._zone is Zone_Yowyn) || EClass.rnd(3) == 0) ? CharaGen.Create(SpawnListChara.Get("slaver_animal", (SourceChara.Row s) => EClass.sources.races.map[s.race].tag.Contains("animal") && s.quality == 0).Select().id) : CharaGen.Create(SpawnListChara.Get("slaver_horse", (SourceChara.Row s) => s.id == "horse_yowyn").Select().id))); // [!code --]
		SourceRace.Row value2; // [!code ++]
		SourceRace.Row value; // [!code ++]
		Chara item = ((owner.trait.SlaverType != SlaverType.Animal) ? CharaGen.Create(SpawnListChara.Get("slaver_human", (SourceChara.Row s) => EClass.sources.races.map.TryGetValue(s.race, out value2) && value2.tag.Contains("human") && s.quality == 0).Select().id) : ((!(EClass._zone is Zone_Yowyn) || EClass.rnd(3) == 0) ? CharaGen.Create(SpawnListChara.Get("slaver_animal", (SourceChara.Row s) => EClass.sources.races.map.TryGetValue(s.race, out value) && value.tag.Contains("animal") && s.quality == 0).Select().id) : CharaGen.Create(SpawnListChara.Get("slaver_horse", (SourceChara.Row s) => s.id == "horse_yowyn").Select().id))); // [!code ++]
		list.Add(item);
	}
	dateRefresh = EClass.world.date.GetRaw(168);
```

## SourceImporter

[`where c.IsDirtyOrEmpty`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/SourceImporter.cs#L107-L118)
```cs:line-numbers=107
			ISheet[] item = value2.Item3;
			foreach (ISheet sheet in item)
			{
				SourceData.BaseRow[] item2 = LoadBySheetName(sheet, value2.Item1).Item2; // [!code --]
				if (!item2.IsEmpty()) // [!code --]
				ExcelParser.allowTrimming = true; // [!code ++]
				try // [!code ++]
				{
					sourceCache.EmplaceCache(sheet.SheetName, item2); // [!code --]
					value?.sourceRows.UnionWith(item2); // [!code --]
					Debug.Log($"#source workbook {arg}:{sheet.SheetName}:{item2.Length}"); // [!code --]
					SourceData.BaseRow[] item2 = LoadBySheetName(sheet, value2.Item1).Item2; // [!code ++]
					if (!item2.IsEmpty()) // [!code ++]
					{ // [!code ++]
						sourceCache.EmplaceCache(sheet.SheetName, item2); // [!code ++]
						value?.sourceRows.UnionWith(item2); // [!code ++]
						Debug.Log($"#source workbook {arg}:{sheet.SheetName}:{item2.Length}"); // [!code ++]
					} // [!code ++]
				} // [!code ++]
				finally // [!code ++]
				{ // [!code ++]
					ExcelParser.allowTrimming = false; // [!code ++]
				}
			}
			continue;
```

[`where c.IsDirtyOrEmpty`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/SourceImporter.cs#L132-L137)
```cs:line-numbers=132
	foreach (SourceCache sourceCache2 in array2)
	{
		string text2 = sourceCache2.SheetFile.ShortPath();
		string key; // [!code ++]
		if (sourceCache2.IsDirtyOrEmpty)
		{
			if (!dictionary.TryGetValue(sourceCache2, out var value3) || value3.Item2.Length == 0)
```

[`where c.IsDirtyOrEmpty`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/SourceImporter.cs#L146-L168)
```cs:line-numbers=146
				{
					continue;
				}
				var (sourceData, array4) = LoadBySheetName(sheet2, value3.Item1); // [!code --]
				if ((object)sourceData != null) // [!code --]
				key = sheet2.SheetName; // [!code ++]
				int allowTrimming; // [!code ++]
				switch (key) // [!code ++]
				{
					int? num2 = array4?.Length; // [!code --]
					if (num2.HasValue && num2.GetValueOrDefault() > 0) // [!code --]
				default: // [!code ++]
					allowTrimming = ((!(key == "Word")) ? 1 : 0); // [!code ++]
					break; // [!code ++]
				case "General": // [!code ++]
				case "Game": // [!code ++]
				case "Note": // [!code ++]
				case "List": // [!code ++]
					allowTrimming = 0; // [!code ++]
					break; // [!code ++]
				} // [!code ++]
				ExcelParser.allowTrimming = (byte)allowTrimming != 0; // [!code ++]
				try // [!code ++]
				{ // [!code ++]
					var (sourceData, array4) = LoadBySheetName(sheet2, value3.Item1); // [!code ++]
					if ((object)sourceData != null) // [!code ++]
					{
						sourceCache2.EmplaceCache(sheet2.SheetName, array4); // [!code --]
						sourceCache2.Mod?.sourceRows.UnionWith(array4); // [!code --]
						hashSet.Add(sourceData); // [!code --]
						int? num2 = array4?.Length; // [!code ++]
						if (num2.HasValue && num2.GetValueOrDefault() > 0) // [!code ++]
						{ // [!code ++]
							sourceCache2.EmplaceCache(sheet2.SheetName, array4); // [!code ++]
							sourceCache2.Mod?.sourceRows.UnionWith(array4); // [!code ++]
							hashSet.Add(sourceData); // [!code ++]
						} // [!code ++]
					}
				}
				finally // [!code ++]
				{ // [!code ++]
					ExcelParser.allowTrimming = false; // [!code ++]
				} // [!code ++]
			}
			continue;
		}
		foreach (KeyValuePair<string, SourceData.BaseRow[]> item3 in sourceCache2.Source)
		{
			item3.Deconstruct(out var key, out var value4); // [!code --]
			item3.Deconstruct(out key, out var value4); // [!code ++]
			string text3 = key;
			SourceData.BaseRow[] array5 = value4;
			SourceData sourceData2 = FindSourceByName(text3);
```

## SourceMaterial

[`public void Init()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/SourceMaterial.cs#L120-L139)
```cs:line-numbers=120
			matColors = new MatColors();
			foreach (string item in hashSet)
			{
				var (text, s, _) = CustomSourceContent.GetParams(item); // [!code --]
				switch (text) // [!code --]
				if (item.StartsWith("addColorMain") || item.StartsWith("addCol_Main")) // [!code ++]
				{
				case "addCol_Main": // [!code --]
				case "addColorMain": // [!code --]
					matColors.main = s.ToColor(); // [!code --]
					break; // [!code --]
				case "addCol_Alt": // [!code --]
				case "addColorAlt": // [!code --]
					matColors.alt = s.ToColor(); // [!code --]
					break; // [!code --]
					matColors.main = item.ExtractInBetween('(', ')').ToColor(); // [!code ++]
				} // [!code ++]
				else if (item.StartsWith("addColorAlt") || item.StartsWith("addCol_Alt")) // [!code ++]
				{ // [!code ++]
					matColors.alt = item.ExtractInBetween('(', ')').ToColor(); // [!code ++]
				}
			}
			Core.Instance.Colors.matColors[alias] = matColors;
			Debug.Log($"#mod-content adding new material '{alias}'/{id}, main '{matColors.main}', alt '{matColors.alt}'"); // [!code ++]
		}
		matColor = matColors.main;
		altColor = matColors.alt;
```

## TCUI

[`public override void OnDraw(ref Vector3 pos)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/TCUI.cs#L27-L33)
```cs:line-numbers=27
			Vector3 position = Camera.main.WorldToScreenPoint(_pos);
			position.z = 0f;
			Vector3 vector = FixPos;
			if (render.hasActor && !render.actor.isPCC && (bool)render.actor && (bool)render.actor.sr.sprite) // [!code --]
			if (render != null && render.hasActor && !render.actor.isPCC && (bool)render.actor && (bool)render.actor.sr && (bool)render.actor.sr.sprite) // [!code ++]
			{
				float num = 128f / render.actor.sr.sprite.rect.height;
				int pivotY = render.owner.Pref.pivotY;
```

## Trait

[`public virtual void OnRenderTile(Point point, HitResult result, int dir)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/Trait.cs#L749-L754)
```cs:line-numbers=749
		}
	}

	public virtual void OnListInteraction(InvOwner.ListInteraction list, ButtonGrid b, bool context) // [!code ++]
	{ // [!code ++]
		Card card = owner; // [!code ++]
		Thing t = card as Thing; // [!code ++]
		if (t == null) // [!code ++]
		{ // [!code ++]
			return; // [!code ++]
		} // [!code ++]
		if (!LayerChara.Instance) // [!code ++]
		{ // [!code ++]
			if (CanRead(EClass.pc)) // [!code ++]
			{ // [!code ++]
				list.Add("invRead", 110, delegate // [!code ++]
				{ // [!code ++]
					t.DoAct(new AI_Read // [!code ++]
					{ // [!code ++]
						target = t // [!code ++]
					}); // [!code ++]
				}, "remove"); // [!code ++]
			} // [!code ++]
			if (CanUse(EClass.pc)) // [!code ++]
			{ // [!code ++]
				list.Add(LangUse, 120, delegate // [!code ++]
				{ // [!code ++]
					if (OnUse(EClass.pc)) // [!code ++]
					{ // [!code ++]
						EClass.player.EndTurn(); // [!code ++]
					} // [!code ++]
				}, "use"); // [!code ++]
			} // [!code ++]
			if (CanDrink(EClass.pc)) // [!code ++]
			{ // [!code ++]
				list.Add("invDrink", 130, delegate // [!code ++]
				{ // [!code ++]
					t.DoAct(new AI_Drink // [!code ++]
					{ // [!code ++]
						target = t // [!code ++]
					}); // [!code ++]
				}, "remove"); // [!code ++]
			} // [!code ++]
			if (CanEat(EClass.pc)) // [!code ++]
			{ // [!code ++]
				list.Add("invFood", 140, delegate // [!code ++]
				{ // [!code ++]
					t.DoAct(new AI_Eat // [!code ++]
					{ // [!code ++]
						cook = false, // [!code ++]
						target = t // [!code ++]
					}); // [!code ++]
				}, "remove"); // [!code ++]
			} // [!code ++]
			if (IsBlendBase) // [!code ++]
			{ // [!code ++]
				list.Add("invBlend", 150, delegate // [!code ++]
				{ // [!code ++]
					LayerDragGrid.Create(new InvOwnerBlend(t)); // [!code ++]
				}, "blend"); // [!code ++]
			} // [!code ++]
		} // [!code ++]
		if (!context || !CanName) // [!code ++]
		{ // [!code ++]
			return; // [!code ++]
		} // [!code ++]
		list.Add("changeName", 200, delegate // [!code ++]
		{ // [!code ++]
			Dialog.InputName("dialogChangeName", t.c_refText.IsEmpty(""), delegate(bool cancel, string text) // [!code ++]
			{ // [!code ++]
				if (!cancel) // [!code ++]
				{ // [!code ++]
					t.c_refText = text; // [!code ++]
				} // [!code ++]
			}); // [!code ++]
		}); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public virtual int CompareTo(Card b)
	{
		return 0;
```

## TraitCard

[`public override int GetMatColor()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/TraitCard.cs#L6-L9)
```cs:line-numbers=6
	{
		return -3;
	}
 // [!code ++]
	public override void OnListInteraction(InvOwner.ListInteraction list, ButtonGrid b, bool context) // [!code ++]
	{ // [!code ++]
		base.OnListInteraction(list, b, context); // [!code ++]
		if (context) // [!code ++]
		{ // [!code ++]
			list.Add("invCollect", 150, delegate // [!code ++]
			{ // [!code ++]
				ContentCodex.Collect(owner as Thing); // [!code ++]
			}); // [!code ++]
		} // [!code ++]
	} // [!code ++]
}
```

## TraitDiary

[`public override void OnRead(Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/TraitDiary.cs#L14-L19)
```cs:line-numbers=14
		chara.MakeAlly(msg: false);
		chara.PlaySound("identify");
		chara.PlayEffect("teleport");
		if (c.IsPC && GetParam(1) == "sister") // [!code ++]
		{ // [!code ++]
			Steam.GetAchievement(ID_Achievement.DIARY); // [!code ++]
		} // [!code ++]
		owner.ModNum(-1);
	}
}
```

## TraitLightSource

[`public class TraitLightSource : TraitTorch`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/TraitLightSource.cs#L1-L4)
```cs:line-numbers=1
public class TraitLightSource : TraitTorch
{
	public int LightRadius => GetParam(1).ToInt();
 // [!code ++]
	public override void OnListInteraction(InvOwner.ListInteraction list, ButtonGrid b, bool context) // [!code ++]
	{ // [!code ++]
		base.OnListInteraction(list, b, context); // [!code ++]
		if (!context) // [!code ++]
		{ // [!code ++]
			return; // [!code ++]
		} // [!code ++]
		list.Add("customBrightness", 300, delegate // [!code ++]
		{ // [!code ++]
			UIContextMenu uIContextMenu = EClass.ui.CreateContextMenuInteraction(); // [!code ++]
			uIContextMenu.AddSlider("brightness", (float a) => a.ToString() ?? "", EClass.player.customLightMod, delegate(float b) // [!code ++]
			{ // [!code ++]
				EClass.player.customLightMod = (int)b; // [!code ++]
				EClass.pc.RecalculateFOV(); // [!code ++]
			}, 1f, 6f, isInt: true, hideOther: false); // [!code ++]
			uIContextMenu.Show(); // [!code ++]
		}); // [!code ++]
	} // [!code ++]
}
```

## TraitTrolley

[`public override void OnActivateTrap(Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/db319d49229b570edb5b64522e88dbd0bac6d1c9/Elin/TraitTrolley.cs#L64-L77)
```cs:line-numbers=64

	public virtual string GetIdSound()
	{
		if (!(owner.id == "play_ride_kin")) // [!code --]
		switch (owner.id) // [!code ++]
		{
		case "play_ride_kin": // [!code ++]
			return "ride_kin"; // [!code ++]
		case "ride_boat": // [!code ++]
		case "ride_boat2": // [!code ++]
			return "ride_boat"; // [!code ++]
		case "play_ride_big": // [!code ++]
			if (owner.idSkin != 0) // [!code ++]
			{ // [!code ++]
				return "ride_trolley"; // [!code ++]
			} // [!code ++]
			return "ride_kin"; // [!code ++]
		default: // [!code ++]
			if (owner.idSkin != 7)
			{
				return "ride_trolley";
			}
			return "ride_bike";
		}
		return "ride_kin"; // [!code --]
	} // [!code ++]
 // [!code ++]
	public virtual bool ShouldPlaySound(Point p) // [!code ++]
	{ // [!code ++]
		string id = owner.id; // [!code ++]
		if (id == "ride_boat" || id == "ride_boat2") // [!code ++]
		{ // [!code ++]
			return p.cell.IsTopWaterAndNoSnow; // [!code ++]
		} // [!code ++]
		return true; // [!code ++]
	}
}
```
