---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 9 files modified.
version: EA 23.342.1 Nightly
changes: AI_Shear/ActEffect/Card/Chara/CharaBody/DNA/ELEMENT/TraitFoodEggFertilized/WindowChara
---

# EA 23.342.1 Nightly

September 4, 2026

9 files modified.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [Chara (1)](#chara)
```cs:no-line-numbers
public List<Element> ListAvailabeFeats(bool pet = false, bool showAll = false) // [!code --]
public List<Element> ListAvailabeFeats(bool pet = false, bool showAll = false, bool purchaseFeat = false) // [!code ++]
```
## AI_Shear

[`public static Thing GetFur(Chara c, int mod = 100)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/419e8d247654773a31fee3db8002bd688dade75a/Elin/AI_Shear.cs#L120-L142)
```cs:line-numbers=120
int furLv = GetFurLv(c);
string text = "fiber";
string idMat = "wool";
string text2 = c.id; // [!code --]
if (!(text2 == "putty_snow")) // [!code --]
switch (c.id) // [!code ++]
{
	if (text2 == "putty_snow_gold") // [!code --]
	{ // [!code --]
		idMat = "gold"; // [!code --]
	} // [!code --]
	else if (!c.Chara.race.fur.IsEmpty()) // [!code --]
case "putty_snow": // [!code ++]
	idMat = "cashmere"; // [!code ++]
	break; // [!code ++]
case "putty_snow_gold": // [!code ++]
	idMat = "gold"; // [!code ++]
	break; // [!code ++]
case "putty_forest": // [!code ++]
	text = "leaf_palulu"; // [!code ++]
	idMat = "grass"; // [!code ++]
	break; // [!code ++]
default: // [!code ++]
	if (!c.Chara.race.fur.IsEmpty()) // [!code ++]
	{
		string[] array = c.Chara.race.fur.Split('/');
		text = array[0];
		idMat = array[1];
	}
} // [!code --]
else // [!code --]
{ // [!code --]
	idMat = "cashmere"; // [!code --]
	break; // [!code ++]
}
Thing thing = ThingGen.Create(text, idMat);
int num = mod * furLv + furLv * furLv * 10;
```

## ActEffect

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/419e8d247654773a31fee3db8002bd688dade75a/Elin/ActEffect.cs#L2375-L2386)
```cs:line-numbers=2375
int num10 = 0;
foreach (Element item8 in list3)
{
	for (int num11 = 0; num11 < item8.vBase; num11++) // [!code --]
	int dNABonus = item8.GetDNABonus(EClass.pc); // [!code ++]
	if (dNABonus < item8.vBase) // [!code ++]
	{
		num10 += item8.source.cost.TryGet(num11); // [!code --]
		for (int num11 = 0; num11 < item8.vBase - dNABonus; num11++) // [!code ++]
		{ // [!code ++]
			num10 += item8.source.cost.TryGet(num11); // [!code ++]
		} // [!code ++]
		EClass.pc.SetFeat(item8.id, dNABonus); // [!code ++]
	}
	item8.vBase = 0; // [!code --]
	EClass.pc.SetFeat(item8.Value, 0); // [!code --]
}
EClass.pc.feat += num10;
EClass.pc.Refresh();
```

## Card

[`public Card SetLv(int a)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/419e8d247654773a31fee3db8002bd688dade75a/Elin/Card.cs#L3075-L3081)
```cs:line-numbers=3075
{
	return this;
}
isScaled = true; // [!code --]
if (!isScaled && flag && LV >= 50) // [!code ++]
{ // [!code ++]
	isScaled = true; // [!code ++]
} // [!code ++]
Rand.SetSeed(uid);
ElementContainer elementContainer = new ElementContainer();
elementContainer.ApplyElementMap(uid, SourceValueType.Chara, Chara.job.elementMap, LV);
```

[`public bool HaveFur()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/419e8d247654773a31fee3db8002bd688dade75a/Elin/Card.cs#L8410-L8421)
```cs:line-numbers=8410
	{
		return false;
	}
	string text = id; // [!code --]
	if (text == "putty_snow" || text == "putty_snow_gold") // [!code --]
	switch (id) // [!code ++]
	{
	case "putty_snow": // [!code ++]
	case "putty_snow_gold": // [!code ++]
	case "putty_forest": // [!code ++]
		return true;
	default: // [!code ++]
		return !Chara.race.fur.IsEmpty(); // [!code ++]
	}
	return !Chara.race.fur.IsEmpty(); // [!code --]
}

public bool CanBeSheared()
```

## Chara

[`public void GetRevived()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/419e8d247654773a31fee3db8002bd688dade75a/Elin/Chara.cs#L5444-L5450)
```cs:line-numbers=5444
		MoveZone(homeZone);
	}
}
else if (!EClass._zone.IsPCFaction || homeBranch != EClass.Branch || base.c_wasInPcParty) // [!code --]
else if ((!EClass._zone.IsPCFaction || homeBranch != EClass.Branch || base.c_wasInPcParty) && EClass._zone.events.GetEvent<ZoneEventWedding>() == null) // [!code ++]
{
	EClass.pc.party.AddMemeber(this, showMsg: true);
}
```

[`public void ModCorruption(int a)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/419e8d247654773a31fee3db8002bd688dade75a/Elin/Chara.cs#L10473-L10485)
```cs:line-numbers=10473
	corruption = num4 * 100 + corruption % 100;
}

public List<Element> ListAvailabeFeats(bool pet = false, bool showAll = false) // [!code --]
public List<Element> ListAvailabeFeats(bool pet = false, bool showAll = false, bool purchaseFeat = false) // [!code ++]
{
	List<Element> list = new List<Element>();
	foreach (SourceElement.Row item in EClass.sources.elements.rows.Where((SourceElement.Row a) => a.group == "FEAT" && a.cost[0] != -1 && !a.categorySub.IsEmpty()))
	{
		Feat feat = elements.GetOrCreateElement(item.id) as Feat;
		int num = ((feat.ValueWithoutLink <= 0) ? 1 : (feat.ValueWithoutLink + 1));
		if (purchaseFeat) // [!code ++]
		{ // [!code ++]
			num -= feat.GetDNABonus(this); // [!code ++]
		} // [!code ++]
		if (num <= feat.source.max && !feat.HasTag("class") && !feat.HasTag("hidden") && !feat.HasTag("innate") && (!pet || !feat.HasTag("noPet")) && (showAll || feat.IsPurchaseFeatReqMet(elements)))
		{
			list.Add(Element.Create(feat.id, num) as Feat);
```

[`public void SetFeat(int id, int value = 1, bool msg = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/419e8d247654773a31fee3db8002bd688dade75a/Elin/Chara.cs#L10492-L10497)
```cs:line-numbers=10492
{
	Feat feat = elements.GetElement(id) as Feat;
	int num = 0;
	if (feat == null) // [!code ++]
	{ // [!code ++]
		Debug.Log("1: null/" + EClass.sources.elements.map[id].id); // [!code ++]
	} // [!code ++]
	else // [!code ++]
	{ // [!code ++]
		Debug.Log("1:" + feat.Name + "/" + feat.Value); // [!code ++]
	} // [!code ++]
	if (feat != null && feat.Value > 0)
	{
		if (value == feat.Value)
```

[`public void SetFeat(int id, int value = 1, bool msg = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/419e8d247654773a31fee3db8002bd688dade75a/Elin/Chara.cs#L10502-L10507)
```cs:line-numbers=10502
	feat.Apply(-feat.Value, elements);
}
feat = elements.SetBase(id, value - (feat?.vSource ?? 0)) as Feat;
if (feat == null) // [!code ++]
{ // [!code ++]
	Debug.Log("1: null" + EClass.sources.elements.map[id].id); // [!code ++]
} // [!code ++]
else // [!code ++]
{ // [!code ++]
	Debug.Log("1:" + feat.Name + "/" + feat.Value); // [!code ++]
} // [!code ++]
if (feat != null && feat.Value != 0)
{
	feat.Apply(feat.Value, elements);
```

## CharaBody

[`public bool Equip(Thing thing, BodySlot slot = null, bool msg = true)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/419e8d247654773a31fee3db8002bd688dade75a/Elin/CharaBody.cs#L265-L271)
```cs:line-numbers=265
	thing.c_DNA.Apply(owner, reverse: false);
	Msg.Say("equip_relic");
	owner.feat -= thing.c_DNA.cost;
	thing.SetBool(135, enable: true); // [!code --]
	if (!EClass.debug.enable) // [!code ++]
	{ // [!code ++]
		thing.SetBool(135, enable: true); // [!code ++]
	} // [!code ++]
}
if (owner.IsPC)
{
```

## DNA

[`public int GetBodySlot()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/419e8d247654773a31fee3db8002bd688dade75a/Elin/DNA.cs#L745-L750)
```cs:line-numbers=745
	return -1;
}

public Element GetElement(int id) // [!code ++]
{ // [!code ++]
	for (int i = 0; i < vals.Count; i += 2) // [!code ++]
	{ // [!code ++]
		if (vals[i] == id) // [!code ++]
		{ // [!code ++]
			return Element.Create(vals[i], vals[i + 1]); // [!code ++]
		} // [!code ++]
	} // [!code ++]
	return null; // [!code ++]
} // [!code ++]
 // [!code ++]
public void ReplaceBodySlot(int slot)
{
	for (int i = 0; i < vals.Count; i += 2)
```

## ELEMENT

[`public bool IsFactionElement(Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/419e8d247654773a31fee3db8002bd688dade75a/Elin/ELEMENT.cs#L528-L533)
```cs:line-numbers=528
	return false;
}

public int GetDNABonus(Chara c) // [!code ++]
{ // [!code ++]
	int num = 0; // [!code ++]
	foreach (BodySlot slot in c.body.slots) // [!code ++]
	{ // [!code ++]
		if (slot.thing != null && slot.thing.c_DNA != null) // [!code ++]
		{ // [!code ++]
			Element element = slot.thing.c_DNA.GetElement(id); // [!code ++]
			if (element != null) // [!code ++]
			{ // [!code ++]
				num += element.Value; // [!code ++]
			} // [!code ++]
		} // [!code ++]
	} // [!code ++]
	if (c.c_genes != null) // [!code ++]
	{ // [!code ++]
		foreach (DNA item in c.c_genes.items) // [!code ++]
		{ // [!code ++]
			Element element2 = item.GetElement(id); // [!code ++]
			if (element2 != null) // [!code ++]
			{ // [!code ++]
				num += element2.Value; // [!code ++]
			} // [!code ++]
		} // [!code ++]
	} // [!code ++]
	return num; // [!code ++]
} // [!code ++]
 // [!code ++]
public Element GetParent(Card c)
{
	if (!source.aliasParent.IsEmpty())
```

## TraitFoodEggFertilized

[`public static Chara Incubate(Thing egg, Point pos, Card incubator = null)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/419e8d247654773a31fee3db8002bd688dade75a/Elin/TraitFoodEggFertilized.cs#L65-L70)
```cs:line-numbers=65
public static void MakeBaby(Chara c, int baby)
{
	c.SetFeat(1232, baby, msg: true);
	c.isScaled = true; // [!code ++]
	switch (c.id)
	{
	case "putty_snow":
```

## WindowChara

[`void List2(string lang, string idSubCat)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/419e8d247654773a31fee3db8002bd688dade75a/Elin/WindowChara.cs#L844-L850)
```cs:line-numbers=844
Dialog.YesNo("dialogBuyFeat".lang(a.CostLearn.ToString() ?? "", a.FullName), delegate
{
	EClass.pc.feat -= a.CostLearn;
	EClass.pc.SetFeat(a.id, a.Value, msg: true); // [!code --]
	EClass.pc.SetFeat(a.id, a.Value + a.GetDNABonus(EClass.pc), msg: true); // [!code ++]
	RefreshSkill(idTab);
	RefreshFeatMode();
	RefreshStatic();
```

[`void List2(string lang, string idSubCat)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/419e8d247654773a31fee3db8002bd688dade75a/Elin/WindowChara.cs#L858-L864)
```cs:line-numbers=858
},
onList = delegate
{
	foreach (Element item10 in chara.ListAvailabeFeats(pet: false, EClass.game.config.showAllFeat)) // [!code --]
	foreach (Element item10 in chara.ListAvailabeFeats(pet: false, EClass.game.config.showAllFeat, purchaseFeat: true)) // [!code ++]
	{
		if (item10.source.categorySub == idSubCat)
		{
```
