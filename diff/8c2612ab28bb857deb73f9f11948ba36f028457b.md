---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 11 files modified.
version: EA 23.320 Nyaightly Patch 1
changes: CharaRenderer/FortuneRollData/ListPeopleParty/Map/Quest/QuestRandom/TaskClean/Thing/ThingGen/Trait/TraitBrewery
---

# EA 23.320 Nyaightly Patch 1

June 22, 2026

11 files modified.

## Important Changes

**None.**
## CharaRenderer

[`public void DrawHat()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/CharaRenderer.cs#L343-L349)
```cs:line-numbers=343
		RenderObject.currentParam.x += 0.01f * (float)pref.equipX;
		RenderObject.currentParam.y += num + 0.01f * (float)(pref.equipY + cardRow.pref.equipY);
		RenderObject.currentParam.z += -0.09f;
		RenderObject.currentParam.tile = cardRow._tiles[owner.uid % cardRow._tiles.Length] * ((!flag) ? 1 : (-1)); // [!code --]
		RenderObject.currentParam.tile = cardRow._tiles[MathEx.IntIDOverflowTempFix(owner.uid) % cardRow._tiles.Length] * ((!flag) ? 1 : (-1)); // [!code ++]
		RenderObject.currentParam.mat = cardRow.DefaultMaterial;
		RenderObject.currentParam.matColor = cardRow.GetColorInt(RenderObject.currentParam.mat);
		cardRow.renderData.Draw(RenderObject.currentParam);
```

## FortuneRollData

[`public List<List<string>> GetPrizeList()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/FortuneRollData.cs#L184-L190)
```cs:line-numbers=184
		{
			"computer", "834", "1090", "goods_figure", "goods_canvas", "mb_1", "mb_2", "mb_3", "mb_4", "mb_5",
			"1174", "1085", "toilet", "714", "nobility", "plat", "1165", "mathammer", "medal", "bbq",
			"panty", "beehive", "ticket_resident", "lovepotion", "crystal_sun", "goods_crim", "ride_boat", "scrubber" // [!code --]
			"panty", "beehive", "ticket_resident", "lovepotion", "crystal_sun", "goods_crim", "ride_boat" // [!code ++]
		},
		new List<string>
		{
```

[`public List<List<string>> GetPrizeList()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/FortuneRollData.cs#L194-L201)
```cs:line-numbers=194
			"boat3",
			"medal",
			"ride_boat2",
			"play_ride_big", // [!code --]
			"scrubber" // [!code --]
			"play_ride_big" // [!code ++]
		}
	};
	if (EClass.pc.faction.IsGlobalPolicyActive(2712))
```

## ListPeopleParty

[`public override void OnRefreshMenu()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/ListPeopleParty.cs#L63-L69)
```cs:line-numbers=63
			foreach (int uid in setup.uids)
			{
				Chara chara3 = EClass.game.cards.globalCharas.Find(uid);
				if (chara3 != null && !chara3.IsPC && CanJoinParty(chara3)) // [!code --]
				if (chara3 != null && !chara3.IsPC && CanJoinParty(chara3) && chara3.IsPCFaction) // [!code ++]
				{
					JoinParty(chara3);
					if (chara3.uid == setup.ride)
```

[`public override void OnRefreshMenu()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/ListPeopleParty.cs#L108-L114)
```cs:line-numbers=108
						text = text + " " + "party_parasite".lang();
					}
				}
				n.note.AddText(text, (chara2 == null || chara2.isDead) ? FontColor.Bad : ((!CanJoinParty(chara2)) ? FontColor.Warning : FontColor.Good)); // [!code --]
				n.note.AddText(text, (chara2 == null || chara2.isDead || !chara2.IsPCFaction) ? FontColor.Bad : ((!CanJoinParty(chara2)) ? FontColor.Warning : FontColor.Good)); // [!code ++]
			}
			n.note.Build();
		});
```

## Map

[`public void TryShatter(Point pos, int ele, int power, Card CC = null)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/Map.cs#L1348-L1354)
```cs:line-numbers=1348
				List<SourceThing.Row> list3 = new List<SourceThing.Row>();
				foreach (RecipeSource item4 in RecipeManager.list)
				{
					if (!(item4.row is SourceThing.Row { isOrigin: false } row) || row.components.IsEmpty() || (row.components.Length >= 3 && !row.components[2].StartsWith('+')) || !row.Category.IsChildOf("meal") || (row.HasTag(CTAG.dish_fail) && power > EClass.rnd(500))) // [!code --]
					if (!(item4.row is SourceThing.Row { isOrigin: false, chance: not 0 } row) || row.components.IsEmpty() || (row.components.Length >= 3 && !row.components[2].StartsWith('+')) || !row.Category.IsChildOf("meal") || (row.HasTag(CTAG.dish_fail) && power > EClass.rnd(500))) // [!code ++]
					{
						continue;
					}
```

## Quest

[`public enum SubReward`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/Quest.cs#L51-L65)
```cs:line-numbers=51
	[JsonProperty]
	public int rewardMoney;

	[JsonProperty] // [!code --]
	public int bonusMoney; // [!code --]
 // [!code --]
	[JsonProperty]
	public int startDate;

	[JsonProperty]
	public int dangerLv;

	[JsonProperty] // [!code ++]
	public long bonusMoney; // [!code ++]
 // [!code ++]
	[JsonProperty]
	public bool isNew;

```

## QuestRandom

[`public class QuestRandom : Quest`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/QuestRandom.cs#L14-L20)
```cs:line-numbers=14

	public override void OnDropReward()
	{
		long num = (long)bonusMoney * (long)(55 + difficulty * 15) / 100; // [!code --]
		long num = bonusMoney * (55 + difficulty * 15) / 100; // [!code ++]
		int num2 = MathEx.ClampToInt(num + rewardMoney, 10000000);
		if (num2 > 0)
		{
```

## TaskClean

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/TaskClean.cs#L55-L61)
```cs:line-numbers=55
		num = ((num <= 0) ? 1 : (2 + num / 10));
		if (num > 1)
		{
			List<Point> list = EClass._map.ListPointsInSquare(dest, num - 1); // [!code --]
			List<Point> list = EClass._map.ListPointsInSquare(dest, num - 1, mustBeWalkable: false); // [!code ++]
			list.Sort((Point a, Point b) => a.Distance(dest) - b.Distance(dest));
			foreach (Point item in list)
			{
```

## Thing

[`public override string GetName(NameStyle style, int _num = -1)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/Thing.cs#L595-L602)
```cs:line-numbers=595
		});
	}
	text = idUnknown;
	goto IL_047d; // [!code --]
	IL_047d: // [!code --]
	goto IL_0499; // [!code ++]
	IL_0499: // [!code ++]
	if (GetBool(125))
	{
		text = "_halfeaten".lang(text);
```

[`public override string GetName(NameStyle style, int _num = -1)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/Thing.cs#L770-L776)
```cs:line-numbers=770
		}
		if (!(text11 == "*r"))
		{
			text = ((!source.name2.IsEmpty()) ? source.GetTextArray("name2")[0].Replace("#1b", base.c_extraNameRef.IsEmpty((base.refCard == null) ? text11 : (base.refCard.isChara ? base.refCard.GetName() : base.refCard.GetText()))).Replace("#1", base.c_extraNameRef.IsEmpty(text11)) : (source.naming.Contains("last") ? (text + Lang.space + text11) : (source.naming.Contains("first") ? (text11 + Lang.space + text) : ((!source.naming.Contains("of")) ? (text6.IsEmpty() ? "_of3" : "_of2").lang(text11, text) : "_of".lang(text11, text))))); // [!code --]
			text = ((!source.name2.IsEmpty()) ? source.GetTextArray("name2")[0].Replace("#1g", EClass.pc.faith.NameShort).Replace("#1b", base.c_extraNameRef.IsEmpty((base.refCard == null) ? text11 : (base.refCard.isChara ? base.refCard.GetName() : base.refCard.GetText()))).Replace("#1", base.c_extraNameRef.IsEmpty(text11)) : (source.naming.Contains("last") ? (text + Lang.space + text11) : (source.naming.Contains("first") ? (text11 + Lang.space + text) : ((!source.naming.Contains("of")) ? (text6.IsEmpty() ? "_of3" : "_of2").lang(text11, text) : "_of".lang(text11, text))))); // [!code ++]
		}
		else
		{
```

[`public override string GetName(NameStyle style, int _num = -1)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/Thing.cs#L781-L787)
```cs:line-numbers=781
				}
			}
		}
		goto IL_047d; // [!code --]
		goto IL_0499; // [!code ++]
	}

	public override string GetHoverText()
```

## ThingGen

[`public static void CreateTreasureContent(Thing t, int lv, TreasureType type, boo`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/ThingGen.cs#L288-L293)
```cs:line-numbers=288
		t.Add("money", EClass.rndHalf(1000 + num * 100));
		t.Add("money2", EClass.rndHalf(Mathf.Min(3 + num / 10, 10)));
		t.Add("medal", EClass.rnd(3));
		TryAddScrubber(25); // [!code ++]
		break;
	case TreasureType.BossNefia:
	case TreasureType.BossQuest:
```

[`public static void CreateTreasureContent(Thing t, int lv, TreasureType type, boo`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/ThingGen.cs#L307-L312)
```cs:line-numbers=307
			t.Add("book_skill", 1, lv);
		}
		t.c_lockLv /= 2;
		TryAddScrubber(5); // [!code ++]
		break;
	case TreasureType.SurvivalRaid:
		t.Add("plat", EClass.rndHalf(Mathf.Min(3 + num / 10, 15)));
```

[`public static void CreateTreasureContent(Thing t, int lv, TreasureType type, boo`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/ThingGen.cs#L315-L320)
```cs:line-numbers=315
		t.Add("book_skill", 1, lv);
		t.Add("book_skill", 1, lv);
		t.c_lockLv /= 3;
		TryAddScrubber(2); // [!code ++]
		break;
	case TreasureType.RandomChest:
		flag2 = false;
```

[`public static void CreateTreasureContent(Thing t, int lv, TreasureType type, boo`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/ThingGen.cs#L352-L357)
```cs:line-numbers=352
		{
			t.Add("medal");
		}
		TryAddScrubber(30); // [!code ++]
		break;
	}
	if (type == TreasureType.RandomChest && EClass.rnd(2) == 0)
```

[`void SetRarity(int mtp)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/ThingGen.cs#L412-L417)
```cs:line-numbers=412
				rarity = rarity
			});
		}
		void TryAddScrubber(int chance) // [!code ++]
		{ // [!code ++]
			if (EClass.rnd(chance) == 0) // [!code ++]
			{ // [!code ++]
				Thing thing2 = Create("scrubber"); // [!code ++]
				thing2.rarity = Rarity.Legendary; // [!code ++]
				thing2.elements.SetBase(770, 1 + EClass.rnd(EClass.rnd(Mathf.Min(10 + (int)Mathf.Sqrt(lv) * 2, 30) + 1))); // [!code ++]
				t.AddThing(thing2); // [!code ++]
			} // [!code ++]
		} // [!code ++]
	}

	public static void TryLickChest(Thing chest)
```

## Trait

[`public virtual void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/Trait.cs#L1705-L1713)
```cs:line-numbers=1705
	owner.c_dateStockExpire = EClass.world.date.GetRaw(24 * RestockDay);
	owner.isRestocking = true;
	t.things.DestroyAll((Thing _t) => _t.GetInt(101) != 0);
	foreach (Thing thing10 in t.things) // [!code --]
	foreach (Thing thing11 in t.things) // [!code ++]
	{
		thing10.invX = -1; // [!code --]
		thing11.invX = -1; // [!code ++]
	}
	switch (ShopType)
	{
```

[`public virtual void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/Trait.cs#L1724-L1771)
```cs:line-numbers=1724
		{
			break;
		}
		int num5 = 0; // [!code --]
		foreach (Thing thing11 in c_copyContainer.things) // [!code --]
		int num7 = 0; // [!code ++]
		foreach (Thing thing12 in c_copyContainer.things) // [!code ++]
		{
			if (!owner.trait.CanCopy(thing11)) // [!code --]
			if (!owner.trait.CanCopy(thing12)) // [!code ++]
			{
				continue;
			}
			Thing thing2 = thing11.Duplicate(1); // [!code --]
			thing2.isStolen = false; // [!code --]
			thing2.isCopy = true; // [!code --]
			thing2.c_priceFix = 0; // [!code --]
			foreach (Element item in thing2.elements.dict.Values.Where((Element e) => e.HasTag("noInherit")).ToList()) // [!code --]
			Thing thing6 = thing12.Duplicate(1); // [!code ++]
			thing6.isStolen = false; // [!code ++]
			thing6.isCopy = true; // [!code ++]
			thing6.c_priceFix = 0; // [!code ++]
			foreach (Element item in thing6.elements.dict.Values.Where((Element e) => e.HasTag("noInherit")).ToList()) // [!code ++]
			{
				thing2.elements.Remove(item.id); // [!code --]
				thing6.elements.Remove(item.id); // [!code ++]
			}
			int num6 = 1; // [!code --]
			int num8 = 1; // [!code ++]
			switch (owner.trait.CopyShop)
			{
			case CopyShopType.Item:
			{
				num6 = (1000 + owner.c_invest * 100) / (thing2.GetPrice(CurrencyType.Money, sell: false, PriceType.CopyShop) + 50); // [!code --]
				num8 = (1000 + owner.c_invest * 100) / (thing6.GetPrice(CurrencyType.Money, sell: false, PriceType.CopyShop) + 50); // [!code ++]
				int[] array = new int[3] { 704, 703, 702 };
				foreach (int ele in array)
				{
					if (thing2.HasElement(ele)) // [!code --]
					if (thing6.HasElement(ele)) // [!code ++]
					{
						num6 = 1; // [!code --]
						num8 = 1; // [!code ++]
					}
				}
				break;
			}
			case CopyShopType.Spellbook:
				thing2.c_charges = thing11.c_charges; // [!code --]
				thing6.c_charges = thing12.c_charges; // [!code ++]
				break;
			}
			if (num6 > 1 && thing2.trait.CanStack) // [!code --]
			if (num8 > 1 && thing6.trait.CanStack) // [!code ++]
			{
				thing2.SetNum(num6); // [!code --]
				thing6.SetNum(num8); // [!code ++]
			}
			AddThing(thing2); // [!code --]
			num5++; // [!code --]
			if (num5 > owner.trait.NumCopyItem) // [!code --]
			AddThing(thing6); // [!code ++]
			num7++; // [!code ++]
			if (num7 > owner.trait.NumCopyItem) // [!code ++]
			{
				break;
			}
```

[`public virtual void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/Trait.cs#L1820-L1826)
```cs:line-numbers=1820
		break;
	case ShopType.RedBook:
	{
		for (int num9 = 0; num9 < 30; num9++) // [!code --]
		for (int k = 0; k < 30; k++) // [!code ++]
		{
			AddThing(ThingGen.CreateFromFilter("shop_seeker"));
		}
```

[`public virtual void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/Trait.cs#L1858-L1863)
```cs:line-numbers=1858
		Add("block_kiwi", EClass.rndHalf(6), 0);
		Add("block_peach", EClass.rndHalf(6), 0);
		Add("water", 1, 0);
		if (EClass.rndSeed(EClass.debug.enable ? 1 : 3, seed) == 0) // [!code ++]
		{ // [!code ++]
			Thing thing5 = Add("scrubber", 1, 0); // [!code ++]
			thing5.rarity = Rarity.Legendary; // [!code ++]
			thing5.elements.SetBase(770, 1 + EClass.rndSeed(EClass.rndSeed(20, seed) + 1, seed)); // [!code ++]
		} // [!code ++]
		if (EClass.rndSeed(EClass.debug.enable ? 1 : 5, seed) == 0)
		{
			Add("hammer_garokk", 1, 0);
```

[`public virtual void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/Trait.cs#L1981-L1989)
```cs:line-numbers=1981
				break;
			case ShopType.Influence:
			{
				bool num7 = owner.id == "big_sister"; // [!code --]
				TraitTicketFurniture.SetZone(num7 ? EClass.game.spatials.Find("little_garden") : EClass._zone, Add("ticket_furniture", 1, 0).SetNum(99)); // [!code --]
				if (num7) // [!code --]
				bool num5 = owner.id == "big_sister"; // [!code ++]
				TraitTicketFurniture.SetZone(num5 ? EClass.game.spatials.Find("little_garden") : EClass._zone, Add("ticket_furniture", 1, 0).SetNum(99)); // [!code ++]
				if (num5) // [!code ++]
				{
					Add("littleball", 10, 0);
					if (!owner.Chara.affinity.CanGiveCard())
```

[`public virtual void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/Trait.cs#L2001-L2020)
```cs:line-numbers=2001
							owner.Chara.ModExp(287, 1000);
						}
					}
					Thing thing4 = CraftUtil.MakeLoveLunch(owner.Chara); // [!code --]
					thing4.elements.SetBase(1229, 1); // [!code --]
					AddThing(thing4); // [!code --]
					Thing thing3 = CraftUtil.MakeLoveLunch(owner.Chara); // [!code ++]
					thing3.elements.SetBase(1229, 1); // [!code ++]
					AddThing(thing3); // [!code ++]
					break;
				}
				for (int n = 0; n < 10; n++)
				{
					Thing thing5 = ThingGen.Create(EClass._zone.IsFestival ? "1123" : ((EClass.rnd(3) == 0) ? "1169" : "1160")); // [!code --]
					thing5.DyeRandom(); // [!code --]
					AddThing(thing5); // [!code --]
					Thing thing4 = ThingGen.Create(EClass._zone.IsFestival ? "1123" : ((EClass.rnd(3) == 0) ? "1169" : "1160")); // [!code ++]
					thing4.DyeRandom(); // [!code ++]
					AddThing(thing4); // [!code ++]
				}
				if (EClass._zone is Zone_Exile)
				{
					for (int num8 = 0; num8 < 30; num8++) // [!code --]
					for (int num6 = 0; num6 < 30; num6++) // [!code ++]
					{
						Add("1235", 1, -1);
						Add("1236", 1, -1);
```

[`public virtual void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/Trait.cs#L2057-L2066)
```cs:line-numbers=2057
				AddThing(ThingGen.CreatePerfume(9503, 5));
				for (int l = 0; l < 5; l++)
				{
					Thing thing3 = ThingGen.CreateFromCategory("seasoning").SetNum(10); // [!code --]
					thing3.elements.SetBase(2, 40); // [!code --]
					thing3.c_priceFix = 1000; // [!code --]
					AddThing(thing3); // [!code --]
					Thing thing2 = ThingGen.CreateFromCategory("seasoning").SetNum(10); // [!code ++]
					thing2.elements.SetBase(2, 40); // [!code ++]
					thing2.c_priceFix = 1000; // [!code ++]
					AddThing(thing2); // [!code ++]
				}
				break;
			}
```

[`public virtual void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/Trait.cs#L2180-L2188)
```cs:line-numbers=2180
					continue;
				}
				string[] array2 = item4.row.recipeKey;
				for (int k = 0; k < array2.Length; k++) // [!code --]
				for (int num9 = 0; num9 < array2.Length; num9++) // [!code ++]
				{
					if (array2[k] == text) // [!code --]
					if (array2[num9] == text) // [!code ++]
					{
						NoRestock(ThingGen.CreateRecipe(item4.id));
						break;
```

[`public virtual void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/Trait.cs#L2283-L2300)
```cs:line-numbers=2283
					{
						TreasureType treasureType = ((EClass.rnd(10) == 0) ? TreasureType.BossNefia : ((EClass.rnd(10) == 0) ? TreasureType.Map : TreasureType.RandomChest));
						int num13 = EClass.rnd(EClass.rnd(ShopLv + (EClass.debug.enable ? 200 : 50)) + 1) + 1;
						Thing thing6 = ThingGen.Create(treasureType switch // [!code --]
						Thing thing7 = ThingGen.Create(treasureType switch // [!code ++]
						{
							TreasureType.Map => "chest_treasure", 
							TreasureType.BossNefia => "chest_boss", 
							_ => "chest3", 
						});
						thing6.c_lockedHard = true; // [!code --]
						thing6.c_lockLv = num13; // [!code --]
						thing6.c_priceAdd = 2000 + num13 * 250 * ((treasureType == TreasureType.RandomChest) ? 1 : 5); // [!code --]
						thing6.c_revealLock = true; // [!code --]
						ThingGen.CreateTreasureContent(thing6, num13, treasureType, clearContent: true); // [!code --]
						AddThing(thing6); // [!code --]
						thing7.c_lockedHard = true; // [!code ++]
						thing7.c_lockLv = num13; // [!code ++]
						thing7.c_priceAdd = 2000 + num13 * 250 * ((treasureType == TreasureType.RandomChest) ? 1 : 5); // [!code ++]
						thing7.c_revealLock = true; // [!code ++]
						ThingGen.CreateTreasureContent(thing7, num13, treasureType, clearContent: true); // [!code ++]
						AddThing(thing7); // [!code ++]
					}
				}
				break;
```

[`public virtual void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/Trait.cs#L2356-L2408)
```cs:line-numbers=2356
				}
				AddThing(ThingGen.CreateScroll(8780, EClass.rndHalf(5)));
			}
			foreach (Thing thing12 in t.things) // [!code --]
			foreach (Thing thing13 in t.things) // [!code ++]
			{
				thing12.c_idBacker = 0; // [!code --]
				thing13.c_idBacker = 0; // [!code ++]
				if (ShopType != ShopType.Copy)
				{
					thing12.TryMakeRandomItem(ShopLv); // [!code --]
					if (thing12.Num == 1) // [!code --]
					thing13.TryMakeRandomItem(ShopLv); // [!code ++]
					if (thing13.Num == 1) // [!code ++]
					{
						thing12.SetNum(thing12.trait.DefaultStock); // [!code --]
						thing13.SetNum(thing13.trait.DefaultStock); // [!code ++]
					}
					if (thing12.trait is TraitFoodMeal) // [!code --]
					if (thing13.trait is TraitFoodMeal) // [!code ++]
					{
						CraftUtil.MakeDish(thing12, ShopLv, owner.Chara); // [!code --]
						CraftUtil.MakeDish(thing13, ShopLv, owner.Chara); // [!code ++]
					}
					if (thing12.IsFood && owner.id == "rodwyn") // [!code --]
					if (thing13.IsFood && owner.id == "rodwyn") // [!code ++]
					{
						SourceElement.Row row = EClass.sources.elements.rows.Where((SourceElement.Row e) => !e.foodEffect.IsEmpty() && e.category != "feat" && e.chance > 0).RandomItem(); // [!code --]
						thing12.elements.SetBase(row.id, 10 + EClass.rnd(10)); // [!code --]
						SourceElement.Row row = EClass.sources.elements.rows.Where((SourceElement.Row e) => !e.foodEffect.IsEmpty() && !e.tag.Contains("noRodwyn") && e.category != "feat" && e.chance > 0).RandomItem(); // [!code ++]
						thing13.elements.SetBase(row.id, 10 + EClass.rnd(10)); // [!code ++]
					}
				}
				if (CurrencyType == CurrencyType.Casino_coin)
				{
					thing12.noSell = true; // [!code --]
					thing13.noSell = true; // [!code ++]
				}
				if (Guild.Thief.IsCurrentZone)
				{
					thing12.isStolen = true; // [!code --]
					thing13.isStolen = true; // [!code ++]
				}
				if (!(thing12.trait is TraitErohon)) // [!code --]
				if (!(thing13.trait is TraitErohon)) // [!code ++]
				{
					thing12.c_IDTState = 0; // [!code --]
					thing13.c_IDTState = 0; // [!code ++]
				}
				if (CurrencyType == CurrencyType.Money && (thing12.category.IsChildOf("meal") || thing12.category.IsChildOf("preserved")) && thing12.id != "ration" && !thing12.IsUnique) // [!code --]
				if (CurrencyType == CurrencyType.Money && (thing13.category.IsChildOf("meal") || thing13.category.IsChildOf("preserved")) && thing13.id != "ration" && !thing13.IsUnique) // [!code ++]
				{
					thing12.c_priceFix = -70; // [!code --]
					thing13.c_priceFix = -70; // [!code ++]
				}
				if (ShopType == ShopType.TravelMerchant)
				{
					thing12.c_priceFix = 200; // [!code --]
					thing13.c_priceFix = 200; // [!code ++]
				}
				if (thing12.trait is TraitErohon) // [!code --]
				if (thing13.trait is TraitErohon) // [!code ++]
				{
					thing12.c_IDTState = 5; // [!code --]
					thing13.c_IDTState = 5; // [!code ++]
				}
				if (thing12.IsContainer && !thing12.c_revealLock) // [!code --]
				if (thing13.IsContainer && !thing13.c_revealLock) // [!code ++]
				{
					thing12.RemoveThings(); // [!code --]
					thing13.RemoveThings(); // [!code ++]
					t.c_lockLv = 0;
				}
			}
```

[`public virtual void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/Trait.cs#L2423-L2437)
```cs:line-numbers=2423
			Thing Add(string id, int a, int idSkin)
			{
				CardBlueprint.SetNormalRarity();
				Thing thing9 = ThingGen.Create(id, -1, ShopLv).SetNum(a); // [!code --]
				thing9.idSkin = ((idSkin == -1) ? EClass.rnd(thing9.source.skins.Length + 1) : idSkin); // [!code --]
				return t.AddThing(thing9); // [!code --]
				Thing thing10 = ThingGen.Create(id, -1, ShopLv).SetNum(a); // [!code ++]
				thing10.idSkin = ((idSkin == -1) ? EClass.rnd(thing10.source.skins.Length + 1) : idSkin); // [!code ++]
				return t.AddThing(thing10); // [!code ++]
			}
			void AddAdvWeek(int i)
			{
				Thing thing7 = ThingGen.CreateRedBook("advweek_" + i); // [!code --]
				thing7.c_priceFix = -90; // [!code --]
				AddThing(thing7); // [!code --]
				Thing thing8 = ThingGen.CreateRedBook("advweek_" + i); // [!code ++]
				thing8.c_priceFix = -90; // [!code ++]
				AddThing(thing8); // [!code ++]
			}
			void AddCassette(int idCas, string idQuest, int phase)
			{
```

[`void NoRestock(Thing _t)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/Trait.cs#L2466-L2474)
```cs:line-numbers=2466
				}
				void NoRestockId(string _id, int num, int idSkin)
				{
					Thing thing8 = ThingGen.Create(_id).SetNum(num); // [!code --]
					thing8.idSkin = idSkin; // [!code --]
					NoRestock(thing8); // [!code --]
					Thing thing9 = ThingGen.Create(_id).SetNum(num); // [!code ++]
					thing9.idSkin = idSkin; // [!code ++]
					NoRestock(thing9); // [!code ++]
				}
			}

```

[`public Thing CreateStock()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/Trait.cs#L2611-L2620)
```cs:line-numbers=2611
				}
				return FromFilter("shop_bread");
			case ShopType.Sweet:
				if (EClass.rnd(3) == 0) // [!code --]
				if (EClass.rnd(6) == 0) // [!code ++]
				{
					return Create("dough");
				}
				if (EClass.rnd(4) == 0) // [!code ++]
				{ // [!code ++]
					return Create("cream"); // [!code ++]
				} // [!code ++]
				return FromFilter("shop_sweet");
			case ShopType.Curry:
				if (EClass.rnd(3) == 0)
```

## TraitBrewery

[`public override bool OnChildDecay(Card c, bool firstDecay)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/8c2612ab28bb857deb73f9f11948ba36f028457b/Elin/TraitBrewery.cs#L49-L55)
```cs:line-numbers=49
		{
			return true;
		}
		if (!c.IsFood && !(c.trait is TraitDrinkMilk)) // [!code --]
		if (!c.IsFood && c.sourceCard._origin != "milk") // [!code ++]
		{
			return true;
		}
```
