---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 15 files modified.
version: EA 23.338.2 Stable
changes: ActEffect/BaseCondition/BaseSong/Card/Chara/Check/DNA/Dice/Party/SerializedCards/StanceSongPebble/StanceSongValor/TraitCrafter/Widget/Zone_WindRest
---

# EA 23.338.2 Stable

August 24, 2026

15 files modified.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [Dice (6)](#dice)
```cs:no-line-numbers
public static int Roll_Normal(long num, long sides, int bonus = 0) // [!code --]
public static long Roll_Normal(long num, long sides, int bonus = 0) // [!code ++]
```
```cs:no-line-numbers
public static int Roll_Precise(int num, int sides, int bonus = 0) // [!code --]
public static long Roll_Precise(int num, int sides, int bonus = 0) // [!code ++]
```
```cs:no-line-numbers
public static int Roll(int num, int sides, int bonus = 0, Card card = null) // [!code --]
public static long Roll(int num, int sides, int bonus = 0, Card card = null) // [!code ++]
```
```cs:no-line-numbers
public static int RollMax(int num, int sides, int bonus = 0) // [!code --]
public static long RollMax(int num, int sides, int bonus = 0) // [!code ++]
```
```cs:no-line-numbers
public int Roll() // [!code --]
public long Roll() // [!code ++]
```
```cs:no-line-numbers
public int RollMax() // [!code --]
public long RollMax() // [!code ++]
```
### [StanceSongPebble (1)](#stancesongpebble)
```cs:no-line-numbers
public override void TickSong() // [!code --]

```
### [StanceSongValor (1)](#stancesongvalor)
```cs:no-line-numbers
public override void TickSong() // [!code --]

```
## ActEffect

[`public static bool DamageEle(Card CC, EffectId id, int power, Element e, List<Po…)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/ActEffect.cs#L368-L377)
```cs:line-numbers=368
	num4 = num4 * 100 / (100 + RapidCount * 50);
}
num4 = num4 * Act.powerMod / 100;
if (num4 > 99999999) // [!code --]
{ // [!code --]
	num4 = 99999999L; // [!code --]
} // [!code --]
c.DamageHP(num4, e.id, power * num / 100, attackSource, chara ?? CC);
if (c.IsAliveInCurrentZone)
{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/ActEffect.cs#L1229-L1235)
```cs:line-numbers=1229
	EClass.game.religions.Element.Talk("ability");
}
Dice dice = Dice.Create("ActManaAbsorb", power, CC, (actRef.refThing != null) ? null : actRef.act);
TC.mana.Mod(dice.Roll() + TC.mana.max / 5); // [!code --]
TC.mana.Mod(MathEx.ClampToInt(dice.Roll() + TC.mana.max / 5)); // [!code ++]
TC.PlaySound("heal");
TC.PlayEffect("heal");
if (TC == CC)
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/ActEffect.cs#L1599-L1605)
```cs:line-numbers=1599
EClass.ui.AddLayer<LayerList>().SetStringList(delegate
{
	List<string> list12 = new List<string>();
	for (int num21 = 0; num21 < 10; num21++) // [!code --]
	for (int num20 = 0; num20 < 10; num20++) // [!code ++]
	{
		list12.Add(AliasGen.GetRandomAlias());
	}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/ActEffect.cs#L1840-L1847)
```cs:line-numbers=1840
			{
				effect.Play(center, 0f, pos);
			});
			int num16 = Dice.Create("SpShutterHex", power, CC, (actRef.refThing != null) ? null : actRef.act).Roll(); // [!code --]
			item6.DamageHP(num16, 919, power, AttackSource.None, CC, showEffect: false); // [!code --]
			long dmg = Dice.Create("SpShutterHex", power, CC, (actRef.refThing != null) ? null : actRef.act).Roll(); // [!code ++]
			item6.DamageHP(dmg, 919, power, AttackSource.None, CC, showEffect: false); // [!code ++]
		}
	}
}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/ActEffect.cs#L2006-L2031)
```cs:line-numbers=2006
List<Thing> list = new List<Thing>();
TC.things.Foreach(delegate(Thing t)
{
	int num20 = 0; // [!code --]
	int num19 = 0; // [!code ++]
	if ((t.isEquipped || t.IsRangedWeapon || blessed) && t.blessedState < BlessedState.Normal)
	{
		if (t.blessedState == BlessedState.Cursed)
		{
			num20 = EClass.rnd(200); // [!code --]
			num19 = EClass.rnd(200); // [!code ++]
		}
		if (t.blessedState == BlessedState.Doomed)
		{
			num20 = EClass.rnd(1000); // [!code --]
			num19 = EClass.rnd(1000); // [!code ++]
		}
		if (blessed)
		{
			num20 /= 2; // [!code --]
			num19 /= 2; // [!code ++]
		}
		if (id == EffectId.UncurseEQGreater)
		{
			num20 /= 10; // [!code --]
			num19 /= 10; // [!code ++]
		}
		if (power >= num20) // [!code --]
		if (power >= num19) // [!code ++]
		{
			TC.Say("uncurseEQ_success", t);
			t.SetBlessedState(BlessedState.Normal);
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/ActEffect.cs#L2630-L2637)
```cs:line-numbers=2630
if (TC.HasElement(1211))
{
	TC.Say("drinkSaltWater_snail", TC);
	int num19 = ((TC.hp > 10) ? (TC.hp - EClass.rnd(10)) : 10000); // [!code --]
	TC.DamageHP(num19, AttackSource.None, CC); // [!code --]
	int num18 = ((TC.hp > 10) ? (TC.hp - EClass.rnd(10)) : 10000); // [!code ++]
	TC.DamageHP(num18, AttackSource.None, CC); // [!code ++]
}
else if (TC.IsPC)
{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/ActEffect.cs#L2758-L2783)
```cs:line-numbers=2758
{
	TC.PlayEffect("buff");
	TC.PlaySound("offering");
	int num17 = TC.Evalue(1423); // [!code --]
	if (num17 > 0) // [!code --]
	int num16 = TC.Evalue(1423); // [!code ++]
	if (num16 > 0) // [!code ++]
	{
		int num18 = 0; // [!code --]
		if (flag && num17 > 1) // [!code --]
		int num17 = 0; // [!code ++]
		if (flag && num16 > 1) // [!code ++]
		{
			num18 = -1; // [!code --]
			num17 = -1; // [!code ++]
		}
		else if (num17 < 15) // [!code --]
		else if (num16 < 15) // [!code ++]
		{
			num18 = ((!blessed) ? 1 : 2); // [!code --]
			if (num18 + num17 > 15) // [!code --]
			num17 = ((!blessed) ? 1 : 2); // [!code ++]
			if (num17 + num16 > 15) // [!code ++]
			{
				num18 = 1; // [!code --]
				num17 = 1; // [!code ++]
			}
		}
		if (num18 != 0) // [!code --]
		if (num17 != 0) // [!code ++]
		{
			TC.SetFeat(1423, num17 + num18, msg: true); // [!code --]
			TC.SetInt(134, TC.GetInt(134) + num18); // [!code --]
			TC.SetFeat(1423, num16 + num17, msg: true); // [!code ++]
			TC.SetInt(134, TC.GetInt(134) + num17); // [!code ++]
		}
	}
}
```

## BaseCondition

[`public virtual void SetOwner(Chara _owner, bool onDeserialize = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/BaseCondition.cs#L216-L239)
```cs:line-numbers=216
	owner = _owner;
	phase = GetPhase();
	RefreshEmoIcon();
	if (UseElements) // [!code --]
	{ // [!code --]
		elements = new ElementContainerCondition(); // [!code --]
		if (base.source.elements.Length != 0) // [!code --]
		{ // [!code --]
			for (int i = 0; i < base.source.elements.Length; i += 2) // [!code --]
			{ // [!code --]
				elements.SetBase(GetElementSource(i).id, base.source.elements[i + 1].Calc(power, 0, P2)); // [!code --]
			} // [!code --]
		} // [!code --]
		elements.SetParent(owner); // [!code --]
	} // [!code --]
	RefreshElements(); // [!code ++]
	if (onDeserialize)
	{
		OnChangePhase(phase, phase);
	}
}

public void RefreshElements() // [!code ++]
{ // [!code ++]
	if (!UseElements) // [!code ++]
	{ // [!code ++]
		return; // [!code ++]
	} // [!code ++]
	if (elements != null) // [!code ++]
	{ // [!code ++]
		elements.SetParent(); // [!code ++]
	} // [!code ++]
	elements = new ElementContainerCondition(); // [!code ++]
	if (base.source.elements.Length != 0) // [!code ++]
	{ // [!code ++]
		for (int i = 0; i < base.source.elements.Length; i += 2) // [!code ++]
		{ // [!code ++]
			elements.SetBase(GetElementSource(i).id, base.source.elements[i + 1].Calc(power, 0, P2)); // [!code ++]
		} // [!code ++]
	} // [!code ++]
	elements.SetParent(owner); // [!code ++]
} // [!code ++]
 // [!code ++]
public void RefreshEmoIcon()
{
	if (EmoIcon > owner.emoIcon)
```

## BaseSong

[`public virtual void TickSong()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/BaseSong.cs#L29-L54)
```cs:line-numbers=29
	{
	}
}
public class BaseSong<T> : BaseSong where T : Condition // [!code ++]
{ // [!code ++]
	public virtual int BaseDuration => 10; // [!code ++]
 // [!code ++]
	public override void TickSong() // [!code ++]
	{ // [!code ++]
		foreach (Chara item in owner.pos.ListCharasInRadius(owner, 4, (Chara c) => !c.IsDeadOrSleeping && ((!owner.IsPCFactionOrMinion) ? (!c.IsHostile(owner)) : c.IsPCFactionOrMinion))) // [!code ++]
		{ // [!code ++]
			T condition = item.GetCondition<T>(); // [!code ++]
			if (condition == null) // [!code ++]
			{ // [!code ++]
				item.AddCondition<T>(base.power); // [!code ++]
				continue; // [!code ++]
			} // [!code ++]
			if (condition.power != base.power) // [!code ++]
			{ // [!code ++]
				condition.power = base.power; // [!code ++]
				condition.RefreshElements(); // [!code ++]
			} // [!code ++]
			condition.SetValue(BaseDuration + owner.Evalue(1294) * 2, owner.Evalue(1294)); // [!code ++]
		} // [!code ++]
	} // [!code ++]
} // [!code ++]
```

## Card

[`public virtual long ApplyProtection(long dmg, int mod = 100)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/Card.cs#L4231-L4237)
```cs:line-numbers=4231
	bonus = 0;
	dmg = dmg * 100 / Mathf.Max(100 + num, 1);
}
int num4 = Dice.Roll(num2, sides, bonus, this); // [!code --]
long num4 = Dice.Roll(num2, sides, bonus, this); // [!code ++]
dmg -= num4 * mod / 100;
if (dmg < 0)
{
```

## Chara

[`void ForeachEnemy(Action<Chara> action)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/Chara.cs#L6418-L6435)
```cs:line-numbers=6418
{
	if (_pts.Count == 0)
	{
		for (int num10 = EClass._map.charas.Count - 1; num10 >= 0; num10--) // [!code --]
		for (int num11 = EClass._map.charas.Count - 1; num11 >= 0; num11--) // [!code ++]
		{
			Chara chara3 = EClass._map.charas[num10]; // [!code --]
			Chara chara3 = EClass._map.charas[num11]; // [!code ++]
			if (chara3 != this && CanSeeLos(chara3) && chara3.IsHostile(this))
			{
				_pts.Add(chara3);
			}
		}
	}
	for (int num11 = _pts.Count - 1; num11 >= 0; num11--) // [!code --]
	for (int num12 = _pts.Count - 1; num12 >= 0; num12--) // [!code ++]
	{
		action(_pts[num11]); // [!code --]
		action(_pts[num12]); // [!code ++]
	}
}
void ForeachParty(Action<Chara> action)
```

[`void ForeachParty(Action<Chara> action)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/Chara.cs#L6459-L6467)
```cs:line-numbers=6459
				}
			}
		}
		for (int num9 = _pts.Count - 1; num9 >= 0; num9--) // [!code --]
		int num9 = _pts.IndexOf(this); // [!code ++]
		if (num9 != -1) // [!code ++]
		{
			action(_pts[num9]); // [!code --]
			_pts.RemoveAt(num9); // [!code ++]
			_pts.Insert(0, this); // [!code ++]
		} // [!code ++]
		for (int num10 = _pts.Count - 1; num10 >= 0; num10--) // [!code ++]
		{ // [!code ++]
			action(_pts[num10]); // [!code ++]
		}
	}
}
```

[`public void Cuddle(Chara c, bool headpat = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/Chara.cs#L6585-L6591)
```cs:line-numbers=6585
}
if (faith == EClass.game.religions.MoonShadow && c.IsPCParty)
{
	foreach (Chara member in party.members) // [!code --]
	foreach (Chara member in c.party.members) // [!code ++]
	{
		if (!member.IsPC && CanSeeLos(member))
		{
```

## Check

[`public int GetFinalDC(Chara p, Card tg = null)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/Check.cs#L111-L132)
```cs:line-numbers=111
public Result Perform(Chara p, Card tg = null)
{
	int finalDC = GetFinalDC(p, tg);
	int num = Dice.Roll(1, 20); // [!code --]
	long num = Dice.Roll(1, 20); // [!code ++]
	if (EClass.debug.logDice)
	{
		string text = "Check:" + source.id + " dc=" + finalDC + " roll=" + num + "  ";
		Debug.Log(num switch
		{
			20 => text + "CriticalPass",  // [!code --]
			1 => text + "CriticalFail",  // [!code --]
			20L => text + "CriticalPass",  // [!code ++]
			1L => text + "CriticalFail",  // [!code ++]
			_ => (num < finalDC) ? (text + "Fail") : (text + "Pass"), 
		});
	}
	switch (num)
	{
	case 20: // [!code --]
	case 20L: // [!code ++]
		return Result.CriticalPass;
	case 1: // [!code --]
	case 1L: // [!code ++]
		return Result.CriticalFail;
	default:
		if (num >= finalDC)
```

## DNA

[`public void Apply(Chara c, bool reverse)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/DNA.cs#L229-L241)
```cs:line-numbers=229
	num3 = 0;
}
c.SetFeat(num, num3, !reverse);
if (reverse && num == 1423) // [!code --]
if (reverse && num == 1423 && c.GetInt(134) > 0) // [!code ++]
{
	int @int = c.GetInt(134); // [!code --]
	if (@int > 0) // [!code --]
	int num4 = c.elements.ValueWithoutLink(num) - c.GetInt(134); // [!code ++]
	if (num4 < 0) // [!code ++]
	{
		c.SetFeat(num, -@int); // [!code --]
		num4 = 0; // [!code ++]
	}
	c.SetFeat(num, num4); // [!code ++]
	c.SetInt(134);
}
break;
```

## Dice

[`public class Dice`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/Dice.cs#L16-L55)
```cs:line-numbers=16
public static int MaxValue => 214748364;

public static int Roll_Normal(long num, long sides, int bonus = 0) // [!code --]
public static long Roll_Normal(long num, long sides, int bonus = 0) // [!code ++]
{
	double num2 = (double)(num * (sides + 1)) / 2.0; // [!code --]
	double num3 = Math.Sqrt((double)(num * (sides * sides - 1)) / 12.0); // [!code --]
	double num2 = num; // [!code ++]
	double num3 = sides; // [!code ++]
	double num4 = num2 * (num3 + 1.0) / 2.0; // [!code ++]
	double num5 = Math.Sqrt(num2 * (num3 * num3 - 1.0) / 12.0); // [!code ++]
	double d = (double)(rnd(1000000) + 1) / 1000001.0;
	double num4 = (double)(rnd(1000000) + 1) / 1000001.0; // [!code --]
	double num5 = Math.Sqrt(-2.0 * Math.Log(d)) * Math.Cos(Math.PI * 2.0 * num4); // [!code --]
	long num6 = (long)Math.Floor(num2 + num3 * num5 + 0.5); // [!code --]
	long num7 = num * sides; // [!code --]
	if (num6 < num) // [!code --]
	double num6 = (double)(rnd(1000000) + 1) / 1000001.0; // [!code ++]
	double num7 = Math.Sqrt(-2.0 * Math.Log(d)) * Math.Cos(Math.PI * 2.0 * num6); // [!code ++]
	double num8 = Math.Floor(num4 + num5 * num7 + 0.5); // [!code ++]
	long num9 = ((sides != 0L && num > long.MaxValue / sides) ? long.MaxValue : (num * sides)); // [!code ++]
	if (num8 < (double)num) // [!code ++]
	{
		num6 = num; // [!code --]
		num8 = num; // [!code ++]
	}
	if (num6 > num7) // [!code --]
	if (num8 > (double)num9) // [!code ++]
	{
		num6 = num7; // [!code --]
		num8 = num9; // [!code ++]
	}
	return (int)Mathf.Clamp(num6 + bonus, -MaxValue, MaxValue); // [!code --]
	long num10 = (long)num8; // [!code ++]
	if (bonus > 0 && num10 > long.MaxValue - bonus) // [!code ++]
	{ // [!code ++]
		return long.MaxValue; // [!code ++]
	} // [!code ++]
	if (bonus < 0 && num10 < long.MinValue - bonus) // [!code ++]
	{ // [!code ++]
		return long.MinValue; // [!code ++]
	} // [!code ++]
	return num10 + bonus; // [!code ++]
}

public static int Roll_Precise(int num, int sides, int bonus = 0) // [!code --]
public static long Roll_Precise(int num, int sides, int bonus = 0) // [!code ++]
{
	long num2 = 0L;
	for (int i = 0; i < num; i++)
	{
		num2 += rnd(sides) + 1;
	}
	return (int)Mathf.Clamp(num2 + bonus, -MaxValue, MaxValue); // [!code --]
	return num2 + bonus; // [!code ++]
}

public static int Roll(int num, int sides, int bonus = 0, Card card = null) // [!code --]
public static long Roll(int num, int sides, int bonus = 0, Card card = null) // [!code ++]
{
	int a = 1;
	bool flag = true;
	int num2 = 0; // [!code --]
	long num2 = 0L; // [!code ++]
	if (card != null)
	{
		int num3 = card.Evalue(78);
```

[`public static int Roll(int num, int sides, int bonus = 0, Card card = null)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/Dice.cs#L58-L64)
```cs:line-numbers=58
}
for (int i = 0; i < Mathf.Min(a, 20); i++)
{
	int num4 = ((num >= 10) ? Roll_Normal(num, sides, bonus) : Roll_Precise(num, sides, bonus)); // [!code --]
	long num4 = ((num >= 10) ? Roll_Normal(num, sides, bonus) : Roll_Precise(num, sides, bonus)); // [!code ++]
	if (i == 0 || (flag && num4 > num2) || (!flag && num4 < num2))
	{
		num2 = num4;
```

[`public static int Roll(int num, int sides, int bonus = 0, Card card = null)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/Dice.cs#L67-L75)
```cs:line-numbers=67
	return num2;
}

public static int RollMax(int num, int sides, int bonus = 0) // [!code --]
public static long RollMax(int num, int sides, int bonus = 0) // [!code ++]
{
	return (int)Mathf.Clamp((long)num * (long)sides + bonus, -MaxValue, MaxValue); // [!code --]
	return (long)num * (long)sides + bonus; // [!code ++]
}

public static int rnd(int a)
```

[`public static Dice Parse(string raw)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/Dice.cs#L102-L113)
```cs:line-numbers=102
	return dice;
}

public int Roll() // [!code --]
public long Roll() // [!code ++]
{
	return Roll(num, sides, bonus, card);
}

public int RollMax() // [!code --]
public long RollMax() // [!code ++]
{
	return RollMax(num, sides, bonus);
}
```

## Party

[`public List<Chara> SetMembers()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/Party.cs#L47-L54)
```cs:line-numbers=47
{
	if (!hashSet.Contains(uidMember))
	{
		hashSet.Add(uidMember); // [!code --]
		members.Add(RefChara.Get(uidMember)); // [!code --]
		Chara chara = RefChara.Get(uidMember); // [!code ++]
		if (chara != null) // [!code ++]
		{ // [!code ++]
			hashSet.Add(uidMember); // [!code ++]
			members.Add(chara); // [!code ++]
		} // [!code ++]
	}
}
return _members;
```

## SerializedCards

[`public void Restore(Map map, Map orgMap, bool addToZone, PartialMap partial = nu…)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/SerializedCards.cs#L736-L742)
```cs:line-numbers=736
}
if (source.isOrigin)
{
	text = SpawnListThing.Get("origin_" + text, (SourceThing.Row a) => a.origin == source).GetFirst().id; // [!code --]
	SpawnList spawnList = SpawnListThing.Get("origin_" + text, (SourceThing.Row a) => a.origin == source); // [!code ++]
	if (spawnList.rows.Count == 0) // [!code ++]
	{ // [!code ++]
		continue; // [!code ++]
	} // [!code ++]
	text = spawnList.GetFirst().id; // [!code ++]
}
card2 = ThingGen.Create(text, -1, EClass._zone.DangerLv);
card2.ChangeMaterial((card4.idMat == -1) ? card2.DefaultMaterial.id : card4.idMat);
```

## StanceSongPebble

[`public class StanceSongPebble : BaseSong`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/StanceSongPebble.cs#L1-L19)
```cs:line-numbers=1
public class StanceSongPebble : BaseSong // [!code --]
public class StanceSongPebble : BaseSong<ConSongPebble> // [!code ++]
{
	public override int IdAbility => 6754;
 // [!code --]
	public override void TickSong() // [!code --]
	{ // [!code --]
		foreach (Chara item in owner.pos.ListCharasInRadius(owner, 4, (Chara c) => !c.IsDeadOrSleeping && ((!owner.IsPCFactionOrMinion) ? (!c.IsHostile(owner)) : c.IsPCFactionOrMinion))) // [!code --]
		{ // [!code --]
			if (!item.HasCondition<ConSongPebble>()) // [!code --]
			{ // [!code --]
				item.AddCondition<ConSongPebble>(base.power, owner.Evalue(1294)); // [!code --]
			} // [!code --]
			else // [!code --]
			{ // [!code --]
				item.GetCondition<ConSongPebble>()?.SetValue(10 + owner.Evalue(1294) * 2, owner.Evalue(1294)); // [!code --]
			} // [!code --]
		} // [!code --]
	} // [!code --]
}
```

## StanceSongValor

[`public class StanceSongValor : BaseSong`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/StanceSongValor.cs#L1-L19)
```cs:line-numbers=1
public class StanceSongValor : BaseSong // [!code --]
public class StanceSongValor : BaseSong<ConSongValor> // [!code ++]
{
	public override int IdAbility => 6752;
 // [!code --]
	public override void TickSong() // [!code --]
	{ // [!code --]
		foreach (Chara item in owner.pos.ListCharasInRadius(owner, 4, (Chara c) => !c.IsDeadOrSleeping && ((!owner.IsPCFactionOrMinion) ? (!c.IsHostile(owner)) : c.IsPCFactionOrMinion))) // [!code --]
		{ // [!code --]
			if (!item.HasCondition<ConSongValor>()) // [!code --]
			{ // [!code --]
				item.AddCondition<ConSongValor>(base.power); // [!code --]
			} // [!code --]
			else // [!code --]
			{ // [!code --]
				item.GetCondition<ConSongValor>()?.SetValue(10 + owner.Evalue(1294) * 2, owner.Evalue(1294)); // [!code --]
			} // [!code --]
		} // [!code --]
	} // [!code --]
}
```

## TraitCrafter

[`public bool IsIngredient(int idx, SourceRecipe.Row r, Card c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/TraitCrafter.cs#L191-L199)
```cs:line-numbers=191
}
if (text == "any")
{
	if (this is TraitDyeMaker && !c.category.GetRoot().tag.Contains("dye") && !c.category.tag.Contains("dye")) // [!code --]
	if (this is TraitDyeMaker) // [!code ++]
	{
		return false; // [!code --]
		if (c.id == "dye") // [!code ++]
		{ // [!code ++]
			return false; // [!code ++]
		} // [!code ++]
		if (!c.category.GetRoot().tag.Contains("dye") && !c.category.tag.Contains("dye")) // [!code ++]
		{ // [!code ++]
			return false; // [!code ++]
		} // [!code ++]
	}
	if (!c.IsUnique && !c.IsImportant && !c.trait.CanOnlyCarry)
	{
```

## Widget

[`public void Reactivate()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/Widget.cs#L330-L356)
```cs:line-numbers=330
public virtual void OnManagerActivate()
{
	goCover = Util.Instantiate("UI/Widget/CoverWidget", base.transform).gameObject; // [!code --]
	RectTransform rectTransform = goCover.transform.Rect(); // [!code --]
	RectTransform rectTransform2 = dragPanel.Rect(); // [!code --]
	if ((bool)rectTransform && (bool)rectTransform2) // [!code --]
	{ // [!code --]
		rectTransform.pivot = rectTransform2.pivot; // [!code --]
		rectTransform.anchorMin = rectTransform2.anchorMin; // [!code --]
		rectTransform.anchorMax = rectTransform2.anchorMax; // [!code --]
		rectTransform.anchoredPosition = rectTransform2.anchoredPosition; // [!code --]
		rectTransform.sizeDelta = rectTransform2.sizeDelta; // [!code --]
	} // [!code --]
	UIDragPanel componentInChildren = goCover.GetComponentInChildren<UIDragPanel>(); // [!code --]
	componentInChildren.autoAnchor = config.userAnchor == RectPosition.Auto; // [!code --]
	componentInChildren.onDrag = OnChangePosition; // [!code --]
	componentInChildren.container = dragPanel.container; // [!code --]
	componentInChildren.target = dragPanel.target; // [!code --]
	componentInChildren.bound = dragPanel.bound; // [!code --]
	if (this is WidgetSideScreen) // [!code --]
	{ // [!code --]
		componentInChildren.enable = false; // [!code --]
	if (!(this == null) && (bool)this) // [!code ++]
	{ // [!code ++]
		goCover = Util.Instantiate("UI/Widget/CoverWidget", base.transform).gameObject; // [!code ++]
		RectTransform rectTransform = goCover.transform.Rect(); // [!code ++]
		RectTransform rectTransform2 = dragPanel.Rect(); // [!code ++]
		if ((bool)rectTransform && (bool)rectTransform2) // [!code ++]
		{ // [!code ++]
			rectTransform.pivot = rectTransform2.pivot; // [!code ++]
			rectTransform.anchorMin = rectTransform2.anchorMin; // [!code ++]
			rectTransform.anchorMax = rectTransform2.anchorMax; // [!code ++]
			rectTransform.anchoredPosition = rectTransform2.anchoredPosition; // [!code ++]
			rectTransform.sizeDelta = rectTransform2.sizeDelta; // [!code ++]
		} // [!code ++]
		UIDragPanel componentInChildren = goCover.GetComponentInChildren<UIDragPanel>(); // [!code ++]
		componentInChildren.autoAnchor = config.userAnchor == RectPosition.Auto; // [!code ++]
		componentInChildren.onDrag = OnChangePosition; // [!code ++]
		componentInChildren.container = dragPanel.container; // [!code ++]
		componentInChildren.target = dragPanel.target; // [!code ++]
		componentInChildren.bound = dragPanel.bound; // [!code ++]
		if (this is WidgetSideScreen) // [!code ++]
		{ // [!code ++]
			componentInChildren.enable = false; // [!code ++]
		} // [!code ++]
		goCover.GetComponentInChildren<UIText>().SetText(("Widget" + ID).lang()); // [!code ++]
	}
	goCover.GetComponentInChildren<UIText>().SetText(("Widget" + ID).lang()); // [!code --]
}

public virtual void OnManagerDeactivate()
```

## Zone_WindRest

[`public override void OnAfterSimulate()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/35fac67c8cd3adbc145f37a55b0f04b1347dbb73/Elin/Zone_WindRest.cs#L43-L48)
```cs:line-numbers=43
	TryRevive("eureka", 60, 61);
	TryRevive("billy", 35, 64);
}
List<Thing> list = EClass._map.ListThing<TraitDaggerGrave>(); // [!code ++]
foreach (Thing item in list) // [!code ++]
{ // [!code ++]
	if (item.pos.x != 52 || item.pos.z != 46) // [!code ++]
	{ // [!code ++]
		continue; // [!code ++]
	} // [!code ++]
	{ // [!code ++]
		foreach (Thing item2 in list) // [!code ++]
		{ // [!code ++]
			if (item2.c_idBacker == 1239) // [!code ++]
			{ // [!code ++]
				item.MoveImmediate(item2.pos); // [!code ++]
				item2.MoveImmediate(new Point(52, 46)); // [!code ++]
				break; // [!code ++]
			} // [!code ++]
		} // [!code ++]
		break; // [!code ++]
	} // [!code ++]
} // [!code ++]
static void TryRevive(string id, int x, int y)
{
	if (EClass.game.cards.globalCharas.Find(id) == null)
```
