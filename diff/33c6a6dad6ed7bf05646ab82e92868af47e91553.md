---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 12 files modified. 1 new file created.
version: +EA 23.312 Nightly Patch 4 - Plugin.BaseCore
changes: BaseModManager/BaseModPackage/ClassCache/ERROR/ExcelDataList/ExcelParser/+GodTalkDataList/IO/Lang/MOD/SourceData/TalkDataList/ToneDataList
---

# +EA 23.312 Nightly Patch 4 - Plugin.BaseCore

June 4, 2026

12 files modified. 1 new file created.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [ClassCache (1)](#classcache)
```cs:no-line-numbers
public T Create<T2>(Type type) // [!code --]
public Func<T> CreateDelegate(Type type) // [!code ++]
```
### [Lang (1)](#lang)
```cs:no-line-numbers
public static HashSet<string> extraExcelDialogs = new HashSet<string>(); // [!code --]
public static List<Func<List<string>>> excelDialogLoaders = new List<Func<List<string>>>(); // [!code ++]
```
## BaseModManager

[`public void Parse<T>(ModItemList<T> list) where T : UnityEngine.Object`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/BaseModManager.cs#L34-L39)
```cs:line-numbers=34

	private static readonly Dictionary<string, HashSet<Action<object>>> _eventHandlers = new Dictionary<string, HashSet<Action<object>>>();

	private static readonly Dictionary<(string, Delegate), Action<object>> _typedEventHandlers = new Dictionary<(string, Delegate), Action<object>>(); // [!code ++]
 // [!code ++]
	public DirectoryInfo dirWorkshop;

	public int priorityIndex;
```

[`public virtual void Init(string path, string defaultPackage = "_Elona")`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/BaseModManager.cs#L46-L51)
```cs:line-numbers=46
	Debug.Log("Initializing ModManager:" + defaultPackage + "/" + path);
	Instance = this;
	rootMod = (rootDefaultPacakge = path);
	_eventHandlers.Clear(); // [!code ++]
	_typedEventHandlers.Clear(); // [!code ++]
	if (!defaultPackage.IsEmpty())
	{
		rootDefaultPacakge = rootMod + defaultPackage + "/";
```

[`public virtual void Init(string path, string defaultPackage = "_Elona")`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/BaseModManager.cs#L55-L86)
```cs:line-numbers=55
public void InitLang()
{
	Debug.Log("Initializing Langs: " + Lang.langCode);
	foreach (LangSetting value in MOD.langs.Values) // [!code --]
	MOD.listTalk.Clear(); // [!code ++]
	MOD.listGodTalk.Clear(); // [!code ++]
	MOD.tones.Clear(); // [!code ++]
	LangSetting langSetting = MOD.langs.TryGetValue(Lang.langCode); // [!code ++]
	if (langSetting == null) // [!code ++]
	{
		if (value.id != Lang.langCode) // [!code --]
		{ // [!code --]
			continue; // [!code --]
		} // [!code --]
		new DirectoryInfo(value.dir); // [!code --]
		FileInfo[] files = new DirectoryInfo(value.dir + "Data").GetFiles(); // [!code --]
		foreach (FileInfo fileInfo in files) // [!code --]
		Debug.LogError("Lang: " + Lang.langCode + " is not set"); // [!code ++]
		langSetting = MOD.langs.FirstItem(); // [!code ++]
	} // [!code ++]
	FileInfo[] files = new DirectoryInfo(langSetting.dir + "Data").GetFiles(); // [!code ++]
	foreach (FileInfo fileInfo in files) // [!code ++]
	{ // [!code ++]
		switch (fileInfo.Name) // [!code ++]
		{
			if (fileInfo.Name == "Alias.xlsx") // [!code --]
			{ // [!code --]
				Lang.alias = new ExcelData(fileInfo.FullName); // [!code --]
			} // [!code --]
			if (fileInfo.Name == "Name.xlsx") // [!code --]
			{ // [!code --]
				Lang.names = new ExcelData(fileInfo.FullName); // [!code --]
			} // [!code --]
			if (fileInfo.Name == "chara_talk.xlsx") // [!code --]
			{ // [!code --]
				MOD.listTalk.items.Add(new ExcelData(fileInfo.FullName)); // [!code --]
			} // [!code --]
			if (fileInfo.Name == "chara_tone.xlsx") // [!code --]
			{ // [!code --]
				MOD.tones.items.Add(new ExcelData(fileInfo.FullName)); // [!code --]
			} // [!code --]
		case "Alias.xlsx": // [!code ++]
			Lang.alias = new ExcelData(fileInfo.FullName); // [!code ++]
			break; // [!code ++]
		case "Name.xlsx": // [!code ++]
			Lang.names = new ExcelData(fileInfo.FullName); // [!code ++]
			break; // [!code ++]
		case "chara_talk.xlsx": // [!code ++]
			MOD.listTalk.Add(new ExcelData(fileInfo.FullName)); // [!code ++]
			break; // [!code ++]
		case "god_talk.xlsx": // [!code ++]
			MOD.listGodTalk.Add(new ExcelData(fileInfo.FullName)); // [!code ++]
			break; // [!code ++]
		case "chara_tone.xlsx": // [!code ++]
			MOD.tones.Add(new ExcelData(fileInfo.FullName)); // [!code ++]
			break; // [!code ++]
		}
	}
}
```

[`public virtual void ParseExtra(DirectoryInfo dir, BaseModPackage package)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/BaseModManager.cs#L91-L102)
```cs:line-numbers=91

	public static void SubscribeEvent(string eventId, Action<object> handler)
	{
		if (!_eventHandlers.TryGetValue(eventId, out var value)) // [!code --]
		if (handler != null) // [!code ++]
		{ // [!code ++]
			if (!_eventHandlers.TryGetValue(eventId, out var value)) // [!code ++]
			{ // [!code ++]
				value = new HashSet<Action<object>>(); // [!code ++]
				_eventHandlers[eventId] = value; // [!code ++]
			} // [!code ++]
			value.Add(handler); // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static void SubscribeEvent(string eventId, Action handler) // [!code ++]
	{ // [!code ++]
		if (handler != null) // [!code ++]
		{ // [!code ++]
			Action<object> action = delegate // [!code ++]
			{ // [!code ++]
				handler(); // [!code ++]
			}; // [!code ++]
			(string, Delegate) key = (eventId, handler); // [!code ++]
			_typedEventHandlers[key] = action; // [!code ++]
			SubscribeEvent(eventId, action); // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static void SubscribeEvent<T>(string eventId, Action<T> handler) // [!code ++]
	{ // [!code ++]
		if (handler == null) // [!code ++]
		{
			value = new HashSet<Action<object>>(); // [!code --]
			_eventHandlers[eventId] = value; // [!code --]
			return; // [!code ++]
		}
		value.Add(handler); // [!code --]
		Action<object> action = delegate(object data) // [!code ++]
		{ // [!code ++]
			if (!(data is T obj)) // [!code ++]
			{ // [!code ++]
				if (data != null) // [!code ++]
				{ // [!code ++]
					throw new InvalidCastException($"{handler.Method.DeclaringType?.Assembly.GetName()} " + "expects " + typeof(T).Name + ", got " + data.GetType().Name); // [!code ++]
				} // [!code ++]
				handler(default(T)); // [!code ++]
			} // [!code ++]
			else // [!code ++]
			{ // [!code ++]
				handler(obj); // [!code ++]
			} // [!code ++]
		}; // [!code ++]
		(string, Delegate) key = (eventId, handler); // [!code ++]
		_typedEventHandlers[key] = action; // [!code ++]
		SubscribeEvent(eventId, action); // [!code ++]
	}

	public static void UnsubscribeEvent(string eventId, Action<object> handler)
```

[`public static void UnsubscribeEvent(string eventId, Action<object> handler)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/BaseModManager.cs#L111-L116)
```cs:line-numbers=111
		}
	}

	public static void UnsubscribeEvent(string eventId, Action handler) // [!code ++]
	{ // [!code ++]
		if (handler != null) // [!code ++]
		{ // [!code ++]
			(string, Delegate) key = (eventId, handler); // [!code ++]
			if (_typedEventHandlers.TryGetValue(key, out var value)) // [!code ++]
			{ // [!code ++]
				UnsubscribeEvent(eventId, value); // [!code ++]
				_typedEventHandlers.Remove(key); // [!code ++]
			} // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static void UnsubscribeEvent<T>(string eventId, Action<T> handler) // [!code ++]
	{ // [!code ++]
		if (handler != null) // [!code ++]
		{ // [!code ++]
			(string, Delegate) key = (eventId, handler); // [!code ++]
			if (_typedEventHandlers.TryGetValue(key, out var value)) // [!code ++]
			{ // [!code ++]
				UnsubscribeEvent(eventId, value); // [!code ++]
				_typedEventHandlers.Remove(key); // [!code ++]
			} // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static void PublishEvent(string eventId, object data = null)
	{
		if (!_eventHandlers.TryGetValue(eventId, out var value))
```

[`public static void PublishEvent(string eventId, object data = null)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/BaseModManager.cs#L130-L135)
```cs:line-numbers=130
		}
	}

	public static void PublishEvent<T>(string eventId, T data = default(T)) // [!code ++]
	{ // [!code ++]
		PublishEvent(eventId, (object)data); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static bool HasEventSubscriber(string eventId)
	{
		int? num = _eventHandlers.GetValueOrDefault(eventId)?.Count;
```

## BaseModPackage

[`public void Parse()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/BaseModPackage.cs#L159-L171)
```cs:line-numbers=159
	{
		if (directoryInfo.Name == "Actor")
		{
			FileInfo[] files = directoryInfo.GetFiles(); // [!code --]
			FileInfo[] files = directoryInfo.GetFiles("*.xlsx", SearchOption.TopDirectoryOnly); // [!code ++]
			foreach (FileInfo fileInfo in files)
			{
				if (fileInfo.Name.EndsWith(".xlsx")) // [!code --]
				{ // [!code --]
					MOD.actorSources.items.Add(new ExcelData(fileInfo.FullName)); // [!code --]
				} // [!code --]
				MOD.actorSources.Add(new ExcelData(fileInfo.FullName)); // [!code ++]
			}
			DirectoryInfo[] directories2 = directoryInfo.GetDirectories();
			foreach (DirectoryInfo directoryInfo2 in directories2)
```

## ClassCache

[`public class ClassCache<T>`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/ClassCache.cs#L7-L31)
```cs:line-numbers=7
{
	public Dictionary<string, Func<T>> dict = new Dictionary<string, Func<T>>();

	public T Create<T2>(Type type) // [!code --]
	public Func<T> CreateDelegate(Type type) // [!code ++]
	{
		Func<T> result = () => default(T); // [!code ++]
		if (type == null)
		{
			return default(T); // [!code --]
		} // [!code --]
		Func<T> func = dict.TryGetValue(type.Name); // [!code --]
		if (func != null) // [!code --]
		{ // [!code --]
			return func(); // [!code --]
			return result; // [!code ++]
		}
		ConstructorInfo constructor = type.GetConstructor(Type.EmptyTypes);
		if (constructor == null) // [!code --]
		if (constructor != null) // [!code ++]
		{
			return default(T); // [!code --]
			result = Expression.Lambda<Func<T>>(Expression.New(constructor), Array.Empty<ParameterExpression>()).Compile(); // [!code ++]
		}
		func = Expression.Lambda<Func<T>>(Expression.New(constructor), Array.Empty<ParameterExpression>()).Compile(); // [!code --]
		dict.Add(type.Name, func); // [!code --]
		return func(); // [!code --]
		return result; // [!code ++]
	}

	public T Create<T2>(string id, string assembly)
```

[`public T Create<T2>(string id, string assembly)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/ClassCache.cs#L38-L53)
```cs:line-numbers=38
		Type type = Type.GetType(id + ", " + assembly);
		if (type == null)
		{
			foreach (string assembly2 in ClassCache.assemblies) // [!code --]
			for (int num = ClassCache.typeLoaders.Count - 1; num >= 0; num--) // [!code ++]
			{
				type = Type.GetType(id + ", " + assembly2); // [!code --]
				type = ClassCache.typeLoaders[num](id); // [!code ++]
				if (type != null)
				{
					break;
				}
			}
		}
		return Create<T2>(type); // [!code --]
		func = CreateDelegate(type); // [!code ++]
		dict[id] = func; // [!code ++]
		return func(); // [!code ++]
	}
}
public class ClassCache
```

[`public class ClassCache`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/ClassCache.cs#L56-L63)
```cs:line-numbers=56

	public static HashSet<string> assemblies = new HashSet<string>();

	public static List<Func<string, Type>> typeLoaders = new List<Func<string, Type>> { LoadTypeFromGlobalNamespace }; // [!code ++]
 // [!code ++]
	public static T Create<T>(string id, string assembly = "Assembly-CSharp")
	{
		return (T)caches.Create<T>(id, assembly);
	}
 // [!code ++]
	public static Type LoadTypeFromGlobalNamespace(string id) // [!code ++]
	{ // [!code ++]
		foreach (string assembly in assemblies) // [!code ++]
		{ // [!code ++]
			Type type = Type.GetType(id + ", " + assembly); // [!code ++]
			if (type != null) // [!code ++]
			{ // [!code ++]
				return type; // [!code ++]
			} // [!code ++]
		} // [!code ++]
		return null; // [!code ++]
	} // [!code ++]
}
```

## ERROR

[`public class ERROR`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/ERROR.cs#L1-L4)
```cs:line-numbers=1
public class ERROR
{
	public static string msg;
 // [!code ++]
	public static int lastImported; // [!code ++]
}
```

## ExcelDataList

[`public virtual void OnInitialize()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/ExcelDataList.cs#L46-L57)
```cs:line-numbers=46
	{
	}

	public bool HasRow(string id) // [!code ++]
	{ // [!code ++]
		Initialize(); // [!code ++]
		return all.ContainsKey(id); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public Dictionary<string, string> GetRow(string id)
	{
		if (!initialized) // [!code --]
		{ // [!code --]
			Initialize(); // [!code --]
		} // [!code --]
		return all.TryGetValue(id) ?? list[0]; // [!code --]
		Initialize(); // [!code ++]
		return all.TryGetValue(id); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void Add(ExcelData data) // [!code ++]
	{ // [!code ++]
		items.Add(data); // [!code ++]
		Reload(); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void Clear() // [!code ++]
	{ // [!code ++]
		items.Clear(); // [!code ++]
		Reload(); // [!code ++]
	}
}
```

## ExcelParser

[`public static string[] GetStringArray(int id)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/ExcelParser.cs#L145-L151)
```cs:line-numbers=145
	string str = GetStr(id);
	if (str != null)
	{
		return str.Split(','); // [!code --]
		return str.Split(',').ToArray(); // [!code ++]
	}
	return Array.Empty<string>();
}
```

## +GodTalkDataList

::: details File Created
```cs
using System.Collections.Generic;

public class GodTalkDataList : ExcelDataList
{
	public override int StartIndex => 3;

	public override void Initialize()
	{
		if (initialized)
		{
			return;
		}
		foreach (ExcelData item in items)
		{
			item.startIndex = StartIndex;
			item.BuildMap();
			foreach (KeyValuePair<string, Dictionary<string, string>> item2 in item.sheets["_default"].map)
			{
				item2.Deconstruct(out var key, out var value);
				string key2 = key;
				Dictionary<string, string> dictionary = value;
				if (all.TryAdd(key2, dictionary))
				{
					list.Add(dictionary);
					continue;
				}
				foreach (KeyValuePair<string, string> item3 in dictionary)
				{
					item3.Deconstruct(out key, out var value2);
					string key3 = key;
					string value3 = value2;
					all[key2][key3] = value3;
				}
			}
		}
		initialized = true;
	}

	public string GetTalk(string id, string idTopic)
	{
		Dictionary<string, string> row = GetRow(idTopic);
		return ((row == null) ? null : row.TryGetValue(id)?.SplitByNewline().RandomItem()) ?? "";
	}
}
```

:::
## IO

[`public static T LoadFile<T>(string path, bool compress = false, JsonSerializerSe`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/IO.cs#L119-L125)
```cs:line-numbers=119
{
	if (!File.Exists(path))
	{
		Debug.Log("File does not exist:" + path); // [!code --]
		Debug.Log("#io File does not exist:" + path); // [!code ++]
		return default(T);
	}
	string value = (IsCompressed(path) ? Decompress(path) : File.ReadAllText(path));
```

## Lang

[`using System;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/Lang.cs#L1-L5)
```cs:line-numbers=1
using System;
using System.Collections.Generic;
using System.Linq; // [!code ++]
using System.Text;

public class Lang
```

[`public class Words`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/Lang.cs#L59-L65)
```cs:line-numbers=59

	public static ExcelData excelDialog;

	public static HashSet<string> extraExcelDialogs = new HashSet<string>(); // [!code --]
	public static List<Func<List<string>>> excelDialogLoaders = new List<Func<List<string>>>(); // [!code ++]

	public static bool IsBuiltin(string id)
	{
```

[`public static ExcelData.Sheet GetDialogSheet(string idSheet)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/Lang.cs#L215-L223)
```cs:line-numbers=215
		{
			list.Add(new ExcelData(CorePath.CorePackage.TextDialogLocal + "dialog.xlsx"));
		}
		foreach (string extraExcelDialog in extraExcelDialogs) // [!code --]
		foreach (Func<List<string>> excelDialogLoader in excelDialogLoaders) // [!code ++]
		{
			list.Add(new ExcelData(extraExcelDialog)); // [!code --]
			list.AddRange(from f in excelDialogLoader() // [!code ++]
				select new ExcelData(f)); // [!code ++]
		}
		for (int i = 0; i < excelDialog.book.NumberOfSheets; i++)
		{
```

## MOD

[`public class MOD`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/MOD.cs#L9-L14)
```cs:line-numbers=9

	public static TalkDataList listTalk = new TalkDataList();

	public static GodTalkDataList listGodTalk = new GodTalkDataList(); // [!code ++]
 // [!code ++]
	public static ToneDataList tones = new ToneDataList();

	public static ExcelDataList actorSources = new ExcelDataList();
```

[`public class MOD`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/MOD.cs#L22-L25)
```cs:line-numbers=22
	public static List<FileInfo> listPartialMaps = new List<FileInfo>();

	public static Dictionary<string, FileInfo> sounds = new Dictionary<string, FileInfo>();
 // [!code ++]
	public static void ResetResources() // [!code ++]
	{ // [!code ++]
		langs.Clear(); // [!code ++]
		listTalk.Clear(); // [!code ++]
		listGodTalk.Clear(); // [!code ++]
		tones.Clear(); // [!code ++]
		actorSources.Clear(); // [!code ++]
		sprites = new ModItemList<Sprite>(); // [!code ++]
		listMaps.Clear(); // [!code ++]
		listPartialMaps.Clear(); // [!code ++]
		sounds.Clear(); // [!code ++]
	} // [!code ++]
}
```

## SourceData

[`public override bool ImportData(ISheet sheet, string bookname, bool overwrite =`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/SourceData.cs#L145-L170)
```cs:line-numbers=145
	nameBook = bookname;
	SourceData.rowHeader = sheet.GetRow(0);
	SourceData.rowDefault = sheet.GetRow(2);
	IRow row = sheet.GetRow(3); // [!code ++]
	if (SourceData.rowHeader == null || row == null) // [!code ++]
	{ // [!code ++]
		ERROR.lastImported = 0; // [!code ++]
		return true; // [!code ++]
	} // [!code ++]
	IReadOnlyDictionary<string, int> rowMapping = GetRowMapping();
	Dictionary<string, int> dictionary = null; // [!code --]
	Dictionary<string, int> header = null; // [!code ++]
	if (rowMapping != null)
	{
		dictionary = new Dictionary<string, int>(StringComparer.OrdinalIgnoreCase); // [!code --]
		foreach (ICell item in SourceData.rowHeader.Cells.Where((ICell c) => c.ToString() != "0" && !c.ToString().IsEmpty())) // [!code --]
		header = new Dictionary<string, int>(StringComparer.OrdinalIgnoreCase); // [!code ++]
		foreach (ICell cell in SourceData.rowHeader.Cells) // [!code ++]
		{
			dictionary[item.ToString().Trim()] = item.ColumnIndex; // [!code --]
			string text = cell.ToString().Trim(); // [!code ++]
			if (!(text == "0") && !text.IsEmpty()) // [!code ++]
			{ // [!code ++]
				header[text] = cell.ColumnIndex; // [!code ++]
			} // [!code ++]
		}
		if (!rowMapping.Keys.All(dictionary.ContainsKey) || rowMapping.Count > dictionary.Count) // [!code --]
		if (!rowMapping.All((KeyValuePair<string, int> kv) => header.TryGetValue(kv.Key, 0) == kv.Value)) // [!code ++]
		{
			string[] array = rowMapping.Keys.Except(dictionary.Keys).ToArray(); // [!code --]
			string[] array = rowMapping.Keys.Except(header.Keys).ToArray(); // [!code ++]
			if (array.Length != 0)
			{
				Debug.LogWarning("#source ill-format file with missing columns, init with empty values\n" + ExcelParser.GetRowHeaderDiff(rowMapping, dictionary)); // [!code --]
				Debug.LogWarning("#source ill-format file with missing columns, init with empty values\n" + ExcelParser.GetRowHeaderDiff(rowMapping, header)); // [!code ++]
				string[] array2 = array;
				foreach (string text in array2) // [!code --]
				foreach (string text2 in array2) // [!code ++]
				{
					SourceData.rowHeader.CreateCell(276, CellType.String).SetCellValue(text); // [!code --]
					dictionary[text] = 276; // [!code --]
					SourceData.rowHeader.CreateCell(276, CellType.String).SetCellValue(text2); // [!code ++]
					header[text2] = 276; // [!code ++]
				}
			}
			else
```

[`public override bool ImportData(ISheet sheet, string bookname, bool overwrite =`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/SourceData.cs#L174-L198)
```cs:line-numbers=174
		}
		else
		{
			dictionary = null; // [!code --]
			header = null; // [!code ++]
		}
	}
	int num = 0;
	for (int j = 3; j <= sheet.LastRowNum; j++)
	{
		SourceData.row = sheet.GetRow(j);
		if (string.IsNullOrEmpty(SourceData.row?.GetCell(0)?.ToString())) // [!code --]
		try // [!code ++]
		{
			break; // [!code --]
			if (string.IsNullOrEmpty(SourceData.row?.GetCell(0)?.ToString())) // [!code ++]
			{ // [!code ++]
				break; // [!code ++]
			} // [!code ++]
			T val = ((header != null) ? CreateRowByMapping(header) : CreateRow()); // [!code ++]
			val.OnImportData(this); // [!code ++]
			rows.Add(val); // [!code ++]
			num++; // [!code ++]
			continue; // [!code ++]
		} // [!code ++]
		catch (Exception arg) // [!code ++]
		{ // [!code ++]
			Debug.LogError($"#source failed to create row#{j + 1}\n{arg}"); // [!code ++]
			continue; // [!code ++]
		}
		T val = ((dictionary != null) ? CreateRowByMapping(dictionary) : CreateRow()); // [!code --]
		val.OnImportData(this); // [!code --]
		rows.Add(val); // [!code --]
		num++; // [!code --]
	}
	string text2 = sheet.SheetName + "/" + sheet.LastRowNum + "/" + num; // [!code --]
	Debug.Log(text2); // [!code --]
	ERROR.msg = text2; // [!code --]
	Debug.Log(sheet.SheetName + "/" + sheet.LastRowNum + "/" + num); // [!code ++]
	ERROR.lastImported = num; // [!code ++]
	OnAfterImportData();
	initialized = false;
	return true;
```

[`public virtual T CreateRowByMapping(IReadOnlyDictionary<string, int> mapping)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/SourceData.cs#L212-L217)
```cs:line-numbers=212
		return null;
	}

	public override BaseRow[] ExportRows() // [!code ++]
	{ // [!code ++]
		return rows.OfType<BaseRow>().ToArray(); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public override int ImportRows(IEnumerable<BaseRow> sourceRows)
	{
		int num = 0;
```

[`public virtual void RemoveDuplicateRows()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/SourceData.cs#L242-L248)
```cs:line-numbers=242
	}
	if (field.FieldType != typeof(T2))
	{
		Debug.LogError($"#source override: {arg} id field mismatch {field.FieldType} != {typeof(T2)}"); // [!code --]
		Debug.LogError($"#source-override: {arg} id field mismatch {field.FieldType} != {typeof(T2)}"); // [!code ++]
	}
	for (int num = rows.Count - 1; num >= 0; num--)
	{
```

[`public virtual void RemoveDuplicateRows()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/SourceData.cs#L254-L265)
```cs:line-numbers=254
		}
		else if (!flag)
		{
			Debug.Log($"#source override: {arg} {val2}"); // [!code --]
			Debug.Log($"#source-override: {arg} {val2}"); // [!code ++]
		}
	}
	if (rows.Count != list.Count)
	{
		Debug.Log($"#source override: {arg} total/{rows.Count} -> unique/{list.Count}"); // [!code --]
		Debug.Log($"#source-override: {arg} total/{rows.Count} -> unique/{list.Count}"); // [!code ++]
		list.Reverse();
		rows = list;
	}
```

[`public virtual int ImportRows(IEnumerable<BaseRow> sourceRows)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/SourceData.cs#L899-L904)
```cs:line-numbers=899
		return 0;
	}

	public virtual BaseRow[] ExportRows() // [!code ++]
	{ // [!code ++]
		return Array.Empty<BaseRow>(); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public virtual void BackupSource()
	{
	}
```

## TalkDataList

[`public override void OnInitialize()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/TalkDataList.cs#L22-L38)
```cs:line-numbers=22

	public string GetTalk(string id, string idTopic, bool useDefault = false, bool human = true)
	{
		if (!initialized) // [!code --]
		{ // [!code --]
			Initialize(); // [!code --]
		} // [!code --]
		bool flag = false;
		Dictionary<string, string> dictionary = all.TryGetValue(idTopic); // [!code --]
		if (dictionary == null) // [!code --]
		Dictionary<string, string> row = GetRow(idTopic); // [!code ++]
		if (row == null) // [!code ++]
		{
			return "";
		}
		string text = dictionary.TryGetValue(id); // [!code --]
		string text = row.TryGetValue(id); // [!code ++]
		if (text.IsEmpty())
		{
			if (!useDefault)
```

[`public string GetTalk(string id, string idTopic, bool useDefault = false, bool h`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/TalkDataList.cs#L40-L46)
```cs:line-numbers=40
			Debug.LogError("id not found:" + id);
			return "";
		}
		text = dictionary.TryGetValue("default"); // [!code --]
		text = row.TryGetValue("default"); // [!code ++]
		flag = true;
	}
	text = text.Split(new string[3] { "\r\n", "\r", "\n" }, StringSplitOptions.None).RandomItem();
```

## ToneDataList

[`public override void OnInitialize()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/ToneDataList.cs#L10-L19)
```cs:line-numbers=10

	public StringBuilder ApplyTone(string id, ref string text, int gender)
	{
		if (!initialized) // [!code --]
		{ // [!code --]
			Initialize(); // [!code --]
		} // [!code --]
		Initialize(); // [!code ++]
		Dictionary<string, string> dictionary = all[id];
		StringBuilder stringBuilder = new StringBuilder();
		bool flag = false;
```

[`public StringBuilder ApplyTone(string id, ref string text, int gender)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/33c6a6dad6ed7bf05646ab82e92868af47e91553/Elin/Plugins.basecore/ToneDataList.cs#L57-L66)
```cs:line-numbers=57

	public string GetToneID(string id, int gender)
	{
		if (!initialized) // [!code --]
		{ // [!code --]
			Initialize(); // [!code --]
		} // [!code --]
		Initialize(); // [!code ++]
		if (!Lang.isJP)
		{
			return id + "|I|YOU";
```
