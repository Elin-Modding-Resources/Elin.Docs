---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 8 files modified.
version: EA 23.336 Nightly
changes: ACT/ActEffect/Card/Chara/FoodEffect/Game/Thing/TraitChara
---

# EA 23.336 Nightly

August 17, 2026

8 files modified.

## Important Changes

**None.**
## ACT

[`public bool CanPerform(Chara _cc, Card _tc = null, Point _tp = null)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/ACT.cs#L345-L351)
```cs:line-numbers=345
if (tt.Range == TargetRange.Self)
{
	TP.Set(CC.pos);
	TC = CC; // [!code --]
	return CanPerform();
}
if (CC.IsPC && EClass.core.config.game.shiftToUseNegativeAbilityOnSelf)
```

## ActEffect

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/ActEffect.cs#L1153-L1177)
```cs:line-numbers=1153
case EffectId.LittleSisterMigration:
case EffectId.SilverCatMigration:
{
	bool flag3 = id == EffectId.SilverCatMigration; // [!code --]
	if (!EClass.game.IsSurvival && ((flag3 && EClass._zone.id != "startVillage2") || (!flag3 && !(EClass._zone is Zone_LittleGarden)))) // [!code --]
	bool flag5 = id == EffectId.SilverCatMigration; // [!code ++]
	if (!EClass.game.IsSurvival && ((flag5 && EClass._zone.id != "startVillage2") || (!flag5 && !(EClass._zone is Zone_LittleGarden)))) // [!code ++]
	{
		Msg.SayNothingHappen();
		return;
	}
	List<Chara> list2 = new List<Chara>();
	bool flag4 = false; // [!code --]
	bool flag6 = false; // [!code ++]
	foreach (Chara chara3 in EClass._map.charas)
	{
		if (!chara3.IsPCFactionOrMinion && chara3.id == (flag3 ? "cat_silver" : "littleOne")) // [!code --]
		if (!chara3.IsPCFactionOrMinion && chara3.id == (flag5 ? "cat_silver" : "littleOne")) // [!code ++]
		{
			if (flag4) // [!code --]
			if (flag6) // [!code ++]
			{
				flag4 = false; // [!code --]
				flag6 = false; // [!code ++]
				continue;
			}
			list2.Add(chara3);
			flag4 = true; // [!code --]
			flag6 = true; // [!code ++]
		}
	}
	if (list2.Count == 0)
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/ActEffect.cs#L1185-L1193)
```cs:line-numbers=1185
	item2.PlayEffect("revive");
	item2.Destroy();
}
Msg.Say(flag3 ? "cat_migration" : "little_migration", list2.Count.ToString() ?? ""); // [!code --]
Msg.Say(flag5 ? "cat_migration" : "little_migration", list2.Count.ToString() ?? ""); // [!code ++]
EClass._zone.ModInfluence(list2.Count);
if (flag3) // [!code --]
if (flag5) // [!code ++]
{
	EClass.player.stats.catDepart += list2.Count;
	break;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/ActEffect.cs#L1249-L1257)
```cs:line-numbers=1249
TC.PlaySound("curse3");
TC.PlayEffect("curse");
TC.Say("forgetItems", TC);
int num4 = power / 50 + 1 + EClass.rnd(3); // [!code --]
int num3 = power / 50 + 1 + EClass.rnd(3); // [!code ++]
List<Thing> source = TC.things.List((Thing t) => t.c_IDTState == 0);
for (int j = 0; j < num4; j++) // [!code --]
for (int j = 0; j < num3; j++) // [!code ++]
{
	source.RandomItem().c_IDTState = 5;
}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/ActEffect.cs#L1263-L1272)
```cs:line-numbers=1263
case EffectId.EnchantArmorGreat:
{
	bool armor = id == EffectId.EnchantArmor || id == EffectId.EnchantArmorGreat;
	bool flag5 = id == EffectId.EnchantWeaponGreat || id == EffectId.EnchantArmorGreat; // [!code --]
	bool flag3 = id == EffectId.EnchantWeaponGreat || id == EffectId.EnchantArmorGreat; // [!code ++]
	if (!tc.isThing)
	{
		LayerDragGrid.CreateEnchant(CC, armor, flag5, state); // [!code --]
		LayerDragGrid.CreateEnchant(CC, armor, flag3, state); // [!code ++]
		return;
	}
	cc.PlaySound("identify");
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/ActEffect.cs#L1277-L1284)
```cs:line-numbers=1277
	tc.ModEncLv(-1);
	break;
}
int num3 = (flag5 ? 4 : 2) + (blessed ? 1 : 0); // [!code --]
if (tc.encLV >= num3) // [!code --]
int num4 = (flag3 ? 4 : 2) + (blessed ? 1 : 0); // [!code ++]
if (tc.encLV >= num4) // [!code ++]
{
	cc.Say("enc_resist", tc);
	break;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/ActEffect.cs#L1290-L1310)
```cs:line-numbers=1290
case EffectId.Identify:
case EffectId.GreaterIdentify:
{
	bool flag6 = id == EffectId.GreaterIdentify; // [!code --]
	bool flag4 = id == EffectId.GreaterIdentify; // [!code ++]
	if (flag)
	{
		Redirect(EffectId.ForgetItems, flag6 ? BlessedState.Cursed : BlessedState.Normal, default(ActRef)); // [!code --]
		Redirect(EffectId.ForgetItems, flag4 ? BlessedState.Cursed : BlessedState.Normal, default(ActRef)); // [!code ++]
		break;
	}
	if (!tc.isThing) // [!code --]
	if (!tc.isThing || (actRef.refThing == null && cc.IsPC && tc.GetRootCard() == tc)) // [!code ++]
	{
		int count = ((!blessed) ? 1 : (flag6 ? (2 + EClass.rnd(2)) : (3 + EClass.rnd(3)))); // [!code --]
		LayerDragGrid.CreateIdentify(CC, flag6, state, 0, count); // [!code --]
		int count = ((!blessed) ? 1 : (flag4 ? (2 + EClass.rnd(2)) : (3 + EClass.rnd(3)))); // [!code ++]
		LayerDragGrid.CreateIdentify(tc.isChara ? tc.Chara : CC, flag4, state, 0, count); // [!code ++]
		return;
	}
	cc.PlaySound("identify");
	cc.PlayEffect("identify");
	tc.Thing.Identify(cc.IsPCParty, (!flag6) ? IDTSource.Identify : IDTSource.SuperiorIdentify); // [!code --]
	tc.Thing.Identify(cc.IsPCParty, (!flag4) ? IDTSource.Identify : IDTSource.SuperiorIdentify); // [!code ++]
	break;
}
case EffectId.Uncurse:
```

## Card

[`public bool TryStackTo(Thing to)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/Card.cs#L3578-L3584)
```cs:line-numbers=3578
{
	return false;
}
to.decay = (to.decay * to.Num + decay * Num) / (to.Num + Num); // [!code --]
to.decay = MathEx.ClampToInt(((long)to.decay * (long)to.Num + (long)decay * (long)Num) / ((long)to.Num + (long)Num)); // [!code ++]
to.ModNum(Num);
if (c_isImportant)
{
```

## Chara

[`public void RestockEquip(bool onCreate)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/Chara.cs#L4986-L4991)
```cs:line-numbers=4986
		EQ_ID("sword_zephir");
	}
	break;
case "adv_schwert": // [!code ++]
	if (onCreate) // [!code ++]
	{ // [!code ++]
		EQ_ID("sword", -1, Rarity.Legendary); // [!code ++]
	} // [!code ++]
	if (onCreate) // [!code ++]
	{ // [!code ++]
		EQ_ID("dagger", -1, Rarity.Legendary); // [!code ++]
	} // [!code ++]
	break; // [!code ++]
case "adv_mesherada":
	if (onCreate)
	{
```

[`public bool CanEat(Thing t, bool shouldEat = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/Chara.cs#L8027-L8032)
```cs:line-numbers=8027
{
	return false;
}
if ((IsPC || (homeBranch != null && homeBranch.GetCivility() >= 50)) && (t.HasTag(CTAG.dish_fail) || t.id == "731")) // [!code ++]
{ // [!code ++]
	return false; // [!code ++]
} // [!code ++]
if (!t.IsNegativeGift && !t.HasTag(CTAG.ignoreUse) && !t.isEquipped)
{
	return t.trait.CanEat(this);
```

## FoodEffect

[`public static void Proc(Chara c, Thing food, bool consume = true)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/FoodEffect.cs#L213-L218)
```cs:line-numbers=213
	num3 *= 2;
}
num3 = num3 * (100 + c.Evalue(1235) * 10) / (100 + c.Evalue(1234) * 10 + c.Evalue(1236) * 15);
if (num3 < 1) // [!code ++]
{ // [!code ++]
	num3 = 1; // [!code ++]
} // [!code ++]
c.hunger.Mod(-num3);
if (flag2)
{
```

## Game

[`public void OnLoad()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/Game.cs#L428-L433)
```cs:line-numbers=428
		}
	}
	EClass.pc.angle = player.angle;
	ApplyFix(); // [!code ++]
	if (!version.Equals(EClass.core.version)) // [!code ++]
	{ // [!code ++]
		player.recipes.OnVersionUpdate(); // [!code ++]
	} // [!code ++]
	if (cards.container_deposit == null) // [!code ++]
	{ // [!code ++]
		cards.container_deposit = ThingGen.Create("container_deposit"); // [!code ++]
		if (player.bankMoney > 0) // [!code ++]
		{ // [!code ++]
			cards.container_deposit.Add("money", player.bankMoney); // [!code ++]
		} // [!code ++]
	} // [!code ++]
	foreach (Quest item4 in quests.list) // [!code ++]
	{ // [!code ++]
		if (item4 is QuestSequence && !EClass.sources.quests.map.ContainsKey(item4.idSource)) // [!code ++]
		{ // [!code ++]
			item4.phase = 0; // [!code ++]
		} // [!code ++]
	} // [!code ++]
	foreach (Quest global in quests.globalList) // [!code ++]
	{ // [!code ++]
		if (global is QuestSequence && !EClass.sources.quests.map.ContainsKey(global.idSource)) // [!code ++]
		{ // [!code ++]
			global.phase = 0; // [!code ++]
		} // [!code ++]
	} // [!code ++]
	foreach (KeyValuePair<int, Spatial> item5 in EClass.game.spatials.map.ToList()) // [!code ++]
	{ // [!code ++]
		if (item5.Value == null) // [!code ++]
		{ // [!code ++]
			EClass.game.spatials.map.Remove(item5.Key); // [!code ++]
		} // [!code ++]
	} // [!code ++]
} // [!code ++]
 // [!code ++]
public void ApplyFix() // [!code ++]
{ // [!code ++]
	EClass.pc.elements.CheckSkillActions();
	if (cards.listAdv.Count == 0)
	{
```

[`public void OnLoad()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/Game.cs#L437-L447)
```cs:line-numbers=437
{
	if (!(q is QuestRandom))
	{
		foreach (Quest item4 in quests.list) // [!code --]
		foreach (Quest item in quests.list) // [!code ++]
		{
			if (q != item4 && item4.id == q.id) // [!code --]
			if (q != item && item.id == q.id) // [!code ++]
			{
				quests.list.Remove(item4); // [!code --]
				quests.list.Remove(item); // [!code ++]
				break;
			}
		}
```

[`public void OnLoad()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/Game.cs#L461-L466)
```cs:line-numbers=461
		}
	}
});
if (version.IsBelow(0, 23, 336)) // [!code ++]
{ // [!code ++]
	AddAdventurer("adv_schwert"); // [!code ++]
} // [!code ++]
TryAddQuest("into_darkness", "exile_kettle");
if (version.IsBelow(0, 23, 221) && EClass.game.quests.IsCompleted("curry"))
{
```

[`public void OnLoad()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/Game.cs#L472-L482)
```cs:line-numbers=472
}
if (version.IsBelow(0, 23, 182))
{
	foreach (Chara value2 in cards.globalCharas.Values) // [!code --]
	foreach (Chara value in cards.globalCharas.Values) // [!code ++]
	{
		if (!value2.IsUnique && value2.c_lockedAge != 0) // [!code --]
		if (!value.IsUnique && value.c_lockedAge != 0) // [!code ++]
		{
			value2.elements.SetBase(1243, 1); // [!code --]
			value.elements.SetBase(1243, 1); // [!code ++]
		}
	}
}
```

[`public void OnLoad()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/Game.cs#L517-L527)
```cs:line-numbers=517
}
if (version.IsBelow(0, 23, 72))
{
	foreach (Chara value3 in EClass.game.cards.globalCharas.Values) // [!code --]
	foreach (Chara value2 in EClass.game.cards.globalCharas.Values) // [!code ++]
	{
		if (!value3.isDead) // [!code --]
		if (!value2.isDead) // [!code ++]
		{
			value3.c_wasInPcParty = false; // [!code --]
			value2.c_wasInPcParty = false; // [!code ++]
		}
	}
}
```

[`public void OnLoad()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/Game.cs#L549-L559)
```cs:line-numbers=549
}
if (version.IsBelow(0, 23, 51))
{
	foreach (Chara value4 in EClass.game.cards.globalCharas.Values) // [!code --]
	foreach (Chara value3 in EClass.game.cards.globalCharas.Values) // [!code ++]
	{
		if (!(value4.id != "adv") && value4.IsPCFaction) // [!code --]
		if (!(value3.id != "adv") && value3.IsPCFaction) // [!code ++]
		{
			value4.idSkin = value4.uid % (value4.source._tiles.Length - 4) / 2 * 2 + ((!value4.IsMale) ? 1 : 0); // [!code --]
			value3.idSkin = value3.uid % (value3.source._tiles.Length - 4) / 2 * 2 + ((!value3.IsMale) ? 1 : 0); // [!code ++]
		}
	}
}
```

[`public void OnLoad()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/Game.cs#L615-L623)
```cs:line-numbers=615
});
if (version.IsBelow(0, 22, 20))
{
	foreach (Chara value5 in cards.globalCharas.Values) // [!code --]
	foreach (Chara value4 in cards.globalCharas.Values) // [!code ++]
	{
		value5.SetBool(18, enable: false); // [!code --]
		value4.SetBool(18, enable: false); // [!code ++]
	}
}
if (version.IsBelow(0, 22, 22))
```

[`public void OnLoad()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/Game.cs#L626-L634)
```cs:line-numbers=626
	{
		EClass.pc.faithElements.SetParent();
	}
	foreach (Element item5 in EClass.pc.elements.ListElements((Element e) => e.source.categorySub == "god")) // [!code --]
	foreach (Element item2 in EClass.pc.elements.ListElements((Element e) => e.source.categorySub == "god")) // [!code ++]
	{
		EClass.pc.SetFeat(item5.id, 0); // [!code --]
		EClass.pc.SetFeat(item2.id, 0); // [!code ++]
	}
	EClass.pc.RefreshFaithElement();
}
```

[`public void OnLoad()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/Game.cs#L636-L674)
```cs:line-numbers=636
{
	player.debt = 20000000;
}
if (!version.Equals(EClass.core.version)) // [!code --]
{ // [!code --]
	player.recipes.OnVersionUpdate(); // [!code --]
} // [!code --]
if (cards.container_deposit == null) // [!code --]
{ // [!code --]
	cards.container_deposit = ThingGen.Create("container_deposit"); // [!code --]
	if (player.bankMoney > 0) // [!code --]
	{ // [!code --]
		cards.container_deposit.Add("money", player.bankMoney); // [!code --]
	} // [!code --]
} // [!code --]
foreach (Quest item6 in quests.list) // [!code --]
{ // [!code --]
	if (item6 is QuestSequence && !EClass.sources.quests.map.ContainsKey(item6.idSource)) // [!code --]
	{ // [!code --]
		item6.phase = 0; // [!code --]
	} // [!code --]
} // [!code --]
foreach (Quest global2 in quests.globalList) // [!code --]
{ // [!code --]
	if (global2 is QuestSequence && !EClass.sources.quests.map.ContainsKey(global2.idSource)) // [!code --]
	{ // [!code --]
		global2.phase = 0; // [!code --]
	} // [!code --]
} // [!code --]
foreach (KeyValuePair<int, Spatial> item7 in EClass.game.spatials.map.ToList()) // [!code --]
{ // [!code --]
	if (item7.Value == null) // [!code --]
	{ // [!code --]
		EClass.game.spatials.map.Remove(item7.Key); // [!code --]
	} // [!code --]
} // [!code --]
void TryAddQuest(string idQuest, string idReqQuest)
{
	if (quests.completedIDs.Contains(idReqQuest) && !quests.completedIDs.Contains(idQuest) && quests.GetGlobal(idQuest) == null && quests.Get(idQuest) == null)
```

[`public void StartNewGame()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/Game.cs#L963-L969)
```cs:line-numbers=963
public void AddAdventurers()
{
	List<Zone> source = world.region.ListTowns();
	string[] array = new string[7] { "adv_kiria", "adv_gaki", "adv_wini", "adv_verna", "adv_ivory", "adv_mesherada", "adv_yukiimo" }; // [!code --]
	string[] array = new string[8] { "adv_kiria", "adv_gaki", "adv_wini", "adv_verna", "adv_ivory", "adv_mesherada", "adv_yukiimo", "adv_schwert" }; // [!code ++]
	for (int i = 0; i < EClass.setting.balance.numAdv; i++)
	{
		Zone zone = source.RandomItem();
```

## Thing

[`public override bool CanStackTo(Thing to)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/Thing.cs#L1792-L1803)
```cs:line-numbers=1792
			_ = 770;
		}
	}
	int num2 = (to.encLV = Mathf.CeilToInt(1f * (float)(base.encLV * base.Num + to.encLV * to.Num) / (float)(base.Num + to.Num))); // [!code --]
	int num2 = (to.encLV = Mathf.CeilToInt(MathEx.ClampToInt(((long)base.encLV * (long)base.Num + (long)to.encLV * (long)to.Num) / ((long)base.Num + (long)to.Num)))); // [!code ++]
	base.encLV = num2;
	foreach (Element value2 in elements.dict.Values)
	{
		Element element2 = to.elements.GetElement(value2.id);
		value2.vBase = (element2.vBase = (value2.vBase * base.Num + element2.vBase * to.Num) / (base.Num + to.Num)); // [!code --]
		value2.vBase = (element2.vBase = MathEx.ClampToInt(((long)value2.vBase * (long)base.Num + (long)element2.vBase * (long)to.Num) / ((long)base.Num + (long)to.Num))); // [!code ++]
	}
	return true;
}
```

## TraitChara

[`public virtual int GetAdvRank()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/217c63f0bb842eadfa450f0b585fd293bd9888b6/Elin/TraitChara.cs#L169-L174)
```cs:line-numbers=169
case "adv_yukiimo":
case "adv_kiria":
case "adv_ivory":
case "adv_schwert": // [!code ++]
	min = 4;
	break;
}
```
