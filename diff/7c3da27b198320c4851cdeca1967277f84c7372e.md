---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 1 file modified.
version: +EA 23.264 Nightly - Plugin.BaseCore
changes: ClassExtension
---

# +EA 23.264 Nightly - Plugin.BaseCore

January 30, 2026

1 file modified.

## Important Changes

**None.**
## ClassExtension

[`public static string Evalute(this string[] array, float val)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/7c3da27b198320c4851cdeca1967277f84c7372e/Elin/Plugins.basecore/ClassExtension.cs#L818-L841)
```cs:line-numbers=818

	public static string ToShortNumber(this int a)
	{
		string text; // [!code --]
		if (a < 0) // [!code ++]
		{ // [!code ++]
			string text; // [!code ++]
			if (a > -1000000) // [!code ++]
			{ // [!code ++]
				if (a <= -1000) // [!code ++]
				{ // [!code ++]
					return a / 1000 + "K"; // [!code ++]
				} // [!code ++]
				text = a.ToString(); // [!code ++]
				if (text == null) // [!code ++]
				{ // [!code ++]
					return ""; // [!code ++]
				} // [!code ++]
			} // [!code ++]
			else // [!code ++]
			{ // [!code ++]
				text = a / 1000000 + "M"; // [!code ++]
			} // [!code ++]
			return text; // [!code ++]
		} // [!code ++]
		string text2; // [!code ++]
		if (a < 1000000)
		{
			if (a >= 1000)
			{
				return a / 1000 + "K";
			}
			text = a.ToString(); // [!code --]
			if (text == null) // [!code --]
			text2 = a.ToString(); // [!code ++]
			if (text2 == null) // [!code ++]
			{
				return "";
			}
		}
		else
		{
			text = a / 1000000 + "M"; // [!code --]
			text2 = a / 1000000 + "M"; // [!code ++]
		}
		return text; // [!code --]
		return text2; // [!code ++]
	}

	public static string ToFormat(this int a)
```
