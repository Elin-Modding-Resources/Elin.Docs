---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 10 files modified.
version: EA 23.298 Nightly
changes: CardActor/ConTransmuteHuman/GrowSystem/LayerPixelPaint/LayerShippingResult/StanceSongEnd/Trait/TraitCanvas/TraitDrawingPaperM/TraitToolWaterCan
---

# EA 23.298 Nightly

April 24, 2026

10 files modified.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [Trait (1)](#trait)
```cs:no-line-numbers
public void OnBarter(bool reroll = false) // [!code --]
public virtual void OnBarter(bool reroll = false) // [!code ++]
```
## CardActor

[`public virtual void RefreshSprite()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/eaca55f4ae768b36f335481940eb32174f873002/Elin/CardActor.cs#L261-L275)
```cs:line-numbers=261
		break;
	}
	int dir = owner.dir;
	SpriteRenderer spriteRenderer = sr; // [!code --]
	Sprite sprite2 = (sr.sprite = owner.GetSprite(dir)); // [!code --]
	Sprite sprite4 = (spriteRenderer.sprite = sprite2); // [!code --]
	Sprite sprite5 = sprite4; // [!code --]
	Sprite sprite = owner.GetSprite(dir); // [!code ++]
	if (!sr.sprite) // [!code ++]
	{ // [!code ++]
		sr.sprite = sprite; // [!code ++]
	} // [!code ++]
	sr.flipX = dir == 1 || dir == 3;
	mpb.SetTexture("_MainTex", sprite5.texture); // [!code --]
	Vector4 value = new Vector4(sprite5.textureRect.min.x / (float)sprite5.texture.width, sprite5.textureRect.min.y / (float)sprite5.texture.height, sprite5.textureRect.max.x / (float)sprite5.texture.width, sprite5.textureRect.max.y / (float)sprite5.texture.height); // [!code --]
	mpb.SetTexture("_MainTex", sprite.texture); // [!code ++]
	Vector4 value = new Vector4(sprite.textureRect.min.x / (float)sprite.texture.width, sprite.textureRect.min.y / (float)sprite.texture.height, sprite.textureRect.max.x / (float)sprite.texture.width, sprite.textureRect.max.y / (float)sprite.texture.height); // [!code ++]
	mpb.SetVector("_Rect", value);
	mpb.SetFloat("_PixelHeight", sprite5.rect.height); // [!code --]
	mpb.SetFloat("_PixelHeight", sprite.rect.height); // [!code ++]
	if (!sr2)
	{
		return;
```

[`public virtual void RefreshSprite()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/eaca55f4ae768b36f335481940eb32174f873002/Elin/CardActor.cs#L277-L290)
```cs:line-numbers=277
	if ((bool)sr2.sprite && (bool)sr2.sprite.texture)
	{
		sr2.flipX = dir == 1 || dir == 3;
		sprite5 = sr2.sprite; // [!code --]
		if ((bool)sprite5) // [!code --]
		sprite = sr2.sprite; // [!code ++]
		if ((bool)sprite) // [!code ++]
		{
			_ = sprite5.textureRect; // [!code --]
			value = new Vector4(sprite5.textureRect.min.x / (float)sprite5.texture.width, sprite5.textureRect.min.y / (float)sprite5.texture.height, sprite5.textureRect.max.x / (float)sprite5.texture.width, sprite5.textureRect.max.y / (float)sprite5.texture.height); // [!code --]
			mpb2.SetTexture("_MainTex", sprite5.texture); // [!code --]
			_ = sprite.textureRect; // [!code ++]
			value = new Vector4(sprite.textureRect.min.x / (float)sprite.texture.width, sprite.textureRect.min.y / (float)sprite.texture.height, sprite.textureRect.max.x / (float)sprite.texture.width, sprite.textureRect.max.y / (float)sprite.texture.height); // [!code ++]
			mpb2.SetTexture("_MainTex", sprite.texture); // [!code ++]
			mpb2.SetVector("_Rect", value);
			mpb2.SetFloat("_PixelHeight", sprite5.rect.height); // [!code --]
			mpb2.SetFloat("_PixelHeight", sprite.rect.height); // [!code ++]
		}
	}
	else
```

[`public virtual void RefreshSprite()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/eaca55f4ae768b36f335481940eb32174f873002/Elin/CardActor.cs#L295-L300)
```cs:line-numbers=295

	public virtual void Kill()
	{
		sr.sprite = null; // [!code ++]
		PoolManager.Despawn(base.transform);
	}
}
```

## ConTransmuteHuman

[`public override void OnBeforeStart()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/eaca55f4ae768b36f335481940eb32174f873002/Elin/ConTransmuteHuman.cs#L34-L40)
```cs:line-numbers=34
	{
		List<Chara> list = owner.pos.ListCharasInRadius(owner, 5, delegate(Chara c)
		{
			if (c.IsHumanSpeak) // [!code --]
			if (!c.IsMultisize && c.IsHumanSpeak) // [!code ++]
			{
				CardRenderer renderer = c.renderer;
				if (renderer != null && !renderer.hasActor)
```

## GrowSystem

[`public bool CanGrow(VirtualDate date)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/eaca55f4ae768b36f335481940eb32174f873002/Elin/GrowSystem.cs#L294-L311)
```cs:line-numbers=294
	{
		return false;
	}
	if (GrowUndersea) // [!code --]
	if (EClass._zone.IsUnderwater) // [!code ++]
	{
		if (EClass._zone.IsUnderwater || cell.sourceFloor.tileType.IsDeepWater) // [!code --]
		if (!GrowUndersea) // [!code ++]
		{
			return true; // [!code --]
			return false; // [!code ++]
		}
	}
	else if (EClass._zone.IsUnderwater) // [!code --]
	else if (cell.sourceFloor.tileType.IsDeepWater) // [!code ++]
	{
		return false; // [!code --]
		if (!GrowUndersea) // [!code ++]
		{ // [!code ++]
			return false; // [!code ++]
		} // [!code ++]
	}
	if (!GrowOnLand) // [!code --]
	else if (!GrowOnLand) // [!code ++]
	{
		return false;
	}
```

## LayerPixelPaint

[`public void SetCanvas(TraitCanvas c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/eaca55f4ae768b36f335481940eb32174f873002/Elin/LayerPixelPaint.cs#L36-L41)
```cs:line-numbers=36
		thing.renderer.RefreshSprite();
		Close();
	};
	paint.scale = c.SizeGrid; // [!code ++]
	paint.size = new Vector2Int(c.Width, c.Height);
	InitPaint();
	if (c.owner.c_textureData != null)
```

## LayerShippingResult

[`using System.Collections.Generic;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/eaca55f4ae768b36f335481940eb32174f873002/Elin/LayerShippingResult.cs#L1-L3)
```cs:line-numbers=1
using System.Collections.Generic; // [!code ++]
using DG.Tweening;
using UnityEngine;
using UnityEngine.UI;
```

[`public class LayerShippingResult : ELayer`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/eaca55f4ae768b36f335481940eb32174f873002/Elin/LayerShippingResult.cs#L53-L74)
```cs:line-numbers=53
	private new void Awake()
	{
		Rand.SetSeed(ELayer.game.seed + ELayer.player.stats.days);
		int num = ELayer.rnd(spriteBG.Length); // [!code --]
		if (num == 1 && ELayer.game.cards.globalCharas.Find("corgon") == null) // [!code --]
		List<int> list = new List<int> { 0, 1 }; // [!code ++]
		Add("corgon", 2); // [!code ++]
		Add("loytel", 3); // [!code ++]
		Add("farris", 4); // [!code ++]
		if (ELayer.game.quests.IsCompleted("negotiation_darkness") && !ELayer.player.flags.killedDuponne) // [!code ++]
		{
			num = 0; // [!code --]
			Add("demitas", 6); // [!code ++]
		}
		if (num == 2 && ELayer.game.cards.globalCharas.Find("loytel") == null) // [!code --]
		else // [!code ++]
		{
			num = 0; // [!code --]
		} // [!code --]
		if (num == 3 && ELayer.game.cards.globalCharas.Find("farris") == null) // [!code --]
		{ // [!code --]
			num = 0; // [!code --]
			Add("demitas", 5); // [!code ++]
		}
		int num = list.RandomItem(); // [!code ++]
		imageBG.sprite = spriteBG[num];
		Rand.SetSeed();
		base.Awake();
		void Add(string id, int index) // [!code ++]
		{ // [!code ++]
			Chara chara = ELayer.game.cards.globalCharas.Find(id); // [!code ++]
			if (chara != null && chara.IsPCFaction) // [!code ++]
			{ // [!code ++]
				list.Add(index); // [!code ++]
			} // [!code ++]
		} // [!code ++]
	}

	public void Show()
```

## StanceSongEnd

[`public override void TickSong()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/eaca55f4ae768b36f335481940eb32174f873002/Elin/StanceSongEnd.cs#L17-L23)
```cs:line-numbers=17
			Act act = Element.Create((EClass.rnd(2) == 0) ? 50402 : ((EClass.rnd(2) == 0) ? 50401 : 50400), owner.CHA) as Act;
			ActEffect.ProcAt(EffectId.Hand, act.GetPower(owner), BlessedState.Normal, owner, item, item.pos, isNeg: true, new ActRef
			{
				act = act // [!code --]
				act = act, // [!code ++]
				aliasEle = act.source.aliasRef // [!code ++]
			});
		}
	}
```

## Trait

[`public Emo2 GetRestockedIcon()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/eaca55f4ae768b36f335481940eb32174f873002/Elin/Trait.cs#L1528-L1534)
```cs:line-numbers=1528
		return Emo2.none;
	}

	public void OnBarter(bool reroll = false) // [!code --]
	public virtual void OnBarter(bool reroll = false) // [!code ++]
	{
		Thing t = owner.things.Find("chest_merchant");
		if (t == null)
```

## TraitCanvas

[`public class TraitCanvas : Trait`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/eaca55f4ae768b36f335481940eb32174f873002/Elin/TraitCanvas.cs#L15-L20)
```cs:line-numbers=15

	public virtual int Height => 28;

	public virtual int SizeGrid => 16; // [!code ++]
 // [!code ++]
	public virtual TraitPainter.Type CanvasType => TraitPainter.Type.Paint;

	public override void OnSetCardGrid(ButtonGrid b)
```

## TraitDrawingPaperM

[`public class TraitDrawingPaperM : TraitDrawingPaper`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/eaca55f4ae768b36f335481940eb32174f873002/Elin/TraitDrawingPaperM.cs#L3-L6)
```cs:line-numbers=3
	public override int Width => 64;

	public override int Height => 64;
 // [!code ++]
	public override int SizeGrid => 15; // [!code ++]
}
```

## TraitToolWaterCan

[`public class TraitToolWaterCan : TraitTool`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/eaca55f4ae768b36f335481940eb32174f873002/Elin/TraitToolWaterCan.cs#L8-L19)
```cs:line-numbers=8

	public override void TrySetHeldAct(ActPlan p)
	{
		if (p.TrySetAct(new ActDrawWater // [!code ++]
		{ // [!code ++]
			waterCan = this // [!code ++]
		}, owner)) // [!code ++]
		{ // [!code ++]
			return; // [!code ++]
		} // [!code ++]
		foreach (Chara item in p.pos.ListCharas()) // [!code ++]
		{ // [!code ++]
			if (item.IsPCFactionOrMinion && item.HasCondition<ConBurning>() && p.TrySetAct(new ActWater // [!code ++]
			{ // [!code ++]
				waterCan = this // [!code ++]
			}, owner)) // [!code ++]
			{ // [!code ++]
				return; // [!code ++]
			} // [!code ++]
		} // [!code ++]
		if (!p.TrySetAct(new TaskWater
		{
			dest = p.pos
		}, owner) && !p.TrySetAct(new ActDrawWater // [!code --]
		{ // [!code --]
			waterCan = this // [!code --]
		}, owner))
		{
			p.TrySetAct(new ActWater
```
