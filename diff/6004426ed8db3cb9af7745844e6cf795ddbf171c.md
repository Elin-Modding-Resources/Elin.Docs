---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 18 files modified.
version: EA 23.343.1 Nightly
changes: AI_UseCrafter/AM_Paint/Card/CardActor/Chara/CharaActor/CharaRenderer/Core/DNA/LayerEditPlaylist/LayerMod/ModManager/SourcePref/SpriteData/SurvivalManager/TCUI/Trait/Zone
---

# EA 23.343.1 Nightly

September 7, 2026

18 files modified.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [LayerMod (4)](#layermod)
```cs:no-line-numbers
private static void AddPreviewBox(UINote n, Sprite sprite, Action onClick) // [!code --]

```
```cs:no-line-numbers
private static void FitValue(UIItem item) // [!code --]

```
```cs:no-line-numbers
private static void AddIdList(UINote n, string lang, string[][] rows) // [!code --]

```
```cs:no-line-numbers
private void CreatePresetUI() // [!code --]

```
## AI_UseCrafter

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/AI_UseCrafter.cs#L273-L279)
```cs:line-numbers=273
	}
}
Rand.SetSeed();
if (crafter is TraitCookerMicrowave && recipe.id == "onsentamago" && EClass.rnd(3) != 0) // [!code --]
if (owner != null && crafter is TraitCookerMicrowave && recipe.id == "onsentamago" && EClass.rnd(3) != 0) // [!code ++]
{
	int power = EClass.curve((200 + ings[0].Quality * 5) * (100 + owner.Evalue(287) * 10) / 100, 400, 100);
	ActEffect.ProcAt(EffectId.Explosive, power, BlessedState.Normal, crafter.owner.ExistsOnMap ? crafter.owner : owner, owner, owner.pos, isNeg: true, new ActRef
```

## AM_Paint

[`public override void OnUpdateInput()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/AM_Paint.cs#L58-L64)
```cs:line-numbers=58
srRect.transform.position = Camera.main.ScreenToWorldPoint(mousePosition).SetZ(-100f);
float num = 0.02f / EClass.screen.Zoom;
Sprite sprite = canvas.owner.GetSprite();
srRect.transform.localScale = new Vector3((float)sprite.texture.width * num, (float)sprite.texture.height * num, 1f); // [!code --]
srRect.transform.localScale = new Vector3(SpriteData.BaseWidth(sprite) * num, SpriteData.BaseHeight(sprite) * num, 1f); // [!code ++]
if (!EInput.leftMouse.clicked)
{
	return;
```

## Card

[`public Card SetLv(int a)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/Card.cs#L3075-L3081)
```cs:line-numbers=3075
{
	return this;
}
if (LV >= Mathf.Max(50, sourceCard.LV)) // [!code --]
if (LV > Mathf.Max(50, sourceCard.LV)) // [!code ++]
{
	isScaled = true;
}
```

[`public virtual void SetImage(Image image)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/Card.cs#L6598-L6604)
```cs:line-numbers=6598
if (spriteReplacer != null)
{
	sprite = spriteReplacer.GetSprite();
	RenderData renderData = ResourceCache.Load<RenderData>("Scene/Render/Data/chara_custom_" + ((sprite.texture.height <= 128) ? "128" : "256")); // [!code --]
	RenderData renderData = ResourceCache.Load<RenderData>("Scene/Render/Data/chara_custom_" + ((SpriteData.BaseHeight(sprite) <= 128f) ? "128" : "256")); // [!code ++]
	sourceRenderCard.SetImage(image, sprite, colorInt, setNativeSize: true, 0, 0, renderData, spriteReplacer.data.pref ?? Pref);
	return;
}
```

[`public byte[] GetPaintData()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/Card.cs#L7556-L7563)
```cs:line-numbers=7556
{
	Sprite sprite = GetSprite();
	Texture2D texture2D = ScreenCapture.CaptureScreenshotAsTexture();
	int num = sprite.texture.width * 2; // [!code --]
	int num2 = sprite.texture.height * 2; // [!code --]
	int num = (int)(SpriteData.BaseWidth(sprite) * 2f); // [!code ++]
	int num2 = (int)(SpriteData.BaseHeight(sprite) * 2f); // [!code ++]
	int x = (int)Mathf.Clamp(Input.mousePosition.x - (float)(num / 2), 1f, texture2D.width - num - 1);
	int y = (int)Mathf.Clamp(Input.mousePosition.y - (float)(num2 / 2), 1f, texture2D.height - num2 - 1);
	Color[] pixels = texture2D.GetPixels(x, y, num, num2);
```

## CardActor

[`public virtual void OnRender(RenderParam p)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/CardActor.cs#L230-L235)
```cs:line-numbers=230
	}
}

public virtual void ReloadSprite() // [!code ++]
{ // [!code ++]
	sr.sprite = null; // [!code ++]
	RefreshSprite(); // [!code ++]
} // [!code ++]
 // [!code ++]
public virtual void RefreshSprite()
{
	switch (type)
```

## Chara

[`public override SourcePref Pref`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/Chara.cs#L810-L816)
```cs:line-numbers=810
	if (spriteReplacer.data.pref == null && !spriteReplacer.data.tryFixPrefNotLoadedAtStart)
	{
		spriteReplacer.data.tryFixPrefNotLoadedAtStart = true;
		spriteReplacer.data.LoadPref(); // [!code --]
		spriteReplacer.data.LoadPrefAndFixDensity(); // [!code ++]
	}
	return spriteReplacer.data?.pref ?? EClass.core.refs.prefs.replacer1;
}
```

## CharaActor

[`public override void NextFrame()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/CharaActor.cs#L22-L27)
```cs:line-numbers=22
	RefreshSprite();
}

public override void ReloadSprite() // [!code ++]
{ // [!code ++]
	if (owner != null) // [!code ++]
	{ // [!code ++]
		sr.sprite = owner.GetSprite(); // [!code ++]
		RefreshSprite(); // [!code ++]
	} // [!code ++]
} // [!code ++]
 // [!code ++]
public override void RefreshSprite()
{
	Sprite sprite = sr.sprite;
```

## CharaRenderer

[`public override void SetOwner(Card c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/CharaRenderer.cs#L69-L75)
```cs:line-numbers=69
	SpriteReplacer spriteReplacer = SpriteReplacer.dictSkins.TryGetValue(owner.c_idSpriteReplacer);
	if (spriteReplacer != null)
	{
		data = ResourceCache.Load<RenderData>("Scene/Render/Data/chara_custom_" + ((spriteReplacer.GetSprite().texture.height <= 128) ? "128" : "256")); // [!code --]
		data = ResourceCache.Load<RenderData>("Scene/Render/Data/chara_custom_" + ((SpriteData.BaseHeight(spriteReplacer.GetSprite()) <= 128f) ? "128" : "256")); // [!code ++]
		owner.spriteReplacer = spriteReplacer;
	}
}
```

## Core

[`public void OnApplicationFocus(bool focus)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/Core.cs#L450-L456)
```cs:line-numbers=450
	}
	foreach (SpriteReplacer value in SpriteReplacer.dictSkins.Values)
	{
		value.data?.LoadPref(); // [!code --]
		value.data?.LoadPrefAndFixDensity(); // [!code ++]
	} // [!code ++]
	if (RenderObject.syncList != null) // [!code ++]
	{ // [!code ++]
		foreach (ISyncScreen sync in RenderObject.syncList) // [!code ++]
		{ // [!code ++]
			if (sync is CardRenderer { hasActor: not false } cardRenderer && !cardRenderer.actor.isPCC) // [!code ++]
			{ // [!code ++]
				cardRenderer.actor.ReloadSprite(); // [!code ++]
			} // [!code ++]
		} // [!code ++]
	}
	WidgetRoster.SetDirty();
}
```

## DNA

[`public static Thing GenerateManiGene(Card owner)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/DNA.cs#L167-L174)
```cs:line-numbers=167
CardRow r = SpawnList.Get("chara").Select(100);
Rand.SetSeed(owner.c_seed);
Thing thing = GenerateGene(r, Type.Superior, owner.LV, owner.c_seed);
thing.c_DNA.cost = thing.c_DNA.cost / 2; // [!code --]
thing.c_DNA.cost = 0; // [!code ++]
thing.c_DNA.isManiGene = true;
thing.ChangeMaterial(36); // [!code ++]
thing.MakeRefFrom("mani");
Rand.SetSeed();
owner.c_seed++;
```

## LayerEditPlaylist

[`public class ListBGM : ListOwner<BGMData, ItemGeneral>`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/LayerEditPlaylist.cs#L18-L22)
```cs:line-numbers=18
public bool single;

public bool sort = true; // [!code ++]
 // [!code ++]
public new LayerEditPlaylist layer => base.layer as LayerEditPlaylist;

public override string IdTitle => idTitle;
```

[`public override void List()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/LayerEditPlaylist.cs#L77-L83)
```cs:line-numbers=77
},
onList = delegate
{
	items.Sort((BGMData a, BGMData b) => a.id - b.id); // [!code --]
	if (sort) // [!code ++]
	{ // [!code ++]
		items.Sort((BGMData a, BGMData b) => a.id - b.id); // [!code ++]
	} // [!code ++]
	foreach (BGMData item in items)
	{
		list.Add(item);
```

[`public void Refresh()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/LayerEditPlaylist.cs#L210-L216)
```cs:line-numbers=210
multi.AddOwner(0, new ListBGM
{
	items = list,
	idTitle = "wPlaylist" // [!code --]
	idTitle = "wPlaylist", // [!code ++]
	sort = false // [!code ++]
});
multi.AddOwner(1, new ListBGM
{
```

## LayerMod

[`public void OnPointerEnter(PointerEventData eventData)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/LayerMod.cs#L29-L36)
```cs:line-numbers=29
	}
}

private const float previewBoxHeight = 240f; // [!code --]
 // [!code --]
private const string urlWorkshopItem = "https://steamcommunity.com/sharedfiles/filedetails/?id=";

public static LayerMod Instance;
```

[`public void OnPointerEnter(PointerEventData eventData)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/LayerMod.cs#L56-L60)
```cs:line-numbers=56
public UINote panelNote;

public UIItem infoTitle; // [!code ++]
 // [!code ++]
public UIItem infoPreview; // [!code ++]
 // [!code ++]
public UIItem infoPreviewNote; // [!code ++]
 // [!code ++]
public UIItem infoAuthor; // [!code ++]
 // [!code ++]
public UIItem infoVersion; // [!code ++]
 // [!code ++]
public UIItem infoId; // [!code ++]
 // [!code ++]
public UIItem infoWorkshop; // [!code ++]
 // [!code ++]
public UIItem infoStatus; // [!code ++]
 // [!code ++]
public UIItem infoProblems; // [!code ++]
 // [!code ++]
public UIItem infoTags; // [!code ++]
 // [!code ++]
public UIItem infoRequires; // [!code ++]
 // [!code ++]
public UIItem infoIncompatible; // [!code ++]
 // [!code ++]
public UIItem infoLoadAfter; // [!code ++]
 // [!code ++]
public UIItem infoLoadBefore; // [!code ++]
 // [!code ++]
public UIItem infoDescription; // [!code ++]
 // [!code ++]
public GameObject infoSpace; // [!code ++]
 // [!code ++]
private UIButton buttonFilter;

private BaseModPackage panelTarget;
```

[`public override void OnInit()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/LayerMod.cs#L148-L155)
```cs:line-numbers=148
list.callbacks = CreateCallbacks(list, builtin: false);
list2.callbacks = CreateCallbacks(list2, builtin: true);
InitSearchUI();
CreatePresetUI(); // [!code --]
panelHeader.SetText("info".lang()); // [!code --]
windows[0].AddBottomButton("mod_preset", ShowPresetMenu); // [!code ++]
infoPreview.button1.SetOnClick(delegate // [!code ++]
{ // [!code ++]
	ShowPreview(panelTarget); // [!code ++]
}); // [!code ++]
RefreshLists();
ShowInfo(FirstInfoTarget());
list.dragEdgeSize = list.callbacks.GetMold()?.Rect().sizeDelta.y ?? 34f;
```

[`private void InitSearchUI()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/LayerMod.cs#L328-L342)
```cs:line-numbers=328
	if ((bool)buttonClearSearch)
	{
		buttonClearSearch.SetOnClick(ClearSearchText);
		buttonClearSearch.SetActive(enable: false); // [!code --]
	}
	buttonFilter = windows[0].AddBottomButton("mod_filter", CycleFilter);
	RefreshFilterButton();
	if ((bool)textNoResult) // [!code --]
	{ // [!code --]
		textNoResult.SetText("noResult".lang(), FontColor.Passive); // [!code --]
		textNoResult.SetActive(enable: false); // [!code --]
	} // [!code --]
}

public void Search(string s)
```

[`private BaseModPackage FirstInfoTarget()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/LayerMod.cs#L510-L642)
```cs:line-numbers=510
private void ShowInfo(BaseModPackage p)
{
	if (!panelNote || p == null || p == panelTarget) // [!code --]
	{ // [!code --]
		return; // [!code --]
	} // [!code --]
	panelTarget = p; // [!code --]
	UINote n = panelNote; // [!code --]
	n.Clear(); // [!code --]
	n.AddHeader(p.title.IsEmpty(p.dirInfo.Name)); // [!code --]
	PreviewEntry thumb = GetThumb(p); // [!code --]
	if ((bool)thumb.sprite) // [!code --]
	if (p != null && p != panelTarget) // [!code ++]
	{
		AddPreviewBox(n, thumb.sprite, delegate // [!code --]
		panelTarget = p; // [!code ++]
		infoTitle.text1.SetText(p.title.IsEmpty(p.dirInfo.Name)); // [!code ++]
		PreviewEntry thumb = GetThumb(p); // [!code ++]
		infoPreview.SetActive(thumb.sprite); // [!code ++]
		if ((bool)thumb.sprite) // [!code ++]
		{
			ShowPreview(p); // [!code --]
		}); // [!code --]
		if (thumb.kind == ModPreview.PreviewType.Gif) // [!code --]
			infoPreview.image1.sprite = thumb.sprite; // [!code ++]
		} // [!code ++]
		SetInfo(value: (thumb.sprite ? ((thumb.kind == ModPreview.PreviewType.Gif) ? "mod_preview_gif" : null) : ((thumb.kind == ModPreview.PreviewType.None) ? "mod_preview_none" : "mod_preview_unsupported"))?.lang(), item: infoPreviewNote); // [!code ++]
		SetTopic(infoAuthor, p.author, null); // [!code ++]
		SetTopic(infoVersion, p.version, null); // [!code ++]
		SetTopic(infoId, p.id, null); // [!code ++]
		string text = ModLoadOrderPreset.WorkshopId(p); // [!code ++]
		SetTopic(infoWorkshop, text.IsEmpty() ? p.dirInfo.Name : text, text.IsEmpty() ? "mod_filter_local" : "mod_filter_workshop"); // [!code ++]
		SetTopic(infoStatus, (!p.builtin && !p.willActivate) ? "mod_filter_disabled".lang() : null, null); // [!code ++]
		List<string> list = new List<string>(); // [!code ++]
		if (p.blockedBy != null) // [!code ++]
		{
			n.AddText("mod_preview_gif".lang(), FontColor.Passive); // [!code --]
			list.Add("mod_info_blocked".lang(p.blockedBy.title.IsEmpty(p.blockedBy.id))); // [!code ++]
		}
		if (!p.langDepError.IsEmpty()) // [!code ++]
		{ // [!code ++]
			string[] array = p.langDepError.Split('\t'); // [!code ++]
			list.Add(array[0].lang(string.Join("mod_info_or".lang(), array, 1, array.Length - 1))); // [!code ++]
		} // [!code ++]
		if (p.duplicateOf != null) // [!code ++]
		{ // [!code ++]
			list.Add("mod_info_duplicate".lang(p.duplicateOf.title.IsEmpty(p.duplicateOf.dirInfo?.Name).IsEmpty(p.duplicateOf.id))); // [!code ++]
		} // [!code ++]
		if (!p.IsValidVersion()) // [!code ++]
		{ // [!code ++]
			list.Add("mod_info_old_version".lang(p.version, ELayer.core.versionMod.GetText())); // [!code ++]
		} // [!code ++]
		if (!p.parseError.IsEmpty()) // [!code ++]
		{ // [!code ++]
			list.Add("mod_info_parse_error".lang(p.parseError)); // [!code ++]
		} // [!code ++]
		SetInfo(infoProblems, string.Join("\n", list)); // [!code ++]
		UIItem item = infoTags; // [!code ++]
		string[] tags = p.tags; // [!code ++]
		SetTopic(item, (tags != null && tags.Length > 0) ? string.Join(", ", p.tags) : null, null); // [!code ++]
		SetTopic(infoRequires, IdList(p.dependency, alternatives: true), null); // [!code ++]
		SetTopic(infoIncompatible, IdList(p.incompatible, alternatives: false), null); // [!code ++]
		SetTopic(infoLoadAfter, IdList(p.loadAfter, alternatives: false), null); // [!code ++]
		SetTopic(infoLoadBefore, IdList(p.loadBefore, alternatives: false), null); // [!code ++]
		infoSpace.SetActive(!p.description.IsEmpty()); // [!code ++]
		SetInfo(infoDescription, p.description); // [!code ++]
		panelNote.Build(); // [!code ++]
		panelScroll.content.anchoredPosition = Vector2.zero; // [!code ++]
	} // [!code ++]
	static string IdList(string[][] rows, bool alternatives) // [!code ++]
	{ // [!code ++]
		if (rows == null) // [!code ++]
		{ // [!code ++]
			return null; // [!code ++]
		} // [!code ++]
		string sep = (alternatives ? "mod_info_or".lang() : "\n"); // [!code ++]
		return string.Join("\n", from row in rows // [!code ++]
			where row != null && row.Length > 0 // [!code ++]
			select string.Join(sep, row.Where((string id) => !id.IsEmpty())) into line // [!code ++]
			where !line.IsEmpty() // [!code ++]
			select line); // [!code ++]
	}
	else // [!code --]
	{ // [!code --]
		n.AddText(((thumb.kind == ModPreview.PreviewType.None) ? "mod_preview_none" : "mod_preview_unsupported").lang(), FontColor.Passive); // [!code --]
	} // [!code --]
	if (!p.author.IsEmpty()) // [!code --]
	{ // [!code --]
		AddTopic("author", p.author); // [!code --]
	} // [!code --]
	if (!p.version.IsEmpty()) // [!code --]
	{ // [!code --]
		AddTopic("version", p.version); // [!code --]
	} // [!code --]
	if (!p.id.IsEmpty()) // [!code --]
	{ // [!code --]
		FitValue(AddTopic("mod_info_id", p.id)); // [!code --]
	} // [!code --]
	string text = ModLoadOrderPreset.WorkshopId(p); // [!code --]
	FitValue(AddTopic(text.IsEmpty() ? "mod_filter_local" : "mod_filter_workshop", text.IsEmpty() ? p.dirInfo.Name : text)); // [!code --]
	if (!p.builtin && !p.willActivate) // [!code --]
	{ // [!code --]
		AddTopic("status", "mod_filter_disabled".lang()); // [!code --]
	} // [!code --]
	if (p.blockedBy != null) // [!code --]
	{ // [!code --]
		n.AddText("mod_info_blocked".lang(p.blockedBy.title.IsEmpty(p.blockedBy.id)), FontColor.Bad); // [!code --]
	} // [!code --]
	if (!p.langDepError.IsEmpty()) // [!code --]
	{ // [!code --]
		n.AddText(p.langDepError, FontColor.Bad); // [!code --]
	} // [!code --]
	if (p.duplicateOf != null) // [!code --]
	{ // [!code --]
		n.AddText("mod_info_duplicate".lang(p.duplicateOf.dirInfo?.Name ?? p.duplicateOf.id), FontColor.Bad); // [!code --]
	} // [!code --]
	if (!p.IsValidVersion()) // [!code --]
	{ // [!code --]
		n.AddText("mod_info_old_version".lang(p.version, ELayer.core.versionMod.GetText()), FontColor.Bad); // [!code --]
	} // [!code --]
	if (!p.parseError.IsEmpty()) // [!code --]
	{ // [!code --]
		n.AddText(p.parseError, FontColor.Bad); // [!code --]
	} // [!code --]
	string[] tags = p.tags; // [!code --]
	if (tags != null && tags.Length > 0) // [!code --]
	{ // [!code --]
		AddTopic("mod_info_tags", string.Join(", ", p.tags)); // [!code --]
	} // [!code --]
	AddIdList(n, "mod_info_requires", p.dependency); // [!code --]
	AddIdList(n, "incompatible", p.incompatible); // [!code --]
	AddIdList(n, "mod_info_load_after", p.loadAfter); // [!code --]
	AddIdList(n, "mod_info_load_before", p.loadBefore); // [!code --]
	if (!p.description.IsEmpty()) // [!code --]
	{ // [!code --]
		n.Space(8); // [!code --]
		n.AddText(p.description); // [!code --]
	} // [!code --]
	n.Build(); // [!code --]
	panelScroll.content.anchoredPosition = Vector2.zero; // [!code --]
	UIItem AddTopic(string lang, string value) // [!code --]
	{ // [!code --]
		UIItem uIItem = n.AddTopic(lang, value); // [!code --]
		uIItem.text1.alignment = TextAnchor.LowerLeft; // [!code --]
		return uIItem; // [!code --]
	} // [!code --]
} // [!code --]
 // [!code --]
private static void AddPreviewBox(UINote n, Sprite sprite, Action onClick) // [!code --]
{ // [!code --]
	n.AddImage(sprite); // [!code --]
	RectTransform rectTransform = n.transform.GetChild(n.transform.childCount - 1).Rect(); // [!code --]
	rectTransform.sizeDelta = new Vector2(rectTransform.sizeDelta.x, 240f); // [!code --]
	Image componentInChildren = rectTransform.GetComponentInChildren<Image>(); // [!code --]
	RectTransform rectTransform2 = componentInChildren.rectTransform; // [!code --]
	rectTransform2.anchorMin = Vector2.zero; // [!code --]
	rectTransform2.anchorMax = Vector2.one; // [!code --]
	rectTransform2.pivot = new Vector2(0.5f, 0.5f); // [!code --]
	rectTransform2.anchoredPosition = Vector2.zero; // [!code --]
	rectTransform2.sizeDelta = Vector2.zero; // [!code --]
	componentInChildren.preserveAspect = true; // [!code --]
	Button button = componentInChildren.gameObject.AddComponent<Button>(); // [!code --]
	button.transition = Selectable.Transition.None; // [!code --]
	button.onClick.AddListener(delegate // [!code --]
	static void SetInfo(UIItem uIItem, string value) // [!code ++]
	{
		onClick(); // [!code --]
	}); // [!code --]
} // [!code --]
 // [!code --]
private static void FitValue(UIItem item) // [!code --]
{ // [!code --]
	if ((bool)item && (bool)item.text2) // [!code --]
	{ // [!code --]
		item.text2.resizeTextForBestFit = true; // [!code --]
		item.text2.horizontalOverflow = HorizontalWrapMode.Wrap; // [!code --]
		item.text2.resizeTextMinSize = 9; // [!code --]
		item.text2.resizeTextMaxSize = item.text2.fontSize; // [!code --]
		uIItem.SetActive(!value.IsEmpty()); // [!code ++]
		if (!value.IsEmpty()) // [!code ++]
		{ // [!code ++]
			uIItem.text1.SetText(value); // [!code ++]
		} // [!code ++]
	}
} // [!code --]
 // [!code --]
private static void AddIdList(UINote n, string lang, string[][] rows) // [!code --]
{ // [!code --]
	if (rows != null && rows.Length != 0) // [!code --]
	static void SetTopic(UIItem uIItem, string value, string lang) // [!code ++]
	{
		List<string> list = (from row in rows // [!code --]
			where row != null && row.Length > 0 && !row[0].IsEmpty() // [!code --]
			select row[0]).ToList(); // [!code --]
		if (list.Count > 0) // [!code --]
		uIItem.SetActive(!value.IsEmpty()); // [!code ++]
		if (!value.IsEmpty()) // [!code ++]
		{
			n.AddTopic(lang, string.Join(", ", list)).text1.alignment = TextAnchor.LowerLeft; // [!code --]
			if (lang != null) // [!code ++]
			{ // [!code ++]
				uIItem.text1.SetText(lang.lang()); // [!code ++]
			} // [!code ++]
			uIItem.text2.SetText(value); // [!code ++]
		}
	}
}
```

[`private void ShowPreview(BaseModPackage a)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/LayerMod.cs#L666-L676)
```cs:line-numbers=666
	});
}

private void CreatePresetUI() // [!code --]
{ // [!code --]
	windows[0].AddBottomButton("mod_preset", ShowPresetMenu); // [!code --]
} // [!code --]
 // [!code --]
private void ShowPresetMenu()
{
	SE.Click();
```

## ModManager

[`private string UnmetDependency(BaseModPackage p, Dictionary<string, BaseModPacka…)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/ModManager.cs#L595-L607)
```cs:line-numbers=595
	{
		if (baseModPackage != null)
		{
			return "requires " + baseModPackage.title.IsEmpty(baseModPackage.dirInfo?.Name) + ", which loads after this mod - move it above"; // [!code --]
			return "mod_info_dep_order\t" + baseModPackage.title.IsEmpty(baseModPackage.dirInfo?.Name); // [!code ++]
		}
		if (baseModPackage2 != null)
		{
			return "requires " + baseModPackage2.title.IsEmpty(baseModPackage2.dirInfo?.Name) + ", which is installed but did not load"; // [!code --]
			return "mod_info_dep_failed\t" + baseModPackage2.title.IsEmpty(baseModPackage2.dirInfo?.Name); // [!code ++]
		}
		return "requires " + string.Join(" or ", array) + ", which is not installed"; // [!code --]
		return "mod_info_dep_missing\t" + string.Join('\t', array); // [!code ++]
	}
}
return null;
```

## SourcePref

[`[Serializable]`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/SourcePref.cs#L9-L15)
```cs:line-numbers=9
[Serializable]
public class SourcePref : EClass, ISerializationCallbackReceiver
{
	public const int TotalPrefInts = 26; // [!code ++]
 // [!code ++]
	[JsonProperty]
	public int[] ints = new int[25]; // [!code --]
	public int[] ints = new int[26]; // [!code ++]

	public BitArray32 _bits1;
```

[`public float heightFix`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/SourcePref.cs#L323-L328)
```cs:line-numbers=323
	}
}

public int scaleTex // [!code ++]
{ // [!code ++]
	get // [!code ++]
	{ // [!code ++]
		return ints[25]; // [!code ++]
	} // [!code ++]
	set // [!code ++]
	{ // [!code ++]
		ints[25] = value; // [!code ++]
	} // [!code ++]
} // [!code ++]
 // [!code ++]
public float ScaleTex // [!code ++]
{ // [!code ++]
	get // [!code ++]
	{ // [!code ++]
		Validate(); // [!code ++]
		if (ints[25] > 0) // [!code ++]
		{ // [!code ++]
			return (float)ints[25] * 0.01f; // [!code ++]
		} // [!code ++]
		return 1f; // [!code ++]
	} // [!code ++]
} // [!code ++]
 // [!code ++]
public bool bypassShadow
{
	get
```

[`public bool bypassShadow`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/SourcePref.cs#L346-L375)
```cs:line-numbers=346
public void OnAfterDeserialize()
{
	if (ints.Length >= 25) // [!code --]
	{ // [!code --]
		_bits1.SetInt(ints[22]); // [!code --]
	} // [!code --]
	else // [!code --]
	{ // [!code --]
		Validate(); // [!code --]
	} // [!code --]
	Validate(); // [!code ++]
	_bits1.SetInt(ints[22]); // [!code ++]
}

public void OnBeforeSerialize()
{
	Validate(); // [!code ++]
	ints[22] = _bits1.ToInt();
}

public void Validate()
{
	if (ints.Length < 25) // [!code --]
	if (ints.Length < 26) // [!code ++]
	{
		Array.Resize(ref ints, 25); // [!code --]
		Array.Resize(ref ints, 26); // [!code ++]
	}
}

public void WriteIni(string path)
{
	Validate(); // [!code ++]
	FileIniDataParser fileIniDataParser = new FileIniDataParser();
	if (File.Exists(path))
	{
```

[`public void WriteIni(string path)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/SourcePref.cs#L395-L400)
```cs:line-numbers=395
iniData.Global["height"] = ints[3].ToString();
iniData.Global["heightFix"] = ints[24].ToString();
iniData.Global["scaleIcon"] = scaleIcon.ToString();
iniData.Global["scaleTex"] = scaleTex.ToString(); // [!code ++]
iniData.Global["liquidMod"] = liquidMod.ToString();
iniData.Global["liquidModMax"] = liquidModMax.ToString();
iniData.Global["hatY"] = ints[23].ToString();
```

[`public static SourcePref ReadFromIni(string path)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/SourcePref.cs#L425-L430)
```cs:line-numbers=425
sourcePref.ints[3] = iniData.Global["height"].ToInt();
sourcePref.ints[24] = iniData.Global["heightFix"].ToInt();
sourcePref.scaleIcon = iniData.Global["scaleIcon"].ToInt();
sourcePref.scaleTex = iniData.Global["scaleTex"].ToInt(); // [!code ++]
sourcePref.liquidMod = iniData.Global["liquidMod"].ToInt();
sourcePref.liquidModMax = iniData.Global["liquidModMax"].ToInt();
sourcePref.ints[23] = iniData.Global["hatY"].ToInt();
```

## SpriteData

[`public class SpriteData`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/SpriteData.cs#L8-L12)
```cs:line-numbers=8
public class SpriteData
{
	public const float DefaultPPU = 100f; // [!code ++]
 // [!code ++]
	public string id;

	public string path;
```

[`public class SpriteData`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/SpriteData.cs#L32-L36)
```cs:line-numbers=32
public bool tryFixPrefNotLoadedAtStart;

public static float BaseWidth(Sprite s) // [!code ++]
{ // [!code ++]
	return s.rect.width * 100f / s.pixelsPerUnit; // [!code ++]
} // [!code ++]
 // [!code ++]
public static float BaseHeight(Sprite s) // [!code ++]
{ // [!code ++]
	return s.rect.height * 100f / s.pixelsPerUnit; // [!code ++]
} // [!code ++]
 // [!code ++]
public void Init()
{
	try
```

[`public void LoadPref()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/SpriteData.cs#L55-L60)
```cs:line-numbers=55
	}
}

public void LoadPrefAndFixDensity() // [!code ++]
{ // [!code ++]
	float a = pref?.ScaleTex ?? 1f; // [!code ++]
	LoadPref(); // [!code ++]
	if (sprites != null && !Mathf.Approximately(a, pref?.ScaleTex ?? 1f)) // [!code ++]
	{ // [!code ++]
		LoadSprites(); // [!code ++]
	} // [!code ++]
} // [!code ++]
 // [!code ++]
public void LoadAnimationIni()
{
	if (!File.Exists(path + ".ini"))
```

[`public void LoadSprites()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/SpriteData.cs#L97-L109)
```cs:line-numbers=97
	}
	int num = tex.width / frame;
	int height = tex.height;
	if (sprites == null || sprites.Length != frame) // [!code --]
	float num2 = 100f * (pref?.ScaleTex ?? 1f); // [!code ++]
	if (sprites == null || sprites.Length != frame || !sprites[0] || !Mathf.Approximately(sprites[0].rect.width, num) || !Mathf.Approximately(sprites[0].rect.height, height) || !Mathf.Approximately(sprites[0].pixelsPerUnit, num2)) // [!code ++]
	{
		sprites = new Sprite[frame];
	} // [!code --]
	Vector2 pivot = new Vector2(0.5f, 0.5f); // [!code --]
	for (int i = 0; i < frame; i++) // [!code --]
	{ // [!code --]
		sprites[i] = Sprite.Create(tex, new Rect(i * num, 0f, num, height), pivot, 100f, 0u, SpriteMeshType.FullRect); // [!code --]
		Vector2 pivot = new Vector2(0.5f, 0.5f); // [!code ++]
		for (int i = 0; i < frame; i++) // [!code ++]
		{ // [!code ++]
			sprites[i] = Sprite.Create(tex, new Rect(i * num, 0f, num, height), pivot, num2, 0u, SpriteMeshType.FullRect); // [!code ++]
		} // [!code ++]
	}
}
```

## SurvivalManager

[`bool Next()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/SurvivalManager.cs#L455-L461)
```cs:line-numbers=455
Point pos2 = point.GetNearestPoint(allowBlock: false, allowChara: false, allowInstalled: false, ignoreCenter: true) ?? point;
if (searchWreck2 == 20)
{
	Thing container = ThingGen.CreateParcel(null, ThingGen.CreateRecipe("container_shipping"), ThingGen.CreateRecipe("stonecutter"), ThingGen.CreateRecipe("workbench2"), ThingGen.Create("crystal_earth").SetNum(3)); // [!code --]
	Thing container = ThingGen.CreateParcel(null, ThingGen.CreateRecipe("container_shipping"), ThingGen.CreateRecipe("stonecutter"), ThingGen.CreateRecipe("factory_stone"), ThingGen.CreateRecipe("workbench2"), ThingGen.Create("crystal_earth").SetNum(3)); // [!code ++]
	Meteor(pos2, delegate
	{
		EClass._zone.AddCard(container, pos2);
```

## TCUI

[`public override void OnDraw(ref Vector3 pos)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/TCUI.cs#L29-L35)
```cs:line-numbers=29
Vector3 vector = FixPos;
if (render != null && render.hasActor && !render.actor.isPCC && (bool)render.actor && (bool)render.actor.sr && (bool)render.actor.sr.sprite)
{
	float num = 128f / render.actor.sr.sprite.rect.height; // [!code --]
	float num = 128f / SpriteData.BaseHeight(render.actor.sr.sprite); // [!code ++]
	int pivotY = render.owner.Pref.pivotY;
	vector = FixPos + new Vector3(0f, num * ((float)pivotY - 48f), 0f);
}
```

## Trait

[`public virtual void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/Trait.cs#L2337-L2342)
```cs:line-numbers=2337
	}
	break;
}
ShopType shopType = ShopType; // [!code ++]
if ((uint)(shopType - 24) <= 1u && EClass._zone is Zone_Yowyn) // [!code ++]
{ // [!code ++]
	Add("milk_kumiromi", EClass.rndHalf(6), 0); // [!code ++]
} // [!code ++]
switch (owner.id)
{
case "rodwyn":
```

## Zone

[`public Chara SpawnMob(Point pos = null, SpawnSetting setting = null)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/Zone.cs#L3069-L3075)
```cs:line-numbers=3069
	return null;
}
long num3 = ((setting.fixedLv == -1) ? cardRow.LV : setting.fixedLv);
bool flag = setting.fixedLv != -1 || DangerLvBoost > 0; // [!code --]
bool flag = setting.fixedLv != -1 || setting.addLv > 0 || DangerLvBoost > 0; // [!code ++]
num3 += setting.addLv;
if (ScaleType == ZoneScaleType.Void)
{
```

[`public Chara SpawnMob(Point pos = null, SpawnSetting setting = null)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6004426ed8db3cb9af7745844e6cf795ddbf171c/Elin/Zone.cs#L3077-L3082)
```cs:line-numbers=3077
	flag = true;
}
num3 += DangerLvBoost;
if (flag && num3 <= Mathf.Max(50, cardRow.LV)) // [!code ++]
{ // [!code ++]
	flag = false; // [!code ++]
} // [!code ++]
if (setting.rarity == Rarity.Random && cardRow.quality == 0)
{
	if (EClass.rnd(EClass.pc.HasElement(1271) ? 80 : 100) == 0)
```
