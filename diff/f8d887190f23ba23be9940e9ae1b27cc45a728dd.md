---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 10 files modified.
version: +EA 23.283 Nightly - Plugin.BaseCore
changes: BaseModManager/BaseModPackage/ClassCache/ClassExtension/EAction/EInput/ExcelParser/Lang/MOD/SourceData
---

# +EA 23.283 Nightly - Plugin.BaseCore

March 4, 2026

10 files modified.

## Important Changes

**None.**
## BaseModManager

[`using System;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/BaseModManager.cs#L1-L6)
```cs:line-numbers=1
using System;
using System.Collections.Generic;
using System.IO;
using System.Linq; // [!code ++]
using UnityEngine;

public class BaseModManager
```

[`public void Parse<T>(ModItemList<T> list) where T : UnityEngine.Object`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/BaseModManager.cs#L31-L36)
```cs:line-numbers=31

	public static List<string> listChainLoad = new List<string>();

	private static readonly Dictionary<string, HashSet<Action<object>>> _eventHandlers = new Dictionary<string, HashSet<Action<object>>>(); // [!code ++]
 // [!code ++]
	public DirectoryInfo dirWorkshop;

	public int priorityIndex;
```

[`public void InitLang()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/BaseModManager.cs#L85-L88)
```cs:line-numbers=85
	public virtual void ParseExtra(DirectoryInfo dir, BaseModPackage package)
	{
	}
 // [!code ++]
	public static void SubscribeEvent(string eventId, Action<object> handler) // [!code ++]
	{ // [!code ++]
		if (!_eventHandlers.TryGetValue(eventId, out var value)) // [!code ++]
		{ // [!code ++]
			value = new HashSet<Action<object>>(); // [!code ++]
			_eventHandlers[eventId] = value; // [!code ++]
		} // [!code ++]
		value.Add(handler); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static void UnsubscribeEvent(string eventId, Action<object> handler) // [!code ++]
	{ // [!code ++]
		if (_eventHandlers.TryGetValue(eventId, out var value)) // [!code ++]
		{ // [!code ++]
			value.Remove(handler); // [!code ++]
			if (value.Count == 0) // [!code ++]
			{ // [!code ++]
				_eventHandlers.Remove(eventId); // [!code ++]
			} // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static void PublishEvent(string eventId, object data = null) // [!code ++]
	{ // [!code ++]
		if (!_eventHandlers.TryGetValue(eventId, out var value)) // [!code ++]
		{ // [!code ++]
			return; // [!code ++]
		} // [!code ++]
		foreach (Action<object> item in value.ToList()) // [!code ++]
		{ // [!code ++]
			try // [!code ++]
			{ // [!code ++]
				item(data); // [!code ++]
			} // [!code ++]
			catch (Exception arg) // [!code ++]
			{ // [!code ++]
				Debug.LogError($"#event '{eventId}' throws error in one of the handlers\n{arg}"); // [!code ++]
			} // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static bool HasEventSubscriber(string eventId) // [!code ++]
	{ // [!code ++]
		int? num = _eventHandlers.GetValueOrDefault(eventId)?.Count; // [!code ++]
		if (num.HasValue) // [!code ++]
		{ // [!code ++]
			return num.GetValueOrDefault() > 0; // [!code ++]
		} // [!code ++]
		return false; // [!code ++]
	} // [!code ++]
}
```

## BaseModPackage

[`public void Activate()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/BaseModPackage.cs#L146-L152)
```cs:line-numbers=146
{
	if (!hasPublishedPackage && installed && dirInfo.Exists && willActivate)
	{
		Debug.Log("Activating(" + loadPriority + ") :" + title + "/" + id); // [!code --]
		Debug.Log("Activating(" + loadPriority + ") : " + title + "/" + id); // [!code ++]
		activated = true;
		Parse();
	}
```

## ClassCache

[`public T Create<T2>(Type type)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/ClassCache.cs#L30-L35)
```cs:line-numbers=30

	public T Create<T2>(string id, string assembly)
	{
		Func<T> func = dict.TryGetValue(id); // [!code ++]
		if (func != null) // [!code ++]
		{ // [!code ++]
			return func(); // [!code ++]
		} // [!code ++]
		Type type = Type.GetType(id + ", " + assembly);
		if (type == null)
		{
```

## ClassExtension

[`using System.Linq;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/ClassExtension.cs#L5-L10)
```cs:line-numbers=5
using System.Linq;
using System.Reflection;
using System.Text;
using System.Text.RegularExpressions; // [!code ++]
using Pluralize.NET;
using UnityEngine;
using UnityEngine.Events;
```

[`public static void Set<T1, T2>(this Dictionary<T1, T2> dic, Dictionary<T1, T2> f`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/ClassExtension.cs#L640-L645)
```cs:line-numbers=640
		}
	}

	public static string[] SplitByNewline(this string text) // [!code ++]
	{ // [!code ++]
		if (string.IsNullOrEmpty(text)) // [!code ++]
		{ // [!code ++]
			return new string[0]; // [!code ++]
		} // [!code ++]
		return text.Split(new string[3] { "\r\n", "\n", "\r" }, StringSplitOptions.None); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static string RemoveNewline(this string text) // [!code ++]
	{ // [!code ++]
		if (string.IsNullOrEmpty(text)) // [!code ++]
		{ // [!code ++]
			return text; // [!code ++]
		} // [!code ++]
		return text.Replace("\r", "").Replace("\n", ""); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static string RemoveAllTags(this string text) // [!code ++]
	{ // [!code ++]
		if (string.IsNullOrEmpty(text)) // [!code ++]
		{ // [!code ++]
			return text; // [!code ++]
		} // [!code ++]
		return Regex.Replace(text, "<.*?>", ""); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static int Calc(this string str, int power = 0, int ele = 0, int p2 = 0)
	{
		return Cal.Calcuate(str.Replace("p2", p2.ToString() ?? "").Replace("p", power.ToString() ?? "").Replace("e", ele.ToString() ?? "")
```

## EAction

[`public enum EAction`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/EAction.cs#L43-L47)
```cs:line-numbers=43
	CancelUI = 53,
	Dump = 54,
	Mute = 55,
	Meditate = 56 // [!code --]
	Meditate = 56, // [!code ++]
	Console = 57 // [!code ++]
}
```

## EInput

[`public class KeyMapManager`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/EInput.cs#L192-L205)
```cs:line-numbers=192
			action = EAction.Chat
		};

		public KeyMap console = new KeyMap // [!code ++]
		{ // [!code ++]
			action = EAction.Console // [!code ++]
		}; // [!code ++]
 // [!code ++]
		public List<KeyMap> List()
		{
			return new List<KeyMap>
			{
				axisUp, axisDown, axisLeft, axisRight, axisUpLeft, axisUpRight, axisDownLeft, axisDownRight, journal, chara,
				inventory, ability, log, fire, chat, wait, mouseLeft, mouseMiddle, mouseRight, report, // [!code --]
				quickSave, quickLoad, autoCombat, emptyHand, switchHotbar, examine, getAll, dump, mute, meditate, // [!code --]
				search // [!code --]
				inventory, ability, log, fire, chat, console, wait, mouseLeft, mouseMiddle, mouseRight, // [!code ++]
				report, quickSave, quickLoad, autoCombat, emptyHand, switchHotbar, examine, getAll, dump, mute, // [!code ++]
				meditate, search // [!code ++]
			};
		}
	}
```

[`private static EAction GetAction()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/EInput.cs#L1067-L1072)
```cs:line-numbers=1067
	{
		return EAction.Chat;
	}
	if (Input.GetKeyDown(keys.console.key)) // [!code ++]
	{ // [!code ++]
		return EAction.Console; // [!code ++]
	} // [!code ++]
	if (keyFire.Update())
	{
		return keyFire.Action;
```

## ExcelParser

[`using System;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/ExcelParser.cs#L1-L8)
```cs:line-numbers=1
using System;
using System.Text; // [!code ++]
using NPOI.SS.UserModel;
using UnityEngine; // [!code ++]

public class ExcelParser
{
	public static string path; // [!code ++]
 // [!code ++]
	public static IRow row;

	public static IRow rowDefault;
```

[`public static bool IsNull(ICell cell)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/ExcelParser.cs#L18-L28)
```cs:line-numbers=18

	public static int GetInt(int id)
	{
		if (int.TryParse(GetStr(id), out var result)) // [!code --]
		string str = GetStr(id); // [!code ++]
		if (str.IsEmpty()) // [!code ++]
		{ // [!code ++]
			return 0; // [!code ++]
		} // [!code ++]
		if (!int.TryParse(str, out var result)) // [!code ++]
		{
			return result; // [!code --]
			ReportIllFormat<int>(id); // [!code ++]
		}
		return 0; // [!code --]
		return result; // [!code ++]
	}

	public static int GetInt(int col, IRow _row)
```

[`public static int GetInt(int col, IRow _row)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/ExcelParser.cs#L34-L45)
```cs:line-numbers=34
	public static bool GetBool(int id)
	{
		string str = GetStr(id);
		bool result; // [!code ++]
		return str switch
		{
			"0" => false,  // [!code --]
			"1" => true,  // [!code --]
			null => false, 
			_ => bool.Parse(str),  // [!code --]
			"1" => true,  // [!code ++]
			"0" => false,  // [!code ++]
			_ => bool.TryParse(str, out result) && result,  // [!code ++]
		};
	}

```

[`public static bool GetBool(int col, IRow _row)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/ExcelParser.cs#L52-L92)
```cs:line-numbers=52
	public static double GetDouble(int id)
	{
		string str = GetStr(id);
		if (str != null) // [!code --]
		if (str.IsEmpty()) // [!code ++]
		{ // [!code ++]
			return 0.0; // [!code ++]
		} // [!code ++]
		if (!double.TryParse(str, out var result)) // [!code ++]
		{
			return double.Parse(str); // [!code --]
			ReportIllFormat<double>(id); // [!code ++]
		}
		return 0.0; // [!code --]
		return result; // [!code ++]
	}

	public static float GetFloat(int id)
	{
		string str = GetStr(id);
		if (str != null) // [!code --]
		if (str.IsEmpty()) // [!code ++]
		{
			return float.Parse(str); // [!code --]
			return 0f; // [!code ++]
		}
		return 0f; // [!code --]
		if (!float.TryParse(str, out var result)) // [!code ++]
		{ // [!code ++]
			ReportIllFormat<float>(id); // [!code ++]
		} // [!code ++]
		return result; // [!code ++]
	}

	public static float[] GetFloatArray(int id)
	{
		string str = GetStr(id);
		if (str != null) // [!code --]
		if (str.IsEmpty()) // [!code ++]
		{ // [!code ++]
			return Array.Empty<float>(); // [!code ++]
		} // [!code ++]
		string[] array = str.Split(','); // [!code ++]
		float[] array2 = new float[array.Length]; // [!code ++]
		for (int i = 0; i < array.Length; i++) // [!code ++]
		{
			return Array.ConvertAll(str.Split(','), float.Parse); // [!code --]
			if (!float.TryParse(array[i], out array2[i])) // [!code ++]
			{ // [!code ++]
				ReportIllFormat<float>(id); // [!code ++]
				array2[i] = 0f; // [!code ++]
			} // [!code ++]
		}
		return new float[0]; // [!code --]
		return array2; // [!code ++]
	}

	public static int[] GetIntArray(int id)
	{
		string str = GetStr(id);
		if (str != null) // [!code --]
		if (str.IsEmpty()) // [!code ++]
		{
			return Array.ConvertAll(str.Split(','), int.Parse); // [!code --]
			return Array.Empty<int>(); // [!code ++]
		} // [!code ++]
		string[] array = str.Split(','); // [!code ++]
		int[] array2 = new int[array.Length]; // [!code ++]
		for (int i = 0; i < array.Length; i++) // [!code ++]
		{ // [!code ++]
			if (!int.TryParse(array[i], out array2[i])) // [!code ++]
			{ // [!code ++]
				ReportIllFormat<int>(id); // [!code ++]
				array2[i] = 0; // [!code ++]
			} // [!code ++]
		}
		return new int[0]; // [!code --]
		return array2; // [!code ++]
	}

	public static string[] GetStringArray(int id)
```

[`public static string[] GetStringArray(int id)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/ExcelParser.cs#L96-L102)
```cs:line-numbers=96
		{
			return str.Split(',');
		}
		return new string[0]; // [!code --]
		return Array.Empty<string>(); // [!code ++]
	}

	public static string GetString(int id)
```

[`public static string GetStr(int id, bool useDefault = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/ExcelParser.cs#L141-L144)
```cs:line-numbers=141
		}
		return cell.StringCellValue;
	}
 // [!code ++]
	public static string ToLetterId(int id) // [!code ++]
	{ // [!code ++]
		string text = ""; // [!code ++]
		while (id >= 0) // [!code ++]
		{ // [!code ++]
			text = (char)(id % 26 + 65) + text; // [!code ++]
			id /= 26; // [!code ++]
			id--; // [!code ++]
		} // [!code ++]
		return text; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static void ReportIllFormat<T>(int id) // [!code ++]
	{ // [!code ++]
		StringBuilder stringBuilder = new StringBuilder(); // [!code ++]
		string name = typeof(T).Name; // [!code ++]
		ICell cell = row.Cells.TryGet(id, returnNull: true); // [!code ++]
		string value = ((row.RowNum < 3) ? ", SourceData begins at the 4th row. 3rd row is the default value row." : $", default:'{rowDefault.Cells.TryGet(id, returnNull: true)}'"); // [!code ++]
		stringBuilder.AppendLine("$source ill-format file: " + path); // [!code ++]
		stringBuilder.Append($"row#{row.RowNum + 1}, cell'{id + 1}'/'{ToLetterId(id)}', expected:'{name}', read:'{cell}'"); // [!code ++]
		stringBuilder.AppendLine(value); // [!code ++]
		Debug.LogError(stringBuilder); // [!code ++]
	} // [!code ++]
}
```

## Lang

[`using System;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/Lang.cs#L1-L5)
```cs:line-numbers=1
using System;
using System.Collections.Generic;
using System.Linq; // [!code ++]
using System.Text;

public class Lang
```

[`public class Words`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/Lang.cs#L59-L64)
```cs:line-numbers=59

	public static ExcelData excelDialog;

	public static HashSet<string> extraExcelDialogs = new HashSet<string>(); // [!code ++]
 // [!code ++]
	public static bool IsBuiltin(string id)
	{
		if (!(id == "JP"))
```

[`public static ExcelData.Sheet GetDialogSheet(string idSheet)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/Lang.cs#L206-L229)
```cs:line-numbers=206
	{
		if (excelDialog == null)
		{
			string path = CorePath.CorePackage.TextDialog + "dialog.xlsx"; // [!code --]
			excelDialog = new ExcelData(); // [!code --]
			excelDialog.path = path; // [!code --]
			excelDialog = new ExcelData(CorePath.CorePackage.TextDialog + "dialog.xlsx"); // [!code ++]
			excelDialog.LoadBook(); // [!code ++]
			for (int i = 0; i < excelDialog.book.NumberOfSheets; i++) // [!code ++]
			{ // [!code ++]
				string sheetName = excelDialog.book.GetSheetAt(i).SheetName; // [!code ++]
				excelDialog.BuildMap(sheetName); // [!code ++]
				foreach (ExcelData item in extraExcelDialogs.Select((string f) => new ExcelData(f))) // [!code ++]
				{ // [!code ++]
					item.BuildMap(sheetName); // [!code ++]
					ExcelData.Sheet sheet = item.sheets[sheetName]; // [!code ++]
					if (item.book.GetSheet(sheetName) == null) // [!code ++]
					{ // [!code ++]
						sheet.list.Clear(); // [!code ++]
						sheet.map.Clear(); // [!code ++]
					} // [!code ++]
					foreach (var (text2, value) in sheet.map) // [!code ++]
					{ // [!code ++]
						if (!text2.IsEmpty()) // [!code ++]
						{ // [!code ++]
							excelDialog.sheets[sheetName].map[text2] = value; // [!code ++]
						} // [!code ++]
					} // [!code ++]
				} // [!code ++]
			} // [!code ++]
		}
		excelDialog.BuildMap(idSheet); // [!code --]
		return excelDialog.sheets[idSheet];
	}

	public static string[] GetDialog(string idSheet, string idTopic)
	{
		ExcelData.Sheet dialogSheet = GetDialogSheet(idSheet); // [!code --]
		string key = "text" + (isBuiltin ? ("_" + langCode) : ""); // [!code --]
		Dictionary<string, string> dictionary = dialogSheet.map.TryGetValue(idTopic); // [!code --]
		if (dictionary == null || !dictionary.ContainsKey(key)) // [!code --]
		Dictionary<string, string> dictionary = GetDialogSheet(idSheet).map.TryGetValue(idTopic); // [!code ++]
		if (dictionary == null) // [!code ++]
		{
			return new string[1] { idTopic };
		}
		string text = dictionary[key]; // [!code --]
		string key = "text_" + langCode; // [!code ++]
		string text = dictionary.GetValueOrDefault(key) ?? dictionary.GetValueOrDefault("text"); // [!code ++]
		if (text.IsEmpty())
		{
			text = dictionary["text_EN"];
```

## MOD

[`public class MOD`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/MOD.cs#L20-L23)
```cs:line-numbers=20
	public static List<FileInfo> listMaps = new List<FileInfo>();

	public static List<FileInfo> listPartialMaps = new List<FileInfo>();
 // [!code ++]
	public static Dictionary<string, FileInfo> sounds = new Dictionary<string, FileInfo>(); // [!code ++]
}
```

## SourceData

[`public class FieldMap`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/SourceData.cs#L81-L86)
```cs:line-numbers=81

	public override void Init()
	{
		Debug.Log("Initializing:" + base.name); // [!code ++]
		if (initialized)
		{
			Debug.Log("#init Skipping sourceData.Init:" + base.name);
```

[`public override void Init()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/SourceData.cs#L88-L93)
```cs:line-numbers=88
	}
	initialized = true;
	editorListString.Clear();
	RemoveDuplicateRows(); // [!code ++]
	int num = 0;
	foreach (T row in rows)
	{
```

[`public virtual T CreateRow()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/SourceData.cs#L171-L176)
```cs:line-numbers=171
		return null;
	}

	public override int ImportRows(IEnumerable<BaseRow> sourceRows) // [!code ++]
	{ // [!code ++]
		int num = 0; // [!code ++]
		foreach (BaseRow sourceRow in sourceRows) // [!code ++]
		{ // [!code ++]
			if (sourceRow is T val) // [!code ++]
			{ // [!code ++]
				val.OnImportData(this); // [!code ++]
				rows.Add(val); // [!code ++]
				num++; // [!code ++]
			} // [!code ++]
		} // [!code ++]
		OnAfterImportData(); // [!code ++]
		initialized = false; // [!code ++]
		return num; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public virtual void RemoveDuplicateRows() // [!code ++]
	{ // [!code ++]
		HashSet<T2> hashSet = new HashSet<T2>(); // [!code ++]
		List<T> list = new List<T>(rows.Count); // [!code ++]
		bool flag = typeof(T).DeclaringType.Name.Contains("Lang"); // [!code ++]
		string arg = GetType().Name; // [!code ++]
		System.Reflection.FieldInfo field = typeof(T).GetField("id"); // [!code ++]
		if (field.FieldType != typeof(T2)) // [!code ++]
		{ // [!code ++]
			Debug.LogError($"#source override: {arg} id field mismatch {field.FieldType} != {typeof(T2)}"); // [!code ++]
		} // [!code ++]
		for (int num = rows.Count - 1; num >= 0; num--) // [!code ++]
		{ // [!code ++]
			T val = rows[num]; // [!code ++]
			T2 val2 = (T2)field.GetValue(val); // [!code ++]
			if (hashSet.Add(val2)) // [!code ++]
			{ // [!code ++]
				list.Add(val); // [!code ++]
			} // [!code ++]
			else if (!flag) // [!code ++]
			{ // [!code ++]
				Debug.Log($"#source override: {arg} {val2}"); // [!code ++]
			} // [!code ++]
		} // [!code ++]
		if (rows.Count != list.Count) // [!code ++]
		{ // [!code ++]
			Debug.Log($"#source override: {arg} total/{rows.Count} -> unique/{list.Count}"); // [!code ++]
			list.Reverse(); // [!code ++]
			rows = list; // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public override void BackupSource()
	{
		_backupRows = rows;
```

[`public enum AutoID`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/SourceData.cs#L529-L540)
```cs:line-numbers=529
	[Serializable]
	public class BaseRow
	{
		private static readonly Dictionary<Type, Dictionary<string, FieldInfo>> _fieldCache = new Dictionary<Type, Dictionary<string, FieldInfo>>(); // [!code ++]
 // [!code ++]
		public int _index;

		public virtual bool UseAlias => false;

		public virtual string GetAlias => "";

		public Dictionary<string, FieldInfo> GetRowFields() // [!code ++]
		{ // [!code ++]
			Type type = GetType(); // [!code ++]
			if (_fieldCache.TryGetValue(type, out var value)) // [!code ++]
			{ // [!code ++]
				return value; // [!code ++]
			} // [!code ++]
			value = new Dictionary<string, FieldInfo>(); // [!code ++]
			FieldInfo[] fields = type.GetFields(BindingFlags.Instance | BindingFlags.Public); // [!code ++]
			foreach (FieldInfo fieldInfo in fields) // [!code ++]
			{ // [!code ++]
				value[fieldInfo.Name] = fieldInfo; // [!code ++]
			} // [!code ++]
			return _fieldCache[type] = value; // [!code ++]
		} // [!code ++]
 // [!code ++]
		public virtual string GetName()
		{
			return GetText();
```

[`public virtual string GetEditorListName()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/SourceData.cs#L552-L585)
```cs:line-numbers=552

		public string GetText(string id = "name", bool returnNull = false)
		{
			FieldInfo field = GetType().GetField(id + LangSuffix); // [!code --]
			if (!Lang.isBuiltin && (field == null || (field.GetValue(this) as string).IsEmpty())) // [!code --]
			Dictionary<string, FieldInfo> rowFields = GetRowFields(); // [!code ++]
			if (rowFields.TryGetValue(id + LangSuffix, out var value)) // [!code ++]
			{
				FieldInfo field2 = GetType().GetField(id); // [!code --]
				if (field2 != null && !(field2.GetValue(this) as string).IsEmpty()) // [!code --]
				string text = value.GetValue(this) as string; // [!code ++]
				if (!text.IsEmpty()) // [!code ++]
				{
					return field2.GetValue(this) as string; // [!code --]
					return text; // [!code ++]
				}
			}
			if (field != null) // [!code --]
			if (!Lang.isBuiltin && rowFields.TryGetValue(id, out var value2)) // [!code ++]
			{
				return (field.GetValue(this) as string).IsEmpty(returnNull ? null : ""); // [!code --]
				string text2 = value2.GetValue(this) as string; // [!code ++]
				if (!text2.IsEmpty()) // [!code ++]
				{ // [!code ++]
					return text2; // [!code ++]
				} // [!code ++]
			}
			return ""; // [!code --]
			if (!returnNull) // [!code ++]
			{ // [!code ++]
				return ""; // [!code ++]
			} // [!code ++]
			return null; // [!code ++]
		}

		public string[] GetTextArray(string id)
		{
			if (!Lang.isBuiltin) // [!code --]
			Dictionary<string, FieldInfo> rowFields = GetRowFields(); // [!code ++]
			if (rowFields.TryGetValue(id + LangSuffix, out var value) && value.GetValue(this) is string[] { Length: >0 } array) // [!code ++]
			{
				FieldInfo field = GetType().GetField(id + Lang.suffix); // [!code --]
				if (field != null && field.GetValue(this) is string[] array && array.Length != 0) // [!code --]
				{ // [!code --]
					return array; // [!code --]
				} // [!code --]
				return GetType().GetField(id).GetValue(this) as string[]; // [!code --]
				return array; // [!code ++]
			}
			return GetType().GetField(id + Lang.suffix).GetValue(this) as string[]; // [!code --]
			if (!Lang.isBuiltin && rowFields.TryGetValue(id, out var value2) && value2.GetValue(this) is string[] { Length: >0 } array2) // [!code ++]
			{ // [!code ++]
				return array2; // [!code ++]
			} // [!code ++]
			return Array.Empty<string>(); // [!code ++]
		}

		public virtual void SetID(ref int count)
```

[`public virtual void SetID(ref int count)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/SourceData.cs#L591-L596)
```cs:line-numbers=591
		public virtual void OnImportData(SourceData data)
		{
		}
 // [!code ++]
		public virtual IDictionary<string, string> ExportTexts(string idField = "id") // [!code ++]
		{ // [!code ++]
			Dictionary<string, FieldInfo> rowFields = GetRowFields(); // [!code ++]
			object obj = rowFields.GetValueOrDefault(idField)?.GetValue(this); // [!code ++]
			SortedDictionary<string, string> sortedDictionary = new SortedDictionary<string, string>(); // [!code ++]
			if (obj == null) // [!code ++]
			{ // [!code ++]
				return sortedDictionary; // [!code ++]
			} // [!code ++]
			string name = GetType().DeclaringType.Name; // [!code ++]
			foreach (var (text2, jp2) in rowFields) // [!code ++]
			{ // [!code ++]
				if (!text2.EndsWith("_JP")) // [!code ++]
				{ // [!code ++]
					continue; // [!code ++]
				} // [!code ++]
				string text3 = text2[..^3]; // [!code ++]
				if (rowFields.TryGetValue(text3, out var value) && rowFields.ContainsKey(text3 + "_L")) // [!code ++]
				{ // [!code ++]
					string text4 = GetFieldText(jp2, value); // [!code ++]
					if (!text4.IsEmpty()) // [!code ++]
					{ // [!code ++]
						sortedDictionary[$"{name}.{obj}.{text3}"] = text4; // [!code ++]
					} // [!code ++]
				} // [!code ++]
			} // [!code ++]
			return sortedDictionary; // [!code ++]
			string GetFieldText(FieldInfo jp, FieldInfo en) // [!code ++]
			{ // [!code ++]
				object obj2 = jp.GetValue(this); // [!code ++]
				object obj3 = en.GetValue(this); // [!code ++]
				if (!Lang.isJP) // [!code ++]
				{ // [!code ++]
					object obj4 = obj2; // [!code ++]
					object obj5 = obj3; // [!code ++]
					obj3 = obj4; // [!code ++]
					obj2 = obj5; // [!code ++]
				} // [!code ++]
				if (obj2 is string str) // [!code ++]
				{ // [!code ++]
					return str.IsEmpty(obj3 as string); // [!code ++]
				} // [!code ++]
				if (obj2 is string[] array) // [!code ++]
				{ // [!code ++]
					return string.Join(',', (array.Length != 0) ? array : (obj3 as string[])); // [!code ++]
				} // [!code ++]
				return null; // [!code ++]
			} // [!code ++]
		} // [!code ++]
 // [!code ++]
		public virtual void ImportTexts(IReadOnlyDictionary<string, string> texts, string idField = "id") // [!code ++]
		{ // [!code ++]
			Dictionary<string, FieldInfo> rowFields = GetRowFields(); // [!code ++]
			object obj = rowFields.GetValueOrDefault(idField)?.GetValue(this); // [!code ++]
			if (obj == null) // [!code ++]
			{ // [!code ++]
				return; // [!code ++]
			} // [!code ++]
			string name = GetType().DeclaringType.Name; // [!code ++]
			foreach (var (text2, fieldInfo2) in rowFields) // [!code ++]
			{ // [!code ++]
				if (!text2.EndsWith("_L")) // [!code ++]
				{ // [!code ++]
					continue; // [!code ++]
				} // [!code ++]
				string text3 = text2[..^2]; // [!code ++]
				if (rowFields.ContainsKey(text3) && rowFields.ContainsKey(text3 + "_JP")) // [!code ++]
				{ // [!code ++]
					fieldInfo2.SetValue(this, null); // [!code ++]
					if (texts.TryGetValue($"{name}.{obj}.{text3}", out var value2) && !value2.IsEmpty()) // [!code ++]
					{ // [!code ++]
						SetFieldText(fieldInfo2, value2); // [!code ++]
					} // [!code ++]
				} // [!code ++]
			} // [!code ++]
			void SetFieldText(FieldInfo l, string value) // [!code ++]
			{ // [!code ++]
				if (l.FieldType == typeof(string)) // [!code ++]
				{ // [!code ++]
					l.SetValue(this, value); // [!code ++]
				} // [!code ++]
				else if (l.FieldType == typeof(string[])) // [!code ++]
				{ // [!code ++]
					l.SetValue(this, value.IsEmpty() ? Array.Empty<string>() : value.Split(',')); // [!code ++]
				} // [!code ++]
			} // [!code ++]
		} // [!code ++]
	}

	public static string LangSuffix;
```

[`public virtual bool ImportData(ISheet sheet, string bookname, bool overwrite = f`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f8d887190f23ba23be9940e9ae1b27cc45a728dd/Elin/Plugins.basecore/SourceData.cs#L667-L672)
```cs:line-numbers=667
		return false;
	}

	public virtual int ImportRows(IEnumerable<BaseRow> sourceRows) // [!code ++]
	{ // [!code ++]
		return 0; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public virtual void BackupSource()
	{
	}
```
