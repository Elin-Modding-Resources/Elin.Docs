---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 10 files modified.
version: EA 23.340.3 Nightly
changes: CINT/Card/Chara/CharaBody/DNA/InvOwner/InvOwnerEquip/InvOwnerGene/AssemblyInfo/RecipeCard
---

# EA 23.340.3 Nightly

August 30, 2026

10 files modified.

## Important Changes

**None.**
## CINT

[`public class CINT`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/313234b04cd7d07a9e9debe418396683c81278ab/Elin/CINT.cs#L185-L188)
```cs:line-numbers=185
	public const int uidAttune = 133;

	public const int peakyAdded = 134;
 // [!code ++]
	public const int isSleepLock = 135; // [!code ++]
}
```

## Card

[`public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSourc…)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/313234b04cd7d07a9e9debe418396683c81278ab/Elin/Card.cs#L4467-L4473)
```cs:line-numbers=4467
}
if (HasElement(1218) && attackSource != AttackSource.ManaBackfire && (hp > 0 || Evalue(1421) <= 0))
{
	dmg = dmg * (1000 - Mathf.Min(Evalue(1218), 1000) / ((!flag) ? 1 : 2)) / 1000; // [!code --]
	dmg = dmg * (1000 - Mathf.Min(Evalue(1218), 999) / ((!flag) ? 1 : 2)) / 1000; // [!code ++]
	if (dmg <= 0 && EClass.rnd(4) == 0)
	{
		dmg++;
```

## Chara

[`public void OnSleep(int power, int days = 1, bool isSunLit = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/313234b04cd7d07a9e9debe418396683c81278ab/Elin/Chara.cs#L10342-L10347)
```cs:line-numbers=10342
{
	stamina.Set(1);
}
if (IsPC) // [!code ++]
{ // [!code ++]
	foreach (BodySlot slot in body.slots) // [!code ++]
	{ // [!code ++]
		if (slot.thing != null) // [!code ++]
		{ // [!code ++]
			slot.thing.SetBool(135, enable: false); // [!code ++]
		} // [!code ++]
	} // [!code ++]
} // [!code ++]
if (isWeakToSunlight && isSunLit)
{
	base.hp = 0;
```

## CharaBody

[`public bool IsEquippable(Thing thing, BodySlot slot, bool text = true)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/313234b04cd7d07a9e9debe418396683c81278ab/Elin/CharaBody.cs#L122-L146)
```cs:line-numbers=122
case 31:
	if (owner.HasElement(1555))
	{
		return CannotEquip(); // [!code --]
		return CannotEquip("cannnotEquip"); // [!code ++]
	}
	break;
case 33:
	if (owner.HasElement(1554))
	{
		return CannotEquip(); // [!code --]
		return CannotEquip("cannnotEquip"); // [!code ++]
	}
	break;
case 39:
	if (owner.HasElement(1552))
	{
		return CannotEquip(); // [!code --]
		return CannotEquip("cannnotEquip"); // [!code ++]
	}
	break;
}
if (slot.thing != null && slot.thing.blessedState <= BlessedState.Cursed) // [!code --]
if (slot.thing != null) // [!code ++]
{
	return CannotEquip(); // [!code --]
	if (slot.thing.blessedState <= BlessedState.Cursed) // [!code ++]
	{ // [!code ++]
		return CannotEquip("cannnotEquip"); // [!code ++]
	} // [!code ++]
	if (slot.thing.c_DNA != null && slot.thing.GetBool(135)) // [!code ++]
	{ // [!code ++]
		return CannotEquip("isSleepLock"); // [!code ++]
	} // [!code ++]
}
if (thing.c_uidAttune != 0 && thing.c_uidAttune != owner.uid)
{
```

[`public bool IsEquippable(Thing thing, BodySlot slot, bool text = true)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/313234b04cd7d07a9e9debe418396683c81278ab/Elin/CharaBody.cs#L167-L177)
```cs:line-numbers=167
	return false;
}
return true;
bool CannotEquip() // [!code --]
bool CannotEquip(string idLang) // [!code ++]
{
	if (text)
	{
		Msg.Say("cannnotEquip", slot.element.GetName().ToLower()); // [!code --]
		Msg.Say(idLang, slot.element.GetName().ToLower()); // [!code ++]
	}
	return false;
}
```

[`public bool Equip(Thing thing, BodySlot slot = null, bool msg = true)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/313234b04cd7d07a9e9debe418396683c81278ab/Elin/CharaBody.cs#L258-L264)
```cs:line-numbers=258
	thing.c_DNA.Apply(owner, reverse: false);
	Msg.Say("equip_relic");
	owner.feat -= thing.c_DNA.cost;
	owner.stamina.Mod(-1 - EClass.rnd(5)); // [!code --]
	thing.SetBool(135, enable: true); // [!code ++]
}
if (owner.IsPC)
{
```

## DNA

[`void AddBody()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/313234b04cd7d07a9e9debe418396683c81278ab/Elin/DNA.cs#L401-L407)
```cs:line-numbers=401
for (int j = 0; j < 100; j++)
{
	BodySlot bodySlot2 = model.body.slots.RandomItem();
	if (bodySlot2 != null && bodySlot2.elementId != 40 && bodySlot2.elementId != 44) // [!code --]
	if (bodySlot2 != null && bodySlot2.elementId != 40 && bodySlot2.elementId != 44 && bodySlot2.elementId != 46) // [!code ++]
	{
		bodySlot = bodySlot2;
		break;
```

[`public DNA Relicize()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/313234b04cd7d07a9e9debe418396683c81278ab/Elin/DNA.cs#L781-L786)
```cs:line-numbers=781
	if (element.source.category == "slot" || element.HasTag("noRelic") || element.HasTag("permaGene"))
	{
		vals.RemoveRange(i, 2);
		i -= 2; // [!code ++]
	}
}
cost /= 5;
```

## InvOwner

[`public virtual bool OnDrag(DragItemCard.DragInfo from, DragItemCard.DragInfo to,…)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/313234b04cd7d07a9e9debe418396683c81278ab/Elin/InvOwner.cs#L1219-L1228)
```cs:line-numbers=1219
InvOwnerEquip invOwnerEquip = to.invOwner as InvOwnerEquip;
Chara chara = invOwnerEquip.owner.Chara;
BodySlot slot = invOwnerEquip.slot;
if (execute && to.thing != null && to.thing.blessedState <= BlessedState.Cursed) // [!code --]
if (execute && to.thing != null) // [!code ++]
{
	Msg.Say("unequipCursed", to.thing); // [!code --]
	return false; // [!code --]
	if (to.thing.blessedState <= BlessedState.Cursed) // [!code ++]
	{ // [!code ++]
		Msg.Say("unequipCursed", to.thing); // [!code ++]
		return false; // [!code ++]
	} // [!code ++]
	if (to.thing.c_DNA != null && to.thing.GetBool(135)) // [!code ++]
	{ // [!code ++]
		Msg.Say("isSleepLock", to.thing); // [!code ++]
		return false; // [!code ++]
	} // [!code ++]
}
if (from.thing.category.slot == slot.elementId)
{
```

[`public virtual void ListInteractions(ListInteraction list, Thing t, Trait trait,…)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/313234b04cd7d07a9e9debe418396683c81278ab/Elin/InvOwner.cs#L1507-L1539)
```cs:line-numbers=1507
{
	list.Add("invEquip", 90, delegate
	{
		if (slot.thing != null && slot.thing.blessedState <= BlessedState.Cursed) // [!code --]
		if (slot.thing != null) // [!code ++]
		{
			Msg.Say("unequipCursed", slot.thing); // [!code --]
			SE.Play("curse3"); // [!code --]
			if (slot.thing.blessedState <= BlessedState.Cursed) // [!code ++]
			{ // [!code ++]
				Msg.Say("unequipCursed", slot.thing); // [!code ++]
				SE.Play("curse3"); // [!code ++]
				return; // [!code ++]
			} // [!code ++]
			if (slot.thing.c_DNA != null && slot.thing.GetBool(135)) // [!code ++]
			{ // [!code ++]
				Msg.Say("isSleepLock", slot.thing); // [!code ++]
				SE.Play("curse3"); // [!code ++]
				return; // [!code ++]
			} // [!code ++]
		}
		else // [!code --]
		if (EClass.pc.held == t) // [!code ++]
		{
			if (EClass.pc.held == t) // [!code --]
			EClass.pc.PickHeld(); // [!code ++]
		} // [!code ++]
		Thing thing = slot.thing; // [!code ++]
		body.Equip(t, slot); // [!code ++]
		EClass.Sound.Play("equip"); // [!code ++]
		if (thing != null) // [!code ++]
		{ // [!code ++]
			if (tParent != null) // [!code ++]
			{
				EClass.pc.PickHeld(); // [!code --]
				tParent.AddThing(thing); // [!code ++]
				thing.invX = tInvX; // [!code ++]
				thing.invY = tInvY; // [!code ++]
			}
			Thing thing = slot.thing; // [!code --]
			body.Equip(t, slot); // [!code --]
			EClass.Sound.Play("equip"); // [!code --]
			if (thing != null) // [!code --]
			else if (thing.parent is Card && (thing.parent as Card).things.IsOverflowing()) // [!code ++]
			{
				if (tParent != null) // [!code --]
				{ // [!code --]
					tParent.AddThing(thing); // [!code --]
					thing.invX = tInvX; // [!code --]
					thing.invY = tInvY; // [!code --]
				} // [!code --]
				else if (thing.parent is Card && (thing.parent as Card).things.IsOverflowing()) // [!code --]
				{ // [!code --]
					thing.parent.RemoveCard(thing); // [!code --]
					EClass.pc.Pick(thing); // [!code --]
				} // [!code --]
				thing.parent.RemoveCard(thing); // [!code ++]
				EClass.pc.Pick(thing); // [!code ++]
			}
		}
	});
```

## InvOwnerEquip

[`public override void ListInteractions(ListInteraction list, Thing t, Trait trait…)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/313234b04cd7d07a9e9debe418396683c81278ab/Elin/InvOwnerEquip.cs#L41-L50)
```cs:line-numbers=41
public override bool IsFailByCurse(Thing t)
{
	if (t != null && t.blessedState <= BlessedState.Cursed) // [!code --]
	if (t != null) // [!code ++]
	{
		Msg.Say("unequipCursed", t); // [!code --]
		SE.Play("curse3"); // [!code --]
		return true; // [!code --]
		if (t.blessedState <= BlessedState.Cursed) // [!code ++]
		{ // [!code ++]
			Msg.Say("unequipCursed", t); // [!code ++]
			SE.Play("curse3"); // [!code ++]
			return true; // [!code ++]
		} // [!code ++]
		if (t.c_DNA != null && t.GetBool(135)) // [!code ++]
		{ // [!code ++]
			Msg.Say("isSleepLock", t); // [!code ++]
			SE.Play("curse3"); // [!code ++]
			return true; // [!code ++]
		} // [!code ++]
	}
	return false;
}
```

## InvOwnerGene

[`public InvOwnerGene(Card owner = null, Chara _tg = null)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/313234b04cd7d07a9e9debe418396683c81278ab/Elin/InvOwnerGene.cs#L16-L23)
```cs:line-numbers=16
public override bool ShouldShowGuide(Thing t)
{
	if (t.c_DNA != null) // [!code --]
	if (t.c_DNA != null && tg.feat >= t.c_DNA.cost) // [!code ++]
	{
		return tg.feat >= t.c_DNA.cost; // [!code --]
		return !t.IsEquipment; // [!code ++]
	}
	return false;
}
```

## AssemblyInfo

[`using System.Security;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/313234b04cd7d07a9e9debe418396683c81278ab/Elin/Properties/AssemblyInfo.cs#L4-L13)
```cs:line-numbers=4
using System.Security;
using System.Security.Permissions;

[assembly: AssemblyFileVersion("0.23.340.2")] // [!code --]
[assembly: AssemblyInformationalVersion("0.23.340.2+Nightly")] // [!code --]
[assembly: AssemblyMetadata("GameVersion", "0.23.340.2")] // [!code --]
[assembly: AssemblyMetadata("GameVersionText", "EA 23.340 Patch 2")] // [!code --]
[assembly: AssemblyFileVersion("0.23.340.3")] // [!code ++]
[assembly: AssemblyInformationalVersion("0.23.340.3+Nightly")] // [!code ++]
[assembly: AssemblyMetadata("GameVersion", "0.23.340.3")] // [!code ++]
[assembly: AssemblyMetadata("GameVersionText", "EA 23.340 Patch 3")] // [!code ++]
[assembly: AssemblyMetadata("GameVersionInt", "23340")]
[assembly: AssemblyMetadata("ReleaseChannel", "Nightly")]
[assembly: AssemblyVersion("0.0.0.0")]
```

## RecipeCard

[`public override Thing Craft(BlessedState blessed, bool sound = false, List<Thing…)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/313234b04cd7d07a9e9debe418396683c81278ab/Elin/RecipeCard.cs#L251-L257)
```cs:line-numbers=251
		}
	}
}
thing.Identify(show: false); // [!code --]
thing.Identify(show: false, IDTSource.SuperiorIdentify); // [!code ++]
thing.isCrafted = true;
if (!flag3)
{
```
