---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 8 files modified.
version: EA 23.265 Nightly Patch 2
changes: Chara/ConTransmuteMimic/Dialog/Player/RecipeCard/RendererReplacer/SerializedCards/Zone
---

# EA 23.265 Nightly Patch 2

February 3, 2026

8 files modified.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [Dialog (1)](#dialog)
```cs:no-line-numbers
public static void TryWarn(string lang, Action action, bool yes = true) // [!code --]
public static void TryWarn(string lang, Action action, bool yes = true, string text = null) // [!code ++]
```
### [Player (1)](#player)
```cs:no-line-numbers
public List<Chara> listSummon = new List<Chara>(); // [!code --]
public List<Chara> listCarryoverMap = new List<Chara>(); // [!code ++]
```
### [Zone (1)](#zone)
```cs:no-line-numbers
public void OnCharaDie() // [!code --]
public void RefreshDeathSentense() // [!code ++]
```
## Chara

[`public void MoveZone(Zone z, ZoneTransition transition)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/cd73c4e897325ae140874f7d172e7a203aedfc4a/Elin/Chara.cs#L3394-L3401)
```cs:line-numbers=3394
		{
			EClass._zone.RemoveCard(item);
		}
		EClass.player.listSummon = EClass._map.charas.Where((Chara c) => c.c_uidMaster != 0 && c.FindMaster() == EClass.pc && c.c_minionType == MinionType.Default).ToList(); // [!code --]
		foreach (Chara item2 in EClass.player.listSummon) // [!code --]
		EClass.player.listCarryoverMap = EClass._map.charas.Where((Chara c) => c.c_uidMaster != 0 && c.FindMaster() == EClass.pc && c.c_minionType == MinionType.Default).ToList(); // [!code ++]
		if (!z.IsRegion && (z is Zone_Tent || currentZone is Zone_Tent)) // [!code ++]
		{ // [!code ++]
			foreach (Chara chara in EClass._map.charas) // [!code ++]
			{ // [!code ++]
				if (chara.c_uidMaster == 0 && !chara.IsGlobal && !chara.isNPCProperty && (chara.id == "reaper" || chara.id == "messenger_death") && ((chara.enemy != null && chara.enemy.IsPCFactionOrMinion) || chara.Dist(EClass.pc) < 5)) // [!code ++]
				{ // [!code ++]
					EClass.player.listCarryoverMap.Add(chara); // [!code ++]
				} // [!code ++]
			} // [!code ++]
		} // [!code ++]
		foreach (Chara item2 in EClass.player.listCarryoverMap) // [!code ++]
		{
			EClass._zone.RemoveCard(item2);
		}
```

[`public override void Die(Element e = null, Card origin = null, AttackSource atta`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/cd73c4e897325ae140874f7d172e7a203aedfc4a/Elin/Chara.cs#L5617-L5623)
```cs:line-numbers=5617
			chara2.MakeMinion((origin.IsPCParty || origin.IsPCPartyMinion) ? EClass.pc : origin.Chara, MinionType.Friend);
			Msg.Say("plant_pop", this, chara2);
		}
		EClass._zone.OnCharaDie(); // [!code --]
		EClass._zone.RefreshDeathSentense(); // [!code ++]
		EClass._zone.events.OnCharaDie(this);
	}

```

## ConTransmuteMimic

[`public override void Tick()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/cd73c4e897325ae140874f7d172e7a203aedfc4a/Elin/ConTransmuteMimic.cs#L23-L32)
```cs:line-numbers=23

	public override void OnBeforeStart()
	{
		List<Thing> list = owner.things.List((Thing t) => !t.IsMultisize && t.c_isImportant, onlyAccessible: true); // [!code --]
		List<Thing> list = owner.things.List((Thing t) => !t.source.multisize && t.c_isImportant, onlyAccessible: true); // [!code ++]
		if (list.Count == 0)
		{
			list = owner.things.List((Thing t) => !t.IsMultisize && !t.isEquipped, onlyAccessible: true); // [!code --]
			list = owner.things.List((Thing t) => !t.source.multisize && !t.isEquipped, onlyAccessible: true); // [!code ++]
		}
		if (list.Count > 0)
		{
```

## Dialog

[`public static Dialog List<TValue>(string langDetail, ICollection<TValue> items,`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/cd73c4e897325ae140874f7d172e7a203aedfc4a/Elin/Dialog.cs#L287-L296)
```cs:line-numbers=287
		return d;
	}

	public static void TryWarn(string lang, Action action, bool yes = true) // [!code --]
	public static void TryWarn(string lang, Action action, bool yes = true, string text = null) // [!code ++]
	{
		Dialog d = Layer.Create<Dialog>();
		d.textDetail.SetText(lang.lang() + " "); // [!code --]
		d.textDetail.SetText(text ?? (lang.lang() + " ")); // [!code ++]
		d.SetOnKill(delegate
		{
			ELayer.core.actionsNextFrame.Add(delegate
```

[`public static void TryWarn(string lang, Action action, bool yes = true)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/cd73c4e897325ae140874f7d172e7a203aedfc4a/Elin/Dialog.cs#L334-L343)
```cs:line-numbers=334
	{
		d.list.AddButton(null, Lang.Get("no_dontask"), delegate
		{
			string text = lang; // [!code --]
			if (!(text == "warn_parallels")) // [!code --]
			string text2 = lang; // [!code ++]
			if (!(text2 == "warn_parallels")) // [!code ++]
			{
				if (text == "warn_linuxMod") // [!code --]
				if (text2 == "warn_linuxMod") // [!code ++]
				{
					ELayer.core.config.ignoreLinuxModWarning = true;
				}
```

[`public static void TryWarnSlaughter(Action action, Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/cd73c4e897325ae140874f7d172e7a203aedfc4a/Elin/Dialog.cs#L369-L375)
```cs:line-numbers=369
		}
		else
		{
			TryWarn("warn_slaughter".lang(c.Name), action); // [!code --]
			TryWarn("warn_slaughter", action, yes: true, "warn_slaughter".lang(c.Name)); // [!code ++]
		}
	}

```

## Player

[`public void OnLeaveZone()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/cd73c4e897325ae140874f7d172e7a203aedfc4a/Elin/Player.cs#L1220-L1226)
```cs:line-numbers=1220

	public PlayingSong playingSong;

	public List<Chara> listSummon = new List<Chara>(); // [!code --]
	public List<Chara> listCarryoverMap = new List<Chara>(); // [!code ++]

	public int autoCombatStartHP;

```

## RecipeCard

[`public string GetIdFailDish()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/cd73c4e897325ae140874f7d172e7a203aedfc4a/Elin/RecipeCard.cs#L391-L396)
```cs:line-numbers=391
		"meal_egg" => "fail_egg", 
		"meal_rice" => "fail_rice", 
		"meal_soup" => "fail_drink", 
		"meal_cookie" => "fail_nuts",  // [!code ++]
		_ => "fail_dish", 
	};
}
```

## RendererReplacer

[`public class RendererReplacer : EClass`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/cd73c4e897325ae140874f7d172e7a203aedfc4a/Elin/RendererReplacer.cs#L10-L16)
```cs:line-numbers=10

	public static RendererReplacer CreateFrom(string id, int shift = 0, int mat = -1)
	{
		CardRow cardRow = EClass.sources.cards.map[id]; // [!code --]
		CardRow cardRow = EClass.sources.cards.map.TryGetValue(id); // [!code ++]
		if (cardRow == null || cardRow.tiles.Length == 0) // [!code ++]
		{ // [!code ++]
			cardRow = EClass.sources.cards.map["money2"]; // [!code ++]
		} // [!code ++]
		return new RendererReplacer
		{
			tile = cardRow._tiles[0] + shift,
```

## SerializedCards

[`public void Restore(Map map, Map orgMap, bool addToZone, PartialMap partial = nu`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/cd73c4e897325ae140874f7d172e7a203aedfc4a/Elin/SerializedCards.cs#L872-L877)
```cs:line-numbers=872
			{
				card2.Chara.ScaleByPrincipal();
			}
			if (isUserZone) // [!code ++]
			{ // [!code ++]
				card2.isNPCProperty = true; // [!code ++]
			} // [!code ++]
		}
		if (card2.trait is TraitFakeTile)
		{
```

## Zone

[`public void Activate()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/cd73c4e897325ae140874f7d172e7a203aedfc4a/Elin/Zone.cs#L824-L830)
```cs:line-numbers=824
		}
		foreach (Chara c3 in map.serializedCharas)
		{
			if (c3.c_uidMaster == 0 || EClass.player.listSummon.Find((Chara c2) => c2.uid == c3.uid) == null) // [!code --]
			if (c3.c_uidMaster == 0 || EClass.player.listCarryoverMap.Find((Chara c2) => c2.uid == c3.uid) == null) // [!code ++]
			{
				map.charas.Add(c3);
				map.AddCardOnActivate(c3);
```

[`public void OnVisit()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/cd73c4e897325ae140874f7d172e7a203aedfc4a/Elin/Zone.cs#L1082-L1094)
```cs:line-numbers=1082
	}
	if (!EClass.game.isLoading)
	{
		foreach (Chara chara2 in EClass._map.charas) // [!code --]
		{ // [!code --]
			if (chara2.HasCondition<ConDeathSentense>()) // [!code --]
			{ // [!code --]
				chara2.RemoveCondition<ConDeathSentense>(); // [!code --]
			} // [!code --]
		} // [!code --]
		RefreshDeathSentense(); // [!code ++]
	}
	RefreshCriminal();
	EClass._map.rooms.AssignCharas();
```

[`public void OnVisit()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/cd73c4e897325ae140874f7d172e7a203aedfc4a/Elin/Zone.cs#L1116-L1122)
```cs:line-numbers=1116
		}
	}

	public void OnCharaDie() // [!code --]
	public void RefreshDeathSentense() // [!code ++]
	{
		foreach (Chara chara in EClass._map.charas)
		{
```

[`public void AddGlobalCharasOnActivate()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/cd73c4e897325ae140874f7d172e7a203aedfc4a/Elin/Zone.cs#L1626-L1632)
```cs:line-numbers=1626
			map.AddCardOnActivate(c);
		}
	}
	foreach (Chara item in EClass.player.listSummon) // [!code --]
	foreach (Chara item in EClass.player.listCarryoverMap) // [!code ++]
	{
		Point nearestPoint = spawnPosPC.GetNearestPoint(allowBlock: false, allowChara: false, allowInstalled: true, ignoreCenter: true);
		item.enemy = null;
```

[`public void AddGlobalCharasOnActivate()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/cd73c4e897325ae140874f7d172e7a203aedfc4a/Elin/Zone.cs#L1634-L1640)
```cs:line-numbers=1634
			map.charas.Add(item);
			map.AddCardOnActivate(item);
		}
		EClass.player.listSummon.Clear(); // [!code --]
		EClass.player.listCarryoverMap.Clear(); // [!code ++]
	}

	public void Deactivate()
```
