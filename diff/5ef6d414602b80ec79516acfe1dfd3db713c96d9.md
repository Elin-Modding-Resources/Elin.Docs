---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 4 files modified.
version: EA 23.237 Stable
changes: BaseCondition/Card/CraftUtil/TraitAltar
---

# EA 23.237 Stable

November 19, 2025

4 files modified.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [TraitAltar (1)](#traitaltar)
```cs:no-line-numbers
public override void OnSetOwner() // [!code --]

```
## BaseCondition

[`public override void _WriteNote(UINote n, bool asChild = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/5ef6d414602b80ec79516acfe1dfd3db713c96d9/Elin/BaseCondition.cs#L436-L449)
```cs:line-numbers=436
	}
	for (int j = 0; j < base.source.elements.Length; j += 2)
	{
		Element element = Element.Create(GetElementSource(j).id, base.source.elements[j + 1].Calc(power, 0, P2)); // [!code --]
		if (elements != null && elements.Has(element.id) && elements.Value(element.id) != element.Value) // [!code --]
		Element e = Element.Create(GetElementSource(j).id, base.source.elements[j + 1].Calc(power, 0, P2)); // [!code ++]
		if (e.IsFlag) // [!code ++]
		{
			list.Add("modValue".lang(element.Name, ((element.Value < 0) ? "" : "+") + elements.Value(element.id) + " (" + element.Value + ")")); // [!code --]
			list.Add(e.Name.TagColorGoodBad(() => e.Value >= 0)); // [!code ++]
		} // [!code ++]
		else if (elements != null && elements.Has(e.id) && elements.Value(e.id) != e.Value) // [!code ++]
		{ // [!code ++]
			list.Add("modValue".lang(e.Name, ((e.Value < 0) ? "" : "+") + elements.Value(e.id) + " (" + e.Value + ")").TagColor(() => e.Value >= 0)); // [!code ++]
		}
		else
		{
			list.Add("modValue".lang(element.Name, ((element.Value < 0) ? "" : "+") + element.Value)); // [!code --]
			list.Add("modValue".lang(e.Name, ((e.Value < 0) ? "" : "+") + e.Value).TagColorGoodBad(() => e.Value >= 0)); // [!code ++]
		}
	}
	if (list.Count <= 0)
```

## Card

[`public void PurgeDuplicateArtifact(Thing af)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/5ef6d414602b80ec79516acfe1dfd3db713c96d9/Elin/Card.cs#L3222-L3228)
```cs:line-numbers=3222
	{
		list.Add(chara);
	}
	if (EClass.pc.IsEyth && EClass.pc.HasElement(1228)) // [!code --]
	if (EClass.pc.IsEyth && EClass.pc.HasElement(1228) && af.c_idDeity == EClass.pc.faith.id) // [!code ++]
	{
		foreach (Chara item in list)
		{
```

## CraftUtil

[`public static Thing MakeBloodMeal(Chara sucker, Chara feeder)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/5ef6d414602b80ec79516acfe1dfd3db713c96d9/Elin/CraftUtil.cs#L132-L138)
```cs:line-numbers=132
	{
		thing.elements.Remove(701);
	}
	thing.elements.ModBase(710, 20 + (int)Mathf.Min(Mathf.Sqrt(sucker.Evalue(6607) + sucker.LER), 50f)); // [!code --]
	thing.elements.ModBase(710, 20 + (int)Mathf.Min(Mathf.Sqrt(sucker.Evalue(6607) + Mathf.Max(sucker.LER, 0)), 50f)); // [!code ++]
	thing.elements.SetTo(2, Mathf.Min(EClass.curve(feeder.LV, 30, 10, 65), 200));
	return thing;
}
```

## TraitAltar

[`public class TraitAltar : Trait`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/5ef6d414602b80ec79516acfe1dfd3db713c96d9/Elin/TraitAltar.cs#L16-L30)
```cs:line-numbers=16

	public string StrDeity => Deity.NameDomain;

	public bool IsEyth => idDeity == "eyth"; // [!code --]
 // [!code --]
	public override void OnSetOwner() // [!code --]
	{ // [!code --]
		if (idDeity == "void") // [!code --]
		{ // [!code --]
			owner.c_idDeity = "eyth"; // [!code --]
		} // [!code --]
	} // [!code --]
	public bool IsEyth => Deity.IsEyth; // [!code ++]

	public override void OnCreate(int lv)
	{
```

[`public override void TrySetAct(ActPlan p)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/5ef6d414602b80ec79516acfe1dfd3db713c96d9/Elin/TraitAltar.cs#L83-L88)
```cs:line-numbers=83

	public override bool CanOffer(Card c)
	{
		Debug.Log(Deity); // [!code ++]
		Debug.Log(Deity.id); // [!code ++]
		Debug.Log(IsEyth); // [!code ++]
		if (c != null && c.HasTag(CTAG.godArtifact))
		{
			if (EClass.pc.IsEyth && EClass.pc.HasElement(1228))
```
