---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 6 files modified.
version: EA 23.342 Nightly
changes: ActEffect/Chara/CoreConfig/EffectId/Game/UIInventory
---

# EA 23.342 Nightly

September 2, 2026

6 files modified.

## Important Changes

**None.**
## ActEffect

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L1652-L1663)
```cs:line-numbers=1652
}
EClass.ui.AddLayer<LayerList>().SetStringList(delegate
{
	List<string> list12 = new List<string>(); // [!code --]
	List<string> list13 = new List<string>(); // [!code ++]
	for (int j = 0; j < 10; j++)
	{
		list12.Add(AliasGen.GetRandomAlias()); // [!code --]
		list13.Add(AliasGen.GetRandomAlias()); // [!code ++]
	}
	return list12; // [!code --]
	return list13; // [!code ++]
}, delegate(int a, string b)
{
	c._alias = b;
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L1814-L1828)
```cs:line-numbers=1814
	break;
case EffectId.StripBlessing:
{
	List<Condition> list5 = new List<Condition>(); // [!code --]
	List<Condition> list6 = new List<Condition>(); // [!code ++]
	foreach (Condition condition4 in TC.conditions)
	{
		if (GetBlessingDifficulty(condition4) > 0 && EClass.rnd(GetBlessingDifficulty(condition4)) == 0)
		{
			list5.Add(condition4); // [!code --]
			list6.Add(condition4); // [!code ++]
		}
	}
	if (list5.Count == 0) // [!code --]
	if (list6.Count == 0) // [!code ++]
	{
		CC.SayNothingHappans();
		break;
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L1830-L1838)
```cs:line-numbers=1830
TC.pos.PlayEffect("holyveil");
TC.pos.PlaySound("holyveil");
TC.Say("unpolluted", TC);
list5.Shuffle(); // [!code --]
list6.Shuffle(); // [!code ++]
{
	foreach (Condition item5 in list5) // [!code --]
	foreach (Condition item5 in list6) // [!code ++]
	{
		item5.Kill();
		if (CC.IsHostile(TC))
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L1849-L1863)
```cs:line-numbers=1849
{
	break;
}
int num18 = 0; // [!code --]
int num21 = 0; // [!code ++]
foreach (Condition condition5 in TC.conditions)
{
	if (condition5.Type == ConditionType.Debuff)
	{
		num18++; // [!code --]
		num21++; // [!code ++]
	}
}
if (num18 == 0) // [!code --]
if (num21 == 0) // [!code ++]
{
	CC.SayNothingHappans();
	break;
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L1874-L1887)
```cs:line-numbers=1874
});
TC.Say("abShutterHex", TC);
Point center = CC.pos.Copy();
List<Chara> list8 = TC.pos.ListCharasInRadius(TC, 4, (Chara chara3) => chara3 == TC || chara3.IsHostile(CC)); // [!code --]
for (int num19 = 0; num19 < num18; num19++) // [!code --]
List<Chara> list10 = TC.pos.ListCharasInRadius(TC, 4, (Chara chara3) => chara3 == TC || chara3.IsHostile(CC)); // [!code ++]
for (int num22 = 0; num22 < num21; num22++) // [!code ++]
{
	TweenUtil.Delay((float)num19 * 0.1f, delegate // [!code --]
	TweenUtil.Delay((float)num22 * 0.1f, delegate // [!code ++]
	{
		center.PlaySound("shutterhex");
	});
	foreach (Chara item6 in list8) // [!code --]
	foreach (Chara item6 in list10) // [!code ++]
	{
		if (item6.ExistsOnMap)
		{
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L1890-L1896)
```cs:line-numbers=1890
Color startColor = (componentInChildren.endColor = EClass.Colors.elementColors["eleHoly"]);
componentInChildren.startColor = startColor;
Point pos = item6.pos.Copy();
TweenUtil.Delay((float)num19 * 0.1f, delegate // [!code --]
TweenUtil.Delay((float)num22 * 0.1f, delegate // [!code ++]
{
	effect.Play(center, 0f, pos);
});
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L1950-L1956)
```cs:line-numbers=1950
	TC.Say((actRef.n1 == "money") ? "abStealNegateMoney" : "abStealNegate", TC);
	break;
}
Thing thing5 = null; // [!code --]
Thing thing7 = null; // [!code ++]
bool flag11 = actRef.n1 == "food";
if (actRef.n1 == "money")
{
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L1958-L1964)
```cs:line-numbers=1958
	if (currency > 0)
	{
		currency = Mathf.Clamp(EClass.rnd(currency / 10), 1, 100 + EClass.rndHalf(Mathf.Min(CC.LV, 1000000) * 200));
		thing5 = ThingGen.Create("money").SetNum(currency); // [!code --]
		thing7 = ThingGen.Create("money").SetNum(currency); // [!code ++]
		TC.ModCurrency(-currency);
	}
}
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L1969-L1975)
```cs:line-numbers=1969
{
	func = (Thing thing8) => thing8.IsFood;
}
List<Thing> list6 = TC.things.List(delegate(Thing thing8) // [!code --]
List<Thing> list12 = TC.things.List(delegate(Thing thing8) // [!code ++]
{
	if (thing8.parentCard?.trait is TraitChestMerchant || thing8.trait is TraitTool || thing8.IsThrownWeapon)
	{
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L1977-L2006)
```cs:line-numbers=1977
		}
		return thing8.trait.CanBeDestroyed && thing8.things.Count == 0 && thing8.invY != 1 && thing8.trait.CanBeStolen && !thing8.trait.CanOnlyCarry && !thing8.IsUnique && !thing8.isEquipped && thing8.blessedState == BlessedState.Normal && func(thing8);
	}, onlyAccessible: true);
	if (list6.Count > 0) // [!code --]
	if (list12.Count > 0) // [!code ++]
	{
		thing5 = list6.RandomItem(); // [!code --]
		if (thing5.Num > 1) // [!code --]
		thing7 = list12.RandomItem(); // [!code ++]
		if (thing7.Num > 1) // [!code ++]
		{
			thing5 = thing5.Split(1); // [!code --]
			thing7 = thing7.Split(1); // [!code ++]
		}
	}
	CC.AddCooldown(6640, 200);
}
if (thing5 == null) // [!code --]
if (thing7 == null) // [!code ++]
{
	CC.Say("abStealNothing", CC, TC);
	break;
}
thing5.SetInt(116, 1); // [!code --]
TC.PlaySound(thing5.material.GetSoundDrop(thing5.sourceCard)); // [!code --]
CC.Pick(thing5, msg: false); // [!code --]
CC.Say("abSteal", CC, TC, thing5.Name); // [!code --]
thing7.SetInt(116, 1); // [!code ++]
TC.PlaySound(thing7.material.GetSoundDrop(thing7.sourceCard)); // [!code ++]
CC.Pick(thing7, msg: false); // [!code ++]
CC.Say("abSteal", CC, TC, thing7.Name); // [!code ++]
if (actRef.n1 == "food")
{
	if (CC.hunger.value != 0)
	{
		CC.InstantEat(thing5); // [!code --]
		CC.InstantEat(thing7); // [!code ++]
	}
}
else
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2025-L2031)
```cs:line-numbers=2025
{
	break;
}
List<Thing> list10 = TC.things.List(delegate(Thing thing8) // [!code --]
List<Thing> list4 = TC.things.List(delegate(Thing thing8) // [!code ++]
{
	if (!thing8.isEquipped || thing8.blessedState == BlessedState.Doomed || thing8.IsToolbelt)
	{
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2033-L2047)
```cs:line-numbers=2033
		}
		return (thing8.blessedState < BlessedState.Blessed || EClass.rnd(10) == 0) ? true : false;
	});
	if (list10.Count == 0) // [!code --]
	if (list4.Count == 0) // [!code ++]
	{
		CC.SayNothingHappans();
		break;
	}
	Thing thing7 = list10.RandomItem(); // [!code --]
	TC.Say("curse_hit", TC, thing7); // [!code --]
	thing7.SetBlessedState((thing7.blessedState == BlessedState.Cursed) ? BlessedState.Doomed : BlessedState.Cursed); // [!code --]
	LayerInventory.SetDirty(thing7); // [!code --]
	Thing thing5 = list4.RandomItem(); // [!code ++]
	TC.Say("curse_hit", TC, thing5); // [!code ++]
	thing5.SetBlessedState((thing5.blessedState == BlessedState.Cursed) ? BlessedState.Doomed : BlessedState.Cursed); // [!code ++]
	LayerInventory.SetDirty(thing5); // [!code ++]
	break;
}
case EffectId.UncurseEQ:
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2057-L2085)
```cs:line-numbers=2057
}
int success = 0;
int fail = 0;
List<Thing> list11 = new List<Thing>(); // [!code --]
List<Thing> list9 = new List<Thing>(); // [!code ++]
TC.things.Foreach(delegate(Thing thing8)
{
	int num23 = 0; // [!code --]
	int num25 = 0; // [!code ++]
	if ((thing8.isEquipped || thing8.IsRangedWeapon || blessed) && thing8.blessedState < BlessedState.Normal)
	{
		if (thing8.blessedState == BlessedState.Cursed)
		{
			num23 = EClass.rnd(200); // [!code --]
			num25 = EClass.rnd(200); // [!code ++]
		}
		if (thing8.blessedState == BlessedState.Doomed)
		{
			num23 = EClass.rnd(1000); // [!code --]
			num25 = EClass.rnd(1000); // [!code ++]
		}
		if (blessed)
		{
			num23 /= 2; // [!code --]
			num25 /= 2; // [!code ++]
		}
		if (id == EffectId.UncurseEQGreater)
		{
			num23 /= 10; // [!code --]
			num25 /= 10; // [!code ++]
		}
		if (power >= num23) // [!code --]
		if (power >= num25) // [!code ++]
		{
			TC.Say("uncurseEQ_success", thing8);
			thing8.SetBlessedState(BlessedState.Normal);
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2089-L2095)
```cs:line-numbers=2089
	}
	LayerInventory.SetDirty(thing8);
	success++;
	list11.Add(thing8); // [!code --]
	list9.Add(thing8); // [!code ++]
}
else
{
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2097-L2103)
```cs:line-numbers=2097
		}
	}
});
foreach (Thing item7 in list11) // [!code --]
foreach (Thing item7 in list9) // [!code ++]
{
	item7.GetRootCard()?.TryStack(item7);
}
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2123-L2140)
```cs:line-numbers=2123
		text2 = text3;
	}
}
Condition condition2 = Condition.Create(text2, power, delegate(Condition con) // [!code --]
Condition condition = Condition.Create(text2, power, delegate(Condition con) // [!code ++]
{
	if (!actRef.aliasEle.IsEmpty())
	{
		con.SetElement(EClass.sources.elements.alias[actRef.aliasEle].id);
	}
});
condition2.isPerfume = TC.IsPC && actRef.isPerfume; // [!code --]
Condition condition3 = TC.AddCondition(condition2); // [!code --]
if (condition3 != null && condition3.isPerfume) // [!code --]
condition.isPerfume = TC.IsPC && actRef.isPerfume; // [!code ++]
Condition condition2 = TC.AddCondition(condition); // [!code ++]
if (condition2 != null && condition2.isPerfume) // [!code ++]
{
	condition3.value = 3; // [!code --]
	condition2.value = 3; // [!code ++]
	Msg.Say("perfume", TC);
}
if (!text3.IsEmpty())
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2149-L2155)
```cs:line-numbers=2149
bool hex = CC.IsHostile(TC);
List<SourceStat.Row> list7 = EClass.sources.stats.rows.Where((SourceStat.Row con) => con.tag.Contains("random") && con.group == (hex ? "Debuff" : "Buff")).ToList();
int power2 = power;
for (int num17 = 0; num17 < 4 + EClass.rnd(2); num17++) // [!code --]
for (int num20 = 0; num20 < 4 + EClass.rnd(2); num20++) // [!code ++]
{
	SourceStat.Row row2 = list7.RandomItem();
	list7.Remove(row2);
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2174-L2207)
```cs:line-numbers=2174
{
	power = power * 2 / 3;
}
int num14 = TC.WIL * (isPowerful ? 20 : 5); // [!code --]
ConHolyVeil condition = TC.GetCondition<ConHolyVeil>(); // [!code --]
if (condition != null) // [!code --]
int num13 = TC.WIL * (isPowerful ? 20 : 5); // [!code ++]
ConHolyVeil condition3 = TC.GetCondition<ConHolyVeil>(); // [!code ++]
if (condition3 != null) // [!code ++]
{
	num14 += condition.power * 5; // [!code --]
	num13 += condition3.power * 5; // [!code ++]
}
num14 /= EClass.sources.stats.alias[n].hexPower; // [!code --]
if (id != EffectId.DebuffKizuami && EClass.rnd(power) < num14) // [!code --]
num13 /= EClass.sources.stats.alias[n].hexPower; // [!code ++]
if (id != EffectId.DebuffKizuami && EClass.rnd(power) < num13) // [!code ++]
{
	int num15 = 10; // [!code --]
	int num16 = (TC.IsPowerful ? 40 : 90); // [!code --]
	int num14 = 10; // [!code ++]
	int num15 = (TC.IsPowerful ? 40 : 90); // [!code ++]
	if (CC.IsPCFaction)
	{
		num15 += EClass.curve(CC.LER, 100, 50) / 10; // [!code --]
		num14 += EClass.curve(CC.LER, 100, 50) / 10; // [!code ++]
	}
	if (TC.IsPowerful)
	{
		num15 /= 2; // [!code --]
		num14 /= 2; // [!code ++]
	}
	if (condition != null) // [!code --]
	if (condition3 != null) // [!code ++]
	{
		num15 /= 5; // [!code --]
		num14 /= 5; // [!code ++]
	}
	if (num15 >= num16) // [!code --]
	if (num14 >= num15) // [!code ++]
	{
		num15 = num16; // [!code --]
		num14 = num15; // [!code ++]
	}
	if (EClass.rnd(100) > num15) // [!code --]
	if (EClass.rnd(100) > num14) // [!code ++]
	{
		TC.Say("debuff_resist", TC);
		CC.DoHostileAction(TC);
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2242-L2255)
```cs:line-numbers=2242
case EffectId.Ally:
{
	Msg.Say("gainAlly");
	Chara chara = CharaGen.CreateFromFilter("chara", cc.LV); // [!code --]
	EClass._zone.AddCard(chara, cc.pos.GetNearestPoint(allowBlock: false, allowChara: false)); // [!code --]
	Chara chara2 = CharaGen.CreateFromFilter("chara", cc.LV); // [!code ++]
	EClass._zone.AddCard(chara2, cc.pos.GetNearestPoint(allowBlock: false, allowChara: false)); // [!code ++]
	if (cc.IsPCFactionOrMinion)
	{
		chara.MakeAlly(msg: false); // [!code --]
		chara2.MakeAlly(msg: false); // [!code ++]
	}
	chara.PlaySound("identify"); // [!code --]
	chara.PlayEffect("teleport"); // [!code --]
	chara2.PlaySound("identify"); // [!code ++]
	chara2.PlayEffect("teleport"); // [!code ++]
	break;
}
case EffectId.Wish:
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2310-L2325)
```cs:line-numbers=2310
	tc.bio.SetGender(gender2);
	tc.Say("transGender", tc, Gender.Name(tc.bio.gender));
	tc.Talk("tail");
	int age3 = tc.bio.GetAge(tc.Chara); // [!code --]
	if (blessed && age3 > 1) // [!code --]
	int age2 = tc.bio.GetAge(tc.Chara); // [!code ++]
	if (blessed && age2 > 1) // [!code ++]
	{
		tc.Say("ageDown", tc);
		tc.bio.SetAge(tc.Chara, age3 - 1); // [!code --]
		tc.bio.SetAge(tc.Chara, age2 - 1); // [!code ++]
	}
	else if (flag)
	{
		tc.Say("ageUp", tc);
		tc.bio.SetAge(tc.Chara, age3 + 1); // [!code --]
		tc.bio.SetAge(tc.Chara, age2 + 1); // [!code ++]
	}
	break;
}
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2327-L2346)
```cs:line-numbers=2327
{
	tc.PlaySound("mutation");
	tc.PlayEffect("mutation");
	int num13 = ((actRef.refThing != null) ? actRef.refThing.GetInt(118) : actRef.refVal); // [!code --]
	if (num13 == 0) // [!code --]
	int num17 = ((actRef.refThing != null) ? actRef.refThing.GetInt(118) : actRef.refVal); // [!code ++]
	if (num17 == 0) // [!code ++]
	{
		num13 = tc.GetInt(118); // [!code --]
		if (num13 == 0) // [!code --]
		num17 = tc.GetInt(118); // [!code ++]
		if (num17 == 0) // [!code ++]
		{
			num13 = EClass.game.seed + tc.uid; // [!code --]
			num17 = EClass.game.seed + tc.uid; // [!code ++]
		}
		num13++; // [!code --]
		num17++; // [!code ++]
	}
	tc.Say("transBlood", tc);
	tc.Talk("tail");
	tc.c_bloodData = null;
	tc.SetInt(118, num13); // [!code --]
	tc.SetInt(118, num17); // [!code ++]
	break; // [!code ++]
} // [!code ++]
case EffectId.ResetFeat: // [!code ++]
{ // [!code ++]
	if (!tc.IsPC) // [!code ++]
	{ // [!code ++]
		tc.SayNothingHappans(); // [!code ++]
		break; // [!code ++]
	} // [!code ++]
	tc.PlaySound("mutation"); // [!code ++]
	tc.PlayEffect("mutation"); // [!code ++]
	List<Element> list3 = new List<Element>(); // [!code ++]
	foreach (Element value in EClass.pc.elements.dict.Values) // [!code ++]
	{ // [!code ++]
		if (value.source.category != "feat" || value.id == 1644) // [!code ++]
		{ // [!code ++]
			continue; // [!code ++]
		} // [!code ++]
		switch (value.source.categorySub) // [!code ++]
		{ // [!code ++]
		case "general": // [!code ++]
		case "attribute": // [!code ++]
		case "skill": // [!code ++]
		case "special": // [!code ++]
			if (value.vBase > 0) // [!code ++]
			{ // [!code ++]
				list3.Add(value); // [!code ++]
			} // [!code ++]
			break; // [!code ++]
		} // [!code ++]
	} // [!code ++]
	int num10 = 0; // [!code ++]
	foreach (Element item8 in list3) // [!code ++]
	{ // [!code ++]
		for (int num11 = 0; num11 < item8.vBase; num11++) // [!code ++]
		{ // [!code ++]
			num10 += item8.source.cost.TryGet(num11); // [!code ++]
		} // [!code ++]
		item8.vBase = 0; // [!code ++]
		EClass.pc.SetFeat(item8.Value, 0); // [!code ++]
	} // [!code ++]
	EClass.pc.feat += num10; // [!code ++]
	EClass.pc.Refresh(); // [!code ++]
	EClass.pc.RefreshFaithElement(); // [!code ++]
	LayerAbility.Redraw(); // [!code ++]
	LayerInventory.SetDirtyAll(); // [!code ++]
	LayerEquip.SetDirty(); // [!code ++]
	tc.Say("resetFeat"); // [!code ++]
	break;
}
case EffectId.Youth:
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2367-L2373)
```cs:line-numbers=2367
	tc.SayNothingHappans();
	break;
}
int age2 = tc.bio.GetAge(tc.Chara); // [!code --]
int age3 = tc.bio.GetAge(tc.Chara); // [!code ++]
if (flag)
{
	if (tc.c_lockedAge != 0)
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2375-L2381)
```cs:line-numbers=2375
		tc.Say("eternalYouth2", tc);
		tc.c_lockedAge = 0;
		tc.elements.Remove(1243);
		tc.bio.SetAge(tc.Chara, age2); // [!code --]
		tc.bio.SetAge(tc.Chara, age3); // [!code ++]
	}
	Redirect(EffectId.Youth, BlessedState.Cursed, default(ActRef));
}
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2387-L2393)
```cs:line-numbers=2387
{
	tc.PlaySound("dropRewardXmas");
	tc.Say("eternalYouth1", tc);
	tc.c_lockedAge = age2 + 1; // [!code --]
	tc.c_lockedAge = age3 + 1; // [!code ++]
	tc.elements.SetBase(1243, 1);
	if (blessed)
	{
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2431-L2446)
```cs:line-numbers=2431
	break;
case EffectId.Revive:
{
	List<KeyValuePair<int, Chara>> list3 = EClass.game.cards.globalCharas.Where((KeyValuePair<int, Chara> a) => a.Value.isDead && a.Value.faction == EClass.pc.faction && !a.Value.isSummon && a.Value.c_wasInPcParty).ToList(); // [!code --]
	List<KeyValuePair<int, Chara>> list11 = EClass.game.cards.globalCharas.Where((KeyValuePair<int, Chara> a) => a.Value.isDead && a.Value.faction == EClass.pc.faction && !a.Value.isSummon && a.Value.c_wasInPcParty).ToList(); // [!code ++]
	if (TC.IsPCFaction || TC.IsPCFactionMinion)
	{
		if (TC.IsPC && list3.Count == 0) // [!code --]
		if (TC.IsPC && list11.Count == 0) // [!code ++]
		{
			list3 = EClass.game.cards.globalCharas.Where((KeyValuePair<int, Chara> a) => a.Value.CanRevive() && a.Value.isDead && a.Value.faction == EClass.pc.faction && !a.Value.isSummon).ToList(); // [!code --]
			list11 = EClass.game.cards.globalCharas.Where((KeyValuePair<int, Chara> a) => a.Value.CanRevive() && a.Value.isDead && a.Value.faction == EClass.pc.faction && !a.Value.isSummon).ToList(); // [!code ++]
		}
		if (list3.Count > 0) // [!code --]
		if (list11.Count > 0) // [!code ++]
		{
			list3.RandomItem().Value.Chara.GetRevived(); // [!code --]
			list11.RandomItem().Value.Chara.GetRevived(); // [!code ++]
			break;
		}
	}
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2453-L2476)
```cs:line-numbers=2453
case EffectId.DamageMindGreat:
case EffectId.Weaken:
{
	bool flag10 = id == EffectId.DamageBody || id == EffectId.DamageBodyGreat; // [!code --]
	bool flag9 = id == EffectId.DamageBody || id == EffectId.DamageBodyGreat; // [!code ++]
	bool mind = id == EffectId.DamageMind || id == EffectId.DamageMindGreat;
	int num10 = ((id == EffectId.DamageBody || id == EffectId.DamageMind) ? 1 : (4 + EClass.rnd(4))); // [!code --]
	int num18 = ((id == EffectId.DamageBody || id == EffectId.DamageMind) ? 1 : (4 + EClass.rnd(4))); // [!code ++]
	if (id == EffectId.Weaken)
	{
		flag10 = EClass.rnd(2) == 0; // [!code --]
		mind = !flag10; // [!code --]
		num10 = 1; // [!code --]
		flag9 = EClass.rnd(2) == 0; // [!code ++]
		mind = !flag9; // [!code ++]
		num18 = 1; // [!code ++]
	}
	else
	{
		TC.PlayEffect("debuff");
		TC.PlaySound("debuff");
	}
	TC.Say(flag10 ? "damageBody" : "damageMind", TC); // [!code --]
	for (int num11 = 0; num11 < num10; num11++) // [!code --]
	TC.Say(flag9 ? "damageBody" : "damageMind", TC); // [!code ++]
	for (int num19 = 0; num19 < num18; num19++) // [!code ++]
	{
		TC.DamageTempElements(power, flag10, mind, id != EffectId.Weaken); // [!code --]
		TC.DamageTempElements(power, flag9, mind, id != EffectId.Weaken); // [!code ++]
	}
	if (TC.IsPC)
	{
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2483-L2516)
```cs:line-numbers=2483
case EffectId.EnhanceBodyGreat:
case EffectId.EnhanceMindGreat:
{
	bool flag12 = id == EffectId.EnhanceBody || id == EffectId.EnhanceBodyGreat; // [!code --]
	bool flag10 = id == EffectId.EnhanceBody || id == EffectId.EnhanceBodyGreat; // [!code ++]
	bool mind2 = id == EffectId.EnhanceMind || id == EffectId.EnhanceMindGreat;
	if (id != EffectId.EnhanceBody && id != EffectId.EnhanceMind)
	{
		EClass.rnd(4);
	}
	TC.Say(flag12 ? "enhanceBody" : "enhanceMind", TC); // [!code --]
	TC.Say(flag10 ? "enhanceBody" : "enhanceMind", TC); // [!code ++]
	TC.PlayEffect("buff");
	TC.PlaySound("buff");
	TC.EnhanceTempElements(power, flag12, mind2, onlyRenew: true); // [!code --]
	TC.EnhanceTempElements(power, flag10, mind2, onlyRenew: true); // [!code ++]
	break;
}
case EffectId.RestoreBody:
case EffectId.RestoreMind:
{
	bool flag9 = id == EffectId.RestoreBody; // [!code --]
	bool flag12 = id == EffectId.RestoreBody; // [!code ++]
	if (flag)
	{
		Redirect(flag9 ? EffectId.DamageBodyGreat : EffectId.DamageMindGreat, BlessedState.Normal, default(ActRef)); // [!code --]
		Redirect(flag12 ? EffectId.DamageBodyGreat : EffectId.DamageMindGreat, BlessedState.Normal, default(ActRef)); // [!code ++]
		break;
	}
	TC.Say(flag9 ? "restoreBody" : "restoreMind", TC); // [!code --]
	TC.Say(flag12 ? "restoreBody" : "restoreMind", TC); // [!code ++]
	TC.PlaySound("heal");
	TC.PlayEffect("heal");
	TC.CureHost(flag9 ? CureType.CureBody : CureType.CureMind, power, state); // [!code --]
	TC.CureHost(flag12 ? CureType.CureBody : CureType.CureMind, power, state); // [!code ++]
	if (blessed)
	{
		Redirect(flag9 ? EffectId.EnhanceBodyGreat : EffectId.EnhanceMindGreat, BlessedState.Normal, default(ActRef)); // [!code --]
		Redirect(flag12 ? EffectId.EnhanceBodyGreat : EffectId.EnhanceMindGreat, BlessedState.Normal, default(ActRef)); // [!code ++]
	}
	break;
}
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2565-L2592)
```cs:line-numbers=2565
	Redirect(EffectId.CurseEQ, BlessedState.Normal, default(ActRef));
	break;
}
IList<Condition> list4 = TC.conditions.Copy(); // [!code --]
list4.Shuffle(); // [!code --]
IList<Condition> list5 = TC.conditions.Copy(); // [!code ++]
list5.Shuffle(); // [!code ++]
CC.PlaySound("remove_hex");
CC.PlayEffect("remove_hex");
foreach (Condition item8 in list4) // [!code --]
foreach (Condition item9 in list5) // [!code ++]
{
	if (item8.Type != ConditionType.Debuff || item8.IsKilled) // [!code --]
	if (item9.Type != ConditionType.Debuff || item9.IsKilled) // [!code ++]
	{
		continue;
	}
	if (EClass.rnd(power * (CC.IsPowerful ? 5 : 2)) > EClass.rnd(item8.power) || (id == EffectId.RemoveHex && EClass.rnd(2) == 0)) // [!code --]
	if (EClass.rnd(power * (CC.IsPowerful ? 5 : 2)) > EClass.rnd(item9.power) || (id == EffectId.RemoveHex && EClass.rnd(2) == 0)) // [!code ++]
	{
		CC.Say("removeHex", TC, item8.Name.ToLower()); // [!code --]
		item8.Kill(); // [!code --]
		CC.Say("removeHex", TC, item9.Name.ToLower()); // [!code ++]
		item9.Kill(); // [!code ++]
	}
	else
	{
		if (id == EffectId.RemoveHexAll)
		{
			item8.power /= 2; // [!code --]
			item9.power /= 2; // [!code ++]
		}
		CC.Say("removeHex_fail", TC, item8.Name.ToLower()); // [!code --]
		CC.Say("removeHex_fail", TC, item9.Name.ToLower()); // [!code ++]
	}
	if (id == EffectId.RemoveHex)
	{
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2621-L2631)
```cs:line-numbers=2621
		CC.SayNothingHappans();
		break;
	}
	Chara chara2 = CharaGen.Create("baby"); // [!code --]
	chara2.c_altName = TC.c_altName ?? TC.source.GetName(TC); // [!code --]
	Chara chara = CharaGen.Create("baby"); // [!code ++]
	chara.c_altName = TC.c_altName ?? TC.source.GetName(TC); // [!code ++]
	TC.AddCondition(Condition.Create(power, delegate(ConTransmuteHuman con)
	{
		con.chara = chara2; // [!code --]
		con.chara = chara; // [!code ++]
	}));
	break;
}
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2684-L2691)
```cs:line-numbers=2684
if (TC.HasElement(1211))
{
	TC.Say("drinkSaltWater_snail", TC);
	int num22 = ((TC.hp > 10) ? (TC.hp - EClass.rnd(10)) : 10000); // [!code --]
	TC.DamageHP(num22, AttackSource.None, CC); // [!code --]
	int num16 = ((TC.hp > 10) ? (TC.hp - EClass.rnd(10)) : 10000); // [!code ++]
	TC.DamageHP(num16, AttackSource.None, CC); // [!code ++]
}
else if (TC.IsPC)
{
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2812-L2837)
```cs:line-numbers=2812
{
	TC.PlayEffect("buff");
	TC.PlaySound("offering");
	int num20 = TC.Evalue(1423); // [!code --]
	if (num20 > 0) // [!code --]
	int num23 = TC.Evalue(1423); // [!code ++]
	if (num23 > 0) // [!code ++]
	{
		int num21 = 0; // [!code --]
		if (flag && num20 > 1) // [!code --]
		int num24 = 0; // [!code ++]
		if (flag && num23 > 1) // [!code ++]
		{
			num21 = -1; // [!code --]
			num24 = -1; // [!code ++]
		}
		else if (num20 < 15) // [!code --]
		else if (num23 < 15) // [!code ++]
		{
			num21 = ((!blessed) ? 1 : 2); // [!code --]
			if (num21 + num20 > 15) // [!code --]
			num24 = ((!blessed) ? 1 : 2); // [!code ++]
			if (num24 + num23 > 15) // [!code ++]
			{
				num21 = 1; // [!code --]
				num24 = 1; // [!code ++]
			}
		}
		if (num21 != 0) // [!code --]
		if (num24 != 0) // [!code ++]
		{
			TC.SetFeat(1423, num20 + num21, msg: true); // [!code --]
			TC.SetInt(134, TC.GetInt(134) + num21); // [!code --]
			TC.SetFeat(1423, num23 + num24, msg: true); // [!code ++]
			TC.SetInt(134, TC.GetInt(134) + num24); // [!code ++]
		}
	}
}
```

[`void AddCon<T>(int rate, int p2) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/ActEffect.cs#L2879-L2888)
```cs:line-numbers=2879
{
	power /= 4;
}
List<Thing> list9 = TC.things.List((Thing thing8) => (thing8.Num <= 1 && thing8.IsEquipmentOrRanged && !thing8.IsToolbelt && !thing8.IsLightsource && thing8.isEquipped) ? true : false); // [!code --]
if (list9.Count != 0) // [!code --]
List<Thing> list8 = TC.things.List((Thing thing8) => (thing8.Num <= 1 && thing8.IsEquipmentOrRanged && !thing8.IsToolbelt && !thing8.IsLightsource && thing8.isEquipped) ? true : false); // [!code ++]
if (list8.Count != 0) // [!code ++]
{
	Thing thing6 = list9.RandomItem(); // [!code --]
	Thing thing6 = list8.RandomItem(); // [!code ++]
	TC.Say("acid_hit", TC);
	if (thing6.isAcidproof)
	{
```

## Chara

[`public void SetFeat(int id, int value = 1, bool msg = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/Chara.cs#L10502-L10508)
```cs:line-numbers=10502
	feat.Apply(-feat.Value, elements);
}
feat = elements.SetBase(id, value - (feat?.vSource ?? 0)) as Feat;
if (feat.Value != 0) // [!code --]
if (feat != null && feat.Value != 0) // [!code ++]
{
	feat.Apply(feat.Value, elements);
}
```

[`public void SetFeat(int id, int value = 1, bool msg = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/Chara.cs#L10515-L10553)
```cs:line-numbers=10515
	{
		return;
	}
	if (feat.source.textInc.IsEmpty()) // [!code --]
	if (feat != null) // [!code ++]
	{
		PlaySound("ding_skill"); // [!code --]
		Msg.SetColor(Msg.colors.Ding); // [!code --]
		Say("gainFeat", this, feat.FullName); // [!code --]
	} // [!code --]
	else // [!code --]
	{ // [!code --]
		bool flag = value < num; // [!code --]
		if (feat.source.tag.Contains("neg")) // [!code --]
		if (feat.source.textInc.IsEmpty()) // [!code ++]
		{
			flag = !flag; // [!code --]
			PlaySound("ding_skill"); // [!code ++]
			Msg.SetColor(Msg.colors.Ding); // [!code ++]
			Say("gainFeat", this, feat.FullName); // [!code ++]
		}
		switch (id) // [!code --]
		else // [!code ++]
		{
		case 1270: // [!code --]
			if (!flag) // [!code --]
			bool flag = value < num; // [!code ++]
			if (feat.source.tag.Contains("neg")) // [!code ++]
			{
				PlaySound("chime_angel"); // [!code --]
				flag = !flag; // [!code ++]
			}
			break; // [!code --]
		case 1271: // [!code --]
			if (!flag) // [!code --]
			switch (id) // [!code ++]
			{
				PlaySound("chime_devil"); // [!code --]
			case 1270: // [!code ++]
				if (!flag) // [!code ++]
				{ // [!code ++]
					PlaySound("chime_angel"); // [!code ++]
				} // [!code ++]
				break; // [!code ++]
			case 1271: // [!code ++]
				if (!flag) // [!code ++]
				{ // [!code ++]
					PlaySound("chime_devil"); // [!code ++]
				} // [!code ++]
				break; // [!code ++]
			default: // [!code ++]
				PlaySound("mutation"); // [!code ++]
				break; // [!code ++]
			}
			break; // [!code --]
		default: // [!code --]
			PlaySound("mutation"); // [!code --]
			break; // [!code --]
			Msg.SetColor(flag ? Msg.colors.Negative : Msg.colors.Ding); // [!code ++]
			Say((value > num) ? feat.source.GetText("textInc") : feat.source.GetText("textDec"), this, feat.FullName); // [!code ++]
		}
		Msg.SetColor(flag ? Msg.colors.Negative : Msg.colors.Ding); // [!code --]
		Say((value > num) ? feat.source.GetText("textInc") : feat.source.GetText("textDec"), this, feat.FullName); // [!code --]
	}
	elements.CheckSkillActions();
}
```

## CoreConfig

[`public class UISetting`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/CoreConfig.cs#L392-L395)
```cs:line-numbers=392
	public int outlineAlpha;

	public int maxInvColumn = 40; // [!code ++]
 // [!code ++]
	public float ScrollSensitivity => baseScrollSens * scrollSens * scrollSens;
}
```

## EffectId

[`public enum EffectId`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/EffectId.cs#L144-L148)
```cs:line-numbers=144
	Fuse = 320,
	BlueDrag = 321,
	Comet = 322,
	Missile = 323 // [!code --]
	Missile = 323, // [!code ++]
	ResetFeat = 324 // [!code ++]
}
```

## Game

[`public void ApplyFix()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/Game.cs#L502-L510)
```cs:line-numbers=502
});
if (version.nightly)
{
	if (EClass.game.quests.IsCompleted("into_darkness") && EClass.pc.body.GetSlot(46, onlyEmpty: false) == null) // [!code --]
	if (EClass.game.quests.IsCompleted("into_darkness")) // [!code ++]
	{
		CoreDebug._AddBodyPart(CoreDebug.BodyCode.relic); // [!code --]
		if (EClass.pc.body.GetSlot(46, onlyEmpty: false) == null) // [!code ++]
		{ // [!code ++]
			CoreDebug._AddBodyPart(CoreDebug.BodyCode.relic); // [!code ++]
		} // [!code ++]
		if (!player.recipes.IsKnown("relic_lesser") || EClass.debug.enable) // [!code ++]
		{ // [!code ++]
			player.recipes.Add("relic_lesser"); // [!code ++]
		} // [!code ++]
	}
}
else if (EClass.pc.body.GetSlot(46, onlyEmpty: false) != null)
```

## UIInventory

[`public void RefreshMenu()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c0b0a984236d6404ce70216b57d9f2ae1fea3a1d/Elin/UIInventory.cs#L745-L751)
```cs:line-numbers=745
	{
		data.columns = (int)num;
		RefreshGrid();
	}, 0f, 20f, isInt: true, hideOther: false); // [!code --]
	}, 0f, EMono.core.config.ui.maxInvColumn, isInt: true, hideOther: false); // [!code ++]
}
uIContextMenu6.AddButton("colorGrid", delegate
{
```
