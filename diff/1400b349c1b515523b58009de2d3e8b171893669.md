---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 2 files modified.
version: +EA 23.180 Nightly - Plugin.UI
changes: TooltipManager/UITooltip
---

# +EA 23.180 Nightly - Plugin.UI

August 15, 2025

2 files modified.

## Important Changes

**None.**
## TooltipManager

[`public void ShowTooltip(TooltipData data, Transform target)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/1400b349c1b515523b58009de2d3e8b171893669/Elin/Plugins.UI/TooltipManager.cs#L38-L109)
```cs:line-numbers=38
				return;
			}
		}
		UITooltip uITooltip = tooltips[0]; // [!code --]
		UITooltip t = tooltips[0]; // [!code ++]
		UITooltip[] array = tooltips;
		foreach (UITooltip uITooltip2 in array) // [!code --]
		foreach (UITooltip uITooltip in array) // [!code ++]
		{
			if (uITooltip2.name == data.id) // [!code --]
			if (uITooltip.name == data.id) // [!code ++]
			{
				uITooltip = uITooltip2; // [!code --]
				t = uITooltip; // [!code ++]
			}
		}
		uITooltip.SetActive(enable: true); // [!code --]
		uITooltip.SetData(data); // [!code --]
		if (uITooltip.cg.alpha < 0.1f) // [!code --]
		t.SetActive(enable: true); // [!code ++]
		t.SetData(data); // [!code ++]
		if (t.cg.alpha < 0.1f) // [!code ++]
		{
			uITooltip.cg.alpha = 1f; // [!code --]
			t.cg.alpha = 1f; // [!code ++]
		}
		float timer = 0f; // [!code --]
		uITooltip.hideFunc = delegate // [!code --]
		t.hideTimer = 0f; // [!code ++]
		t.hideFunc = delegate // [!code ++]
		{
			if (target == null || !InputModuleEX.IsPointerOver(target))
			{
				timer += Time.smoothDeltaTime; // [!code --]
				return timer > 0.2f; // [!code --]
				t.hideTimer += Time.smoothDeltaTime; // [!code ++]
				return t.hideTimer > 0.2f; // [!code ++]
			}
			timer = 0f; // [!code --]
			t.hideTimer = 0f; // [!code ++]
			return false;
		};
		if (uITooltip.followType == UITooltip.FollowType.None) // [!code --]
		if (t.followType == UITooltip.FollowType.None) // [!code ++]
		{
			return;
		}
		Vector3 vector = ((uITooltip.followType == UITooltip.FollowType.Mouse) ? EInput.uiMousePosition : target.position); // [!code --]
		if (uITooltip.followType == UITooltip.FollowType.Pivot) // [!code --]
		Vector3 vector = ((t.followType == UITooltip.FollowType.Mouse) ? EInput.uiMousePosition : target.position); // [!code ++]
		if (t.followType == UITooltip.FollowType.Pivot) // [!code ++]
		{
			GameObject gameObject = target.FindTagInParents("PivotTooltip", includeInactive: false);
			if ((bool)gameObject)
			{
				uITooltip.Rect().pivot = gameObject.transform.Rect().pivot; // [!code --]
				t.Rect().pivot = gameObject.transform.Rect().pivot; // [!code ++]
				vector = gameObject.transform.position;
			}
			else
			{
				uITooltip.Rect().pivot = uITooltip.orgPivot; // [!code --]
				t.Rect().pivot = t.orgPivot; // [!code ++]
			}
		}
		uITooltip.transform.position = vector + data.offset + uITooltip.offset; // [!code --]
		Util.ClampToScreen(uITooltip.Rect(), 20); // [!code --]
		t.transform.position = vector + data.offset + t.offset; // [!code ++]
		Util.ClampToScreen(t.Rect(), 20); // [!code ++]
	}

	public void HideTooltips(bool immediate = false)
	{
		UITooltip[] array = tooltips;
		foreach (UITooltip uITooltip in array) // [!code --]
		foreach (UITooltip t in array) // [!code ++]
		{
			if (immediate)
			{
				uITooltip.cg.alpha = 0f; // [!code --]
				uITooltip.data = null; // [!code --]
				uITooltip.SetActive(enable: false); // [!code --]
				continue; // [!code --]
				t.cg.alpha = 0f; // [!code ++]
				t.data = null; // [!code ++]
				t.SetActive(enable: false); // [!code ++]
			}
			float timer = 0f; // [!code --]
			uITooltip.hideFunc = delegate // [!code --]
			else // [!code ++]
			{
				timer += Time.smoothDeltaTime; // [!code --]
				return timer > 0.2f; // [!code --]
			}; // [!code --]
				t.hideFunc = delegate // [!code ++]
				{ // [!code ++]
					t.hideTimer += Time.smoothDeltaTime; // [!code ++]
					return t.hideTimer > 0.2f; // [!code ++]
				}; // [!code ++]
			} // [!code ++]
		}
	}

```

## UITooltip

[`public enum FollowType`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/1400b349c1b515523b58009de2d3e8b171893669/Elin/Plugins.UI/UITooltip.cs#L44-L49)
```cs:line-numbers=44

	public LayoutElement layout;

	[NonSerialized] // [!code ++]
	public float hideTimer; // [!code ++]
 // [!code ++]
	private void Awake()
	{
		orgPivot = this.Rect().pivot;
```
