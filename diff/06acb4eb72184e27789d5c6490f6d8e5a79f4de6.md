---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 24 files modified. 2 new files created.
version: EA 23.296 Nightly
changes: AM_Bird/BaseGameScreen/BaseListPeople/CalcMoney/Card/CardRenderer/Chara/CharaActorPCC/CharaRenderer/CoreConfig/CoreDebug/DramaCustomSequence/ID_Achievement/LayerEditPCC/+LayerPickPCC/RenderData/SourcePref/SpriteData/Thing/Trait/TraitAltar/TraitBrewery/TraitMirror/UICharaMaker/+UIItemPCCPart/WindowChara
---

# EA 23.296 Nightly

April 18, 2026

24 files modified. 2 new files created.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [SpriteData (4)](#spritedata)
```cs:no-line-numbers
public Sprite[] GetSprites() // [!code --]
public void LoadSprites() // [!code ++]
```
```cs:no-line-numbers
public Sprite GetSprite() // [!code --]

```
```cs:no-line-numbers
public void Validate() // [!code --]

```
```cs:no-line-numbers
public void Load() // [!code --]

```
## AM_Bird

[`public class AM_Bird : AM_MiniGame`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/AM_Bird.cs#L3-L9)
```cs:line-numbers=3
public override void OnActivate()
{
	EClass.scene.transFocus = EClass.scene.flock._roamers.RandomItem().transform;
	EClass.screen.SetTargetZoomIndex(1); // [!code --]
	EClass.screen.SetTargetZoomIndex(2); // [!code ++]
	EClass.screen.RefreshTilt();
	EClass.ui.layerFloat.SetActive(enable: false);
}
```

[`public override void OnDeactivate()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/AM_Bird.cs#L33-L39)
```cs:line-numbers=33
	{
		EClass.scene.transFocus = null;
		EClass.screen.RefreshTilt();
		EClass.screen.SetTargetZoomIndex(1); // [!code --]
		EClass.screen.SetTargetZoomIndex(2); // [!code ++]
		EClass.ui.layerFloat.SetActive(enable: true);
	}
}
```

## BaseGameScreen

[`public virtual void RefreshPosition()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/BaseGameScreen.cs#L445-L453)
```cs:line-numbers=445
	{
		float z = position.z;
		targetZoom = TargetZoom;
		for (int i = 0; i < EMono.core.config.camera.zooms.Length; i++) // [!code --]
		for (int i = 0; i < EMono.core.config.camera.listZoom.Length; i++) // [!code ++]
		{
			if (targetZoom == EMono.core.config.camera.zooms[i]) // [!code --]
			if (targetZoom == EMono.core.config.camera.listZoom[i]) // [!code ++]
			{
				targetZoomIndex = i;
			}
```

[`public virtual void RefreshPosition()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/BaseGameScreen.cs#L477-L497)
```cs:line-numbers=477
		pushBack = Vector3.zero;
		int num = scrollX - scrollY;
		int num2 = scrollY + scrollX;
		if ((float)num <= mapLimit.x) // [!code --]
		if ((float)num * Zoom <= mapLimit.x) // [!code ++]
		{
			pushBack.x = mapLimit.x - (float)num; // [!code --]
			pushBack.x = mapLimit.x - (float)num * Zoom; // [!code ++]
		}
		if ((float)num2 <= mapLimit.y) // [!code --]
		if ((float)num2 * Zoom <= mapLimit.y) // [!code ++]
		{
			pushBack.y = mapLimit.y - (float)num2; // [!code --]
			pushBack.y = mapLimit.y - (float)num2 * Zoom; // [!code ++]
		}
		if ((float)num >= (float)EMono._map.Size + mapLimit.width - (float)(width / 2)) // [!code --]
		if ((float)num >= (float)EMono._map.Size + mapLimit.width - (float)(width / 2) * Zoom) // [!code ++]
		{
			pushBack.x = (float)EMono._map.Size + mapLimit.width - (float)(width / 2) - (float)num; // [!code --]
			pushBack.x = (float)EMono._map.Size + mapLimit.width - (float)(width / 2) * Zoom - (float)num; // [!code ++]
		}
		if ((float)num2 >= (float)EMono._map.Size + mapLimit.height - (float)(height / 2)) // [!code --]
		if ((float)num2 >= (float)EMono._map.Size + mapLimit.height - (float)(height / 2) * Zoom) // [!code ++]
		{
			pushBack.y = (float)EMono._map.Size + mapLimit.height - (float)(height / 2) - (float)num2; // [!code --]
			pushBack.y = (float)EMono._map.Size + mapLimit.height - (float)(height / 2) * Zoom - (float)num2; // [!code ++]
		}
		position += Quaternion.Euler(planeAngle) * pushBack * Core.delta * pushbackSpeed;
	}
```

[`public void SetTargetZoomIndex(int index)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/BaseGameScreen.cs#L866-L872)
```cs:line-numbers=866
{
	targetZoomIndex = index;
	float num = targetZoom;
	targetZoom = EMono.core.config.camera.zooms[targetZoomIndex]; // [!code --]
	targetZoom = EMono.core.config.camera.listZoom[targetZoomIndex]; // [!code ++]
	if (num != targetZoom)
	{
		zoomTimer = 0f;
```

[`public void ModTargetZoomIndex(int a)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/BaseGameScreen.cs#L879-L886)
```cs:line-numbers=879
	{
		int num = targetZoomIndex;
		targetZoomIndex -= a;
		int num2 = ((!EMono.core.config.camera.extendZoomMin) ? 1 : 0); // [!code --]
		int num3 = (EMono.core.config.camera.extendZoomMax ? 4 : 3); // [!code --]
		int num2 = ((!EMono.core.config.camera.extendZoomMin) ? 2 : 0); // [!code ++]
		int num3 = (EMono.core.config.camera.extendZoomMax ? 5 : 4); // [!code ++]
		if (targetZoomIndex < num2)
		{
			targetZoomIndex = num2;
```

## BaseListPeople

[`public override void OnClick(Chara c, ItemGeneral i)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/BaseListPeople.cs#L364-L370)
```cs:line-numbers=364
				{
					uIContextMenu.AddButton("editPCC", delegate
					{
						EClass.ui.AddLayer<LayerEditPCC>().Activate(c, UIPCC.Mode.Full); // [!code --]
						EClass.ui.AddLayer<LayerEditPCC>("LayerPCC/LayerEditPCC").Activate(c, UIPCC.Mode.Full); // [!code ++]
					});
					uIContextMenu.AddButton("togglePCC", delegate
					{
```

## CalcMoney

[`public static int Whore(Chara seller, Chara buyer)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/CalcMoney.cs#L66-L78)
```cs:line-numbers=66

	public static int InvestShop(Chara c, Chara tc)
	{
		long a = Guild.Merchant.InvestPrice(tc.c_invest * 700); // [!code --]
		return Mathf.Max(b: Invest(Guild.Merchant.InvestPrice(tc.c_invest * tc.c_invest * 80 + 200)), a: Invest(a)); // [!code --]
		long a = Guild.Merchant.InvestPrice((long)tc.c_invest * 700L); // [!code ++]
		return Mathf.Max(b: Invest(Guild.Merchant.InvestPrice((long)tc.c_invest * (long)tc.c_invest * 80 + 200)), a: Invest(a)); // [!code ++]
	}

	public static int InvestZone(Chara c)
	{
		long a = EClass._zone.development * 50; // [!code --]
		long a = (long)EClass._zone.development * 50L; // [!code ++]
		return Mathf.Max(b: Invest((long)EClass._zone.development * (long)EClass._zone.development / 4 + 500), a: Invest(a));
	}
}
```

## Card

[`public virtual void SetImage(Image image)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/Card.cs#L6377-L6384)
```cs:line-numbers=6377
		SpriteReplacer spriteReplacer = SpriteReplacer.dictSkins.TryGetValue(c_idSpriteReplacer);
		if (spriteReplacer != null)
		{
			RenderData renderData = ResourceCache.Load<RenderData>("Scene/Render/Data/chara_custom_" + ((spriteReplacer.data.GetSprite().texture.height <= 128) ? "128" : "256")); // [!code --]
			sprite = spriteReplacer.data.GetSprite(); // [!code --]
			sprite = spriteReplacer.GetSprite(); // [!code ++]
			RenderData renderData = ResourceCache.Load<RenderData>("Scene/Render/Data/chara_custom_" + ((sprite.texture.height <= 128) ? "128" : "256")); // [!code ++]
			sourceRenderCard.SetImage(image, sprite, colorInt, setNativeSize: true, 0, 0, renderData, spriteReplacer.data.pref ?? Pref);
			return;
		}
```

## CardRenderer

[`public override void Draw(RenderParam p, ref Vector3 v, bool drawShadow)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/CardRenderer.cs#L300-L306)
```cs:line-numbers=300
			pref = replacer.pref;
			SubPassData.Current = SubPassData.Default;
		}
		if (isChara) // [!code --]
		if (isChara || hasActor) // [!code ++]
		{
			p.x += pref.x * (float)((!owner.flipX) ? 1 : (-1));
			p.z += pref.z;
```

[`public override void OnEnterScreen()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/CardRenderer.cs#L432-L438)
```cs:line-numbers=432
{
	if (isSynced)
	{
		Debug.LogError("renderer alraedy synced:" + owner); // [!code --]
		Debug.LogError("renderer already synced:" + owner); // [!code ++]
	}
	isSynced = true;
	if (!usePass)
```

[`public SourcePref GetPref()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/CardRenderer.cs#L782-L791)
```cs:line-numbers=782
		{
			return EClass.core.refs.prefs.pcc;
		}
		if (owner.sourceCard.replacer?.data?.pref != null) // [!code --]
		{ // [!code --]
			return owner.sourceCard.replacer.data.pref; // [!code --]
		} // [!code --]
		return owner.Pref;
	}
}
```

## Chara

[`public override SourcePref Pref`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/Chara.cs#L801-L831)
```cs:line-numbers=801
	{
		get
		{
			if (spriteReplacer != null) // [!code --]
			if (spriteReplacer == null) // [!code ++]
			{
				return spriteReplacer.data?.pref ?? EClass.core.refs.prefs.replacer1; // [!code --]
			} // [!code --]
			if (base.idSkin > 0) // [!code --]
			{ // [!code --]
				switch (sourceCard.tiles.TryGet(base.idSkin)) // [!code --]
				if (base.idSkin > 0) // [!code ++]
				{
				case 2319: // [!code --]
				case 2619: // [!code --]
				case 2621: // [!code --]
				case 2623: // [!code --]
				case 2625: // [!code --]
					return EClass.core.refs.prefs.sonwputit1; // [!code --]
				case 2320: // [!code --]
				case 2611: // [!code --]
				case 2620: // [!code --]
				case 2622: // [!code --]
				case 2624: // [!code --]
				case 2626: // [!code --]
				case 2828: // [!code --]
					return EClass.core.refs.prefs.snowputit2; // [!code --]
					switch (sourceCard.tiles.TryGet(base.idSkin)) // [!code ++]
					{ // [!code ++]
					case 2319: // [!code ++]
					case 2619: // [!code ++]
					case 2621: // [!code ++]
					case 2623: // [!code ++]
					case 2625: // [!code ++]
						return EClass.core.refs.prefs.sonwputit1; // [!code ++]
					case 2320: // [!code ++]
					case 2611: // [!code ++]
					case 2620: // [!code ++]
					case 2622: // [!code ++]
					case 2624: // [!code ++]
					case 2626: // [!code ++]
					case 2828: // [!code ++]
						return EClass.core.refs.prefs.snowputit2; // [!code ++]
					} // [!code ++]
				}
				return sourceCard.replacer.data?.pref ?? sourceCard.pref; // [!code ++]
			}
			return sourceCard.pref; // [!code --]
			return spriteReplacer.data?.pref ?? EClass.core.refs.prefs.replacer1; // [!code ++]
		}
	}

```

## CharaActorPCC

[`public override void OnRender(RenderParam p)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/CharaActorPCC.cs#L106-L135)
```cs:line-numbers=106
		case 0:
		{
			Thing thing = ((owner.IsPC && EMono.player.currentHotItem.RenderThing != null) ? EMono.player.currentHotItem.RenderThing : ((!flag && owner.body.slotMainHand?.thing != null) ? owner.body.slotMainHand.thing : null));
			if (thing != null) // [!code --]
			if (thing == null) // [!code ++]
			{
				bool flag2 = num == 0 || num == 1; // [!code --]
				if (thing.trait.InvertHeldSprite) // [!code --]
				{ // [!code --]
					flag2 = !flag2; // [!code --]
				} // [!code --]
				Vector3[] mainHandPos = EMono.setting.render.mainHandPos; // [!code --]
				Vector3[] mainHand = EMono.setting.render.animeWalk[provider.currentFrame].mainHand; // [!code --]
				SourcePref pref = thing.sourceCard.pref; // [!code --]
				thing.dir = ((!flag2) ? 1 : 0); // [!code --]
				thing.SetRenderParam(p); // [!code --]
				p.x = org.x + mainHandPos[num].x + mainHand[num].x + (flag2 ? 0.01f : (-0.01f)) * (float)pref.equipX; // [!code --]
				p.y = org.y + mainHandPos[num].y + mainHand[num].y + 0.01f * (float)pref.equipY; // [!code --]
				p.z = org.z - thing.renderer.data.offset.z + mainHandPos[num].z + mainHand[num].z; // [!code --]
				p.v.x = p.x; // [!code --]
				p.v.y = p.y; // [!code --]
				p.v.z = p.z; // [!code --]
				if (thing.renderer.hasActor) // [!code --]
				break; // [!code ++]
			} // [!code ++]
			bool flag2 = num == 0 || num == 1; // [!code ++]
			if (thing.trait.InvertHeldSprite) // [!code ++]
			{ // [!code ++]
				flag2 = !flag2; // [!code ++]
			} // [!code ++]
			Vector3[] mainHandPos = EMono.setting.render.mainHandPos; // [!code ++]
			Vector3[] mainHand = EMono.setting.render.animeWalk[provider.currentFrame].mainHand; // [!code ++]
			SourcePref pref = thing.Pref; // [!code ++]
			thing.dir = ((!flag2) ? 1 : 0); // [!code ++]
			thing.SetRenderParam(p); // [!code ++]
			p.x = org.x + mainHandPos[num].x + mainHand[num].x + (flag2 ? 0.01f : (-0.01f)) * (float)pref.equipX; // [!code ++]
			p.y = org.y + mainHandPos[num].y + mainHand[num].y + 0.01f * (float)pref.equipY; // [!code ++]
			p.z = org.z - thing.renderer.data.offset.z + mainHandPos[num].z + mainHand[num].z; // [!code ++]
			if (thing.renderer.hasActor) // [!code ++]
			{ // [!code ++]
				thing.renderer.RefreshSprite(); // [!code ++]
				if (!flag2) // [!code ++]
				{
					thing.renderer.RefreshSprite(); // [!code --]
					p.x -= thing.renderer.data._offset.x * 2f; // [!code ++]
				}
				thing.renderer.Draw(p, ref p.v, drawShadow: false); // [!code --]
			}
			p.v.x = p.x; // [!code ++]
			p.v.y = p.y; // [!code ++]
			p.v.z = p.z; // [!code ++]
			thing.renderer.Draw(p, ref p.v, drawShadow: false); // [!code ++]
			break;
		}
		case -1:
```

[`public override void OnRender(RenderParam p)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/CharaActorPCC.cs#L158-L180)
```cs:line-numbers=158
				break;
			}
			Thing thing2 = owner.body.slotOffHand.thing;
			if (thing2 != null) // [!code --]
			if (thing2 == null) // [!code ++]
			{ // [!code ++]
				break; // [!code ++]
			} // [!code ++]
			bool flag4 = num == 1 || num == 3; // [!code ++]
			Vector3[] offHandPos = EMono.setting.render.offHandPos; // [!code ++]
			Vector3[] offHand = EMono.setting.render.animeWalk[provider.currentFrame].offHand; // [!code ++]
			SourcePref pref2 = thing2.Pref; // [!code ++]
			thing2.dir = ((!flag4) ? 1 : 0); // [!code ++]
			thing2.SetRenderParam(p); // [!code ++]
			p.x = org.x + offHandPos[num].x + offHand[num].x + (flag4 ? 0.01f : (-0.01f)) * (float)pref2.equipX; // [!code ++]
			p.y = org.y + offHandPos[num].y + offHand[num].y + 0.01f * (float)pref2.equipY; // [!code ++]
			p.z = org.z - thing2.renderer.data.offset.z + offHandPos[num].z + offHand[num].z; // [!code ++]
			if (thing2.renderer.hasActor) // [!code ++]
			{
				bool flag4 = num == 1 || num == 3; // [!code --]
				Vector3[] offHandPos = EMono.setting.render.offHandPos; // [!code --]
				Vector3[] offHand = EMono.setting.render.animeWalk[provider.currentFrame].offHand; // [!code --]
				SourcePref pref2 = thing2.source.pref; // [!code --]
				thing2.dir = ((!flag4) ? 1 : 0); // [!code --]
				thing2.SetRenderParam(p); // [!code --]
				p.x = org.x + offHandPos[num].x + offHand[num].x + (flag4 ? 0.01f : (-0.01f)) * (float)pref2.equipX; // [!code --]
				p.y = org.y + offHandPos[num].y + offHand[num].y + 0.01f * (float)pref2.equipY; // [!code --]
				p.z = org.z - thing2.renderer.data.offset.z + offHandPos[num].z + offHand[num].z; // [!code --]
				if (thing2.renderer.hasActor) // [!code --]
				thing2.renderer.RefreshSprite(); // [!code ++]
				if (!flag4) // [!code ++]
				{
					thing2.renderer.RefreshSprite(); // [!code --]
					p.x -= thing2.renderer.data._offset.x * 2f; // [!code ++]
				}
				thing2.renderer.Draw(p); // [!code --]
			}
			p.v.x = p.x; // [!code ++]
			p.v.y = p.y; // [!code ++]
			p.v.z = p.z; // [!code ++]
			thing2.renderer.Draw(p, ref p.v, drawShadow: false); // [!code ++]
			break;
		}
		case -1:
```

## CharaRenderer

[`public override void SetOwner(Card c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/CharaRenderer.cs#L69-L75)
```cs:line-numbers=69
		SpriteReplacer spriteReplacer = SpriteReplacer.dictSkins.TryGetValue(owner.c_idSpriteReplacer);
		if (spriteReplacer != null)
		{
			data = ResourceCache.Load<RenderData>("Scene/Render/Data/chara_custom_" + ((spriteReplacer.data.GetSprite().texture.height <= 128) ? "128" : "256")); // [!code --]
			data = ResourceCache.Load<RenderData>("Scene/Render/Data/chara_custom_" + ((spriteReplacer.GetSprite().texture.height <= 128) ? "128" : "256")); // [!code ++]
			owner.spriteReplacer = spriteReplacer;
		}
	}
```

## CoreConfig

[`public class CameraConfig`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/CoreConfig.cs#L98-L103)
```cs:line-numbers=98
		public int moveframe;

		public float[] zooms;
 // [!code ++]
		public float[] listZoom = new float[7] { 0.125f, 0.25f, 0.5f, 0.75f, 1f, 1.5f, 2f }; // [!code ++]
	}

	[Serializable]
```

## CoreDebug

[`public void InitDebugCommands()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/CoreDebug.cs#L1500-L1506)
```cs:line-numbers=1500
	});
	Add(cat2, "Edit PCC", delegate
	{
		EClass.ui.AddLayer<LayerEditPCC>().Activate(EClass.pc, UIPCC.Mode.Full); // [!code --]
		EClass.ui.AddLayer<LayerEditPCC>("LayerPCC/LayerEditPCC").Activate(EClass.pc, UIPCC.Mode.Full); // [!code ++]
	});
	Add(cat2, "COM_Teleport", COM_Teleport);
	Add(cat2, "LOG_Spatials", LOG_Spatials);
```

## DramaCustomSequence

[`public void Build(Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/DramaCustomSequence.cs#L856-L861)
```cs:line-numbers=856
		if (c.affinity.CanBecomeMama() || EClass.pc.faith == EClass.game.religions.MoonShadow || EClass.debug.enable)
		{
			Steam.GetAchievement(ID_Achievement.MAMA);
			string id2 = c.id; // [!code ++]
			if (!(id2 == "farris")) // [!code ++]
			{ // [!code ++]
				if (id2 == "quru") // [!code ++]
				{ // [!code ++]
					Steam.GetAchievement(ID_Achievement.MAMA3); // [!code ++]
				} // [!code ++]
			} // [!code ++]
			else // [!code ++]
			{ // [!code ++]
				Steam.GetAchievement(ID_Achievement.MAMA2); // [!code ++]
			} // [!code ++]
			TempTalkTopic("mama_yes", StepEnd);
			EClass.pc.SetAI(new AI_Fuck
			{
```

## ID_Achievement

[`public enum ID_Achievement`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/ID_Achievement.cs#L27-L31)
```cs:line-numbers=27
	OYS,
	MAMA,
	PRIEST,
	PRIEST2 // [!code --]
	PRIEST2, // [!code ++]
	MAMA2, // [!code ++]
	MAMA3 // [!code ++]
}
```

## LayerEditPCC

[`public void Activate(Chara _chara, UIPCC.Mode m, string idUniform = null, Action`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/LayerEditPCC.cs#L51-L56)
```cs:line-numbers=51
			actor.Reset();
		});
	};
	uiPCC.onRefreshParts = SetPccPicker; // [!code ++]
	PCCData newData = new PCCData();
	newData.Set(chara.pccData);
	backup = IO.DeepCopy(newData);
```

[`public void OnClickChangePortrait()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/LayerEditPCC.cs#L125-L130)
```cs:line-numbers=125
		});
	}

	public void OnClickPartPicker(UIItemPCC uiItemPcc) // [!code ++]
	{ // [!code ++]
		ELayer.ui.AddLayer<LayerPickPCC>("LayerPCC/LayerPickPCC").Activate(uiPCC, uiItemPcc); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public override void OnKill()
	{
		Apply();
```

[`public void SetPortraitSlider()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/LayerEditPCC.cs#L165-L168)
```cs:line-numbers=165
			}, (ModItem<Sprite> a) => (!a.id.IsEmpty()) ? a.id : "None");
		}
	}
 // [!code ++]
	public void SetPccPicker() // [!code ++]
	{ // [!code ++]
		foreach (UIItemPCC item in uiPCC.items) // [!code ++]
		{ // [!code ++]
			item.buttonPicker.onClick.AddListener(delegate // [!code ++]
			{ // [!code ++]
				OnClickPartPicker(item); // [!code ++]
			}); // [!code ++]
			item.buttonPicker.GetComponent<ButtonGeneral>().tooltip.lang = "LayerPickPCC".lang(); // [!code ++]
		} // [!code ++]
	} // [!code ++]
}
```

## +LayerPickPCC

::: details File Created
```cs
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.RegularExpressions;
using Empyrean.Utils;
using UnityEngine;
using UnityEngine.UI;

public class LayerPickPCC : ELayer
{
	public UIItemPCCPart mold;

	public LayoutGroup layoutParts;

	private UIItemPCC _uiItemPcc;

	private UIPCC _uiPcc;

	private List<UIItemPCCPart> _items = new List<UIItemPCCPart>();

	private List<Outline> _outlines = new List<Outline>();

	private static readonly Dictionary<PCC.Part, BaseModPackage> _cached = new Dictionary<PCC.Part, BaseModPackage>();

	private static readonly Regex _idRegex = new Regex("(\\d+)|(\\D+)", RegexOptions.Compiled);

	public void Activate(UIPCC uiPcc, UIItemPCC uiItemPcc)
	{
		_uiPcc = uiPcc;
		_uiItemPcc = uiItemPcc;
		PopulateButtons();
	}

	private void Update()
	{
		SpriteProvider provider = _uiPcc.actor.provider;
		int frame = (provider.currentFrame + 2) % 4;
		foreach (UIItemPCCPart item in _items)
		{
			item.SetSpriteIndex(provider.currentDir, frame);
		}
	}

	public void PopulateButtons()
	{
		List<PCC.Part> list = _uiItemPcc.parts.ToList();
		list.Sort(PartSorter);
		foreach (PCC.Part part in list)
		{
			UIItemPCCPart item = Util.Instantiate(mold, layoutParts);
			if (part == _uiItemPcc.part)
			{
				SetOutline(item.outline);
			}
			if (part == null)
			{
				item.button.onClick.AddListener(delegate
				{
					_uiItemPcc.slider.value = 0f;
					SetOutline(item.outline);
				});
				item.button.tooltip.lang = "empty";
			}
			else
			{
				ModItem<Texture2D> modItem = part.modTextures.TryGetValue("walk");
				if (modItem == null)
				{
					break;
				}
				item.button.tooltip.lang = GetPartProviderString(part);
				item.button.onClick.AddListener(delegate
				{
					_uiItemPcc.slider.value = _uiItemPcc.parts.IndexOf(part);
					SetOutline(item.outline);
				});
				item.SetSprites(IO.LoadPNG(modItem.fileInfo.FullName));
				HSVColor hsv = _uiPcc.pcc.data.GetColor(_uiItemPcc.idPartSet).ToHsv();
				hsv.v = 1f;
				hsv.a = 1f;
				item.button.icon.color = hsv.ToRGB();
			}
			_items.Add(item);
		}
		void SetOutline(Outline outline)
		{
			_outlines.Add(outline);
			foreach (Outline outline in _outlines)
			{
				outline.enabled = false;
			}
			outline.enabled = true;
		}
	}

	public static string GetPartProviderString(PCC.Part part)
	{
		return "ID: " + part.id + "\n<i>" + GetPartProvider(part).title.TagColor(Color.cyan) + "</i>\n" + part.dir.ShortPath();
	}

	public static BaseModPackage GetPartProvider(PCC.Part part)
	{
		if (part == null)
		{
			return null;
		}
		if (_cached.TryGetValue(part, out var value))
		{
			return value;
		}
		string dir = part.dir;
		value = BaseModManager.Instance.packages.FirstOrDefault((BaseModPackage p) => dir.StartsWith(p.dirInfo.FullName)) ?? new BaseModPackage
		{
			title = "unknownAuthor".lang()
		};
		return _cached[part] = value;
	}

	public static int PartSorter(PCC.Part lhs, PCC.Part rhs)
	{
		if (lhs == null)
		{
			if (rhs != null)
			{
				return -1;
			}
			return 0;
		}
		if (rhs == null)
		{
			return 1;
		}
		int num = GetPartSortOrder(lhs);
		int num2 = GetPartSortOrder(rhs);
		if (num != num2)
		{
			return num - num2;
		}
		string id = lhs.id;
		string id2 = rhs.id;
		if (id == id2)
		{
			return 0;
		}
		int result;
		bool flag = int.TryParse(id, out result);
		int result2;
		bool flag2 = int.TryParse(id2, out result2);
		if (flag && flag2)
		{
			return result.CompareTo(result2);
		}
		if (flag)
		{
			return -1;
		}
		if (flag2)
		{
			return 1;
		}
		MatchCollection matchCollection = _idRegex.Matches(id);
		MatchCollection matchCollection2 = _idRegex.Matches(id2);
		int num3 = Math.Min(matchCollection.Count, matchCollection2.Count);
		for (int i = 0; i < num3; i++)
		{
			string value = matchCollection[i].Value;
			string value2 = matchCollection2[i].Value;
			int result3;
			bool num4 = int.TryParse(value, out result3);
			int result4;
			bool flag3 = int.TryParse(value2, out result4);
			int num5 = ((!(num4 && flag3)) ? string.Compare(value, value2, StringComparison.OrdinalIgnoreCase) : result3.CompareTo(result4));
			if (num5 != 0)
			{
				return num5;
			}
		}
		return matchCollection.Count - matchCollection2.Count;
		static int GetPartSortOrder(PCC.Part part)
		{
			int num6 = BaseModManager.Instance.packages.IndexOf(GetPartProvider(part));
			if (num6 < 0)
			{
				return int.MaxValue;
			}
			return num6;
		}
	}
}
```

:::
## RenderData

[`public class RenderData : EScriptable, IRenderer`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/RenderData.cs#L7-L13)
```cs:line-numbers=7

	public static Quaternion deadRotation = Quaternion.Euler(0f, 0f, 90f);

	protected Vector3 _offset; // [!code --]
	[NonSerialized] // [!code ++]
	public Vector3 _offset; // [!code ++]

	public static GameSetting.RenderSetting renderSetting;

```

## SourcePref

[`public void WriteIni(string path)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/SourcePref.cs#L366-L384)
```cs:line-numbers=366
		}
		File.CreateText(path).Close();
		IniData iniData = fileIniDataParser.ReadFile(path, Encoding.UTF8);
		iniData.Global["x"] = ints[4].ToString() ?? ""; // [!code --]
		iniData.Global["y"] = ints[5].ToString() ?? ""; // [!code --]
		iniData.Global["z"] = ints[2].ToString() ?? ""; // [!code --]
		iniData.Global["pivotX"] = pivotX.ToString() ?? ""; // [!code --]
		iniData.Global["pivotY"] = pivotY.ToString() ?? ""; // [!code --]
		iniData.Global["shadow"] = shadow.ToString() ?? ""; // [!code --]
		iniData.Global["shadowX"] = shadowX.ToString() ?? ""; // [!code --]
		iniData.Global["shadowY"] = shadowY.ToString() ?? ""; // [!code --]
		iniData.Global["height"] = ints[3].ToString() ?? ""; // [!code --]
		iniData.Global["scaleIcon"] = scaleIcon.ToString() ?? ""; // [!code --]
		iniData.Global["liquidMod"] = liquidMod.ToString() ?? ""; // [!code --]
		iniData.Global["hatY"] = ints[23].ToString() ?? ""; // [!code --]
		iniData.Global["flags"] = ints[1].ToString() ?? ""; // [!code --]
		iniData.Global["x"] = ints[4].ToString(); // [!code ++]
		iniData.Global["y"] = ints[5].ToString(); // [!code ++]
		iniData.Global["z"] = ints[2].ToString(); // [!code ++]
		iniData.Global["pivotX"] = pivotX.ToString(); // [!code ++]
		iniData.Global["pivotY"] = pivotY.ToString(); // [!code ++]
		iniData.Global["shadow"] = shadow.ToString(); // [!code ++]
		iniData.Global["shadowX"] = shadowX.ToString(); // [!code ++]
		iniData.Global["shadowY"] = shadowY.ToString(); // [!code ++]
		iniData.Global["shadowRX"] = shadowRX.ToString(); // [!code ++]
		iniData.Global["shadowRY"] = shadowRY.ToString(); // [!code ++]
		iniData.Global["shadowBX"] = shadowBX.ToString(); // [!code ++]
		iniData.Global["shadowBY"] = shadowBY.ToString(); // [!code ++]
		iniData.Global["shadowBRX"] = shadowBRX.ToString(); // [!code ++]
		iniData.Global["shadowBRY"] = shadowBRY.ToString(); // [!code ++]
		iniData.Global["height"] = ints[3].ToString(); // [!code ++]
		iniData.Global["heightFix"] = ints[24].ToString(); // [!code ++]
		iniData.Global["scaleIcon"] = scaleIcon.ToString(); // [!code ++]
		iniData.Global["liquidMod"] = liquidMod.ToString(); // [!code ++]
		iniData.Global["liquidModMax"] = liquidModMax.ToString(); // [!code ++]
		iniData.Global["hatY"] = ints[23].ToString(); // [!code ++]
		iniData.Global["equipX"] = equipX.ToString(); // [!code ++]
		iniData.Global["equipY"] = equipY.ToString(); // [!code ++]
		iniData.Global["stackX"] = ints[17].ToString(); // [!code ++]
		fileIniDataParser.WriteFile(path, iniData);
	}

```

[`public static SourcePref ReadFromIni(string path)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/SourcePref.cs#L393-L405)
```cs:line-numbers=393
	sourcePref.pivotY = iniData.Global["pivotY"].ToInt();
	sourcePref.shadow = iniData.Global["shadow"].ToInt();
	sourcePref.shadowX = iniData.Global["shadowX"].ToInt();
	sourcePref.shadowRX = -sourcePref.shadowX; // [!code --]
	int num2 = (sourcePref.shadowRY = iniData.Global["shadowY"].ToInt()); // [!code --]
	sourcePref.shadowY = num2; // [!code --]
	sourcePref.shadowY = iniData.Global["shadowY"].ToInt(); // [!code ++]
	sourcePref.shadowRX = iniData.Global["shadowRX"].ToInt(); // [!code ++]
	sourcePref.shadowRY = iniData.Global["shadowRY"].ToInt(); // [!code ++]
	sourcePref.shadowBX = iniData.Global["shadowBX"].ToInt(); // [!code ++]
	sourcePref.shadowBY = iniData.Global["shadowBY"].ToInt(); // [!code ++]
	sourcePref.shadowBRX = iniData.Global["shadowBRX"].ToInt(); // [!code ++]
	sourcePref.shadowBRY = iniData.Global["shadowBRY"].ToInt(); // [!code ++]
	sourcePref.ints[3] = iniData.Global["height"].ToInt();
	sourcePref.ints[24] = iniData.Global["heightFix"].ToInt(); // [!code ++]
	sourcePref.scaleIcon = iniData.Global["scaleIcon"].ToInt();
	sourcePref.liquidMod = iniData.Global["liquidMod"].ToInt();
	sourcePref.liquidModMax = iniData.Global["liquidModMax"].ToInt(); // [!code ++]
	sourcePref.ints[23] = iniData.Global["hatY"].ToInt();
	sourcePref.equipX = iniData.Global["equipX"].ToInt(); // [!code ++]
	sourcePref.equipY = iniData.Global["equipY"].ToInt(); // [!code ++]
	sourcePref.ints[17] = iniData.Global["stackX"].ToInt(); // [!code ++]
	sourcePref.ints[1] = iniData.Global["flags"].ToInt();
	return sourcePref;
}
```

## SpriteData

[`public class SpriteData`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/SpriteData.cs#L15-L21)
```cs:line-numbers=15

	public Sprite[] sprites;

	public DateTime date; // [!code --]
	public DateTime dateTex; // [!code ++]
 // [!code ++]
	public DateTime dateIni; // [!code ++]
 // [!code ++]
	public DateTime datePref; // [!code ++]

	public int frame = 1;

```

[`public void LoadAnimationIni()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/SpriteData.cs#L53-L111)
```cs:line-numbers=53
	{
		if (!File.Exists(path + ".ini"))
		{
			frame = 1; // [!code ++]
			scale = 50; // [!code ++]
			time = 0.2f; // [!code ++]
			return;
		}
		try
		{
			IniData iniData = new FileIniDataParser().ReadFile(path + ".ini", Encoding.UTF8);
			if (!int.TryParse(iniData.GetKey("frame") ?? "1", out frame)) // [!code --]
			{ // [!code --]
				frame = 1; // [!code --]
			} // [!code --]
			if (!int.TryParse(iniData.GetKey("scale") ?? "50", out scale)) // [!code --]
			{ // [!code --]
				scale = 50; // [!code --]
			} // [!code --]
			if (!float.TryParse(iniData.GetKey("time") ?? "0.2", out time)) // [!code --]
			{ // [!code --]
				time = 0.2f; // [!code --]
			} // [!code --]
			frame = Mathf.Max(1, (!int.TryParse(iniData.GetKey("frame"), out var result)) ? 1 : result); // [!code ++]
			scale = (int.TryParse(iniData.GetKey("scale"), out var result2) ? result2 : 50); // [!code ++]
			time = (float.TryParse(iniData.GetKey("time"), out var result3) ? Mathf.Max(0.016f, result3) : 0.2f); // [!code ++]
		}
		catch (Exception exception)
		{
			Debug.LogException(exception);
			frame = 1; // [!code --]
			scale = 50; // [!code --]
			time = 0.2f; // [!code --]
		}
	}

	public Sprite[] GetSprites() // [!code --]
	public void LoadSprites() // [!code ++]
	{
		Validate(); // [!code --]
		return sprites; // [!code --]
	} // [!code --]
 // [!code --]
	public Sprite GetSprite() // [!code --]
	{ // [!code --]
		Validate(); // [!code --]
		return sprites[0]; // [!code --]
	} // [!code --]
 // [!code --]
	public void Validate() // [!code --]
	{ // [!code --]
		DateTime lastWriteTime = File.GetLastWriteTime(path + ".png"); // [!code --]
		if (date.Year == 1 || !(date == lastWriteTime)) // [!code --]
		{ // [!code --]
			Load(); // [!code --]
			date = lastWriteTime; // [!code --]
		} // [!code --]
	} // [!code --]
 // [!code --]
	public void Load() // [!code --]
	{ // [!code --]
		LoadPref(); // [!code --]
		LoadAnimationIni(); // [!code --]
		Texture2D texture2D = IO.LoadPNG(path + ".png");
		Debug.Log(texture2D.width + "/" + texture2D.height + "/" + path);
		if ((bool)tex)
```

[`public void Load()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/SpriteData.cs#L124-L134)
```cs:line-numbers=124
		}
		int num = tex.width / frame;
		int height = tex.height;
		sprites = new Sprite[frame]; // [!code --]
		if (sprites == null || sprites.Length != frame) // [!code ++]
		{ // [!code ++]
			sprites = new Sprite[frame]; // [!code ++]
		} // [!code ++]
		Vector2 pivot = new Vector2(0.5f, 0.5f);
		for (int i = 0; i < frame; i++)
		{
			sprites[i] = Sprite.Create(tex, new Rect(i * num, 0f, num, height), pivot, 100f, 0u, SpriteMeshType.FullRect);
		}
	}
 // [!code ++]
	public Sprite[] GetSprites() // [!code ++]
	{ // [!code ++]
		if (sprites == null) // [!code ++]
		{ // [!code ++]
			Load(); // [!code ++]
		} // [!code ++]
		return sprites; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public Sprite GetSprite() // [!code ++]
	{ // [!code ++]
		if (sprites == null) // [!code ++]
		{ // [!code ++]
			Load(); // [!code ++]
		} // [!code ++]
		return sprites.TryGet(0, returnNull: true); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void Validate() // [!code ++]
	{ // [!code ++]
		if (IsDirty(".png", ref dateTex) || IsDirty(".ini", ref dateIni) || IsDirty(".pref", ref datePref)) // [!code ++]
		{ // [!code ++]
			Load(); // [!code ++]
		} // [!code ++]
		bool IsDirty(string p, ref DateTime lastChecked) // [!code ++]
		{ // [!code ++]
			string text = path + p; // [!code ++]
			if (!File.Exists(text)) // [!code ++]
			{ // [!code ++]
				return false; // [!code ++]
			} // [!code ++]
			DateTime lastWriteTimeUtc = File.GetLastWriteTimeUtc(text); // [!code ++]
			if (lastWriteTimeUtc == lastChecked) // [!code ++]
			{ // [!code ++]
				return false; // [!code ++]
			} // [!code ++]
			lastChecked = lastWriteTimeUtc; // [!code ++]
			return true; // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void Load() // [!code ++]
	{ // [!code ++]
		LoadAnimationIni(); // [!code ++]
		LoadPref(); // [!code ++]
		LoadSprites(); // [!code ++]
	} // [!code ++]
}
```

## Thing

[`public override SourcePref Pref`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/Thing.cs#L51-L65)
```cs:line-numbers=51
		get
		{
			SourcePref pref = trait.GetPref();
			if (pref == null) // [!code --]
			if (pref != null) // [!code ++]
			{
				if (source.origin == null || source.pref.UsePref) // [!code --]
				{ // [!code --]
					return source.pref; // [!code --]
				} // [!code --]
				pref = source.origin.pref; // [!code --]
				return pref; // [!code ++]
			}
			return pref; // [!code --]
			CardRow cardRow = ((source.origin != null && !source.pref.UsePref) ? source.origin : source); // [!code ++]
			return cardRow.replacer.data?.pref ?? cardRow.pref; // [!code ++]
		}
	}

```

## Trait

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/Trait.cs#L2023-L2029)
```cs:line-numbers=2023
				break;
			case ShopType.Moyer:
			{
				for (int num14 = 1; num14 <= 20; num14++) // [!code --]
				for (int num14 = 1; num14 <= 21; num14++) // [!code ++]
				{
					AddAdvWeek(num14);
				}
```

## TraitAltar

[`public void OnOffer(Chara c, Thing t)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/TraitAltar.cs#L185-L191)
```cs:line-numbers=185
		}
		if (Deity.id != EClass.pc.faith.id)
		{
			bool flag = EClass.rnd(EClass.pc.faith.GetOfferingValue(t, t.Num)) > EClass.rnd(200); // [!code --]
			bool flag = EClass.rnd(EClass.pc.faith.GetOfferingValue(t, t.Num)) > EClass.rnd(200) || IsEyth; // [!code ++]
			if (GetParam(1) != null || EClass.pc.IsEyth)
			{
				Msg.Say("nothingHappens");
```

## TraitBrewery

[`public override bool CanChildDecay(Card c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/TraitBrewery.cs#L37-L43)
```cs:line-numbers=37

	public override bool OnChildDecay(Card c, bool firstDecay)
	{
		if (c.trait is TraitFoodFishSlice) // [!code --]
		if (c.trait is TraitFoodFishSlice || c.HasElement(707)) // [!code ++]
		{
			return true;
		}
```

## TraitMirror

[`public override void TrySetAct(ActPlan p)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/TraitMirror.cs#L12-L23)
```cs:line-numbers=12
{
	p.TrySetAct("actDress", delegate
	{
		EClass.ui.AddLayer<LayerEditPCC>().Activate(EClass.pc, UIPCC.Mode.Body); // [!code --]
		EClass.ui.AddLayer<LayerEditPCC>("LayerPCC/LayerEditPCC").Activate(EClass.pc, UIPCC.Mode.Body); // [!code ++]
		return false;
	}, owner);
	p.TrySetAct("actDress2", delegate
	{
		EClass.ui.AddLayer<LayerEditPCC>().Activate(EClass.pc, UIPCC.Mode.Extra); // [!code --]
		EClass.ui.AddLayer<LayerEditPCC>("LayerPCC/LayerEditPCC").Activate(EClass.pc, UIPCC.Mode.Extra); // [!code ++]
		return false;
	}, owner);
}
```

## UICharaMaker

[`public void RerollBio(bool keepParent)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/UICharaMaker.cs#L441-L447)
```cs:line-numbers=441

	public void EditPCC()
	{
		EMono.ui.AddLayer<LayerEditPCC>().Activate(chara, UIPCC.Mode.Body, null, delegate // [!code --]
		EMono.ui.AddLayer<LayerEditPCC>("LayerPCC/LayerEditPCC").Activate(chara, UIPCC.Mode.Body, null, delegate // [!code ++]
		{
			portrait.SetChara(chara);
			SetPortraitSlider();
```

## +UIItemPCCPart

::: details File Created
```cs
using System;
using UnityEngine;
using UnityEngine.UI;

public class UIItemPCCPart : EMono
{
	public ButtonGeneral button;

	public Outline outline;

	[NonSerialized]
	public Sprite[] sprites = new Sprite[16];

	private bool _init;

	public void SetSpriteIndex(int dir, int frame)
	{
		if (_init)
		{
			button.icon.sprite = sprites[dir * 4 + frame];
		}
	}

	public void SetSprites(Texture2D texture)
	{
		int num = texture.width / 4;
		int num2 = texture.height / 4;
		for (int i = 0; i < 4; i++)
		{
			for (int j = 0; j < 4; j++)
			{
				int num3 = j * num;
				int num4 = (3 - i) * num2;
				Rect rect = new Rect(num3, num4, num, num2);
				sprites[i * 4 + j] = Sprite.Create(texture, rect, new Vector2(0.5f, 0.5f), 100f, 0u, SpriteMeshType.FullRect);
			}
		}
		_init = true;
	}
}
```

:::
## WindowChara

[`public void OnClickPortrait()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/06acb4eb72184e27789d5c6490f6d8e5a79f4de6/Elin/WindowChara.cs#L491-L497)
```cs:line-numbers=491
{
	if (chara.IsPC)
	{
		EClass.ui.AddLayer<LayerEditPCC>().Activate(chara, UIPCC.Mode.Body, null, delegate // [!code --]
		EClass.ui.AddLayer<LayerEditPCC>("LayerPCC/LayerEditPCC").Activate(chara, UIPCC.Mode.Body, null, delegate // [!code ++]
		{
			portrait.SetChara(chara);
		});
```
