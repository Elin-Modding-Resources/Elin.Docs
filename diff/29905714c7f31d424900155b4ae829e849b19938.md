---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 18 files modified.
version: EA 23.304 Nightly
changes: AI_Fuck/ActEffect/ActRanged/BaseCondition/Card/Chara/CharaBody/ConBuffStats/ConMiasma/ConTransmuteHuman/Condition/DNA/ElementContainerCard/GoalCombat/RendererReplacer/SourceStat/WidgetSearch/Zone_Town
---

# EA 23.304 Nightly

May 9, 2026

18 files modified.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [Card (1)](#card)
```cs:no-line-numbers
public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSource = AttackSource.None, Card origin = null, bool showEffect = true, Thing weapon = null, Chara originalTarget = null) // [!code --]
public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSource = AttackSource.None, Card origin = null, bool showEffect = true, Thing weapon = null, Chara originalTarget = null, int resistPenetrationLevel = 0) // [!code ++]
```
### [CharaBody (1)](#charabody)
```cs:no-line-numbers
public BodySlot GetSlot(int elementId, bool onlyEmpty = true, bool secondSlot = false) // [!code --]
public BodySlot GetSlot(int elementId, bool onlyEmpty = true, bool getSecondSlot = false) // [!code ++]
```
## AI_Fuck

[`public void Finish()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/AI_Fuck.cs#L487-L493)
```cs:line-numbers=487
			Thing thing = null;
			for (int j = 0; j < 10; j++)
			{
				thing = target.MakeGene((j < 3) ? DNA.Type.Superior : DNA.Type.Default); // [!code --]
				for (int k = 0; k < 20; k++) // [!code ++]
				{ // [!code ++]
					thing = target.MakeGene((j < 3) ? DNA.Type.Superior : DNA.Type.Default); // [!code ++]
					if (EClass.rnd(10) < chara.body.slots.Count - 2) // [!code ++]
					{ // [!code ++]
						break; // [!code ++]
					} // [!code ++]
					if (thing.c_DNA.GetBodySlot() != -1) // [!code ++]
					{ // [!code ++]
						if (chara.body.GetSlot(35, onlyEmpty: false) == null && EClass.rnd(2) == 0) // [!code ++]
						{ // [!code ++]
							thing.c_DNA.ReplaceBodySlot(35); // [!code ++]
						} // [!code ++]
						else if (chara.body.GetSlot(32, onlyEmpty: false) == null && EClass.rnd(2) == 0) // [!code ++]
						{ // [!code ++]
							thing.c_DNA.ReplaceBodySlot(32); // [!code ++]
						} // [!code ++]
						break; // [!code ++]
					} // [!code ++]
				} // [!code ++]
				thing.c_DNA.MakeSlimeFood(chara);
				if (thing.c_DNA.GetInvalidAction(chara) != null || thing.c_DNA.GetInvalidFeat(chara) != null)
				{
```

## ActEffect

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActEffect.cs#L1578-L1589)
```cs:line-numbers=1578
		}
		EClass.ui.AddLayer<LayerList>().SetStringList(delegate
		{
			List<string> list11 = new List<string>(); // [!code --]
			List<string> list12 = new List<string>(); // [!code ++]
			for (int num17 = 0; num17 < 10; num17++)
			{
				list11.Add(AliasGen.GetRandomAlias()); // [!code --]
				list12.Add(AliasGen.GetRandomAlias()); // [!code ++]
			}
			return list11; // [!code --]
			return list12; // [!code ++]
		}, delegate(int a, string b)
		{
			c2._alias = b;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActEffect.cs#L1759-L1773)
```cs:line-numbers=1759
		{
			break;
		}
		int num9 = 0; // [!code --]
		int num10 = 0; // [!code ++]
		foreach (Condition condition5 in TC.conditions)
		{
			if (condition5.Type == ConditionType.Debuff)
			{
				num9++; // [!code --]
				num10++; // [!code ++]
			}
		}
		if (num9 == 0) // [!code --]
		if (num10 == 0) // [!code ++]
		{
			CC.SayNothingHappans();
			break;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActEffect.cs#L1785-L1791)
```cs:line-numbers=1785
		TC.Say("abShutterHex", TC);
		Point center = CC.pos.Copy();
		List<Chara> list4 = TC.pos.ListCharasInRadius(TC, 4, (Chara c) => c == TC || c.IsHostile(CC));
		for (int k = 0; k < num9; k++) // [!code --]
		for (int k = 0; k < num10; k++) // [!code ++]
		{
			TweenUtil.Delay((float)k * 0.1f, delegate
			{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActEffect.cs#L1804-L1811)
```cs:line-numbers=1804
					{
						effect.Play(center, 0f, pos);
					});
					int num10 = Dice.Create("SpShutterHex", power, CC, (actRef.refThing != null) ? null : actRef.act).Roll(); // [!code --]
					item6.DamageHP(num10, 919, power, AttackSource.None, CC, showEffect: false); // [!code --]
					int num11 = Dice.Create("SpShutterHex", power, CC, (actRef.refThing != null) ? null : actRef.act).Roll(); // [!code ++]
					item6.DamageHP(num11, 919, power, AttackSource.None, CC, showEffect: false); // [!code ++]
				}
			}
		}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActEffect.cs#L1861-L1867)
```cs:line-numbers=1861
			break;
		}
		Thing thing7 = null;
		bool flag11 = actRef.n1 == "food"; // [!code --]
		bool flag12 = actRef.n1 == "food"; // [!code ++]
		if (actRef.n1 == "money")
		{
			int currency = TC.GetCurrency();
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActEffect.cs#L1875-L1885)
```cs:line-numbers=1875
		else
		{
			Func<Thing, bool> func = (Thing t) => true;
			if (flag11) // [!code --]
			if (flag12) // [!code ++]
			{
				func = (Thing t) => t.IsFood;
			}
			List<Thing> list10 = TC.things.List(delegate(Thing t) // [!code --]
			List<Thing> list11 = TC.things.List(delegate(Thing t) // [!code ++]
			{
				if (t.parentCard?.trait is TraitChestMerchant || t.trait is TraitTool || t.IsThrownWeapon)
				{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActEffect.cs#L1887-L1895)
```cs:line-numbers=1887
				}
				return t.trait.CanBeDestroyed && t.things.Count == 0 && t.invY != 1 && t.trait.CanBeStolen && !t.trait.CanOnlyCarry && !t.IsUnique && !t.isEquipped && t.blessedState == BlessedState.Normal && func(t);
			}, onlyAccessible: true);
			if (list10.Count > 0) // [!code --]
			if (list11.Count > 0) // [!code ++]
			{
				thing7 = list10.RandomItem(); // [!code --]
				thing7 = list11.RandomItem(); // [!code ++]
				if (thing7.Num > 1)
				{
					thing7 = thing7.Split(1);
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActEffect.cs#L2085-L2097)
```cs:line-numbers=2085
			power = power * 2 / 3;
		}
		int a2 = power;
		int num11 = TC.WIL * (isPowerful ? 20 : 5); // [!code --]
		int num12 = TC.WIL * (isPowerful ? 20 : 5); // [!code ++]
		ConHolyVeil condition = TC.GetCondition<ConHolyVeil>();
		if (condition != null)
		{
			num11 += condition.power * 5; // [!code --]
			num12 += condition.power * 5; // [!code ++]
		}
		if (id != EffectId.DebuffKizuami && EClass.rnd(a2) < num11 / EClass.sources.stats.alias[n].hexPower && EClass.rnd(10) != 0) // [!code --]
		if (id != EffectId.DebuffKizuami && EClass.rnd(a2) < num12 / EClass.sources.stats.alias[n].hexPower && EClass.rnd(10) != 0) // [!code ++]
		{
			TC.Say("debuff_resist", TC);
			CC.DoHostileAction(TC);
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActEffect.cs#L2105-L2110)
```cs:line-numbers=2105
			{
				con.SetElement(EClass.sources.elements.alias[actRef.aliasEle].id);
			}
			(con as ConMiasma)?.SetResistPenetrationLevel(TC, CC); // [!code ++]
		}));
		if (n == "ConBane" && CC.HasElement(1416))
		{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActEffect.cs#L2215-L2249)
```cs:line-numbers=2215
	{
		tc.PlaySound("mutation");
		tc.PlayEffect("mutation");
		int num12 = ((actRef.refThing != null) ? actRef.refThing.GetInt(118) : actRef.refVal); // [!code --]
		if (num12 == 0) // [!code --]
		int num13 = ((actRef.refThing != null) ? actRef.refThing.GetInt(118) : actRef.refVal); // [!code ++]
		if (num13 == 0) // [!code ++]
		{
			num12 = tc.GetInt(118); // [!code --]
			if (num12 == 0) // [!code --]
			num13 = tc.GetInt(118); // [!code ++]
			if (num13 == 0) // [!code ++]
			{
				num12 = EClass.game.seed + tc.uid; // [!code --]
				num13 = EClass.game.seed + tc.uid; // [!code ++]
			}
			num12++; // [!code --]
			num13++; // [!code ++]
		}
		tc.Say("transBlood", tc);
		tc.Talk("tail");
		tc.c_bloodData = null;
		tc.SetInt(118, num12); // [!code --]
		tc.SetInt(118, num13); // [!code ++]
		break;
	}
	case EffectId.Youth:
	{
		tc.PlaySound("mutation");
		tc.PlayEffect("mutation");
		int age = tc.bio.GetAge(tc.Chara); // [!code --]
		if (!flag && age <= 0) // [!code --]
		int age2 = tc.bio.GetAge(tc.Chara); // [!code ++]
		if (!flag && age2 <= 0) // [!code ++]
		{
			tc.SayNothingHappans();
			break;
		}
		age = Mathf.Max(0, age * 100 / (flag ? 75 : (blessed ? 400 : 200))) + (flag ? 1 : 0); // [!code --]
		age2 = Mathf.Max(0, age2 * 100 / (flag ? 75 : (blessed ? 400 : 200))) + (flag ? 1 : 0); // [!code ++]
		tc.Say(flag ? "ageUp" : "ageDown", tc);
		tc.bio.SetAge(tc.Chara, age); // [!code --]
		tc.bio.SetAge(tc.Chara, age2); // [!code ++]
		break;
	}
	case EffectId.EternalYouth:
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActEffect.cs#L2255-L2261)
```cs:line-numbers=2255
			tc.SayNothingHappans();
			break;
		}
		int age2 = tc.bio.GetAge(tc.Chara); // [!code --]
		int age = tc.bio.GetAge(tc.Chara); // [!code ++]
		if (flag)
		{
			if (tc.c_lockedAge != 0)
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActEffect.cs#L2263-L2269)
```cs:line-numbers=2263
				tc.Say("eternalYouth2", tc);
				tc.c_lockedAge = 0;
				tc.elements.Remove(1243);
				tc.bio.SetAge(tc.Chara, age2); // [!code --]
				tc.bio.SetAge(tc.Chara, age); // [!code ++]
			}
			Redirect(EffectId.Youth, BlessedState.Cursed, default(ActRef));
		}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActEffect.cs#L2275-L2281)
```cs:line-numbers=2275
		{
			tc.PlaySound("dropRewardXmas");
			tc.Say("eternalYouth1", tc);
			tc.c_lockedAge = age2 + 1; // [!code --]
			tc.c_lockedAge = age + 1; // [!code ++]
			tc.elements.SetBase(1243, 1);
			if (blessed)
			{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActEffect.cs#L2319-L2334)
```cs:line-numbers=2319
		break;
	case EffectId.Revive:
	{
		List<KeyValuePair<int, Chara>> list9 = EClass.game.cards.globalCharas.Where((KeyValuePair<int, Chara> a) => a.Value.isDead && a.Value.faction == EClass.pc.faction && !a.Value.isSummon && a.Value.c_wasInPcParty).ToList(); // [!code --]
		List<KeyValuePair<int, Chara>> list10 = EClass.game.cards.globalCharas.Where((KeyValuePair<int, Chara> a) => a.Value.isDead && a.Value.faction == EClass.pc.faction && !a.Value.isSummon && a.Value.c_wasInPcParty).ToList(); // [!code ++]
		if (TC.IsPCFaction || TC.IsPCFactionMinion)
		{
			if (TC.IsPC && list9.Count == 0) // [!code --]
			if (TC.IsPC && list10.Count == 0) // [!code ++]
			{
				list9 = EClass.game.cards.globalCharas.Where((KeyValuePair<int, Chara> a) => a.Value.CanRevive() && a.Value.isDead && a.Value.faction == EClass.pc.faction && !a.Value.isSummon).ToList(); // [!code --]
				list10 = EClass.game.cards.globalCharas.Where((KeyValuePair<int, Chara> a) => a.Value.CanRevive() && a.Value.isDead && a.Value.faction == EClass.pc.faction && !a.Value.isSummon).ToList(); // [!code ++]
			}
			if (list9.Count > 0) // [!code --]
			if (list10.Count > 0) // [!code ++]
			{
				list9.RandomItem().Value.Chara.GetRevived(); // [!code --]
				list10.RandomItem().Value.Chara.GetRevived(); // [!code ++]
				break;
			}
		}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActEffect.cs#L2341-L2364)
```cs:line-numbers=2341
	case EffectId.DamageMindGreat:
	case EffectId.Weaken:
	{
		bool flag12 = id == EffectId.DamageBody || id == EffectId.DamageBodyGreat; // [!code --]
		bool flag11 = id == EffectId.DamageBody || id == EffectId.DamageBodyGreat; // [!code ++]
		bool mind2 = id == EffectId.DamageMind || id == EffectId.DamageMindGreat;
		int num14 = ((id == EffectId.DamageBody || id == EffectId.DamageMind) ? 1 : (4 + EClass.rnd(4))); // [!code --]
		int num15 = ((id == EffectId.DamageBody || id == EffectId.DamageMind) ? 1 : (4 + EClass.rnd(4))); // [!code ++]
		if (id == EffectId.Weaken)
		{
			flag12 = EClass.rnd(2) == 0; // [!code --]
			mind2 = !flag12; // [!code --]
			num14 = 1; // [!code --]
			flag11 = EClass.rnd(2) == 0; // [!code ++]
			mind2 = !flag11; // [!code ++]
			num15 = 1; // [!code ++]
		}
		else
		{
			TC.PlayEffect("debuff");
			TC.PlaySound("debuff");
		}
		TC.Say(flag12 ? "damageBody" : "damageMind", TC); // [!code --]
		for (int m = 0; m < num14; m++) // [!code --]
		TC.Say(flag11 ? "damageBody" : "damageMind", TC); // [!code ++]
		for (int m = 0; m < num15; m++) // [!code ++]
		{
			TC.DamageTempElements(power, flag12, mind2, id != EffectId.Weaken); // [!code --]
			TC.DamageTempElements(power, flag11, mind2, id != EffectId.Weaken); // [!code ++]
		}
		if (TC.IsPC)
		{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActEffect.cs#L2414-L2430)
```cs:line-numbers=2414
		{
			EClass.game.religions.Healing.Talk("ability");
		}
		int num15 = Dice.Create((actRef.act != null && EClass.sources.calc.map.ContainsKey(actRef.act.ID)) ? actRef.act.ID : "SpHealLight", power, CC, (actRef.refThing != null) ? null : actRef.act).Roll(); // [!code --]
		int num9 = Dice.Create((actRef.act != null && EClass.sources.calc.map.ContainsKey(actRef.act.ID)) ? actRef.act.ID : "SpHealLight", power, CC, (actRef.refThing != null) ? null : actRef.act).Roll(); // [!code ++]
		if (actRef.refThing != null)
		{
			num15 = num15 * (100 + actRef.refThing.Evalue(750) * 10) / 100; // [!code --]
			num9 = num9 * (100 + actRef.refThing.Evalue(750) * 10) / 100; // [!code ++]
		}
		if (flag)
		{
			TC.DamageHP(num15 / 2, 919, power); // [!code --]
			TC.DamageHP(num9 / 2, 919, power); // [!code ++]
			break;
		}
		TC.HealHPHost(num15, (actRef.refThing == null && id != EffectId.JureHeal) ? HealSource.Magic : HealSource.Item); // [!code --]
		TC.HealHPHost(num9, (actRef.refThing == null && id != EffectId.JureHeal) ? HealSource.Magic : HealSource.Item); // [!code ++]
		TC.CureHost(CureType.Heal, power, state);
		TC.Say((power >= 300) ? "heal_heavy" : "heal_light", TC);
		break;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActEffect.cs#L2440-L2464)
```cs:line-numbers=2440
		break;
	case EffectId.RemoveHex:
	case EffectId.RemoveHexAll:
	{ // [!code ++]
		if (flag)
		{
			Redirect(EffectId.CurseEQ, BlessedState.Normal, default(ActRef));
			break;
		}
		foreach (Condition item8 in TC.conditions.Copy()) // [!code --]
		IList<Condition> list9 = TC.conditions.Copy(); // [!code ++]
		list9.Shuffle(); // [!code ++]
		CC.PlaySound("remove_hex"); // [!code ++]
		CC.PlayEffect("remove_hex"); // [!code ++]
		foreach (Condition item8 in list9) // [!code ++]
		{
			if (item8.Type == ConditionType.Debuff && !item8.IsKilled && EClass.rnd(power * (CC.IsPowerful ? 5 : 2)) > EClass.rnd(item8.power)) // [!code --]
			if (item8.Type != ConditionType.Debuff || item8.IsKilled) // [!code ++]
			{ // [!code ++]
				continue; // [!code ++]
			} // [!code ++]
			if (EClass.rnd(power * (CC.IsPowerful ? 5 : 2)) > EClass.rnd(item8.power) || (id == EffectId.RemoveHex && EClass.rnd(2) == 0)) // [!code ++]
			{
				CC.Say("removeHex", TC, item8.Name.ToLower());
				item8.Kill();
				if (id == EffectId.RemoveHex) // [!code --]
			} // [!code ++]
			else // [!code ++]
			{ // [!code ++]
				if (id == EffectId.RemoveHexAll) // [!code ++]
				{
					break; // [!code --]
					item8.power /= 2; // [!code ++]
				}
				CC.Say("removeHex_fail", TC, item8.Name.ToLower()); // [!code ++]
			} // [!code ++]
			if (id == EffectId.RemoveHex) // [!code ++]
			{ // [!code ++]
				break; // [!code ++]
			}
		}
		TC.AddCondition<ConHolyVeil>(power / 2); // [!code --]
		if (blessed) // [!code ++]
		{ // [!code ++]
			TC.AddCondition<ConHolyVeil>(power / 2); // [!code ++]
		} // [!code ++]
		break;
	} // [!code ++]
	case EffectId.CureCorruption:
		TC.PlaySound("heal");
		TC.PlayEffect("heal");
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActEffect.cs#L2544-L2551)
```cs:line-numbers=2544
		if (TC.HasElement(1211))
		{
			TC.Say("drinkSaltWater_snail", TC);
			int num13 = ((TC.hp > 10) ? (TC.hp - EClass.rnd(10)) : 10000); // [!code --]
			TC.DamageHP(num13, AttackSource.None, CC); // [!code --]
			int num14 = ((TC.hp > 10) ? (TC.hp - EClass.rnd(10)) : 10000); // [!code ++]
			TC.DamageHP(num14, AttackSource.None, CC); // [!code ++]
		}
		else if (TC.IsPC)
		{
```

## ActRanged

[`public override bool Perform()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ActRanged.cs#L266-L271)
```cs:line-numbers=266
		if (EClass.rnd(2) == 0)
		{
			Act.CC.RemoveCondition<ConInvisibility>();
			Act.CC.RemoveCondition<ConDark>(); // [!code ++]
		}
		if (weapon.trait is TraitToolRangeCane)
		{
```

## BaseCondition

[`public bool isPerfume`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/BaseCondition.cs#L98-L103)
```cs:line-numbers=98

	public virtual bool HasDuration => base.source.duration != "-1";

	public virtual int MaxDuration => base.source.durationMax; // [!code ++]
 // [!code ++]
	public virtual bool PreventRegen => false;

	public virtual bool ShouldRefresh => false;
```

[`public virtual int EvaluatePower(int p)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/BaseCondition.cs#L423-L429)
```cs:line-numbers=423

	public virtual int EvaluateTurn(int p)
	{
		return Mathf.Max(1, base.source.duration.Calc(p, 0, P2)); // [!code --]
		return Mathf.Min(Mathf.Max(1, base.source.duration.Calc(p, 0, P2)), (MaxDuration == 0) ? int.MaxValue : MaxDuration); // [!code ++]
	}

	public virtual BaseNotification CreateNotification()
```

## Card

[`public virtual long ApplyProtection(long dmg, int mod = 100)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L4148-L4159)
```cs:line-numbers=4148
		return dmg;
	}

	public int GetResistPenetrationLevel(int idEle, AttackSource attackSource, Card origin = null) // [!code ++]
	{ // [!code ++]
		if (origin == null) // [!code ++]
		{ // [!code ++]
			return 0; // [!code ++]
		} // [!code ++]
		int num = origin.Evalue(1238); // [!code ++]
		if (attackSource == AttackSource.MagicSword) // [!code ++]
		{ // [!code ++]
			num += 2; // [!code ++]
			if (origin.HasElement(1247)) // [!code ++]
			{ // [!code ++]
				num++; // [!code ++]
			} // [!code ++]
		} // [!code ++]
		if (attackSource == AttackSource.MagicArrow && origin.HasElement(1244)) // [!code ++]
		{ // [!code ++]
			num++; // [!code ++]
		} // [!code ++]
		if (attackSource == AttackSource.MagicHand && origin.HasElement(1246)) // [!code ++]
		{ // [!code ++]
			num++; // [!code ++]
		} // [!code ++]
		if (idEle == 916 && (HasElement(1253) || origin.HasElement(1253))) // [!code ++]
		{ // [!code ++]
			num++; // [!code ++]
		} // [!code ++]
		return num; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void DamageHP(long dmg, AttackSource attackSource = AttackSource.None, Card origin = null)
	{
		DamageHP(dmg, 0, 0, attackSource, origin);
	}

	public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSource = AttackSource.None, Card origin = null, bool showEffect = true, Thing weapon = null, Chara originalTarget = null) // [!code --]
	public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSource = AttackSource.None, Card origin = null, bool showEffect = true, Thing weapon = null, Chara originalTarget = null, int resistPenetrationLevel = 0) // [!code ++]
	{
		if (hp < 0)
		{
```

[`public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSourc`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L4237-L4267)
```cs:line-numbers=4237
		}
		if (!e.source.aliasRef.IsEmpty() && attackSource != AttackSource.ManaBackfire)
		{
			int num3 = ((origin != null) ? origin.Evalue(1238) : 0); // [!code --]
			if (origin != null) // [!code --]
			{ // [!code --]
				if (attackSource == AttackSource.MagicSword) // [!code --]
				{ // [!code --]
					num3 += 2; // [!code --]
					if (origin.HasElement(1247)) // [!code --]
					{ // [!code --]
						num3++; // [!code --]
					} // [!code --]
				} // [!code --]
				if (attackSource == AttackSource.MagicArrow && origin.HasElement(1244)) // [!code --]
				{ // [!code --]
					num3++; // [!code --]
				} // [!code --]
				if (attackSource == AttackSource.MagicHand && origin.HasElement(1246)) // [!code --]
				{ // [!code --]
					num3++; // [!code --]
				} // [!code --]
				if (e.id == 916 && (HasElement(1253) || origin.HasElement(1253))) // [!code --]
				{ // [!code --]
					num3++; // [!code --]
				} // [!code --]
			} // [!code --]
			dmg = Element.GetResistDamage(dmg, Evalue(e.source.aliasRef), num3); // [!code --]
			dmg = Element.GetResistDamage(dmg, Evalue(e.source.aliasRef), (resistPenetrationLevel != 0) ? resistPenetrationLevel : GetResistPenetrationLevel(e.id, attackSource, origin)); // [!code ++]
			dmg = dmg * 100 / (100 + Mathf.Clamp(Evalue(961) * 5, -50, 200));
			dmg = dmg * Mathf.Max(100 - Evalue(93), 10) / 100;
		}
```

[`public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSourc`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L4303-L4312)
```cs:line-numbers=4303
		}
		if (EClass.game.principal.enableDamageReduction && IsPCFaction)
		{
			int num4 = ((origin != null) ? origin.LV : EClass._zone.DangerLv); // [!code --]
			if (num4 > 50) // [!code --]
			int num3 = ((origin != null) ? origin.LV : EClass._zone.DangerLv); // [!code ++]
			if (num3 > 50) // [!code ++]
			{
				dmg = dmg * (100 - (int)Mathf.Min(95f, Mathf.Sqrt(num4 - 50))) / 100; // [!code --]
				dmg = dmg * (100 - (int)Mathf.Min(95f, Mathf.Sqrt(num3 - 50))) / 100; // [!code ++]
			}
		}
		if (attackSource == AttackSource.Throw || attackSource == AttackSource.Range)
```

[`public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSourc`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L4332-L4357)
```cs:line-numbers=4332
		}
		if (EClass.pc.HasElement(1207) && isChara)
		{
			int num4 = 0; // [!code ++]
			int num5 = 0;
			int num6 = 0; // [!code --]
			foreach (Condition condition3 in Chara.conditions)
			{
				if (condition3.Type == ConditionType.Buff)
				{
					num5++; // [!code --]
					num4++; // [!code ++]
				}
				else if (condition3.Type == ConditionType.Debuff)
				{
					num6++; // [!code --]
					num5++; // [!code ++]
				}
			}
			if (IsPCFactionOrMinion)
			{
				dmg = dmg * 100 / Mathf.Min(100 + num5 * 5, 120); // [!code --]
				dmg = dmg * 100 / Mathf.Min(100 + num4 * 5, 120); // [!code ++]
			}
			else
			{
				dmg = dmg * Mathf.Min(100 + num6 * 5, 120) / 100; // [!code --]
				dmg = dmg * Mathf.Min(100 + num5 * 5, 120) / 100; // [!code ++]
			}
		}
		if (IsPCParty && EClass.pc.ai is GoalAutoCombat)
```

[`public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSourc`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L4368-L4377)
```cs:line-numbers=4368
		}
		if (dmg >= MaxHP / 10 && Evalue(68) > 0)
		{
			int num7 = MaxHP / 10; // [!code --]
			long num8 = dmg - num7; // [!code --]
			num8 = num8 * 100 / (200 + Evalue(68) * 10); // [!code --]
			dmg = num7 + num8; // [!code --]
			int num6 = MaxHP / 10; // [!code ++]
			long num7 = dmg - num6; // [!code ++]
			num7 = num7 * 100 / (200 + Evalue(68) * 10); // [!code ++]
			dmg = num6 + num7; // [!code ++]
		}
	}
	if (origin != null && origin.IsPC && EClass.pc.Evalue(654) > 0)
```

[`public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSourc`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L4382-L4414)
```cs:line-numbers=4382
	{
		dmg = 0L;
	}
	long num9 = 99999999L; // [!code --]
	long num8 = 99999999L; // [!code ++]
	if (origin != null && (attackSource == AttackSource.Melee || attackSource == AttackSource.Range))
	{
		ConStrife condition = origin.GetCondition<ConStrife>();
		if (condition != null)
		{
			num9 = num9 * (100 + condition.lv * 5) / 100; // [!code --]
			num8 = num8 * (100 + condition.lv * 5) / 100; // [!code ++]
		}
	}
	if (dmg > num9) // [!code --]
	if (dmg > num8) // [!code ++]
	{
		dmg = num9; // [!code --]
		dmg = num8; // [!code ++]
	}
	float num10 = Mathf.Clamp(dmg * 6 / MaxHP, 0f, 4f) + (float)((dmg > 0) ? 1 : 0); // [!code --]
	int num11 = hp; // [!code --]
	float num9 = Mathf.Clamp(dmg * 6 / MaxHP, 0f, 4f) + (float)((dmg > 0) ? 1 : 0); // [!code ++]
	int num10 = hp; // [!code ++]
	if (Evalue(1421) > 0)
	{
		long num12 = 0L; // [!code --]
		long num13 = dmg; // [!code --]
		long num11 = 0L; // [!code ++]
		long num12 = dmg; // [!code ++]
		if (hp > 0)
		{
			num13 = dmg - hp; // [!code --]
			num12 = dmg - hp; // [!code ++]
			hp -= (int)dmg;
			num12 += dmg; // [!code --]
			num11 += dmg; // [!code ++]
			if (hp < 0 && Chara.mana.value >= 0)
			{
				num12 += hp; // [!code --]
				num11 += hp; // [!code ++]
				hp = 0;
			}
		}
```

[`public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSourc`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L4416-L4437)
```cs:line-numbers=4416
		{
			if (Evalue(1421) >= 2)
			{
				num13 /= 2; // [!code --]
				num12 /= 2; // [!code ++]
			}
			dmg = num13; // [!code --]
			dmg = num12; // [!code ++]
			if (Chara.mana.value > 0)
			{
				num13 -= Chara.mana.value; // [!code --]
				num12 -= Chara.mana.value; // [!code ++]
				Chara.mana.value -= (int)dmg;
				num12 += dmg; // [!code --]
				num11 += dmg; // [!code ++]
			}
			if (Chara.mana.value <= 0)
			{
				hp -= (int)num13; // [!code --]
				num12 += num13; // [!code --]
				hp -= (int)num12; // [!code ++]
				num11 += num12; // [!code ++]
			}
		}
		dmg = num12; // [!code --]
		dmg = num11; // [!code ++]
	}
	else
	{
```

[`public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSourc`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L4499-L4505)
```cs:line-numbers=4499
				if (origin != null && origin != this && Evalue(436) > 0 && !HasCondition<ConFractured>())
				{
					int half = (HasElement(1218) ? MaxHP : (MaxHP / 2));
					if (num11 > half) // [!code --]
					if (num10 > half) // [!code ++]
					{
						EvadeDeath(delegate
						{
```

[`public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSourc`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L4507-L4513)
```cs:line-numbers=4507
							Chara.AddCondition<ConFractured>((int)Mathf.Max(10f, 30f - Mathf.Sqrt(Evalue(436))));
							hp = Mathf.Min(half * (int)Mathf.Sqrt(Evalue(436) * 2) / 100, MaxHP / 3);
						});
						goto IL_1111; // [!code --]
						goto IL_1066; // [!code ++]
					}
				}
				if (zoneInstanceBout != null && (bool)LayerDrama.Instance)
```

[`public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSourc`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L4535-L4541)
```cs:line-numbers=4535
						if (EClass.player.invlunerable)
						{
							EvadeDeath(null);
							goto IL_1111; // [!code --]
							goto IL_1066; // [!code ++]
						}
					}
					if (Evalue(1220) > 0 && Chara.stamina.value >= (IsPC ? (Chara.stamina.max / 2) : (Chara.stamina.max / 3 * 2)))
```

[`public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSourc`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L4553-L4560)
```cs:line-numbers=4553
			}
		}
	}
	goto IL_1111; // [!code --]
	IL_1111: // [!code --]
	goto IL_1066; // [!code ++]
	IL_1066: // [!code ++]
	if (trait.CanBeAttacked)
	{
		renderer.PlayAnime(AnimeID.HitObj);
```

[`public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSourc`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L4746-L4774)
```cs:line-numbers=4746
	}
	else if ((attackSource == AttackSource.Melee || attackSource == AttackSource.Range) && origin != null && originalTarget == null)
	{
		(IsPC ? EClass.pc : origin).Say("dmgMelee" + num10 + (IsPC ? "pc" : ""), origin, this); // [!code --]
		(IsPC ? EClass.pc : origin).Say("dmgMelee" + num9 + (IsPC ? "pc" : ""), origin, this); // [!code ++]
	}
	else if (isChara)
	{
		int num14 = ((attackSource != AttackSource.Condition && attackSource != AttackSource.WeaponEnchant) ? 1 : 2); // [!code --]
		if (num10 >= (float)num14) // [!code --]
		int num13 = ((attackSource != AttackSource.Condition && attackSource != AttackSource.WeaponEnchant) ? 1 : 2); // [!code ++]
		if (num9 >= (float)num13) // [!code ++]
		{
			if (e != Element.Void)
			{
				Say("dmg_" + e.source.alias, this);
			}
			if (e == Element.Void || num10 >= 2f) // [!code --]
			if (e == Element.Void || num9 >= 2f) // [!code ++]
			{
				Say("dmg" + num10, this); // [!code --]
				Say("dmg" + num9, this); // [!code ++]
			}
		}
	}
	onEvade?.Invoke();
	if (isChara)
	{
		for (int num15 = Chara.conditions.Count - 1; num15 >= 0; num15--) // [!code --]
		for (int num14 = Chara.conditions.Count - 1; num14 >= 0; num14--) // [!code ++]
		{
			Chara.conditions[num15].OnHit(origin, attackSource); // [!code --]
			Chara.conditions[num14].OnHit(origin, attackSource); // [!code ++]
		}
		if (Chara.mimicry == null && HasElement(1429))
		{
```

[`public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSourc`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L4790-L4797)
```cs:line-numbers=4790
			}
			if (attackSource == AttackSource.Melee || attackSource == AttackSource.Range)
			{
				int num16 = origin.Dist(this); // [!code --]
				if (attackSource == AttackSource.Melee && HasElement(1221) && num16 <= Evalue(1221)) // [!code --]
				int num15 = origin.Dist(this); // [!code ++]
				if (attackSource == AttackSource.Melee && HasElement(1221) && num15 <= Evalue(1221)) // [!code ++]
				{
					int ele2 = ((Chara.MainElement == Element.Void) ? 924 : Chara.MainElement.id);
					if (id == "hedgehog_ether")
```

[`public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSourc`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L4801-L4807)
```cs:line-numbers=4801
					Say("reflect_thorne", origin, this);
					origin.DamageHP((int)Mathf.Clamp(dmg / 10, 1f, MaxHP / (origin.IsPowerful ? 200 : 20)), ele2, Power, AttackSource.Condition, this);
				}
				if (HasElement(1223) && num16 <= Evalue(1223)) // [!code --]
				if (HasElement(1223) && num15 <= Evalue(1223)) // [!code ++]
				{
					int ele3 = ((Chara.MainElement == Element.Void) ? 923 : Chara.MainElement.id);
					Say("reflect_acid", this, origin);
```

[`public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSourc`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L4828-L4842)
```cs:line-numbers=4828
			}
		}
	}
	int num17 = ((EClass.rnd(2) == 0) ? 1 : 0); // [!code --]
	int num16 = ((EClass.rnd(2) == 0) ? 1 : 0); // [!code ++]
	if (attackSource == AttackSource.Condition)
	{
		num17 = 1 + EClass.rnd(2); // [!code --]
		num16 = 1 + EClass.rnd(2); // [!code ++]
	}
	if (num17 > 0) // [!code --]
	if (num16 > 0) // [!code ++]
	{
		bool flag3 = Chara.HasCondition<ConPoison>() || ((e.id == 915 || e.id == 923) && ResistLv(Evalue(955)) < 4);
		AddBlood(num17, flag3 ? 6 : (-1)); // [!code --]
		AddBlood(num16, flag3 ? 6 : (-1)); // [!code ++]
	}
	if (dmg > 0 || (origin != null && origin.HasElement(1345)))
	{
```

[`public void DamageHP(long dmg, int ele, int eleP = 100, AttackSource attackSourc`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L4850-L4863)
```cs:line-numbers=4850
	}
	if (IsPC)
	{
		float num18 = (float)hp / (float)MaxHP; // [!code --]
		float num17 = (float)hp / (float)MaxHP; // [!code ++]
		if (Evalue(1421) > 0)
		{
			num18 = (float)Chara.mana.value / (float)Chara.mana.max; // [!code --]
			num17 = (float)Chara.mana.value / (float)Chara.mana.max; // [!code ++]
		}
		if (num18 < 0.3f) // [!code --]
		if (num17 < 0.3f) // [!code ++]
		{
			PlaySound("heartbeat", 1f - num18 * 2f); // [!code --]
			PlaySound("heartbeat", 1f - num17 * 2f); // [!code ++]
		}
	}
	if (!IsPC && hp < MaxHP / 5 && Evalue(423) <= 0 && dmg * 100 / MaxHP + 10 > EClass.rnd(IsPowerful ? 400 : 150) && !HasCondition<ConFear>())
```

[`void ProcAbsorb()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L4924-L4960)
```cs:line-numbers=4924
	{
		if (origin != null && origin.isChara && isChara && (weapon == null || !weapon.HasElement(486)))
		{
			int num19 = origin.Evalue(662) + (weapon?.Evalue(662, ignoreGlobalElement: true) ?? 0); // [!code --]
			int num20 = origin.Evalue(661) + (weapon?.Evalue(661, ignoreGlobalElement: true) ?? 0); // [!code --]
			if (num19 != 0) // [!code --]
			int num18 = origin.Evalue(662) + (weapon?.Evalue(662, ignoreGlobalElement: true) ?? 0); // [!code ++]
			int num19 = origin.Evalue(661) + (weapon?.Evalue(661, ignoreGlobalElement: true) ?? 0); // [!code ++]
			if (num18 != 0) // [!code ++]
			{
				Debug.Log(num19); // [!code --]
				Debug.Log(num18); // [!code ++]
			}
			if (num19 > 0 && attackSource == AttackSource.Melee && origin.isChara && !origin.Chara.ignoreSPAbsorb && Chara.IsHostile(origin as Chara)) // [!code --]
			if (num18 > 0 && attackSource == AttackSource.Melee && origin.isChara && !origin.Chara.ignoreSPAbsorb && Chara.IsHostile(origin as Chara)) // [!code ++]
			{
				int num21 = EClass.rnd(3 + (int)Mathf.Clamp(dmg / 100, 0f, num19 / 10)); // [!code --]
				origin.Chara.stamina.Mod(num21); // [!code --]
				int num20 = EClass.rnd(3 + (int)Mathf.Clamp(dmg / 100, 0f, num18 / 10)); // [!code ++]
				origin.Chara.stamina.Mod(num20); // [!code ++]
				if (IsAliveInCurrentZone)
				{
					Chara.stamina.Mod(-num21); // [!code --]
					Chara.stamina.Mod(-num20); // [!code ++]
				}
			}
			if (origin.HasElement(1350) && attackSource == AttackSource.Melee)
			{
				int num22 = EClass.rndHalf(2 + (int)Mathf.Clamp(dmg / 10, 0f, origin.Chara.GetPietyValue() + 10)); // [!code --]
				origin.Chara.mana.Mod(num22); // [!code --]
				int num21 = EClass.rndHalf(2 + (int)Mathf.Clamp(dmg / 10, 0f, origin.Chara.GetPietyValue() + 10)); // [!code ++]
				origin.Chara.mana.Mod(num21); // [!code ++]
				if (IsAliveInCurrentZone)
				{
					Chara.mana.Mod(-num22); // [!code --]
					Chara.mana.Mod(-num21); // [!code ++]
				}
			}
			if (num20 > 0 && attackSource == AttackSource.Melee) // [!code --]
			if (num19 > 0 && attackSource == AttackSource.Melee) // [!code ++]
			{
				int num23 = EClass.rnd(2 + (int)Mathf.Clamp(dmg / 10, 0f, num20 + 10)); // [!code --]
				origin.Chara.mana.Mod(num23); // [!code --]
				int num22 = EClass.rnd(2 + (int)Mathf.Clamp(dmg / 10, 0f, num19 + 10)); // [!code ++]
				origin.Chara.mana.Mod(num22); // [!code ++]
				if (IsAliveInCurrentZone)
				{
					Chara.mana.Mod(-num23); // [!code --]
					Chara.mana.Mod(-num22); // [!code ++]
				}
			}
		}
```

[`public virtual void Die(Element e = null, Card origin = null, AttackSource attac`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L5157-L5162)
```cs:line-numbers=5157
		{
			if (!(t.trait is TraitChestMerchant))
			{
				t.isHidden = false; // [!code ++]
				EClass._zone.AddCard(t, _pos);
			}
		});
```

[`public int RefreshColor()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Card.cs#L6305-L6311)
```cs:line-numbers=6305
{
	if (isChara)
	{
		if (Chara.mimicry != null) // [!code --]
		if (Chara.mimicry != null && Chara.mimicry.Card != this) // [!code ++]
		{
			_colorInt = Chara.mimicry.Card.RefreshColor();
		}
```

## Chara

[`protected override void OnDeserialized()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Chara.cs#L1382-L1388)
```cs:line-numbers=1382

	public override string GetName(NameStyle style, int num = -1)
	{
		if (mimicry != null) // [!code --]
		if (mimicry != null && mimicry.Card != this) // [!code ++]
		{
			return mimicry.GetName(style, num);
		}
```

[`public override MoveResult _Move(Point newPoint, MoveType type = MoveType.Walk)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Chara.cs#L3203-L3209)
```cs:line-numbers=3203
	}
	if (IsPC)
	{
		if (renderer.anime == null && renderer.replacer != null) // [!code --]
		if (renderer.anime == null && renderer.replacer != null && renderer.replacer.pccData == null) // [!code ++]
		{
			renderer.PlayAnime(AnimeID.Hop);
		}
```

[`public override CardRenderer _CreateRenderer()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Chara.cs#L6819-L6824)
```cs:line-numbers=6819
			{
				charaRenderer.replacer = rendererReplacer;
				charaRenderer.data = rendererReplacer.data;
				charaRenderer.pccData = rendererReplacer.pccData; // [!code ++]
				break;
			}
		}
```

[`public override string GetHoverText()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Chara.cs#L6942-L6948)
```cs:line-numbers=6942
	{
		return mimicry.GetHoverText();
	}
	string text = ((mimicry != null) ? mimicry.GetName(NameStyle.Full) : base.Name); // [!code --]
	string text = ((mimicry != null && mimicry.Card != this) ? mimicry.GetName(NameStyle.Full) : base.Name); // [!code ++]
	if (IsFriendOrAbove())
	{
		text = text.TagColor(EClass.Colors.colorFriend);
```

[`public bool ShouldThrowAway(Thing t, ClearInventoryType type)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Chara.cs#L8713-L8730)
```cs:line-numbers=8713
	switch (type)
	{
	case ClearInventoryType.SellAtTown:
		switch (t.category.id) // [!code --]
		if (!t.isGifted && !t.isNPCProperty) // [!code ++]
		{
		default: // [!code --]
			if (!t.isGifted && !t.isNPCProperty) // [!code --]
			{ // [!code --]
				return false; // [!code --]
			} // [!code --]
			break; // [!code --]
		case "fish": // [!code --]
		case "junk": // [!code --]
		case "garbage": // [!code --]
			break; // [!code --]
			return false; // [!code ++]
		}
		break;
	case ClearInventoryType.Purge:
```

## CharaBody

[`public void RefreshBodyParts()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/CharaBody.cs#L315-L321)
```cs:line-numbers=315
		int elementId = bodySlot.elementId;
		if (elementId == 35)
		{
			if (slotMainHand == null) // [!code --]
			if (slotMainHand == null || slotMainHand == bodySlot) // [!code ++]
			{
				slotMainHand = bodySlot;
			}
```

[`public BodySlot GetSlot(Thing t, bool onlyEmpty = false, bool secondSlot = false`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/CharaBody.cs#L380-L393)
```cs:line-numbers=380
		return null;
	}

	public BodySlot GetSlot(int elementId, bool onlyEmpty = true, bool secondSlot = false) // [!code --]
	public BodySlot GetSlot(int elementId, bool onlyEmpty = true, bool getSecondSlot = false) // [!code ++]
	{
		bool flag = true;
		foreach (BodySlot slot in slots)
		{
			if (elementId == slot.elementId && (!onlyEmpty || slot.thing == null))
			{
				if (!(secondSlot && flag)) // [!code --]
				if (!(getSecondSlot && flag)) // [!code ++]
				{
					return slot;
				}
```

## ConBuffStats

[``](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ConBuffStats.cs#L3-L15)
```cs:line-numbers=3

public class ConBuffStats : Condition
{
	public override string Name => (isDebuff ? "debuff" : "buff").lang(); // [!code --]
	public override string Name => (IsDebuff ? "debuff" : "buff").lang(); // [!code ++]

	public override ConditionType Type
	{
		get
		{
			if (!isDebuff) // [!code --]
			if (!IsDebuff) // [!code ++]
			{
				return ConditionType.Buff;
			}
```

[`public override ConditionType Type`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ConBuffStats.cs#L17-L23)
```cs:line-numbers=17
		}
	}

	public bool isDebuff => base.refVal2 == 222; // [!code --]
	public bool IsDebuff => base.refVal2 == 222; // [!code ++]
 // [!code ++]
	public override int MaxDuration // [!code ++]
	{ // [!code ++]
		get // [!code ++]
		{ // [!code ++]
			if (!IsDebuff) // [!code ++]
			{ // [!code ++]
				return 0; // [!code ++]
			} // [!code ++]
			return base.MaxDuration; // [!code ++]
		} // [!code ++]
	} // [!code ++]

	public override bool AllowMultipleInstance => true;

```

[`public override ConditionType Type`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ConBuffStats.cs#L25-L31)
```cs:line-numbers=25

	public override bool IsOverrideConditionMet(Condition c, int turn)
	{
		if ((c as ConBuffStats).isDebuff == isDebuff) // [!code --]
		if ((c as ConBuffStats).IsDebuff == IsDebuff) // [!code ++]
		{
			return base.IsOverrideConditionMet(c, turn);
		}
```

[`public override int GetPhase()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ConBuffStats.cs#L53-L59)
```cs:line-numbers=53

	public override Color GetColor(SkinColorProfile c)
	{
		if (!isDebuff) // [!code --]
		if (!IsDebuff) // [!code ++]
		{
			return c.textGood;
		}
```

[`public override string GetText()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ConBuffStats.cs#L70-L76)
```cs:line-numbers=70
	string[] list = Lang.GetList("buff_" + EClass.sources.elements.map[base.refVal].alias);
	if (list != null)
	{
		if (!isDebuff) // [!code --]
		if (!IsDebuff) // [!code ++]
		{
			return list[0];
		}
```

[`public override void PlayEffect()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ConBuffStats.cs#L83-L91)
```cs:line-numbers=83
	{
		if (!Condition.ignoreEffect)
		{
			owner.PlaySound(isDebuff ? "debuff" : "buff"); // [!code --]
			owner.PlayEffect(isDebuff ? "debuff" : "buff"); // [!code --]
			owner.Say(isDebuff ? "buffStats_curse" : "buffStats", owner, EClass.sources.elements.map[base.refVal].GetName().ToLower()); // [!code --]
			owner.PlaySound(IsDebuff ? "debuff" : "buff"); // [!code ++]
			owner.PlayEffect(IsDebuff ? "debuff" : "buff"); // [!code ++]
			owner.Say(IsDebuff ? "buffStats_curse" : "buffStats", owner, EClass.sources.elements.map[base.refVal].GetName().ToLower()); // [!code ++]
		}
	}

```

[`public override bool CanStack(Condition c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ConBuffStats.cs#L101-L107)
```cs:line-numbers=101
	public override void SetOwner(Chara _owner, bool onDeserialize = false)
	{
		base.SetOwner(_owner);
		elements.SetBase(base.refVal, CalcValue() * ((!isDebuff) ? 1 : (-1))); // [!code --]
		elements.SetBase(base.refVal, CalcValue() * ((!IsDebuff) ? 1 : (-1))); // [!code ++]
		elements.SetParent(owner);
	}

```

[`public int CalcValue()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ConBuffStats.cs#L120-L125)
```cs:line-numbers=120

	public override void OnWriteNote(List<string> list)
	{
		list.Add((isDebuff ? "hintDebuffStats" : "hintBuffStats").lang(base.sourceElement.GetName(), CalcValue().ToString() ?? "")); // [!code --]
		list.Add((IsDebuff ? "hintDebuffStats" : "hintBuffStats").lang(base.sourceElement.GetName(), CalcValue().ToString() ?? "")); // [!code ++]
	}
}
```

## ConMiasma

[`public class ConMiasma : TimeDebuff`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ConMiasma.cs#L6-L17)
```cs:line-numbers=6
{
	public override bool IsElemental => true;

	public void SetResistPenetrationLevel(Chara tc, Chara cc) // [!code ++]
	{ // [!code ++]
		base.refVal2 = tc.GetResistPenetrationLevel(base.refVal, AttackSource.Condition, cc); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public override void Tick()
	{
		Dice dice = Dice.Create("miasma_", base.power);
		try
		{
			owner.DamageHP(dice.Roll(), base.refVal, EClass.rnd(base.power / 2) + base.power / 4, AttackSource.Condition); // [!code --]
			owner.DamageHP(dice.Roll(), base.refVal, EClass.rnd(base.power / 2) + base.power / 4, AttackSource.Condition, null, showEffect: true, null, null, base.refVal2); // [!code ++]
			if (owner.IsAliveInCurrentZone && base.value > 1)
			{
				for (int i = 0; i < 6; i++)
```

[`public override void Tick()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ConMiasma.cs#L24-L29)
```cs:line-numbers=24
						{
							con.givenByPcParty = base.givenByPcParty;
							con.SetElement(base.refVal);
							con.refVal2 = base.refVal2; // [!code ++]
						}));
						if (condition != null)
						{
```

## ConTransmuteHuman

[`public override bool ShouldEndMimicry(Act act)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ConTransmuteHuman.cs#L42-L47)
```cs:line-numbers=42

	public override RendererReplacer GetRendererReplacer()
	{
		if (chara.IsPCC) // [!code ++]
		{ // [!code ++]
			return RendererReplacer.CreateFromPCC(chara.id, chara.pccData); // [!code ++]
		} // [!code ++]
		return RendererReplacer.CreateFrom(chara.id);
	}

```

[`public override void OnBeforeStart()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ConTransmuteHuman.cs#L62-L68)
```cs:line-numbers=62
			}
			else
			{
				List<Chara> list2 = owner.pos.ListCharasInRadius(owner, 5, delegate(Chara c) // [!code --]
				List<Chara> list2 = owner.pos.ListCharasInRadius(owner, 6, delegate(Chara c) // [!code ++]
				{
					if (!c.IsMultisize && c.IsHumanSpeak)
					{
```

[`public void NyaruSpecial()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ConTransmuteHuman.cs#L92-L98)
```cs:line-numbers=92
{
	if (chara != null && chara.id != owner.id)
	{
		chara = CharaGen.Create(owner.id); // [!code --]
		chara = owner; // [!code ++]
	}
	else
	{
```

## Condition

[`public static Condition Create(string alias, int power = 100, Action<Condition>`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Condition.cs#L57-L62)
```cs:line-numbers=57
	public virtual void OnStacked(int p)
	{
		base.value += EvaluateTurn(p);
		if (MaxDuration != 0 && base.value > MaxDuration) // [!code ++]
		{ // [!code ++]
			base.value = MaxDuration; // [!code ++]
		} // [!code ++]
		SetPhase();
	}

```

## DNA

[`public Element GetInvalidAction(Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/DNA.cs#L705-L710)
```cs:line-numbers=705
		return null;
	}

	public int GetBodySlot() // [!code ++]
	{ // [!code ++]
		for (int i = 0; i < vals.Count; i += 2) // [!code ++]
		{ // [!code ++]
			Element element = Element.Create(vals[i], vals[i + 1]); // [!code ++]
			if (element.source.category == "slot") // [!code ++]
			{ // [!code ++]
				return element.id; // [!code ++]
			} // [!code ++]
		} // [!code ++]
		return -1; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void ReplaceBodySlot(int slot) // [!code ++]
	{ // [!code ++]
		for (int i = 0; i < vals.Count; i += 2) // [!code ++]
		{ // [!code ++]
			if (Element.Create(vals[i], vals[i + 1]).source.category == "slot") // [!code ++]
			{ // [!code ++]
				vals[i] = slot; // [!code ++]
			} // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void MakeSlimeFood(Chara c)
	{
		int num = c.Evalue(6608) + 10;
```

## ElementContainerCard

[`public override void OnTrain(int ele)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/ElementContainerCard.cs#L74-L80)
```cs:line-numbers=74

	public override void OnModTempPotential(Element e, int v, int threshMsg)
	{
		if (threshMsg == 0 || Mathf.Abs(v) >= threshMsg) // [!code --]
		if ((threshMsg == 0 || Mathf.Abs(v) >= threshMsg) && v != 0) // [!code ++]
		{
			string lang = ((v > 0) ? "potentialInc" : "potentialDec");
			if (owner.IsPCFaction && v > 0)
```

## GoalCombat

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/GoalCombat.cs#L153-L159)
```cs:line-numbers=153
		{
			CursorSystem.ignoreCount = 1;
		}
		if (tc.host != null && (tc.hp == 0 || EClass.rnd(5) == 0)) // [!code --]
		if (tc.host != null && (tc.hp == 0 || EClass.rnd(5) == 0 || tc.host.isRestrained)) // [!code ++]
		{
			tc = owner.SetEnemy(tc.host);
		}
```

## RendererReplacer

[`public class RendererReplacer : EClass`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/RendererReplacer.cs#L8-L13)
```cs:line-numbers=8

	public SourcePref pref;

	public PCCData pccData; // [!code ++]
 // [!code ++]
	public static RendererReplacer CreateFrom(string id, int shift = 0, int mat = -1)
	{
		CardRow cardRow = EClass.sources.cards.map.TryGetValue(id);
```

[`public static RendererReplacer CreateFrom(string id, int shift = 0, int mat = -1`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/RendererReplacer.cs#L23-L26)
```cs:line-numbers=23
			mat = mat
		};
	}
 // [!code ++]
	public static RendererReplacer CreateFromPCC(string id, PCCData pccData) // [!code ++]
	{ // [!code ++]
		CardRow cardRow = EClass.sources.cards.map.TryGetValue(id); // [!code ++]
		if (cardRow == null) // [!code ++]
		{ // [!code ++]
			cardRow = EClass.sources.cards.map["money2"]; // [!code ++]
		} // [!code ++]
		return new RendererReplacer // [!code ++]
		{ // [!code ++]
			data = cardRow.renderData, // [!code ++]
			pref = cardRow.pref, // [!code ++]
			pccData = pccData // [!code ++]
		}; // [!code ++]
	} // [!code ++]
}
```

## SourceStat

[`public class Row : BaseRow`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/SourceStat.cs#L22-L27)
```cs:line-numbers=22

		public string duration;

		public int durationMax; // [!code ++]
 // [!code ++]
		public int hexPower;

		public string[] negate;
```

[`public override Row CreateRow()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/SourceStat.cs#L110-L139)
```cs:line-numbers=110
			group = SourceData.GetString(5),
			curse = SourceData.GetString(6),
			duration = SourceData.GetString(7),
			hexPower = SourceData.GetInt(8), // [!code --]
			negate = SourceData.GetStringArray(9), // [!code --]
			defenseAttb = SourceData.GetStringArray(10), // [!code --]
			resistance = SourceData.GetStringArray(11), // [!code --]
			gainRes = SourceData.GetInt(12), // [!code --]
			elements = SourceData.GetStringArray(13), // [!code --]
			nullify = SourceData.GetStringArray(14), // [!code --]
			tag = SourceData.GetStringArray(15), // [!code --]
			phase = SourceData.GetIntArray(16), // [!code --]
			colors = SourceData.GetString(17), // [!code --]
			element = SourceData.GetString(18), // [!code --]
			effect = SourceData.GetStringArray(19), // [!code --]
			strPhase_JP = SourceData.GetStringArray(20), // [!code --]
			strPhase = SourceData.GetStringArray(21), // [!code --]
			textPhase_JP = SourceData.GetString(22), // [!code --]
			textPhase = SourceData.GetString(23), // [!code --]
			textEnd_JP = SourceData.GetString(24), // [!code --]
			textEnd = SourceData.GetString(25), // [!code --]
			textPhase2_JP = SourceData.GetString(26), // [!code --]
			textPhase2 = SourceData.GetString(27), // [!code --]
			gradient = SourceData.GetString(28), // [!code --]
			invert = SourceData.GetBool(29), // [!code --]
			detail_JP = SourceData.GetString(30), // [!code --]
			detail = SourceData.GetString(31) // [!code --]
			durationMax = SourceData.GetInt(8), // [!code ++]
			hexPower = SourceData.GetInt(9), // [!code ++]
			negate = SourceData.GetStringArray(10), // [!code ++]
			defenseAttb = SourceData.GetStringArray(11), // [!code ++]
			resistance = SourceData.GetStringArray(12), // [!code ++]
			gainRes = SourceData.GetInt(13), // [!code ++]
			elements = SourceData.GetStringArray(14), // [!code ++]
			nullify = SourceData.GetStringArray(15), // [!code ++]
			tag = SourceData.GetStringArray(16), // [!code ++]
			phase = SourceData.GetIntArray(17), // [!code ++]
			colors = SourceData.GetString(18), // [!code ++]
			element = SourceData.GetString(19), // [!code ++]
			effect = SourceData.GetStringArray(20), // [!code ++]
			strPhase_JP = SourceData.GetStringArray(21), // [!code ++]
			strPhase = SourceData.GetStringArray(22), // [!code ++]
			textPhase_JP = SourceData.GetString(23), // [!code ++]
			textPhase = SourceData.GetString(24), // [!code ++]
			textEnd_JP = SourceData.GetString(25), // [!code ++]
			textEnd = SourceData.GetString(26), // [!code ++]
			textPhase2_JP = SourceData.GetString(27), // [!code ++]
			textPhase2 = SourceData.GetString(28), // [!code ++]
			gradient = SourceData.GetString(29), // [!code ++]
			invert = SourceData.GetBool(30), // [!code ++]
			detail_JP = SourceData.GetString(31), // [!code ++]
			detail = SourceData.GetString(32) // [!code ++]
		};
	}

```

## WidgetSearch

[`public override void Search(string s)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/WidgetSearch.cs#L133-L139)
```cs:line-numbers=133
		{
			foreach (Chara chara in EMono._map.charas)
			{
				if (chara.IsNeutralOrAbove() && (chara.Name.ToLower().Contains(s) || chara.sourceCard.GetSearchName(jp: false).Contains(s))) // [!code --]
				if (!chara.IsNeutralOrAbove()) // [!code ++]
				{ // [!code ++]
					continue; // [!code ++]
				} // [!code ++]
				if (chara.mimicry != null && !chara.IsPCFactionOrMinion && !EMono.debug.showExtra) // [!code ++]
				{ // [!code ++]
					if (chara.mimicry.Card.Name.ToLower().Contains(s) || chara.mimicry.Card.sourceCard.GetSearchName(jp: false).Contains(s)) // [!code ++]
					{ // [!code ++]
						newCards.Add(chara); // [!code ++]
					} // [!code ++]
				} // [!code ++]
				else if (chara.Name.ToLower().Contains(s) || chara.sourceCard.GetSearchName(jp: false).Contains(s)) // [!code ++]
				{
					newCards.Add(chara);
				}
```

## Zone_Town

[`public override void OnRegenerate()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/29905714c7f31d424900155b4ae829e849b19938/Elin/Zone_Town.cs#L16-L30)
```cs:line-numbers=16
		{
			Add("mad_rich");
		}
		if (EClass.rnd(EClass.debug.enable ? 1 : 8) == 0) // [!code --]
		bool flag = this is Zone_Kapul && IsFestival; // [!code ++]
		if (EClass.rnd((EClass.debug.enable || flag) ? 1 : 8) == 0) // [!code ++]
		{
			Add((EClass.rnd(EClass.debug.enable ? 1 : 4) == 0) ? "unseenhand" : "murderer"); // [!code --]
			Add((EClass.rnd((EClass.debug.enable || flag) ? 1 : 4) == 0) ? "unseenhand" : "murderer").AddCondition<ConTransmuteHuman>(); // [!code ++]
		}
		void Add(string id) // [!code --]
		Chara Add(string id) // [!code ++]
		{
			Chara chara = CharaGen.Create(id);
			chara.isSubsetCard = true;
			EClass._zone.AddCard(chara, GetSpawnPos(chara));
			return chara; // [!code ++]
		}
	}
}
```
