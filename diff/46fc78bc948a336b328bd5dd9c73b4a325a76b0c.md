---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 36 files modified. 3 new files created.
version: EA 23.283 Nightly
changes: AI_PlayMusic/AM_Adv/Card/CardRenderer/Chara/ContentConfigInput/Core/CoreDebug/Dialog/DramaCustomSequence/DramaManager/+EVENT/FEAT/Game/GrowSystem/Hobby/ID_Achievement/LayerConsole/Map/ModManager/ModPackage/ModUtil/Msg/Party/Recipe/RenderRow/Scene/SourceCard/SourceChara/SourceElement/+SourceImporter/SourceManager/SourceMaterial/SourceThingV/Steam/TCText/TaskDig/+TraitDeedLostRing/TraitFigure
---

# EA 23.283 Nightly

March 4, 2026

36 files modified. 3 new files created.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [Map (1)](#map)
```cs:no-line-numbers
public void SetBridge(int x, int z, int height = 0, int idMat = 0, int idBridge = 0, int dir = 0) // [!code --]
public void SetBridge(int x, int z, int height = 0, int idMat = 0, int idBridge = 0, int dir = 0, byte idPillar = 0) // [!code ++]
```
### [ModManager (9)](#modmanager)
```cs:no-line-numbers
private void HandleResults(UgcQuery query) // [!code --]
public void LoadLoadOrder() // [!code ++]
```
```cs:no-line-numbers
public IEnumerator RefreshMods(Action onComplete, bool syncMods) // [!code --]
public void ImportSourceLocalizations(string lang) // [!code ++]
```
```cs:no-line-numbers
public void SaveLoadOrder() // [!code --]
public void ImportModDialogs() // [!code ++]
```
```cs:no-line-numbers
public void LoadLoadOrder() // [!code --]
public IEnumerator RefreshMods(Action onComplete, bool syncMods) // [!code ++]
```
```cs:no-line-numbers
public override void ParseExtra(DirectoryInfo dir, BaseModPackage package) // [!code --]
public void LoadCustomPackage() // [!code ++]
```
```cs:no-line-numbers
public void UpdateDialogs(DirectoryInfo dir, string dirTemp) // [!code --]

```
```cs:no-line-numbers
public void UpdateTalks(DirectoryInfo dir, string dirTemp) // [!code --]

```
```cs:no-line-numbers
public void UpdateExcelBook(FileInfo f, string dirTemp, bool updateOnlyText) // [!code --]
public void InitPackagesMeta() // [!code ++]
```
```cs:no-line-numbers
public int UpdateExcelSheet(ISheet destSheet, ISheet oldSheet, bool updateOnlytext) // [!code --]
public void ActivatePackages() // [!code ++]
```
### [ModUtil (1)](#modutil)
```cs:no-line-numbers
public static void Test() // [!code --]

```
### [Party (1)](#party)
```cs:no-line-numbers
public void ModExpPartySkill(int ele, int a) // [!code --]
public void ModExp(int ele, int a) // [!code ++]
```
## AI_PlayMusic

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/AI_PlayMusic.cs#L156-L162)
```cs:line-numbers=156
			idSong = "violin_furusato";
			break;
		default:
			idSong = "violin_chaconne"; // [!code --]
			idSong = ((EClass.Sound.GetData("Instrument/" + tool.id) is BGMData) ? tool.id : "violin_chaconne"); // [!code ++]
			break;
		}
		if (owner.IsPC && EClass.player.knownSongs.ContainsKey(idSong))
```

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/AI_PlayMusic.cs#L181-L189)
```cs:line-numbers=181
			EClass.player.playingSong = playing;
		}
	}
	if (owner.IsPC) // [!code --]
	if (owner.IsPC && EClass.Sound.GetData("Instrument/" + idSong) is BGMData bGMData) // [!code ++]
	{
		(EClass.Sound.GetData("Instrument/" + idSong) as BGMData).song.index = playing.index; // [!code --]
		bGMData.song.index = playing.index; // [!code ++]
	}
	if (Application.isEditor && owner.IsPC)
	{
```

## AM_Adv

[`public override void _OnUpdateInput()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/AM_Adv.cs#L824-L859)
```cs:line-numbers=824
	}
	switch (EInput.action)
	{
	case EAction.Console: // [!code ++]
		EClass.ui.AddLayer<LayerConsole>(); // [!code ++]
		break; // [!code ++]
	case EAction.Chat:
		Dialog.InputName("", "", delegate(bool cancel, string text)
		{
			if (!cancel && !text.IsEmpty())
			{
				Chara chara = EClass.pc; // [!code --]
				if ((text[0] == '@' || text[0] == '＠') && text.Length > 1 && int.TryParse(text[1].ToString().Normalize(NormalizationForm.FormKC), out var result)) // [!code --]
				{ // [!code --]
					text = text.Substring(2); // [!code --]
					chara = EClass.pc.party.members.TryGet(result); // [!code --]
				} // [!code --]
				if (text.StartsWith('@')) // [!code --]
				{ // [!code --]
					text = text.Substring(1); // [!code --]
				} // [!code --]
				chara.SayRaw(text); // [!code --]
				Msg.SetColor("ono"); // [!code --]
				string text2 = text; // [!code --]
				if (!text.StartsWith('*') && !text.StartsWith('(') && !text.StartsWith('（')) // [!code --]
				string[] array = text.SplitByNewline(); // [!code ++]
				foreach (string obj in array) // [!code ++]
				{
					text = text.Bracket(); // [!code --]
				} // [!code --]
				Msg.Say(text, chara); // [!code --]
				if (text2 == "nyan") // [!code --]
				{ // [!code --]
					Msg.SetColor("save"); // [!code --]
					Msg.Say("*" + EClass.player.stats.lastChuryu + " nyan*"); // [!code --]
					Chara chara = EClass.pc; // [!code ++]
					string text2 = obj; // [!code ++]
					if ((text2[0] == '@' || text2[0] == '＠') && text2.Length > 1 && int.TryParse(text2[1].ToString().Normalize(NormalizationForm.FormKC), out var result)) // [!code ++]
					{ // [!code ++]
						text2 = text2.Substring(2); // [!code ++]
						chara = EClass.pc.party.members.TryGet(result); // [!code ++]
					} // [!code ++]
					if (text2.StartsWith('@')) // [!code ++]
					{ // [!code ++]
						text2 = text2.Substring(1); // [!code ++]
					} // [!code ++]
					chara.SayRaw(text2); // [!code ++]
					string text3 = text2; // [!code ++]
					Msg.SetColor("ono"); // [!code ++]
					if (!text2.StartsWith('*') && !text2.StartsWith('(') && !text2.StartsWith('（')) // [!code ++]
					{ // [!code ++]
						text2 = text2.Bracket(); // [!code ++]
					} // [!code ++]
					Msg.Say(text2.RemoveAllTags(), chara); // [!code ++]
					OnEnterChat(text3); // [!code ++]
				}
			}
		}, Dialog.InputType.None); // [!code --]
		}, Dialog.InputType.Chat); // [!code ++]
		break;
	case EAction.Search:
		EClass.ui.widgets.Toggle("Search")?.SoundActivate();
```

[`public override void _OnUpdateInput()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/AM_Adv.cs#L1043-L1048)
```cs:line-numbers=1043
		}
	}

	public virtual void OnEnterChat(string text) // [!code ++]
	{ // [!code ++]
		if (text == "nyan") // [!code ++]
		{ // [!code ++]
			Msg.SetColor("save"); // [!code ++]
			Msg.Say("*" + EClass.player.stats.lastChuryu + " nyan*"); // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void ShowAllAction()
	{
		planAll.Update(mouseTarget);
```

## Card

[`public void ModExp(int ele, int a)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Card.cs#L2686-L2691)
```cs:line-numbers=2686
		}
	}

	public void ModExpParty(int ele, int a) // [!code ++]
	{ // [!code ++]
		if (isChara) // [!code ++]
		{ // [!code ++]
			if (Chara.party == null) // [!code ++]
			{ // [!code ++]
				elements.ModExp(ele, a); // [!code ++]
			} // [!code ++]
			else // [!code ++]
			{ // [!code ++]
				Chara.party.ModExp(ele, a); // [!code ++]
			} // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public bool IsChildOf(Card c)
	{
		return GetRootCard() == c;
```

[`private void _OnDeserialized(StreamingContext context)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Card.cs#L2744-L2777)
```cs:line-numbers=2744
		{
			isDestroyed = true;
		}
		if (version >= 5) // [!code --]
		{ // [!code --]
			return; // [!code --]
		} // [!code --]
		if (version < 3 && isChara && HasElement(1210)) // [!code --]
		{ // [!code --]
			elements.ModBase(960, -5 * Evalue(1210)); // [!code --]
		} // [!code --]
		if (version < 4 && isChara && HasElement(1210)) // [!code --]
		{ // [!code --]
			elements.ModBase(423, Evalue(1210)); // [!code --]
		} // [!code --]
		if (version < 5)
		{
			if (isChara && Chara.race.id == "horse" && Chara.body.GetSlot(30, onlyEmpty: false) == null) // [!code --]
			if (version < 3 && isChara && HasElement(1210)) // [!code ++]
			{
				Chara.body.AddBodyPart(30); // [!code --]
				Chara.body.RefreshBodyParts(); // [!code --]
				elements.ModBase(960, -5 * Evalue(1210)); // [!code ++]
			}
			if (isChara && Chara.race.id == "bike" && id != "bike_cub") // [!code --]
			if (version < 4 && isChara && HasElement(1210)) // [!code ++]
			{
				Rand.SetSeed(uid); // [!code --]
				Chara.body.AddBodyPart(33); // [!code --]
				Chara.SetFeat(1423, 1 + EClass.rnd(10)); // [!code --]
				Rand.SetSeed(); // [!code --]
				elements.ModBase(423, Evalue(1210)); // [!code ++]
			}
			if (version < 5) // [!code ++]
			{ // [!code ++]
				if (isChara && Chara.race.id == "horse" && Chara.body.GetSlot(30, onlyEmpty: false) == null) // [!code ++]
				{ // [!code ++]
					Chara.body.AddBodyPart(30); // [!code ++]
					Chara.body.RefreshBodyParts(); // [!code ++]
				} // [!code ++]
				if (isChara && Chara.race.id == "bike" && id != "bike_cub") // [!code ++]
				{ // [!code ++]
					Rand.SetSeed(uid); // [!code ++]
					Chara.body.AddBodyPart(33); // [!code ++]
					Chara.SetFeat(1423, 1 + EClass.rnd(10)); // [!code ++]
					Rand.SetSeed(); // [!code ++]
				} // [!code ++]
			} // [!code ++]
			version = 5; // [!code ++]
		}
		version = 5; // [!code --]
		BaseModManager.PublishEvent(isChara ? "elin.chara_created" : "elin.thing_created", this); // [!code ++]
	}

	protected virtual void OnDeserialized()
```

[`public void Create(string _id, int _idMat = -1, int genLv = -1)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Card.cs#L2886-L2891)
```cs:line-numbers=2886
			SetHidden();
		}
		isFloating = Pref.Float;
		BaseModManager.PublishEvent(isChara ? "elin.chara_created" : "elin.thing_created", this); // [!code ++]
	}

	public virtual void OnBeforeCreate()
```

## CardRenderer

[`public override void Draw(RenderParam p, ref Vector3 v, bool drawShadow)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/CardRenderer.cs#L250-L271)
```cs:line-numbers=250
				SourceChara.Row row = EClass.sources.charas.map.TryGetValue(owner.c_idRefCard) ?? EClass.sources.charas.map["putty"];
				renderData = row.renderData;
				pref = row.pref;
				int matColor = traitFigure.GetMatColor(); // [!code ++]
				drawShadow = traitFigure.ShowShadow; // [!code ++]
				if (row._tiles.Length == 0 || data.pass == null)
				{
					renderData = owner.sourceCard.renderData; // [!code --]
					pref = owner.sourceCard.pref; // [!code --]
				} // [!code --]
				else // [!code --]
				{ // [!code --]
					if (EClass.core.config.game.antiSpider && row.skinAntiSpider != 0) // [!code --]
					if (traitFigure.extraRenderer == null) // [!code ++]
					{ // [!code ++]
						traitFigure.extraRenderer = new CharaRenderer(); // [!code ++]
						traitFigure.extraRenderer.SetOwner(CharaGen.Create(row.id)); // [!code ++]
					} // [!code ++]
					if (matColor >= -3) // [!code ++]
					{
						owner.refVal = row.skinAntiSpider; // [!code --]
						if (matColor == -3) // [!code ++]
						{ // [!code ++]
							MatColors matColors = EClass.core.Colors.matColors["ether"]; // [!code ++]
							p.matColor = BaseTileMap.GetColorInt(ref matColors.main, 100) * -1; // [!code ++]
						} // [!code ++]
					}
					p.tile = row._tiles[owner.refVal % row._tiles.Length] * ((owner.dir % 2 == 0) ? 1 : (-1)); // [!code --]
					p.matColor = traitFigure.GetMatColor(); // [!code --]
					drawShadow = traitFigure.ShowShadow; // [!code --]
					pref = row.pref; // [!code --]
					else // [!code ++]
					{ // [!code ++]
						p.matColor = matColor; // [!code ++]
					} // [!code ++]
					traitFigure.extraRenderer.Draw(p, ref v, drawShadow); // [!code ++]
					return; // [!code ++]
				}
				if (EClass.core.config.game.antiSpider && row.skinAntiSpider != 0) // [!code ++]
				{ // [!code ++]
					owner.refVal = row.skinAntiSpider; // [!code ++]
				} // [!code ++]
				p.tile = row._tiles[owner.refVal % row._tiles.Length] * ((owner.dir % 2 == 0) ? 1 : (-1)); // [!code ++]
				p.matColor = matColor; // [!code ++]
				pref = row.pref; // [!code ++]
				p.x += pref.x * (float)((owner.dir % 2 == 0) ? 1 : (-1));
				p.y += pref.y;
				p.z += pref.z;
```

## Chara

[`public void ShowDialog()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Chara.cs#L7350-L7356)
```cs:line-numbers=7350
	{
		flag = false;
	}
	if (flag && File.Exists(CorePath.DramaData + id + ".xlsx")) // [!code --]
	if (flag && File.Exists(PackageIterator.GetFiles("Dialog/Drama/" + id + ".xlsx").LastOrDefault()?.FullName ?? (CorePath.DramaData + id + ".xlsx"))) // [!code ++]
	{
		ShowDialog(id);
	}
```

[`public void Divorce(Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Chara.cs#L8238-L8243)
```cs:line-numbers=8238
	if (base.c_love != null)
	{
		base.c_love.dateMarriage = 0;
		base.c_love.dateWedding = 0; // [!code ++]
		SetFeat(1275, 0);
		EClass.player.stats.divorced++;
	}
```

## ContentConfigInput

[`public override void OnInstantiate()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/ContentConfigInput.cs#L306-L311)
```cs:line-numbers=306
			uIList.Add(keys.mute);
			uIList.Add(keys.meditate);
			uIList.Add(keys.chat);
			uIList.Add(keys.console); // [!code ++]
		}
	};
	_refreshList();
```

## Core

[`public void SetLang(string langCode, bool force = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Core.cs#L719-L724)
```cs:line-numbers=719
				game.Kill();
			}
			config.OnSetLang();
			BaseModManager.PublishEvent("elin.source.lang_set", langCode); // [!code ++]
		}
	}

```

[`public static int GetElement(string id)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Core.cs#L750-L759)
```cs:line-numbers=750
	{
		sourceElement.Init();
	}
	if (!sourceElement.alias.TryGetValue(id ?? "_void", out var value)) // [!code --]
	if (!sourceElement.alias.TryGetValue(id ?? (id = "_void"), out var value)) // [!code ++]
	{
		Debug.LogError("exception:" + id); // [!code --]
		value = sourceElement.rows[0]; // [!code --]
		if (sourceElement.fuzzyAlias.TryGetValue(id, out var value2)) // [!code ++]
		{ // [!code ++]
			Debug.Log("#element lookup: " + id + " -> " + value2); // [!code ++]
			value = sourceElement.alias[value2]; // [!code ++]
		} // [!code ++]
		else // [!code ++]
		{ // [!code ++]
			Debug.LogError("#element not found: " + id); // [!code ++]
			value = sourceElement.rows[0]; // [!code ++]
		} // [!code ++]
	}
	return value.id;
}
```

## CoreDebug

[`public void UpdateInput()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/CoreDebug.cs#L976-L1014)
```cs:line-numbers=976
	}
	if (Input.GetKeyDown(KeyCode.F3))
	{
		for (int i = 0; i < 10; i++) // [!code --]
		Thing thing = EClass._map.things.First((Thing _t) => _t.id == "cloud"); // [!code ++]
		if (thing != null) // [!code ++]
		{
			Thing thing = ThingGen.Create("egg_fertilized"); // [!code --]
			thing.TryMakeRandomItem(40); // [!code --]
			thing.SetEncLv(200); // [!code --]
			EClass.pc.Pick(thing); // [!code --]
			Debug.Log(EClass.sources.cards.map["cloud"].pref == thing.Pref); // [!code ++]
			Debug.Log(EClass.sources.cards.map["cloud"].pref == thing.renderer.GetPref()); // [!code ++]
		}
		foreach (Chara deadChara in EClass._map.deadCharas) // [!code --]
		{ // [!code --]
			Debug.Log(deadChara); // [!code --]
		} // [!code --]
		EClass.core.steam.CheckUpdate(); // [!code --]
		EClass.player.flags.loytelMartLv++; // [!code --]
		Msg.Say("loytelmart:" + EClass.player.flags.loytelMartLv); // [!code --]
		Guild.Fighter.relation.rank = 20; // [!code --]
		Guild.Mage.relation.rank = 20; // [!code --]
		Guild.Thief.relation.rank = 20; // [!code --]
		Guild.Merchant.relation.rank = 20; // [!code --]
		if (EClass.Branch != null) // [!code --]
		{ // [!code --]
			EClass.Branch.ModExp(EClass.Branch.GetNextExp()); // [!code --]
		} // [!code --]
		foreach (Chara item in EClass._map.charas.ToList()) // [!code --]
		{ // [!code --]
			item.AddExp(item.ExpToNext); // [!code --]
			item.Vomit(); // [!code --]
		} // [!code --]
		EClass.pc.PlayEffect("boost"); // [!code --]
		EClass.pc.PlaySound("boost"); // [!code --]
		EClass.pc.elements.SetBase(306, 100); // [!code --]
		EClass.pc.elements.SetBase(85, 100); // [!code --]
		EClass.pc.feat += 10; // [!code --]
		EClass.player.totalFeat += 10; // [!code --]
		return;
	}
	if (Input.GetKeyDown(KeyCode.F4))
```

[`public void UpdateInput()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/CoreDebug.cs#L1080-L1086)
```cs:line-numbers=1080
		if (Input.GetKey(KeyCode.F9))
		{
			EClass.scene.paused = false;
			for (int j = 0; j < advanceMin; j++) // [!code --]
			for (int i = 0; i < advanceMin; i++) // [!code ++]
			{
				EClass.game.updater.FixedUpdate();
			}
```

[`public void UpdateInput()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/CoreDebug.cs#L1161-L1169)
```cs:line-numbers=1161
	{
		if (Input.GetMouseButtonDown(0))
		{
			foreach (Card item2 in list) // [!code --]
			foreach (Card item in list) // [!code ++]
			{
				Debug.Log(item2.Name + "/" + item2.dir + "/" + item2.flipX + "/" + item2.angle); // [!code --]
				Debug.Log(item.Name + "/" + item.dir + "/" + item.flipX + "/" + item.angle); // [!code ++]
			}
		}
		if (Input.GetMouseButtonDown(1))
```

[`public void UpdateInput()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/CoreDebug.cs#L1196-L1204)
```cs:line-numbers=1196
		}
		if (key)
		{
			foreach (Card item3 in EClass._map.Roaming.all) // [!code --]
			foreach (Card item2 in EClass._map.Roaming.all) // [!code ++]
			{
				EClass._zone.RemoveCard(item3); // [!code --]
				EClass._zone.RemoveCard(item2); // [!code ++]
			}
		}
		else if (hitPoint.detail != null)
```

[`public void UpdateInput()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/CoreDebug.cs#L1277-L1285)
```cs:line-numbers=1277
		{
			break;
		}
		foreach (Card item4 in hitPoint.ListCards()) // [!code --]
		foreach (Card item3 in hitPoint.ListCards()) // [!code ++]
		{
			item4.renderer.PlayAnime(num5.ToEnum<AnimeID>()); // [!code --]
			item3.renderer.PlayAnime(num5.ToEnum<AnimeID>()); // [!code ++]
		}
		Debug.Log(num5.ToEnum<AnimeID>());
		break;
```

[`public static string Spawn(string id, int num = 1, string aliasMat = "", int obj`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/CoreDebug.cs#L2054-L2059)
```cs:line-numbers=2054
		EClass._zone.AddCard(thing, EClass.pc.pos);
		return "Spawned " + thing.Name;
	}
	_ = EClass.sources.charas.map; // [!code ++]
	if (EClass.sources.charas.map.ContainsKey(id))
	{
		Chara chara = CharaGen.Create(id);
```

[`public static string Spawn(string id, int num = 1, string aliasMat = "", int obj`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/CoreDebug.cs#L2067-L2072)
```cs:line-numbers=2067
		return "'" + id + "' does not exist in the database.";
	}

	[ConsoleCommand("")] // [!code ++]
	public static string SpawnFigure(string id) // [!code ++]
	{ // [!code ++]
		if (!CheatEnabled()) // [!code ++]
		{ // [!code ++]
			return EnableCheat; // [!code ++]
		} // [!code ++]
		if (!EClass.sources.charas.map.ContainsKey(id)) // [!code ++]
		{ // [!code ++]
			return "'" + id + "' does not exist in the database."; // [!code ++]
		} // [!code ++]
		Thing thing = ThingGen.Create("figure"); // [!code ++]
		Thing thing2 = ThingGen.Create("figure3"); // [!code ++]
		string c_idRefCard = (thing2.c_idRefCard = id); // [!code ++]
		thing.c_idRefCard = c_idRefCard; // [!code ++]
		EClass.pc.DropThing(thing); // [!code ++]
		EClass.pc.DropThing(thing2); // [!code ++]
		return "Spawned figures for '" + id + "'"; // [!code ++]
	} // [!code ++]
 // [!code ++]
	[ConsoleCommand("")]
	public static string TestSpawn(int lv, int num, int lvRange = -1)
	{
```

[`public static string FlyMode()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/CoreDebug.cs#L2201-L2206)
```cs:line-numbers=2201
[ConsoleCommand("")]
public static string ChangeRace(string id = "?")
{
	if (!CheatEnabled()) // [!code ++]
	{ // [!code ++]
		return EnableCheat; // [!code ++]
	} // [!code ++]
	if (EClass.sources.races.map.ContainsKey(id))
	{
		Thing thing = null;
```

[`public static string ResetAchievement(ID_Achievement id)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/CoreDebug.cs#L2256-L2261)
```cs:line-numbers=2256
[ConsoleCommand("")]
public static string ChangeJob(string id = "?")
{
	if (!CheatEnabled()) // [!code ++]
	{ // [!code ++]
		return EnableCheat; // [!code ++]
	} // [!code ++]
	if (EClass.sources.jobs.map.ContainsKey(id))
	{
		EClass.pc.ChangeJob(id);
```

## Dialog

[`public enum InputType`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Dialog.cs#L11-L17)
```cs:line-numbers=11
		Default,
		Password,
		Item,
		DistributionFilter // [!code --]
		DistributionFilter, // [!code ++]
		Chat // [!code ++]
	}

	public Text textConfetti;
```

[`private void Update()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Dialog.cs#L120-L125)
```cs:line-numbers=120
		}
	}

	public override bool OnBack() // [!code ++]
	{ // [!code ++]
		if (keymap != null && (!EInput.rightMouse.pressing || EInput.rightMouse.usedKey)) // [!code ++]
		{ // [!code ++]
			return false; // [!code ++]
		} // [!code ++]
		return base.OnBack(); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public override void OnUpdateInput()
	{
		if ((bool)input && option.canClose && (Input.GetKeyDown(KeyCode.Return) || Input.GetKeyDown(KeyCode.KeypadEnter)) && keymap == null)
```

[`public static Dialog InputName(string langDetail, string text, Action<bool, stri`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Dialog.cs#L531-L536)
```cs:line-numbers=531
	d.note.AddText(langDetail.lang()).text1.alignment = TextAnchor.MiddleCenter;
	switch (inputType)
	{
	case InputType.Chat: // [!code ++]
		d.input.field.characterLimit = 500; // [!code ++]
		break; // [!code ++]
	case InputType.DistributionFilter:
		d.input.field.characterLimit = 100;
		break;
```

## DramaCustomSequence

[`using System;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/DramaCustomSequence.cs#L1-L6)
```cs:line-numbers=1
using System;
using System.Collections.Generic;
using System.IO; // [!code --]
using System.Linq;
using UnityEngine;

```

[`public string GetRumor(Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/DramaCustomSequence.cs#L1717-L1731)
```cs:line-numbers=1717

	public bool HasTopic(string idSheet, string idTopic)
	{
		string path = CorePath.CorePackage.TextDialog + "dialog.xlsx"; // [!code --]
		if (!File.Exists(path)) // [!code --]
		{ // [!code --]
			return false; // [!code --]
		} // [!code --]
		ExcelData excelData = new ExcelData(); // [!code --]
		excelData.path = path; // [!code --]
		excelData.BuildMap(idSheet); // [!code --]
		return excelData.sheets[idSheet].map.ContainsKey(idTopic); // [!code --]
		return Lang.GetDialogSheet(idSheet).map.ContainsKey(idTopic); // [!code ++]
	}

	public string GetText(Chara c, string idSheet, string idTopic)
```

## DramaManager

[`public class DramaManager : EMono`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/DramaManager.cs#L58-L63)
```cs:line-numbers=58

	private string lastIF2;

	private SoundSource lastSound; // [!code ++]
 // [!code ++]
	public DramaEventTalk lastTalk;

	public bool enableTone;
```

[`public DramaSequence Load()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/DramaManager.cs#L123-L129)
```cs:line-numbers=123
		sequence.AddActor("tg", tg);
	}
	sequence.AddActor("pc", new Person(EMono.pc));
	string text = CorePath.DramaData + setup.book + ".xlsx"; // [!code --]
	string text = PackageIterator.GetFiles("Dialog/Drama/" + setup.book + ".xlsx").LastOrDefault()?.FullName ?? (CorePath.DramaData + setup.book + ".xlsx"); // [!code ++]
	ExcelData excelData = dictCache.TryGetValue(text);
	if (excelData != null && excelData.IsModified())
	{
```

[`public DramaSequence Load()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/DramaManager.cs#L141-L153)
```cs:line-numbers=141
		foreach (Dictionary<string, string> item in new ExcelData
		{
			maxEmptyRows = 10,
			path = CorePath.DramaDataLocal + setup.book + ".xlsx" // [!code --]
			path = (PackageIterator.GetFiles("Dialog/Drama/" + setup.book + ".xlsx").LastOrDefault()?.FullName ?? (CorePath.DramaDataLocal + setup.book + ".xlsx")) // [!code ++]
		}.BuildList(setup.sheet))
		{
			string text2 = item["id"];
			if (!text2.IsEmpty() && !item["text"].IsEmpty()) // [!code --]
			if (!text2.IsEmpty()) // [!code ++]
			{
				dictLocalize.Add(text2, item["text"]); // [!code --]
				string text3 = item.TryGetValue("text_" + Lang.langCode) ?? item["text"]; // [!code ++]
				if (!dictLocalize.TryAdd(text2, text3)) // [!code ++]
				{ // [!code ++]
					string text4 = dictLocalize[text2]; // [!code ++]
					dictLocalize[text2] = ("[DUPLICATE ID '" + text2 + "']\n").TagColor(Color.red) + text4; // [!code ++]
					Debug.LogError("#drama duplicate id '" + text2 + "' at " + text + "\n" + text4 + " -> " + text3); // [!code ++]
				} // [!code ++]
			}
		}
	}
```

[`public void ParseLine(Dictionary<string, string> item)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/DramaManager.cs#L562-L568)
```cs:line-numbers=562
			{
				SoundManager.current.MuteBGMFor(float.Parse(p3, CultureInfo.InvariantCulture));
			}
			EMono.Sound.Play(p2); // [!code --]
			lastSound?.Stop(); // [!code ++]
			lastSound = EMono.Sound.Play(p2); // [!code ++]
		});
		break;
	case "haltPlaylist":
```

[`public void ParseLine(Dictionary<string, string> item)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/DramaManager.cs#L1015-L1020)
```cs:line-numbers=1015
		});
		break;
	default:
	{ // [!code ++]
		EVENT.ElinDramaParseActionEventArgs elinDramaParseActionEventArgs = new EVENT.ElinDramaParseActionEventArgs // [!code ++]
		{ // [!code ++]
			dm = this, // [!code ++]
			line = item // [!code ++]
		}; // [!code ++]
		elinDramaParseActionEventArgs.SetData(action); // [!code ++]
		BaseModManager.PublishEvent("elin.drama.parse_action", elinDramaParseActionEventArgs); // [!code ++]
		if (elinDramaParseActionEventArgs.IsUsed) // [!code ++]
		{ // [!code ++]
			return; // [!code ++]
		} // [!code ++]
		if (!flag)
		{
			break;
```

[`public void ParseLine(Dictionary<string, string> item)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/DramaManager.cs#L1034-L1039)
```cs:line-numbers=1034
		})) as DramaEventTalk;
		lastTalk.center = p2 == "center";
		break;
	} // [!code ++]
	case "new":
	case "saveBGM":
	case "checkAffinity":
```

## +EVENT

::: details File Created
```cs
using System.Collections.Generic;

public static class EVENT
{
	public class ElinEventArgs<T>
	{
		public bool IsUsed { get; private set; }

		public T data { get; private set; }

		public virtual void SetData(T newData)
		{
			data = newData;
		}

		public virtual void Use()
		{
			IsUsed = true;
		}
	}

	public class ElinFeatApplyEventArgs : ElinEventArgs<int>
	{
		public Feat feat;

		public ElementContainer owner;

		public bool hint;
	}

	public class ElinDramaParseActionEventArgs : ElinEventArgs<string>
	{
		public DramaManager dm;

		public Dictionary<string, string> line;
	}

	public const string Undefined = "undefined";

	public const string ModsActivated = "elin.mods.activated";

	public const string SourceImporting = "elin.source.importing";

	public const string SourceImported = "elin.source.imported";

	public const string SourceLangSet = "elin.source.lang_set";

	public const string PreSave = "elin.game.pre_save";

	public const string PostSave = "elin.game.post_save";

	public const string PreLoad = "elin.game.pre_load";

	public const string PostLoad = "elin.game.post_load";

	public const string NewGame = "elin.game.start_new";

	public const string PreSceneInit = "elin.scene.pre_init";

	public const string PostSceneInit = "elin.scene.post_init";

	public const string FeatApply = "elin.feat.apply";

	public const string DramaParseAction = "elin.drama.parse_action";

	public const string CharaCreated = "elin.chara_created";

	public const string ThingCreated = "elin.thing_created";
}
```

:::
## FEAT

[`public List<string> Apply(int a, ElementContainer owner, bool hint = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/FEAT.cs#L399-L404)
```cs:line-numbers=399
	{
		hints.Clear();
	}
	EVENT.ElinFeatApplyEventArgs elinFeatApplyEventArgs = new EVENT.ElinFeatApplyEventArgs // [!code ++]
	{ // [!code ++]
		feat = this, // [!code ++]
		owner = owner, // [!code ++]
		hint = hint // [!code ++]
	}; // [!code ++]
	elinFeatApplyEventArgs.SetData(a); // [!code ++]
	BaseModManager.PublishEvent("elin.feat.apply", elinFeatApplyEventArgs); // [!code ++]
	a = elinFeatApplyEventArgs.data; // [!code ++]
	int value = base.Value;
	int A = Mathf.Abs(a);
	int invert = ((a >= 0) ? 1 : (-1));
```

## Game

[`public static void Load(string id, bool cloud)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Game.cs#L328-L333)
```cs:line-numbers=328
		EClass.game.Kill();
	}
	OnBeforeInstantiate();
	GameIOContext data = new GameIOContext(text); // [!code ++]
	BaseModManager.PublishEvent("elin.game.pre_load", data); // [!code ++]
	EClass.core.game = GameIO.LoadGame(id, text, cloud);
	EClass.game.isCloud = cloud;
	EClass.game.isLoading = true;
```

[`public static void Load(string id, bool cloud)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Game.cs#L355-L360)
```cs:line-numbers=355
			WidgetSideScreen.Instance.OnChangeResolution();
		}
		EClass.game.isLoading = false;
		BaseModManager.PublishEvent("elin.game.post_load", data); // [!code ++]
	}

	public void OnLoad()
```

[`public void StartNewGame()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Game.cs#L953-L958)
```cs:line-numbers=953
		AddAdventurers();
		player.OnStartNewGame();
		EClass.scene.Init(Scene.Mode.StartGame);
		GameIOContext data = new GameIOContext((isCloud ? CorePath.RootSaveCloud : CorePath.RootSave) + id); // [!code ++]
		BaseModManager.PublishEvent("elin.game.start_new", data); // [!code ++]
	}

	public void AddAdventurers()
```

[`public bool Save(bool isAutoSave = false, bool silent = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Game.cs#L1061-L1068)
```cs:line-numbers=1061
	GameIndex gameIndex = null;
	try
	{
		GameIOContext data = new GameIOContext(GameIO.pathCurrentSave); // [!code ++]
		BaseModManager.PublishEvent("elin.game.pre_save", data); // [!code ++]
		OnBeforeSave();
		gameIndex = GameIO.SaveGame();
		BaseModManager.PublishEvent("elin.game.post_save", data); // [!code ++]
	}
	catch (Exception ex)
	{
```

## GrowSystem

[`public void PopHarvest(Chara c, Thing t = null, int num = -1)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/GrowSystem.cs#L652-L662)
```cs:line-numbers=652
	}
	ApplySeed(t);
	PlantData plantData = EClass._map.TryGetPlant(cell);
	bool flag = false; // [!code ++]
	if (plantData != null && plantData.size > 0)
	{
		t.c_weight = t.SelfWeight * (80 + plantData.size * plantData.size * 100) / 100;
		t.SetBool(115, enable: true);
		t.isWeightChanged = true;
		flag = true; // [!code ++]
	}
	t.SetBlessedState(BlessedState.Normal);
	if (source._growth.Length > 4)
```

[`public void PopHarvest(Chara c, Thing t = null, int num = -1)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/GrowSystem.cs#L687-L692)
```cs:line-numbers=687
			t.SetNum(num);
		}
		TryPick(cell, t, c);
		if (flag) // [!code ++]
		{ // [!code ++]
			c.ModExpParty(207, Mathf.Min(t.c_weight / 100, 500)); // [!code ++]
		} // [!code ++]
	}

	public void ApplySeed(Thing t)
```

## Hobby

[`public class Hobby : EClass`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Hobby.cs#L5-L11)
```cs:line-numbers=5
{
	public int id;

	public SourceHobby.Row source => EClass.sources.hobbies.map[id]; // [!code --]
	public SourceHobby.Row source => EClass.sources.hobbies.map.TryGetValue(id) ?? EClass.sources.hobbies.alias["Walk"]; // [!code ++]

	public string Name => source.GetName();

```

## ID_Achievement

[`public enum ID_Achievement`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/ID_Achievement.cs#L16-L20)
```cs:line-numbers=16
	LARNNEIRE2,
	CHURYU2,
	NINO,
	CHURYU3 // [!code --]
	CHURYU3, // [!code ++]
	DEMITAS, // [!code ++]
	KETTLE // [!code ++]
}
```

## LayerConsole

[`public override void OnAfterInit()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/LayerConsole.cs#L11-L17)
```cs:line-numbers=11

	public override void OnUpdateInput()
	{
		if (Input.GetKeyDown(KeyCode.Escape)) // [!code --]
		if (Input.GetKeyDown(KeyCode.Escape) || EInput.action == EAction.Console) // [!code ++]
		{
			Close();
		}
```

## Map

[`public void SetFloor(int x, int z, int idMat, int idFloor, int dir)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Map.cs#L969-L981)
```cs:line-numbers=969
		RefreshNeighborTiles(x, z);
	}

	public void SetBridge(int x, int z, int height = 0, int idMat = 0, int idBridge = 0, int dir = 0) // [!code --]
	public void SetBridge(int x, int z, int height = 0, int idMat = 0, int idBridge = 0, int dir = 0, byte idPillar = 0) // [!code ++]
	{
		Cell cell = cells[x, z];
		cell.bridgeHeight = (byte)height;
		cell._bridgeMat = (byte)idMat;
		cell._bridge = (byte)idBridge;
		cell.bridgePillar = 0; // [!code --]
		cell.bridgePillar = idPillar; // [!code ++]
		cell.floorDir = dir;
		if (cell.room != null)
		{
```

[`public void MineFloor(Point point, Chara c = null, bool recoverBlock = false, bo`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Map.cs#L1764-L1770)
```cs:line-numbers=1764
	if (point.cell.HasBridge && removePlatform)
	{
		DropBlockComponent(EClass.pc.pos, point.sourceBridge, point.matBridge, recoverBlock, isPlatform: true, c);
		EClass._map.SetBridge(point.x, point.z); // [!code --]
		EClass._map.SetBridge(point.x, point.z, 0, 0, 0, 0, 0); // [!code ++]
		if (point.IsSky)
		{
			EClass.pc.Kick(point, ignoreSelf: true);
```

## ModManager

[`using System.Collections;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/ModManager.cs#L2-L42)
```cs:line-numbers=2
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Linq; // [!code ++]
using HeathenEngineering.SteamworksIntegration;
using HeathenEngineering.SteamworksIntegration.API;
using IniParser.Model;
using NPOI.SS.UserModel; // [!code --]
using NPOI.XSSF.UserModel; // [!code --]
using Newtonsoft.Json; // [!code ++]
using Steamworks;
using UnityEngine;

[Serializable]
public class ModManager : BaseModManager // [!code --]
public class ModManager : ModManagerCore // [!code ++]
{
	public struct SheetIndex // [!code --]
	{ // [!code --]
		public int dest; // [!code --]
 // [!code --]
		public int old; // [!code --]
	} // [!code --]
 // [!code --]
	public static List<object> ListPluginObject = new List<object>();

	public static bool disableMod = false; // [!code --]
	public static bool disableMod; // [!code ++]
 // [!code ++]
	private LoadingScreen _loading; // [!code ++]

	public List<FileInfo> replaceFiles = new List<FileInfo>();

	private Action ImportModGodTalks; // [!code ++]
 // [!code ++]
	public static List<string> ListChainLoad => BaseModManager.listChainLoad;

	public static DirectoryInfo DirWorkshop => BaseModManager.Instance.dirWorkshop; // [!code --]
	public static DirectoryInfo DirWorkshop => Instance.dirWorkshop; // [!code ++]
 // [!code ++]
	public new static ModManager Instance => (ModManager)BaseModManager.Instance; // [!code ++]

	public static bool IsInitialized => BaseModManager.isInitialized;

	public override void Init(string path, string defaultPackage = "_Elona")
	{
		base.Init(path, defaultPackage);
		IniData elinIni = Core.GetElinIni(); // [!code --]
		Debug.Log("IsOffline:" + BaseCore.IsOffline);
		IniData elinIni = Core.GetElinIni(); // [!code ++]
		if (elinIni != null)
		{
			if (BaseCore.IsOffline)
```

[`public override void Init(string path, string defaultPackage = "_Elona")`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/ModManager.cs#L49-L266)
```cs:line-numbers=49
			}
			else
			{
				DirectoryInfo parent = new DirectoryInfo(App.Client.GetAppInstallDirectory(SteamSettings.behaviour.settings.applicationId)).Parent.Parent; // [!code --]
				dirWorkshop = new DirectoryInfo(parent.FullName + "/workshop/content/2135150"); // [!code --]
				elinIni.Global["path_workshop"] = dirWorkshop.FullName ?? ""; // [!code --]
				string path2 = Path.Combine(App.Client.GetAppInstallDirectory(SteamSettings.behaviour.settings.applicationId), "../../workshop/content/2135150"); // [!code ++]
				dirWorkshop = new DirectoryInfo(path2); // [!code ++]
				elinIni.Global["path_workshop"] = dirWorkshop.FullName; // [!code ++]
				Core.SaveElinIni(elinIni);
			}
		}
		if (dirWorkshop != null && !dirWorkshop.Exists) // [!code --]
		if (!dirWorkshop.Exists) // [!code ++]
		{
			dirWorkshop = null;
		}
		Debug.Log("dirWorkshop:" + dirWorkshop); // [!code --]
		Debug.Log("Mod Init:" + BaseModManager.rootDefaultPacakge); // [!code --]
		packages.Clear(); // [!code --]
		DirectoryInfo[] directories = new DirectoryInfo(BaseModManager.rootMod).GetDirectories(); // [!code --]
		Array.Reverse(directories); // [!code --]
		DirectoryInfo[] array = directories; // [!code --]
		foreach (DirectoryInfo directoryInfo in array) // [!code --]
		Debug.Log("Workshop:" + dirWorkshop); // [!code ++]
		Debug.Log("Packages:" + BaseModManager.rootMod); // [!code ++]
		Debug.Log("Core Mod:" + BaseModManager.rootDefaultPacakge); // [!code ++]
		BaseModManager.SubscribeEvent("elin.source.lang_set", delegate(object lang) // [!code ++]
		{
			if (!EClass.debug.skipMod || !Application.isEditor || !(directoryInfo.Name != "_Elona")) // [!code --]
			if (ModManagerCore.useLocalizations) // [!code ++]
			{
				AddPackage(directoryInfo, isInPackages: true); // [!code --]
				ImportSourceLocalizations(lang as string); // [!code ++]
			}
		} // [!code --]
		DirectoryInfo[] directories2 = new DirectoryInfo(CorePath.custom).GetDirectories(); // [!code --]
		ModPackage package = new ModPackage(); // [!code --]
		array = directories2; // [!code --]
		foreach (DirectoryInfo dir in array) // [!code --]
			ImportModDialogs(); // [!code ++]
		}); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void SaveLoadOrder() // [!code ++]
	{ // [!code ++]
		if (!disableMod) // [!code ++]
		{
			ParseExtra(dir, package); // [!code --]
			List<string> contents = (from p in packages // [!code ++]
				where !p.builtin && p.dirInfo.Exists // [!code ++]
				select $"{p.dirInfo.FullName},{(p.willActivate ? 1 : 0)}").ToList(); // [!code ++]
			File.WriteAllLines(CorePath.rootExe + "loadorder.txt", contents); // [!code ++]
		}
	}

	private void HandleResults(UgcQuery query) // [!code --]
	public void LoadLoadOrder() // [!code ++]
	{
		foreach (WorkshopItem results in query.ResultsList) // [!code --]
		string path = CorePath.rootExe + "loadorder.txt"; // [!code ++]
		if (!File.Exists(path)) // [!code ++]
		{ // [!code ++]
			return; // [!code ++]
		} // [!code ++]
		Dictionary<string, BaseModPackage> dictionary = new Dictionary<string, BaseModPackage>(); // [!code ++]
		foreach (BaseModPackage package in packages) // [!code ++]
		{
			if (results.IsSubscribed) // [!code --]
			if (!package.builtin) // [!code ++]
			{
				AddWorkshopPackage(results); // [!code --]
				dictionary[package.dirInfo.FullName] = package; // [!code ++]
			}
		}
		int num = 0; // [!code ++]
		string[] array = File.ReadAllLines(path); // [!code ++]
		for (int i = 0; i < array.Length; i++) // [!code ++]
		{ // [!code ++]
			string[] array2 = array[i].Split(','); // [!code ++]
			if (dictionary.TryGetValue(array2[0], out var value)) // [!code ++]
			{ // [!code ++]
				value.loadPriority = num; // [!code ++]
				value.willActivate = array2[1] == "1"; // [!code ++]
			} // [!code ++]
			num++; // [!code ++]
		} // [!code ++]
	}

	public IEnumerator RefreshMods(Action onComplete, bool syncMods) // [!code --]
	public void ImportSourceLocalizations(string lang) // [!code ++]
	{
		bool sync = !BaseCore.IsOffline && syncMods && UserGeneratedContent.Client.GetNumSubscribedItems() != 0; // [!code --]
		LoadingScreen loading = (sync ? Util.Instantiate<LoadingScreen>("LoadingScreen") : null); // [!code --]
		if (!disableMod && (!EClass.debug.skipMod || !Application.isEditor)) // [!code --]
		string text = lang + "/SourceLocalization.json"; // [!code ++]
		(FileInfo file, EMod package)[] filesEx = PackageIterator.GetFilesEx(text); // [!code ++]
		SortedDictionary<string, string> sortedDictionary = new SortedDictionary<string, string>(); // [!code ++]
		Dictionary<string, SortedDictionary<string, string>> dictionary = new Dictionary<string, SortedDictionary<string, string>>(); // [!code ++]
		(FileInfo, EMod)[] array = filesEx; // [!code ++]
		string key; // [!code ++]
		string value; // [!code ++]
		for (int i = 0; i < array.Length; i++) // [!code ++]
		{
			if (sync) // [!code --]
			var (fileInfo, eMod) = array[i]; // [!code ++]
			try // [!code ++]
			{
				UgcQuery activeQuery = UgcQuery.GetSubscribed(withLongDescription: false, withMetadata: false, withKeyValueTags: false, withAdditionalPreviews: false, 0u); // [!code --]
				activeQuery.Execute(HandleResults); // [!code --]
				loading?.Log("Fetching subscribed Mods...(Hit ESC to cancel)"); // [!code --]
				while (activeQuery.handle != UGCQueryHandle_t.Invalid && !UnityEngine.Input.GetKey(KeyCode.Escape)) // [!code --]
				SortedDictionary<string, string> sortedDictionary2 = IO.LoadFile<SortedDictionary<string, string>>(fileInfo.FullName); // [!code ++]
				foreach (KeyValuePair<string, string> item in sortedDictionary2) // [!code ++]
				{
					yield return new WaitForEndOfFrame(); // [!code --]
					item.Deconstruct(out key, out value); // [!code ++]
					string key2 = key; // [!code ++]
					string value2 = value; // [!code ++]
					sortedDictionary[key2] = value2; // [!code ++]
				}
				dictionary[eMod.id] = sortedDictionary2; // [!code ++]
			}
			else // [!code --]
			catch (Exception arg) // [!code ++]
			{
				loading?.Log("Fetching offline Mods."); // [!code --]
				if (dirWorkshop != null) // [!code --]
				{ // [!code --]
					DirectoryInfo[] directories = dirWorkshop.GetDirectories(); // [!code --]
					foreach (DirectoryInfo dir in directories) // [!code --]
					{ // [!code --]
						AddPackage(dir); // [!code --]
					} // [!code --]
				} // [!code --]
				Debug.LogError($"#source localization failed to load {fileInfo.ShortPath()}\n{arg}"); // [!code ++]
			}
		}
		if (sync) // [!code --]
		JsonSerializerSettings setting = new JsonSerializerSettings // [!code ++]
		{ // [!code ++]
			PreserveReferencesHandling = PreserveReferencesHandling.None, // [!code ++]
			NullValueHandling = NullValueHandling.Ignore // [!code ++]
		}; // [!code ++]
		foreach (EMod value4 in base.MappedPackages.Values) // [!code ++]
		{
			bool valid = false; // [!code --]
			while (!valid) // [!code --]
			if (!(value4 is ModPackage modPackage) || value4.builtin || !value4.activated) // [!code ++]
			{
				valid = true; // [!code --]
				foreach (BaseModPackage package in packages) // [!code --]
				{ // [!code --]
					WorkshopItem workshopItem = package.item as WorkshopItem; // [!code --]
					if (!package.installed && workshopItem != null && !workshopItem.IsBanned) // [!code --]
					{ // [!code --]
						valid = false; // [!code --]
						string text = "Downloading " + workshopItem.Title + ": "; // [!code --]
						if (!package.progressText) // [!code --]
						{ // [!code --]
							package.progressText = loading.Log(text); // [!code --]
						} // [!code --]
						if (package.downloadStarted && workshopItem.DownloadCompletion >= 1f) // [!code --]
						{ // [!code --]
							package.progressText.text = text + "Done!"; // [!code --]
							package.installed = true; // [!code --]
						} // [!code --]
						else if (workshopItem.IsDownloading || workshopItem.IsDownloadPending) // [!code --]
						{ // [!code --]
							package.progressText.text = text + (int)(workshopItem.DownloadCompletion * 100f) + "%"; // [!code --]
						} // [!code --]
						else if (!package.downloadStarted) // [!code --]
						{ // [!code --]
							package.downloadStarted = true; // [!code --]
							workshopItem.DownloadItem(highPriority: true); // [!code --]
							Debug.Log("start downloading:" + workshopItem.Title + "/" + workshopItem.IsInstalled + "/" + package.installed + "/" + workshopItem.IsDownloading + "/" + workshopItem.IsDownloadPending + "/" + workshopItem.DownloadCompletion); // [!code --]
						} // [!code --]
					} // [!code --]
				} // [!code --]
				if (!valid && UnityEngine.Input.GetKey(KeyCode.Escape)) // [!code --]
				{ // [!code --]
					break; // [!code --]
				} // [!code --]
				yield return new WaitForEndOfFrame(); // [!code --]
				continue; // [!code ++]
			}
		} // [!code --]
		foreach (BaseModPackage package2 in packages) // [!code --]
		{ // [!code --]
			package2.Init(); // [!code --]
			Debug.Log(package2.title + "/" + package2.id + "/" + package2.installed + "/" + package2.dirInfo.FullName); // [!code --]
		} // [!code --]
		LoadLoadOrder(); // [!code --]
		packages.Sort((BaseModPackage a, BaseModPackage b) => a.loadPriority - b.loadPriority); // [!code --]
		foreach (BaseModPackage package3 in packages) // [!code --]
		{ // [!code --]
			if (package3.isInPackages || !package3.willActivate) // [!code --]
			HashSet<SourceData.BaseRow> sourceRows = value4.sourceRows; // [!code ++]
			if (sourceRows == null || sourceRows.Count <= 0) // [!code ++]
			{ // [!code ++]
				continue; // [!code ++]
			} // [!code ++]
			modPackage.ImportSourceLocalizations(sortedDictionary); // [!code ++]
			if (!ModManagerCore.generateLocalizations || (!modPackage.isInPackages && !Application.isEditor)) // [!code ++]
			{
				continue;
			}
			foreach (BaseModPackage package4 in packages) // [!code --]
			if (Lang.IsBuiltin(modPackage.Mapping.SourceLangMod) && Lang.IsBuiltin(lang)) // [!code ++]
			{ // [!code ++]
				break; // [!code ++]
			} // [!code ++]
			try // [!code ++]
			{
				if (package4.isInPackages && package3.id == package4.id) // [!code --]
				SortedDictionary<string, string> sortedDictionary3 = dictionary.GetValueOrDefault(modPackage.id) ?? new SortedDictionary<string, string>(); // [!code ++]
				SortedDictionary<string, string> sortedDictionary4 = modPackage.ExportSourceLocalizations(); // [!code ++]
				SortedDictionary<string, string> final = new SortedDictionary<string, string>(); // [!code ++]
				foreach (KeyValuePair<string, string> item2 in sortedDictionary4) // [!code ++]
				{
					package4.hasPublishedPackage = true; // [!code --]
					item2.Deconstruct(out value, out key); // [!code ++]
					string key3 = value; // [!code ++]
					string defaultValue = key; // [!code ++]
					final[key3] = sortedDictionary3.GetValueOrDefault(key3, defaultValue); // [!code ++]
				} // [!code ++]
				if (sortedDictionary3.Count != final.Count || sortedDictionary3.Any((KeyValuePair<string, string> kv) => !final.TryGetValue(kv.Key, out var value3) || value3 != kv.Value)) // [!code ++]
				{ // [!code ++]
					IO.SaveFile(Path.Combine(modPackage.dirInfo.FullName, "LangMod", text), final, compress: false, setting); // [!code ++]
					Debug.Log($"#source localization updated {text} / {modPackage}"); // [!code ++]
				}
			}
			catch (Exception arg2) // [!code ++]
			{ // [!code ++]
				Debug.LogError($"#source localization failed to generate {modPackage}\n{arg2}"); // [!code ++]
			} // [!code ++]
		}
		loading?.Log("Total number of Mods:" + packages.Count); // [!code --]
		if ((bool)loading) // [!code --]
		{ // [!code --]
			loading.Log("Activating Mods..."); // [!code --]
			yield return new WaitForEndOfFrame(); // [!code --]
			yield return new WaitForEndOfFrame(); // [!code --]
		} // [!code --]
		BaseModManager.listChainLoad.Clear(); // [!code --]
		ListPluginObject.Clear(); // [!code --]
		foreach (BaseModPackage package5 in packages) // [!code --]
	} // [!code ++]
 // [!code ++]
	public void ImportAllModSourceSheets() // [!code ++]
	{ // [!code ++]
		try // [!code ++]
		{
			if (package5.IsValidVersion() && (!disableMod || package5.builtin)) // [!code --]
			SourceImporter.HotInit(new SourceData[2] // [!code ++]
			{ // [!code ++]
				EClass.sources.elements, // [!code ++]
				EClass.sources.materials // [!code ++]
			}); // [!code ++]
			List<string> list = new List<string>(); // [!code ++]
			foreach (EMod value in base.MappedPackages.Values) // [!code ++]
			{
				package5.Activate(); // [!code --]
				if (package5.activated) // [!code --]
				if (value.builtin || !value.activated) // [!code ++]
				{ // [!code ++]
					continue; // [!code ++]
				} // [!code ++]
				foreach (FileInfo sourceSheet in value.Mapping.SourceSheets) // [!code ++]
				{
					BaseModManager.listChainLoad.Add(package5.dirInfo.FullName); // [!code --]
					if (!sourceSheet.Name.StartsWith(".") && !sourceSheet.Name.Contains("~")) // [!code ++]
					{ // [!code ++]
						ModUtil.sourceImporter.fileProviders[sourceSheet.FullName] = value; // [!code ++]
						list.Add(sourceSheet.FullName); // [!code ++]
					} // [!code ++]
				}
			}
			ModUtil.sourceImporter.ImportFilesCached(list); // [!code ++]
		}
		ModUtil.OnModsActivated(); // [!code --]
		ModUtil.LoadTypeFallback(); // [!code --]
		BaseModManager.isInitialized = true; // [!code --]
		yield return new WaitForEndOfFrame(); // [!code --]
		onComplete?.Invoke(); // [!code --]
		if ((bool)loading) // [!code --]
		catch (Exception message) // [!code ++]
		{
			UnityEngine.Object.Destroy(loading.gameObject); // [!code --]
			Debug.LogError(message); // [!code ++]
		}
		yield return null; // [!code --]
		Debug.Log("#source finished importing workbooks"); // [!code ++]
	}

	public void SaveLoadOrder() // [!code --]
	public void ImportModDialogs() // [!code ++]
	{ // [!code ++]
		Lang.extraExcelDialogs = new HashSet<string>(PathComparer.Default); // [!code ++]
		Lang.excelDialog = null; // [!code ++]
		FileInfo[] files = PackageIterator.GetFiles("Dialog/dialog.xlsx"); // [!code ++]
		foreach (FileInfo fileInfo in files) // [!code ++]
		{ // [!code ++]
			Lang.extraExcelDialogs.Add(fileInfo.FullName); // [!code ++]
			Debug.Log("#dialog loaded " + fileInfo.ShortPath()); // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void ImportAllModGodTalks() // [!code ++]
	{
		if (disableMod) // [!code --]
		if (Lang.setting?.dir == null) // [!code ++]
		{
			return;
		}
		List<string> list = new List<string>(); // [!code --]
		foreach (BaseModPackage package in packages) // [!code --]
		Dictionary<string, Dictionary<string, string>> map = EClass.sources.dataGodTalk.sheets["_default"].map; // [!code ++]
		foreach (ExcelData item in from f in PackageIterator.GetFiles("Data/god_talk.xlsx") // [!code ++]
			select new ExcelData(f.FullName, 3)) // [!code ++]
		{
			if (!package.builtin && Directory.Exists(package.dirInfo.FullName)) // [!code --]
			foreach (KeyValuePair<string, Dictionary<string, string>> item2 in map) // [!code ++]
			{
				string item = package.dirInfo.FullName + "," + (package.willActivate ? 1 : 0); // [!code --]
				list.Add(item); // [!code --]
				item2.Deconstruct(out var key, out var _); // [!code ++]
				string text = key; // [!code ++]
				if (text.IsEmpty()) // [!code ++]
				{ // [!code ++]
					continue; // [!code ++]
				} // [!code ++]
				Dictionary<string, string> valueOrDefault = item.sheets["_default"].map.GetValueOrDefault(text); // [!code ++]
				if (valueOrDefault == null) // [!code ++]
				{ // [!code ++]
					continue; // [!code ++]
				} // [!code ++]
				foreach (KeyValuePair<string, string> item3 in valueOrDefault) // [!code ++]
				{ // [!code ++]
					item3.Deconstruct(out key, out var value2); // [!code ++]
					string text2 = key; // [!code ++]
					string value3 = value2; // [!code ++]
					if (text2 != "id") // [!code ++]
					{ // [!code ++]
						map[text][text2] = value3; // [!code ++]
					} // [!code ++]
				} // [!code ++]
			}
			Debug.Log("#dialog loaded " + item.path.ShortPath()); // [!code ++]
		}
		File.WriteAllLines(CorePath.rootExe + "loadorder.txt", list); // [!code --]
	}

	public void LoadLoadOrder() // [!code --]
	public IEnumerator RefreshMods(Action onComplete, bool syncMods) // [!code ++]
	{
		string path = CorePath.rootExe + "loadorder.txt"; // [!code --]
		if (!File.Exists(path)) // [!code --]
		{ // [!code --]
			return; // [!code --]
		} // [!code --]
		Dictionary<string, BaseModPackage> dictionary = new Dictionary<string, BaseModPackage>(); // [!code --]
		foreach (BaseModPackage package in packages) // [!code --]
		bool flag = !BaseCore.IsOffline && syncMods && UserGeneratedContent.Client.GetNumSubscribedItems() != 0; // [!code ++]
		_loading = Util.Instantiate<LoadingScreen>("LoadingScreen"); // [!code ++]
		WaitForEndOfFrame awaiter = new WaitForEndOfFrame(); // [!code ++]
		packages.Clear(); // [!code ++]
		disableMod |= Application.isEditor && EClass.debug.skipMod; // [!code ++]
		LoadLocalPackages(); // [!code ++]
		LoadCustomPackage(); // [!code ++]
		if (!disableMod) // [!code ++]
		{
			if (!package.builtin) // [!code --]
			_loading.Log("Loading workshop contents..."); // [!code ++]
			if (flag) // [!code ++]
			{
				dictionary[package.dirInfo.FullName] = package; // [!code --]
				yield return LoadWorkshopPackages(); // [!code ++]
			} // [!code ++]
			else if (dirWorkshop != null) // [!code ++]
			{ // [!code ++]
				DirectoryInfo[] directories = dirWorkshop.GetDirectories(); // [!code ++]
				foreach (DirectoryInfo dir in directories) // [!code ++]
				{ // [!code ++]
					AddPackage(dir); // [!code ++]
				} // [!code ++]
			}
		}
		int num = 0; // [!code --]
		string[] array = File.ReadAllLines(path); // [!code --]
		for (int i = 0; i < array.Length; i++) // [!code --]
		InitPackagesMeta(); // [!code ++]
		LoadLoadOrder(); // [!code ++]
		packages.Sort((BaseModPackage a, BaseModPackage b) => a.loadPriority - b.loadPriority); // [!code ++]
		foreach (BaseModPackage item in packages.Where((BaseModPackage p) => !p.isInPackages && p.willActivate)) // [!code ++]
		{
			string[] array2 = array[i].Split(','); // [!code --]
			if (dictionary.ContainsKey(array2[0])) // [!code --]
			if (mappedPackages.TryGetValue(item.id, out var value) && value.isInPackages) // [!code ++]
			{
				BaseModPackage baseModPackage = dictionary[array2[0]]; // [!code --]
				baseModPackage.loadPriority = num; // [!code --]
				baseModPackage.willActivate = array2[1] == "1"; // [!code --]
				value.hasPublishedPackage = true; // [!code ++]
				mappedPackages[item.id] = item as ModPackage; // [!code ++]
			}
			num++; // [!code --]
		}
		packages.RemoveAll((BaseModPackage p) => p.hasPublishedPackage); // [!code ++]
		_loading.Log($"Total number of mods:{packages.Count}"); // [!code ++]
		_loading.Log("Activating Mods..."); // [!code ++]
		yield return awaiter; // [!code ++]
		ActivatePackages(); // [!code ++]
		BaseModManager.isInitialized = true; // [!code ++]
		yield return awaiter; // [!code ++]
		onComplete?.Invoke(); // [!code ++]
		if ((bool)_loading) // [!code ++]
		{ // [!code ++]
			UnityEngine.Object.Destroy(_loading.gameObject); // [!code ++]
		} // [!code ++]
		yield return null; // [!code ++]
	}

	public ModPackage AddPackage(DirectoryInfo dir, bool isInPackages = false)
```

[`public ModPackage AddPackage(DirectoryInfo dir, bool isInPackages = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/ModManager.cs#L268-L686)
```cs:line-numbers=268
		ModPackage modPackage = new ModPackage
		{
			dirInfo = dir,
			installed = true // [!code --]
			installed = true, // [!code ++]
			isInPackages = isInPackages, // [!code ++]
			loadPriority = priorityIndex, // [!code ++]
			Mapping = new FileMapping(dir) // [!code ++]
		};
		packages.Add(modPackage);
		modPackage.isInPackages = isInPackages; // [!code --]
		modPackage.loadPriority = priorityIndex; // [!code --]
		priorityIndex++;
		return modPackage;
	}

	public ModPackage AddWorkshopPackage(WorkshopItem item, bool isInPackages = false)
	{
		UserGeneratedContent.Client.GetItemInstallInfo(item.FileId, out var _, out var folderPath, out var _); // [!code --]
		DirectoryInfo directoryInfo = new DirectoryInfo(folderPath); // [!code --]
		ModPackage modPackage = new ModPackage // [!code --]
		{ // [!code --]
			item = item, // [!code --]
			dirInfo = directoryInfo, // [!code --]
			installed = directoryInfo.Exists, // [!code --]
			banned = item.IsBanned // [!code --]
		}; // [!code --]
		packages.Add(modPackage); // [!code --]
		modPackage.isInPackages = isInPackages; // [!code --]
		modPackage.loadPriority = priorityIndex; // [!code --]
		priorityIndex++; // [!code --]
		ulong sizeOnDisk; // [!code ++]
		string folderPath; // [!code ++]
		DateTime timeStamp; // [!code ++]
		bool itemInstallInfo = UserGeneratedContent.Client.GetItemInstallInfo(item.FileId, out sizeOnDisk, out folderPath, out timeStamp); // [!code ++]
		DirectoryInfo dir = new DirectoryInfo(folderPath); // [!code ++]
		ModPackage modPackage = AddPackage(dir, isInPackages); // [!code ++]
		modPackage.installed = itemInstallInfo; // [!code ++]
		modPackage.banned = item.IsBanned; // [!code ++]
		return modPackage;
	}

	public int CountUserMod()
	{
		int num = 0; // [!code --]
		foreach (BaseModPackage package in packages) // [!code --]
		return packages.Count((BaseModPackage p) => !p.builtin); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void LoadLocalPackages() // [!code ++]
	{ // [!code ++]
		_loading.Log("Loading local Package..."); // [!code ++]
		DirectoryInfo[] directories = new DirectoryInfo(BaseModManager.rootMod).GetDirectories(); // [!code ++]
		Array.Reverse(directories); // [!code ++]
		DirectoryInfo[] array = directories; // [!code ++]
		foreach (DirectoryInfo directoryInfo in array) // [!code ++]
		{
			if (!package.builtin) // [!code --]
			if (!disableMod || !(directoryInfo.Name != "_Elona") || !(directoryInfo.Name != "_Lang_Chinese")) // [!code ++]
			{
				num++; // [!code --]
				AddPackage(directoryInfo, isInPackages: true); // [!code ++]
			}
		}
		return num; // [!code --]
	}

	public override void ParseExtra(DirectoryInfo dir, BaseModPackage package) // [!code --]
	public void LoadCustomPackage() // [!code ++]
	{
		switch (dir.Name) // [!code --]
		{ // [!code --]
		case "TalkText": // [!code --]
		_loading.Log("Loading user Custom..."); // [!code ++]
		DirectoryInfo[] directories = new DirectoryInfo(CorePath.custom).GetDirectories(); // [!code ++]
		ModPackage package = new ModPackage(); // [!code ++]
		DirectoryInfo[] array = directories; // [!code ++]
		foreach (DirectoryInfo dir in array) // [!code ++]
		{
			FileInfo[] files = dir.GetFiles(); // [!code --]
			foreach (FileInfo fileInfo in files) // [!code --]
			{ // [!code --]
				if (fileInfo.Name.EndsWith(".xlsx")) // [!code --]
				{ // [!code --]
					TalkText.modList.Add(new ExcelData(fileInfo.FullName)); // [!code --]
				} // [!code --]
			} // [!code --]
			break; // [!code --]
			ParseExtra(dir, package); // [!code ++]
		}
		case "Map": // [!code --]
	} // [!code ++]
 // [!code ++]
	public IEnumerator LoadWorkshopPackages() // [!code ++]
	{ // [!code ++]
		WaitForEndOfFrame awaiter = new WaitForEndOfFrame(); // [!code ++]
		UgcQuery activeQuery = UgcQuery.GetSubscribed(withLongDescription: false, withMetadata: false, withKeyValueTags: false, withAdditionalPreviews: false, 0u); // [!code ++]
		activeQuery.Execute(HandleWorkshopQuery); // [!code ++]
		_loading.Log("Fetching subscriptions...(Hit ESC to cancel)"); // [!code ++]
		while (activeQuery.handle != UGCQueryHandle_t.Invalid && !UnityEngine.Input.GetKey(KeyCode.Escape)) // [!code ++]
		{
			if (package.builtin) // [!code --]
			{ // [!code --]
				break; // [!code --]
			} // [!code --]
			FileInfo[] files = dir.GetFiles(); // [!code --]
			foreach (FileInfo fileInfo5 in files) // [!code --]
			{ // [!code --]
				if (fileInfo5.Name.EndsWith(".z")) // [!code --]
				{ // [!code --]
					MOD.listMaps.Add(fileInfo5); // [!code --]
				} // [!code --]
			} // [!code --]
			break; // [!code --]
			yield return awaiter; // [!code ++]
		}
		case "Map Piece": // [!code --]
		yield return UpdateWorkshopPackages(); // [!code ++]
		void HandleWorkshopQuery(UgcQuery query) // [!code ++]
		{
			if (package.builtin) // [!code --]
			foreach (WorkshopItem results in query.ResultsList) // [!code ++]
			{
				break; // [!code --]
			} // [!code --]
			FileInfo[] files = dir.GetFiles(); // [!code --]
			foreach (FileInfo fileInfo3 in files) // [!code --]
			{ // [!code --]
				if (fileInfo3.Name.EndsWith(".mp")) // [!code --]
				{ // [!code --]
					MOD.listPartialMaps.Add(fileInfo3); // [!code --]
				} // [!code --]
				AddWorkshopPackage(results); // [!code ++]
			}
			break; // [!code --]
		}
		case "Texture Replace": // [!code --]
	} // [!code ++]
 // [!code ++]
	private IEnumerator UpdateWorkshopPackages() // [!code ++]
	{ // [!code ++]
		_loading?.Log("Updating subscriptions..."); // [!code ++]
		WaitForEndOfFrame awaiter = new WaitForEndOfFrame(); // [!code ++]
		while (true) // [!code ++]
		{
			FileInfo[] files = dir.GetFiles(); // [!code --]
			foreach (FileInfo fileInfo6 in files) // [!code --]
			bool flag = false; // [!code ++]
			foreach (BaseModPackage item in packages.Where((BaseModPackage p) => !p.installed)) // [!code ++]
			{
				if (fileInfo6.Name.EndsWith(".png")) // [!code --]
				if (!(item.item is WorkshopItem { IsBanned: false } workshopItem)) // [!code ++]
				{
					replaceFiles.Add(fileInfo6); // [!code --]
					continue; // [!code ++]
				}
			} // [!code --]
			break; // [!code --]
		} // [!code --]
		case "Texture": // [!code --]
		{ // [!code --]
			FileInfo[] files = dir.GetFiles(); // [!code --]
			foreach (FileInfo fileInfo4 in files) // [!code --]
			{ // [!code --]
				if (fileInfo4.Name.EndsWith(".png")) // [!code --]
				flag = true; // [!code ++]
				string text = "Downloading " + workshopItem.Title + ": "; // [!code ++]
				BaseModPackage baseModPackage = item; // [!code ++]
				if ((object)baseModPackage.progressText == null) // [!code ++]
				{
					SpriteReplacer.dictModItems[fileInfo4.Name.Replace(".png", "")] = fileInfo4.GetFullFileNameWithoutExtension(); // [!code --]
					baseModPackage.progressText = _loading?.Log(text); // [!code ++]
				}
			} // [!code --]
			break; // [!code --]
		} // [!code --]
		case "Portrait": // [!code --]
		{ // [!code --]
			FileInfo[] files = dir.GetFiles(); // [!code --]
			foreach (FileInfo fileInfo2 in files) // [!code --]
			{ // [!code --]
				if (fileInfo2.Name.EndsWith(".png")) // [!code --]
				if (item.downloadStarted && workshopItem.DownloadCompletion >= 1f) // [!code ++]
				{
					if (fileInfo2.Name.StartsWith("BG_")) // [!code --]
					{ // [!code --]
						Portrait.modPortraitBGs.Add(fileInfo2); // [!code --]
					} // [!code --]
					else if (fileInfo2.Name.StartsWith("BGF_")) // [!code --]
					item.installed = true; // [!code ++]
					if ((bool)item.progressText) // [!code ++]
					{
						Portrait.modPortraitBGFs.Add(fileInfo2); // [!code --]
						item.progressText.text = text + "Done!"; // [!code ++]
					}
					else if (fileInfo2.Name.EndsWith("-full.png")) // [!code --]
					{ // [!code --]
						Portrait.modFull.Add(fileInfo2); // [!code --]
					} // [!code --]
					else if (fileInfo2.Name.EndsWith("-overlay.png")) // [!code --]
					{ // [!code --]
						Portrait.modOverlays.Add(fileInfo2); // [!code --]
					} // [!code --]
					else // [!code --]
					{ // [!code --]
						Portrait.modPortraits.Add(fileInfo2); // [!code --]
					} // [!code --]
					Portrait.allIds.Add(fileInfo2.Name); // [!code --]
				} // [!code --]
			} // [!code --]
			break; // [!code --]
		} // [!code --]
		case "Lang": // [!code --]
		{ // [!code --]
			DirectoryInfo[] directories = dir.GetDirectories(); // [!code --]
			foreach (DirectoryInfo directoryInfo in directories) // [!code --]
			{ // [!code --]
				if (!directoryInfo.Name.StartsWith("_") && !TryAddLang(directoryInfo, isNew: false)) // [!code --]
				{ // [!code --]
					EClass.sources.Init(); // [!code --]
					Debug.Log("Generating Language Mod Contents:" + directoryInfo.FullName); // [!code --]
					IO.CopyDir(CorePath.packageCore + "Lang/EN", directoryInfo.FullName); // [!code --]
					Directory.CreateDirectory(directoryInfo.FullName + "/Dialog"); // [!code --]
					IO.CopyDir(CorePath.packageCore + "Lang/_Dialog", directoryInfo.FullName + "/Dialog"); // [!code --]
					EClass.sources.ExportSourceTexts(directoryInfo.FullName + "/Game"); // [!code --]
					IO.Copy(CorePath.packageCore + "Lang/lang.ini", directoryInfo.FullName + "/"); // [!code --]
					TryAddLang(directoryInfo, isNew: true); // [!code --]
				}
			} // [!code --]
			break; // [!code --]
		} // [!code --]
		} // [!code --]
		bool TryAddLang(DirectoryInfo dirLang, bool isNew) // [!code --]
		{ // [!code --]
			string name = dirLang.Name; // [!code --]
			FileInfo[] files2 = dirLang.GetFiles(); // [!code --]
			foreach (FileInfo fileInfo7 in files2) // [!code --]
			{ // [!code --]
				if (fileInfo7.Name == "lang.ini") // [!code --]
				else if (workshopItem.IsDownloading || workshopItem.IsDownloadPending) // [!code ++]
				{
					LangSetting langSetting = new LangSetting(fileInfo7.FullName) // [!code --]
					int num = Mathf.FloorToInt(workshopItem.DownloadCompletion * 100f); // [!code ++]
					if ((bool)item.progressText) // [!code ++]
					{
						id = name, // [!code --]
						dir = dirLang.FullName + "/" // [!code --]
					}; // [!code --]
					if (isNew) // [!code --]
					{ // [!code --]
						langSetting.SetVersion(); // [!code --]
					} // [!code --]
					else if ((Application.isEditor || Lang.runUpdate) && !Lang.IsBuiltin(dirLang.Name) && langSetting.GetVersion() != EClass.core.version.GetInt()) // [!code --]
					{ // [!code --]
						Log.system = "Updated Language Files:" + Environment.NewLine + Environment.NewLine; // [!code --]
						Debug.Log("Updating Language:" + langSetting.name + "/" + langSetting.GetVersion() + "/" + EClass.core.version.GetInt()); // [!code --]
						EClass.sources.Init(); // [!code --]
						string text = dirLang.FullName + "/Game"; // [!code --]
						Directory.Move(text, text + "_temp"); // [!code --]
						EClass.sources.ExportSourceTexts(text); // [!code --]
						EClass.sources.UpdateSourceTexts(text); // [!code --]
						IO.DeleteDirectory(text + "_temp"); // [!code --]
						text = dirLang.FullName + "/Dialog"; // [!code --]
						Directory.Move(text, text + "_temp"); // [!code --]
						IO.CopyDir(CorePath.packageCore + "Lang/_Dialog", text); // [!code --]
						UpdateDialogs(new DirectoryInfo(text), text + "_temp"); // [!code --]
						IO.DeleteDirectory(text + "_temp"); // [!code --]
						text = dirLang.FullName + "/Data"; // [!code --]
						IO.CopyDir(text, text + "_temp"); // [!code --]
						IO.Copy(CorePath.packageCore + "Lang/EN/Data/god_talk.xlsx", text); // [!code --]
						IO.Copy(CorePath.packageCore + "Lang/EN/Data/chara_talk.xlsx", text); // [!code --]
						UpdateTalks(new DirectoryInfo(text), text + "_temp"); // [!code --]
						IO.DeleteDirectory(text + "_temp"); // [!code --]
						langSetting.SetVersion(); // [!code --]
						IO.SaveText(dirLang.FullName + "/update.txt", Log.system); // [!code --]
						item.progressText.text = text + num + "%"; // [!code ++]
					}
					MOD.langs[name] = langSetting; // [!code --]
					return true; // [!code --]
				} // [!code ++]
				else if (!item.downloadStarted) // [!code ++]
				{ // [!code ++]
					item.downloadStarted = true; // [!code ++]
					workshopItem.DownloadItem(highPriority: true); // [!code ++]
					Debug.Log("Start downloading: " + workshopItem.Title + " | " + $"Installed={workshopItem.IsInstalled}, " + $"Downloading={workshopItem.IsDownloading}, " + $"Pending={workshopItem.IsDownloadPending}"); // [!code ++]
				}
			}
			return false; // [!code --]
		} // [!code --]
	} // [!code --]
 // [!code --]
	public void UpdateDialogs(DirectoryInfo dir, string dirTemp) // [!code --]
	{ // [!code --]
		DirectoryInfo[] directories = dir.GetDirectories(); // [!code --]
		foreach (DirectoryInfo directoryInfo in directories) // [!code --]
		{ // [!code --]
			UpdateDialogs(directoryInfo, dirTemp + "/" + directoryInfo.Name); // [!code --]
		} // [!code --]
		FileInfo[] files = dir.GetFiles(); // [!code --]
		foreach (FileInfo fileInfo in files) // [!code --]
		{ // [!code --]
			if (fileInfo.Name.EndsWith("xlsx")) // [!code --]
			if (!flag) // [!code ++]
			{
				UpdateExcelBook(fileInfo, dirTemp, updateOnlyText: true); // [!code --]
				yield break; // [!code ++]
			}
		} // [!code --]
	} // [!code --]
 // [!code --]
	public void UpdateTalks(DirectoryInfo dir, string dirTemp) // [!code --]
	{ // [!code --]
		FileInfo[] files = dir.GetFiles(); // [!code --]
		foreach (FileInfo fileInfo in files) // [!code --]
		{ // [!code --]
			if (fileInfo.Name == "god_talk.xlsx" || fileInfo.Name == "chara_talk.xlsx") // [!code --]
			if (UnityEngine.Input.GetKey(KeyCode.Escape)) // [!code ++]
			{
				UpdateExcelBook(fileInfo, dirTemp, updateOnlyText: false); // [!code --]
				break; // [!code ++]
			}
			yield return awaiter; // [!code ++]
		}
		Debug.Log("Workshop updating cancelled"); // [!code ++]
	}

	public void UpdateExcelBook(FileInfo f, string dirTemp, bool updateOnlyText) // [!code --]
	public void InitPackagesMeta() // [!code ++]
	{
		string path = dirTemp + "/" + f.Name; // [!code --]
		if (!File.Exists(path)) // [!code --]
		{ // [!code --]
			return; // [!code --]
		} // [!code --]
		XSSFWorkbook xSSFWorkbook; // [!code --]
		using (FileStream @is = File.Open(path, FileMode.Open, FileAccess.Read, FileShare.ReadWrite)) // [!code --]
		{ // [!code --]
			xSSFWorkbook = new XSSFWorkbook((Stream)@is); // [!code --]
		} // [!code --]
		XSSFWorkbook xSSFWorkbook2; // [!code --]
		using (FileStream is2 = File.Open(f.FullName, FileMode.Open, FileAccess.Read, FileShare.ReadWrite)) // [!code --]
		{ // [!code --]
			xSSFWorkbook2 = new XSSFWorkbook((Stream)is2); // [!code --]
		} // [!code --]
		for (int i = 0; i < xSSFWorkbook2.NumberOfSheets; i++) // [!code --]
		foreach (BaseModPackage package in packages) // [!code ++]
		{
			ISheet sheetAt = xSSFWorkbook2.GetSheetAt(i); // [!code --]
			ISheet sheet = xSSFWorkbook.GetSheet(sheetAt.SheetName); // [!code --]
			if (sheet == null) // [!code --]
			try // [!code ++]
			{
				Log.system = Log.system + "Old sheet not found:" + sheetAt.SheetName + Environment.NewLine; // [!code --]
				continue; // [!code --]
				package.Init(); // [!code ++]
				mappedPackages[package.id] = package as ModPackage; // [!code ++]
				_loading?.Log(package.ToString()); // [!code ++]
			}
			int num = UpdateExcelSheet(sheetAt, sheet, updateOnlyText); // [!code --]
			Log.system = Log.system + ((num == 0) ? "(No Changes) " : "(Updated) ") + f.FullName + "(" + sheetAt.SheetName + ")" + Environment.NewLine; // [!code --]
			if (num != 0) // [!code --]
			catch (Exception ex) // [!code ++]
			{
				Log.system = Log.system + num + Environment.NewLine; // [!code --]
				package.willActivate = false; // [!code ++]
				_loading?.Log("Mod " + package.title + "/" + package.id + " has failed to initialize, reason: " + ex.Message); // [!code ++]
			}
			Log.system += Environment.NewLine; // [!code --]
		}
		using FileStream stream = new FileStream(f.FullName, FileMode.Create, FileAccess.Write, FileShare.ReadWrite); // [!code --]
		xSSFWorkbook2.Write(stream); // [!code --]
	}

	public int UpdateExcelSheet(ISheet destSheet, ISheet oldSheet, bool updateOnlytext) // [!code --]
	public void ActivatePackages() // [!code ++]
	{
		Dictionary<string, string[]> dictionary = new Dictionary<string, string[]>(); // [!code --]
		int num = 0; // [!code --]
		int num2 = 0; // [!code --]
		int num3 = 10; // [!code --]
		IRow row2 = destSheet.GetRow(0); // [!code --]
		IRow row3 = oldSheet.GetRow(0); // [!code --]
		List<SheetIndex> list = new List<SheetIndex>(); // [!code --]
		int cellnum = FindField(row2, "id"); // [!code --]
		int cellnum2 = FindField(row3, "id"); // [!code --]
		for (int i = 0; i < row2.LastCellNum; i++) // [!code --]
		BaseModManager.listChainLoad.Clear(); // [!code ++]
		ListPluginObject.Clear(); // [!code ++]
		foreach (ModPackage package in packages) // [!code ++]
		{
			ICell cell = row2.GetCell(i); // [!code --]
			if (cell == null) // [!code --]
			{ // [!code --]
				break; // [!code --]
			} // [!code --]
			string stringCellValue = cell.StringCellValue; // [!code --]
			if (stringCellValue == "id" || (updateOnlytext && stringCellValue != "text")) // [!code --]
			if ((disableMod && !package.builtin) || !package.IsValidVersion()) // [!code ++]
			{
				continue;
			}
			for (int j = 0; j < row3.LastCellNum; j++) // [!code --]
			{ // [!code --]
				cell = row3.GetCell(j); // [!code --]
				if (cell == null) // [!code --]
				{ // [!code --]
					break; // [!code --]
				} // [!code --]
				if (cell.StringCellValue == stringCellValue) // [!code --]
				{ // [!code --]
					list.Add(new SheetIndex // [!code --]
					{ // [!code --]
						dest = i, // [!code --]
						old = j // [!code --]
					}); // [!code --]
					Debug.Log(destSheet.SheetName + "/" + stringCellValue + "/" + i + "/" + j); // [!code --]
					break; // [!code --]
				} // [!code --]
			} // [!code --]
		} // [!code --]
		for (int k = 2; k <= oldSheet.LastRowNum; k++) // [!code --]
		{ // [!code --]
			IRow row4 = oldSheet.GetRow(k); // [!code --]
			if (row4 == null) // [!code --]
			try // [!code ++]
			{
				if (num2 >= num3) // [!code --]
				package.Activate(); // [!code ++]
				if (package.activated) // [!code ++]
				{
					break; // [!code --]
					BaseModManager.listChainLoad.Add(package.dirInfo.FullName); // [!code ++]
				}
				num2++; // [!code --]
				continue; // [!code --]
			} // [!code --]
			num2 = 0; // [!code --]
			ICell cell2 = row4.GetCell(cellnum2); // [!code --]
			if (cell2 == null) // [!code --]
			{ // [!code --]
				continue; // [!code --]
			} // [!code --]
			string text = ((cell2.CellType == CellType.Numeric) ? cell2.NumericCellValue.ToString() : cell2.StringCellValue); // [!code --]
			if (text.IsEmpty()) // [!code --]
			{ // [!code --]
				continue; // [!code --]
			}
			string[] array = new string[list.Count]; // [!code --]
			for (int l = 0; l < list.Count; l++) // [!code --]
			catch (Exception ex) // [!code ++]
			{
				ICell cell3 = row4.GetCell(list[l].old); // [!code --]
				if (cell3 != null) // [!code --]
				{ // [!code --]
					string stringCellValue2 = cell3.StringCellValue; // [!code --]
					if (!stringCellValue2.IsEmpty()) // [!code --]
					{ // [!code --]
						array[l] = stringCellValue2; // [!code --]
					} // [!code --]
				} // [!code --]
				_loading.Log("Failed to activate mod: " + package.title + ", reason: " + ex.Message); // [!code ++]
			}
			dictionary.Add(text, array); // [!code --]
		}
		num2 = 0; // [!code --]
		for (int m = 2; m <= destSheet.LastRowNum; m++) // [!code --]
		ModUtil.OnModsActivated(); // [!code ++]
		ModUtil.LoadTypeFallback(); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public override void ParseExtra(DirectoryInfo dir, BaseModPackage package) // [!code ++]
	{ // [!code ++]
		ModPackage modPackage = (ModPackage)package; // [!code ++]
		switch (dir.Name) // [!code ++]
		{
			IRow row5 = destSheet.GetRow(m); // [!code --]
			if (row5 == null) // [!code --]
			{ // [!code --]
				if (num2 >= num3) // [!code --]
				{ // [!code --]
					break; // [!code --]
				} // [!code --]
				num2++; // [!code --]
				continue; // [!code --]
			} // [!code --]
			num2 = 0; // [!code --]
			ICell cell4 = row5.GetCell(cellnum); // [!code --]
			if (cell4 == null) // [!code --]
			{ // [!code --]
				continue; // [!code --]
			} // [!code --]
			string text2 = ((cell4.CellType == CellType.Numeric) ? cell4.NumericCellValue.ToString() : cell4.StringCellValue); // [!code --]
			if (text2.IsEmpty() || !dictionary.ContainsKey(text2)) // [!code --]
			{ // [!code --]
				continue; // [!code --]
			} // [!code --]
			string[] array2 = dictionary[text2]; // [!code --]
			for (int n = 0; n < list.Count; n++) // [!code --]
		case "TalkText": // [!code ++]
			modPackage.ParseTalkText(dir); // [!code ++]
			break; // [!code ++]
		case "Map": // [!code ++]
			if (!package.builtin) // [!code ++]
			{
				ICell cell5 = row5.GetCell(list[n].dest) ?? row5.CreateCell(list[n].dest, CellType.String); // [!code --]
				if (cell5 != null) // [!code --]
				{ // [!code --]
					cell5.SetCellValue(array2[n]); // [!code --]
					cell5.SetCellType(CellType.String); // [!code --]
					cell5.SetAsActiveCell(); // [!code --]
					num++; // [!code --]
				} // [!code --]
				modPackage.ParseMap(dir); // [!code ++]
			}
		} // [!code --]
		return num; // [!code --]
		static int FindField(IRow row, string id) // [!code --]
		{ // [!code --]
			for (int num4 = 0; num4 < row.LastCellNum; num4++) // [!code --]
			break; // [!code ++]
		case "Map Piece": // [!code ++]
			if (!package.builtin) // [!code ++]
			{
				ICell cell6 = row.GetCell(num4); // [!code --]
				if (cell6 == null) // [!code --]
				{ // [!code --]
					break; // [!code --]
				} // [!code --]
				if (cell6.StringCellValue == id) // [!code --]
				{ // [!code --]
					return num4; // [!code --]
				} // [!code --]
				modPackage.ParseMapPiece(dir); // [!code ++]
			}
			return -1; // [!code --]
			break; // [!code ++]
		case "Texture Replace": // [!code ++]
			replaceFiles.AddRange(modPackage.ParseTextureReplace(dir)); // [!code ++]
			break; // [!code ++]
		case "Texture": // [!code ++]
			modPackage.ParseTexture(dir); // [!code ++]
			break; // [!code ++]
		case "Portrait": // [!code ++]
			modPackage.ParsePortrait(dir); // [!code ++]
			break; // [!code ++]
		case "LangMod": // [!code ++]
			modPackage.ParseLangMod(dir); // [!code ++]
			break; // [!code ++]
		case "Sound": // [!code ++]
			modPackage.ParseSound(dir); // [!code ++]
			break; // [!code ++]
		case "Lang": // [!code ++]
			modPackage.AddOrUpdateLang(dir); // [!code ++]
			break; // [!code ++]
		}
	}
}
```

## ModPackage

[`public class ModPackage : BaseModPackage`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/ModPackage.cs#L1-L3)
```cs:line-numbers=1
public class ModPackage : BaseModPackage // [!code --]
using System; // [!code ++]
using System.Collections.Generic; // [!code ++]
using System.IO; // [!code ++]
using System.Linq; // [!code ++]
using NPOI.SS.UserModel; // [!code ++]
using NPOI.XSSF.UserModel; // [!code ++]
using UnityEngine; // [!code ++]
 // [!code ++]
public class ModPackage : EMod // [!code ++]
{
	public struct SheetIndex // [!code ++]
	{ // [!code ++]
		public int dest; // [!code ++]
 // [!code ++]
		public int old; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public IReadOnlyList<FileInfo> ParseTalkText(DirectoryInfo dir) // [!code ++]
	{ // [!code ++]
		List<FileInfo> list = new List<FileInfo>(); // [!code ++]
		FileInfo[] files = dir.GetFiles("*.xlsx", SearchOption.TopDirectoryOnly); // [!code ++]
		foreach (FileInfo fileInfo in files) // [!code ++]
		{ // [!code ++]
			ExcelData excelData = new ExcelData(fileInfo.FullName); // [!code ++]
			TalkText.modList.Add(excelData); // [!code ++]
			talkTexts.Add(excelData); // [!code ++]
			list.Add(fileInfo); // [!code ++]
		} // [!code ++]
		if (list.Count > 0) // [!code ++]
		{ // [!code ++]
			Debug.Log($"TalkText: {talkTexts.Count}"); // [!code ++]
		} // [!code ++]
		return list; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public IReadOnlyList<FileInfo> ParseMap(DirectoryInfo dir, bool addToList = true) // [!code ++]
	{ // [!code ++]
		List<FileInfo> list = new List<FileInfo>(); // [!code ++]
		FileInfo[] files = dir.GetFiles("*.z", SearchOption.TopDirectoryOnly); // [!code ++]
		foreach (FileInfo fileInfo in files) // [!code ++]
		{ // [!code ++]
			string key = Path.ChangeExtension(fileInfo.Name, null); // [!code ++]
			if (addToList) // [!code ++]
			{ // [!code ++]
				MOD.listMaps.Add(fileInfo); // [!code ++]
			} // [!code ++]
			maps[key] = fileInfo; // [!code ++]
			list.Add(fileInfo); // [!code ++]
		} // [!code ++]
		if (list.Count > 0) // [!code ++]
		{ // [!code ++]
			Debug.Log($"Map: {maps.Count}"); // [!code ++]
		} // [!code ++]
		return list; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public IReadOnlyList<FileInfo> ParseMapPiece(DirectoryInfo dir, bool addToList = true) // [!code ++]
	{ // [!code ++]
		List<FileInfo> list = new List<FileInfo>(); // [!code ++]
		FileInfo[] files = dir.GetFiles("*.mp", SearchOption.TopDirectoryOnly); // [!code ++]
		foreach (FileInfo fileInfo in files) // [!code ++]
		{ // [!code ++]
			string key = Path.ChangeExtension(fileInfo.Name, null); // [!code ++]
			if (addToList) // [!code ++]
			{ // [!code ++]
				MOD.listPartialMaps.Add(fileInfo); // [!code ++]
			} // [!code ++]
			partialMaps[key] = fileInfo; // [!code ++]
			list.Add(fileInfo); // [!code ++]
		} // [!code ++]
		if (list.Count > 0) // [!code ++]
		{ // [!code ++]
			Debug.Log($"PartialMap: {partialMaps.Count}"); // [!code ++]
		} // [!code ++]
		return list; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public IReadOnlyList<FileInfo> ParseTextureReplace(DirectoryInfo dir) // [!code ++]
	{ // [!code ++]
		List<FileInfo> list = new List<FileInfo>(); // [!code ++]
		FileInfo[] files = dir.GetFiles("*.png", SearchOption.TopDirectoryOnly); // [!code ++]
		foreach (FileInfo fileInfo in files) // [!code ++]
		{ // [!code ++]
			string key = Path.ChangeExtension(fileInfo.Name, null); // [!code ++]
			textureReplaces[key] = fileInfo; // [!code ++]
			list.Add(fileInfo); // [!code ++]
		} // [!code ++]
		if (list.Count > 0) // [!code ++]
		{ // [!code ++]
			Debug.Log($"Texture Replace: {textureReplaces.Count}"); // [!code ++]
		} // [!code ++]
		return list; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public IReadOnlyList<FileInfo> ParseTexture(DirectoryInfo dir) // [!code ++]
	{ // [!code ++]
		List<FileInfo> list = new List<FileInfo>(); // [!code ++]
		FileInfo[] files = dir.GetFiles("*.png", SearchOption.AllDirectories); // [!code ++]
		foreach (FileInfo fileInfo in files) // [!code ++]
		{ // [!code ++]
			string key = Path.ChangeExtension(Path.GetRelativePath(dir.FullName, fileInfo.FullName).NormalizePath(), null); // [!code ++]
			string text = Path.ChangeExtension(fileInfo.FullName, null); // [!code ++]
			Dictionary<string, string> dictModItems = SpriteReplacer.dictModItems; // [!code ++]
			string value = (textures[key] = text); // [!code ++]
			dictModItems[key] = value; // [!code ++]
			list.Add(fileInfo); // [!code ++]
		} // [!code ++]
		if (list.Count > 0) // [!code ++]
		{ // [!code ++]
			Debug.Log($"Texture: {textures.Count}"); // [!code ++]
		} // [!code ++]
		return list; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public IReadOnlyList<FileInfo> ParsePortrait(DirectoryInfo dir) // [!code ++]
	{ // [!code ++]
		List<FileInfo> list = new List<FileInfo>(); // [!code ++]
		FileInfo[] files = dir.GetFiles("*.png", SearchOption.TopDirectoryOnly); // [!code ++]
		foreach (FileInfo fileInfo in files) // [!code ++]
		{ // [!code ++]
			string name = fileInfo.Name; // [!code ++]
			if (name.StartsWith("BG_", StringComparison.Ordinal)) // [!code ++]
			{ // [!code ++]
				Portrait.modPortraitBGs.Add(fileInfo); // [!code ++]
			} // [!code ++]
			else if (name.StartsWith("BGF_", StringComparison.Ordinal)) // [!code ++]
			{ // [!code ++]
				Portrait.modPortraitBGFs.Add(fileInfo); // [!code ++]
			} // [!code ++]
			else if (name.EndsWith("-full.png", StringComparison.Ordinal)) // [!code ++]
			{ // [!code ++]
				Portrait.modFull.Add(fileInfo); // [!code ++]
			} // [!code ++]
			else if (name.EndsWith("-overlay.png", StringComparison.Ordinal)) // [!code ++]
			{ // [!code ++]
				Portrait.modOverlays.Add(fileInfo); // [!code ++]
			} // [!code ++]
			else // [!code ++]
			{ // [!code ++]
				Portrait.modPortraits.Add(fileInfo); // [!code ++]
			} // [!code ++]
			Portrait.allIds.Add(name); // [!code ++]
			portraits[name] = fileInfo; // [!code ++]
			list.Add(fileInfo); // [!code ++]
		} // [!code ++]
		if (list.Count > 0) // [!code ++]
		{ // [!code ++]
			Debug.Log($"Portrait: {portraits.Count}"); // [!code ++]
		} // [!code ++]
		return list; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void ParseLangMod(DirectoryInfo dir) // [!code ++]
	{ // [!code ++]
		if (Mapping == null) // [!code ++]
		{ // [!code ++]
			Mapping = new FileMapping(dir.Parent); // [!code ++]
		} // [!code ++]
		Mapping.RebuildLangModMapping(EClass.core.config?.lang ?? EClass.core.debug.langCode.ToString()); // [!code ++]
		Lang.excelDialog = null; // [!code ++]
		Debug.Log("LangMod: " + string.Join(", ", Mapping.OrderedLangMods)); // [!code ++]
		if (Mapping.SourceSheets.Count > 0) // [!code ++]
		{ // [!code ++]
			Debug.Log($"Source Sheets: {Mapping.SourceSheets.Count}, {Mapping.SourceLangMod}"); // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public IReadOnlyList<FileInfo> ParseSound(DirectoryInfo dir) // [!code ++]
	{ // [!code ++]
		List<FileInfo> list = new List<FileInfo>(); // [!code ++]
		FileInfo[] files = dir.GetFiles("*.*", SearchOption.AllDirectories); // [!code ++]
		foreach (FileInfo fileInfo in files) // [!code ++]
		{ // [!code ++]
			if (ModUtil.GetAudioType(fileInfo.Extension) != 0) // [!code ++]
			{ // [!code ++]
				string key = Path.ChangeExtension(Path.GetRelativePath(dir.FullName, fileInfo.FullName).NormalizePath(), null); // [!code ++]
				Dictionary<string, FileInfo> dictionary = MOD.sounds; // [!code ++]
				FileInfo value = (sounds[key] = fileInfo); // [!code ++]
				dictionary[key] = value; // [!code ++]
				list.Add(fileInfo); // [!code ++]
			} // [!code ++]
		} // [!code ++]
		if (list.Count > 0) // [!code ++]
		{ // [!code ++]
			Debug.Log($"Sound: {sounds.Count}"); // [!code ++]
		} // [!code ++]
		int num = sounds.Keys.Count((string k) => k.StartsWith("BGM/")); // [!code ++]
		if (num > 0) // [!code ++]
		{ // [!code ++]
			Debug.Log($"BGM: {num}"); // [!code ++]
		} // [!code ++]
		return list; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public SortedDictionary<string, string> ExportSourceLocalizations() // [!code ++]
	{ // [!code ++]
		SortedDictionary<string, string> sortedDictionary = new SortedDictionary<string, string>(); // [!code ++]
		try // [!code ++]
		{ // [!code ++]
			foreach (SourceData.BaseRow sourceRow in sourceRows) // [!code ++]
			{ // [!code ++]
				foreach (KeyValuePair<string, string> item in sourceRow.ExportTexts(sourceRow.UseAlias ? "alias" : "id")) // [!code ++]
				{ // [!code ++]
					item.Deconstruct(out var key, out var value); // [!code ++]
					string key2 = key; // [!code ++]
					string value2 = value; // [!code ++]
					sortedDictionary[key2] = value2; // [!code ++]
				} // [!code ++]
			} // [!code ++]
		} // [!code ++]
		catch (Exception arg) // [!code ++]
		{ // [!code ++]
			Debug.LogError($"#source failed to export localization for {title}/{id}\n{arg}"); // [!code ++]
		} // [!code ++]
		return sortedDictionary; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void ImportSourceLocalizations(IReadOnlyDictionary<string, string> texts) // [!code ++]
	{ // [!code ++]
		foreach (SourceData.BaseRow sourceRow in sourceRows) // [!code ++]
		{ // [!code ++]
			sourceRow.ImportTexts(texts, sourceRow.UseAlias ? "alias" : "id"); // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void ClearSourceLocalizations(string lang) // [!code ++]
	{ // [!code ++]
		Mapping.RebuildLangModMapping(lang); // [!code ++]
		for (FileInfo fileInfo = Mapping.RelocateFile("SourceLocalization.json"); fileInfo != null; fileInfo = Mapping.RelocateFile("SourceLocalization.json")) // [!code ++]
		{ // [!code ++]
			fileInfo.Delete(); // [!code ++]
			Debug.Log("#source localization deleted " + fileInfo.ShortPath()); // [!code ++]
		} // [!code ++]
		Mapping.RebuildLangModMapping(Lang.langCode); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void AddOrUpdateLang(DirectoryInfo dir) // [!code ++]
	{ // [!code ++]
		Core core = EClass.core; // [!code ++]
		SourceManager sources = core.sources; // [!code ++]
		DirectoryInfo[] directories = dir.GetDirectories(); // [!code ++]
		foreach (DirectoryInfo directoryInfo in directories) // [!code ++]
		{ // [!code ++]
			if (!directoryInfo.Name.StartsWith("_") && !TryAddLang(directoryInfo, isNew: false)) // [!code ++]
			{ // [!code ++]
				Debug.Log("Generating Language Mod Contents:" + directoryInfo.FullName); // [!code ++]
				IO.CopyDir(CorePath.packageCore + "Lang/EN", directoryInfo.FullName); // [!code ++]
				Directory.CreateDirectory(directoryInfo.FullName + "/Dialog"); // [!code ++]
				IO.CopyDir(CorePath.packageCore + "Lang/_Dialog", directoryInfo.FullName + "/Dialog"); // [!code ++]
				sources.ExportSourceTexts(directoryInfo.FullName + "/Game"); // [!code ++]
				IO.Copy(CorePath.packageCore + "Lang/lang.ini", directoryInfo.FullName + "/"); // [!code ++]
				TryAddLang(directoryInfo, isNew: true); // [!code ++]
			} // [!code ++]
		} // [!code ++]
		bool TryAddLang(DirectoryInfo dirLang, bool isNew) // [!code ++]
		{ // [!code ++]
			string name = dirLang.Name; // [!code ++]
			FileInfo[] files = dirLang.GetFiles(); // [!code ++]
			foreach (FileInfo fileInfo in files) // [!code ++]
			{ // [!code ++]
				if (fileInfo.Name == "lang.ini") // [!code ++]
				{ // [!code ++]
					LangSetting langSetting = new LangSetting(fileInfo.FullName) // [!code ++]
					{ // [!code ++]
						id = name, // [!code ++]
						dir = dirLang.FullName + "/" // [!code ++]
					}; // [!code ++]
					if (isNew) // [!code ++]
					{ // [!code ++]
						langSetting.SetVersion(); // [!code ++]
					} // [!code ++]
					else if ((Application.isEditor || Lang.runUpdate) && !Lang.IsBuiltin(dirLang.Name) && langSetting.GetVersion() < core.version.GetInt()) // [!code ++]
					{ // [!code ++]
						sources.Init(); // [!code ++]
						Log.system = "Updated Language Files:" + Environment.NewLine + Environment.NewLine; // [!code ++]
						Debug.Log("Updating Language:" + langSetting.name + "/" + langSetting.GetVersion() + "/" + core.version.GetInt()); // [!code ++]
						string text = dirLang.FullName + "/Game"; // [!code ++]
						Directory.Move(text, text + "_temp"); // [!code ++]
						sources.ExportSourceTexts(text); // [!code ++]
						sources.UpdateSourceTexts(text); // [!code ++]
						IO.DeleteDirectory(text + "_temp"); // [!code ++]
						text = dirLang.FullName + "/Dialog"; // [!code ++]
						Directory.Move(text, text + "_temp"); // [!code ++]
						IO.CopyDir(CorePath.packageCore + "Lang/_Dialog", text); // [!code ++]
						UpdateDialogs(new DirectoryInfo(text), text + "_temp"); // [!code ++]
						IO.DeleteDirectory(text + "_temp"); // [!code ++]
						text = dirLang.FullName + "/Data"; // [!code ++]
						IO.CopyDir(text, text + "_temp"); // [!code ++]
						IO.Copy(CorePath.packageCore + "Lang/EN/Data/god_talk.xlsx", text); // [!code ++]
						IO.Copy(CorePath.packageCore + "Lang/EN/Data/chara_talk.xlsx", text); // [!code ++]
						UpdateTalks(new DirectoryInfo(text), text + "_temp"); // [!code ++]
						IO.DeleteDirectory(text + "_temp"); // [!code ++]
						langSetting.SetVersion(); // [!code ++]
						IO.SaveText(dirLang.FullName + "/update.txt", Log.system); // [!code ++]
					} // [!code ++]
					MOD.langs[name] = langSetting; // [!code ++]
					return true; // [!code ++]
				} // [!code ++]
			} // [!code ++]
			return false; // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void UpdateDialogs(DirectoryInfo dir, string dirTemp) // [!code ++]
	{ // [!code ++]
		DirectoryInfo[] directories = dir.GetDirectories(); // [!code ++]
		foreach (DirectoryInfo directoryInfo in directories) // [!code ++]
		{ // [!code ++]
			UpdateDialogs(directoryInfo, dirTemp + "/" + directoryInfo.Name); // [!code ++]
		} // [!code ++]
		FileInfo[] files = dir.GetFiles(); // [!code ++]
		foreach (FileInfo fileInfo in files) // [!code ++]
		{ // [!code ++]
			if (fileInfo.Name.EndsWith("xlsx")) // [!code ++]
			{ // [!code ++]
				UpdateExcelBook(fileInfo, dirTemp, updateOnlyText: true); // [!code ++]
			} // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void UpdateTalks(DirectoryInfo dir, string dirTemp) // [!code ++]
	{ // [!code ++]
		FileInfo[] files = dir.GetFiles(); // [!code ++]
		foreach (FileInfo fileInfo in files) // [!code ++]
		{ // [!code ++]
			if (fileInfo.Name == "god_talk.xlsx" || fileInfo.Name == "chara_talk.xlsx") // [!code ++]
			{ // [!code ++]
				UpdateExcelBook(fileInfo, dirTemp, updateOnlyText: false); // [!code ++]
			} // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void UpdateExcelBook(FileInfo f, string dirTemp, bool updateOnlyText) // [!code ++]
	{ // [!code ++]
		string path = dirTemp + "/" + f.Name; // [!code ++]
		if (!File.Exists(path)) // [!code ++]
		{ // [!code ++]
			return; // [!code ++]
		} // [!code ++]
		XSSFWorkbook xSSFWorkbook; // [!code ++]
		using (FileStream @is = File.Open(path, FileMode.Open, FileAccess.Read, FileShare.ReadWrite)) // [!code ++]
		{ // [!code ++]
			xSSFWorkbook = new XSSFWorkbook((Stream)@is); // [!code ++]
		} // [!code ++]
		XSSFWorkbook xSSFWorkbook2; // [!code ++]
		using (FileStream is2 = File.Open(f.FullName, FileMode.Open, FileAccess.Read, FileShare.ReadWrite)) // [!code ++]
		{ // [!code ++]
			xSSFWorkbook2 = new XSSFWorkbook((Stream)is2); // [!code ++]
		} // [!code ++]
		for (int i = 0; i < xSSFWorkbook2.NumberOfSheets; i++) // [!code ++]
		{ // [!code ++]
			ISheet sheetAt = xSSFWorkbook2.GetSheetAt(i); // [!code ++]
			ISheet sheet = xSSFWorkbook.GetSheet(sheetAt.SheetName); // [!code ++]
			if (sheet == null) // [!code ++]
			{ // [!code ++]
				Log.system = Log.system + "Old sheet not found:" + sheetAt.SheetName + Environment.NewLine; // [!code ++]
				continue; // [!code ++]
			} // [!code ++]
			int num = UpdateExcelSheet(sheetAt, sheet, updateOnlyText); // [!code ++]
			Log.system = Log.system + ((num == 0) ? "(No Changes) " : "(Updated) ") + f.FullName + "(" + sheetAt.SheetName + ")" + Environment.NewLine; // [!code ++]
			if (num != 0) // [!code ++]
			{ // [!code ++]
				Log.system = Log.system + num + Environment.NewLine; // [!code ++]
			} // [!code ++]
			Log.system += Environment.NewLine; // [!code ++]
		} // [!code ++]
		using FileStream stream = new FileStream(f.FullName, FileMode.Create, FileAccess.Write, FileShare.ReadWrite); // [!code ++]
		xSSFWorkbook2.Write(stream); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public int UpdateExcelSheet(ISheet destSheet, ISheet oldSheet, bool updateOnlytext) // [!code ++]
	{ // [!code ++]
		Dictionary<string, string[]> dictionary = new Dictionary<string, string[]>(); // [!code ++]
		int num = 0; // [!code ++]
		int num2 = 0; // [!code ++]
		int num3 = 10; // [!code ++]
		IRow row2 = destSheet.GetRow(0); // [!code ++]
		IRow row3 = oldSheet.GetRow(0); // [!code ++]
		List<SheetIndex> list = new List<SheetIndex>(); // [!code ++]
		int cellnum = FindField(row2, "id"); // [!code ++]
		int cellnum2 = FindField(row3, "id"); // [!code ++]
		for (int i = 0; i < row2.LastCellNum; i++) // [!code ++]
		{ // [!code ++]
			ICell cell = row2.GetCell(i); // [!code ++]
			if (cell == null) // [!code ++]
			{ // [!code ++]
				break; // [!code ++]
			} // [!code ++]
			string stringCellValue = cell.StringCellValue; // [!code ++]
			if (stringCellValue == "id" || (updateOnlytext && stringCellValue != "text")) // [!code ++]
			{ // [!code ++]
				continue; // [!code ++]
			} // [!code ++]
			for (int j = 0; j < row3.LastCellNum; j++) // [!code ++]
			{ // [!code ++]
				cell = row3.GetCell(j); // [!code ++]
				if (cell == null) // [!code ++]
				{ // [!code ++]
					break; // [!code ++]
				} // [!code ++]
				if (cell.StringCellValue == stringCellValue) // [!code ++]
				{ // [!code ++]
					list.Add(new SheetIndex // [!code ++]
					{ // [!code ++]
						dest = i, // [!code ++]
						old = j // [!code ++]
					}); // [!code ++]
					Debug.Log(destSheet.SheetName + "/" + stringCellValue + "/" + i + "/" + j); // [!code ++]
					break; // [!code ++]
				} // [!code ++]
			} // [!code ++]
		} // [!code ++]
		for (int k = 2; k <= oldSheet.LastRowNum; k++) // [!code ++]
		{ // [!code ++]
			IRow row4 = oldSheet.GetRow(k); // [!code ++]
			if (row4 == null) // [!code ++]
			{ // [!code ++]
				if (num2 >= num3) // [!code ++]
				{ // [!code ++]
					break; // [!code ++]
				} // [!code ++]
				num2++; // [!code ++]
				continue; // [!code ++]
			} // [!code ++]
			num2 = 0; // [!code ++]
			ICell cell2 = row4.GetCell(cellnum2); // [!code ++]
			if (cell2 == null) // [!code ++]
			{ // [!code ++]
				continue; // [!code ++]
			} // [!code ++]
			string text = ((cell2.CellType == CellType.Numeric) ? cell2.NumericCellValue.ToString() : cell2.StringCellValue); // [!code ++]
			if (text.IsEmpty()) // [!code ++]
			{ // [!code ++]
				continue; // [!code ++]
			} // [!code ++]
			string[] array = new string[list.Count]; // [!code ++]
			for (int l = 0; l < list.Count; l++) // [!code ++]
			{ // [!code ++]
				ICell cell3 = row4.GetCell(list[l].old); // [!code ++]
				if (cell3 != null) // [!code ++]
				{ // [!code ++]
					string stringCellValue2 = cell3.StringCellValue; // [!code ++]
					if (!stringCellValue2.IsEmpty()) // [!code ++]
					{ // [!code ++]
						array[l] = stringCellValue2; // [!code ++]
					} // [!code ++]
				} // [!code ++]
			} // [!code ++]
			dictionary.Add(text, array); // [!code ++]
		} // [!code ++]
		num2 = 0; // [!code ++]
		for (int m = 2; m <= destSheet.LastRowNum; m++) // [!code ++]
		{ // [!code ++]
			IRow row5 = destSheet.GetRow(m); // [!code ++]
			if (row5 == null) // [!code ++]
			{ // [!code ++]
				if (num2 >= num3) // [!code ++]
				{ // [!code ++]
					break; // [!code ++]
				} // [!code ++]
				num2++; // [!code ++]
				continue; // [!code ++]
			} // [!code ++]
			num2 = 0; // [!code ++]
			ICell cell4 = row5.GetCell(cellnum); // [!code ++]
			if (cell4 == null) // [!code ++]
			{ // [!code ++]
				continue; // [!code ++]
			} // [!code ++]
			string text2 = ((cell4.CellType == CellType.Numeric) ? cell4.NumericCellValue.ToString() : cell4.StringCellValue); // [!code ++]
			if (text2.IsEmpty() || !dictionary.ContainsKey(text2)) // [!code ++]
			{ // [!code ++]
				continue; // [!code ++]
			} // [!code ++]
			string[] array2 = dictionary[text2]; // [!code ++]
			for (int n = 0; n < list.Count; n++) // [!code ++]
			{ // [!code ++]
				ICell cell5 = row5.GetCell(list[n].dest) ?? row5.CreateCell(list[n].dest, CellType.String); // [!code ++]
				if (cell5 != null) // [!code ++]
				{ // [!code ++]
					cell5.SetCellValue(array2[n]); // [!code ++]
					cell5.SetCellType(CellType.String); // [!code ++]
					cell5.SetAsActiveCell(); // [!code ++]
					num++; // [!code ++]
				} // [!code ++]
			} // [!code ++]
		} // [!code ++]
		return num; // [!code ++]
		static int FindField(IRow row, string id) // [!code ++]
		{ // [!code ++]
			for (int num4 = 0; num4 < row.LastCellNum; num4++) // [!code ++]
			{ // [!code ++]
				ICell cell6 = row.GetCell(num4); // [!code ++]
				if (cell6 == null) // [!code ++]
				{ // [!code ++]
					break; // [!code ++]
				} // [!code ++]
				if (cell6.StringCellValue == id) // [!code ++]
				{ // [!code ++]
					return num4; // [!code ++]
				} // [!code ++]
			} // [!code ++]
			return -1; // [!code ++]
		} // [!code ++]
	} // [!code ++]
}
```

## ModUtil

[`using System;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/ModUtil.cs#L1-L21)
```cs:line-numbers=1
using System;
using System.Collections.Generic;
using System.Diagnostics; // [!code ++]
using System.IO;
using System.Linq; // [!code ++]
using System.Reflection; // [!code ++]
using System.Threading; // [!code ++]
using NPOI.SS.UserModel;
using NPOI.XSSF.UserModel;
using UnityEngine;
using UnityEngine.Networking; // [!code ++]

public class ModUtil : EClass
{
	public static SourceImporter sourceImporter = new SourceImporter(SourceMapping); // [!code ++]
 // [!code ++]
	public static Dictionary<string, string> fallbackTypes = new Dictionary<string, string>();

	public static void Test() // [!code --]
	{ // [!code --]
		ImportExcel("", "", EClass.sources.charas); // [!code --]
	} // [!code --]
	public static IReadOnlyDictionary<string, SourceData> SourceMapping => (from f in typeof(SourceManager).GetFields() // [!code ++]
		where typeof(SourceData).IsAssignableFrom(f.FieldType) // [!code ++]
		select f).ToDictionary((FieldInfo f) => f.FieldType.Name, (FieldInfo f) => f.GetValue(EClass.sources) as SourceData); // [!code ++]

	public static void OnModsActivated()
	{
		SoundManager.current.soundLoaders.Add(LoadSoundData); // [!code ++]
		BaseModManager.PublishEvent("elin.mods.activated"); // [!code ++]
	}

	public static void LoadTypeFallback()
```

[`public static void RegisterSerializedTypeFallback(string nameAssembly, string na`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/ModUtil.cs#L49-L55)
```cs:line-numbers=49

	public static void ImportExcel(string pathToExcelFile, string sheetName, SourceData source)
	{
		Debug.Log("ImportExcel source:" + source?.ToString() + " Path:" + pathToExcelFile); // [!code --]
		UnityEngine.Debug.Log("ImportExcel source:" + source?.ToString() + " Path:" + pathToExcelFile); // [!code ++]
		using FileStream @is = File.Open(pathToExcelFile, FileMode.Open, FileAccess.Read, FileShare.ReadWrite);
		XSSFWorkbook xSSFWorkbook = new XSSFWorkbook((Stream)@is);
		for (int i = 0; i < xSSFWorkbook.NumberOfSheets; i++)
```

[`public static void ImportExcel(string pathToExcelFile, string sheetName, SourceD`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/ModUtil.cs#L59-L80)
```cs:line-numbers=59
			{
				continue;
			}
			Debug.Log("Importing Sheet:" + sheetName); // [!code --]
			UnityEngine.Debug.Log("Importing Sheet:" + sheetName); // [!code ++]
			try
			{
				ExcelParser.path = pathToExcelFile; // [!code ++]
				if (!source.ImportData(sheetAt, new FileInfo(pathToExcelFile).Name, overwrite: true))
				{
					Debug.LogError(ERROR.msg); // [!code --]
					UnityEngine.Debug.LogError(ERROR.msg); // [!code ++]
					break;
				}
				Debug.Log("Imported " + sheetAt.SheetName); // [!code --]
				UnityEngine.Debug.Log("Imported " + sheetAt.SheetName); // [!code ++]
				source.Reset();
			}
			catch (Exception ex)
			{
				Debug.LogError("[Error] Skipping import " + sheetAt.SheetName + " :" + ex.Message + "/" + ex.Source + "/" + ex.StackTrace); // [!code --]
				UnityEngine.Debug.LogError("[Error] Skipping import " + sheetAt.SheetName + " :" + ex.Message + "/" + ex.Source + "/" + ex.StackTrace); // [!code ++]
				break;
			}
		}
	}
 // [!code ++]
	public static ModPackage FindSourceRowPackage(SourceData.BaseRow row) // [!code ++]
	{ // [!code ++]
		return ModManagerCore.Instance.MappedPackages.Values.FirstOrDefault((EMod p) => p.sourceRows.Contains(row)) as ModPackage; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static ModPackage GetModPackage(string modId) // [!code ++]
	{ // [!code ++]
		return ModManagerCore.Instance.MappedPackages.GetValueOrDefault(modId) as ModPackage; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static SerializableSoundData GetSoundMeta(string soundPath) // [!code ++]
	{ // [!code ++]
		string path = Path.ChangeExtension(soundPath, ".json"); // [!code ++]
		SerializableSoundData serializableSoundData; // [!code ++]
		if (File.Exists(path)) // [!code ++]
		{ // [!code ++]
			try // [!code ++]
			{ // [!code ++]
				serializableSoundData = IO.LoadFile<SerializableSoundData>(path); // [!code ++]
				if (serializableSoundData.dataVersion == SerializableSoundData.SoundDataMetaVersion.V1) // [!code ++]
				{ // [!code ++]
					return serializableSoundData; // [!code ++]
				} // [!code ++]
			} // [!code ++]
			catch // [!code ++]
			{ // [!code ++]
			} // [!code ++]
		} // [!code ++]
		serializableSoundData = new SerializableSoundData(); // [!code ++]
		if (soundPath.NormalizePath().Contains("/Sound/BGM/")) // [!code ++]
		{ // [!code ++]
			serializableSoundData.type = SoundData.Type.BGM; // [!code ++]
			serializableSoundData.bgmDataOptional = new SerializableBGMData(); // [!code ++]
		} // [!code ++]
		IO.SaveFile(path, serializableSoundData); // [!code ++]
		return serializableSoundData; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static AudioType GetAudioType(string extension) // [!code ++]
	{ // [!code ++]
		return extension.ToLowerInvariant().Trim() switch // [!code ++]
		{ // [!code ++]
			".acc" => AudioType.ACC,  // [!code ++]
			".mp3" => AudioType.MPEG,  // [!code ++]
			".ogg" => AudioType.OGGVORBIS,  // [!code ++]
			".wav" => AudioType.WAV,  // [!code ++]
			_ => AudioType.UNKNOWN,  // [!code ++]
		}; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static SoundData LoadSoundData(string soundId) // [!code ++]
	{ // [!code ++]
		if (!MOD.sounds.TryGetValue(soundId, out var value) || !value.Exists) // [!code ++]
		{ // [!code ++]
			return null; // [!code ++]
		} // [!code ++]
		return LoadSoundData(value); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static SoundData LoadSoundData(FileInfo soundFile) // [!code ++]
	{ // [!code ++]
		string fileNameWithoutExtension = Path.GetFileNameWithoutExtension(soundFile.FullName); // [!code ++]
		string fullName = soundFile.FullName; // [!code ++]
		AudioType audioType = GetAudioType(soundFile.Extension); // [!code ++]
		bool stream = fileNameWithoutExtension.StartsWith("BGM/") && audioType == AudioType.OGGVORBIS; // [!code ++]
		using UnityWebRequest unityWebRequest = AudioClipStream.GetAudioClip("file://" + fullName, audioType, compressed: false, stream); // [!code ++]
		unityWebRequest.SendWebRequest(); // [!code ++]
		Stopwatch stopwatch = Stopwatch.StartNew(); // [!code ++]
		while (!unityWebRequest.isDone && stopwatch.ElapsedMilliseconds < 5000) // [!code ++]
		{ // [!code ++]
			Thread.Sleep(1); // [!code ++]
		} // [!code ++]
		if (unityWebRequest.result != UnityWebRequest.Result.Success) // [!code ++]
		{ // [!code ++]
			UnityEngine.Debug.LogError("#sound '" + fileNameWithoutExtension + "' failed to load: " + unityWebRequest.error.IsEmpty("timeout")); // [!code ++]
			return null; // [!code ++]
		} // [!code ++]
		AudioClip content = DownloadHandlerAudioClip.GetContent(unityWebRequest); // [!code ++]
		int? num = content?.samples; // [!code ++]
		if (!num.HasValue || num.GetValueOrDefault() <= 0) // [!code ++]
		{ // [!code ++]
			UnityEngine.Debug.LogError($"#sound '{fileNameWithoutExtension}' sample is null: {audioType}"); // [!code ++]
			return null; // [!code ++]
		} // [!code ++]
		content.name = fileNameWithoutExtension; // [!code ++]
		SoundData soundData = GetSoundMeta(fullName).ToSoundData(); // [!code ++]
		if (soundData is BGMData bGMData) // [!code ++]
		{ // [!code ++]
			bGMData._name = Path.GetFileNameWithoutExtension(fullName); // [!code ++]
			if (bGMData.song == null) // [!code ++]
			{ // [!code ++]
				bGMData.song = new BGMData.SongData(); // [!code ++]
				bGMData.song.parts.Add(new BGMData.Part()); // [!code ++]
			} // [!code ++]
		} // [!code ++]
		soundData.clip = content; // [!code ++]
		soundData.name = fileNameWithoutExtension; // [!code ++]
		UnityEngine.Debug.Log($"#sound '{fileNameWithoutExtension}' loaded: {audioType}/{content.length}s"); // [!code ++]
		SoundManager.current.dictData[fileNameWithoutExtension] = soundData; // [!code ++]
		return soundData; // [!code ++]
	} // [!code ++]
}
```

## Msg

[`public static string GetGameText(string idLang, Card card = null)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Msg.cs#L265-L273)
```cs:line-numbers=265
	{
		mainText.Append(mainText.spriteDestroy);
	}
	if (!row.color.IsEmpty()) // [!code --]
	if (!row.color.IsEmpty() && colors.colors.TryGetValue(row.color, out var value)) // [!code ++]
	{
		SetColor(colors.colors[row.color]); // [!code --]
		SetColor(value); // [!code ++]
	}
	if (!row.sound.IsEmpty())
	{
```

## Party

[`public Element GetPartySkill(int ele)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Party.cs#L136-L143)
```cs:line-numbers=136
		return GetBestSkill(ele);
	}

	public void ModExpPartySkill(int ele, int a) // [!code --]
	public void ModExp(int ele, int a) // [!code ++]
	{
		foreach (Chara member in members) // [!code ++]
		{ // [!code ++]
			member.ModExp(ele, a); // [!code ++]
		} // [!code ++]
	}

	public Element GetBestSkill(int ele)
```

## Recipe

[`public virtual void Build(Chara chara, Card t, Point pos, int mat, int dir, int`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Recipe.cs#L682-L693)
```cs:line-numbers=682
		EClass._map.SetFloor(pos.x, pos.z, mat, tileRow.id, dir);
		break;
	case "Bridge":
	{ // [!code ++]
		if (pos.sourceObj.tileType.RemoveOnFloorChange)
		{
			EClass._map.SetObj(pos.x, pos.z);
		}
		EClass._map.SetBridge(pos.x, pos.z, Mathf.Clamp(bridgeHeight + altitude, 0, 255), mat, tileRow.id, dir); // [!code --]
		byte idPillar = (byte)((tileRow.id == 130) ? 255u : 0u); // [!code ++]
		EClass._map.SetBridge(pos.x, pos.z, Mathf.Clamp(bridgeHeight + altitude, 0, 255), mat, tileRow.id, dir, idPillar); // [!code ++]
		break;
	} // [!code ++]
	case "Obj":
		EClass._map.SetObj(pos.x, pos.z, mat, tileRow.id, 1, dir, ignoreRandomMat: true);
		if (tileType.ChangeBlockDir || pos.growth is GrowSystemTreeSingle)
```

## RenderRow

[`public void SetRenderData()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/RenderRow.cs#L221-L226)
```cs:line-numbers=221
			pref = sources.cards.map[aliasPref].pref;
		}
	}
	if (pref == null) // [!code ++]
	{ // [!code ++]
		pref = new SourcePref(); // [!code ++]
	} // [!code ++]
	if (!this.renderData.initialized)
	{
		this.renderData.Init();
```

[`public void SetRenderData()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/RenderRow.cs#L238-L244)
```cs:line-numbers=238
		{
			useAltColor = true;
		}
		DefaultMaterial = sources.materials.alias[defMat.IsEmpty("granite")]; // [!code --]
		Dictionary<string, SourceMaterial.Row> alias = sources.materials.alias; // [!code ++]
		if (!alias.TryGetValue(defMat.IsEmpty("granite"), out var defaultMaterial)) // [!code ++]
		{ // [!code ++]
			defaultMaterial = alias["granite"]; // [!code ++]
			Debug.LogError($"#source failed to set def mat '{defMat}' - {ModUtil.FindSourceRowPackage(this)}"); // [!code ++]
		} // [!code ++]
		DefaultMaterial = defaultMaterial; // [!code ++]
	}

	public virtual void SetTiles()
```

## Scene

[`public void Init(Mode newMode)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Scene.cs#L181-L186)
```cs:line-numbers=181
	EMono.ui.RemoveLayers();
	BuildMenu.Deactivate();
	ScreenFlash.Reset();
	BaseModManager.PublishEvent("elin.scene.pre_init", newMode); // [!code ++]
	mode = newMode;
	switch (newMode)
	{
```

[`public void Init(Mode newMode)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Scene.cs#L336-L341)
```cs:line-numbers=336
		}
		etherBlossom.SetActive(mode == Mode.Zone && EMono._zone is Zone_WindRest);
		bubble.SetActive(mode == Mode.Zone && EMono._zone.IsUnderwater);
		BaseModManager.PublishEvent("elin.scene.post_init", newMode); // [!code ++]
	}

	public void OnKillGame()
```

## SourceCard

[`using System;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/SourceCard.cs#L1-L3)
```cs:line-numbers=1
using System; // [!code ++]
using System.Collections.Generic;
using UnityEngine;

```

[`public class SourceCard : EClass`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/SourceCard.cs#L13-L18)
```cs:line-numbers=13

	public void Init()
	{
		rows.Clear(); // [!code ++]
		map.Clear(); // [!code ++]
		firstVariations.Clear(); // [!code ++]
		crafters.Clear(); // [!code ++]
		foreach (SourceThing.Row row in EClass.sources.things.rows)
		{
			AddRow(row);
```

[`public void AddRow(CardRow row, bool isChara = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/SourceCard.cs#L63-L70)
```cs:line-numbers=63
			}
			row.origin.isOrigin = true;
		}
		row.SetRenderData(); // [!code --]
		rows.Add(row); // [!code --]
		map[row.id] = row; // [!code --]
		try // [!code ++]
		{ // [!code ++]
			row.SetRenderData(); // [!code ++]
			rows.Add(row); // [!code ++]
			map[row.id] = row; // [!code ++]
		} // [!code ++]
		catch (Exception arg2) // [!code ++]
		{ // [!code ++]
			ModPackage arg = ModUtil.FindSourceRowPackage(row); // [!code ++]
			Debug.LogError($"#source failed to set render data for '{row.id}' - {arg}\n{arg2}"); // [!code ++]
		} // [!code ++]
	}
}
```

## SourceChara

[`public class Row : CardRow`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/SourceChara.cs#L76-L82)
```cs:line-numbers=76

		public override string GetAlias => "n";

		public SourceRace.Row race_row => _race_row ?? (_race_row = base.sources.races.map[race]); // [!code --]
		public SourceRace.Row race_row // [!code ++]
		{ // [!code ++]
			get // [!code ++]
			{ // [!code ++]
				SourceRace.Row row = _race_row; // [!code ++]
				if (row == null) // [!code ++]
				{ // [!code ++]
					SourceRace.Row obj = base.sources.races.map.TryGetValue(race) ?? base.sources.races.map["norland"]; // [!code ++]
					SourceRace.Row row2 = obj; // [!code ++]
					_race_row = obj; // [!code ++]
					row = row2; // [!code ++]
				} // [!code ++]
				return row; // [!code ++]
			} // [!code ++]
		} // [!code ++]

		public override string RecipeID => id;

```

## SourceElement

[`public bool IsEncAppliable(SourceCategory.Row cat)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/SourceElement.cs#L279-L284)
```cs:line-numbers=279
	[NonSerialized]
	public List<Row> hobbies = new List<Row>();

	[NonSerialized] // [!code ++]
	public Dictionary<string, string> fuzzyAlias = new Dictionary<string, string>(StringComparer.OrdinalIgnoreCase); // [!code ++]
 // [!code ++]
	public override string[] ImportFields => new string[8] { "altname", "textPhase", "textExtra", "textInc", "textDec", "textAlt", "adjective", "levelBonus" };

	public override Row CreateRow()
```

[`public override void SetRow(Row r)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/SourceElement.cs#L351-L356)
```cs:line-numbers=351

	public override void OnInit()
	{
		hobbies.Clear(); // [!code ++]
		foreach (Row row in rows)
		{
			if (row.id >= 100 && row.id < 400)
```

[`public override void OnInit()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/SourceElement.cs#L363-L368)
```cs:line-numbers=363
			row.isSpell = row.categorySub == "spell";
			row.isTrait = row.tag.Contains("trait");
		}
		fuzzyAlias.Clear(); // [!code ++]
		foreach (string key in alias.Keys) // [!code ++]
		{ // [!code ++]
			fuzzyAlias[key] = key; // [!code ++]
		} // [!code ++]
	}

	public override void OnAfterImportData()
```

[`public override void OnAfterImportData()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/SourceElement.cs#L396-L420)
```cs:line-numbers=396

	public void AddRow(Row ele, int id, string idOrg)
	{
		if (Application.isEditor || !map.ContainsKey(id)) // [!code --]
		if (map.ContainsKey(id)) // [!code ++]
		{
			Row row = EClass.sources.elements.alias[idOrg]; // [!code --]
			System.Reflection.FieldInfo[] fields = row.GetType().GetFields(); // [!code --]
			Row row2 = new Row(); // [!code --]
			System.Reflection.FieldInfo[] array = fields; // [!code --]
			foreach (System.Reflection.FieldInfo fieldInfo in array) // [!code --]
			{ // [!code --]
				row2.SetField(fieldInfo.Name, row.GetField<object>(fieldInfo.Name)); // [!code --]
			} // [!code --]
			row2.id = id; // [!code --]
			row2.idMold = row.id; // [!code --]
			row2.alias = row.alias + ele.alias.Remove(0, 3); // [!code --]
			row2.aliasRef = ele.alias; // [!code --]
			row2.aliasParent = ele.aliasParent; // [!code --]
			row2.chance = row.chance * ele.chance / 100; // [!code --]
			row2.LV = row.LV; // [!code --]
			row2.OnImportData(EClass.sources.elements); // [!code --]
			rows.Add(row2); // [!code --]
			return; // [!code ++]
		} // [!code ++]
		Row row = EClass.sources.elements.alias[idOrg]; // [!code ++]
		Dictionary<string, System.Reflection.FieldInfo> rowFields = row.GetRowFields(); // [!code ++]
		Row row2 = new Row(); // [!code ++]
		foreach (System.Reflection.FieldInfo value in rowFields.Values) // [!code ++]
		{ // [!code ++]
			value.SetValue(row2, value.GetValue(row)); // [!code ++]
		}
		row2.id = id; // [!code ++]
		row2.idMold = row.id; // [!code ++]
		row2.alias = row.alias + ele.alias.Remove(0, 3); // [!code ++]
		row2.aliasRef = ele.alias; // [!code ++]
		row2.aliasParent = ele.aliasParent; // [!code ++]
		row2.chance = row.chance * ele.chance / 100; // [!code ++]
		row2.LV = row.LV; // [!code ++]
		row2.OnImportData(EClass.sources.elements); // [!code ++]
		rows.Add(row2); // [!code ++]
	}
}
```

## +SourceImporter

::: details File Created
```cs
using System;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Linq;
using NPOI.SS.UserModel;
using NPOI.XSSF.UserModel;
using UnityEngine;

public class SourceImporter : EClass
{
	private readonly IReadOnlyDictionary<string, SourceData> _sourceMapping;

	public Dictionary<string, EMod> fileProviders = new Dictionary<string, EMod>(PathComparer.Default);

	public SourceImporter(IReadOnlyDictionary<string, SourceData> sourceMapping)
	{
		_sourceMapping = sourceMapping;
	}

	public SourceData FindSourceByName(string name)
	{
		string[] array = new string[3]
		{
			"Source" + name,
			"Lang" + name,
			name
		};
		foreach (string key in array)
		{
			if (_sourceMapping.TryGetValue(key, out var value))
			{
				return value;
			}
		}
		return null;
	}

	private (SourceData, SourceData.BaseRow[]) LoadBySheetName(ISheet sheet, string file)
	{
		string sheetName = sheet.SheetName;
		try
		{
			SourceData sourceData = FindSourceByName(sheetName);
			if ((object)sourceData == null)
			{
				Debug.Log("#source skipping sheet " + sheetName);
				return (null, null);
			}
			IList list;
			if (!(sourceData is SourceThingV))
			{
				list = sourceData.GetField<IList>("rows");
			}
			else
			{
				IList rows = EClass.sources.things.rows;
				list = rows;
			}
			IList list2 = list;
			int count = list2.Count;
			Debug.Log("#source loading sheet " + sheetName);
			ExcelParser.path = file;
			string fileNameWithoutExtension = Path.GetFileNameWithoutExtension(file);
			if (!sourceData.ImportData(sheet, fileNameWithoutExtension, overwrite: true))
			{
				throw new SourceParseException("#source failed to import data " + sourceData.GetType().Name + ":" + fileNameWithoutExtension + "/" + sheetName);
			}
			SourceData.BaseRow[] item = Array.Empty<SourceData.BaseRow>();
			int num = ERROR.msg.Split('/')[^1].ToInt();
			if (num > 0)
			{
				item = list2.OfType<SourceData.BaseRow>().Skip(count).Take(num)
					.ToArray();
			}
			return (sourceData, item);
		}
		catch (Exception arg)
		{
			Debug.LogError($"#source failed to load sheet {sheetName}\n{arg}");
		}
		return (null, null);
	}

	public IEnumerable<SourceData> ImportFilesCached(IEnumerable<string> imports, bool resetData = true)
	{
		SourceCache[] array = imports.Select(SourceCache.GetOrCreate).Distinct().ToArray();
		Dictionary<SourceCache, (string, ISheet[], ISheet)> dictionary = (from c in array
			where c.IsDirtyOrEmpty
			select PrefetchWorkbook(c.SheetFile.FullName)).ToArray().ToDictionary(((string file, ISheet[] sheets, ISheet element) p) => SourceCache.GetOrCreate(p.file));
		SourceElement elements = EClass.sources.elements;
		HashSet<SourceData> hashSet = new HashSet<SourceData> { elements };
		SourceCache[] array2 = array;
		foreach (SourceCache sourceCache in array2)
		{
			string arg = sourceCache.SheetFile.ShortPath();
			if (fileProviders.TryGetValue(sourceCache.SheetFile.FullName, out var value))
			{
				sourceCache.SetMod(value);
			}
			SourceData.BaseRow[] rows;
			if (sourceCache.IsDirtyOrEmpty)
			{
				if (dictionary.TryGetValue(sourceCache, out var value2) && value2.Item3 != null)
				{
					SourceData.BaseRow[] item = LoadBySheetName(value2.Item3, value2.Item1).Item2;
					sourceCache.EmplaceCache("Element", item);
					value?.sourceRows.UnionWith(item);
					Debug.Log(string.Format("#source workbook {0}:{1}:{2}", arg, "Element", item.Length));
				}
			}
			else if (sourceCache.TryGetCache("Element", out rows))
			{
				int num = elements.ImportRows(rows);
				value?.sourceRows.UnionWith(rows);
				Debug.Log(string.Format("#source workbook-cache {0}:{1}:{2}", arg, "Element", num));
			}
		}
		array2 = array;
		foreach (SourceCache sourceCache2 in array2)
		{
			string text = sourceCache2.SheetFile.ShortPath();
			if (sourceCache2.IsDirtyOrEmpty)
			{
				if (!dictionary.TryGetValue(sourceCache2, out var value3) || value3.Item2.Length == 0)
				{
					continue;
				}
				Debug.Log("#source workbook " + text);
				ISheet[] item2 = value3.Item2;
				foreach (ISheet sheet in item2)
				{
					if (sheet.SheetName == "Element")
					{
						continue;
					}
					var (sourceData, array3) = LoadBySheetName(sheet, value3.Item1);
					if ((object)sourceData != null)
					{
						int? num2 = array3?.Length;
						if (num2.HasValue && num2.GetValueOrDefault() > 0)
						{
							sourceCache2.EmplaceCache(sheet.SheetName, array3);
							sourceCache2.Mod?.sourceRows.UnionWith(array3);
							hashSet.Add(sourceData);
						}
					}
				}
				continue;
			}
			foreach (KeyValuePair<string, SourceData.BaseRow[]> item3 in sourceCache2.Source)
			{
				item3.Deconstruct(out var key, out var value4);
				string text2 = key;
				SourceData.BaseRow[] array4 = value4;
				SourceData sourceData2 = FindSourceByName(text2);
				if (!(sourceData2 is SourceThingV))
				{
					if (sourceData2 is SourceElement || (object)sourceData2 == null)
					{
						continue;
					}
				}
				else
				{
					sourceData2 = EClass.sources.things;
				}
				if (array4 == null)
				{
					Debug.Log("#source cached rows are empty " + text + ":" + text2);
					continue;
				}
				int num3 = sourceData2.ImportRows(array4);
				sourceCache2.Mod?.sourceRows.UnionWith(array4);
				Debug.Log($"#source workbook-cache {text}:{text2}:{num3}");
				hashSet.Add(sourceData2);
			}
		}
		SourceCache.FinalizeCache();
		SourceCache.ClearDetail();
		if (resetData)
		{
			HotInit(hashSet);
		}
		return hashSet;
	}

	public static void HotInit(IEnumerable<SourceData> sourceData)
	{
		Debug.Log("#source resetting data...");
		foreach (SourceData sourceDatum in sourceData)
		{
			try
			{
				sourceDatum.Reset();
				sourceDatum.Init();
			}
			catch (Exception arg)
			{
				Debug.LogError($"#source failed to reset dirty data {sourceDatum.GetType().Name}\n{arg}");
			}
		}
		Debug.Log("#source initialized data");
	}

	private (string file, ISheet[] sheets, ISheet element) PrefetchWorkbook(string file)
	{
		using FileStream @is = File.Open(file, FileMode.Open, FileAccess.Read, FileShare.ReadWrite);
		XSSFWorkbook xSSFWorkbook = new XSSFWorkbook((Stream)@is);
		List<ISheet> list = new List<ISheet>();
		ISheet item = null;
		for (int i = 0; i < xSSFWorkbook.NumberOfSheets; i++)
		{
			ISheet sheetAt = xSSFWorkbook.GetSheetAt(i);
			SourceData sourceData = FindSourceByName(sheetAt.SheetName);
			if (!(sourceData is SourceElement))
			{
				if ((object)sourceData != null)
				{
					list.Add(sheetAt);
				}
			}
			else
			{
				item = sheetAt;
			}
		}
		Debug.Log("#source workbook-prefetch " + file.ShortPath());
		return (file: file, sheets: list.ToArray(), element: item);
	}
}
```

:::
## SourceManager

[`using System.Collections.Generic;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/SourceManager.cs#L1-L5)
```cs:line-numbers=1
using System.Collections.Generic;
using System.Reflection;
using UnityEngine; // [!code ++]

public class SourceManager : EMono
{
```

[`public void OnChangeLang()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/SourceManager.cs#L114-L119)
```cs:line-numbers=114

	public void Init()
	{
		Debug.Log("SourceManager Init"); // [!code ++]
		if (initialized)
		{
			return;
```

[`public void Init()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/SourceManager.cs#L128-L133)
```cs:line-numbers=128
			list.Add((SourceData)fieldInfo.GetValue(this));
		}
	}
	BaseModManager.PublishEvent("elin.source.importing"); // [!code ++]
	if (ModManagerCore.enableSheetLoading) // [!code ++]
	{ // [!code ++]
		ModManager.Instance.ImportAllModSourceSheets(); // [!code ++]
	} // [!code ++]
	elements.Init();
	materials.Init();
	charas.Init();
```

[`public void Init()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/SourceManager.cs#L165-L175)
```cs:line-numbers=165
		keyItems.Init();
		ACT.Init();
		TimeTable.Init();
		Element.ListAttackElements.Clear(); // [!code --]
		List<SourceElement.Row> listAttackElements = Element.ListAttackElements; // [!code ++]
		listAttackElements.Clear(); // [!code ++]
		for (int j = 910; j < 927; j++)
		{
			Element.ListAttackElements.Add(EMono.sources.elements.map[j]); // [!code --]
			listAttackElements.Add(EMono.sources.elements.map[j]); // [!code ++]
		} // [!code ++]
		BaseModManager.PublishEvent("elin.source.imported"); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void Reload() // [!code ++]
	{ // [!code ++]
		initialized = false; // [!code ++]
		foreach (SourceData item in list) // [!code ++]
		{ // [!code ++]
			item.Reset(); // [!code ++]
		}
		Init(); // [!code ++]
	}

	public void ExportSourceTexts(string path)
```

## SourceMaterial

[`public override void OnImportData(SourceData data)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/SourceMaterial.cs#L113-L118)
```cs:line-numbers=113
	public void Init()
	{
		MatColors matColors = Core.Instance.Colors.matColors.TryGetValue(alias);
		if (matColors == null) // [!code ++]
		{ // [!code ++]
			matColors = new MatColors(); // [!code ++]
			string[] array = tag; // [!code ++]
			foreach (string text in array) // [!code ++]
			{ // [!code ++]
				if (text.StartsWith("addCol")) // [!code ++]
				{ // [!code ++]
					if (text.Contains("_Main")) // [!code ++]
					{ // [!code ++]
						matColors.main = text.ExtractInBetween('(', ')').ToColor(); // [!code ++]
					} // [!code ++]
					if (text.Contains("_Alt")) // [!code ++]
					{ // [!code ++]
						matColors.alt = text.ExtractInBetween('(', ')').ToColor(); // [!code ++]
					} // [!code ++]
				} // [!code ++]
			} // [!code ++]
		} // [!code ++]
		matColor = matColors.main;
		altColor = matColors.alt;
		SetTiles();
```

## SourceThingV

[`public override void OnAfterImportData()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/SourceThingV.cs#L100-L116)
```cs:line-numbers=100
	{
		dictionary[row2.id] = row2;
	}
	System.Reflection.FieldInfo[] fields = EClass.sources.things.rows[0].GetType().GetFields(); // [!code --]
	Dictionary<string, System.Reflection.FieldInfo> rowFields = new SourceThing.Row().GetRowFields(); // [!code ++]
	foreach (Row row3 in rows)
	{
		SourceThing.Row row = new SourceThing.Row();
		SourceThing.Row o = dictionary[row3._origin]; // [!code --]
		System.Reflection.FieldInfo[] array = fields; // [!code --]
		foreach (System.Reflection.FieldInfo fieldInfo in array) // [!code --]
		SourceThing.Row obj = dictionary[row3._origin]; // [!code ++]
		string key; // [!code ++]
		foreach (KeyValuePair<string, System.Reflection.FieldInfo> item in rowFields) // [!code ++]
		{
			if (!(fieldInfo.Name == "parse")) // [!code --]
			item.Deconstruct(out key, out var value); // [!code ++]
			string text = key; // [!code ++]
			System.Reflection.FieldInfo fieldInfo = value; // [!code ++]
			if (!(text == "parse")) // [!code ++]
			{
				row.SetField(fieldInfo.Name, o.GetField<object>(fieldInfo.Name)); // [!code --]
				fieldInfo.SetValue(row, fieldInfo.GetValue(obj)); // [!code ++]
			}
		}
		row.id = row3.id;
```

[`public override void OnAfterImportData()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/SourceThingV.cs#L198-L204)
```cs:line-numbers=198
		row.recipeKey = row3.recipeKey;
		if (!row3.parse.IsEmpty())
		{
			switch (row3._origin) // [!code --]
			key = row3._origin; // [!code ++]
			switch (key) // [!code ++]
			{
			case "lamp_ceil2":
			case "window":
```

[`public override void OnAfterImportData()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/SourceThingV.cs#L210-L267)
```cs:line-numbers=210
				string[] parse = row3.parse;
				for (int i = 0; i < parse.Length; i++)
				{
					string[] array2 = parse[i].Split('/'); // [!code --]
					switch (array2[0]) // [!code --]
					string[] array = parse[i].Split('/'); // [!code ++]
					switch (array[0]) // [!code ++]
					{
					case "elec":
						row.electricity = array2[1].ToInt(); // [!code --]
						row.electricity = array[1].ToInt(); // [!code ++]
						break;
					case "render":
						row._idRenderData = array2[1]; // [!code --]
						row._idRenderData = array[1]; // [!code ++]
						break;
					case "tiletype":
						row._tileType = array2[1]; // [!code --]
						row._tileType = array[1]; // [!code ++]
						break;
					case "anime":
						row.anime = ((array2.Length <= 4) ? ((array2.Length <= 3) ? new int[2] // [!code --]
						row.anime = ((array.Length <= 4) ? ((array.Length <= 3) ? new int[2] // [!code ++]
						{
							array2[1].ToInt(), // [!code --]
							array2[2].ToInt() // [!code --]
							array[1].ToInt(), // [!code ++]
							array[2].ToInt() // [!code ++]
						} : new int[3]
						{
							array2[1].ToInt(), // [!code --]
							array2[2].ToInt(), // [!code --]
							array2[3].ToInt() // [!code --]
							array[1].ToInt(), // [!code ++]
							array[2].ToInt(), // [!code ++]
							array[3].ToInt() // [!code ++]
						}) : new int[4]
						{
							array2[1].ToInt(), // [!code --]
							array2[2].ToInt(), // [!code --]
							array2[3].ToInt(), // [!code --]
							array2[4].ToInt() // [!code --]
							array[1].ToInt(), // [!code ++]
							array[2].ToInt(), // [!code ++]
							array[3].ToInt(), // [!code ++]
							array[4].ToInt() // [!code ++]
						});
						break;
					case "skin":
					{
						string[] array3 = array2[1].Split('|'); // [!code --]
						row.skins = new int[array3.Length]; // [!code --]
						for (int j = 0; j < array3.Length; j++) // [!code --]
						string[] array2 = array[1].Split('|'); // [!code ++]
						row.skins = new int[array2.Length]; // [!code ++]
						for (int j = 0; j < array2.Length; j++) // [!code ++]
						{
							row.skins[j] = array3[j].ToInt(); // [!code --]
							row.skins[j] = array2[j].ToInt(); // [!code ++]
						}
						break;
					}
					case "alt":
						row.altTiles = new int[1] { array2[1].ToInt() }; // [!code --]
						row.altTiles = new int[1] { array[1].ToInt() }; // [!code ++]
						row.ignoreAltFix = true;
						break;
					case "naming":
						row.naming = array2[1]; // [!code --]
						row.naming = array[1]; // [!code ++]
						break;
					case "ex":
						row.idActorEx = array2[1]; // [!code --]
						row.idActorEx = array[1]; // [!code ++]
						break;
					case "sound":
						row.idSound = array2[1]; // [!code --]
						row.idSound = array[1]; // [!code ++]
						break;
					case "color":
						row.colorMod = 100;
```

[`public override void OnAfterImportData()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/SourceThingV.cs#L276-L283)
```cs:line-numbers=276
					{
						int[] second = new int[2]
						{
							Core.GetCurrent().sources.elements.alias[array2[1]].id, // [!code --]
							array2[2].ToInt() // [!code --]
							Core.GetCurrent().sources.elements.alias[array[1]].id, // [!code ++]
							array[2].ToInt() // [!code ++]
						};
						row.elements = row.elements.Concat(second).ToArray();
						break;
```

## Steam

[`public static bool HasDLC(ID_DLC id)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Steam.cs#L50-L55)
```cs:line-numbers=50

	public static void GetAchievement(ID_Achievement id)
	{
		if (BaseCore.IsOffline) // [!code ++]
		{ // [!code ++]
			return; // [!code ++]
		} // [!code ++]
		try
		{
			foreach (AchievementObject achievement in Instance.steamworks.settings.achievements)
```

[`public static void GetAchievement(ID_Achievement id)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/Steam.cs#L63-L71)
```cs:line-numbers=63
				try
				{
					achievement.Unlock();
					achievement.Store(); // [!code ++]
					EClass.ui.Say("sys_acv".lang(achievement.Name), Resources.Load<Sprite>("Media/Graphics/Icon/Achievement/acv_" + id));
					SE.Play("achievement");
					achievement.Store(); // [!code --]
					return;
				}
				catch (Exception ex)
```

## TCText

[`public class TCText : TCUI`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/TCText.cs#L15-L20)
```cs:line-numbers=15

	public void Say(string s, float duration = 0f)
	{
		if (s.IsEmpty()) // [!code ++]
		{ // [!code ++]
			return; // [!code ++]
		} // [!code ++]
		PopItem p;
		switch (s[0])
		{
```

## TaskDig

[`public override void OnProgressComplete()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/TaskDig.cs#L175-L181)
```cs:line-numbers=175
	switch (mode)
	{
	case Mode.Default:
		EClass._map.SetBridge(pos.x, pos.z); // [!code --]
		EClass._map.SetBridge(pos.x, pos.z, 0, 0, 0, 0, 0); // [!code ++]
		break;
	case Mode.RemoveFloor:
		EClass._map.MineFloor(pos, owner);
```

## +TraitDeedLostRing

::: details File Created
```cs
public class TraitDeedLostRing : TraitTool
{
	public override void TrySetHeldAct(ActPlan p)
	{
		p.pos.ListCharas().ForEach(delegate(Chara a)
		{
			if (a.IsMarried)
			{
				p.TrySetAct("actReissue", delegate
				{
					EClass.pc.Say("use_whip", a, owner);
					a.Talk("death_other");
					EClass.pc.PlaySound("whip");
					a.PlayAnime(AnimeID.Shiver);
					a.PlaySound("dropRewardXmas");
					owner.ModNum(-1);
					Thing thing = ThingGen.Create("amulet_engagement");
					thing.Attune(a);
					thing.elements.ModBase(484, 3);
					if (thing.rarity < Rarity.Mythical)
					{
						thing.rarity = Rarity.Mythical;
					}
					EClass.player.DropReward(thing, silent: true);
					thing = ThingGen.Create("ring_engagement");
					thing.Attune(a);
					thing.elements.ModBase(484, 3);
					if (thing.rarity < Rarity.Mythical)
					{
						thing.rarity = Rarity.Mythical;
					}
					EClass.player.DropReward(thing);
					return false;
				}, a);
			}
		});
	}
}
```

:::
## TraitFigure

[``](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/46fc78bc948a336b328bd5dd9c73b4a325a76b0c/Elin/TraitFigure.cs#L3-L8)
```cs:line-numbers=3

public class TraitFigure : Trait
{
	public CardRenderer extraRenderer; // [!code ++]
 // [!code ++]
	public override bool CanBeHallucinated => false;

	public CardRow source => EClass.sources.cards.map.TryGetValue(owner.c_idRefCard);
```
