---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 1 file modified.
version: +EA 23.296 Nightly - Plugin.BaseCore
changes: SourceData
---

# +EA 23.296 Nightly - Plugin.BaseCore

April 18, 2026

1 file modified.

## Important Changes

**None.**
## SourceData

[`public virtual void ImportTexts(IReadOnlyDictionary<string, string> texts, strin`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/72fed2ac349a86d5957d0ade64fadd4cf67d4aa7/Elin/Plugins.basecore/SourceData.cs#L729-L734)
```cs:line-numbers=729
			return;
		}
		string name = GetType().DeclaringType.Name;
		string langCode = Lang.langCode; // [!code ++]
		foreach (var (text2, fieldInfo2) in rowFields)
		{
			if (!text2.EndsWith("_L"))
```

[`public virtual void ImportTexts(IReadOnlyDictionary<string, string> texts, strin`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/72fed2ac349a86d5957d0ade64fadd4cf67d4aa7/Elin/Plugins.basecore/SourceData.cs#L736-L749)
```cs:line-numbers=736
				continue;
			}
			string text3 = text2[..^2];
			if (rowFields.ContainsKey(text3) && rowFields.ContainsKey(text3 + "_JP")) // [!code --]
			if (!texts.TryGetValue($"{name}.{obj}.{text3}", out var value2) || value2.IsEmpty() || !rowFields.TryGetValue(text3, out var value3) || !rowFields.TryGetValue(text3 + "_JP", out var value4)) // [!code ++]
			{
				fieldInfo2.SetValue(this, null); // [!code --]
				if (texts.TryGetValue($"{name}.{obj}.{text3}", out var value2) && !value2.IsEmpty()) // [!code --]
				continue; // [!code ++]
			} // [!code ++]
			fieldInfo2.SetValue(this, null); // [!code ++]
			SetFieldText(fieldInfo2, value2); // [!code ++]
			if (!(langCode == "EN")) // [!code ++]
			{ // [!code ++]
				if (langCode == "JP") // [!code ++]
				{
					SetFieldText(fieldInfo2, value2); // [!code --]
					SetFieldText(value4, value2); // [!code ++]
				}
			}
			else // [!code ++]
			{ // [!code ++]
				SetFieldText(value3, value2); // [!code ++]
			} // [!code ++]
		}
		void SetFieldText(FieldInfo l, string value)
		{
```
