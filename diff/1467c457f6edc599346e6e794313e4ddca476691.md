---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 1 file modified.
version: +EA 23.250 Nightly Patch 1 - Plugin.BaseCore
changes: Lang
---

# +EA 23.250 Nightly Patch 1 - Plugin.BaseCore

December 20, 2025

1 file modified.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [Lang (1)](#lang)
```cs:no-line-numbers
public static string[] GetDialog(string idSheet, string idTopic) // [!code --]
public static ExcelData.Sheet GetDialogSheet(string idSheet) // [!code ++]
```
## Lang

[`public static string _ChangeNum(int prev, int now)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/1467c457f6edc599346e6e794313e4ddca476691/Elin/Plugins.basecore/Lang.cs#L202-L208)
```cs:line-numbers=202
		return prev + " ⇒ " + now;
	}

	public static string[] GetDialog(string idSheet, string idTopic) // [!code --]
	public static ExcelData.Sheet GetDialogSheet(string idSheet) // [!code ++]
	{
		if (excelDialog == null)
		{
```

[`public static string[] GetDialog(string idSheet, string idTopic)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/1467c457f6edc599346e6e794313e4ddca476691/Elin/Plugins.basecore/Lang.cs#L211-L219)
```cs:line-numbers=211
			excelDialog.path = path;
		}
		excelDialog.BuildMap(idSheet);
		ExcelData.Sheet sheet = excelDialog.sheets[idSheet]; // [!code --]
		return excelDialog.sheets[idSheet]; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static string[] GetDialog(string idSheet, string idTopic) // [!code ++]
	{ // [!code ++]
		ExcelData.Sheet dialogSheet = GetDialogSheet(idSheet); // [!code ++]
		string key = "text" + (isBuiltin ? ("_" + langCode) : "");
		Dictionary<string, string> dictionary = sheet.map.TryGetValue(idTopic); // [!code --]
		Dictionary<string, string> dictionary = dialogSheet.map.TryGetValue(idTopic); // [!code ++]
		if (dictionary == null || !dictionary.ContainsKey(key))
		{
			return new string[1] { idTopic };
```
