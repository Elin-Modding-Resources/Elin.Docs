---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 3 files modified.
version: +EA 23.283 Nightly Patch 2 - Plugin.BaseCore
changes: ExcelParser/IO/SourceData
---

# +EA 23.283 Nightly Patch 2 - Plugin.BaseCore

March 6, 2026

3 files modified.

## Important Changes

**None.**
## ExcelParser

[`public static void ReportIllFormat<T>(int id)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f60563898387a6667c03d582be0c7e696412b2d7/Elin/Plugins.basecore/ExcelParser.cs#L196-L205)
```cs:line-numbers=196
{
	StringBuilder stringBuilder = new StringBuilder();
	string name = typeof(T).Name;
	ICell cell = row.Cells.TryGet(id, returnNull: true); // [!code --]
	string value = ((row.RowNum < 3) ? ", SourceData begins at the 4th row. 3rd row is the default value row." : $", default:'{rowDefault.Cells.TryGet(id, returnNull: true)}'"); // [!code --]
	ICell cell = row?.Cells.TryGet(id, returnNull: true); // [!code ++]
	IRow obj = row; // [!code ++]
	string value = ((obj != null && obj.RowNum >= 3) ? $", default:'{rowDefault?.Cells.TryGet(id, returnNull: true)}'" : ", SourceData begins at the 4th row. 3rd row is the default value row."); // [!code ++]
	stringBuilder.AppendLine("$source ill-format file: " + path);
	stringBuilder.Append($"row#{row.RowNum + 1}, cell'{id + 1}'/'{ToLetterId(id)}', expected:'{name}', read:'{cell}'"); // [!code --]
	object[] array = new object[5]; // [!code ++]
	IRow obj2 = row; // [!code ++]
	array[0] = ((obj2 != null) ? new int?(obj2.RowNum + 1) : null); // [!code ++]
	array[1] = id + 1; // [!code ++]
	array[2] = ToLetterId(id); // [!code ++]
	array[3] = name; // [!code ++]
	array[4] = cell; // [!code ++]
	stringBuilder.Append(string.Format("row#{0}, cell'{1}'/'{2}', expected:'{3}', read:'{4}'", array)); // [!code ++]
	stringBuilder.AppendLine(value);
	Debug.LogError(stringBuilder);
}
```

## IO

[`public static byte[] ReadLZ4(byte[] bytes)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f60563898387a6667c03d582be0c7e696412b2d7/Elin/Plugins.basecore/IO.cs#L201-L217)
```cs:line-numbers=201

	public static bool IsCompressed(string path)
	{
		byte[] array; // [!code --]
		using (BinaryReader binaryReader = new BinaryReader(File.OpenRead(path))) // [!code --]
		using FileStream fileStream = File.OpenRead(path); // [!code ++]
		if (fileStream.Length == 0L) // [!code ++]
		{
			binaryReader.BaseStream.Seek(0L, SeekOrigin.Begin); // [!code --]
			array = binaryReader.ReadBytes(4); // [!code --]
			return false; // [!code ++]
		}
		if (array.Length > 3 && array[0] == 123 && array[1] == 13 && array[2] == 10 && array[3] == 32) // [!code --]
		bool flag; // [!code ++]
		while (true) // [!code ++]
		{
			return false; // [!code --]
			switch (fileStream.ReadByte()) // [!code ++]
			{ // [!code ++]
			case 9: // [!code ++]
			case 10: // [!code ++]
			case 13: // [!code ++]
			case 32: // [!code ++]
				continue; // [!code ++]
			case -1: // [!code ++]
				return false; // [!code ++]
			case 34: // [!code ++]
			case 45: // [!code ++]
			case 48: // [!code ++]
			case 49: // [!code ++]
			case 50: // [!code ++]
			case 51: // [!code ++]
			case 52: // [!code ++]
			case 53: // [!code ++]
			case 54: // [!code ++]
			case 55: // [!code ++]
			case 56: // [!code ++]
			case 57: // [!code ++]
			case 91: // [!code ++]
			case 102: // [!code ++]
			case 110: // [!code ++]
			case 116: // [!code ++]
			case 123: // [!code ++]
				flag = true; // [!code ++]
				break; // [!code ++]
			default: // [!code ++]
				flag = false; // [!code ++]
				break; // [!code ++]
			} // [!code ++]
			break; // [!code ++]
		}
		return true; // [!code --]
		return !flag; // [!code ++]
	}

	public static void Compress(string path, string text)
```

## SourceData

[`public virtual void RemoveDuplicateRows()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/f60563898387a6667c03d582be0c7e696412b2d7/Elin/Plugins.basecore/SourceData.cs#L194-L202)
```cs:line-numbers=194
{
	HashSet<T2> hashSet = new HashSet<T2>();
	List<T> list = new List<T>(rows.Count);
	bool flag = typeof(T).DeclaringType.Name.Contains("Lang"); // [!code --]
	string arg = GetType().Name;
	System.Reflection.FieldInfo field = typeof(T).GetField("id");
	bool flag = typeof(LangRow).IsAssignableFrom(typeof(T)); // [!code ++]
	if (field == null) // [!code ++]
	{ // [!code ++]
		return; // [!code ++]
	} // [!code ++]
	if (field.FieldType != typeof(T2))
	{
		Debug.LogError($"#source override: {arg} id field mismatch {field.FieldType} != {typeof(T2)}");
```
