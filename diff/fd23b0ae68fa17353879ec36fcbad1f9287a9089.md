---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 2 files modified.
version: +EA 23.295 Stable - Plugin.BaseCore
changes: CorePath/Lang
---

# +EA 23.295 Stable - Plugin.BaseCore

April 11, 2026

2 files modified.

## Important Changes

**None.**
## CorePath

[`public class CorePackage`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/fd23b0ae68fa17353879ec36fcbad1f9287a9089/Elin/Plugins.basecore/CorePath.cs#L48-L64)
```cs:line-numbers=48
	{
		public static string TextData => Lang.setting.dir + "Data/";

		public static string TextDialog // [!code --]
		{ // [!code --]
			get // [!code --]
			{ // [!code --]
				if (Lang.isBuiltin) // [!code --]
				{ // [!code --]
					return packageCore + "Lang/_Dialog/"; // [!code --]
				} // [!code --]
				return Lang.setting.dir + "Dialog/"; // [!code --]
			} // [!code --]
		} // [!code --]
		public static string TextDialog => packageCore + "Lang/_Dialog/"; // [!code ++]
 // [!code ++]
		public static string TextDialogLocal => Lang.setting.dir + "Dialog/"; // [!code ++]

		public static string TextNarration => Lang.setting.dir + "Narration/";

```

## Lang

[`using System;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/fd23b0ae68fa17353879ec36fcbad1f9287a9089/Elin/Plugins.basecore/Lang.cs#L1-L6)
```cs:line-numbers=1
using System;
using System.Collections.Generic;
using System.Linq; // [!code --]
using System.Text;

public class Lang
```

[`public static ExcelData.Sheet GetDialogSheet(string idSheet)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/fd23b0ae68fa17353879ec36fcbad1f9287a9089/Elin/Plugins.basecore/Lang.cs#L211-L221)
```cs:line-numbers=211
	{
		excelDialog = new ExcelData(CorePath.CorePackage.TextDialog + "dialog.xlsx");
		excelDialog.LoadBook();
		List<ExcelData> list = new List<ExcelData>(); // [!code ++]
		if (!isBuiltin) // [!code ++]
		{ // [!code ++]
			list.Add(new ExcelData(CorePath.CorePackage.TextDialogLocal + "dialog.xlsx")); // [!code ++]
		} // [!code ++]
		foreach (string extraExcelDialog in extraExcelDialogs) // [!code ++]
		{ // [!code ++]
			list.Add(new ExcelData(extraExcelDialog)); // [!code ++]
		} // [!code ++]
		for (int i = 0; i < excelDialog.book.NumberOfSheets; i++)
		{
			string sheetName = excelDialog.book.GetSheetAt(i).SheetName;
			excelDialog.BuildMap(sheetName);
			foreach (ExcelData item in extraExcelDialogs.Select((string f) => new ExcelData(f))) // [!code --]
			foreach (ExcelData item in list) // [!code ++]
			{
				item.BuildMap(sheetName);
				ExcelData.Sheet sheet = item.sheets[sheetName];
```
