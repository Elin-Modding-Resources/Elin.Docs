---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 22 files modified.
version: EA 23.301 Nightly
changes: ActEffect/ActMelee/AttackProcess/BaseTileMap/Card/ConTransmute/ConTransmuteHuman/CoreDebug/ENC/FACTION/FEAT/InvOwnerDeliver/InvOwnerDraglet/LayerDragGrid/SKILL/SourceBlock/SourceFloor/SourceObj/SourceThingV/Thing/Trait/WidgetEquip
---

# EA 23.301 Nightly

May 2, 2026

22 files modified.

## Important Changes

**None.**
## ActEffect

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/ActEffect.cs#L1403-L1408)
```cs:line-numbers=1403
		});
		Thing thing4 = ThingGen.Create(tc.id, -1, (int)((long)num6 * (long)power / 400));
		thing4.genLv = num6;
		thing4.idSkin = tc.idSkin; // [!code ++]
		if (tc.c_uidAttune != 0)
		{
			thing4.c_uidAttune = tc.c_uidAttune;
```

## ActMelee

[`void _Attack(BodySlot slot)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/ActMelee.cs#L207-L221)
```cs:line-numbers=207
			int num = 1 + Mathf.Max(Act.CC.Evalue(666), (w != null) ? w.Evalue(666) : 0);
			if (!UseWeaponDist || dist <= 1 || dist <= num || intercepted)
			{
				int num2 = GetWeaponEnc(606, addSelfEnc: false); // [!code --]
				int scatter = GetWeaponEnc(607, addSelfEnc: false); // [!code --]
				int num2 = GetWeaponEnc(606, addSelfEnc: true); // [!code ++]
				int scatter = GetWeaponEnc(607, addSelfEnc: true); // [!code ++]
				splash = GetWeaponEnc(608, addSelfEnc: true);
				chaser = GetWeaponEnc(620, addSelfEnc: true);
				flurry = GetWeaponEnc(621, addSelfEnc: true);
				frustration = GetWeaponEnc(624, addSelfEnc: true);
				int num3 = GetWeaponEnc(622, addSelfEnc: true);
				feint = GetWeaponEnc(623, addSelfEnc: true);
				knockback = Act.CC.Evalue(603); // [!code --]
				knockback = Act.CC.Evalue(603, ignoreGlobalElement: true); // [!code ++]
				mod_talisman = GetWeaponEnc(609, addSelfEnc: true);
				List<Point> list2 = EClass._map.ListPointsInLine(Act.CC.pos, Act.TC.pos, num2 / 10 + ((num2 % 10 > EClass.rnd(10)) ? 1 : 0) + 1);
				if (w != null)
```

## AttackProcess

[`public bool Perform(int count, bool hasHit, float dmgMulti = 1f, bool maxRoll =`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/AttackProcess.cs#L623-L628)
```cs:line-numbers=623
		num2 += weapon.Evalue(91, ignoreGlobalElement: true);
		num3 += weapon.Evalue(603, ignoreGlobalElement: true);
	}
	foreach (Element value in CC.elements.dict.Values) // [!code ++]
	{ // [!code ++]
		if (!value.IsGlobalElement && value.source.categorySub == "eleAttack") // [!code ++]
		{ // [!code ++]
			list.Add(value); // [!code ++]
		} // [!code ++]
	} // [!code ++]
	if (!IsRanged && !isThrow && CC.HasElement(1426))
	{
		list.Add(Element.Create(6650, 100));
```

[`public bool Perform(int count, bool hasHit, float dmgMulti = 1f, bool maxRoll =`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/AttackProcess.cs#L647-L657)
```cs:line-numbers=647
	}
	if (CC.IsPCFaction)
	{
		foreach (Element value in EClass.pc.faction.charaElements.dict.Values) // [!code --]
		foreach (Element value2 in EClass.pc.faction.charaElements.dict.Values) // [!code ++]
		{
			if (value.Value > 0) // [!code --]
			if (value2.Value > 0) // [!code ++]
			{
				list.Add(value); // [!code --]
				list.Add(value2); // [!code ++]
			}
		}
	}
```

[`public bool Perform(int count, bool hasHit, float dmgMulti = 1f, bool maxRoll =`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/AttackProcess.cs#L721-L733)
```cs:line-numbers=721
	ConWeapon conWeapon = null;
	if (weapon != null)
	{
		foreach (Element value2 in weapon.elements.dict.Values) // [!code --]
		foreach (Element value3 in weapon.elements.dict.Values) // [!code ++]
		{
			if (value2.source.categorySub == "eleConvert") // [!code --]
			if (value3.source.categorySub == "eleConvert") // [!code ++]
			{
				num4 = EClass.sources.elements.alias[value2.source.aliasRef].id; // [!code --]
				num5 = 50 + value2.Value * 2; // [!code --]
				num6 = Mathf.Min(value2.Value, 100); // [!code --]
				num4 = EClass.sources.elements.alias[value3.source.aliasRef].id; // [!code ++]
				num5 = 50 + value3.Value * 2; // [!code ++]
				num6 = Mathf.Min(value3.Value, 100); // [!code ++]
				break;
			}
		}
```

## BaseTileMap

[`public virtual void DrawTile()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/BaseTileMap.cs#L1281-L3220)
```cs:line-numbers=1281
			fogged = true;
		}
	}
	goto IL_7b1b; // [!code --]
	IL_6f8a: // [!code --]
	int num3; // [!code --]
	if (!showRoof || !roof || this.cell.room == null || this.cell.Front.room == null || this.cell.Right.room == null) // [!code --]
	goto IL_7b6a; // [!code ++]
	IL_169b: // [!code ++]
	if (this.cell.isSlopeEdge) // [!code ++]
	{
		param.tile = num3; // [!code --]
		rendererFov.Draw(param); // [!code --]
		float num3 = (float)height * _heightMod.y; // [!code ++]
		orgY = param.y; // [!code ++]
		orgZ = param.z; // [!code ++]
		param.dir = this.cell.blockDir; // [!code ++]
		if (snowed) // [!code ++]
		{ // [!code ++]
			param.color = floorLight; // [!code ++]
		} // [!code ++]
		SourceBlock.Row defBlock; // [!code ++]
		if (sourceBlock.tileType.IsFullBlock) // [!code ++]
		{ // [!code ++]
			defBlock = sourceBlock; // [!code ++]
			param.mat = matBlock; // [!code ++]
			param.tile = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length]; // [!code ++]
			param.matColor = ((sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, sourceBlock.colorMod)); // [!code ++]
		} // [!code ++]
		else // [!code ++]
		{ // [!code ++]
			defBlock = sourceFloor._defBlock; // [!code ++]
			param.mat = matFloor; // [!code ++]
			param.tile = defBlock._tiles[this.cell.blockDir % defBlock._tiles.Length]; // [!code ++]
			if (defBlock.id != 1) // [!code ++]
			{ // [!code ++]
				param.matColor = ((sourceFloor.colorMod == 0) ? 104025 : GetColorInt(ref matFloor.matColor, sourceFloor.colorMod)); // [!code ++]
			} // [!code ++]
			else // [!code ++]
			{ // [!code ++]
				param.matColor = 104025f; // [!code ++]
			} // [!code ++]
		} // [!code ++]
		for (int j = 0; (float)j < num3 / heightBlockSize; j++) // [!code ++]
		{ // [!code ++]
			param.y += ugFix.y; // [!code ++]
			param.z += ugFix.z + slopeFixZ * (float)j; // [!code ++]
			defBlock.renderData.Draw(param); // [!code ++]
			if (this.cell.pcSync && EMono.player.lightPower > 0f) // [!code ++]
			{ // [!code ++]
				float num4 = param.tile; // [!code ++]
				param.tile = 0f; // [!code ++]
				rendererFov.Draw(param); // [!code ++]
				param.tile = num4; // [!code ++]
			} // [!code ++]
		} // [!code ++]
		param.y = orgY; // [!code ++]
		param.z = orgZ; // [!code ++]
	}
	goto IL_6fea; // [!code --]
	IL_7b1b: // [!code --]
	if (detail.things.Count == 0 && detail.charas.Count == 0) // [!code --]
	param.color = floorLight; // [!code ++]
	if (!isWater && (this.cell.Front.sourceFloor.tileType.IsWater || this.cell.Right.sourceFloor.tileType.IsWater) && this.cell.sourceBlock.tileType.RenderWaterBlock && !flag) // [!code ++]
	{
		return; // [!code --]
		orgY = param.y; // [!code ++]
		orgZ = param.z; // [!code ++]
		int num5 = 0; // [!code ++]
		if (sourceBlock.tileType.IsFullBlock) // [!code ++]
		{ // [!code ++]
			SourceBlock.Row row3 = sourceBlock; // [!code ++]
			num5 = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length]; // [!code ++]
		} // [!code ++]
		else // [!code ++]
		{ // [!code ++]
			SourceBlock.Row row3 = sourceFloor._defBlock; // [!code ++]
			num5 = row3._tiles[this.cell.blockDir % row3._tiles.Length]; // [!code ++]
		} // [!code ++]
		if (((this.cell.Front.shore / 12) & 1) == 0 && this.cell.Front.sourceFloor.tileType.IsWater && this.cell.Front.height <= height && this.cell.Front.sourceBlock.tileType.RenderWaterBlock) // [!code ++]
		{ // [!code ++]
			param.y = (float)(cz - cx) * screen.tileAlign.y - (this.cell.Front.sourceFloor.tileType.IsDeepWater ? 0.6f : 0.4f) + (float)(int)this.cell.Front.height * _heightMod.y; // [!code ++]
			param.z = 1000f + param.x * screen.tileWeight.x + param.y * screen.tileWeight.z; // [!code ++]
			param.tile = num5 + ((!this.cell.Front.sourceFloor.tileType.IsDeepWater) ? 3000000 : 0); // [!code ++]
			rendererWaterBlock.Draw(param); // [!code ++]
		} // [!code ++]
		if (((this.cell.Right.shore / 12) & 8) == 0 && this.cell.Right.sourceFloor.tileType.IsWater && this.cell.Right.height <= height && this.cell.Right.sourceBlock.tileType.RenderWaterBlock) // [!code ++]
		{ // [!code ++]
			param.y = (float)(cz - cx) * screen.tileAlign.y - (this.cell.Right.sourceFloor.tileType.IsDeepWater ? 0.6f : 0.4f) + (float)(int)this.cell.Right.height * _heightMod.y; // [!code ++]
			param.z = 1000f + param.x * screen.tileWeight.x + param.y * screen.tileWeight.z; // [!code ++]
			param.tile = num5 + ((!this.cell.Right.sourceFloor.tileType.IsDeepWater) ? 3000000 : 0); // [!code ++]
			rendererWaterBlock.Draw(param); // [!code ++]
		} // [!code ++]
		param.y = orgY; // [!code ++]
		param.z = orgZ; // [!code ++]
	}
	int num4 = 0; // [!code --]
	thingPos.x = 0f; // [!code --]
	thingPos.y = 0f; // [!code --]
	thingPos.z = 0f; // [!code --]
	freePos.x = (freePos.y = (freePos.z = 0f)); // [!code --]
	if (this.cell.HasRamp) // [!code --]
	if (showBorder && !this.cell.outOfBounds) // [!code ++]
	{
		Vector3 rampFix = sourceBlock.tileType.GetRampFix(this.cell.blockDir); // [!code --]
		param.x += rampFix.x; // [!code --]
		param.y += rampFix.y; // [!code --]
		param.z += rampFix.z; // [!code --]
		freePos.x += rampFix.x; // [!code --]
		freePos.y += rampFix.y; // [!code --]
		freePos.z += rampFix.z; // [!code --]
		param.matColor = 104025f; // [!code ++]
		if (cx == EMono._map.bounds.x) // [!code ++]
		{ // [!code ++]
			renderBorder.Draw(param, 12 + (EMono.world.date.IsNight ? 4 : 0)); // [!code ++]
		} // [!code ++]
		else if (cx == EMono._map.bounds.maxX) // [!code ++]
		{ // [!code ++]
			renderBorder.Draw(param, 13 + (EMono.world.date.IsNight ? 4 : 0)); // [!code ++]
		} // [!code ++]
		if (cz == EMono._map.bounds.z) // [!code ++]
		{ // [!code ++]
			renderBorder.Draw(param, 14 + (EMono.world.date.IsNight ? 4 : 0)); // [!code ++]
		} // [!code ++]
		else if (cz == EMono._map.bounds.maxZ) // [!code ++]
		{ // [!code ++]
			renderBorder.Draw(param, 15 + (EMono.world.date.IsNight ? 4 : 0)); // [!code ++]
		} // [!code ++]
	}
	param.y += (flag ? 0f : ((this.cell._bridge != 0) ? this.cell.sourceBridge.tileType.FloorHeight : sourceFloor.tileType.FloorHeight)); // [!code --]
	orgPos.x = (orgX = param.x); // [!code --]
	orgPos.y = (orgY = param.y); // [!code --]
	orgPos.z = (orgZ = param.z); // [!code --]
	if (flag && liquidLv > 0) // [!code --]
	if (this.cell.isSkyFloor || (detail != null && detail.anime != null && detail.anime.drawBlock)) // [!code ++]
	{
		if (liquidLv > 10) // [!code --]
		orgY = param.y; // [!code ++]
		orgZ = param.z; // [!code ++]
		SourceBlock.Row defBlock2 = sourceFloor._defBlock; // [!code ++]
		param.mat = matFloor; // [!code ++]
		param.tile = defBlock2._tiles[this.cell.blockDir % defBlock2._tiles.Length]; // [!code ++]
		if (defBlock2.id != 1) // [!code ++]
		{
			liquidLv = TileType.FloorWaterShallow.LiquidLV * 10; // [!code --]
			param.matColor = ((sourceFloor.colorMod == 0) ? 104025 : GetColorInt(ref matFloor.matColor, sourceFloor.colorMod)); // [!code ++]
		}
		liquidLv -= (int)(floatY * 0.5f); // [!code --]
		param.liquidLv = liquidLv; // [!code --]
		param.y -= TileType.FloorWaterShallow.FloorHeight; // [!code --]
		else // [!code ++]
		{ // [!code ++]
			param.matColor = 104025f; // [!code ++]
		} // [!code ++]
		for (int k = 0; k < ((!this.cell.isSkyFloor) ? 1 : EMono._map.config.skyBlockHeight); k++) // [!code ++]
		{ // [!code ++]
			param.y += ugFix.y; // [!code ++]
			param.z += ugFix.z + slopeFixZ * (float)k; // [!code ++]
			defBlock2.renderData.Draw(param); // [!code ++]
		} // [!code ++]
		param.y = orgY; // [!code ++]
		param.z = orgZ; // [!code ++]
	}
	Thing thing = null; // [!code --]
	bool shadow = liquidLv == 0; // [!code --]
	float num5 = 0f; // [!code --]
	float num6 = 0f; // [!code --]
	float num7 = 0f; // [!code --]
	float num8 = 0f; // [!code --]
	bool flag6 = false; // [!code --]
	float num9 = 0f; // [!code --]
	bool flag7 = false; // [!code --]
	float num10 = 0f; // [!code --]
	if (detail.things.Count > 0 && isSeen) // [!code --]
	if (!sourceFloor.tileType.IsSkipFloor) // [!code ++]
	{
		_ = zSetting.max1; // [!code --]
		float num11 = 0f; // [!code --]
		for (int j = 0; j < detail.things.Count; j++) // [!code --]
		if ((hasBridge && sourceBridge.tileType.CastShadowSelf) || this.cell.castFloorShadow) // [!code ++]
		{
			Thing t = detail.things[j]; // [!code --]
			if ((fogged && !t.isRoofItem) || ((t.isHidden || t.trait.HideInAdv || t.isMasked) && !EMono.scene.actionMode.ShowMaskedThings) || (t.isRoofItem && ((this.room == null && !sourceBlock.tileType.IsFullBlock && !EMono._zone.IsPCFaction && !buildMode) || (lowBlock && !showFullWall && this.room != null) || (noRoofMode && currentRoom == null))) || (flag3 && !t.isRoofItem)) // [!code --]
			floorLight2 = _lightMod * light * 0.2f + _baseBrightness + _shadowStrength * floorShadowStrength * (isWater ? 0.7f : (hasBridge ? 1f : (0.6f * (1f - nightRatio)))); // [!code ++]
			if (snowed) // [!code ++]
			{
				continue; // [!code --]
				floorLight2 = (int)((double)floorLight2 * 0.85 * 50.0) * 262144 + snowColorToken; // [!code ++]
			}
			TileType tileType = t.trait.tileType; // [!code --]
			bool isInstalled = t.IsInstalled; // [!code --]
			SourcePref pref = t.Pref; // [!code --]
			if (!isInstalled && t.category.tileDummy != 0) // [!code --]
			else // [!code ++]
			{
				pref = rendererObjDummy.shadowPref; // [!code --]
				floorLight2 = (int)(floorLight2 * 50f) * 262144 + ((this.cell.lightR >= 64) ? 63 : this.cell.lightR) * 4096 + ((this.cell.lightG >= 64) ? 63 : this.cell.lightG) * 64 + ((this.cell.lightB >= 64) ? 63 : this.cell.lightB); // [!code ++]
			}
			float num12 = ((tileType.UseMountHeight && isInstalled) ? 0f : ((pref.height < 0f) ? 0f : ((pref.height == 0f) ? 0.1f : pref.height))); // [!code --]
			if (t.ignoreStackHeight) // [!code --]
			param.color = floorLight2; // [!code ++]
			if (this.cell.lotShade) // [!code ++]
			{
				thingPos.y -= num5; // [!code --]
				thingPos -= altitudeFix * num6; // [!code --]
				floorLight = floorLight2; // [!code ++]
			}
			shadow = thingPos.y < 0.16f && num10 < 0.16f; // [!code --]
			_ = pref.bypassShadow; // [!code --]
			param.shadowFix = 0f - thingPos.y; // [!code --]
			param.liquidLv = ((thingPos.y + (float)t.altitude < 0.1f) ? liquidLv : 0); // [!code --]
			if (t.isRoofItem) // [!code --]
		} // [!code ++]
		floorMatColor = ((sourceFloor.colorMod == 0) ? 104025 : GetColorInt(ref matFloor.matColor, sourceFloor.colorMod)); // [!code ++]
		if (isWater && flag) // [!code ++]
		{ // [!code ++]
			param.y -= 0.01f * floatY; // [!code ++]
		} // [!code ++]
		if (!sourceBlock.tileType.IsSkipFloor || sourceBlock.transparent || hasBridge || this.cell.hasDoor || this.cell.skipRender) // [!code ++]
		{ // [!code ++]
			param.mat = matFloor; // [!code ++]
			param.tile = sourceFloor._tiles[floorDir % sourceFloor._tiles.Length]; // [!code ++]
			param.matColor = floorMatColor; // [!code ++]
			param.snow = snowed; // [!code ++]
			if (sourceFloor.anime.Length != 0) // [!code ++]
			{
				param.snow = isSnowCovered && !this.cell.isClearSnow; // [!code --]
				SetRoofHeight(param, this.cell, cx, cz); // [!code --]
				_actorPos.x = param.x; // [!code --]
				_actorPos.y = param.y; // [!code --]
				_actorPos.z = param.z + num11; // [!code --]
				if (this.room != null) // [!code --]
				int num6 = (int)(Time.realtimeSinceStartup * 1000f / (float)sourceFloor.anime[1] % (float)sourceFloor.anime[0]); // [!code ++]
				param.tile += num6; // [!code ++]
			} // [!code ++]
			if (this.cell.isDeck) // [!code ++]
			{ // [!code ++]
				param.z += 1f; // [!code ++]
				if ((bool)sourceFloor.renderData.subData) // [!code ++]
				{
					param.color = GetRoofLight(this.room.lot); // [!code --]
					sourceFloor.renderData.subData.Draw(param); // [!code ++]
				}
				shadow = false; // [!code --]
				param.liquidLv = 0; // [!code --]
				sourceFloor.renderData.Draw(param); // [!code ++]
				param.z -= 1f; // [!code ++]
			}
			else
			{
				param.snow = snowed; // [!code --]
				_actorPos.x = orgX + num8; // [!code --]
				_actorPos.y = orgY; // [!code --]
				_actorPos.z = orgZ + num11 + thingPos.z; // [!code --]
				if (tileType.CanStack || !isInstalled) // [!code --]
				if ((bool)sourceFloor.renderData.subData) // [!code ++]
				{
					if (thing?.id != t.id) // [!code --]
					sourceFloor.renderData.subData.Draw(param); // [!code ++]
				} // [!code ++]
				sourceFloor.renderData.Draw(param); // [!code ++]
			} // [!code ++]
			int num7 = 0; // [!code ++]
			if (isSnowCovered && sourceFloor == FLOOR.sourceSnow && !this.cell.hasDoor) // [!code ++]
			{ // [!code ++]
				if (!this.cell.Right.IsSnowTile && this.cell.Right.topHeight == this.cell.topHeight) // [!code ++]
				{ // [!code ++]
					num7++; // [!code ++]
				} // [!code ++]
				if (!this.cell.Front.IsSnowTile && this.cell.Front.topHeight == this.cell.topHeight) // [!code ++]
				{ // [!code ++]
					num7 += 2; // [!code ++]
				} // [!code ++]
				if (num7 != 0) // [!code ++]
				{ // [!code ++]
					param.tile = 448 + num7 + 12; // [!code ++]
					param.z -= 0.1f; // [!code ++]
					sourceFloor.renderData.Draw(param); // [!code ++]
					param.z += 0.1f; // [!code ++]
				} // [!code ++]
			} // [!code ++]
			if (this.cell.shadow != 0 && !hasBridge && !this.cell.skipRender) // [!code ++]
			{ // [!code ++]
				if (snowed) // [!code ++]
				{ // [!code ++]
					if (sourceFloor == FLOOR.sourceSnow) // [!code ++]
					{
						_actorPos.x += thingPos.x; // [!code --]
						param.tile = 448 + this.cell.shadow + 8 + (this.cell.HasFence ? 4 : 0); // [!code ++]
						param.z -= 0.01f; // [!code ++]
						sourceFloor.renderData.Draw(param); // [!code ++]
					}
					_actorPos.y += thingPos.y; // [!code --]
					if (t.trait.IgnoreLastStackHeight && (thing == null || !thing.trait.IgnoreLastStackHeight)) // [!code --]
				} // [!code ++]
				else // [!code ++]
				{ // [!code ++]
					pass = passEdge; // [!code ++]
					batch = pass.batches[pass.batchIdx]; // [!code ++]
					batch.matrices[pass.idx].m03 = param.x + ambientShadowFix[this.cell.shadow].x; // [!code ++]
					batch.matrices[pass.idx].m13 = param.y + ambientShadowFix[this.cell.shadow].y; // [!code ++]
					batch.matrices[pass.idx].m23 = param.z + ambientShadowFix[this.cell.shadow].z; // [!code ++]
					batch.tiles[pass.idx] = 448 + this.cell.shadow; // [!code ++]
					batch.colors[pass.idx] = param.color; // [!code ++]
					batch.matColors[pass.idx] = 104025f; // [!code ++]
					pass.idx++; // [!code ++]
					if (pass.idx == pass.batchSize) // [!code ++]
					{
						thingPos.y -= num5; // [!code --]
						if (thing != null) // [!code --]
						{ // [!code --]
							_actorPos.z -= 0.2f; // [!code --]
							thingPos.z -= 0.2f; // [!code --]
						} // [!code --]
						_actorPos.y -= num5; // [!code --]
						pass.NextBatch(); // [!code ++]
					}
					_actorPos.z += renderSetting.thingZ + (float)j * -0.01f + zSetting.mod1 * thingPos.y; // [!code --]
				}
				if (isInstalled) // [!code --]
				if (!sourceFloor.ignoreTransition && !snowed) // [!code ++]
				{
					if (t.TileType.IsRamp) // [!code --]
					Cell back = this.cell.Back; // [!code ++]
					if (back.sourceBlock.transition[0] != -1 && back.isSeen && !back.hasDoor) // [!code ++]
					{
						Vector3 rampFix2 = t.TileType.GetRampFix(t.dir, pref); // [!code --]
						orgX += rampFix2.x; // [!code --]
						orgY += rampFix2.y; // [!code --]
						orgZ += rampFix2.z; // [!code --]
						freePos.x += rampFix2.x; // [!code --]
						freePos.y += rampFix2.y; // [!code --]
						freePos.z += rampFix2.z; // [!code --]
						if (!this.cell.IsTopWater || t.altitude > 0) // [!code --]
						{ // [!code --]
							num10 += rampFix2.y; // [!code --]
						} // [!code --]
						liquidLv -= (int)(rampFix2.y * 150f); // [!code --]
						if (liquidLv < 0) // [!code --]
						{ // [!code --]
							liquidLv = 0; // [!code --]
						} // [!code --]
					} // [!code --]
					else if (!flag7 && t.trait.IsChangeFloorHeight && !t.ignoreStackHeight) // [!code --]
					{ // [!code --]
						orgY += num12 + (float)t.altitude * altitudeFix.y; // [!code --]
						orgZ += (float)t.altitude * altitudeFix.z; // [!code --]
						freePos.y += num12 + (float)t.altitude * altitudeFix.y; // [!code --]
						if (!this.cell.IsTopWater || t.altitude > 0) // [!code --]
						{ // [!code --]
							num10 += num12 + (float)t.altitude * altitudeFix.y; // [!code --]
						} // [!code --]
						_actorPos.x += pref.x * (float)((!t.flipX) ? 1 : (-1)); // [!code --]
						_actorPos.z += pref.z; // [!code --]
						thingPos.z += pref.z; // [!code --]
						liquidLv -= (int)(num12 * 150f); // [!code --]
						if (liquidLv < 0) // [!code --]
						pass = passFloor; // [!code ++]
						batch = pass.batches[pass.batchIdx]; // [!code ++]
						batch.matrices[pass.idx].m03 = param.x + transitionFix[0].x; // [!code ++]
						batch.matrices[pass.idx].m13 = param.y + transitionFix[0].y; // [!code ++]
						batch.matrices[pass.idx].m23 = param.z + transitionFix[0].z; // [!code ++]
						batch.tiles[pass.idx] = 480 + back.sourceBlock.transition[0] + Rand.bytes[index % Rand.MaxBytes] % back.sourceBlock.transition[1]; // [!code ++]
						batch.colors[pass.idx] = param.color; // [!code ++]
						batch.matColors[pass.idx] = GetColorInt(ref back.matBlock.matColor, back.sourceBlock.colorMod); // [!code ++]
						pass.idx++; // [!code ++]
						if (pass.idx == pass.batchSize) // [!code ++]
						{
							liquidLv = 0; // [!code --]
							pass.NextBatch(); // [!code ++]
						}
					}
					else // [!code --]
					back = this.cell.Left; // [!code ++]
					if (back.sourceBlock.transition[0] != -1 && back.isSeen && !back.hasDoor) // [!code ++]
					{
						thingPos.y += num12; // [!code --]
						if (tileType.UseMountHeight) // [!code --]
						{ // [!code --]
							if ((tileType != TileType.Illumination || !this.cell.HasObj) && !tileType.AlwaysShow) // [!code --]
							{ // [!code --]
								if (noRoofMode && currentRoom == null && t.altitude >= lowWallObjAltitude && tileType != TileType.ObjFloat) // [!code --]
								{ // [!code --]
									continue; // [!code --]
								} // [!code --]
								if (hideHang && (this.cell.room?.lot != currentLot || (!this.cell.lotWall && this.cell.room != currentRoom))) // [!code --]
								{ // [!code --]
									Room room = ((t.dir == 0) ? this.cell.Back.room : this.cell.Left.room); // [!code --]
									if (t.trait.AlwaysHideOnLowWall) // [!code --]
									{ // [!code --]
										if (room == null || !room.data.showWallItem) // [!code --]
										{ // [!code --]
											continue; // [!code --]
										} // [!code --]
									} // [!code --]
									else if (t.altitude >= lowWallObjAltitude) // [!code --]
									{ // [!code --]
										continue; // [!code --]
									} // [!code --]
								} // [!code --]
							} // [!code --]
							if (tileType.UseHangZFix) // [!code --]
							{ // [!code --]
								flag6 = true; // [!code --]
							} // [!code --]
							tileType.GetMountHeight(ref _actorPos, Point.shared.Set(index), t.dir, t); // [!code --]
							shadow = false; // [!code --]
							param.liquidLv = 0; // [!code --]
							if (t.freePos) // [!code --]
							{ // [!code --]
								_actorPos.x += t.fx; // [!code --]
								_actorPos.y += t.fy; // [!code --]
							} // [!code --]
						} // [!code --]
						else // [!code --]
						{ // [!code --]
							thingPos.y += (float)t.altitude * altitudeFix.y; // [!code --]
							thingPos.z += (float)t.altitude * altitudeFix.z; // [!code --]
						} // [!code --]
						_actorPos.x += pref.x * (float)((!t.flipX) ? 1 : (-1)); // [!code --]
						_actorPos.z += pref.z; // [!code --]
						if (pref.height >= 0f) // [!code --]
						pass = passFloor; // [!code ++]
						batch = pass.batches[pass.batchIdx]; // [!code ++]
						batch.matrices[pass.idx].m03 = param.x + transitionFix[1].x; // [!code ++]
						batch.matrices[pass.idx].m13 = param.y + transitionFix[1].y; // [!code ++]
						batch.matrices[pass.idx].m23 = param.z + transitionFix[1].z; // [!code ++]
						batch.tiles[pass.idx] = 512 + back.sourceBlock.transition[0] + Rand.bytes[index % Rand.MaxBytes] % back.sourceBlock.transition[1]; // [!code ++]
						batch.colors[pass.idx] = param.color; // [!code ++]
						batch.matColors[pass.idx] = GetColorInt(ref back.matBlock.matColor, back.sourceBlock.colorMod); // [!code ++]
						pass.idx++; // [!code ++]
						if (pass.idx == pass.batchSize) // [!code ++]
						{
							thingPos.z += pref.z; // [!code --]
							pass.NextBatch(); // [!code ++]
						}
					}
					if (!tileType.UseMountHeight && j > 10) // [!code --]
					{ // [!code --]
						flag7 = true; // [!code --]
					} // [!code --]
				}
				else // [!code --]
			} // [!code ++]
			if (this.cell.autotile != 0 && sourceFloor.autotile != 0 && (!hasBridge || this.cell.bridgeHeight - this.cell.height > 3) && !this.cell.skipRender && num7 == 0) // [!code ++]
			{ // [!code ++]
				pass = (isWater ? passAutoTileWater : passAutoTile); // [!code ++]
				batch = pass.batches[pass.batchIdx]; // [!code ++]
				batch.matrices[pass.idx].m03 = param.x; // [!code ++]
				batch.matrices[pass.idx].m13 = param.y; // [!code ++]
				batch.matrices[pass.idx].m23 = param.z + ((hasBridge || this.cell._block != 0) ? 0.8f : 0f); // [!code ++]
				batch.tiles[pass.idx] = (26 + sourceFloor.autotile / 2) * 32 + sourceFloor.autotile % 2 * 16 + this.cell.autotile; // [!code ++]
				batch.colors[pass.idx] = param.color + (float)((int)(sourceFloor.autotileBrightness * 100f) * 262144); // [!code ++]
				batch.matColors[pass.idx] = param.matColor; // [!code ++]
				pass.idx++; // [!code ++]
				if (pass.idx == pass.batchSize) // [!code ++]
				{
					thingPos.y += num12; // [!code --]
					_actorPos.x += pref.x * (float)((!t.flipX) ? 1 : (-1)); // [!code --]
					_actorPos.z += pref.z; // [!code --]
					thingPos.z += pref.z; // [!code --]
					pass.NextBatch(); // [!code ++]
				}
				if (t.isFloating && isWater && !hasBridge && !flag) // [!code --]
			} // [!code ++]
		} // [!code ++]
		if (isWater) // [!code ++]
		{ // [!code ++]
			int num8 = 12; // [!code ++]
			int num9 = this.cell.shore / num8; // [!code ++]
			int num10 = this.cell.shore % num8; // [!code ++]
			bool isShoreSand = this.cell.isShoreSand; // [!code ++]
			if (this.cell.shore != 0) // [!code ++]
			{ // [!code ++]
				Cell cell = ((((uint)num9 & (true ? 1u : 0u)) != 0) ? this.cell.Back : ((((uint)num9 & 2u) != 0) ? this.cell.Right : ((((uint)num9 & 4u) != 0) ? this.cell.Front : this.cell.Left))); // [!code ++]
				if (isShoreSand && !cell.sourceFloor.isBeach) // [!code ++]
				{
					flag = true; // [!code --]
					float num13 = ((this.cell._bridge != 0) ? sourceBridge.tileType.FloorHeight : sourceFloor.tileType.FloorHeight); // [!code --]
					orgY += 0.01f * floatY - num13; // [!code --]
					if (!t.trait.IsChangeFloorHeight) // [!code --]
					{ // [!code --]
						num9 = num12; // [!code --]
					} // [!code --]
					_actorPos.y += 0.01f * floatY - num13; // [!code --]
					if (liquidLv > 10) // [!code --]
					cell = ((((uint)num9 & 8u) != 0) ? this.cell.Left : ((((uint)num9 & 4u) != 0) ? this.cell.Front : ((((uint)num9 & 2u) != 0) ? this.cell.Right : this.cell.Back))); // [!code ++]
				} // [!code ++]
				if (!cell.IsSnowTile) // [!code ++]
				{ // [!code ++]
					param.matColor = GetColorInt(ref cell.matFloor.matColor, cell.sourceFloor.colorMod); // [!code ++]
					if (isShoreSand) // [!code ++]
					{
						liquidLv = TileType.FloorWaterShallow.LiquidLV * 10; // [!code --]
						pass = passShore; // [!code ++]
						batch = pass.batches[pass.batchIdx]; // [!code ++]
						batch.matrices[pass.idx].m03 = param.x; // [!code ++]
						batch.matrices[pass.idx].m13 = param.y; // [!code ++]
						batch.matrices[pass.idx].m23 = param.z; // [!code ++]
						batch.tiles[pass.idx] = 768 + this.cell.shore / num8; // [!code ++]
						batch.colors[pass.idx] = param.color; // [!code ++]
						batch.matColors[pass.idx] = param.matColor; // [!code ++]
						pass.idx++; // [!code ++]
						if (pass.idx == pass.batchSize) // [!code ++]
						{ // [!code ++]
							pass.NextBatch(); // [!code ++]
						} // [!code ++]
						num10 = 2; // [!code ++]
					}
					liquidLv -= (int)(floatY * 0.5f); // [!code --]
					if (liquidLv < 0) // [!code --]
					else // [!code ++]
					{
						liquidLv = 0; // [!code --]
						num10 = cell.sourceFloor.edge; // [!code ++]
					}
					param.liquidLv = liquidLv; // [!code --]
					param.tile = (24 + num10 / 2) * 32 + num10 % 2 * 16 + num9; // [!code ++]
					rendererShore.Draw(param); // [!code ++]
				}
				num5 = num12; // [!code --]
				if (t.sourceCard.multisize && !t.trait.IsGround) // [!code --]
			} // [!code ++]
			if (this.cell.Back.isShoreSand && ((uint)(this.cell.Back.shore / num8) & 8u) != 0 && this.cell.Left.isShoreSand && ((uint)(this.cell.Left.shore / num8) & (true ? 1u : 0u)) != 0) // [!code ++]
			{ // [!code ++]
				param.tile = 785f; // [!code ++]
				param.matColor = GetColorInt(ref this.cell.BackLeft.matFloor.matColor, this.cell.BackLeft.sourceFloor.colorMod); // [!code ++]
				passShore.Add(param); // [!code ++]
				Draw(60); // [!code ++]
			} // [!code ++]
			if (this.cell.Back.isShoreSand && ((uint)(this.cell.Back.shore / num8) & 2u) != 0 && this.cell.Right.isShoreSand && ((uint)(this.cell.Right.shore / num8) & (true ? 1u : 0u)) != 0) // [!code ++]
			{ // [!code ++]
				param.tile = 786f; // [!code ++]
				param.matColor = GetColorInt(ref this.cell.BackRight.matFloor.matColor, this.cell.BackRight.sourceFloor.colorMod); // [!code ++]
				passShore.Add(param); // [!code ++]
				Draw(56); // [!code ++]
			} // [!code ++]
			if (this.cell.Front.isShoreSand && ((uint)(this.cell.Front.shore / num8) & 2u) != 0 && this.cell.Right.isShoreSand && ((uint)(this.cell.Right.shore / num8) & 4u) != 0) // [!code ++]
			{ // [!code ++]
				param.tile = 787f; // [!code ++]
				param.matColor = GetColorInt(ref this.cell.FrontRight.matFloor.matColor, this.cell.FrontRight.sourceFloor.colorMod); // [!code ++]
				passShore.Add(param); // [!code ++]
				Draw(48); // [!code ++]
			} // [!code ++]
			if (this.cell.Front.isShoreSand && ((uint)(this.cell.Front.shore / num8) & 8u) != 0 && this.cell.Left.isShoreSand && ((uint)(this.cell.Left.shore / num8) & 4u) != 0) // [!code ++]
			{ // [!code ++]
				param.tile = 788f; // [!code ++]
				param.matColor = GetColorInt(ref this.cell.FrontLeft.matFloor.matColor, this.cell.FrontLeft.sourceFloor.colorMod); // [!code ++]
				passShore.Add(param); // [!code ++]
				Draw(52); // [!code ++]
			} // [!code ++]
			if (this.cell._bridge != 0 && this.cell.isBridgeEdge && this.cell.bridgePillar != byte.MaxValue) // [!code ++]
			{ // [!code ++]
				pass = passEdge; // [!code ++]
				batch = pass.batches[pass.batchIdx]; // [!code ++]
				batch.matrices[pass.idx].m03 = param.x + waterEdgeBridgeFix.x; // [!code ++]
				batch.matrices[pass.idx].m13 = param.y + waterEdgeBridgeFix.y; // [!code ++]
				batch.matrices[pass.idx].m23 = param.z + waterEdgeBridgeFix.z; // [!code ++]
				batch.tiles[pass.idx] = 616 + waterAnimeIndex % 4; // [!code ++]
				batch.colors[pass.idx] = param.color; // [!code ++]
				batch.matColors[pass.idx] = 104025f; // [!code ++]
				pass.idx++; // [!code ++]
				if (pass.idx == pass.batchSize) // [!code ++]
				{
					num11 += zSetting.multiZ; // [!code --]
					pass.NextBatch(); // [!code ++]
				}
				orgZ += t.renderer.data.stackZ; // [!code --]
				if (param.liquidLv > 0) // [!code --]
			} // [!code ++]
			bool flag6 = false; // [!code ++]
			if (isShoreSand) // [!code ++]
			{ // [!code ++]
				if (((uint)num9 & (true ? 1u : 0u)) != 0) // [!code ++]
				{
					param.liquidLv += pref.liquidMod; // [!code --]
					if (param.liquidLv < 1) // [!code --]
					if (((uint)num9 & 8u) != 0) // [!code ++]
					{
						param.liquidLv = 1; // [!code --]
						if ((num9 & 2) == 0 && (num9 & 4) == 0) // [!code ++]
						{ // [!code ++]
							Draw(16); // [!code ++]
						} // [!code ++]
						flag6 = true; // [!code ++]
					}
					else if (param.liquidLv > 99 + pref.liquidModMax) // [!code --]
					if (((uint)num9 & 2u) != 0) // [!code ++]
					{
						param.liquidLv = 99 + pref.liquidModMax; // [!code --]
						if ((num9 & 8) == 0 && (num9 & 4) == 0) // [!code ++]
						{ // [!code ++]
							Draw(20); // [!code ++]
						} // [!code ++]
						flag6 = true; // [!code ++]
					}
				}
			} // [!code --]
			if (!isInstalled || !tileType.UseMountHeight) // [!code --]
			{ // [!code --]
				if (t.altitude != 0) // [!code --]
				if (((uint)num9 & 4u) != 0) // [!code ++]
				{
					_actorPos += altitudeFix * t.altitude; // [!code --]
					if (!t.isRoofItem) // [!code --]
					if (((uint)num9 & 8u) != 0) // [!code ++]
					{
						num7 += (float)t.altitude; // [!code --]
						num6 = t.altitude; // [!code --]
						if ((num9 & 2) == 0 && (num9 & 1) == 0) // [!code ++]
						{ // [!code ++]
							Draw(24); // [!code ++]
						} // [!code ++]
						flag6 = true; // [!code ++]
					} // [!code ++]
					if (((uint)num9 & 2u) != 0) // [!code ++]
					{ // [!code ++]
						if ((num9 & 8) == 0 && (num9 & 1) == 0) // [!code ++]
						{ // [!code ++]
							Draw(28); // [!code ++]
						} // [!code ++]
						flag6 = true; // [!code ++]
					}
				}
				if (num7 >= 2f && ((this.cell.Back.room != null && this.cell.Back.IsRoomEdge) || (this.cell.Left.room != null && this.cell.Left.IsRoomEdge)) && hideHang && (this.cell.room?.lot != currentLot || (!this.cell.lotWall && this.cell.room != currentRoom))) // [!code --]
				{ // [!code --]
					continue; // [!code --]
				} // [!code --]
				if (t.freePos) // [!code --]
				if (!flag6) // [!code ++]
				{
					if (t.isRoofItem) // [!code --]
					if (!this.cell.Front.sourceFloor.tileType.IsWater && !this.cell.Front.isDeck) // [!code ++]
					{
						_actorPos.x += t.fx; // [!code --]
						_actorPos.y += t.fy - (float)t.altitude * altitudeFix.y; // [!code --]
						Draw(8); // [!code ++]
					}
					else // [!code --]
					if (!this.cell.Right.sourceFloor.tileType.IsWater && !this.cell.Right.isDeck) // [!code ++]
					{
						_actorPos.x = orgX + t.fx - freePos.x; // [!code --]
						_actorPos.y = orgY + t.fy - freePos.y; // [!code --]
						Draw(12); // [!code ++]
					}
				}
				if (t.trait is TraitDoor && (t.trait as TraitDoor).IsOpen()) // [!code --]
				{ // [!code --]
					_actorPos.z += -0.5f; // [!code --]
				} // [!code --]
			}
			if (!t.sourceCard.multisize || (t.pos.x == cx && t.pos.z == cz)) // [!code --]
			if (!flag6) // [!code ++]
			{
				if (iconMode != 0) // [!code --]
				if (!this.cell.Back.sourceFloor.tileType.IsWater && !this.cell.Back.isDeck) // [!code ++]
				{
					int num14 = 0; // [!code --]
					switch (iconMode) // [!code --]
					pass = passEdge; // [!code ++]
					batch = pass.batches[pass.batchIdx]; // [!code ++]
					batch.tiles[pass.idx] = 608 + waterAnimeIndex % 4; // [!code ++]
					batch.matColors[pass.idx] = 104025f; // [!code ++]
					if (((uint)(this.cell.shore / num8) & (true ? 1u : 0u)) != 0) // [!code ++]
					{
					case CardIconMode.Visibility: // [!code --]
						if (t.isMasked) // [!code --]
						{ // [!code --]
							num14 = 17; // [!code --]
						} // [!code --]
						break; // [!code --]
					case CardIconMode.State: // [!code --]
						if (t.placeState == PlaceState.installed) // [!code --]
						if (isShoreSand) // [!code ++]
						{
							num14 = 18; // [!code --]
							param.matColor = GetColorInt(ref this.cell.Back.matFloor.matColor, this.cell.Back.sourceFloor.colorMod); // [!code ++]
							batch.matrices[pass.idx].m03 = param.x + waterEdgeFixShoreSand.x; // [!code ++]
							batch.matrices[pass.idx].m13 = param.y + waterEdgeFixShoreSand.y; // [!code ++]
							batch.matrices[pass.idx].m23 = param.z + waterEdgeFixShoreSand.z; // [!code ++]
							batch.tiles[pass.idx] = 640 + seaAnimeIndexes[waterAnimeIndex % seaAnimeIndexes.Length]; // [!code ++]
							batch.matColors[pass.idx] = param.matColor; // [!code ++]
						}
						break; // [!code --]
					case CardIconMode.Deconstruct: // [!code --]
						if (t.isDeconstructing) // [!code --]
						else // [!code ++]
						{
							num14 = 14; // [!code --]
							batch.matrices[pass.idx].m03 = param.x; // [!code ++]
							batch.matrices[pass.idx].m13 = param.y; // [!code ++]
							batch.matrices[pass.idx].m23 = param.z + waterEdgeFixShore.z; // [!code ++]
						}
						break; // [!code --]
					}
					if (t.isNPCProperty && !EMono.debug.godBuild) // [!code --]
					else // [!code ++]
					{
						num14 = 13; // [!code --]
						batch.matrices[pass.idx].m03 = param.x; // [!code ++]
						batch.matrices[pass.idx].m13 = param.y; // [!code ++]
						batch.matrices[pass.idx].m23 = param.z + waterEdgeFix.z; // [!code ++]
						batch.tiles[pass.idx] += 12f; // [!code ++]
					}
					if (num14 != 0) // [!code --]
					batch.colors[pass.idx] = param.color; // [!code ++]
					pass.idx++; // [!code ++]
					if (pass.idx == pass.batchSize) // [!code ++]
					{
						passGuideBlock.Add(_actorPos.x, _actorPos.y, _actorPos.z - 10f, num14); // [!code --]
						pass.NextBatch(); // [!code ++]
					}
				}
				t.SetRenderParam(param); // [!code --]
				if (_lowblock && t.trait.UseLowblock && !this.cell.HasFullBlock) // [!code --]
				{ // [!code --]
					param.tile += ((param.tile < 0f) ? (-64) : 64); // [!code --]
				} // [!code --]
				if (t.trait is TraitTrolley && EMono.pc.ai is AI_Trolley aI_Trolley && aI_Trolley.trolley.owner == t) // [!code --]
				if (!this.cell.Left.sourceFloor.tileType.IsWater && !this.cell.Left.isDeck) // [!code ++]
				{
					RenderParam _param = new RenderParam(param); // [!code --]
					EMono.core.actionsLateUpdate.Add(delegate // [!code --]
					pass = passEdge; // [!code ++]
					batch = pass.batches[pass.batchIdx]; // [!code ++]
					batch.tiles[pass.idx] = 612 + waterAnimeIndex % 4; // [!code ++]
					batch.matColors[pass.idx] = 104025f; // [!code ++]
					if (((uint)(this.cell.shore / num8) & 8u) != 0) // [!code ++]
					{
						t.SetRenderParam(_param); // [!code --]
						_actorPos.x = EMono.pc.renderer.position.x; // [!code --]
						_actorPos.y = EMono.pc.renderer.position.y - pref.height; // [!code --]
						_actorPos.z = EMono.pc.renderer.position.z + 0.02f; // [!code --]
						t.renderer.Draw(_param, ref _actorPos, !t.noShadow && (shadow || tileType.AlwaysShowShadow)); // [!code --]
					}); // [!code --]
				} // [!code --]
				else // [!code --]
				{ // [!code --]
					t.renderer.Draw(param, ref _actorPos, !t.noShadow && (shadow || tileType.AlwaysShowShadow)); // [!code --]
						if (isShoreSand) // [!code ++]
						{ // [!code ++]
							param.matColor = GetColorInt(ref this.cell.Left.matFloor.matColor, this.cell.Left.sourceFloor.colorMod); // [!code ++]
							batch.matrices[pass.idx].m03 = param.x + waterEdgeFixShoreSand.x; // [!code ++]
							batch.matrices[pass.idx].m13 = param.y + waterEdgeFixShoreSand.y; // [!code ++]
							batch.matrices[pass.idx].m23 = param.z + waterEdgeFixShoreSand.z; // [!code ++]
							batch.tiles[pass.idx] = 644 + seaAnimeIndexes[waterAnimeIndex % seaAnimeIndexes.Length]; // [!code ++]
							batch.matColors[pass.idx] = param.matColor; // [!code ++]
						} // [!code ++]
						else // [!code ++]
						{ // [!code ++]
							batch.matrices[pass.idx].m03 = param.x; // [!code ++]
							batch.matrices[pass.idx].m13 = param.y; // [!code ++]
							batch.matrices[pass.idx].m23 = param.z + waterEdgeFixShore.z; // [!code ++]
						} // [!code ++]
					} // [!code ++]
					else // [!code ++]
					{ // [!code ++]
						batch.matrices[pass.idx].m03 = param.x; // [!code ++]
						batch.matrices[pass.idx].m13 = param.y; // [!code ++]
						batch.matrices[pass.idx].m23 = param.z + waterEdgeFix.z; // [!code ++]
						batch.tiles[pass.idx] += 12f; // [!code ++]
					} // [!code ++]
					batch.colors[pass.idx] = param.color; // [!code ++]
					pass.idx++; // [!code ++]
					if (pass.idx == pass.batchSize) // [!code ++]
					{ // [!code ++]
						pass.NextBatch(); // [!code ++]
					} // [!code ++]
				}
			}
			if (isInstalled) // [!code --]
			{ // [!code --]
				num8 += pref.stackX * (float)((!t.flipX) ? 1 : (-1)); // [!code --]
			} // [!code --]
			param.x = orgX; // [!code --]
			param.y = orgY; // [!code --]
			param.z = orgZ; // [!code --]
			param.color = floorLight; // [!code --]
			thing = t; // [!code --]
			if (pref.Float) // [!code --]
			if (flag) // [!code ++]
			{
				liquidLv = 0; // [!code --]
				param.y += 0.01f * floatY; // [!code ++]
			}
		}
		if (flag) // [!code ++]
		{ // [!code ++]
			param.z -= 1f; // [!code ++]
		} // [!code ++]
	}
	orgY += num9; // [!code --]
	if (detail.charas.Count <= 0) // [!code --]
	if (this.cell.skipRender) // [!code ++]
	{
		if (this.cell.pcSync) // [!code ++]
		{ // [!code ++]
			param.tile = 0f; // [!code ++]
			rendererFov.Draw(param); // [!code ++]
		} // [!code ++]
		return;
	}
	param.shadowFix = 0f - num10; // [!code --]
	param.color += 1310720f; // [!code --]
	float max = zSetting.max2; // [!code --]
	for (int k = 0; k < detail.charas.Count; k++) // [!code --]
	if (hasBridge) // [!code ++]
	{
		Chara chara = detail.charas[k]; // [!code --]
		if (chara.host != null) // [!code --]
		param.y = (float)(cz - cx) * screen.tileAlign.y + (float)(int)this.cell.bridgeHeight * _heightMod.y; // [!code ++]
		param.z = 1000f + param.x * screen.tileWeight.x + param.y * screen.tileWeight.z + (float)(int)this.cell.bridgeHeight * _heightMod.z; // [!code ++]
		if (flag) // [!code ++]
		{
			continue; // [!code --]
			param.y += 0.01f * floatY; // [!code ++]
		}
		if (chara != EMono.pc && chara != LayerDrama.alwaysVisible) // [!code --]
		param.color = floorLight; // [!code ++]
		param.mat = matBridge; // [!code ++]
		floorMatColor = ((sourceBridge.colorMod == 0) ? 104025 : GetColorInt(ref matBridge.matColor, sourceBridge.colorMod)); // [!code ++]
		param.dir = this.cell.floorDir; // [!code ++]
		param.tile = sourceBridge._tiles[this.cell.floorDir % sourceBridge._tiles.Length]; // [!code ++]
		param.matColor = floorMatColor; // [!code ++]
		sourceBridge.renderData.Draw(param); // [!code ++]
		if (this.cell.autotileBridge != 0 && sourceBridge.autotile != 0) // [!code ++]
		{
			if (chara.mimicry != null && chara.mimicry.IsThing) // [!code --]
			{ // [!code --]
				if (flag3 || fogged || !this.cell.isSeen) // [!code --]
				{ // [!code --]
					continue; // [!code --]
				} // [!code --]
			} // [!code --]
			else if (flag3 || fogged || (!showAllCards && !EMono.player.CanSee(chara))) // [!code --]
			pass = passAutoTile; // [!code ++]
			batch = pass.batches[pass.batchIdx]; // [!code ++]
			batch.matrices[pass.idx].m03 = param.x; // [!code ++]
			batch.matrices[pass.idx].m13 = param.y; // [!code ++]
			batch.matrices[pass.idx].m23 = param.z + ((this.cell._block != 0) ? 0.8f : 0f); // [!code ++]
			batch.tiles[pass.idx] = (26 + sourceBridge.autotile / 2) * 32 + sourceBridge.autotile % 2 * 16 + this.cell.autotileBridge; // [!code ++]
			batch.colors[pass.idx] = param.color + (float)((int)(sourceBridge.autotileBrightness * 100f) * 262144); // [!code ++]
			batch.matColors[pass.idx] = param.matColor; // [!code ++]
			pass.idx++; // [!code ++]
			if (pass.idx == pass.batchSize) // [!code ++]
			{
				continue; // [!code --]
				pass.NextBatch(); // [!code ++]
			}
		}
		_actorPos.x = orgX; // [!code --]
		_actorPos.y = orgY; // [!code --]
		_actorPos.z = orgZ; // [!code --]
		chara.SetRenderParam(param); // [!code --]
		_ = chara.IsAliveInCurrentZone; // [!code --]
		if (chara.isRestrained) // [!code --]
		if (this.cell.shadow != 0) // [!code ++]
		{
			TraitShackle restrainer = chara.GetRestrainer(); // [!code --]
			if (restrainer != null) // [!code --]
			if (sourceBridge == FLOOR.sourceSnow) // [!code ++]
			{
				Vector3 getRestrainPos = restrainer.GetRestrainPos; // [!code --]
				if (getRestrainPos != default(Vector3)) // [!code --]
				param.tile = 448 + this.cell.shadow + 8 + (this.cell.HasFence ? 4 : 0); // [!code ++]
				param.z -= 0.01f; // [!code ++]
				sourceBridge.renderData.Draw(param); // [!code ++]
			} // [!code ++]
			else // [!code ++]
			{ // [!code ++]
				pass = passEdge; // [!code ++]
				batch = pass.batches[pass.batchIdx]; // [!code ++]
				batch.matrices[pass.idx].m03 = param.x + ambientShadowFix[this.cell.shadow].x; // [!code ++]
				batch.matrices[pass.idx].m13 = param.y + ambientShadowFix[this.cell.shadow].y; // [!code ++]
				batch.matrices[pass.idx].m23 = param.z + ambientShadowFix[this.cell.shadow].z; // [!code ++]
				batch.tiles[pass.idx] = 448 + this.cell.shadow; // [!code ++]
				batch.colors[pass.idx] = blockLight; // [!code ++]
				batch.matColors[pass.idx] = 104025f; // [!code ++]
				pass.idx++; // [!code ++]
				if (pass.idx == pass.batchSize) // [!code ++]
				{
					Vector3 position = restrainer.owner.renderer.position; // [!code --]
					float defCharaHeight = EMono.setting.render.defCharaHeight; // [!code --]
					float num15 = getRestrainPos.y + defCharaHeight - ((chara.Pref.height == 0f) ? defCharaHeight : chara.source.pref.height); // [!code --]
					_actorPos.x = position.x + getRestrainPos.x * (float)((restrainer.owner.dir % 2 == 0) ? 1 : (-1)); // [!code --]
					_actorPos.y = position.y + num15; // [!code --]
					_actorPos.z = position.z + getRestrainPos.z; // [!code --]
					param.liquidLv = 0; // [!code --]
					param.shadowFix = orgY - _actorPos.y; // [!code --]
					chara.renderer.SetFirst(first: true); // [!code --]
					chara.renderer.Draw(param, ref _actorPos, drawShadow: true); // [!code --]
					param.shadowFix = 0f; // [!code --]
					continue; // [!code --]
					pass.NextBatch(); // [!code ++]
				}
			}
		}
		if (!chara.sourceCard.multisize || (chara.pos.x == cx && chara.pos.z == cz)) // [!code --]
		if (this.cell.isBridgeEdge && this.cell.bridgeHeight - this.cell.height >= 3 && this.cell.bridgePillar != byte.MaxValue && !noSlopMode) // [!code ++]
		{
			if (chara.IsDeadOrSleeping && chara.IsPCC) // [!code --]
			orgY = param.y; // [!code ++]
			orgZ = param.z; // [!code ++]
			param.y += bridgeFix.y; // [!code ++]
			param.z += bridgeFix.z; // [!code ++]
			param.dir = 0; // [!code ++]
			SourceBlock.Row row4 = sourceBridge._bridgeBlock; // [!code ++]
			float num11 = (float)(this.cell.bridgeHeight - this.cell.height) * _heightMod.y; // [!code ++]
			if (this.cell.sourceFloor.tileType == TileType.Sky) // [!code ++]
			{
				float num16 = chara.renderer.data.size.y * 0.3f; // [!code --]
				if (thingPos.y > max) // [!code --]
				{ // [!code --]
					thingPos.y = max; // [!code --]
				} // [!code --]
				float num17 = thingPos.y + num16; // [!code --]
				float num18 = (float)k * -0.01f; // [!code --]
				if (num17 > zSetting.thresh1) // [!code --]
				{ // [!code --]
					num18 = zSetting.mod1; // [!code --]
				} // [!code --]
				_actorPos.x += thingPos.x; // [!code --]
				_actorPos.y += thingPos.y; // [!code --]
				_actorPos.z += renderSetting.laydownZ + num18; // [!code --]
				param.liquidLv = ((thingPos.y == 0f && liquidLv > 0) ? 90 : 0); // [!code --]
				thingPos.y += num16 * 0.8f; // [!code --]
				chara.renderer.Draw(param, ref _actorPos, liquidLv == 0); // [!code --]
				num11 += (float)EMono._map.config.skyBlockHeight; // [!code ++]
			} // [!code ++]
			int num12 = (int)(num11 / heightBlockSize) + 2; // [!code ++]
			if (this.cell.bridgePillar != 0) // [!code ++]
			{ // [!code ++]
				row4 = EMono.sources.blocks.rows[this.cell.bridgePillar]; // [!code ++]
				param.tile = row4._tiles[0] + ((num12 == 2) ? 32 : 0); // [!code ++]
				param.mat = ((sourceBridge.DefaultMaterial == row4.DefaultMaterial) ? sourceBridge.DefaultMaterial : row4.DefaultMaterial); // [!code ++]
				param.matColor = ((row4.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.matColor, row4.colorMod)); // [!code ++]
			}
			else
			{
				param.liquidLv = liquidLv; // [!code --]
				if (isUnderwater) // [!code --]
				{ // [!code --]
					if (chara.Pref.FloatUnderwater) // [!code --]
					{ // [!code --]
						float num19 = ((this.cell._bridge != 0) ? sourceBridge.tileType.FloorHeight : sourceFloor.tileType.FloorHeight); // [!code --]
						float num20 = floatYs[chara.uid % 10] + 10f + (float)(chara.uid % 30); // [!code --]
						orgY += 0.01f * num20 - num19; // [!code --]
						_actorPos.y += 0.01f * num20 - num19; // [!code --]
						param.shadowFix -= 0.01f * num20 - num19; // [!code --]
					} // [!code --]
				} // [!code --]
				else if (liquidLv > 0) // [!code --]
				{ // [!code --]
					if (chara.Pref.Float && !flag && !hasBridge) // [!code --]
					{ // [!code --]
						if (liquidLv > 20) // [!code --]
						{ // [!code --]
							float num21 = ((this.cell._bridge != 0) ? sourceBridge.tileType.FloorHeight : sourceFloor.tileType.FloorHeight); // [!code --]
							orgY += 0.01f * floatY - num21; // [!code --]
							_actorPos.y += 0.01f * floatY - num21; // [!code --]
							int num22 = TileType.FloorWaterShallow.LiquidLV * 10; // [!code --]
							num22 -= (int)(floatY * 0.5f); // [!code --]
							param.liquidLv = num22; // [!code --]
						} // [!code --]
						else // [!code --]
						{ // [!code --]
							param.liquidLv -= 20; // [!code --]
						} // [!code --]
					} // [!code --]
					param.liquidLv += chara.Pref.liquidMod; // [!code --]
					if (param.liquidLv < 1) // [!code --]
					{ // [!code --]
						param.liquidLv = 1; // [!code --]
					} // [!code --]
					else if (param.liquidLv > 99 + chara.Pref.liquidModMax) // [!code --]
					{ // [!code --]
						param.liquidLv = 99 + chara.Pref.liquidModMax; // [!code --]
					} // [!code --]
				} // [!code --]
				if (!chara.IsPC && !chara.renderer.IsMoving && detail.charas.Count > 1 && (detail.charas.Count != 2 || !detail.charas[0].IsDeadOrSleeping || !detail.charas[0].IsPCC)) // [!code --]
				param.mat = matBlock; // [!code ++]
				param.tile = row4._tiles[0] + 32; // [!code ++]
				param.matColor = ((row4.colorMod == 0) ? 104025 : GetColorInt(ref matBridge.matColor, row4.colorMod)); // [!code ++]
			} // [!code ++]
			param.y += ugFixBridgeTop.y; // [!code ++]
			param.z += ugFixBridgeTop.z; // [!code ++]
			for (int l = 0; l < num12; l++) // [!code ++]
			{ // [!code ++]
				if (l == num12 - 1) // [!code ++]
				{
					_actorPos += renderSetting.charaPos[1 + ((num4 < 4) ? num4 : 3)]; // [!code --]
					param.y = (float)(cz - cx) * screen.tileAlign.y + (float)height * _heightMod.y + ugFixBridgeBottom.y; // [!code ++]
					param.z = 1000f + param.x * screen.tileWeight.x + param.y * screen.tileWeight.z + (float)height * _heightMod.z + ugFixBridgeBottom.z; // [!code ++]
				}
				_actorPos.z += 0.01f * (float)k + renderSetting.charaZ; // [!code --]
				num4++; // [!code --]
				if (flag6) // [!code --]
				else // [!code ++]
				{
					_actorPos.z += chara.renderer.data.hangedFixZ; // [!code --]
					param.y += ugFixBridge.y; // [!code ++]
					param.z += ugFixBridge.z; // [!code ++]
				}
				chara.renderer.Draw(param, ref _actorPos, liquidLv == 0); // [!code --]
				row4.renderData.Draw(param); // [!code ++]
			}
			param.y = orgY; // [!code ++]
			param.z = orgZ; // [!code ++]
		}
		param.x = orgX; // [!code --]
		param.y = orgY; // [!code --]
		param.z = orgZ; // [!code --]
	} // [!code --]
	return; // [!code --]
	IL_6fea: // [!code --]
	if (isSnowCovered && (sourceBlock.id != 0 || this.cell.hasDoor) && !snowed && !this.cell.isClearSnow && ((!this.cell.Front.HasRoof && !this.cell.Front.HasBlock) || (!this.cell.Right.HasRoof && !this.cell.Right.HasBlock))) // [!code --]
	{ // [!code --]
		snowed = true; // [!code --]
	}
	if (this.cell.effect != null) // [!code --]
	if (!buildMode && this.cell.highlight != 0) // [!code ++]
	{
		if (this.cell.effect.IsLiquid) // [!code --]
		if (this.cell._block != 0 && !this.cell.hasDoor) // [!code ++]
		{
			SourceCellEffect.Row sourceEffect = this.cell.sourceEffect; // [!code --]
			SourceMaterial.Row defaultMaterial = sourceEffect.DefaultMaterial; // [!code --]
			tile = 4 + Rand.bytes[index % Rand.MaxBytes] % 4; // [!code --]
			param.tile = tile + this.cell.sourceEffect._tiles[0]; // [!code --]
			param.mat = defaultMaterial; // [!code --]
			param.matColor = ((this.cell.effect.color == 0) ? GetColorInt(ref defaultMaterial.matColor, sourceEffect.colorMod) : this.cell.effect.color); // [!code --]
			sourceEffect.renderData.Draw(param); // [!code --]
			screen.guide.DrawWall(this.cell.GetPoint(), EMono.Colors.blockColors.MapHighlight, useMarkerPass: true); // [!code ++]
		}
		else
		{
			param.tile = this.cell.effect.source._tiles[0]; // [!code --]
			SourceCellEffect.Row sourceEffect2 = this.cell.sourceEffect; // [!code --]
			if (sourceEffect2.anime.Length != 0) // [!code --]
			passGuideFloor.Add(this.cell.GetPoint(), (int)this.cell.highlight); // [!code ++]
		} // [!code ++]
	} // [!code ++]
	param.color = blockLight; // [!code ++]
	if (isSnowCovered && (sourceBlock.id != 0 || this.cell.hasDoor) && (snowed || this.cell.isClearSnow) && (this.cell.Front.HasRoof || this.cell.Right.HasRoof)) // [!code ++]
	{ // [!code ++]
		snowed = false; // [!code ++]
	} // [!code ++]
	int num13 = 0; // [!code ++]
	if (sourceBlock.id != 0) // [!code ++]
	{ // [!code ++]
		this.tileType = sourceBlock.tileType; // [!code ++]
		roomHeight = 0f; // [!code ++]
		int blockDir = this.cell.blockDir; // [!code ++]
		bool flag7 = false; // [!code ++]
		switch (wallClipMode) // [!code ++]
		{ // [!code ++]
		case WallClipMode.ByRoom: // [!code ++]
			if (!this.tileType.RepeatBlock) // [!code ++]
			{
				if (sourceEffect2.anime.Length > 2) // [!code --]
				break; // [!code ++]
			} // [!code ++]
			if (currentRoom == null || showFullWall) // [!code ++]
			{ // [!code ++]
				this.room = this.room ?? this.cell.Front.room ?? this.cell.Right.room ?? this.cell.FrontRight.room; // [!code ++]
				_lowblock = lowBlock; // [!code ++]
			} // [!code ++]
			else if (this.room != this.cell.Front.room && (this.cell.Front.room == currentRoom || (this.room?.lot != currentLot && this.cell.Front.room?.lot == currentLot))) // [!code ++]
			{ // [!code ++]
				this.room = this.cell.Front.room; // [!code ++]
				_lowblock = !this.cell.Front.lotWall && !this.cell.Front.fullWall; // [!code ++]
			} // [!code ++]
			else if (this.room != this.cell.Right.room && (this.cell.Right.room == currentRoom || (this.room?.lot != currentLot && this.cell.Right.room?.lot == currentLot))) // [!code ++]
			{ // [!code ++]
				this.room = this.cell.Right.room; // [!code ++]
				_lowblock = !this.cell.Right.lotWall && !this.cell.Right.fullWall; // [!code ++]
			} // [!code ++]
			else if (this.tileType.IsFullBlock && this.room != this.cell.FrontRight.room && (this.cell.FrontRight.room == currentRoom || (this.room?.lot != currentLot && this.cell.FrontRight.room?.lot == currentLot))) // [!code ++]
			{ // [!code ++]
				this.room = this.cell.FrontRight.room; // [!code ++]
				_lowblock = !this.cell.FrontRight.lotWall && !this.cell.FrontRight.fullWall; // [!code ++]
			} // [!code ++]
			else // [!code ++]
			{ // [!code ++]
				this.room = this.room ?? this.cell.Front.room ?? this.cell.Right.room ?? this.cell.FrontRight.room; // [!code ++]
				_lowblock = true; // [!code ++]
				if (!this.tileType.IsFullBlock) // [!code ++]
				{
					float num23 = Time.realtimeSinceStartup * 1000f / (float)sourceEffect2.anime[1] % (float)sourceEffect2.anime[2]; // [!code --]
					if (!(num23 >= (float)sourceEffect2.anime[0])) // [!code --]
					if (this.cell.lotWall) // [!code ++]
					{ // [!code ++]
						_lowblock = false; // [!code ++]
					} // [!code ++]
					else if (this.room == currentRoom) // [!code ++]
					{
						param.tile += num23; // [!code --]
						_lowblock = !this.cell.fullWall; // [!code ++]
					}
				}
				else // [!code --]
			} // [!code ++]
			flag7 = (this.room != null && this.room.data.atrium) || (this.cell.room != null && this.cell.room.data.atrium); // [!code ++]
			if (flag7) // [!code ++]
			{ // [!code ++]
				_lowblock = false; // [!code ++]
			} // [!code ++]
			if (this.room == null && alwaysLowblock) // [!code ++]
			{ // [!code ++]
				_lowblock = true; // [!code ++]
				roomHeight = 0f; // [!code ++]
			} // [!code ++]
			if (this.room != null) // [!code ++]
			{ // [!code ++]
				maxHeight = (float)(cz - cx) * screen.tileAlign.y + (float)this.room.lot.mh * _heightMod.y; // [!code ++]
				if (showRoof) // [!code ++]
				{ // [!code ++]
					roomHeight = this.room.lot.realHeight; // [!code ++]
					break; // [!code ++]
				} // [!code ++]
				if ((noRoofMode && currentRoom == null) || (_lowblock && !this.tileType.ForceRpeatBlock)) // [!code ++]
				{
					float num24 = Time.realtimeSinceStartup * 1000f / (float)sourceEffect2.anime[1] % (float)sourceEffect2.anime[0]; // [!code --]
					param.tile += num24; // [!code --]
					roomHeight = 0f; // [!code ++]
					break; // [!code ++]
				}
				int num14 = ((this.room.data.maxHeight == 0) ? 2 : this.room.data.maxHeight); // [!code ++]
				roomHeight = EMono.setting.render.roomHeightMod * (float)((this.room.lot.height < num14) ? this.room.lot.height : num14) + 0.01f * (float)this.room.lot.heightFix; // [!code ++]
			}
			if (this.cell.effect.IsFire) // [!code --]
			break; // [!code ++]
		case WallClipMode.ByLot: // [!code ++]
			if (defaultBlockHeight > 0f || isIndoor) // [!code ++]
			{
				rendererEffect.Draw(param); // [!code --]
				_lowblock = cx != 0 && cz != Size - 1 && ((!this.cell.Back.HasBlock && !this.cell.Back.isWallEdge) || (!this.cell.Left.HasBlock && !this.cell.Left.isWallEdge) || !this.cell.Back.Left.HasBlock); // [!code ++]
				if (!_lowblock) // [!code ++]
				{ // [!code ++]
					roomHeight = defaultBlockHeight * EMono.setting.render.roomHeightMod; // [!code ++]
					maxHeight = (float)(cz - cx) * screen.tileAlign.y + (float)(int)this.cell.TopHeight * _heightMod.y; // [!code ++]
				} // [!code ++]
				break; // [!code ++]
			} // [!code ++]
			if (showFullWall) // [!code ++]
			{ // [!code ++]
				_lowblock = this.room != null; // [!code ++]
				if (_lowblock) // [!code ++]
				{ // [!code ++]
					if (this.cell.Back.IsRoomEdge && this.cell.Right.IsRoomEdge && this.cell.Back.room == null && this.cell.Right.room == null && this.cell.Right.Front.room?.lot == this.room?.lot) // [!code ++]
					{ // [!code ++]
						_lowblock = false; // [!code ++]
					} // [!code ++]
				} // [!code ++]
				else if (this.cell.Back.room != null && this.cell.Back.room.lot == (this.cell.Front.room ?? this.cell.Right.room)?.lot) // [!code ++]
				{ // [!code ++]
					_lowblock = true; // [!code ++]
				} // [!code ++]
			}
			else
			{
				this.cell.effect.source.renderData.Draw(param); // [!code --]
				_lowblock = lowBlock; // [!code ++]
			}
			if (this.tileType.RepeatBlock) // [!code ++]
			{ // [!code ++]
				this.room = this.room ?? this.cell.Front.room ?? this.cell.Right.room ?? this.cell.FrontRight.room; // [!code ++]
				if (this.room != null && (!noRoofMode || currentRoom != null) && (!showFullWall || currentRoom == null || this.room.lot == currentRoom.lot)) // [!code ++]
				{ // [!code ++]
					roomHeight = ((_lowblock && !this.tileType.ForceRpeatBlock) ? 0f : this.room.lot.realHeight); // [!code ++]
					maxHeight = (float)(cz - cx) * screen.tileAlign.y + (float)this.room.lot.mh * _heightMod.y; // [!code ++]
				} // [!code ++]
			} // [!code ++]
			break; // [!code ++]
		}
	} // [!code --]
	param.color = floorLight; // [!code --]
	if (this.cell.critter != null) // [!code --]
	{ // [!code --]
		Critter critter = this.cell.critter; // [!code --]
		int snowTile = critter.tile; // [!code --]
		if (snowed && critter.SnowTile != 0) // [!code --]
		{ // [!code --]
			critter.x = 0.06f; // [!code --]
			critter.y = -0.06f; // [!code --]
			snowTile = critter.SnowTile; // [!code --]
		} // [!code --]
		else // [!code --]
		if (!_lowblock && (double)roomHeight > 1.2 && this.tileType.RepeatBlock) // [!code ++]
		{
			critter.Update(); // [!code --]
			num13 = 1; // [!code ++]
		}
		pass = passObjSS; // [!code --]
		batch = pass.batches[pass.batchIdx]; // [!code --]
		batch.matrices[pass.idx].m03 = param.x + (float)(int)(critter.x * 100f) * 0.01f; // [!code --]
		batch.matrices[pass.idx].m13 = param.y + (float)(int)(critter.y * 100f) * 0.01f; // [!code --]
		batch.matrices[pass.idx].m23 = param.z; // [!code --]
		batch.tiles[pass.idx] = snowTile * ((!critter.reverse) ? 1 : (-1)); // [!code --]
		batch.colors[pass.idx] = floorLight; // [!code --]
		pass.idx++; // [!code --]
		if (pass.idx == pass.batchSize) // [!code --]
		else if (lowBlock) // [!code ++]
		{
			pass.NextBatch(); // [!code --]
			num13 = 2; // [!code ++]
		}
	} // [!code --]
	if (detail != null) // [!code --]
	{ // [!code --]
		TransAnime anime3 = detail.anime; // [!code --]
		if (anime3 != null && !anime3.animeBlock) // [!code --]
		param.mat = matBlock; // [!code ++]
		param.dir = this.cell.blockDir; // [!code ++]
		param.snow = snowed; // [!code ++]
		switch (this.tileType.blockRenderMode) // [!code ++]
		{
			TransAnime anime4 = detail.anime; // [!code --]
			param.x += anime4.v.x; // [!code --]
			param.y += anime4.v.y; // [!code --]
			param.z += anime4.v.z; // [!code --]
		} // [!code --]
	} // [!code --]
	if (this.cell.obj != 0 && !this.cell.sourceObj.renderData.SkipOnMap) // [!code --]
	{ // [!code --]
		SourceObj.Row sourceObj = this.cell.sourceObj; // [!code --]
		if (!snowed || sourceObj.snowTile <= 0) // [!code --]
		case BlockRenderMode.FullBlock: // [!code ++]
		{
			param.snow = snowed; // [!code --]
			param.mat = this.cell.matObj; // [!code --]
			orgY = param.y; // [!code --]
			if (param.liquidLv > 0) // [!code --]
			bool invisible = sourceBlock.tileType.Invisible; // [!code ++]
			if (invisible && (!buildMode || ActionMode.Cinema.IsActive)) // [!code ++]
			{
				if (sourceObj.pref.Float) // [!code --]
				break; // [!code ++]
			} // [!code ++]
			if (this.cell.isSurrounded) // [!code ++]
			{ // [!code ++]
				switch (innerMode) // [!code ++]
				{
					param.y += 0.01f * floatY; // [!code --]
					if (liquidLv > 10) // [!code --]
					{ // [!code --]
						liquidLv = TileType.FloorWaterShallow.LiquidLV * 10; // [!code --]
					} // [!code --]
					liquidLv -= (int)(floatY * 0.5f); // [!code --]
					param.liquidLv = liquidLv; // [!code --]
				case InnerMode.InnerBlock: // [!code ++]
				case InnerMode.BuildMode: // [!code ++]
					blockLight = _baseBrightness + fogBrightness; // [!code ++]
					param.color = (int)(50f * blockLight) * 262144; // [!code ++]
					param.matColor = 104025f; // [!code ++]
					param.tile = (buildMode ? 1 : 2) + ((_lowblock || defaultBlockHeight > 0f) ? 3000000 : 0); // [!code ++]
					rendererInnerBlock.Draw(param); // [!code ++]
					return; // [!code ++]
				case InnerMode.None: // [!code ++]
				case InnerMode.Height: // [!code ++]
					param.color = blockLight; // [!code ++]
					break; // [!code ++]
				}
				if (sourceObj.tileType.IsWaterTop) // [!code --]
			} // [!code ++]
			if (snowed) // [!code ++]
			{ // [!code ++]
				param.color = floorLight; // [!code ++]
			} // [!code ++]
			param.color -= (int)(_shadowStrength * 0.8f * 50f) * 262144; // [!code ++]
			if (currentRoom != null && !showFullWall) // [!code ++]
			{ // [!code ++]
				_lowblock = true; // [!code ++]
				roomHeight = 0f; // [!code ++]
				if (this.cell.room != currentRoom && (this.cell.Front.room == currentRoom || this.cell.Right.room == currentRoom || this.cell.FrontRight.room == currentRoom) && (this.cell.Back.room != currentRoom || this.cell.Right.room != currentRoom) && (this.cell.Front.room != currentRoom || this.cell.Left.room != currentRoom)) // [!code ++]
				{
					param.liquidLv = 0; // [!code --]
					_lowblock = false; // [!code ++]
				}
				else // [!code --]
				if (!_lowblock) // [!code ++]
				{
					param.liquidLv += sourceObj.pref.liquidMod; // [!code --]
					if (param.liquidLv < 1) // [!code --]
					{ // [!code --]
						param.liquid = 1f; // [!code --]
					} // [!code --]
					else if (param.liquidLv > 99 + sourceObj.pref.liquidModMax) // [!code --]
					{ // [!code --]
						param.liquidLv = 99 + sourceObj.pref.liquidModMax; // [!code --]
					} // [!code --]
					int num15 = ((currentRoom.data.maxHeight == 0) ? 2 : currentRoom.data.maxHeight); // [!code ++]
					roomHeight = EMono.setting.render.roomHeightMod * (float)((currentRoom.lot.height < num15) ? currentRoom.lot.height : num15) + 0.01f * (float)currentRoom.lot.heightFix; // [!code ++]
				}
			}
			if (sourceObj.useAltColor) // [!code --]
			if (flag7) // [!code ++]
			{
				param.matColor = ((sourceObj.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.altColor, sourceObj.colorMod)); // [!code --]
				_lowblock = (!this.cell.Front.HasFullBlock || !this.cell.Right.HasFullBlock) && (!this.cell.Front.HasFullBlock || !this.cell.Left.HasFullBlock) && (!this.cell.Back.HasFullBlock || !this.cell.Right.HasFullBlock) && (!this.cell.Back.HasFullBlock || !this.cell.Left.HasFullBlock); // [!code ++]
				if (_lowblock) // [!code ++]
				{ // [!code ++]
					roomHeight = 0f; // [!code ++]
				} // [!code ++]
			}
			else // [!code --]
			if (invisible) // [!code ++]
			{
				param.matColor = ((sourceObj.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.matColor, sourceObj.colorMod)); // [!code --]
				roomHeight = 0f; // [!code ++]
				_lowblock = false; // [!code ++]
			}
			if (sourceObj.HasGrowth) // [!code --]
			if (this.cell.Things.Count > 0) // [!code ++]
			{
				this.cell.growth.OnRenderTileMap(param); // [!code --]
				_lowblock = false; // [!code ++]
			}
			else // [!code --]
			param.tile = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length] + (_lowblock ? 3000000 : 0); // [!code ++]
			param.matColor = ((sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, sourceBlock.colorMod)); // [!code ++]
			if (roomHeight == 0f) // [!code ++]
			{
				if (this.cell.autotileObj != 0) // [!code --]
				if (!this.cell.hasDoor) // [!code ++]
				{
					param.tile = sourceObj._tiles[0] + this.cell.autotileObj; // [!code --]
					sourceBlock.renderData.Draw(param); // [!code ++]
				}
				else if (sourceObj.tileType.IsUseBlockDir) // [!code --]
			} // [!code ++]
			else // [!code ++]
			{ // [!code ++]
				sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFixBlock, this.cell.hasDoor, this.cell.effect?.FireAmount ?? 0, isBlock: true); // [!code ++]
			} // [!code ++]
			Room room = this.cell.Front.room ?? this.cell.room; // [!code ++]
			if (room == null && this.cell.Right.room != null) // [!code ++]
			{ // [!code ++]
				room = this.cell.Right.room; // [!code ++]
			} // [!code ++]
			if (!invisible && room != null) // [!code ++]
			{ // [!code ++]
				if (room.lot.idDeco != 0 && !this.cell.hasDoor) // [!code ++]
				{
					param.tile = sourceObj._tiles[this.cell.blockDir % sourceObj._tiles.Length]; // [!code --]
					param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room.lot.idDeco); // [!code ++]
					param.matColor = room.lot.colDeco; // [!code ++]
					float y = param.y; // [!code ++]
					param.y += (float)room.lot.decoFix * 0.01f; // [!code ++]
					rendererWallDeco.Draw(param); // [!code ++]
					param.y = y; // [!code ++]
				}
				else // [!code --]
				if (room.lot.idDeco2 != 0 && roomHeight != 0f && (float)room.lot.decoFix2 * 0.01f + heightLimitDeco < roomHeight + maxHeight - param.y) // [!code ++]
				{
					param.tile = sourceObj._tiles[this.cell.objDir % sourceObj._tiles.Length]; // [!code --]
					param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room.lot.idDeco2); // [!code ++]
					param.matColor = room.lot.colDeco2; // [!code ++]
					float y2 = param.y; // [!code ++]
					float num16 = param.z; // [!code ++]
					param.y += (float)room.lot.decoFix2 * 0.01f; // [!code ++]
					param.z += (float)room.lot.decoFix2 * 0.01f * heightModDeco; // [!code ++]
					rendererWallDeco.Draw(param); // [!code ++]
					param.y = y2; // [!code ++]
					param.z = num16; // [!code ++]
				}
				if (_lowblock && sourceObj.tileType.IsSkipLowBlock) // [!code --]
				{ // [!code --]
					param.tile += ((param.tile > 0f) ? 1 : (-1)) * 3000000; // [!code --]
				} // [!code --]
				orgY = param.y; // [!code --]
				orgZ = param.z; // [!code --]
				param.y += sourceObj.pref.y; // [!code --]
				param.z += sourceObj.pref.z; // [!code --]
				sourceObj.renderData.Draw(param); // [!code --]
				param.y = orgY; // [!code --]
				param.z = orgZ; // [!code --]
				int shadow3 = sourceObj.pref.shadow; // [!code --]
				if (shadow3 > 1 && !this.cell.ignoreObjShadow) // [!code --]
				{ // [!code --]
					passShadow.AddShadow(param.x + sourceObj.renderData.offsetShadow.x, param.y + sourceObj.renderData.offsetShadow.y, param.z + sourceObj.renderData.offsetShadow.z, ShadowData.Instance.items[shadow3], sourceObj.pref, 0, param.snow); // [!code --]
				} // [!code --]
				param.y = orgY; // [!code --]
			} // [!code --]
		} // [!code --]
	} // [!code --]
	if (this.cell.decal != 0 && sourceFloor.tileType.AllowBlood) // [!code --]
	{ // [!code --]
		passDecal.Add(param, (int)this.cell.decal, floorLight); // [!code --]
	} // [!code --]
	if (highlightCells) // [!code --]
	{ // [!code --]
		switch (ActionMode.FlagCell.mode) // [!code --]
		{ // [!code --]
		case AM_FlagCell.Mode.flagWallPillar: // [!code --]
			if (this.cell.isToggleWallPillar) // [!code --]
			{ // [!code --]
				passArea.Add(param, 34f, 0f); // [!code --]
			} // [!code --]
			break; // [!code --]
		case AM_FlagCell.Mode.flagSnow: // [!code --]
			if (this.cell.isClearSnow) // [!code --]
			{ // [!code --]
				passArea.Add(param, 34f, 0f); // [!code --]
			} // [!code --]
			break; // [!code --]
		case AM_FlagCell.Mode.flagFloat: // [!code --]
			if (this.cell.isForceFloat) // [!code --]
			{ // [!code --]
				passArea.Add(param, 34f, 0f); // [!code --]
			} // [!code --]
			break; // [!code --]
		case AM_FlagCell.Mode.flagClear: // [!code --]
			if (this.cell.isClearArea) // [!code --]
			{ // [!code --]
				passArea.Add(param, 34f, 0f); // [!code --]
			} // [!code --]
			break; // [!code --]
		} // [!code --]
	} // [!code --]
	if (detail == null) // [!code --]
	{ // [!code --]
		return; // [!code --]
	} // [!code --]
	if (highlightArea && detail.area != null) // [!code --]
	{ // [!code --]
		passArea.Add(param, (int)detail.area.GetTile(index) - ((!subtleHighlightArea) ? 1 : 0), 0f); // [!code --]
	} // [!code --]
	if (detail.footmark != null && sourceFloor.id != 0) // [!code --]
	{ // [!code --]
		param.tile = detail.footmark.tile; // [!code --]
		param.mat = matFloor; // [!code --]
		param.matColor = 104025f; // [!code --]
		renderFootmark.Draw(param); // [!code --]
	} // [!code --]
	goto IL_7b1b; // [!code --]
	IL_169b: // [!code --]
	if (this.cell.isSlopeEdge) // [!code --]
	{ // [!code --]
		float num25 = (float)height * _heightMod.y; // [!code --]
		orgY = param.y; // [!code --]
		orgZ = param.z; // [!code --]
		param.dir = this.cell.blockDir; // [!code --]
		if (snowed) // [!code --]
		{ // [!code --]
			param.color = floorLight; // [!code --]
		} // [!code --]
		SourceBlock.Row defBlock; // [!code --]
		if (sourceBlock.tileType.IsFullBlock) // [!code --]
		{ // [!code --]
			defBlock = sourceBlock; // [!code --]
			param.mat = matBlock; // [!code --]
			param.tile = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length]; // [!code --]
			param.matColor = ((sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, sourceBlock.colorMod)); // [!code --]
		} // [!code --]
		else // [!code --]
		{ // [!code --]
			defBlock = sourceFloor._defBlock; // [!code --]
			param.mat = matFloor; // [!code --]
			param.tile = defBlock._tiles[this.cell.blockDir % defBlock._tiles.Length]; // [!code --]
			if (defBlock.id != 1) // [!code --]
			{ // [!code --]
				param.matColor = ((sourceFloor.colorMod == 0) ? 104025 : GetColorInt(ref matFloor.matColor, sourceFloor.colorMod)); // [!code --]
			} // [!code --]
			else // [!code --]
			{ // [!code --]
				param.matColor = 104025f; // [!code --]
			} // [!code --]
		} // [!code --]
		for (int l = 0; (float)l < num25 / heightBlockSize; l++) // [!code --]
		{ // [!code --]
			param.y += ugFix.y; // [!code --]
			param.z += ugFix.z + slopeFixZ * (float)l; // [!code --]
			defBlock.renderData.Draw(param); // [!code --]
			if (this.cell.pcSync && EMono.player.lightPower > 0f) // [!code --]
			{ // [!code --]
				float num26 = param.tile; // [!code --]
				param.tile = 0f; // [!code --]
				rendererFov.Draw(param); // [!code --]
				param.tile = num26; // [!code --]
			} // [!code --]
		} // [!code --]
		param.y = orgY; // [!code --]
		param.z = orgZ; // [!code --]
	} // [!code --]
	param.color = floorLight; // [!code --]
	if (!isWater && (this.cell.Front.sourceFloor.tileType.IsWater || this.cell.Right.sourceFloor.tileType.IsWater) && this.cell.sourceBlock.tileType.RenderWaterBlock && !flag) // [!code --]
	{ // [!code --]
		orgY = param.y; // [!code --]
		orgZ = param.z; // [!code --]
		int num27 = 0; // [!code --]
		if (sourceBlock.tileType.IsFullBlock) // [!code --]
		{ // [!code --]
			SourceBlock.Row row3 = sourceBlock; // [!code --]
			num27 = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length]; // [!code --]
		} // [!code --]
		else // [!code --]
		{ // [!code --]
			SourceBlock.Row row3 = sourceFloor._defBlock; // [!code --]
			num27 = row3._tiles[this.cell.blockDir % row3._tiles.Length]; // [!code --]
		} // [!code --]
		if (((this.cell.Front.shore / 12) & 1) == 0 && this.cell.Front.sourceFloor.tileType.IsWater && this.cell.Front.height <= height && this.cell.Front.sourceBlock.tileType.RenderWaterBlock) // [!code --]
		{ // [!code --]
			param.y = (float)(cz - cx) * screen.tileAlign.y - (this.cell.Front.sourceFloor.tileType.IsDeepWater ? 0.6f : 0.4f) + (float)(int)this.cell.Front.height * _heightMod.y; // [!code --]
			param.z = 1000f + param.x * screen.tileWeight.x + param.y * screen.tileWeight.z; // [!code --]
			param.tile = num27 + ((!this.cell.Front.sourceFloor.tileType.IsDeepWater) ? 3000000 : 0); // [!code --]
			rendererWaterBlock.Draw(param); // [!code --]
		} // [!code --]
		if (((this.cell.Right.shore / 12) & 8) == 0 && this.cell.Right.sourceFloor.tileType.IsWater && this.cell.Right.height <= height && this.cell.Right.sourceBlock.tileType.RenderWaterBlock) // [!code --]
		{ // [!code --]
			param.y = (float)(cz - cx) * screen.tileAlign.y - (this.cell.Right.sourceFloor.tileType.IsDeepWater ? 0.6f : 0.4f) + (float)(int)this.cell.Right.height * _heightMod.y; // [!code --]
			param.z = 1000f + param.x * screen.tileWeight.x + param.y * screen.tileWeight.z; // [!code --]
			param.tile = num27 + ((!this.cell.Right.sourceFloor.tileType.IsDeepWater) ? 3000000 : 0); // [!code --]
			rendererWaterBlock.Draw(param); // [!code --]
		} // [!code --]
		param.y = orgY; // [!code --]
		param.z = orgZ; // [!code --]
	} // [!code --]
	if (showBorder && !this.cell.outOfBounds) // [!code --]
	{ // [!code --]
		param.matColor = 104025f; // [!code --]
		if (cx == EMono._map.bounds.x) // [!code --]
		{ // [!code --]
			renderBorder.Draw(param, 12 + (EMono.world.date.IsNight ? 4 : 0)); // [!code --]
		} // [!code --]
		else if (cx == EMono._map.bounds.maxX) // [!code --]
		{ // [!code --]
			renderBorder.Draw(param, 13 + (EMono.world.date.IsNight ? 4 : 0)); // [!code --]
		} // [!code --]
		if (cz == EMono._map.bounds.z) // [!code --]
		{ // [!code --]
			renderBorder.Draw(param, 14 + (EMono.world.date.IsNight ? 4 : 0)); // [!code --]
		} // [!code --]
		else if (cz == EMono._map.bounds.maxZ) // [!code --]
		{ // [!code --]
			renderBorder.Draw(param, 15 + (EMono.world.date.IsNight ? 4 : 0)); // [!code --]
		} // [!code --]
	} // [!code --]
	if (this.cell.isSkyFloor || (detail != null && detail.anime != null && detail.anime.drawBlock)) // [!code --]
	{ // [!code --]
		orgY = param.y; // [!code --]
		orgZ = param.z; // [!code --]
		SourceBlock.Row defBlock2 = sourceFloor._defBlock; // [!code --]
		param.mat = matFloor; // [!code --]
		param.tile = defBlock2._tiles[this.cell.blockDir % defBlock2._tiles.Length]; // [!code --]
		if (defBlock2.id != 1) // [!code --]
		{ // [!code --]
			param.matColor = ((sourceFloor.colorMod == 0) ? 104025 : GetColorInt(ref matFloor.matColor, sourceFloor.colorMod)); // [!code --]
		} // [!code --]
		else // [!code --]
		{ // [!code --]
			param.matColor = 104025f; // [!code --]
		} // [!code --]
		for (int m = 0; m < ((!this.cell.isSkyFloor) ? 1 : EMono._map.config.skyBlockHeight); m++) // [!code --]
		{ // [!code --]
			param.y += ugFix.y; // [!code --]
			param.z += ugFix.z + slopeFixZ * (float)m; // [!code --]
			defBlock2.renderData.Draw(param); // [!code --]
		} // [!code --]
		param.y = orgY; // [!code --]
		param.z = orgZ; // [!code --]
	} // [!code --]
	if (!sourceFloor.tileType.IsSkipFloor) // [!code --]
	{ // [!code --]
		if ((hasBridge && sourceBridge.tileType.CastShadowSelf) || this.cell.castFloorShadow) // [!code --]
		{ // [!code --]
			floorLight2 = _lightMod * light * 0.2f + _baseBrightness + _shadowStrength * floorShadowStrength * (isWater ? 0.7f : (hasBridge ? 1f : (0.6f * (1f - nightRatio)))); // [!code --]
			if (snowed) // [!code --]
			{ // [!code --]
				floorLight2 = (int)((double)floorLight2 * 0.85 * 50.0) * 262144 + snowColorToken; // [!code --]
			} // [!code --]
			else // [!code --]
			{ // [!code --]
				floorLight2 = (int)(floorLight2 * 50f) * 262144 + ((this.cell.lightR >= 64) ? 63 : this.cell.lightR) * 4096 + ((this.cell.lightG >= 64) ? 63 : this.cell.lightG) * 64 + ((this.cell.lightB >= 64) ? 63 : this.cell.lightB); // [!code --]
			} // [!code --]
			param.color = floorLight2; // [!code --]
			if (this.cell.lotShade) // [!code --]
			{ // [!code --]
				floorLight = floorLight2; // [!code --]
			} // [!code --]
		} // [!code --]
		floorMatColor = ((sourceFloor.colorMod == 0) ? 104025 : GetColorInt(ref matFloor.matColor, sourceFloor.colorMod)); // [!code --]
		if (isWater && flag) // [!code --]
		{ // [!code --]
			param.y -= 0.01f * floatY; // [!code --]
		} // [!code --]
		if (!sourceBlock.tileType.IsSkipFloor || sourceBlock.transparent || hasBridge || this.cell.hasDoor || this.cell.skipRender) // [!code --]
		{ // [!code --]
			param.mat = matFloor; // [!code --]
			param.tile = sourceFloor._tiles[floorDir % sourceFloor._tiles.Length]; // [!code --]
			param.matColor = floorMatColor; // [!code --]
			param.snow = snowed; // [!code --]
			if (this.cell.isDeck) // [!code --]
			{ // [!code --]
				param.z += 1f; // [!code --]
				if ((bool)sourceFloor.renderData.subData) // [!code --]
				{ // [!code --]
					sourceFloor.renderData.subData.Draw(param); // [!code --]
				} // [!code --]
				sourceFloor.renderData.Draw(param); // [!code --]
				param.z -= 1f; // [!code --]
			} // [!code --]
			else // [!code --]
			{ // [!code --]
				if ((bool)sourceFloor.renderData.subData) // [!code --]
				{ // [!code --]
					sourceFloor.renderData.subData.Draw(param); // [!code --]
				} // [!code --]
				sourceFloor.renderData.Draw(param); // [!code --]
			} // [!code --]
			int num28 = 0; // [!code --]
			if (isSnowCovered && sourceFloor == FLOOR.sourceSnow && !this.cell.hasDoor) // [!code --]
			{ // [!code --]
				if (!this.cell.Right.IsSnowTile && this.cell.Right.topHeight == this.cell.topHeight) // [!code --]
				{ // [!code --]
					num28++; // [!code --]
				} // [!code --]
				if (!this.cell.Front.IsSnowTile && this.cell.Front.topHeight == this.cell.topHeight) // [!code --]
				{ // [!code --]
					num28 += 2; // [!code --]
				} // [!code --]
				if (num28 != 0) // [!code --]
				{ // [!code --]
					param.tile = 448 + num28 + 12; // [!code --]
					param.z -= 0.1f; // [!code --]
					sourceFloor.renderData.Draw(param); // [!code --]
					param.z += 0.1f; // [!code --]
				} // [!code --]
			} // [!code --]
			if (this.cell.shadow != 0 && !hasBridge && !this.cell.skipRender) // [!code --]
			{ // [!code --]
				if (snowed) // [!code --]
				{ // [!code --]
					if (sourceFloor == FLOOR.sourceSnow) // [!code --]
					{ // [!code --]
						param.tile = 448 + this.cell.shadow + 8 + (this.cell.HasFence ? 4 : 0); // [!code --]
						param.z -= 0.01f; // [!code --]
						sourceFloor.renderData.Draw(param); // [!code --]
					} // [!code --]
				} // [!code --]
				else // [!code --]
				{ // [!code --]
					pass = passEdge; // [!code --]
					batch = pass.batches[pass.batchIdx]; // [!code --]
					batch.matrices[pass.idx].m03 = param.x + ambientShadowFix[this.cell.shadow].x; // [!code --]
					batch.matrices[pass.idx].m13 = param.y + ambientShadowFix[this.cell.shadow].y; // [!code --]
					batch.matrices[pass.idx].m23 = param.z + ambientShadowFix[this.cell.shadow].z; // [!code --]
					batch.tiles[pass.idx] = 448 + this.cell.shadow; // [!code --]
					batch.colors[pass.idx] = param.color; // [!code --]
					batch.matColors[pass.idx] = 104025f; // [!code --]
					pass.idx++; // [!code --]
					if (pass.idx == pass.batchSize) // [!code --]
					{ // [!code --]
						pass.NextBatch(); // [!code --]
					} // [!code --]
				} // [!code --]
				if (!sourceFloor.ignoreTransition && !snowed) // [!code --]
				{ // [!code --]
					Cell back = this.cell.Back; // [!code --]
					if (back.sourceBlock.transition[0] != -1 && back.isSeen && !back.hasDoor) // [!code --]
					{ // [!code --]
						pass = passFloor; // [!code --]
						batch = pass.batches[pass.batchIdx]; // [!code --]
						batch.matrices[pass.idx].m03 = param.x + transitionFix[0].x; // [!code --]
						batch.matrices[pass.idx].m13 = param.y + transitionFix[0].y; // [!code --]
						batch.matrices[pass.idx].m23 = param.z + transitionFix[0].z; // [!code --]
						batch.tiles[pass.idx] = 480 + back.sourceBlock.transition[0] + Rand.bytes[index % Rand.MaxBytes] % back.sourceBlock.transition[1]; // [!code --]
						batch.colors[pass.idx] = param.color; // [!code --]
						batch.matColors[pass.idx] = GetColorInt(ref back.matBlock.matColor, back.sourceBlock.colorMod); // [!code --]
						pass.idx++; // [!code --]
						if (pass.idx == pass.batchSize) // [!code --]
						{ // [!code --]
							pass.NextBatch(); // [!code --]
						} // [!code --]
					} // [!code --]
					back = this.cell.Left; // [!code --]
					if (back.sourceBlock.transition[0] != -1 && back.isSeen && !back.hasDoor) // [!code --]
					{ // [!code --]
						pass = passFloor; // [!code --]
						batch = pass.batches[pass.batchIdx]; // [!code --]
						batch.matrices[pass.idx].m03 = param.x + transitionFix[1].x; // [!code --]
						batch.matrices[pass.idx].m13 = param.y + transitionFix[1].y; // [!code --]
						batch.matrices[pass.idx].m23 = param.z + transitionFix[1].z; // [!code --]
						batch.tiles[pass.idx] = 512 + back.sourceBlock.transition[0] + Rand.bytes[index % Rand.MaxBytes] % back.sourceBlock.transition[1]; // [!code --]
						batch.colors[pass.idx] = param.color; // [!code --]
						batch.matColors[pass.idx] = GetColorInt(ref back.matBlock.matColor, back.sourceBlock.colorMod); // [!code --]
						pass.idx++; // [!code --]
						if (pass.idx == pass.batchSize) // [!code --]
						{ // [!code --]
							pass.NextBatch(); // [!code --]
						} // [!code --]
					} // [!code --]
				} // [!code --]
			} // [!code --]
			if (this.cell.autotile != 0 && sourceFloor.autotile != 0 && (!hasBridge || this.cell.bridgeHeight - this.cell.height > 3) && !this.cell.skipRender && num28 == 0) // [!code --]
			{ // [!code --]
				pass = (isWater ? passAutoTileWater : passAutoTile); // [!code --]
				batch = pass.batches[pass.batchIdx]; // [!code --]
				batch.matrices[pass.idx].m03 = param.x; // [!code --]
				batch.matrices[pass.idx].m13 = param.y; // [!code --]
				batch.matrices[pass.idx].m23 = param.z + ((hasBridge || this.cell._block != 0) ? 0.8f : 0f); // [!code --]
				batch.tiles[pass.idx] = (26 + sourceFloor.autotile / 2) * 32 + sourceFloor.autotile % 2 * 16 + this.cell.autotile; // [!code --]
				batch.colors[pass.idx] = param.color + (float)((int)(sourceFloor.autotileBrightness * 100f) * 262144); // [!code --]
				batch.matColors[pass.idx] = param.matColor; // [!code --]
				pass.idx++; // [!code --]
				if (pass.idx == pass.batchSize) // [!code --]
				{ // [!code --]
					pass.NextBatch(); // [!code --]
				} // [!code --]
			} // [!code --]
		} // [!code --]
		if (isWater) // [!code --]
		{ // [!code --]
			int num29 = 12; // [!code --]
			int num30 = this.cell.shore / num29; // [!code --]
			int num31 = this.cell.shore % num29; // [!code --]
			bool isShoreSand = this.cell.isShoreSand; // [!code --]
			if (this.cell.shore != 0) // [!code --]
			{ // [!code --]
				Cell cell = ((((uint)num30 & (true ? 1u : 0u)) != 0) ? this.cell.Back : ((((uint)num30 & 2u) != 0) ? this.cell.Right : ((((uint)num30 & 4u) != 0) ? this.cell.Front : this.cell.Left))); // [!code --]
				if (isShoreSand && !cell.sourceFloor.isBeach) // [!code --]
				{ // [!code --]
					cell = ((((uint)num30 & 8u) != 0) ? this.cell.Left : ((((uint)num30 & 4u) != 0) ? this.cell.Front : ((((uint)num30 & 2u) != 0) ? this.cell.Right : this.cell.Back))); // [!code --]
				} // [!code --]
				if (!cell.IsSnowTile) // [!code --]
				{ // [!code --]
					param.matColor = GetColorInt(ref cell.matFloor.matColor, cell.sourceFloor.colorMod); // [!code --]
					if (isShoreSand) // [!code --]
					{ // [!code --]
						pass = passShore; // [!code --]
						batch = pass.batches[pass.batchIdx]; // [!code --]
						batch.matrices[pass.idx].m03 = param.x; // [!code --]
						batch.matrices[pass.idx].m13 = param.y; // [!code --]
						batch.matrices[pass.idx].m23 = param.z; // [!code --]
						batch.tiles[pass.idx] = 768 + this.cell.shore / num29; // [!code --]
						batch.colors[pass.idx] = param.color; // [!code --]
						batch.matColors[pass.idx] = param.matColor; // [!code --]
						pass.idx++; // [!code --]
						if (pass.idx == pass.batchSize) // [!code --]
						{ // [!code --]
							pass.NextBatch(); // [!code --]
						} // [!code --]
						num31 = 2; // [!code --]
					} // [!code --]
					else // [!code --]
					{ // [!code --]
						num31 = cell.sourceFloor.edge; // [!code --]
					} // [!code --]
					param.tile = (24 + num31 / 2) * 32 + num31 % 2 * 16 + num30; // [!code --]
					rendererShore.Draw(param); // [!code --]
				} // [!code --]
			} // [!code --]
			if (this.cell.Back.isShoreSand && ((uint)(this.cell.Back.shore / num29) & 8u) != 0 && this.cell.Left.isShoreSand && ((uint)(this.cell.Left.shore / num29) & (true ? 1u : 0u)) != 0) // [!code --]
			{ // [!code --]
				param.tile = 785f; // [!code --]
				param.matColor = GetColorInt(ref this.cell.BackLeft.matFloor.matColor, this.cell.BackLeft.sourceFloor.colorMod); // [!code --]
				passShore.Add(param); // [!code --]
				Draw(60); // [!code --]
			} // [!code --]
			if (this.cell.Back.isShoreSand && ((uint)(this.cell.Back.shore / num29) & 2u) != 0 && this.cell.Right.isShoreSand && ((uint)(this.cell.Right.shore / num29) & (true ? 1u : 0u)) != 0) // [!code --]
			{ // [!code --]
				param.tile = 786f; // [!code --]
				param.matColor = GetColorInt(ref this.cell.BackRight.matFloor.matColor, this.cell.BackRight.sourceFloor.colorMod); // [!code --]
				passShore.Add(param); // [!code --]
				Draw(56); // [!code --]
			} // [!code --]
			if (this.cell.Front.isShoreSand && ((uint)(this.cell.Front.shore / num29) & 2u) != 0 && this.cell.Right.isShoreSand && ((uint)(this.cell.Right.shore / num29) & 4u) != 0) // [!code --]
			{ // [!code --]
				param.tile = 787f; // [!code --]
				param.matColor = GetColorInt(ref this.cell.FrontRight.matFloor.matColor, this.cell.FrontRight.sourceFloor.colorMod); // [!code --]
				passShore.Add(param); // [!code --]
				Draw(48); // [!code --]
			}
			if (this.cell.Front.isShoreSand && ((uint)(this.cell.Front.shore / num29) & 8u) != 0 && this.cell.Left.isShoreSand && ((uint)(this.cell.Left.shore / num29) & 4u) != 0) // [!code --]
			room = this.cell.Right.room ?? this.cell.room; // [!code ++]
			if (room == null && this.cell.Front.room != null) // [!code ++]
			{
				param.tile = 788f; // [!code --]
				param.matColor = GetColorInt(ref this.cell.FrontLeft.matFloor.matColor, this.cell.FrontLeft.sourceFloor.colorMod); // [!code --]
				passShore.Add(param); // [!code --]
				Draw(52); // [!code --]
				room = this.cell.Front.room; // [!code ++]
			}
			if (this.cell._bridge != 0 && this.cell.isBridgeEdge && this.cell.bridgePillar != byte.MaxValue) // [!code --]
			if (!invisible && room != null) // [!code ++]
			{
				pass = passEdge; // [!code --]
				batch = pass.batches[pass.batchIdx]; // [!code --]
				batch.matrices[pass.idx].m03 = param.x + waterEdgeBridgeFix.x; // [!code --]
				batch.matrices[pass.idx].m13 = param.y + waterEdgeBridgeFix.y; // [!code --]
				batch.matrices[pass.idx].m23 = param.z + waterEdgeBridgeFix.z; // [!code --]
				batch.tiles[pass.idx] = 616 + waterAnimeIndex % 4; // [!code --]
				batch.colors[pass.idx] = param.color; // [!code --]
				batch.matColors[pass.idx] = 104025f; // [!code --]
				pass.idx++; // [!code --]
				if (pass.idx == pass.batchSize) // [!code --]
				if (room.lot.idDeco != 0 && !this.cell.hasDoor) // [!code ++]
				{
					pass.NextBatch(); // [!code --]
					param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room.lot.idDeco) * -1; // [!code ++]
					param.matColor = room.lot.colDeco; // [!code ++]
					float y3 = param.y; // [!code ++]
					param.y += (float)room.lot.decoFix * 0.01f; // [!code ++]
					rendererWallDeco.Draw(param); // [!code ++]
					param.y = y3; // [!code ++]
				} // [!code ++]
				if (room.lot.idDeco2 != 0 && roomHeight != 0f && (float)room.lot.decoFix2 * 0.01f + heightLimitDeco < roomHeight + maxHeight - param.y) // [!code ++]
				{ // [!code ++]
					param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room.lot.idDeco2) * -1; // [!code ++]
					param.matColor = room.lot.colDeco2; // [!code ++]
					float y4 = param.y; // [!code ++]
					float num17 = param.z; // [!code ++]
					param.y += (float)room.lot.decoFix2 * 0.01f; // [!code ++]
					param.z += (float)room.lot.decoFix2 * 0.01f * heightModDeco; // [!code ++]
					rendererWallDeco.Draw(param); // [!code ++]
					param.y = y4; // [!code ++]
					param.z = num17; // [!code ++]
				}
			}
			bool flag8 = false; // [!code --]
			if (isShoreSand) // [!code --]
			break; // [!code ++]
		} // [!code ++]
		case BlockRenderMode.WallOrFence: // [!code ++]
		{ // [!code ++]
			if (map.config.fullWallHeight) // [!code ++]
			{ // [!code ++]
				showFullWall = true; // [!code ++]
				_lowblock = false; // [!code ++]
			} // [!code ++]
			orgY = param.y; // [!code ++]
			orgZ = param.z; // [!code ++]
			param.color = (this.tileType.IsFence ? (floorLight - (float)((int)(_shadowStrength * 0.8f * 50f) * 262144)) : blockLight); // [!code ++]
			bool flag8 = blockDir == 1 || _lowblock || flag7; // [!code ++]
			bool flag9 = blockDir == 0 || _lowblock || flag7; // [!code ++]
			if (!showFullWall && currentRoom != null) // [!code ++]
			{
				if (((uint)num30 & (true ? 1u : 0u)) != 0) // [!code --]
				if (!flag8) // [!code ++]
				{
					if (((uint)num30 & 8u) != 0) // [!code --]
					if (currentRoom == this.cell.room || (this.cell.lotWall && this.cell.room?.lot == currentLot && this.cell.Front.room != currentRoom)) // [!code ++]
					{
						if ((num30 & 2) == 0 && (num30 & 4) == 0) // [!code --]
						if (!this.cell.IsRoomEdge || (this.cell.Front.room != this.cell.room && this.cell.FrontRight.room != this.cell.room)) // [!code ++]
						{
							Draw(16); // [!code --]
							flag8 = true; // [!code ++]
						}
						flag8 = true; // [!code --]
					}
					if (((uint)num30 & 2u) != 0) // [!code --]
					else if ((!this.cell.Front.lotWall || this.cell.Front.room?.lot != currentLot) && this.cell.Front.room != currentRoom) // [!code ++]
					{
						if ((num30 & 8) == 0 && (num30 & 4) == 0) // [!code --]
						{ // [!code --]
							Draw(20); // [!code --]
						} // [!code --]
						flag8 = true;
					}
				}
				if (((uint)num30 & 4u) != 0) // [!code --]
				if (!flag9) // [!code ++]
				{
					if (((uint)num30 & 8u) != 0) // [!code --]
					if (currentRoom == this.cell.room || (this.cell.lotWall && this.cell.room?.lot == currentLot && this.cell.Right.room != currentRoom)) // [!code ++]
					{
						if ((num30 & 2) == 0 && (num30 & 1) == 0) // [!code --]
						if (!this.cell.IsRoomEdge || (this.cell.Right.room != this.cell.room && this.cell.FrontRight.room != this.cell.room)) // [!code ++]
						{
							Draw(24); // [!code --]
							flag9 = true; // [!code ++]
						}
						flag8 = true; // [!code --]
					}
					if (((uint)num30 & 2u) != 0) // [!code --]
					else if ((!this.cell.Right.lotWall || this.cell.Right.room?.lot != currentLot) && this.cell.Right.room != currentRoom) // [!code ++]
					{
						if ((num30 & 8) == 0 && (num30 & 1) == 0) // [!code --]
						{ // [!code --]
							Draw(28); // [!code --]
						} // [!code --]
						flag8 = true; // [!code --]
						flag9 = true; // [!code ++]
					}
				}
				if (!flag8) // [!code --]
			} // [!code ++]
			if (blockDir == 0 || blockDir == 2) // [!code ++]
			{ // [!code ++]
				param.dir = 0; // [!code ++]
				Room room2 = this.cell.Front.room ?? this.cell.room; // [!code ++]
				if (room2 != null && this.tileType.IsWall) // [!code ++]
				{
					if (!this.cell.Front.sourceFloor.tileType.IsWater && !this.cell.Front.isDeck) // [!code --]
					if (room2.lot.idDeco != 0 && !this.cell.hasDoor) // [!code ++]
					{
						Draw(8); // [!code --]
						param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room2.lot.idDeco); // [!code ++]
						param.matColor = room2.lot.colDeco; // [!code ++]
						param.y += (float)room2.lot.decoFix * 0.01f; // [!code ++]
						rendererWallDeco.Draw(param); // [!code ++]
						param.y = orgY; // [!code ++]
					}
					if (!this.cell.Right.sourceFloor.tileType.IsWater && !this.cell.Right.isDeck) // [!code --]
					if (room2.lot.idDeco2 != 0 && roomHeight != 0f && !flag8 && (float)room2.lot.decoFix2 * 0.01f + heightLimitDeco < roomHeight + maxHeight - param.y) // [!code ++]
					{
						Draw(12); // [!code --]
						param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room2.lot.idDeco2); // [!code ++]
						param.matColor = room2.lot.colDeco2; // [!code ++]
						param.y += (float)room2.lot.decoFix2 * 0.01f; // [!code ++]
						param.z += (float)room2.lot.decoFix2 * 0.01f * heightModDeco; // [!code ++]
						rendererWallDeco.Draw(param); // [!code ++]
						param.y = orgY; // [!code ++]
						param.z = orgZ; // [!code ++]
					}
				}
			} // [!code --]
			if (!flag8) // [!code --]
			{ // [!code --]
				if (!this.cell.Back.sourceFloor.tileType.IsWater && !this.cell.Back.isDeck) // [!code --]
				Cell left = this.cell.Left; // [!code ++]
				if (blockDir == 2 && left.sourceBlock.tileType.IsWallOrFence && !this.cell.crossWall) // [!code ++]
				{
					pass = passEdge; // [!code --]
					batch = pass.batches[pass.batchIdx]; // [!code --]
					batch.tiles[pass.idx] = 608 + waterAnimeIndex % 4; // [!code --]
					batch.matColors[pass.idx] = 104025f; // [!code --]
					if (((uint)(this.cell.shore / num29) & (true ? 1u : 0u)) != 0) // [!code --]
					_sourceBlock = left.sourceBlock; // [!code ++]
					param.mat = left.matBlock; // [!code ++]
				} // [!code ++]
				else // [!code ++]
				{ // [!code ++]
					_sourceBlock = sourceBlock; // [!code ++]
					param.mat = matBlock; // [!code ++]
				} // [!code ++]
				this.tileType = _sourceBlock.tileType; // [!code ++]
				param.tile = (tile = _sourceBlock._tiles[0] + ((flag8 && this.tileType.UseLowBlock) ? 32 : 0)); // [!code ++]
				if (_sourceBlock.useAltColor) // [!code ++]
				{ // [!code ++]
					param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.altColor, _sourceBlock.colorMod)); // [!code ++]
				} // [!code ++]
				else // [!code ++]
				{ // [!code ++]
					param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.matColor, _sourceBlock.colorMod)); // [!code ++]
				} // [!code ++]
				if (roomHeight == 0f || flag8 || !this.tileType.RepeatBlock) // [!code ++]
				{ // [!code ++]
					if (!this.cell.hasDoor) // [!code ++]
					{
						if (isShoreSand) // [!code --]
						{ // [!code --]
							param.matColor = GetColorInt(ref this.cell.Back.matFloor.matColor, this.cell.Back.sourceFloor.colorMod); // [!code --]
							batch.matrices[pass.idx].m03 = param.x + waterEdgeFixShoreSand.x; // [!code --]
							batch.matrices[pass.idx].m13 = param.y + waterEdgeFixShoreSand.y; // [!code --]
							batch.matrices[pass.idx].m23 = param.z + waterEdgeFixShoreSand.z; // [!code --]
							batch.tiles[pass.idx] = 640 + seaAnimeIndexes[waterAnimeIndex % seaAnimeIndexes.Length]; // [!code --]
							batch.matColors[pass.idx] = param.matColor; // [!code --]
						} // [!code --]
						else // [!code --]
						{ // [!code --]
							batch.matrices[pass.idx].m03 = param.x; // [!code --]
							batch.matrices[pass.idx].m13 = param.y; // [!code --]
							batch.matrices[pass.idx].m23 = param.z + waterEdgeFixShore.z; // [!code --]
						} // [!code --]
						_sourceBlock.renderData.Draw(param); // [!code ++]
					} // [!code ++]
				} // [!code ++]
				else // [!code ++]
				{ // [!code ++]
					_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFix, this.cell.hasDoor, this.cell.effect?.FireAmount ?? 0); // [!code ++]
				} // [!code ++]
				param.z += cornerWallFix2.z; // [!code ++]
				if ((blockDir == 2 || (this.cell.Front.HasWallOrFence && this.cell.Front.blockDir != 0)) != this.cell.isToggleWallPillar) // [!code ++]
				{ // [!code ++]
					if (this.cell.Back.IsSnowTile && this.cell.Right.IsSnowTile) // [!code ++]
					{ // [!code ++]
						param.snow = true; // [!code ++]
					} // [!code ++]
					param.tile = _sourceBlock._tiles[0] + ((flag8 && flag9 && this.tileType.UseLowBlock && !flag7) ? 32 : 0) + (this.tileType.IsFence ? 32 : 64); // [!code ++]
					if (roomHeight == 0f || !this.tileType.RepeatBlock || (flag8 && flag9 && !flag7)) // [!code ++]
					{ // [!code ++]
						_sourceBlock.renderData.Draw(param); // [!code ++]
					}
					else
					{
						batch.matrices[pass.idx].m03 = param.x; // [!code --]
						batch.matrices[pass.idx].m13 = param.y; // [!code --]
						batch.matrices[pass.idx].m23 = param.z + waterEdgeFix.z; // [!code --]
						batch.tiles[pass.idx] += 12f; // [!code --]
						_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFix); // [!code ++]
					}
					batch.colors[pass.idx] = param.color; // [!code --]
					pass.idx++; // [!code --]
					if (pass.idx == pass.batchSize) // [!code --]
				} // [!code ++]
				if (!flag8 && !showRoof && this.cell.Left.HasWallOrFence && this.cell.Left.blockDir != 0 && !this.cell.Left.isToggleWallPillar) // [!code ++]
				{ // [!code ++]
					orgX = param.x; // [!code ++]
					param.tile = _sourceBlock._tiles[0] + ((flag8 && this.tileType.UseLowBlock && !flag7) ? 32 : 0) + (this.tileType.IsFence ? 32 : 64); // [!code ++]
					param.x += cornerWallFix3.x; // [!code ++]
					param.y += cornerWallFix3.y; // [!code ++]
					param.z += cornerWallFix3.z; // [!code ++]
					if (!flag7 && (roomHeight == 0f || flag8)) // [!code ++]
					{
						pass.NextBatch(); // [!code --]
						_sourceBlock.renderData.Draw(param); // [!code ++]
					} // [!code ++]
					else // [!code ++]
					{ // [!code ++]
						_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight + cornerWallFix.y, ref renderSetting.peakFix); // [!code ++]
					}
					param.x = orgX; // [!code ++]
				}
				if (!this.cell.Left.sourceFloor.tileType.IsWater && !this.cell.Left.isDeck) // [!code --]
				else if (this.cell.FrontLeft.HasWallOrFence && this.cell.FrontLeft.blockDir != 0 && (!flag8 || !this.cell.Left.HasWall) && !this.cell.isToggleWallPillar) // [!code ++]
				{
					pass = passEdge; // [!code --]
					batch = pass.batches[pass.batchIdx]; // [!code --]
					batch.tiles[pass.idx] = 612 + waterAnimeIndex % 4; // [!code --]
					batch.matColors[pass.idx] = 104025f; // [!code --]
					if (((uint)(this.cell.shore / num29) & 8u) != 0) // [!code --]
					orgX = param.x; // [!code ++]
					param.tile = _sourceBlock._tiles[0] + ((flag8 && this.tileType.UseLowBlock && !flag7) ? 32 : 0) + (this.tileType.IsFence ? 32 : 64); // [!code ++]
					param.x += cornerWallFix.x; // [!code ++]
					param.y += cornerWallFix.y; // [!code ++]
					param.z += cornerWallFix.z; // [!code ++]
					if (!flag7 && (roomHeight == 0f || flag8)) // [!code ++]
					{
						if (isShoreSand) // [!code --]
						{ // [!code --]
							param.matColor = GetColorInt(ref this.cell.Left.matFloor.matColor, this.cell.Left.sourceFloor.colorMod); // [!code --]
							batch.matrices[pass.idx].m03 = param.x + waterEdgeFixShoreSand.x; // [!code --]
							batch.matrices[pass.idx].m13 = param.y + waterEdgeFixShoreSand.y; // [!code --]
							batch.matrices[pass.idx].m23 = param.z + waterEdgeFixShoreSand.z; // [!code --]
							batch.tiles[pass.idx] = 644 + seaAnimeIndexes[waterAnimeIndex % seaAnimeIndexes.Length]; // [!code --]
							batch.matColors[pass.idx] = param.matColor; // [!code --]
						} // [!code --]
						else // [!code --]
						{ // [!code --]
							batch.matrices[pass.idx].m03 = param.x; // [!code --]
							batch.matrices[pass.idx].m13 = param.y; // [!code --]
							batch.matrices[pass.idx].m23 = param.z + waterEdgeFixShore.z; // [!code --]
						} // [!code --]
						_sourceBlock.renderData.Draw(param); // [!code ++]
					}
					else
					{
						batch.matrices[pass.idx].m03 = param.x; // [!code --]
						batch.matrices[pass.idx].m13 = param.y; // [!code --]
						batch.matrices[pass.idx].m23 = param.z + waterEdgeFix.z; // [!code --]
						batch.tiles[pass.idx] += 12f; // [!code --]
						_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight + cornerWallFix.y, ref renderSetting.peakFix); // [!code ++]
					} // [!code ++]
					param.x = orgX; // [!code ++]
				} // [!code ++]
			} // [!code ++]
			if (blockDir == 1 || blockDir == 2) // [!code ++]
			{ // [!code ++]
				param.y = orgY; // [!code ++]
				param.z = orgZ; // [!code ++]
				param.dir = 1; // [!code ++]
				Room room3 = this.cell.Right.room ?? this.cell.room; // [!code ++]
				if (room3 != null && this.tileType.IsWall) // [!code ++]
				{ // [!code ++]
					if (room3.lot.idDeco != 0 && !this.cell.hasDoor) // [!code ++]
					{ // [!code ++]
						param.tile = -EMono.sources.blocks.rows[0].ConvertTile(1000 + room3.lot.idDeco); // [!code ++]
						param.matColor = room3.lot.colDeco; // [!code ++]
						param.y += (float)room3.lot.decoFix * 0.01f; // [!code ++]
						rendererWallDeco.Draw(param); // [!code ++]
						param.y = orgY; // [!code ++]
					} // [!code ++]
					if (room3.lot.idDeco2 != 0 && roomHeight != 0f && !flag9 && (float)room3.lot.decoFix2 * 0.01f + heightLimitDeco < roomHeight + maxHeight - param.y) // [!code ++]
					{ // [!code ++]
						param.tile = -EMono.sources.blocks.rows[0].ConvertTile(1000 + room3.lot.idDeco2); // [!code ++]
						param.matColor = room3.lot.colDeco2; // [!code ++]
						param.y += (float)room3.lot.decoFix2 * 0.01f; // [!code ++]
						param.z += (float)room3.lot.decoFix2 * 0.01f * heightModDeco; // [!code ++]
						rendererWallDeco.Draw(param); // [!code ++]
						param.y = orgY; // [!code ++]
						param.z = orgZ; // [!code ++]
					} // [!code ++]
				} // [!code ++]
				if (blockDir == 2 && this.cell.room == null && this.cell.Right.room != null) // [!code ++]
				{ // [!code ++]
					Room room4 = this.cell.Right.room; // [!code ++]
					maxHeight = (float)(cz - cx) * screen.tileAlign.y + (float)room4.lot.mh * _heightMod.y; // [!code ++]
					if (showRoof) // [!code ++]
					{ // [!code ++]
						roomHeight = room4.lot.realHeight; // [!code ++]
					} // [!code ++]
					else if ((noRoofMode && currentRoom == null) || (_lowblock && !this.tileType.ForceRpeatBlock)) // [!code ++]
					{ // [!code ++]
						roomHeight = 0f; // [!code ++]
					} // [!code ++]
					else // [!code ++]
					{ // [!code ++]
						int num18 = ((room4.data.maxHeight == 0) ? 2 : room4.data.maxHeight); // [!code ++]
						roomHeight = EMono.setting.render.roomHeightMod * (float)((room4.lot.height < num18) ? room4.lot.height : num18) + 0.01f * (float)room4.lot.heightFix; // [!code ++]
					} // [!code ++]
				} // [!code ++]
				Cell back2 = this.cell.Back; // [!code ++]
				if (blockDir == 2 && back2.sourceBlock.tileType.IsWallOrFence && !this.cell.crossWall) // [!code ++]
				{ // [!code ++]
					_sourceBlock = back2.sourceBlock; // [!code ++]
					param.mat = back2.matBlock; // [!code ++]
				} // [!code ++]
				else // [!code ++]
				{ // [!code ++]
					_sourceBlock = sourceBlock; // [!code ++]
					param.mat = matBlock; // [!code ++]
				} // [!code ++]
				this.tileType = _sourceBlock.tileType; // [!code ++]
				param.tile = (tile = -_sourceBlock._tiles[0] + ((flag9 && this.tileType.UseLowBlock) ? (-32) : 0)); // [!code ++]
				if (_sourceBlock.useAltColor) // [!code ++]
				{ // [!code ++]
					param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.altColor, _sourceBlock.colorMod)); // [!code ++]
				} // [!code ++]
				else // [!code ++]
				{ // [!code ++]
					param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.matColor, _sourceBlock.colorMod)); // [!code ++]
				} // [!code ++]
				param.color += _rightWallShade; // [!code ++]
				if (roomHeight == 0f || flag9 || !this.tileType.RepeatBlock) // [!code ++]
				{ // [!code ++]
					if (!this.cell.hasDoor) // [!code ++]
					{ // [!code ++]
						_sourceBlock.renderData.Draw(param); // [!code ++]
					} // [!code ++]
				} // [!code ++]
				else // [!code ++]
				{ // [!code ++]
					_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFix, this.cell.hasDoor, this.cell.effect?.FireAmount ?? 0); // [!code ++]
				} // [!code ++]
				if ((this.cell.Right.HasWallOrFence && this.cell.Right.blockDir != 1) != this.cell.isToggleWallPillar && (blockDir != 2 || !this.cell.isToggleWallPillar)) // [!code ++]
				{ // [!code ++]
					if (this.cell.Left.IsSnowTile && this.cell.Front.IsSnowTile) // [!code ++]
					{ // [!code ++]
						param.snow = true; // [!code ++]
					}
					batch.colors[pass.idx] = param.color; // [!code --]
					pass.idx++; // [!code --]
					if (pass.idx == pass.batchSize) // [!code --]
					orgX = param.x; // [!code ++]
					param.tile = _sourceBlock._tiles[0] + ((flag9 && this.tileType.UseLowBlock && !flag7) ? 32 : 0) + (this.tileType.IsFence ? 32 : 64); // [!code ++]
					if (!flag7 && (roomHeight == 0f || !this.tileType.RepeatBlock || flag9)) // [!code ++]
					{
						pass.NextBatch(); // [!code --]
						_sourceBlock.renderData.Draw(param); // [!code ++]
					} // [!code ++]
					else // [!code ++]
					{ // [!code ++]
						_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFix); // [!code ++]
					}
					param.x = orgX; // [!code ++]
				}
			}
			if (flag) // [!code --]
			{ // [!code --]
				param.y += 0.01f * floatY; // [!code --]
			} // [!code --]
		} // [!code --]
		if (flag) // [!code --]
		{ // [!code --]
			param.z -= 1f; // [!code --]
		} // [!code --]
	} // [!code --]
	if (this.cell.skipRender) // [!code --]
	{ // [!code --]
		if (this.cell.pcSync) // [!code --]
		{ // [!code --]
			param.tile = 0f; // [!code --]
			rendererFov.Draw(param); // [!code --]
		} // [!code --]
		return; // [!code --]
	} // [!code --]
	if (hasBridge) // [!code --]
	{ // [!code --]
		param.y = (float)(cz - cx) * screen.tileAlign.y + (float)(int)this.cell.bridgeHeight * _heightMod.y; // [!code --]
		param.z = 1000f + param.x * screen.tileWeight.x + param.y * screen.tileWeight.z + (float)(int)this.cell.bridgeHeight * _heightMod.z; // [!code --]
		if (flag) // [!code --]
		{ // [!code --]
			param.y += 0.01f * floatY; // [!code --]
		} // [!code --]
		param.color = floorLight; // [!code --]
		param.mat = matBridge; // [!code --]
		floorMatColor = ((sourceBridge.colorMod == 0) ? 104025 : GetColorInt(ref matBridge.matColor, sourceBridge.colorMod)); // [!code --]
		param.dir = this.cell.floorDir; // [!code --]
		param.tile = sourceBridge._tiles[this.cell.floorDir % sourceBridge._tiles.Length]; // [!code --]
		param.matColor = floorMatColor; // [!code --]
		sourceBridge.renderData.Draw(param); // [!code --]
		if (this.cell.autotileBridge != 0 && sourceBridge.autotile != 0) // [!code --]
		{ // [!code --]
			pass = passAutoTile; // [!code --]
			batch = pass.batches[pass.batchIdx]; // [!code --]
			batch.matrices[pass.idx].m03 = param.x; // [!code --]
			batch.matrices[pass.idx].m13 = param.y; // [!code --]
			batch.matrices[pass.idx].m23 = param.z + ((this.cell._block != 0) ? 0.8f : 0f); // [!code --]
			batch.tiles[pass.idx] = (26 + sourceBridge.autotile / 2) * 32 + sourceBridge.autotile % 2 * 16 + this.cell.autotileBridge; // [!code --]
			batch.colors[pass.idx] = param.color + (float)((int)(sourceBridge.autotileBrightness * 100f) * 262144); // [!code --]
			batch.matColors[pass.idx] = param.matColor; // [!code --]
			pass.idx++; // [!code --]
			if (pass.idx == pass.batchSize) // [!code --]
			{ // [!code --]
				pass.NextBatch(); // [!code --]
			} // [!code --]
			param.y = orgY; // [!code ++]
			param.z = orgZ; // [!code ++]
			break; // [!code ++]
		}
		if (this.cell.shadow != 0) // [!code --]
		case BlockRenderMode.HalfBlock: // [!code ++]
			param.color = floorLight; // [!code ++]
			_sourceBlock = ((sourceBlock.id == 5) ? EMono.sources.blocks.rows[matBlock.defBlock] : sourceBlock); // [!code ++]
			param.tile = _sourceBlock._tiles[0]; // [!code ++]
			param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, _sourceBlock.colorMod)); // [!code ++]
			param.tile2 = _sourceBlock.sourceAutoFloor._tiles[0]; // [!code ++]
			param.halfBlockColor = ((_sourceBlock.sourceAutoFloor.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, _sourceBlock.sourceAutoFloor.colorMod)); // [!code ++]
			sourceBlock.renderData.Draw(param); // [!code ++]
			break; // [!code ++]
		case BlockRenderMode.Pillar: // [!code ++]
		{
			if (sourceBridge == FLOOR.sourceSnow) // [!code --]
			RenderData renderData2 = sourceBlock.renderData; // [!code ++]
			param.tile = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length]; // [!code ++]
			param.matColor = ((sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, sourceBlock.colorMod)); // [!code ++]
			int num19 = this.cell.objDir + ((this.cell.objDir >= 7) ? this.cell.objDir : 0) + 1; // [!code ++]
			if (num19 == 0) // [!code ++]
			{
				param.tile = 448 + this.cell.shadow + 8 + (this.cell.HasFence ? 4 : 0); // [!code --]
				param.z -= 0.01f; // [!code --]
				sourceBridge.renderData.Draw(param); // [!code --]
				renderData2.Draw(param); // [!code ++]
			}
			else
			{
				pass = passEdge; // [!code --]
				batch = pass.batches[pass.batchIdx]; // [!code --]
				batch.matrices[pass.idx].m03 = param.x + ambientShadowFix[this.cell.shadow].x; // [!code --]
				batch.matrices[pass.idx].m13 = param.y + ambientShadowFix[this.cell.shadow].y; // [!code --]
				batch.matrices[pass.idx].m23 = param.z + ambientShadowFix[this.cell.shadow].z; // [!code --]
				batch.tiles[pass.idx] = 448 + this.cell.shadow; // [!code --]
				batch.colors[pass.idx] = blockLight; // [!code --]
				batch.matColors[pass.idx] = 104025f; // [!code --]
				pass.idx++; // [!code --]
				if (pass.idx == pass.batchSize) // [!code --]
				{ // [!code --]
					pass.NextBatch(); // [!code --]
				} // [!code --]
				renderData2.DrawRepeat(param, num19, sourceBlock.tileType.RepeatSize); // [!code ++]
			}
			param.tile = renderData2.idShadow; // [!code ++]
			SourcePref shadowPref2 = renderData2.shadowPref; // [!code ++]
			int shadow3 = shadowPref2.shadow; // [!code ++]
			passShadow.AddShadow(param.x + renderData2.offsetShadow.x, param.y + renderData2.offsetShadow.y, param.z + renderData2.offsetShadow.z, ShadowData.Instance.items[shadow3], shadowPref2, 0, param.snow); // [!code ++]
			break; // [!code ++]
		}
		if (this.cell.isBridgeEdge && this.cell.bridgeHeight - this.cell.height >= 3 && this.cell.bridgePillar != byte.MaxValue && !noSlopMode) // [!code --]
		{ // [!code --]
			orgY = param.y; // [!code --]
			orgZ = param.z; // [!code --]
			param.y += bridgeFix.y; // [!code --]
			param.z += bridgeFix.z; // [!code --]
			param.dir = 0; // [!code --]
			SourceBlock.Row row4 = sourceBridge._bridgeBlock; // [!code --]
			float num32 = (float)(this.cell.bridgeHeight - this.cell.height) * _heightMod.y; // [!code --]
			if (this.cell.sourceFloor.tileType == TileType.Sky) // [!code --]
			{ // [!code --]
				num32 += (float)EMono._map.config.skyBlockHeight; // [!code --]
			} // [!code --]
			int num33 = (int)(num32 / heightBlockSize) + 2; // [!code --]
			if (this.cell.bridgePillar != 0) // [!code --]
		default: // [!code ++]
			param.color = floorLight; // [!code ++]
			param.tile = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length] + ((_lowblock && this.tileType.UseLowBlock) ? 3000000 : 0); // [!code ++]
			param.matColor = ((sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, sourceBlock.colorMod)); // [!code ++]
			if (roomHeight == 0f) // [!code ++]
			{
				row4 = EMono.sources.blocks.rows[this.cell.bridgePillar]; // [!code --]
				param.tile = row4._tiles[0] + ((num33 == 2) ? 32 : 0); // [!code --]
				param.mat = ((sourceBridge.DefaultMaterial == row4.DefaultMaterial) ? sourceBridge.DefaultMaterial : row4.DefaultMaterial); // [!code --]
				param.matColor = ((row4.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.matColor, row4.colorMod)); // [!code --]
				sourceBlock.renderData.Draw(param); // [!code ++]
			}
			else
			{
				param.mat = matBlock; // [!code --]
				param.tile = row4._tiles[0] + 32; // [!code --]
				param.matColor = ((row4.colorMod == 0) ? 104025 : GetColorInt(ref matBridge.matColor, row4.colorMod)); // [!code --]
			} // [!code --]
			param.y += ugFixBridgeTop.y; // [!code --]
			param.z += ugFixBridgeTop.z; // [!code --]
			for (int n = 0; n < num33; n++) // [!code --]
			{ // [!code --]
				if (n == num33 - 1) // [!code --]
				{ // [!code --]
					param.y = (float)(cz - cx) * screen.tileAlign.y + (float)height * _heightMod.y + ugFixBridgeBottom.y; // [!code --]
					param.z = 1000f + param.x * screen.tileWeight.x + param.y * screen.tileWeight.z + (float)height * _heightMod.z + ugFixBridgeBottom.z; // [!code --]
				} // [!code --]
				else // [!code --]
				{ // [!code --]
					param.y += ugFixBridge.y; // [!code --]
					param.z += ugFixBridge.z; // [!code --]
				} // [!code --]
				row4.renderData.Draw(param); // [!code --]
				sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFixBlock); // [!code ++]
			}
			param.y = orgY; // [!code --]
			param.z = orgZ; // [!code --]
			break; // [!code ++]
		}
	}
	if (!buildMode && this.cell.highlight != 0) // [!code --]
	if (this.cell.pcSync && EMono.player.lightPower > 0f && !cinemaMode) // [!code ++]
	{
		if (this.cell._block != 0 && !this.cell.hasDoor) // [!code --]
		if (this.cell.room != null || !this.cell.IsRoomEdge || !showRoof) // [!code ++]
		{
			screen.guide.DrawWall(this.cell.GetPoint(), EMono.Colors.blockColors.MapHighlight, useMarkerPass: true); // [!code --]
			goto IL_6fd9; // [!code ++]
		}
		else // [!code --]
		if (this.cell._block == 0 || !this.cell.sourceBlock.tileType.RepeatBlock) // [!code ++]
		{
			passGuideFloor.Add(this.cell.GetPoint(), (int)this.cell.highlight); // [!code --]
			Room obj = this.cell.FrontRight.room; // [!code ++]
			if (obj == null || !obj.HasRoof) // [!code ++]
			{ // [!code ++]
				goto IL_6fd9; // [!code ++]
			} // [!code ++]
		}
	}
	param.color = blockLight; // [!code --]
	if (isSnowCovered && (sourceBlock.id != 0 || this.cell.hasDoor) && (snowed || this.cell.isClearSnow) && (this.cell.Front.HasRoof || this.cell.Right.HasRoof)) // [!code --]
	goto IL_7039; // [!code ++]
	IL_7039: // [!code ++]
	if (isSnowCovered && (sourceBlock.id != 0 || this.cell.hasDoor) && !snowed && !this.cell.isClearSnow && ((!this.cell.Front.HasRoof && !this.cell.Front.HasBlock) || (!this.cell.Right.HasRoof && !this.cell.Right.HasBlock))) // [!code ++]
	{
		snowed = false; // [!code --]
		snowed = true; // [!code ++]
	}
	num3 = 0; // [!code --]
	if (sourceBlock.id != 0) // [!code --]
	if (this.cell.effect != null) // [!code ++]
	{
		this.tileType = sourceBlock.tileType; // [!code --]
		roomHeight = 0f; // [!code --]
		int blockDir = this.cell.blockDir; // [!code --]
		bool flag9 = false; // [!code --]
		switch (wallClipMode) // [!code --]
		if (this.cell.effect.IsLiquid) // [!code ++]
		{
		case WallClipMode.ByRoom: // [!code --]
			if (!this.tileType.RepeatBlock) // [!code --]
			{ // [!code --]
				break; // [!code --]
			} // [!code --]
			if (currentRoom == null || showFullWall) // [!code --]
			{ // [!code --]
				this.room = this.room ?? this.cell.Front.room ?? this.cell.Right.room ?? this.cell.FrontRight.room; // [!code --]
				_lowblock = lowBlock; // [!code --]
			} // [!code --]
			else if (this.room != this.cell.Front.room && (this.cell.Front.room == currentRoom || (this.room?.lot != currentLot && this.cell.Front.room?.lot == currentLot))) // [!code --]
			{ // [!code --]
				this.room = this.cell.Front.room; // [!code --]
				_lowblock = !this.cell.Front.lotWall && !this.cell.Front.fullWall; // [!code --]
			} // [!code --]
			else if (this.room != this.cell.Right.room && (this.cell.Right.room == currentRoom || (this.room?.lot != currentLot && this.cell.Right.room?.lot == currentLot))) // [!code --]
			{ // [!code --]
				this.room = this.cell.Right.room; // [!code --]
				_lowblock = !this.cell.Right.lotWall && !this.cell.Right.fullWall; // [!code --]
			} // [!code --]
			else if (this.tileType.IsFullBlock && this.room != this.cell.FrontRight.room && (this.cell.FrontRight.room == currentRoom || (this.room?.lot != currentLot && this.cell.FrontRight.room?.lot == currentLot))) // [!code --]
			{ // [!code --]
				this.room = this.cell.FrontRight.room; // [!code --]
				_lowblock = !this.cell.FrontRight.lotWall && !this.cell.FrontRight.fullWall; // [!code --]
			} // [!code --]
			else // [!code --]
			{ // [!code --]
				this.room = this.room ?? this.cell.Front.room ?? this.cell.Right.room ?? this.cell.FrontRight.room; // [!code --]
				_lowblock = true; // [!code --]
				if (!this.tileType.IsFullBlock) // [!code --]
				{ // [!code --]
					if (this.cell.lotWall) // [!code --]
					{ // [!code --]
						_lowblock = false; // [!code --]
					} // [!code --]
					else if (this.room == currentRoom) // [!code --]
					{ // [!code --]
						_lowblock = !this.cell.fullWall; // [!code --]
					} // [!code --]
				} // [!code --]
			} // [!code --]
			flag9 = (this.room != null && this.room.data.atrium) || (this.cell.room != null && this.cell.room.data.atrium); // [!code --]
			if (flag9) // [!code --]
			{ // [!code --]
				_lowblock = false; // [!code --]
			} // [!code --]
			if (this.room == null && alwaysLowblock) // [!code --]
			{ // [!code --]
				_lowblock = true; // [!code --]
				roomHeight = 0f; // [!code --]
			} // [!code --]
			if (this.room != null) // [!code --]
			{ // [!code --]
				maxHeight = (float)(cz - cx) * screen.tileAlign.y + (float)this.room.lot.mh * _heightMod.y; // [!code --]
				if (showRoof) // [!code --]
				{ // [!code --]
					roomHeight = this.room.lot.realHeight; // [!code --]
					break; // [!code --]
				} // [!code --]
				if ((noRoofMode && currentRoom == null) || (_lowblock && !this.tileType.ForceRpeatBlock)) // [!code --]
				{ // [!code --]
					roomHeight = 0f; // [!code --]
					break; // [!code --]
				} // [!code --]
				int num34 = ((this.room.data.maxHeight == 0) ? 2 : this.room.data.maxHeight); // [!code --]
				roomHeight = EMono.setting.render.roomHeightMod * (float)((this.room.lot.height < num34) ? this.room.lot.height : num34) + 0.01f * (float)this.room.lot.heightFix; // [!code --]
			} // [!code --]
			break; // [!code --]
		case WallClipMode.ByLot: // [!code --]
			if (defaultBlockHeight > 0f || isIndoor) // [!code --]
			{ // [!code --]
				_lowblock = cx != 0 && cz != Size - 1 && ((!this.cell.Back.HasBlock && !this.cell.Back.isWallEdge) || (!this.cell.Left.HasBlock && !this.cell.Left.isWallEdge) || !this.cell.Back.Left.HasBlock); // [!code --]
				if (!_lowblock) // [!code --]
				{ // [!code --]
					roomHeight = defaultBlockHeight * EMono.setting.render.roomHeightMod; // [!code --]
					maxHeight = (float)(cz - cx) * screen.tileAlign.y + (float)(int)this.cell.TopHeight * _heightMod.y; // [!code --]
				} // [!code --]
				break; // [!code --]
			} // [!code --]
			if (showFullWall) // [!code --]
			SourceCellEffect.Row sourceEffect = this.cell.sourceEffect; // [!code ++]
			SourceMaterial.Row defaultMaterial = sourceEffect.DefaultMaterial; // [!code ++]
			tile = 4 + Rand.bytes[index % Rand.MaxBytes] % 4; // [!code ++]
			param.tile = tile + this.cell.sourceEffect._tiles[0]; // [!code ++]
			param.mat = defaultMaterial; // [!code ++]
			param.matColor = ((this.cell.effect.color == 0) ? GetColorInt(ref defaultMaterial.matColor, sourceEffect.colorMod) : this.cell.effect.color); // [!code ++]
			sourceEffect.renderData.Draw(param); // [!code ++]
		} // [!code ++]
		else // [!code ++]
		{ // [!code ++]
			param.tile = this.cell.effect.source._tiles[0]; // [!code ++]
			SourceCellEffect.Row sourceEffect2 = this.cell.sourceEffect; // [!code ++]
			if (sourceEffect2.anime.Length != 0) // [!code ++]
			{
				_lowblock = this.room != null; // [!code --]
				if (_lowblock) // [!code --]
				if (sourceEffect2.anime.Length > 2) // [!code ++]
				{
					if (this.cell.Back.IsRoomEdge && this.cell.Right.IsRoomEdge && this.cell.Back.room == null && this.cell.Right.room == null && this.cell.Right.Front.room?.lot == this.room?.lot) // [!code --]
					float num20 = Time.realtimeSinceStartup * 1000f / (float)sourceEffect2.anime[1] % (float)sourceEffect2.anime[2]; // [!code ++]
					if (!(num20 >= (float)sourceEffect2.anime[0])) // [!code ++]
					{
						_lowblock = false; // [!code --]
						param.tile += num20; // [!code ++]
					}
				}
				else if (this.cell.Back.room != null && this.cell.Back.room.lot == (this.cell.Front.room ?? this.cell.Right.room)?.lot) // [!code --]
				else // [!code ++]
				{
					_lowblock = true; // [!code --]
					float num21 = Time.realtimeSinceStartup * 1000f / (float)sourceEffect2.anime[1] % (float)sourceEffect2.anime[0]; // [!code ++]
					param.tile += num21; // [!code ++]
				}
			}
			else // [!code --]
			if (this.cell.effect.IsFire) // [!code ++]
			{
				_lowblock = lowBlock; // [!code --]
				rendererEffect.Draw(param); // [!code ++]
			}
			if (this.tileType.RepeatBlock) // [!code --]
			else // [!code ++]
			{
				this.room = this.room ?? this.cell.Front.room ?? this.cell.Right.room ?? this.cell.FrontRight.room; // [!code --]
				if (this.room != null && (!noRoofMode || currentRoom != null) && (!showFullWall || currentRoom == null || this.room.lot == currentRoom.lot)) // [!code --]
				{ // [!code --]
					roomHeight = ((_lowblock && !this.tileType.ForceRpeatBlock) ? 0f : this.room.lot.realHeight); // [!code --]
					maxHeight = (float)(cz - cx) * screen.tileAlign.y + (float)this.room.lot.mh * _heightMod.y; // [!code --]
				} // [!code --]
				this.cell.effect.source.renderData.Draw(param); // [!code ++]
			}
			break; // [!code --]
		}
		if (!_lowblock && (double)roomHeight > 1.2 && this.tileType.RepeatBlock) // [!code --]
	} // [!code ++]
	param.color = floorLight; // [!code ++]
	if (this.cell.critter != null) // [!code ++]
	{ // [!code ++]
		Critter critter = this.cell.critter; // [!code ++]
		int snowTile = critter.tile; // [!code ++]
		if (snowed && critter.SnowTile != 0) // [!code ++]
		{
			num3 = 1; // [!code --]
			critter.x = 0.06f; // [!code ++]
			critter.y = -0.06f; // [!code ++]
			snowTile = critter.SnowTile; // [!code ++]
		}
		else if (lowBlock) // [!code --]
		else // [!code ++]
		{
			num3 = 2; // [!code --]
			critter.Update(); // [!code ++]
		}
		param.mat = matBlock; // [!code --]
		param.dir = this.cell.blockDir; // [!code --]
		param.snow = snowed; // [!code --]
		switch (this.tileType.blockRenderMode) // [!code --]
		pass = passObjSS; // [!code ++]
		batch = pass.batches[pass.batchIdx]; // [!code ++]
		batch.matrices[pass.idx].m03 = param.x + (float)(int)(critter.x * 100f) * 0.01f; // [!code ++]
		batch.matrices[pass.idx].m13 = param.y + (float)(int)(critter.y * 100f) * 0.01f; // [!code ++]
		batch.matrices[pass.idx].m23 = param.z; // [!code ++]
		batch.tiles[pass.idx] = snowTile * ((!critter.reverse) ? 1 : (-1)); // [!code ++]
		batch.colors[pass.idx] = floorLight; // [!code ++]
		pass.idx++; // [!code ++]
		if (pass.idx == pass.batchSize) // [!code ++]
		{
		case BlockRenderMode.FullBlock: // [!code --]
			pass.NextBatch(); // [!code ++]
		} // [!code ++]
	} // [!code ++]
	if (detail != null) // [!code ++]
	{ // [!code ++]
		TransAnime anime3 = detail.anime; // [!code ++]
		if (anime3 != null && !anime3.animeBlock) // [!code ++]
		{
			bool invisible = sourceBlock.tileType.Invisible; // [!code --]
			if (invisible && (!buildMode || ActionMode.Cinema.IsActive)) // [!code --]
			{ // [!code --]
				break; // [!code --]
			} // [!code --]
			if (this.cell.isSurrounded) // [!code --]
			TransAnime anime4 = detail.anime; // [!code ++]
			param.x += anime4.v.x; // [!code ++]
			param.y += anime4.v.y; // [!code ++]
			param.z += anime4.v.z; // [!code ++]
		} // [!code ++]
	} // [!code ++]
	if (this.cell.obj != 0 && !this.cell.sourceObj.renderData.SkipOnMap) // [!code ++]
	{ // [!code ++]
		SourceObj.Row sourceObj = this.cell.sourceObj; // [!code ++]
		if (!snowed || sourceObj.snowTile <= 0) // [!code ++]
		{ // [!code ++]
			param.snow = snowed; // [!code ++]
			param.mat = this.cell.matObj; // [!code ++]
			orgY = param.y; // [!code ++]
			if (param.liquidLv > 0) // [!code ++]
			{
				switch (innerMode) // [!code --]
				if (sourceObj.pref.Float) // [!code ++]
				{
				case InnerMode.InnerBlock: // [!code --]
				case InnerMode.BuildMode: // [!code --]
					blockLight = _baseBrightness + fogBrightness; // [!code --]
					param.color = (int)(50f * blockLight) * 262144; // [!code --]
					param.matColor = 104025f; // [!code --]
					param.tile = (buildMode ? 1 : 2) + ((_lowblock || defaultBlockHeight > 0f) ? 3000000 : 0); // [!code --]
					rendererInnerBlock.Draw(param); // [!code --]
					return; // [!code --]
				case InnerMode.None: // [!code --]
				case InnerMode.Height: // [!code --]
					param.color = blockLight; // [!code --]
					break; // [!code --]
					param.y += 0.01f * floatY; // [!code ++]
					if (liquidLv > 10) // [!code ++]
					{ // [!code ++]
						liquidLv = TileType.FloorWaterShallow.LiquidLV * 10; // [!code ++]
					} // [!code ++]
					liquidLv -= (int)(floatY * 0.5f); // [!code ++]
					param.liquidLv = liquidLv; // [!code ++]
				}
			} // [!code --]
			if (snowed) // [!code --]
			{ // [!code --]
				param.color = floorLight; // [!code --]
			} // [!code --]
			param.color -= (int)(_shadowStrength * 0.8f * 50f) * 262144; // [!code --]
			if (currentRoom != null && !showFullWall) // [!code --]
			{ // [!code --]
				_lowblock = true; // [!code --]
				roomHeight = 0f; // [!code --]
				if (this.cell.room != currentRoom && (this.cell.Front.room == currentRoom || this.cell.Right.room == currentRoom || this.cell.FrontRight.room == currentRoom) && (this.cell.Back.room != currentRoom || this.cell.Right.room != currentRoom) && (this.cell.Front.room != currentRoom || this.cell.Left.room != currentRoom)) // [!code --]
				if (sourceObj.tileType.IsWaterTop) // [!code ++]
				{
					_lowblock = false; // [!code --]
					param.liquidLv = 0; // [!code ++]
				}
				if (!_lowblock) // [!code --]
				else // [!code ++]
				{
					int num35 = ((currentRoom.data.maxHeight == 0) ? 2 : currentRoom.data.maxHeight); // [!code --]
					roomHeight = EMono.setting.render.roomHeightMod * (float)((currentRoom.lot.height < num35) ? currentRoom.lot.height : num35) + 0.01f * (float)currentRoom.lot.heightFix; // [!code --]
					param.liquidLv += sourceObj.pref.liquidMod; // [!code ++]
					if (param.liquidLv < 1) // [!code ++]
					{ // [!code ++]
						param.liquid = 1f; // [!code ++]
					} // [!code ++]
					else if (param.liquidLv > 99 + sourceObj.pref.liquidModMax) // [!code ++]
					{ // [!code ++]
						param.liquidLv = 99 + sourceObj.pref.liquidModMax; // [!code ++]
					} // [!code ++]
				}
			}
			if (flag9) // [!code --]
			if (sourceObj.useAltColor) // [!code ++]
			{
				_lowblock = (!this.cell.Front.HasFullBlock || !this.cell.Right.HasFullBlock) && (!this.cell.Front.HasFullBlock || !this.cell.Left.HasFullBlock) && (!this.cell.Back.HasFullBlock || !this.cell.Right.HasFullBlock) && (!this.cell.Back.HasFullBlock || !this.cell.Left.HasFullBlock); // [!code --]
				if (_lowblock) // [!code --]
				{ // [!code --]
					roomHeight = 0f; // [!code --]
				} // [!code --]
				param.matColor = ((sourceObj.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.altColor, sourceObj.colorMod)); // [!code ++]
			}
			if (invisible) // [!code --]
			else // [!code ++]
			{
				roomHeight = 0f; // [!code --]
				_lowblock = false; // [!code --]
				param.matColor = ((sourceObj.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.matColor, sourceObj.colorMod)); // [!code ++]
			}
			if (this.cell.Things.Count > 0) // [!code --]
			if (sourceObj.HasGrowth) // [!code ++]
			{
				_lowblock = false; // [!code --]
				this.cell.growth.OnRenderTileMap(param); // [!code ++]
			}
			param.tile = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length] + (_lowblock ? 3000000 : 0); // [!code --]
			param.matColor = ((sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, sourceBlock.colorMod)); // [!code --]
			if (roomHeight == 0f) // [!code --]
			else // [!code ++]
			{
				if (!this.cell.hasDoor) // [!code --]
				if (this.cell.autotileObj != 0) // [!code ++]
				{
					sourceBlock.renderData.Draw(param); // [!code --]
					param.tile = sourceObj._tiles[0] + this.cell.autotileObj; // [!code ++]
				} // [!code ++]
				else if (sourceObj.tileType.IsUseBlockDir) // [!code ++]
				{ // [!code ++]
					param.tile = sourceObj._tiles[this.cell.blockDir % sourceObj._tiles.Length]; // [!code ++]
				} // [!code ++]
				else // [!code ++]
				{ // [!code ++]
					param.tile = sourceObj._tiles[this.cell.objDir % sourceObj._tiles.Length]; // [!code ++]
				} // [!code ++]
				if (_lowblock && sourceObj.tileType.IsSkipLowBlock) // [!code ++]
				{ // [!code ++]
					param.tile += ((param.tile > 0f) ? 1 : (-1)) * 3000000; // [!code ++]
				} // [!code ++]
				orgY = param.y; // [!code ++]
				orgZ = param.z; // [!code ++]
				param.y += sourceObj.pref.y; // [!code ++]
				param.z += sourceObj.pref.z; // [!code ++]
				sourceObj.renderData.Draw(param); // [!code ++]
				param.y = orgY; // [!code ++]
				param.z = orgZ; // [!code ++]
				int shadow4 = sourceObj.pref.shadow; // [!code ++]
				if (shadow4 > 1 && !this.cell.ignoreObjShadow) // [!code ++]
				{ // [!code ++]
					passShadow.AddShadow(param.x + sourceObj.renderData.offsetShadow.x, param.y + sourceObj.renderData.offsetShadow.y, param.z + sourceObj.renderData.offsetShadow.z, ShadowData.Instance.items[shadow4], sourceObj.pref, 0, param.snow); // [!code ++]
				}
				param.y = orgY; // [!code ++]
			}
			else // [!code --]
		} // [!code ++]
	} // [!code ++]
	if (this.cell.decal != 0 && sourceFloor.tileType.AllowBlood) // [!code ++]
	{ // [!code ++]
		passDecal.Add(param, (int)this.cell.decal, floorLight); // [!code ++]
	} // [!code ++]
	if (highlightCells) // [!code ++]
	{ // [!code ++]
		switch (ActionMode.FlagCell.mode) // [!code ++]
		{ // [!code ++]
		case AM_FlagCell.Mode.flagWallPillar: // [!code ++]
			if (this.cell.isToggleWallPillar) // [!code ++]
			{
				sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFixBlock, this.cell.hasDoor, this.cell.effect?.FireAmount ?? 0, isBlock: true); // [!code --]
				passArea.Add(param, 34f, 0f); // [!code ++]
			}
			Room room2 = this.cell.Front.room ?? this.cell.room; // [!code --]
			if (room2 == null && this.cell.Right.room != null) // [!code --]
			break; // [!code ++]
		case AM_FlagCell.Mode.flagSnow: // [!code ++]
			if (this.cell.isClearSnow) // [!code ++]
			{
				room2 = this.cell.Right.room; // [!code --]
				passArea.Add(param, 34f, 0f); // [!code ++]
			}
			if (!invisible && room2 != null) // [!code --]
			break; // [!code ++]
		case AM_FlagCell.Mode.flagFloat: // [!code ++]
			if (this.cell.isForceFloat) // [!code ++]
			{
				if (room2.lot.idDeco != 0 && !this.cell.hasDoor) // [!code --]
				{ // [!code --]
					param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room2.lot.idDeco); // [!code --]
					param.matColor = room2.lot.colDeco; // [!code --]
					float y = param.y; // [!code --]
					param.y += (float)room2.lot.decoFix * 0.01f; // [!code --]
					rendererWallDeco.Draw(param); // [!code --]
					param.y = y; // [!code --]
				} // [!code --]
				if (room2.lot.idDeco2 != 0 && roomHeight != 0f && (float)room2.lot.decoFix2 * 0.01f + heightLimitDeco < roomHeight + maxHeight - param.y) // [!code --]
				{ // [!code --]
					param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room2.lot.idDeco2); // [!code --]
					param.matColor = room2.lot.colDeco2; // [!code --]
					float y2 = param.y; // [!code --]
					float num36 = param.z; // [!code --]
					param.y += (float)room2.lot.decoFix2 * 0.01f; // [!code --]
					param.z += (float)room2.lot.decoFix2 * 0.01f * heightModDeco; // [!code --]
					rendererWallDeco.Draw(param); // [!code --]
					param.y = y2; // [!code --]
					param.z = num36; // [!code --]
				} // [!code --]
				passArea.Add(param, 34f, 0f); // [!code ++]
			}
			room2 = this.cell.Right.room ?? this.cell.room; // [!code --]
			if (room2 == null && this.cell.Front.room != null) // [!code --]
			break; // [!code ++]
		case AM_FlagCell.Mode.flagClear: // [!code ++]
			if (this.cell.isClearArea) // [!code ++]
			{ // [!code ++]
				passArea.Add(param, 34f, 0f); // [!code ++]
			} // [!code ++]
			break; // [!code ++]
		} // [!code ++]
	} // [!code ++]
	if (detail == null) // [!code ++]
	{ // [!code ++]
		return; // [!code ++]
	} // [!code ++]
	if (highlightArea && detail.area != null) // [!code ++]
	{ // [!code ++]
		passArea.Add(param, (int)detail.area.GetTile(index) - ((!subtleHighlightArea) ? 1 : 0), 0f); // [!code ++]
	} // [!code ++]
	if (detail.footmark != null && sourceFloor.id != 0) // [!code ++]
	{ // [!code ++]
		param.tile = detail.footmark.tile; // [!code ++]
		param.mat = matFloor; // [!code ++]
		param.matColor = 104025f; // [!code ++]
		renderFootmark.Draw(param); // [!code ++]
	} // [!code ++]
	goto IL_7b6a; // [!code ++]
	IL_7b6a: // [!code ++]
	if (detail.things.Count == 0 && detail.charas.Count == 0) // [!code ++]
	{ // [!code ++]
		return; // [!code ++]
	} // [!code ++]
	int num22 = 0; // [!code ++]
	thingPos.x = 0f; // [!code ++]
	thingPos.y = 0f; // [!code ++]
	thingPos.z = 0f; // [!code ++]
	freePos.x = (freePos.y = (freePos.z = 0f)); // [!code ++]
	if (this.cell.HasRamp) // [!code ++]
	{ // [!code ++]
		Vector3 rampFix = sourceBlock.tileType.GetRampFix(this.cell.blockDir); // [!code ++]
		param.x += rampFix.x; // [!code ++]
		param.y += rampFix.y; // [!code ++]
		param.z += rampFix.z; // [!code ++]
		freePos.x += rampFix.x; // [!code ++]
		freePos.y += rampFix.y; // [!code ++]
		freePos.z += rampFix.z; // [!code ++]
	} // [!code ++]
	param.y += (flag ? 0f : ((this.cell._bridge != 0) ? this.cell.sourceBridge.tileType.FloorHeight : sourceFloor.tileType.FloorHeight)); // [!code ++]
	orgPos.x = (orgX = param.x); // [!code ++]
	orgPos.y = (orgY = param.y); // [!code ++]
	orgPos.z = (orgZ = param.z); // [!code ++]
	if (flag && liquidLv > 0) // [!code ++]
	{ // [!code ++]
		if (liquidLv > 10) // [!code ++]
		{ // [!code ++]
			liquidLv = TileType.FloorWaterShallow.LiquidLV * 10; // [!code ++]
		} // [!code ++]
		liquidLv -= (int)(floatY * 0.5f); // [!code ++]
		param.liquidLv = liquidLv; // [!code ++]
		param.y -= TileType.FloorWaterShallow.FloorHeight; // [!code ++]
	} // [!code ++]
	Thing thing = null; // [!code ++]
	bool shadow = liquidLv == 0; // [!code ++]
	float num23 = 0f; // [!code ++]
	float num24 = 0f; // [!code ++]
	float num25 = 0f; // [!code ++]
	float num26 = 0f; // [!code ++]
	bool flag10 = false; // [!code ++]
	float num27 = 0f; // [!code ++]
	bool flag11 = false; // [!code ++]
	float num28 = 0f; // [!code ++]
	if (detail.things.Count > 0 && isSeen) // [!code ++]
	{ // [!code ++]
		_ = zSetting.max1; // [!code ++]
		float num29 = 0f; // [!code ++]
		for (int m = 0; m < detail.things.Count; m++) // [!code ++]
		{ // [!code ++]
			Thing t = detail.things[m]; // [!code ++]
			if ((fogged && !t.isRoofItem) || ((t.isHidden || t.trait.HideInAdv || t.isMasked) && !EMono.scene.actionMode.ShowMaskedThings) || (t.isRoofItem && ((this.room == null && !sourceBlock.tileType.IsFullBlock && !EMono._zone.IsPCFaction && !buildMode) || (lowBlock && !showFullWall && this.room != null) || (noRoofMode && currentRoom == null))) || (flag3 && !t.isRoofItem)) // [!code ++]
			{
				room2 = this.cell.Front.room; // [!code --]
				continue; // [!code ++]
			}
			if (!invisible && room2 != null) // [!code --]
			TileType tileType = t.trait.tileType; // [!code ++]
			bool isInstalled = t.IsInstalled; // [!code ++]
			SourcePref pref = t.Pref; // [!code ++]
			if (!isInstalled && t.category.tileDummy != 0) // [!code ++]
			{
				if (room2.lot.idDeco != 0 && !this.cell.hasDoor) // [!code --]
				{ // [!code --]
					param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room2.lot.idDeco) * -1; // [!code --]
					param.matColor = room2.lot.colDeco; // [!code --]
					float y3 = param.y; // [!code --]
					param.y += (float)room2.lot.decoFix * 0.01f; // [!code --]
					rendererWallDeco.Draw(param); // [!code --]
					param.y = y3; // [!code --]
				} // [!code --]
				if (room2.lot.idDeco2 != 0 && roomHeight != 0f && (float)room2.lot.decoFix2 * 0.01f + heightLimitDeco < roomHeight + maxHeight - param.y) // [!code --]
				{ // [!code --]
					param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room2.lot.idDeco2) * -1; // [!code --]
					param.matColor = room2.lot.colDeco2; // [!code --]
					float y4 = param.y; // [!code --]
					float num37 = param.z; // [!code --]
					param.y += (float)room2.lot.decoFix2 * 0.01f; // [!code --]
					param.z += (float)room2.lot.decoFix2 * 0.01f * heightModDeco; // [!code --]
					rendererWallDeco.Draw(param); // [!code --]
					param.y = y4; // [!code --]
					param.z = num37; // [!code --]
				} // [!code --]
				pref = rendererObjDummy.shadowPref; // [!code ++]
			}
			break; // [!code --]
		} // [!code --]
		case BlockRenderMode.WallOrFence: // [!code --]
		{ // [!code --]
			if (map.config.fullWallHeight) // [!code --]
			float num30 = ((tileType.UseMountHeight && isInstalled) ? 0f : ((pref.height < 0f) ? 0f : ((pref.height == 0f) ? 0.1f : pref.height))); // [!code ++]
			if (t.ignoreStackHeight) // [!code ++]
			{
				showFullWall = true; // [!code --]
				_lowblock = false; // [!code --]
				thingPos.y -= num23; // [!code ++]
				thingPos -= altitudeFix * num24; // [!code ++]
			}
			orgY = param.y; // [!code --]
			orgZ = param.z; // [!code --]
			param.color = (this.tileType.IsFence ? (floorLight - (float)((int)(_shadowStrength * 0.8f * 50f) * 262144)) : blockLight); // [!code --]
			bool flag10 = blockDir == 1 || _lowblock || flag9; // [!code --]
			bool flag11 = blockDir == 0 || _lowblock || flag9; // [!code --]
			if (!showFullWall && currentRoom != null) // [!code --]
			shadow = thingPos.y < 0.16f && num28 < 0.16f; // [!code ++]
			_ = pref.bypassShadow; // [!code ++]
			param.shadowFix = 0f - thingPos.y; // [!code ++]
			param.liquidLv = ((thingPos.y + (float)t.altitude < 0.1f) ? liquidLv : 0); // [!code ++]
			if (t.isRoofItem) // [!code ++]
			{
				if (!flag10) // [!code --]
				{ // [!code --]
					if (currentRoom == this.cell.room || (this.cell.lotWall && this.cell.room?.lot == currentLot && this.cell.Front.room != currentRoom)) // [!code --]
					{ // [!code --]
						if (!this.cell.IsRoomEdge || (this.cell.Front.room != this.cell.room && this.cell.FrontRight.room != this.cell.room)) // [!code --]
						{ // [!code --]
							flag10 = true; // [!code --]
						} // [!code --]
					} // [!code --]
					else if ((!this.cell.Front.lotWall || this.cell.Front.room?.lot != currentLot) && this.cell.Front.room != currentRoom) // [!code --]
					{ // [!code --]
						flag10 = true; // [!code --]
					} // [!code --]
				} // [!code --]
				if (!flag11) // [!code --]
				param.snow = isSnowCovered && !this.cell.isClearSnow; // [!code ++]
				SetRoofHeight(param, this.cell, cx, cz); // [!code ++]
				_actorPos.x = param.x; // [!code ++]
				_actorPos.y = param.y; // [!code ++]
				_actorPos.z = param.z + num29; // [!code ++]
				if (this.room != null) // [!code ++]
				{
					if (currentRoom == this.cell.room || (this.cell.lotWall && this.cell.room?.lot == currentLot && this.cell.Right.room != currentRoom)) // [!code --]
					{ // [!code --]
						if (!this.cell.IsRoomEdge || (this.cell.Right.room != this.cell.room && this.cell.FrontRight.room != this.cell.room)) // [!code --]
						{ // [!code --]
							flag11 = true; // [!code --]
						} // [!code --]
					} // [!code --]
					else if ((!this.cell.Right.lotWall || this.cell.Right.room?.lot != currentLot) && this.cell.Right.room != currentRoom) // [!code --]
					{ // [!code --]
						flag11 = true; // [!code --]
					} // [!code --]
					param.color = GetRoofLight(this.room.lot); // [!code ++]
				}
				shadow = false; // [!code ++]
				param.liquidLv = 0; // [!code ++]
			}
			if (blockDir == 0 || blockDir == 2) // [!code --]
			else // [!code ++]
			{
				param.dir = 0; // [!code --]
				Room room3 = this.cell.Front.room ?? this.cell.room; // [!code --]
				if (room3 != null && this.tileType.IsWall) // [!code --]
				param.snow = snowed; // [!code ++]
				_actorPos.x = orgX + num26; // [!code ++]
				_actorPos.y = orgY; // [!code ++]
				_actorPos.z = orgZ + num29 + thingPos.z; // [!code ++]
				if (tileType.CanStack || !isInstalled) // [!code ++]
				{
					if (room3.lot.idDeco != 0 && !this.cell.hasDoor) // [!code --]
					{ // [!code --]
						param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room3.lot.idDeco); // [!code --]
						param.matColor = room3.lot.colDeco; // [!code --]
						param.y += (float)room3.lot.decoFix * 0.01f; // [!code --]
						rendererWallDeco.Draw(param); // [!code --]
						param.y = orgY; // [!code --]
					} // [!code --]
					if (room3.lot.idDeco2 != 0 && roomHeight != 0f && !flag10 && (float)room3.lot.decoFix2 * 0.01f + heightLimitDeco < roomHeight + maxHeight - param.y) // [!code --]
					if (thing?.id != t.id) // [!code ++]
					{
						param.tile = EMono.sources.blocks.rows[0].ConvertTile(1000 + room3.lot.idDeco2); // [!code --]
						param.matColor = room3.lot.colDeco2; // [!code --]
						param.y += (float)room3.lot.decoFix2 * 0.01f; // [!code --]
						param.z += (float)room3.lot.decoFix2 * 0.01f * heightModDeco; // [!code --]
						rendererWallDeco.Draw(param); // [!code --]
						param.y = orgY; // [!code --]
						param.z = orgZ; // [!code --]
						_actorPos.x += thingPos.x; // [!code ++]
					}
				} // [!code --]
				Cell left = this.cell.Left; // [!code --]
				if (blockDir == 2 && left.sourceBlock.tileType.IsWallOrFence && !this.cell.crossWall) // [!code --]
				{ // [!code --]
					_sourceBlock = left.sourceBlock; // [!code --]
					param.mat = left.matBlock; // [!code --]
				} // [!code --]
				else // [!code --]
				{ // [!code --]
					_sourceBlock = sourceBlock; // [!code --]
					param.mat = matBlock; // [!code --]
				} // [!code --]
				this.tileType = _sourceBlock.tileType; // [!code --]
				param.tile = (tile = _sourceBlock._tiles[0] + ((flag10 && this.tileType.UseLowBlock) ? 32 : 0)); // [!code --]
				if (_sourceBlock.useAltColor) // [!code --]
				{ // [!code --]
					param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.altColor, _sourceBlock.colorMod)); // [!code --]
				} // [!code --]
				else // [!code --]
				{ // [!code --]
					param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.matColor, _sourceBlock.colorMod)); // [!code --]
				} // [!code --]
				if (roomHeight == 0f || flag10 || !this.tileType.RepeatBlock) // [!code --]
				{ // [!code --]
					if (!this.cell.hasDoor) // [!code --]
					_actorPos.y += thingPos.y; // [!code ++]
					if (t.trait.IgnoreLastStackHeight && (thing == null || !thing.trait.IgnoreLastStackHeight)) // [!code ++]
					{
						_sourceBlock.renderData.Draw(param); // [!code --]
						thingPos.y -= num23; // [!code ++]
						if (thing != null) // [!code ++]
						{ // [!code ++]
							_actorPos.z -= 0.2f; // [!code ++]
							thingPos.z -= 0.2f; // [!code ++]
						} // [!code ++]
						_actorPos.y -= num23; // [!code ++]
					}
					_actorPos.z += renderSetting.thingZ + (float)m * -0.01f + zSetting.mod1 * thingPos.y; // [!code ++]
				}
				else // [!code --]
				{ // [!code --]
					_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFix, this.cell.hasDoor, this.cell.effect?.FireAmount ?? 0); // [!code --]
				} // [!code --]
				param.z += cornerWallFix2.z; // [!code --]
				if ((blockDir == 2 || (this.cell.Front.HasWallOrFence && this.cell.Front.blockDir != 0)) != this.cell.isToggleWallPillar) // [!code --]
				if (isInstalled) // [!code ++]
				{
					if (this.cell.Back.IsSnowTile && this.cell.Right.IsSnowTile) // [!code --]
					{ // [!code --]
						param.snow = true; // [!code --]
					} // [!code --]
					param.tile = _sourceBlock._tiles[0] + ((flag10 && flag11 && this.tileType.UseLowBlock && !flag9) ? 32 : 0) + (this.tileType.IsFence ? 32 : 64); // [!code --]
					if (roomHeight == 0f || !this.tileType.RepeatBlock || (flag10 && flag11 && !flag9)) // [!code --]
					{ // [!code --]
						_sourceBlock.renderData.Draw(param); // [!code --]
					} // [!code --]
					else // [!code --]
					if (t.TileType.IsRamp) // [!code ++]
					{
						_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFix); // [!code --]
						Vector3 rampFix2 = t.TileType.GetRampFix(t.dir, pref); // [!code ++]
						orgX += rampFix2.x; // [!code ++]
						orgY += rampFix2.y; // [!code ++]
						orgZ += rampFix2.z; // [!code ++]
						freePos.x += rampFix2.x; // [!code ++]
						freePos.y += rampFix2.y; // [!code ++]
						freePos.z += rampFix2.z; // [!code ++]
						if (!this.cell.IsTopWater || t.altitude > 0) // [!code ++]
						{ // [!code ++]
							num28 += rampFix2.y; // [!code ++]
						} // [!code ++]
						liquidLv -= (int)(rampFix2.y * 150f); // [!code ++]
						if (liquidLv < 0) // [!code ++]
						{ // [!code ++]
							liquidLv = 0; // [!code ++]
						} // [!code ++]
					}
				} // [!code --]
				if (!flag10 && !showRoof && this.cell.Left.HasWallOrFence && this.cell.Left.blockDir != 0 && !this.cell.Left.isToggleWallPillar) // [!code --]
				{ // [!code --]
					orgX = param.x; // [!code --]
					param.tile = _sourceBlock._tiles[0] + ((flag10 && this.tileType.UseLowBlock && !flag9) ? 32 : 0) + (this.tileType.IsFence ? 32 : 64); // [!code --]
					param.x += cornerWallFix3.x; // [!code --]
					param.y += cornerWallFix3.y; // [!code --]
					param.z += cornerWallFix3.z; // [!code --]
					if (!flag9 && (roomHeight == 0f || flag10)) // [!code --]
					else if (!flag11 && t.trait.IsChangeFloorHeight && !t.ignoreStackHeight) // [!code ++]
					{
						_sourceBlock.renderData.Draw(param); // [!code --]
						orgY += num30 + (float)t.altitude * altitudeFix.y; // [!code ++]
						orgZ += (float)t.altitude * altitudeFix.z; // [!code ++]
						freePos.y += num30 + (float)t.altitude * altitudeFix.y; // [!code ++]
						if (!this.cell.IsTopWater || t.altitude > 0) // [!code ++]
						{ // [!code ++]
							num28 += num30 + (float)t.altitude * altitudeFix.y; // [!code ++]
						} // [!code ++]
						_actorPos.x += pref.x * (float)((!t.flipX) ? 1 : (-1)); // [!code ++]
						_actorPos.z += pref.z; // [!code ++]
						thingPos.z += pref.z; // [!code ++]
						liquidLv -= (int)(num30 * 150f); // [!code ++]
						if (liquidLv < 0) // [!code ++]
						{ // [!code ++]
							liquidLv = 0; // [!code ++]
						} // [!code ++]
					}
					else
					{
						_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight + cornerWallFix.y, ref renderSetting.peakFix); // [!code --]
					} // [!code --]
					param.x = orgX; // [!code --]
				} // [!code --]
				else if (this.cell.FrontLeft.HasWallOrFence && this.cell.FrontLeft.blockDir != 0 && (!flag10 || !this.cell.Left.HasWall) && !this.cell.isToggleWallPillar) // [!code --]
				{ // [!code --]
					orgX = param.x; // [!code --]
					param.tile = _sourceBlock._tiles[0] + ((flag10 && this.tileType.UseLowBlock && !flag9) ? 32 : 0) + (this.tileType.IsFence ? 32 : 64); // [!code --]
					param.x += cornerWallFix.x; // [!code --]
					param.y += cornerWallFix.y; // [!code --]
					param.z += cornerWallFix.z; // [!code --]
					if (!flag9 && (roomHeight == 0f || flag10)) // [!code --]
					{ // [!code --]
						_sourceBlock.renderData.Draw(param); // [!code --]
						thingPos.y += num30; // [!code ++]
						if (tileType.UseMountHeight) // [!code ++]
						{ // [!code ++]
							if ((tileType != TileType.Illumination || !this.cell.HasObj) && !tileType.AlwaysShow) // [!code ++]
							{ // [!code ++]
								if (noRoofMode && currentRoom == null && t.altitude >= lowWallObjAltitude && tileType != TileType.ObjFloat) // [!code ++]
								{ // [!code ++]
									continue; // [!code ++]
								} // [!code ++]
								if (hideHang && (this.cell.room?.lot != currentLot || (!this.cell.lotWall && this.cell.room != currentRoom))) // [!code ++]
								{ // [!code ++]
									Room room5 = ((t.dir == 0) ? this.cell.Back.room : this.cell.Left.room); // [!code ++]
									if (t.trait.AlwaysHideOnLowWall) // [!code ++]
									{ // [!code ++]
										if (room5 == null || !room5.data.showWallItem) // [!code ++]
										{ // [!code ++]
											continue; // [!code ++]
										} // [!code ++]
									} // [!code ++]
									else if (t.altitude >= lowWallObjAltitude) // [!code ++]
									{ // [!code ++]
										continue; // [!code ++]
									} // [!code ++]
								} // [!code ++]
							} // [!code ++]
							if (tileType.UseHangZFix) // [!code ++]
							{ // [!code ++]
								flag10 = true; // [!code ++]
							} // [!code ++]
							tileType.GetMountHeight(ref _actorPos, Point.shared.Set(index), t.dir, t); // [!code ++]
							shadow = false; // [!code ++]
							param.liquidLv = 0; // [!code ++]
							if (t.freePos) // [!code ++]
							{ // [!code ++]
								_actorPos.x += t.fx; // [!code ++]
								_actorPos.y += t.fy; // [!code ++]
							} // [!code ++]
						} // [!code ++]
						else // [!code ++]
						{ // [!code ++]
							thingPos.y += (float)t.altitude * altitudeFix.y; // [!code ++]
							thingPos.z += (float)t.altitude * altitudeFix.z; // [!code ++]
						} // [!code ++]
						_actorPos.x += pref.x * (float)((!t.flipX) ? 1 : (-1)); // [!code ++]
						_actorPos.z += pref.z; // [!code ++]
						if (pref.height >= 0f) // [!code ++]
						{ // [!code ++]
							thingPos.z += pref.z; // [!code ++]
						} // [!code ++]
					}
					else // [!code --]
					if (!tileType.UseMountHeight && m > 10) // [!code ++]
					{
						_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight + cornerWallFix.y, ref renderSetting.peakFix); // [!code --]
						flag11 = true; // [!code ++]
					}
					param.x = orgX; // [!code --]
				}
			} // [!code --]
			if (blockDir == 1 || blockDir == 2) // [!code --]
			{ // [!code --]
				param.y = orgY; // [!code --]
				param.z = orgZ; // [!code --]
				param.dir = 1; // [!code --]
				Room room4 = this.cell.Right.room ?? this.cell.room; // [!code --]
				if (room4 != null && this.tileType.IsWall) // [!code --]
				else // [!code ++]
				{
					if (room4.lot.idDeco != 0 && !this.cell.hasDoor) // [!code --]
					{ // [!code --]
						param.tile = -EMono.sources.blocks.rows[0].ConvertTile(1000 + room4.lot.idDeco); // [!code --]
						param.matColor = room4.lot.colDeco; // [!code --]
						param.y += (float)room4.lot.decoFix * 0.01f; // [!code --]
						rendererWallDeco.Draw(param); // [!code --]
						param.y = orgY; // [!code --]
					} // [!code --]
					if (room4.lot.idDeco2 != 0 && roomHeight != 0f && !flag11 && (float)room4.lot.decoFix2 * 0.01f + heightLimitDeco < roomHeight + maxHeight - param.y) // [!code --]
					{ // [!code --]
						param.tile = -EMono.sources.blocks.rows[0].ConvertTile(1000 + room4.lot.idDeco2); // [!code --]
						param.matColor = room4.lot.colDeco2; // [!code --]
						param.y += (float)room4.lot.decoFix2 * 0.01f; // [!code --]
						param.z += (float)room4.lot.decoFix2 * 0.01f * heightModDeco; // [!code --]
						rendererWallDeco.Draw(param); // [!code --]
						param.y = orgY; // [!code --]
						param.z = orgZ; // [!code --]
					} // [!code --]
					thingPos.y += num30; // [!code ++]
					_actorPos.x += pref.x * (float)((!t.flipX) ? 1 : (-1)); // [!code ++]
					_actorPos.z += pref.z; // [!code ++]
					thingPos.z += pref.z; // [!code ++]
				}
				if (blockDir == 2 && this.cell.room == null && this.cell.Right.room != null) // [!code --]
				if (t.isFloating && isWater && !hasBridge && !flag) // [!code ++]
				{
					Room room5 = this.cell.Right.room; // [!code --]
					maxHeight = (float)(cz - cx) * screen.tileAlign.y + (float)room5.lot.mh * _heightMod.y; // [!code --]
					if (showRoof) // [!code --]
					flag = true; // [!code ++]
					float num31 = ((this.cell._bridge != 0) ? sourceBridge.tileType.FloorHeight : sourceFloor.tileType.FloorHeight); // [!code ++]
					orgY += 0.01f * floatY - num31; // [!code ++]
					if (!t.trait.IsChangeFloorHeight) // [!code ++]
					{
						roomHeight = room5.lot.realHeight; // [!code --]
						num27 = num30; // [!code ++]
					}
					else if ((noRoofMode && currentRoom == null) || (_lowblock && !this.tileType.ForceRpeatBlock)) // [!code --]
					_actorPos.y += 0.01f * floatY - num31; // [!code ++]
					if (liquidLv > 10) // [!code ++]
					{
						roomHeight = 0f; // [!code --]
						liquidLv = TileType.FloorWaterShallow.LiquidLV * 10; // [!code ++]
					}
					else // [!code --]
					liquidLv -= (int)(floatY * 0.5f); // [!code ++]
					if (liquidLv < 0) // [!code ++]
					{
						int num38 = ((room5.data.maxHeight == 0) ? 2 : room5.data.maxHeight); // [!code --]
						roomHeight = EMono.setting.render.roomHeightMod * (float)((room5.lot.height < num38) ? room5.lot.height : num38) + 0.01f * (float)room5.lot.heightFix; // [!code --]
						liquidLv = 0; // [!code ++]
					}
					param.liquidLv = liquidLv; // [!code ++]
				}
				Cell back2 = this.cell.Back; // [!code --]
				if (blockDir == 2 && back2.sourceBlock.tileType.IsWallOrFence && !this.cell.crossWall) // [!code --]
				num23 = num30; // [!code ++]
				if (t.sourceCard.multisize && !t.trait.IsGround) // [!code ++]
				{
					_sourceBlock = back2.sourceBlock; // [!code --]
					param.mat = back2.matBlock; // [!code --]
					num29 += zSetting.multiZ; // [!code ++]
				}
				else // [!code --]
				orgZ += t.renderer.data.stackZ; // [!code ++]
				if (param.liquidLv > 0) // [!code ++]
				{
					_sourceBlock = sourceBlock; // [!code --]
					param.mat = matBlock; // [!code --]
					param.liquidLv += pref.liquidMod; // [!code ++]
					if (param.liquidLv < 1) // [!code ++]
					{ // [!code ++]
						param.liquidLv = 1; // [!code ++]
					} // [!code ++]
					else if (param.liquidLv > 99 + pref.liquidModMax) // [!code ++]
					{ // [!code ++]
						param.liquidLv = 99 + pref.liquidModMax; // [!code ++]
					} // [!code ++]
				}
				this.tileType = _sourceBlock.tileType; // [!code --]
				param.tile = (tile = -_sourceBlock._tiles[0] + ((flag11 && this.tileType.UseLowBlock) ? (-32) : 0)); // [!code --]
				if (_sourceBlock.useAltColor) // [!code --]
			} // [!code ++]
			if (!isInstalled || !tileType.UseMountHeight) // [!code ++]
			{ // [!code ++]
				if (t.altitude != 0) // [!code ++]
				{
					param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.altColor, _sourceBlock.colorMod)); // [!code --]
					_actorPos += altitudeFix * t.altitude; // [!code ++]
					if (!t.isRoofItem) // [!code ++]
					{ // [!code ++]
						num25 += (float)t.altitude; // [!code ++]
						num24 = t.altitude; // [!code ++]
					} // [!code ++]
				}
				else // [!code --]
				if (num25 >= 2f && ((this.cell.Back.room != null && this.cell.Back.IsRoomEdge) || (this.cell.Left.room != null && this.cell.Left.IsRoomEdge)) && hideHang && (this.cell.room?.lot != currentLot || (!this.cell.lotWall && this.cell.room != currentRoom))) // [!code ++]
				{
					param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref param.mat.matColor, _sourceBlock.colorMod)); // [!code --]
					continue; // [!code ++]
				}
				param.color += _rightWallShade; // [!code --]
				if (roomHeight == 0f || flag11 || !this.tileType.RepeatBlock) // [!code --]
				if (t.freePos) // [!code ++]
				{
					if (!this.cell.hasDoor) // [!code --]
					if (t.isRoofItem) // [!code ++]
					{
						_sourceBlock.renderData.Draw(param); // [!code --]
						_actorPos.x += t.fx; // [!code ++]
						_actorPos.y += t.fy - (float)t.altitude * altitudeFix.y; // [!code ++]
					} // [!code ++]
					else // [!code ++]
					{ // [!code ++]
						_actorPos.x = orgX + t.fx - freePos.x; // [!code ++]
						_actorPos.y = orgY + t.fy - freePos.y; // [!code ++]
					}
				}
				else // [!code --]
				if (t.trait is TraitDoor && (t.trait as TraitDoor).IsOpen()) // [!code ++]
				{
					_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFix, this.cell.hasDoor, this.cell.effect?.FireAmount ?? 0); // [!code --]
					_actorPos.z += -0.5f; // [!code ++]
				}
				if ((this.cell.Right.HasWallOrFence && this.cell.Right.blockDir != 1) != this.cell.isToggleWallPillar && (blockDir != 2 || !this.cell.isToggleWallPillar)) // [!code --]
			} // [!code ++]
			if (!t.sourceCard.multisize || (t.pos.x == cx && t.pos.z == cz)) // [!code ++]
			{ // [!code ++]
				if (iconMode != 0) // [!code ++]
				{
					if (this.cell.Left.IsSnowTile && this.cell.Front.IsSnowTile) // [!code --]
					int num32 = 0; // [!code ++]
					switch (iconMode) // [!code ++]
					{
						param.snow = true; // [!code --]
					case CardIconMode.Visibility: // [!code ++]
						if (t.isMasked) // [!code ++]
						{ // [!code ++]
							num32 = 17; // [!code ++]
						} // [!code ++]
						break; // [!code ++]
					case CardIconMode.State: // [!code ++]
						if (t.placeState == PlaceState.installed) // [!code ++]
						{ // [!code ++]
							num32 = 18; // [!code ++]
						} // [!code ++]
						break; // [!code ++]
					case CardIconMode.Deconstruct: // [!code ++]
						if (t.isDeconstructing) // [!code ++]
						{ // [!code ++]
							num32 = 14; // [!code ++]
						} // [!code ++]
						break; // [!code ++]
					}
					orgX = param.x; // [!code --]
					param.tile = _sourceBlock._tiles[0] + ((flag11 && this.tileType.UseLowBlock && !flag9) ? 32 : 0) + (this.tileType.IsFence ? 32 : 64); // [!code --]
					if (!flag9 && (roomHeight == 0f || !this.tileType.RepeatBlock || flag11)) // [!code --]
					if (t.isNPCProperty && !EMono.debug.godBuild) // [!code ++]
					{
						_sourceBlock.renderData.Draw(param); // [!code --]
						num32 = 13; // [!code ++]
					}
					else // [!code --]
					if (num32 != 0) // [!code ++]
					{
						_sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFix); // [!code --]
						passGuideBlock.Add(_actorPos.x, _actorPos.y, _actorPos.z - 10f, num32); // [!code ++]
					}
					param.x = orgX; // [!code --]
				}
				t.SetRenderParam(param); // [!code ++]
				if (_lowblock && t.trait.UseLowblock && !this.cell.HasFullBlock) // [!code ++]
				{ // [!code ++]
					param.tile += ((param.tile < 0f) ? (-64) : 64); // [!code ++]
				} // [!code ++]
				if (t.trait is TraitTrolley && EMono.pc.ai is AI_Trolley aI_Trolley && aI_Trolley.trolley.owner == t) // [!code ++]
				{ // [!code ++]
					RenderParam _param = new RenderParam(param); // [!code ++]
					EMono.core.actionsLateUpdate.Add(delegate // [!code ++]
					{ // [!code ++]
						t.SetRenderParam(_param); // [!code ++]
						_actorPos.x = EMono.pc.renderer.position.x; // [!code ++]
						_actorPos.y = EMono.pc.renderer.position.y - pref.height; // [!code ++]
						_actorPos.z = EMono.pc.renderer.position.z + 0.02f; // [!code ++]
						t.renderer.Draw(_param, ref _actorPos, !t.noShadow && (shadow || tileType.AlwaysShowShadow)); // [!code ++]
					}); // [!code ++]
				} // [!code ++]
				else // [!code ++]
				{ // [!code ++]
					t.renderer.Draw(param, ref _actorPos, !t.noShadow && (shadow || tileType.AlwaysShowShadow)); // [!code ++]
				} // [!code ++]
			} // [!code ++]
			if (isInstalled) // [!code ++]
			{ // [!code ++]
				num26 += pref.stackX * (float)((!t.flipX) ? 1 : (-1)); // [!code ++]
			}
			param.x = orgX; // [!code ++]
			param.y = orgY;
			param.z = orgZ;
			break; // [!code --]
		} // [!code --]
		case BlockRenderMode.HalfBlock: // [!code --]
			param.color = floorLight;
			_sourceBlock = ((sourceBlock.id == 5) ? EMono.sources.blocks.rows[matBlock.defBlock] : sourceBlock); // [!code --]
			param.tile = _sourceBlock._tiles[0]; // [!code --]
			param.matColor = ((_sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, _sourceBlock.colorMod)); // [!code --]
			param.tile2 = _sourceBlock.sourceAutoFloor._tiles[0]; // [!code --]
			param.halfBlockColor = ((_sourceBlock.sourceAutoFloor.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, _sourceBlock.sourceAutoFloor.colorMod)); // [!code --]
			sourceBlock.renderData.Draw(param); // [!code --]
			break; // [!code --]
		case BlockRenderMode.Pillar: // [!code --]
		{ // [!code --]
			RenderData renderData2 = sourceBlock.renderData; // [!code --]
			param.tile = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length]; // [!code --]
			param.matColor = ((sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, sourceBlock.colorMod)); // [!code --]
			int num39 = this.cell.objDir + ((this.cell.objDir >= 7) ? this.cell.objDir : 0) + 1; // [!code --]
			if (num39 == 0) // [!code --]
			{ // [!code --]
				renderData2.Draw(param); // [!code --]
			} // [!code --]
			else // [!code --]
			thing = t; // [!code ++]
			if (pref.Float) // [!code ++]
			{
				renderData2.DrawRepeat(param, num39, sourceBlock.tileType.RepeatSize); // [!code --]
				liquidLv = 0; // [!code ++]
			}
			param.tile = renderData2.idShadow; // [!code --]
			SourcePref shadowPref2 = renderData2.shadowPref; // [!code --]
			int shadow4 = shadowPref2.shadow; // [!code --]
			passShadow.AddShadow(param.x + renderData2.offsetShadow.x, param.y + renderData2.offsetShadow.y, param.z + renderData2.offsetShadow.z, ShadowData.Instance.items[shadow4], shadowPref2, 0, param.snow); // [!code --]
			break; // [!code --]
		}
		default: // [!code --]
			param.color = floorLight; // [!code --]
			param.tile = sourceBlock._tiles[this.cell.blockDir % sourceBlock._tiles.Length] + ((_lowblock && this.tileType.UseLowBlock) ? 3000000 : 0); // [!code --]
			param.matColor = ((sourceBlock.colorMod == 0) ? 104025 : GetColorInt(ref matBlock.matColor, sourceBlock.colorMod)); // [!code --]
			if (roomHeight == 0f) // [!code --]
	} // [!code ++]
	orgY += num27; // [!code ++]
	if (detail.charas.Count <= 0) // [!code ++]
	{ // [!code ++]
		return; // [!code ++]
	} // [!code ++]
	param.shadowFix = 0f - num28; // [!code ++]
	param.color += 1310720f; // [!code ++]
	float max = zSetting.max2; // [!code ++]
	for (int n = 0; n < detail.charas.Count; n++) // [!code ++]
	{ // [!code ++]
		Chara chara = detail.charas[n]; // [!code ++]
		if (chara.host != null) // [!code ++]
		{ // [!code ++]
			continue; // [!code ++]
		} // [!code ++]
		if (chara != EMono.pc && chara != LayerDrama.alwaysVisible) // [!code ++]
		{ // [!code ++]
			if (chara.mimicry != null && chara.mimicry.IsThing) // [!code ++]
			{
				sourceBlock.renderData.Draw(param); // [!code --]
				if (flag3 || fogged || !this.cell.isSeen) // [!code ++]
				{ // [!code ++]
					continue; // [!code ++]
				} // [!code ++]
			}
			else // [!code --]
			else if (flag3 || fogged || (!showAllCards && !EMono.player.CanSee(chara))) // [!code ++]
			{
				sourceBlock.renderData.DrawRepeatTo(param, maxHeight, roomHeight, ref renderSetting.peakFixBlock); // [!code --]
				continue; // [!code ++]
			}
			break; // [!code --]
		}
	} // [!code --]
	if (this.cell.pcSync && EMono.player.lightPower > 0f && !cinemaMode) // [!code --]
	{ // [!code --]
		if (this.cell.room != null || !this.cell.IsRoomEdge || !showRoof) // [!code --]
		_actorPos.x = orgX; // [!code ++]
		_actorPos.y = orgY; // [!code ++]
		_actorPos.z = orgZ; // [!code ++]
		chara.SetRenderParam(param); // [!code ++]
		_ = chara.IsAliveInCurrentZone; // [!code ++]
		if (chara.isRestrained) // [!code ++]
		{
			goto IL_6f8a; // [!code --]
			TraitShackle restrainer = chara.GetRestrainer(); // [!code ++]
			if (restrainer != null) // [!code ++]
			{ // [!code ++]
				Vector3 getRestrainPos = restrainer.GetRestrainPos; // [!code ++]
				if (getRestrainPos != default(Vector3)) // [!code ++]
				{ // [!code ++]
					Vector3 position = restrainer.owner.renderer.position; // [!code ++]
					float defCharaHeight = EMono.setting.render.defCharaHeight; // [!code ++]
					float num33 = getRestrainPos.y + defCharaHeight - ((chara.Pref.height == 0f) ? defCharaHeight : chara.source.pref.height); // [!code ++]
					_actorPos.x = position.x + getRestrainPos.x * (float)((restrainer.owner.dir % 2 == 0) ? 1 : (-1)); // [!code ++]
					_actorPos.y = position.y + num33; // [!code ++]
					_actorPos.z = position.z + getRestrainPos.z; // [!code ++]
					param.liquidLv = 0; // [!code ++]
					param.shadowFix = orgY - _actorPos.y; // [!code ++]
					chara.renderer.SetFirst(first: true); // [!code ++]
					chara.renderer.Draw(param, ref _actorPos, drawShadow: true); // [!code ++]
					param.shadowFix = 0f; // [!code ++]
					continue; // [!code ++]
				} // [!code ++]
			} // [!code ++]
		}
		if (this.cell._block == 0 || !this.cell.sourceBlock.tileType.RepeatBlock) // [!code --]
		if (!chara.sourceCard.multisize || (chara.pos.x == cx && chara.pos.z == cz)) // [!code ++]
		{
			Room obj = this.cell.FrontRight.room; // [!code --]
			if (obj == null || !obj.HasRoof) // [!code --]
			if (chara.IsDeadOrSleeping && chara.IsPCC) // [!code ++]
			{ // [!code ++]
				float num34 = chara.renderer.data.size.y * 0.3f; // [!code ++]
				if (thingPos.y > max) // [!code ++]
				{ // [!code ++]
					thingPos.y = max; // [!code ++]
				} // [!code ++]
				float num35 = thingPos.y + num34; // [!code ++]
				float num36 = (float)n * -0.01f; // [!code ++]
				if (num35 > zSetting.thresh1) // [!code ++]
				{ // [!code ++]
					num36 = zSetting.mod1; // [!code ++]
				} // [!code ++]
				_actorPos.x += thingPos.x; // [!code ++]
				_actorPos.y += thingPos.y; // [!code ++]
				_actorPos.z += renderSetting.laydownZ + num36; // [!code ++]
				param.liquidLv = ((thingPos.y == 0f && liquidLv > 0) ? 90 : 0); // [!code ++]
				thingPos.y += num34 * 0.8f; // [!code ++]
				chara.renderer.Draw(param, ref _actorPos, liquidLv == 0); // [!code ++]
			} // [!code ++]
			else // [!code ++]
			{
				goto IL_6f8a; // [!code --]
				param.liquidLv = liquidLv; // [!code ++]
				if (isUnderwater) // [!code ++]
				{ // [!code ++]
					if (chara.Pref.FloatUnderwater) // [!code ++]
					{ // [!code ++]
						float num37 = ((this.cell._bridge != 0) ? sourceBridge.tileType.FloorHeight : sourceFloor.tileType.FloorHeight); // [!code ++]
						float num38 = floatYs[chara.uid % 10] + 10f + (float)(chara.uid % 30); // [!code ++]
						orgY += 0.01f * num38 - num37; // [!code ++]
						_actorPos.y += 0.01f * num38 - num37; // [!code ++]
						param.shadowFix -= 0.01f * num38 - num37; // [!code ++]
					} // [!code ++]
				} // [!code ++]
				else if (liquidLv > 0) // [!code ++]
				{ // [!code ++]
					if (chara.Pref.Float && !flag && !hasBridge) // [!code ++]
					{ // [!code ++]
						if (liquidLv > 20) // [!code ++]
						{ // [!code ++]
							float num39 = ((this.cell._bridge != 0) ? sourceBridge.tileType.FloorHeight : sourceFloor.tileType.FloorHeight); // [!code ++]
							orgY += 0.01f * floatY - num39; // [!code ++]
							_actorPos.y += 0.01f * floatY - num39; // [!code ++]
							int num40 = TileType.FloorWaterShallow.LiquidLV * 10; // [!code ++]
							num40 -= (int)(floatY * 0.5f); // [!code ++]
							param.liquidLv = num40; // [!code ++]
						} // [!code ++]
						else // [!code ++]
						{ // [!code ++]
							param.liquidLv -= 20; // [!code ++]
						} // [!code ++]
					} // [!code ++]
					param.liquidLv += chara.Pref.liquidMod; // [!code ++]
					if (param.liquidLv < 1) // [!code ++]
					{ // [!code ++]
						param.liquidLv = 1; // [!code ++]
					} // [!code ++]
					else if (param.liquidLv > 99 + chara.Pref.liquidModMax) // [!code ++]
					{ // [!code ++]
						param.liquidLv = 99 + chara.Pref.liquidModMax; // [!code ++]
					} // [!code ++]
				} // [!code ++]
				if (!chara.IsPC && !chara.renderer.IsMoving && detail.charas.Count > 1 && (detail.charas.Count != 2 || !detail.charas[0].IsDeadOrSleeping || !detail.charas[0].IsPCC)) // [!code ++]
				{ // [!code ++]
					_actorPos += renderSetting.charaPos[1 + ((num22 < 4) ? num22 : 3)]; // [!code ++]
				} // [!code ++]
				_actorPos.z += 0.01f * (float)n + renderSetting.charaZ; // [!code ++]
				num22++; // [!code ++]
				if (flag10) // [!code ++]
				{ // [!code ++]
					_actorPos.z += chara.renderer.data.hangedFixZ; // [!code ++]
				} // [!code ++]
				chara.renderer.Draw(param, ref _actorPos, liquidLv == 0); // [!code ++]
			}
		}
		param.x = orgX; // [!code ++]
		param.y = orgY; // [!code ++]
		param.z = orgZ; // [!code ++]
	} // [!code ++]
	return; // [!code ++]
	IL_6fd9: // [!code ++]
	if (!showRoof || !roof || this.cell.room == null || this.cell.Front.room == null || this.cell.Right.room == null) // [!code ++]
	{ // [!code ++]
		param.tile = num13; // [!code ++]
		rendererFov.Draw(param); // [!code ++]
	}
	goto IL_6fea; // [!code --]
	goto IL_7039; // [!code ++]
	void Draw(int tile)
	{
		pass = passEdge;
```

## Card

[`public void HatchEgg()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/Card.cs#L5961-L5967)
```cs:line-numbers=5961
		}
		for (int i = 0; i < 1 + EClass.rnd(num + 1); i++)
		{
			EClass._zone.SpawnMob(array[num], point.GetNearestPoint(allowBlock: false, allowChara: false)); // [!code --]
			Chara chara = EClass._zone.SpawnMob(array[num], point.GetNearestPoint(allowBlock: false, allowChara: false)); // [!code ++]
			if (chara != null && isChara) // [!code ++]
			{ // [!code ++]
				chara.SetHostility(Chara.OriginalHostility); // [!code ++]
			} // [!code ++]
		}
	}

```

## ConTransmute

[`public override void Tick()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/ConTransmute.cs#L15-L20)
```cs:line-numbers=15
	}

	public override void OnStart()
	{ // [!code ++]
		Change(); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void Change() // [!code ++]
	{
		owner._CreateRenderer();
		if (owner.IsPCParty)
```

## ConTransmuteHuman

[`using System.Collections.Generic;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/ConTransmuteHuman.cs#L1-L22)
```cs:line-numbers=1
using System.Collections.Generic;
using System.Linq; // [!code ++]
using Newtonsoft.Json;
using UnityEngine; // [!code ++]

public class ConTransmuteHuman : ConBaseTransmuteMimic
{
	[JsonProperty]
	public Chara chara;

	[JsonProperty] // [!code ++]
	public int lastHP; // [!code ++]
 // [!code ++]
	public override Card Card => chara;

	public bool IsBaby => chara.HasElement(1232);

	public bool IsNyaru => owner?.id == "nyaruruga"; // [!code ++]
 // [!code ++]
	public override bool HasDuration => false;

	public override bool ShouldRevealOnContact => false;

	public override bool ShouldRevealOnPush => false;

	public override bool ShouldRevealOnDamage => (float)EClass.rnd(50) > (float)owner.hp / (float)owner.MaxHP * 100f; // [!code --]
	public override bool ShouldRevealOnDamage // [!code ++]
	{ // [!code ++]
		get // [!code ++]
		{ // [!code ++]
			if (!IsNyaru) // [!code ++]
			{ // [!code ++]
				return (float)EClass.rnd(50) > (float)owner.hp / (float)owner.MaxHP * 100f; // [!code ++]
			} // [!code ++]
			return false; // [!code ++]
		} // [!code ++]
	} // [!code ++]

	public override bool ShouldEndMimicry(Act act)
	{
```

[`public override void OnBeforeStart()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/ConTransmuteHuman.cs#L32-L66)
```cs:line-numbers=32
	{
		if (chara == null)
		{
			List<Thing> list = owner.things.List((Thing t) => t.trait is TraitFigure { source: not null } traitFigure && !traitFigure.source.multisize, onlyAccessible: true); // [!code --]
			if (list.Count > 0) // [!code --]
			if (IsNyaru) // [!code ++]
			{
				chara = CharaGen.Create((list.RandomItem().trait as TraitFigure).source.id); // [!code --]
				NyaruSpecial(); // [!code ++]
			}
			else
			{
				List<Chara> list2 = owner.pos.ListCharasInRadius(owner, 5, delegate(Chara c) // [!code --]
				List<Thing> list = owner.things.List((Thing t) => t.trait is TraitFigure { source: not null } traitFigure && !traitFigure.source.multisize, onlyAccessible: true); // [!code ++]
				if (list.Count > 0) // [!code ++]
				{
					if (!c.IsMultisize && c.IsHumanSpeak) // [!code --]
					chara = CharaGen.Create((list.RandomItem().trait as TraitFigure).source.id); // [!code ++]
				} // [!code ++]
				else // [!code ++]
				{ // [!code ++]
					List<Chara> list2 = owner.pos.ListCharasInRadius(owner, 5, delegate(Chara c) // [!code ++]
					{
						CardRenderer renderer = c.renderer; // [!code --]
						if (renderer != null && !renderer.hasActor) // [!code --]
						if (!c.IsMultisize && c.IsHumanSpeak) // [!code ++]
						{
							return !c.HasElement(1427); // [!code --]
							CardRenderer renderer = c.renderer; // [!code ++]
							if (renderer != null && !renderer.hasActor) // [!code ++]
							{ // [!code ++]
								return !c.HasElement(1427); // [!code ++]
							} // [!code ++]
						}
						return false; // [!code ++]
					}); // [!code ++]
					if (list2.Count > 0) // [!code ++]
					{ // [!code ++]
						chara = list2.RandomItem().Duplicate(); // [!code ++]
					} // [!code ++]
					else // [!code ++]
					{ // [!code ++]
						chara = CharaGen.CreateFromFilter("c_guest"); // [!code ++]
					}
					return false; // [!code --]
				}); // [!code --]
				if (list2.Count > 0) // [!code --]
				{ // [!code --]
					chara = list2.RandomItem().Duplicate(); // [!code --]
				} // [!code --]
				else // [!code --]
				{ // [!code --]
					chara = CharaGen.CreateFromFilter("c_guest"); // [!code --]
				}
			}
		}
		base.OnBeforeStart();
	}
 // [!code ++]
	public void NyaruSpecial() // [!code ++]
	{ // [!code ++]
		IEnumerable<SourceChara.Row> ie = EClass.sources.charas.rows.Where((SourceChara.Row a) => !a.multisize && !a.actCombat.IsEmpty() && !a.HasTag(CTAG.suicide) && !a.HasTag(CTAG.noRandomProduct)); // [!code ++]
		chara = CharaGen.Create(ie.RandomItem().id); // [!code ++]
		lastHP = owner.hp; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public override void Tick() // [!code ++]
	{ // [!code ++]
		if (IsNyaru && Mathf.Abs(owner.hp - lastHP) >= owner.MaxHP / 10) // [!code ++]
		{ // [!code ++]
			NyaruSpecial(); // [!code ++]
			Change(); // [!code ++]
		} // [!code ++]
		base.Tick(); // [!code ++]
	} // [!code ++]
}
```

## CoreDebug

[`public enum StartScene`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/CoreDebug.cs#L58-L63)
```cs:line-numbers=58
		Tefra
	}

	public enum BodyCode // [!code ++]
	{ // [!code ++]
		head = 30, // [!code ++]
		neck = 31, // [!code ++]
		torso = 32, // [!code ++]
		back = 33, // [!code ++]
		waist = 37, // [!code ++]
		arm = 34, // [!code ++]
		hand = 35, // [!code ++]
		finger = 36, // [!code ++]
		leg = 38, // [!code ++]
		foot = 39, // [!code ++]
		lightsource = 45 // [!code ++]
	} // [!code ++]
 // [!code ++]
	[Serializable]
	public class MatColorTest
	{
```

[`public static string Rust()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/CoreDebug.cs#L2218-L2227)
```cs:line-numbers=2218
	[ConsoleCommand("")]
	public static string FlyMode()
	{
		if (!CheatEnabled()) // [!code ++]
		{ // [!code ++]
			return EnableCheat; // [!code ++]
		} // [!code ++]
		EClass.pc.AddCondition<ConLevitate>();
		return "I can fly!";
	}

	[ConsoleCommand("")] // [!code ++]
	public static string AddBodyPart(BodyCode bodyCode) // [!code ++]
	{ // [!code ++]
		if (!CheatEnabled()) // [!code ++]
		{ // [!code ++]
			return EnableCheat; // [!code ++]
		} // [!code ++]
		EClass.pc.body.AddBodyPart((int)bodyCode); // [!code ++]
		EClass.pc.body.RefreshBodyParts(); // [!code ++]
		WidgetEquip.OnChangeBodyPart(); // [!code ++]
		return "Done."; // [!code ++]
	} // [!code ++]
 // [!code ++]
	[ConsoleCommand("")] // [!code ++]
	public static string RemoveBodyPart(BodyCode bodyCode) // [!code ++]
	{ // [!code ++]
		if (!CheatEnabled()) // [!code ++]
		{ // [!code ++]
			return EnableCheat; // [!code ++]
		} // [!code ++]
		EClass.pc.body.RemoveBodyPart((int)bodyCode); // [!code ++]
		EClass.pc.body.RefreshBodyParts(); // [!code ++]
		WidgetEquip.OnChangeBodyPart(); // [!code ++]
		return "Done."; // [!code ++]
	} // [!code ++]
 // [!code ++]
	[ConsoleCommand("")]
	public static string ChangeRace(string id = "?")
	{
```

[`public static string ChangeRace(string id = "?")`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/CoreDebug.cs#L2257-L2262)
```cs:line-numbers=2257
		EClass.pc.body.AddBodyPart(45);
		EClass.pc.body.AddBodyPart(44);
		EClass.pc.body.Equip(thing);
		EClass.pc.body.RefreshBodyParts(); // [!code ++]
		WidgetEquip.OnChangeBodyPart(); // [!code ++]
		return "Done.";
	}
	string text = "";
```

## ENC

[`public class ENC`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/ENC.cs#L20-L26)
```cs:line-numbers=20

	public const int absorbHP = 660;

	public const int convertLightning = 852; // [!code --]
	public const int convertCold = 851; // [!code ++]

	public const int absorbSP = 662;

```

[`public class ENC`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/ENC.cs#L46-L52)
```cs:line-numbers=46

	public const int mod_feint = 623;

	public const int convertCold = 851; // [!code --]
	public const int convertLightning = 852; // [!code ++]

	public const int mod_flurry = 621;

```

[`public class ENC`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/ENC.cs#L102-L108)
```cs:line-numbers=102

	public const int mod_cleave = 622;

	public const int counter = 380; // [!code --]
	public const int basher = 381; // [!code ++]

	public const int rusher = 382;

```

[`public class ENC`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/ENC.cs#L146-L167)
```cs:line-numbers=146

	public const int sustain_MAG = 446;

	public const int basher = 381; // [!code --]
	public const int counter = 380; // [!code ++]

	public const int sustain_figure = 450;

	public const int sustain_CHA = 447; // [!code --]
 // [!code --]
	public const int mod_splash = 608; // [!code --]
 // [!code --]
	public const int mod_scatter = 607; // [!code --]
 // [!code --]
	public const int mod_drill = 606; // [!code --]
 // [!code --]
	public const int nonLethal = 485; // [!code --]
 // [!code --]
	public const int safetyMeasure = 486; // [!code --]
 // [!code --]
	public const int creativeChef = 487;

	public const int deathagony = 488;
```

[`public class ENC`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/ENC.cs#L180-L233)
```cs:line-numbers=180

	public const int mod_ammo_knockback = 603;

	public const int mod_talisman = 609; // [!code --]
 // [!code --]
	public const int mod_ammo_recover = 604;

	public const int mod_precision = 605;

	public const int bane_fish = 467; // [!code --]
	public const int mod_drill = 606; // [!code ++]

	public const int bane_dragon = 460; // [!code --]
	public const int mod_scatter = 607; // [!code ++]

	public const int optimizeMana = 483; // [!code --]
	public const int mod_splash = 608; // [!code ++]

	public const int bane_undead = 461; // [!code --]
	public const int mod_talisman = 609; // [!code ++]

	public const int mod_chaser = 620; // [!code --]
	public const int safetyMeasure = 486; // [!code ++]

	public const int bane_fairy = 462; // [!code --]
	public const int nonLethal = 485; // [!code ++]
 // [!code ++]
	public const int sustain_CHA = 447; // [!code ++]

	public const int force_weapon = 482;

	public const int revealFaith = 481; // [!code --]
	public const int bane_animal = 463; // [!code ++]

	public const int strongStomach = 480; // [!code --]
	public const int bane_man = 464; // [!code ++]

	public const int bane_animal = 463; // [!code --]
	public const int bane_machine = 465; // [!code ++]

	public const int bane_all = 468; // [!code --]
	public const int bane_god = 466; // [!code ++]

	public const int slot_rune = 484;

	public const int bane_man = 464; // [!code --]
	public const int bane_undead = 461; // [!code ++]

	public const int bane_machine = 465; // [!code --]
	public const int bane_fish = 467; // [!code ++]

	public const int bane_god = 466; // [!code --]
	public const int bane_all = 468; // [!code ++]
 // [!code ++]
	public const int strongStomach = 480; // [!code ++]
 // [!code ++]
	public const int bane_dragon = 460; // [!code ++]
 // [!code ++]
	public const int revealFaith = 481; // [!code ++]
 // [!code ++]
	public const int mod_chaser = 620; // [!code ++]
 // [!code ++]
	public const int optimizeMana = 483; // [!code ++]
 // [!code ++]
	public const int bane_fairy = 462; // [!code ++]

	public static readonly int[] IDS = new int[108]
	{
		655, 654, 653, 652, 651, 641, 650, 656, 640, 660,
		852, 662, 663, 664, 665, 666, 667, 850, 624, 865, // [!code --]
		864, 661, 623, 851, 621, 422, 423, 424, 425, 426, // [!code --]
		851, 662, 663, 664, 665, 666, 667, 850, 624, 865, // [!code ++]
		864, 661, 623, 852, 621, 422, 423, 424, 425, 426, // [!code ++]
		427, 428, 429, 430, 431, 432, 435, 436, 437, 438,
		439, 440, 441, 442, 443, 444, 421, 445, 420, 418,
		622, 380, 382, 383, 400, 401, 402, 403, 404, 405, // [!code --]
		622, 381, 382, 383, 400, 401, 402, 403, 404, 405, // [!code ++]
		406, 407, 408, 409, 410, 411, 412, 414, 415, 416,
		417, 419, 446, 381, 450, 447, 608, 607, 606, 485, // [!code --]
		486, 487, 488, 489, 490, 491, 600, 601, 602, 603, // [!code --]
		609, 604, 605, 467, 460, 483, 461, 620, 462, 482, // [!code --]
		481, 480, 463, 468, 484, 464, 465, 466 // [!code --]
		417, 419, 446, 380, 450, 487, 488, 489, 490, 491, // [!code ++]
		600, 601, 602, 603, 604, 605, 606, 607, 608, 609, // [!code ++]
		486, 485, 447, 482, 463, 464, 465, 466, 484, 461, // [!code ++]
		467, 468, 480, 460, 481, 620, 483, 462 // [!code ++]
	};
}
```

## FACTION

[``](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/FACTION.cs#L4-L14)
```cs:line-numbers=4

public class FACTION
{
	public const int bfGum = 3803; // [!code --]
 // [!code --]
	public const int bfMushroom = 3801; // [!code --]
 // [!code --]
	public const int bfSilica = 3802; // [!code --]
	public const int fRation = 2207; // [!code ++]

	public const int actBuildMine = 4001;

```

[`public class FACTION`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/FACTION.cs#L16-L28)
```cs:line-numbers=16

	public const int bfStart = 3900;

	public const int bfChitin = 3805; // [!code ++]
 // [!code ++]
	public const int bfIce = 3804;

	public const int bfGum = 3803; // [!code ++]
 // [!code ++]
	public const int bfSilica = 3802; // [!code ++]
 // [!code ++]
	public const int bfMushroom = 3801; // [!code ++]
 // [!code ++]
	public const int bfCoal = 3800;

	public const int bfChitin = 3805; // [!code --]
	public const int fLoyal = 2117; // [!code ++]

	public const int fAttraction = 2206; // [!code --]
	public const int fConstruction = 2003; // [!code ++]
 // [!code ++]
	public const int fEducation = 2116; // [!code ++]
 // [!code ++]
	public const int bfLandmark5 = 3784; // [!code ++]

	public const int fSafety = 2205;

```

[`public class FACTION`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/FACTION.cs#L42-L60)
```cs:line-numbers=42

	public const int fLuck = 2118;

	public const int fLoyal = 2117; // [!code --]
 // [!code --]
	public const int fEducation = 2116; // [!code --]
 // [!code --]
	public const int fAdmin = 2115;

	public const int fConstruction = 2003; // [!code --]
 // [!code --]
	public const int bfLandmark5 = 3784; // [!code --]
 // [!code --]
	public const int bfLandmark4 = 3783;

	public const int fRation = 2207; // [!code --]
	public const int fAttraction = 2206; // [!code ++]

	public const int bfLandmark2 = 3781;

```

[`public class FACTION`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/FACTION.cs#L88-L97)
```cs:line-numbers=88

	public const int bfCave = 3500;

	public const int bfLandmark1 = 3780; // [!code --]
 // [!code --]
	public const int actBuildInspect = 4006;

	public const int bfLandmark1 = 3780; // [!code ++]
 // [!code ++]
	public const int actBuildRecipe = 4005;

	public const int actBuildCollect = 4004;
```

[`public class FACTION`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/FACTION.cs#L112-L122)
```cs:line-numbers=112

	public static readonly int[] IDS = new int[53]
	{
		3803, 3801, 3802, 4001, 4000, 3900, 3804, 3800, 3805, 2206, // [!code --]
		2205, 2204, 2203, 2202, 2201, 2200, 2120, 2119, 2118, 2117, // [!code --]
		2116, 2115, 2003, 3784, 3783, 2207, 3781, 3782, 3705, 3704, // [!code --]
		2207, 4001, 4000, 3900, 3805, 3804, 3803, 3802, 3801, 3800, // [!code ++]
		2117, 2003, 2116, 3784, 2205, 2204, 2203, 2202, 2201, 2200, // [!code ++]
		2120, 2119, 2118, 2115, 3783, 2206, 3781, 3782, 3705, 3704, // [!code ++]
		3703, 3706, 3702, 3701, 3700, 3606, 3604, 3603, 3602, 3601,
		3605, 3500, 3780, 4006, 4005, 4004, 4003, 4002, 3707, 3708, // [!code --]
		3605, 3500, 4006, 3780, 4005, 4004, 4003, 4002, 3707, 3708, // [!code ++]
		3709, 3600, 3710
	};
}
```

## FEAT

[``](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/FEAT.cs#L3-L40)
```cs:line-numbers=3

public class FEAT
{
	public const int featPER = 1623; // [!code ++]
 // [!code ++]
	public const int featLER = 1624;

	public const int featMAG = 1625;

	public const int featWIL = 1626; // [!code --]
	public const int featHardy = 1630; // [!code ++]

	public const int featDefense = 1631; // [!code --]
	public const int featCHA = 1627; // [!code ++]

	public const int featLuck = 1628;

	public const int featSPD = 1629;

	public const int featHardy = 1630; // [!code --]
 // [!code --]
	public const int featPER = 1623; // [!code --]
	public const int featEND = 1622; // [!code ++]

	public const int featEvade = 1632; // [!code --]
	public const int featDefense = 1631; // [!code ++]

	public const int featCHA = 1627; // [!code --]
	public const int featWIL = 1626; // [!code ++]

	public const int featEND = 1622; // [!code --]
	public const int featDEX = 1621; // [!code ++]

	public const int featManaMeat = 1421;

	public const int featSTR = 1620; // [!code --]
 // [!code --]
	public const int featStamina = 1612;

	public const int featMana = 1611;

	public const int featLife = 1610;

	public const int featManyFace = 1429; // [!code ++]
 // [!code ++]
	public const int featMurderer = 1428;

	public const int featDisguise = 1427;
```

[`public class FEAT`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/FEAT.cs#L49-L65)
```cs:line-numbers=49

	public const int featHealer = 1422;

	public const int featSpotting = 1633; // [!code --]
	public const int featEvade = 1632; // [!code ++]

	public const int featExecutioner = 1420;

	public const int featDEX = 1621; // [!code --]
	public const int featSTR = 1620; // [!code ++]

	public const int featNegotiate = 1634; // [!code --]
	public const int featSpotting = 1633; // [!code ++]

	public const int featGourmet = 1650; // [!code --]
	public const int featDefender = 1649; // [!code ++]

	public const int featFaith = 1636; // [!code --]
	public const int featMartial = 1635; // [!code ++]

	public const int featMilitant = 1419;

```

[`public class FEAT`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/FEAT.cs#L85-L99)
```cs:line-numbers=85

	public const int featModelBeliever = 1655;

	public const int featMartial = 1635; // [!code --]
 // [!code --]
	public const int featHeavyCasting = 1654;

	public const int featDreamWaker = 1653; // [!code ++]
 // [!code ++]
	public const int featRapidArrow = 1652;

	public const int featMagicManner = 1651;

	public const int featDefender = 1649; // [!code --]
	public const int featGourmet = 1650; // [!code ++]

	public const int featRapidMagic = 1648;

```

[`public class FEAT`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/FEAT.cs#L113-L119)
```cs:line-numbers=113

	public const int featAnimalLover = 1640;

	public const int featDreamWaker = 1653; // [!code --]
	public const int featFaith = 1636; // [!code ++]
 // [!code ++]
	public const int featNegotiate = 1634; // [!code ++]

	public const int featSwordsage = 1418;

```

[`public class FEAT`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/FEAT.cs#L301-L323)
```cs:line-numbers=301

	public const int featFoxMaid = 1415;

	public static readonly int[] IDS = new int[149] // [!code --]
	public static readonly int[] IDS = new int[150] // [!code ++]
	{
		1624, 1625, 1626, 1631, 1628, 1629, 1630, 1623, 1632, 1627, // [!code --]
		1622, 1421, 1620, 1612, 1611, 1610, 1428, 1427, 1426, 1425, // [!code --]
		1424, 1423, 1422, 1633, 1420, 1621, 1634, 1650, 1636, 1419, // [!code --]
		1623, 1624, 1625, 1630, 1627, 1628, 1629, 1622, 1631, 1626, // [!code ++]
		1621, 1421, 1612, 1611, 1610, 1429, 1428, 1427, 1426, 1425, // [!code ++]
		1424, 1423, 1422, 1632, 1420, 1620, 1633, 1649, 1635, 1419, // [!code ++]
		1750, 1664, 1663, 1662, 1661, 1660, 1659, 1658, 1657, 1656,
		1655, 1635, 1654, 1652, 1651, 1649, 1648, 1647, 1646, 1645, // [!code --]
		1644, 1643, 1642, 1641, 1640, 1653, 1418, 1345, 1416, 1233, // [!code --]
		1234, 1235, 1236, 1237, 1238, 1239, 1240, 1241, 1242, 1243, // [!code --]
		1244, 1245, 1246, 1247, 1248, 1249, 1250, 1251, 1252, 1417, // [!code --]
		1271, 1272, 1273, 1274, 1275, 1276, 1290, 1291, 1232, 1300, // [!code --]
		1231, 1229, 1200, 1201, 1202, 1203, 1204, 1205, 1206, 1207, // [!code --]
		1208, 1209, 1210, 1211, 1212, 1213, 1214, 1215, 1216, 1217, // [!code --]
		1218, 1219, 1220, 1221, 1222, 1223, 1224, 1225, 1226, 1227, // [!code --]
		1228, 1230, 1305, 1270, 1315, 1414, 1413, 1412, 1411, 1410, // [!code --]
		1409, 1408, 1407, 1406, 1310, 1405, 1404, 1403, 1402, 1401, // [!code --]
		1400, 1355, 1350, 1340, 1335, 1330, 1325, 1320, 1415 // [!code --]
		1655, 1654, 1653, 1652, 1651, 1650, 1648, 1647, 1646, 1645, // [!code ++]
		1644, 1643, 1642, 1641, 1640, 1636, 1634, 1418, 1345, 1416, // [!code ++]
		1233, 1234, 1235, 1236, 1237, 1238, 1239, 1240, 1241, 1242, // [!code ++]
		1243, 1244, 1245, 1246, 1247, 1248, 1249, 1250, 1251, 1252, // [!code ++]
		1417, 1271, 1272, 1273, 1274, 1275, 1276, 1290, 1291, 1232, // [!code ++]
		1300, 1231, 1229, 1200, 1201, 1202, 1203, 1204, 1205, 1206, // [!code ++]
		1207, 1208, 1209, 1210, 1211, 1212, 1213, 1214, 1215, 1216, // [!code ++]
		1217, 1218, 1219, 1220, 1221, 1222, 1223, 1224, 1225, 1226, // [!code ++]
		1227, 1228, 1230, 1305, 1270, 1315, 1414, 1413, 1412, 1411, // [!code ++]
		1410, 1409, 1408, 1407, 1406, 1310, 1405, 1404, 1403, 1402, // [!code ++]
		1401, 1400, 1355, 1350, 1340, 1335, 1330, 1325, 1320, 1415 // [!code ++]
	};
}
public class Feat : Element
```

[`public List<string> Apply(int a, ElementContainer owner, bool hint = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/FEAT.cs#L534-L542)
```cs:line-numbers=534
				_ = a;
				_ = 0;
			}
			if (owner.Chara.IsPC && (bool)WidgetEquip.Instance) // [!code --]
			if (owner.Chara.IsPC) // [!code ++]
			{
				WidgetEquip.Instance.Rebuild(); // [!code --]
				WidgetEquip.OnChangeBodyPart(); // [!code ++]
			}
		}
		ModBase(60, (A > 5) ? ((A - 5) * -3 * invert) : 0, hide: false);
```

## InvOwnerDeliver

[`public enum Mode`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/InvOwnerDeliver.cs#L10-L15)
```cs:line-numbers=10

	public Mode mode;

	public override bool ShowButtonDeliver => mode == Mode.Crop; // [!code ++]
 // [!code ++]
	public override bool SingleTarget => mode == Mode.Tax;

	public override string langTransfer
```

## InvOwnerDraglet

[`public enum ProcessType`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/InvOwnerDraglet.cs#L25-L30)
```cs:line-numbers=25

	public virtual bool AllowStockIngredients => false;

	public virtual bool ShowButtonDeliver => false; // [!code ++]
 // [!code ++]
	public override bool AllowTransfer => true;

	public override bool AllowAutouse => false;
```

## LayerDragGrid

[`public class PutbackInfo`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/LayerDragGrid.cs#L37-L42)
```cs:line-numbers=37

	public UIButton buttonStock;

	public UIButton buttonDeliver; // [!code ++]
 // [!code ++]
	public InvOwnerDraglet owner;

	public UICurrency uiCurrency;
```

[`public LayerDragGrid SetInv(InvOwnerDraglet owner, bool refuelFromLayerDragGrid`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/LayerDragGrid.cs#L104-L109)
```cs:line-numbers=104
	buttonRefuel.SetActive(owner.ShowFuel);
	buttonAutoRefuel.SetActive(owner.ShowFuel);
	buttonStock.SetActive(owner.AllowStockIngredients);
	buttonDeliver.SetActive(owner.ShowButtonDeliver); // [!code ++]
	if (owner.CanTargetAlly && ELayer.pc.party.members.Count > 1)
	{
		listAlly.callbacks = new UIList.Callback<Chara, UIButton>
```

[`public LayerDragGrid SetInv(InvOwnerDraglet owner, bool refuelFromLayerDragGrid`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/LayerDragGrid.cs#L184-L189)
```cs:line-numbers=184
		uiIngredients.Refresh();
		RefreshCost();
	});
	buttonDeliver.SetOnClick(delegate // [!code ++]
	{ // [!code ++]
		InvOwnerDeliver deliver = owner as InvOwnerDeliver; // [!code ++]
		QuestHarvest questHarvest = ELayer.game.quests.Get<QuestHarvest>(); // [!code ++]
		int num = 0; // [!code ++]
		if (questHarvest != null) // [!code ++]
		{ // [!code ++]
			foreach (Thing item in ELayer.pc.things.List((Thing a) => deliver.ShouldShowGuide(a))) // [!code ++]
			{ // [!code ++]
				Msg.Say("farm_chest", item, Lang._weight(item.SelfWeight * item.Num)); // [!code ++]
				questHarvest.weightDelivered += item.SelfWeight * item.Num; // [!code ++]
				item.Destroy(); // [!code ++]
				num++; // [!code ++]
			} // [!code ++]
		} // [!code ++]
		if (num > 0) // [!code ++]
		{ // [!code ++]
			SE.Pick(); // [!code ++]
		} // [!code ++]
		else // [!code ++]
		{ // [!code ++]
			SE.BeepSmall(); // [!code ++]
		} // [!code ++]
		Close(); // [!code ++]
	}); // [!code ++]
	if (owner is InvOwnerRefuel)
	{
		if (refuelFromLayerDragGrid)
```

## SKILL

[`public override bool CanLink(ElementContainer owner)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/SKILL.cs#L298-L306)
```cs:line-numbers=298
	{
		if (base.source.IsWeaponEnc)
		{
			if (base.owner != null && base.owner.Card != null) // [!code --]
			if (base.owner != null && base.owner.Card != null && !base.owner.Card.IsWeapon) // [!code ++]
			{
				return !base.owner.Card.IsWeapon; // [!code --]
				return base.owner.Card.sourceCard.elementMap.ContainsKey(base.source.id); // [!code ++]
			}
			return false;
		}
```

## SourceBlock

[`public class Row : TileRow`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/SourceBlock.cs#L10-L15)
```cs:line-numbers=10

		public string idThing;

		public int[] anime; // [!code ++]
 // [!code ++]
		public int roof;

		public string autoFloor;
```

[`public override Row CreateRow()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/SourceBlock.cs#L85-L109)
```cs:line-numbers=85
			_tileType = SourceData.GetString(8),
			_idRenderData = SourceData.GetString(9),
			tiles = SourceData.GetIntArray(10),
			snowTile = SourceData.GetInt(11), // [!code --]
			colorMod = SourceData.GetInt(12), // [!code --]
			colorType = SourceData.GetString(13), // [!code --]
			value = SourceData.GetInt(14), // [!code --]
			LV = SourceData.GetInt(15), // [!code --]
			recipeKey = SourceData.GetStringArray(16), // [!code --]
			factory = SourceData.GetStringArray(17), // [!code --]
			components = SourceData.GetStringArray(18), // [!code --]
			defMat = SourceData.GetString(19), // [!code --]
			category = SourceData.GetString(20), // [!code --]
			roof = SourceData.GetInt(21), // [!code --]
			autoFloor = SourceData.GetString(22), // [!code --]
			concrete = SourceData.GetBool(23), // [!code --]
			transparent = SourceData.GetBool(24), // [!code --]
			transition = SourceData.GetIntArray(25), // [!code --]
			tag = SourceData.GetStringArray(26), // [!code --]
			soundFoot = SourceData.GetString(27), // [!code --]
			detail_JP = SourceData.GetString(28), // [!code --]
			detail = SourceData.GetString(29) // [!code --]
			anime = SourceData.GetIntArray(11), // [!code ++]
			snowTile = SourceData.GetInt(12), // [!code ++]
			colorMod = SourceData.GetInt(13), // [!code ++]
			colorType = SourceData.GetString(14), // [!code ++]
			value = SourceData.GetInt(15), // [!code ++]
			LV = SourceData.GetInt(16), // [!code ++]
			recipeKey = SourceData.GetStringArray(17), // [!code ++]
			factory = SourceData.GetStringArray(18), // [!code ++]
			components = SourceData.GetStringArray(19), // [!code ++]
			defMat = SourceData.GetString(20), // [!code ++]
			category = SourceData.GetString(21), // [!code ++]
			roof = SourceData.GetInt(22), // [!code ++]
			autoFloor = SourceData.GetString(23), // [!code ++]
			concrete = SourceData.GetBool(24), // [!code ++]
			transparent = SourceData.GetBool(25), // [!code ++]
			transition = SourceData.GetIntArray(26), // [!code ++]
			tag = SourceData.GetStringArray(27), // [!code ++]
			soundFoot = SourceData.GetString(28), // [!code ++]
			detail_JP = SourceData.GetString(29), // [!code ++]
			detail = SourceData.GetString(30) // [!code ++]
		};
	}

```

## SourceFloor

[`public class Row : TileRow`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/SourceFloor.cs#L10-L15)
```cs:line-numbers=10

		public string[] reqHarvest;

		public int[] anime; // [!code ++]
 // [!code ++]
		public string defBlock;

		public string bridgeBlock;
```

[`public override Row CreateRow()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/SourceFloor.cs#L90-L113)
```cs:line-numbers=90
			_tileType = SourceData.GetString(8),
			_idRenderData = SourceData.GetString(9),
			tiles = SourceData.GetIntArray(10),
			colorMod = SourceData.GetInt(11), // [!code --]
			value = SourceData.GetInt(12), // [!code --]
			LV = SourceData.GetInt(13), // [!code --]
			recipeKey = SourceData.GetStringArray(14), // [!code --]
			factory = SourceData.GetStringArray(15), // [!code --]
			components = SourceData.GetStringArray(16), // [!code --]
			defMat = SourceData.GetString(17), // [!code --]
			defBlock = SourceData.GetString(18), // [!code --]
			bridgeBlock = SourceData.GetString(19), // [!code --]
			category = SourceData.GetString(20), // [!code --]
			edge = SourceData.GetInt(21), // [!code --]
			autotile = SourceData.GetInt(22), // [!code --]
			autotilePriority = SourceData.GetInt(23), // [!code --]
			autotileBrightness = SourceData.GetFloat(24), // [!code --]
			soundFoot = SourceData.GetString(25), // [!code --]
			tag = SourceData.GetStringArray(26), // [!code --]
			detail_JP = SourceData.GetString(27), // [!code --]
			detail = SourceData.GetString(28) // [!code --]
			anime = SourceData.GetIntArray(11), // [!code ++]
			colorMod = SourceData.GetInt(12), // [!code ++]
			value = SourceData.GetInt(13), // [!code ++]
			LV = SourceData.GetInt(14), // [!code ++]
			recipeKey = SourceData.GetStringArray(15), // [!code ++]
			factory = SourceData.GetStringArray(16), // [!code ++]
			components = SourceData.GetStringArray(17), // [!code ++]
			defMat = SourceData.GetString(18), // [!code ++]
			defBlock = SourceData.GetString(19), // [!code ++]
			bridgeBlock = SourceData.GetString(20), // [!code ++]
			category = SourceData.GetString(21), // [!code ++]
			edge = SourceData.GetInt(22), // [!code ++]
			autotile = SourceData.GetInt(23), // [!code ++]
			autotilePriority = SourceData.GetInt(24), // [!code ++]
			autotileBrightness = SourceData.GetFloat(25), // [!code ++]
			soundFoot = SourceData.GetString(26), // [!code ++]
			tag = SourceData.GetStringArray(27), // [!code ++]
			detail_JP = SourceData.GetString(28), // [!code ++]
			detail = SourceData.GetString(29) // [!code ++]
		};
	}

```

## SourceObj

[`public class Row : TileRow`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/SourceObj.cs#L18-L23)
```cs:line-numbers=18

		public string valType;

		public int[] anime; // [!code ++]
 // [!code ++]
		public string matCategory;

		public int idRoof;
```

[`public override Row CreateRow()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/SourceObj.cs#L98-L118)
```cs:line-numbers=98
			valType = SourceData.GetString(13),
			_idRenderData = SourceData.GetString(14),
			tiles = SourceData.GetIntArray(15),
			snowTile = SourceData.GetInt(16), // [!code --]
			colorMod = SourceData.GetInt(17), // [!code --]
			colorType = SourceData.GetString(18), // [!code --]
			value = SourceData.GetInt(19), // [!code --]
			LV = SourceData.GetInt(20), // [!code --]
			chance = SourceData.GetInt(21), // [!code --]
			recipeKey = SourceData.GetStringArray(22), // [!code --]
			factory = SourceData.GetStringArray(23), // [!code --]
			components = SourceData.GetStringArray(24), // [!code --]
			defMat = SourceData.GetString(25), // [!code --]
			matCategory = SourceData.GetString(26), // [!code --]
			category = SourceData.GetString(27), // [!code --]
			idRoof = SourceData.GetInt(28), // [!code --]
			detail_JP = SourceData.GetString(29), // [!code --]
			detail = SourceData.GetString(30) // [!code --]
			anime = SourceData.GetIntArray(16), // [!code ++]
			snowTile = SourceData.GetInt(17), // [!code ++]
			colorMod = SourceData.GetInt(18), // [!code ++]
			colorType = SourceData.GetString(19), // [!code ++]
			value = SourceData.GetInt(20), // [!code ++]
			LV = SourceData.GetInt(21), // [!code ++]
			chance = SourceData.GetInt(22), // [!code ++]
			recipeKey = SourceData.GetStringArray(23), // [!code ++]
			factory = SourceData.GetStringArray(24), // [!code ++]
			components = SourceData.GetStringArray(25), // [!code ++]
			defMat = SourceData.GetString(26), // [!code ++]
			matCategory = SourceData.GetString(27), // [!code ++]
			category = SourceData.GetString(28), // [!code ++]
			idRoof = SourceData.GetInt(29), // [!code ++]
			detail_JP = SourceData.GetString(30), // [!code ++]
			detail = SourceData.GetString(31) // [!code ++]
		};
	}

```

## SourceThingV

[`public override void OnAfterImportData()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/SourceThingV.cs#L246-L263)
```cs:line-numbers=246
						break;
					case "skin":
					{
						string[] array2 = array[1].Split('|'); // [!code --]
						row.skins = new int[array2.Length]; // [!code --]
						for (int j = 0; j < array2.Length; j++) // [!code --]
						string[] array3 = array[1].Split('|'); // [!code ++]
						row.skins = new int[array3.Length]; // [!code ++]
						for (int k = 0; k < array3.Length; k++) // [!code ++]
						{
							row.skins[j] = array2[j].ToInt(); // [!code --]
							row.skins[k] = array3[k].ToInt(); // [!code ++]
						}
						break;
					}
					case "alt":
						row.altTiles = new int[1] { array[1].ToInt() }; // [!code --]
					{ // [!code ++]
						string[] array2 = array[1].Split('|'); // [!code ++]
						row.altTiles = new int[array2.Length]; // [!code ++]
						for (int j = 0; j < array2.Length; j++) // [!code ++]
						{ // [!code ++]
							row.altTiles[j] = array2[j].ToInt(); // [!code ++]
						} // [!code ++]
						row.ignoreAltFix = true;
						break;
					} // [!code ++]
					case "naming":
						row.naming = array[1];
						break;
```

## Thing

[`public override void WriteNote(UINote n, Action<UINote> onWriteNote = null, IIns`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/Thing.cs#L1291-L1301)
```cs:line-numbers=1291
			}
			if (!e.IsGlobalElement)
			{
				if (e.source.tag.Contains("weaponEnc") && !base.IsEquipmentOrRangedOrAmmo && !base.IsThrownWeapon && !(trait is TraitToolMusic)) // [!code --]
				bool flag3 = base.IsWeapon || base.IsRangedWeapon || base.IsAmmo || base.IsThrownWeapon || trait is TraitToolMusic; // [!code ++]
				if (base.IsEquipment && base.category.slot != 35 && source.elementMap.ContainsKey(e.id)) // [!code ++]
				{ // [!code ++]
					flag3 = true; // [!code ++]
				} // [!code ++]
				if (e.source.tag.Contains("weaponEnc") && !flag3) // [!code ++]
				{
					return false;
				}
				if (e.source.IsWeaponEnc && !base.category.IsChildOf("shield") && !base.IsEquipmentOrRangedOrAmmo && !base.IsThrownWeapon && !(trait is TraitToolMusic)) // [!code --]
				if (e.source.IsWeaponEnc && !(base.category.IsChildOf("shield") || flag3)) // [!code ++]
				{
					return false;
				}
```

## Trait

[`public virtual void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/Trait.cs#L1923-L1930)
```cs:line-numbers=1923
				Add("sword_zephir", 1, 0).SetReplica(on: true);
				Add("ribbon", 1, 0);
				Add("helm_sage", 1, 0);
				Add("wear_swim_danger", 1, 0); // [!code --]
				Add("wear_swim_danger", 1, 1); // [!code --]
				NoRestockId("wear_swim_danger", 1, 0); // [!code ++]
				NoRestockId("wear_swim_danger", 1, 1); // [!code ++]
				Add("diary_sister", 1, 0);
				Add("diary_catsister", 1, 0);
				Add("diary_lady", 1, 0);
```

## WidgetEquip

[`public override void OnActivate()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/d8bffe333a595553ca4ac4e25af79951cdde8fbd/Elin/WidgetEquip.cs#L50-L55)
```cs:line-numbers=50
		Rebuild();
	}

	public static void OnChangeBodyPart() // [!code ++]
	{ // [!code ++]
		if ((bool)Instance) // [!code ++]
		{ // [!code ++]
			Instance.Rebuild(); // [!code ++]
		} // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void Rebuild()
	{
		RefreshEquipment(listMain);
```
