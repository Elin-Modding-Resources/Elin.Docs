---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 1 file modified.
version: +EA 23.330 Nightly - Plugin.BaseCore
changes: ClassExtension
---

# +EA 23.330 Nightly - Plugin.BaseCore

July 26, 2026

1 file modified.

## Important Changes

**None.**
## ClassExtension

[`public static string Pluralize(string s)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/95333b6db5ec3d1831c6306b6ae58ced8b6c7fe1/Elin/Plugins.basecore/ClassExtension.cs#L1008-L1013)
```cs:line-numbers=1008
		return pluralizer.Pluralize(s);
	}

	public static string GetSpecialArticle(string a) // [!code ++]
	{ // [!code ++]
		if (a.ToLower() == "unicorn") // [!code ++]
		{ // [!code ++]
			return "a "; // [!code ++]
		} // [!code ++]
		return null; // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static string AddArticle(this string s)
	{
		if (!Lang.setting.addArticle || s.Length < 1)
```

[`public static string AddArticle(this string s)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/95333b6db5ec3d1831c6306b6ae58ced8b6c7fe1/Elin/Plugins.basecore/ClassExtension.cs#L1015-L1021)
```cs:line-numbers=1015
			return s;
		}
		char c = s.ToLower()[0];
		s = ((c == 'a' || c == 'i' || c == 'u' || c == 'e' || c == 'o') ? "an " : "a ") + s; // [!code --]
		s = (GetSpecialArticle(s) ?? ((c == 'a' || c == 'i' || c == 'u' || c == 'e' || c == 'o') ? "an " : "a ")) + s; // [!code ++]
		return s;
	}

```

[`public static string AddArticle(this string s, int num, ArticleStyle style = Art`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/95333b6db5ec3d1831c6306b6ae58ced8b6c7fe1/Elin/Plugins.basecore/ClassExtension.cs#L1026-L1032)
```cs:line-numbers=1026
		return s;
	}
	char c = s[0];
	string text = ((num >= 2) ? (num.ToFormat() + " ") : ((c == 'a' || c == 'i' || c == 'u' || c == 'e' || c == 'o') ? "an " : "a ")); // [!code --]
	string text = ((num >= 2) ? (num.ToFormat() + " ") : (GetSpecialArticle(s) ?? ((c == 'a' || c == 'i' || c == 'u' || c == 'e' || c == 'o') ? "an " : "a "))); // [!code ++]
	if (num >= 2 && Lang.setting.pluralize)
	{
		s = ((replace.IsEmpty() || !s.Contains(replace) || s.Contains("limestone stone")) ? Pluralize(s) : s.Replace(replace, Pluralize(replace)));
```
