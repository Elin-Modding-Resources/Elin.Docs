---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 10 files modified. 1 new file created.
version: EA 23.243 Nightly Patch 3
changes: Card/Chara/Core/EClass/FACTION/GameIO/GameSerializationBinder/MATERIAL/Region/SourceMaterial/+UnknownTypePlaceholder
---

# EA 23.243 Nightly Patch 3

December 5, 2025

10 files modified. 1 new file created.

## Important Changes

**None.**
## Card

[`public virtual void SetRenderParam(RenderParam p)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a773f66061367a47c5d6b0338351541ebb9a17e5/Elin/Card.cs#L5972-L5978)
```cs:line-numbers=5972

	public void DyeRandom()
	{
		Dye(EClass.sources.materials.rows.Where((SourceMaterial.Row r) => r.matColor.r != r.matColor.g || r.matColor.g != r.matColor.b || r.matColor.b != r.matColor.r).RandomItem().alias); // [!code --]
		Dye(EClass.sources.materials.rows.Where((SourceMaterial.Row r) => r.tier <= 4 && (r.matColor.r != r.matColor.g || r.matColor.g != r.matColor.b || r.matColor.b != r.matColor.r)).RandomItem().alias); // [!code ++]
	}

	public void Dye(string idMat)
```

## Chara

[`public override MoveResult _Move(Point newPoint, MoveType type = MoveType.Walk)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a773f66061367a47c5d6b0338351541ebb9a17e5/Elin/Chara.cs#L2777-L2783)
```cs:line-numbers=2777
				num2 += 20;
				num *= 1.8f;
			}
			num2 = num2 * 100 / (100 + (int)Mathf.Sqrt(EvalueMax(240, -20) * 10) + EvalueMax(407, -5) * 5); // [!code --]
			num2 = num2 * 100 / (100 + EClass.sqrt(EvalueMax(240, -20) * 10) + EvalueMax(407, -5) * 5); // [!code ++]
			EClass.world.date.AdvanceMin(num2 * 6);
			EClass.player.lastZonePos = null;
			EClass.player.distanceTravel++;
```

[`public bool TryEquipRanged()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a773f66061367a47c5d6b0338351541ebb9a17e5/Elin/Chara.cs#L7784-L7790)
```cs:line-numbers=7784

	public override int GetArmorSkill()
	{
		if (body.GetWeight(armorOnly: true) <= 30000) // [!code --]
		if (body.GetWeight(armorOnly: true) <= (HasElement(1204) ? 6000 : 30000)) // [!code ++]
		{
			return 120;
		}
```

## Core

[`public void Init()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a773f66061367a47c5d6b0338351541ebb9a17e5/Elin/Core.cs#L533-L538)
```cs:line-numbers=533
	StartCoroutine("OnEndOfFrame");
	InvokeRepeating("Update100ms", 0.1f, 0.1f);
	UnityEngine.Object.DestroyImmediate(ui.layoutLang.gameObject);
	GameIO.Init(); // [!code ++]
	CoreConfig.Init();
	if (config.lang.IsEmpty())
	{
```

## EClass

[`public static int curve(int _a, int start, int step, int rate = 75)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a773f66061367a47c5d6b0338351541ebb9a17e5/Elin/EClass.cs#L85-L90)
```cs:line-numbers=85
		return (int)Mathf.Clamp(num, -2.1474836E+09f, 2.1474836E+09f);
	}

	public static int sqrt(int a) // [!code ++]
	{ // [!code ++]
		if (a >= 0) // [!code ++]
		{ // [!code ++]
			return (int)Mathf.Sqrt(a); // [!code ++]
		} // [!code ++]
		return (int)(0f - Mathf.Sqrt(-a)); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static int rndHalf(int a)
	{
		return a / 2 + Rand.rnd(a / 2);
```

## FACTION

[`public List<FactionBranch> GetChildren()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a773f66061367a47c5d6b0338351541ebb9a17e5/Elin/FACTION.cs#L204-L210)
```cs:line-numbers=204
	List<FactionBranch> list = new List<FactionBranch>();
	foreach (Spatial value in EClass.game.spatials.map.Values)
	{
		if (value.mainFaction == this) // [!code --]
		if (value?.mainFaction == this) // [!code ++]
		{
			list.Add((value as Zone).branch);
		}
```

## GameIO

[`using System.IO.Compression;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a773f66061367a47c5d6b0338351541ebb9a17e5/Elin/GameIO.cs#L4-L9)
```cs:line-numbers=4
using System.IO.Compression;
using System.Linq;
using Newtonsoft.Json;
using Newtonsoft.Json.Serialization; // [!code ++]
using UnityEngine;

public class GameIO : EClass
```

[`public class GameIO : EClass`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a773f66061367a47c5d6b0338351541ebb9a17e5/Elin/GameIO.cs#L38-L43)
```cs:line-numbers=38

	public static bool compressSave => EClass.core.config.test.compressSave;

	public static void Init() // [!code ++]
	{ // [!code ++]
		JsonSerializerSettings jsonSerializerSettings = jsReadGame; // [!code ++]
		jsonSerializerSettings.Error = (EventHandler<Newtonsoft.Json.Serialization.ErrorEventArgs>)Delegate.Combine(jsonSerializerSettings.Error, (EventHandler<Newtonsoft.Json.Serialization.ErrorEventArgs>)delegate(object sender, Newtonsoft.Json.Serialization.ErrorEventArgs args) // [!code ++]
		{ // [!code ++]
			if (args.ErrorContext.Error.Message.Contains("UnknownTypePlaceholder")) // [!code ++]
			{ // [!code ++]
				args.ErrorContext.Handled = true; // [!code ++]
			} // [!code ++]
		}); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static void ResetTemp()
	{
		DirectoryInfo directoryInfo = new DirectoryInfo(pathTemp);
```

## GameSerializationBinder

[`public override Type BindToType(string assemblyName, string typeName)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a773f66061367a47c5d6b0338351541ebb9a17e5/Elin/GameSerializationBinder.cs#L14-L19)
```cs:line-numbers=14
	}
	catch (Exception ex)
	{
		Core.Instance.ui.Say("Possible Mod Error, please contact the mod auther."); // [!code ++]
		Core.Instance.ui.Say(ex.Message); // [!code ++]
		Debug.LogError(ex.Message);
		Debug.LogError(assemblyName + "/" + typeName);
		if (ModUtil.fallbackTypes.ContainsKey(typeName))
```

[`public override Type BindToType(string assemblyName, string typeName)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a773f66061367a47c5d6b0338351541ebb9a17e5/Elin/GameSerializationBinder.cs#L21-L31)
```cs:line-numbers=21
				Debug.Log(typeName + "/" + Type.GetType(ModUtil.fallbackTypes[typeName]));
				return Type.GetType(ModUtil.fallbackTypes[typeName]);
			}
			if (typeName.Contains("Quest")) // [!code --]
			if (typeName.StartsWith("Quest")) // [!code ++]
			{
				return typeof(QuestDummy);
			}
			return typeof(object); // [!code --]
			if (typeName.StartsWith("Zone")) // [!code ++]
			{ // [!code ++]
				return typeof(Zone); // [!code ++]
			} // [!code ++]
			return typeof(UnknownTypePlaceholder); // [!code ++]
		}
	}
}
```

## MATERIAL

[`public class MATERIAL : EClass`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a773f66061367a47c5d6b0338351541ebb9a17e5/Elin/MATERIAL.cs#L34-L39)
```cs:line-numbers=34

	public const string aliasNull = "oak";

	public const int MaxRandomTier = 4; // [!code ++]
 // [!code ++]
	public const int MaxTier = 7; // [!code ++]
 // [!code ++]
	public static SourceMaterial.Row sourceSnow = EClass.sources.materials.rows[48];

	public static SourceMaterial.Row sourceIce = EClass.sources.materials.rows[61];
```

[`public static SourceMaterial.Row GetRandomMaterial(int lv, string group = null,`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a773f66061367a47c5d6b0338351541ebb9a17e5/Elin/MATERIAL.cs#L70-L82)
```cs:line-numbers=70
	int num = 0;
	if (tryLevelMatTier)
	{
		num = Mathf.Clamp(lv / 15, 0, tierList.tiers.Length - 1); // [!code --]
		num = Mathf.Clamp(num + EClass.rnd(2) - EClass.rnd(2), 0, tierList.tiers.Length - 1); // [!code --]
		num = Mathf.Clamp(lv / 15, 0, 4); // [!code ++]
		num = Mathf.Clamp(num + EClass.rnd(2) - EClass.rnd(2), 0, 4); // [!code ++]
	}
	else
	{
		int min = ((lv >= 60) ? 2 : ((lv >= 25) ? 1 : 0)); // [!code --]
		num = Mathf.Clamp(EClass.rnd(EClass.rnd(EClass.rnd(lv / 10 + 2) + 1) + 1), min, tierList.tiers.Length - 1); // [!code --]
		num = Mathf.Clamp(min: (lv >= 60) ? 2 : ((lv >= 25) ? 1 : 0), value: EClass.rnd(EClass.rnd(EClass.rnd(lv / 10 + 2) + 1) + 1), max: 4); // [!code ++]
	}
	SourceMaterial.Tier obj = tierList.tiers[num];
	SourceMaterial.Row result = obj.Select();
```

## Region

[`public List<Zone> ListTowns()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a773f66061367a47c5d6b0338351541ebb9a17e5/Elin/Region.cs#L272-L278)
```cs:line-numbers=272
	List<Zone> list = new List<Zone>();
	foreach (Spatial value in EClass.game.spatials.map.Values)
	{
		if (value.CanSpawnAdv) // [!code --]
		if (value != null && value.CanSpawnAdv) // [!code ++]
		{
			list.Add(value as Zone);
		}
```

## SourceMaterial

[`public class TierList`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/a773f66061367a47c5d6b0338351541ebb9a17e5/Elin/SourceMaterial.cs#L271-L278)
```cs:line-numbers=271

		public TierList()
		{
			tiers = new Tier[5]; // [!code --]
			for (int i = 0; i < 5; i++) // [!code --]
			tiers = new Tier[8]; // [!code ++]
			for (int i = 0; i < 8; i++) // [!code ++]
			{
				tiers[i] = new Tier();
			}
```

## +UnknownTypePlaceholder

::: details File Created
```cs
public class UnknownTypePlaceholder
{
}
```

:::