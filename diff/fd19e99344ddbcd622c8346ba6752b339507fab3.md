---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 8 files modified.
version: EA 23.295 Stable
changes: AI_Fuck/FactionBranch/GameDate/ItemStatistics/LayerShippingResult/ModManager/ShippingResult/Steam
---

# EA 23.295 Stable

April 11, 2026

8 files modified.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [ShippingResult (1)](#shippingresult)
```cs:no-line-numbers
public int GetIncome() // [!code --]
public long GetIncome() // [!code ++]
```
## AI_Fuck

[`void SuccubusExp(Chara c, Chara tg)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/fd19e99344ddbcd622c8346ba6752b339507fab3/Elin/AI_Fuck.cs#L571-L577)
```cs:line-numbers=571
		{
			if (c.elements.ValueWithoutLink(item.id) < item.ValueWithoutLink)
			{
				c.elements.ModTempPotential(item.id, Mathf.Min(EClass.rnd(item.ValueWithoutLink - c.elements.ValueWithoutLink(item.id) / 5), (variation == Variation.Succubus) ? 300 : 100)); // [!code --]
				c.elements.ModTempPotential(item.id, Mathf.Min(EClass.rnd(item.ValueWithoutLink - c.elements.ValueWithoutLink(item.id) / 5), (variation == Variation.Succubus) ? 400 : 100)); // [!code ++]
				c.Say("succubus_exp", c, item.Name.ToLower());
				break;
			}
```

## FactionBranch

[`public class FactionBranch : EClass`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/fd19e99344ddbcd622c8346ba6752b339507fab3/Elin/FactionBranch.cs#L8-L16)
```cs:line-numbers=8
{
	public class Statistics : EClass
	{
		[JsonProperty] // [!code --]
		public int ship; // [!code --]
 // [!code --]
		[JsonProperty]
		public int inn;

```

[`public class Statistics : EClass`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/fd19e99344ddbcd622c8346ba6752b339507fab3/Elin/FactionBranch.cs#L28-L33)
```cs:line-numbers=28

		[JsonProperty]
		public int tax;
 // [!code ++]
		[JsonProperty] // [!code ++]
		public long ship; // [!code ++]
	}

	[JsonProperty]
```

## GameDate

[`public void ShipGoods()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/fd19e99344ddbcd622c8346ba6752b339507fab3/Elin/GameDate.cs#L286-L293)
```cs:line-numbers=286
	}
	int num = 0;
	int num2 = 0;
	int num3 = 0; // [!code --]
	int num4 = 0; // [!code --]
	long num3 = 0L; // [!code ++]
	long num4 = 0L; // [!code ++]
	List<Thing> list = new List<Thing>();
	List<string> list2 = new List<string>();
	Zone zone = EClass.game.spatials.Find(EClass.player.uidLastShippedZone);
```

[`public void ShipGoods()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/fd19e99344ddbcd622c8346ba6752b339507fab3/Elin/GameDate.cs#L323-L328)
```cs:line-numbers=323
			});
		}
	}
	if (num3 > int.MaxValue) // [!code ++]
	{ // [!code ++]
		num3 = 2147483647L; // [!code ++]
	} // [!code ++]
	if (list.Count == 0)
	{
		return;
```

[`public void ShipGoods()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/fd19e99344ddbcd622c8346ba6752b339507fab3/Elin/GameDate.cs#L346-L351)
```cs:line-numbers=346
	{
		num4 = shippingBonus2 - shippingBonus;
	}
	if (num4 > int.MaxValue) // [!code ++]
	{ // [!code ++]
		num4 = 2147483647L; // [!code ++]
	} // [!code ++]
	foreach (Thing item2 in list)
	{
		item2.Destroy();
```

[`public void ShipGoods()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/fd19e99344ddbcd622c8346ba6752b339507fab3/Elin/GameDate.cs#L353-L365)
```cs:line-numbers=353
	Thing thing = null;
	Thing thing2 = null;
	string text = "";
	if (num3 != 0) // [!code --]
	if (num3 > 0) // [!code ++]
	{
		thing = ThingGen.Create("money").SetNum(num3); // [!code --]
		thing = ThingGen.Create("money").SetNum((int)num3); // [!code ++]
	}
	if (num4 != 0) // [!code --]
	if (num4 > 0) // [!code ++]
	{
		thing2 = ThingGen.Create("money2").SetNum(num4); // [!code --]
		thing2 = ThingGen.Create("money2").SetNum((int)num4); // [!code ++]
	}
	if (thing != null && thing2 != null)
	{
```

## ItemStatistics

[`public void Refresh(FactionBranch.Statistics stat)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/fd19e99344ddbcd622c8346ba6752b339507fab3/Elin/ItemStatistics.cs#L21-L27)
```cs:line-numbers=21
	textShip.text = F(stat.ship, lastStatistics.ship);
	textShop.text = F(stat.shop, lastStatistics.shop);
	textTax.text = F(stat.tax, lastStatistics.tax);
	static string F(int a, int b) // [!code --]
	static string F(long a, long b) // [!code ++]
	{
		return a.ToFormat() + " (" + b.ToFormat() + ")";
	}
```

## LayerShippingResult

[`public void Refresh()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/fd19e99344ddbcd622c8346ba6752b339507fab3/Elin/LayerShippingResult.cs#L109-L115)
```cs:line-numbers=109
	}
	FactionBranch branch = (ELayer.game.spatials.Find(result.uidZone) ?? ELayer.pc.homeZone).branch;
	float num = (float)update / (float)maxUpdate;
	int income = result.GetIncome(); // [!code --]
	long income = result.GetIncome(); // [!code ++]
	income = (int)((float)income * num);
	long num2 = result.total + income;
	textIncome.text = Lang._currency(income, showUnit: true);
```

## ModManager

[`public ModPackage AddWorkshopPackage(WorkshopItem item, bool isInPackages = fals`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/fd19e99344ddbcd622c8346ba6752b339507fab3/Elin/ModManager.cs#L355-L362)
```cs:line-numbers=355
	string folderPath;
	DateTime timeStamp;
	bool itemInstallInfo = UserGeneratedContent.Client.GetItemInstallInfo(item.FileId, out sizeOnDisk, out folderPath, out timeStamp);
	DirectoryInfo dir = new DirectoryInfo(folderPath); // [!code --]
	ModPackage modPackage = AddPackage(dir, isInPackages); // [!code --]
	DirectoryInfo directoryInfo = new DirectoryInfo(folderPath); // [!code ++]
	if (!directoryInfo.Exists) // [!code ++]
	{ // [!code ++]
		return null; // [!code ++]
	} // [!code ++]
	ModPackage modPackage = AddPackage(directoryInfo, isInPackages); // [!code ++]
	modPackage.installed = itemInstallInfo;
	modPackage.banned = item.IsBanned;
	return modPackage;
```

## ShippingResult

[`public int debt`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/fd19e99344ddbcd622c8346ba6752b339507fab3/Elin/ShippingResult.cs#L123-L131)
```cs:line-numbers=123
		}
	}

	public int GetIncome() // [!code --]
	public long GetIncome() // [!code ++]
	{
		int num = 0; // [!code --]
		long num = 0L; // [!code ++]
		foreach (Item item in items)
		{
			num += item.income;
```

## Steam

[`public static void ResetAllAchievement()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/fd19e99344ddbcd622c8346ba6752b339507fab3/Elin/Steam.cs#L98-L104)
```cs:line-numbers=98

	public static void ResetAchievement(ID_Achievement id)
	{
		AchievementObject achievementObject = Instance.steamworks.settings.achievements.First((AchievementObject a) => a.Id == id.ToString()); // [!code --]
		AchievementObject achievementObject = Instance.steamworks.settings.achievements.FirstOrDefault((AchievementObject a) => a.Id == id.ToString()); // [!code ++]
		if (achievementObject != null)
		{
			achievementObject.ClearAchievement();
```
