---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 6 files modified.
version: EA 23.343.3 Nightly
changes: CurrencyType/DNA/Trait/TraitGodStatue/TraitPowerStatue/UIInventory
---

# EA 23.343.3 Nightly

September 7, 2026

6 files modified.

## Important Changes

**None.**
## CurrencyType

[`public enum CurrencyType`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/CurrencyType.cs#L8-L12)
```cs:line-numbers=8
	Money2,
	BranchMoney,
	Influence,
	Casino_coin // [!code --]
	Casino_coin, // [!code ++]
	BlueCapsule // [!code ++]
}
```

## DNA

[`using System;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/DNA.cs#L1-L5)
```cs:line-numbers=1
using System;
using System.Collections.Generic;
using System.Linq; // [!code ++]
using System.Runtime.Serialization;
using Newtonsoft.Json;
using UnityEngine;
```

[`public enum Type`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/DNA.cs#L11-L15)
```cs:line-numbers=11
	Inferior = 0,
	Default = 3,
	Superior = 5,
	Superimani = 6, // [!code ++]
	Brain = 8
}
```

[`public int slot`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/DNA.cs#L85-L102)
```cs:line-numbers=85
	}
}

public bool isManiGene // [!code --]
{ // [!code --]
	get // [!code --]
	{ // [!code --]
		return bits[0]; // [!code --]
	} // [!code --]
	set // [!code --]
	{ // [!code --]
		bits[0] = value; // [!code --]
	} // [!code --]
} // [!code --]
 // [!code --]
[OnSerializing]
private void _OnSerializing(StreamingContext context)
{
```

[`public static void GenerateManiGene(Card owner)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/DNA.cs#L168-L177)
```cs:line-numbers=168
Rand.SetSeed(owner.c_seed);
CardRow r = SpawnList.Get("chara").Select(100);
Rand.SetSeed(owner.c_seed);
Thing thing = GenerateGene(r, Type.Superior, owner.LV, owner.c_seed); // [!code --]
thing.c_DNA.cost = thing.c_DNA.cost / 2; // [!code --]
thing.c_DNA.isManiGene = true; // [!code --]
thing.ChangeMaterial(36); // [!code --]
Thing thing = GenerateGene(r, Type.Superimani, owner.LV, owner.c_seed); // [!code ++]
thing.MakeRefFrom("mani");
Rand.SetSeed();
owner.c_seed++;
```

[`public void Generate(Type _type, Chara model = null)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/DNA.cs#L358-L367)
```cs:line-numbers=358
	}
	AddSpecial();
	break;
case Type.Superimani: // [!code ++]
	AddRandom(EClass.rnd(EClass.rnd(4)) + 3); // [!code ++]
	if (EClass.rnd(3) == 0) // [!code ++]
	{ // [!code ++]
		AddSpecial(); // [!code ++]
	} // [!code ++]
	AddSpecial(); // [!code ++]
	if (EClass.rnd(3) != 0) // [!code ++]
	{ // [!code ++]
		IEnumerable<SourceElement.Row> enumerable = EClass.sources.elements.map.Values.Where((SourceElement.Row row) => row.tag.Contains("gene_mani") && lv + ((!EClass.debug.enable) ? 1 : 100) >= row.LV); // [!code ++]
		if (enumerable.Count() > 0) // [!code ++]
		{ // [!code ++]
			SourceElement.Row e = enumerable.RandomItem(); // [!code ++]
			AddVal(e.id, (!(e.category == "ability")) ? 1 : 100, allowStack: false, (int v) => e.cost[0], allowNegative: false); // [!code ++]
		} // [!code ++]
	} // [!code ++]
	cost = cost / 2 - 10; // [!code ++]
	break; // [!code ++]
}
if (vals.Count == 0)
{
	for (int i = 0; i < 10; i++) // [!code --]
	for (int num = 0; num < 10; num++) // [!code ++]
	{
		if (EClass.rnd(4) == 0)
		{
```

[`void AddBody()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/DNA.cs#L402-L408)
```cs:line-numbers=402
if (body == 0)
{
	BodySlot bodySlot = null;
	for (int j = 0; j < 100; j++) // [!code --]
	for (int i = 0; i < 100; i++) // [!code ++]
	{
		BodySlot bodySlot2 = model.body.slots.RandomItem();
		if (bodySlot2 != null && bodySlot2.elementId != 40 && bodySlot2.elementId != 44 && bodySlot2.elementId != 46)
```

[`void AddFeat()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/DNA.cs#L423-L442)
```cs:line-numbers=423
	if (listFeat.Count != 0)
	{
		feat++;
		Element e = listFeat.RandomItem(); // [!code --]
		if (maxSlot <= 1 || e.source.geneSlot <= 1) // [!code --]
		Element e2 = listFeat.RandomItem(); // [!code ++]
		if (maxSlot <= 1 || e2.source.geneSlot <= 1) // [!code ++]
		{
			if (e.source.geneSlot > maxSlot) // [!code --]
			if (e2.source.geneSlot > maxSlot) // [!code ++]
			{
				maxSlot = e.source.geneSlot; // [!code --]
				maxSlot = e2.source.geneSlot; // [!code ++]
			}
			AddVal(e.id, 1, allowStack: false, (int v) => e.source.cost[0] * 5, allowNegative: false); // [!code --]
			AddVal(e2.id, 1, allowStack: false, (int v) => e2.source.cost[0] * 5, allowNegative: false); // [!code ++]
		}
	}
}
void AddRandom(int n)
{
	for (int j = 0; j < n; j++) // [!code --]
	for (int i = 0; i < n; i++) // [!code ++]
	{
		if (EClass.debug.enable && EClass.rnd(2) == 0)
		{
```

[`void AddVal(int id, int v, bool allowStack, Func<int, int> funcCost, bool allowN…)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/DNA.cs#L485-L506)
```cs:line-numbers=485
if (v >= 0 || allowNegative)
{
	bool flag = false;
	int num = EClass.curve(v, 20, 10, 90); // [!code --]
	int num2 = EClass.curve(v, 20, 10, 90); // [!code ++]
	if (v < -100)
	{
		num = EClass.curve(Mathf.Abs(v + 100), 20, 10, 90); // [!code --]
		num2 = EClass.curve(Mathf.Abs(v + 100), 20, 10, 90); // [!code ++]
	}
	v = EClass.curve(v, 20, 10, 80);
	for (int j = 0; j < vals.Count; j += 2) // [!code --]
	for (int i = 0; i < vals.Count; i += 2) // [!code ++]
	{
		if (vals[j] == id) // [!code --]
		if (vals[i] == id) // [!code ++]
		{
			if (allowStack)
			{
				v /= 2;
				num /= 2; // [!code --]
				vals[j + 1] += v; // [!code --]
				Debug.Log(vals[j + 1] + ": " + v + "/" + num); // [!code --]
				num2 /= 2; // [!code ++]
				vals[i + 1] += v; // [!code ++]
				Debug.Log(vals[i + 1] + ": " + v + "/" + num2); // [!code ++]
				flag = true;
				break;
			}
```

[`void AddVal(int id, int v, bool allowStack, Func<int, int> funcCost, bool allowN…)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/DNA.cs#L514-L520)
```cs:line-numbers=514
				vals.Add(id);
				vals.Add(v);
			}
			cost += Mathf.Max(0, funcCost(num)); // [!code --]
			cost += Mathf.Max(0, funcCost(num2)); // [!code ++]
		}
	}
}
```

[`public void CalcSlot()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/DNA.cs#L534-L546)
```cs:line-numbers=534
for (int i = 0; i < vals.Count; i += 2)
{
	Element element = Element.Create(vals[i], vals[i + 1]);
	if (element.source.geneSlot < 0) // [!code --]
	if (type != Type.Superimani || !element.HasTag("gene_mani")) // [!code ++]
	{
		slot = 99; // [!code --]
	} // [!code --]
	if (element.source.geneSlot > slot) // [!code --]
	{ // [!code --]
		slot = element.source.geneSlot; // [!code --]
		if (element.source.geneSlot < 0) // [!code ++]
		{ // [!code ++]
			slot = 99; // [!code ++]
		} // [!code ++]
		if (element.source.geneSlot > slot) // [!code ++]
		{ // [!code ++]
			slot = element.source.geneSlot; // [!code ++]
		} // [!code ++]
	}
}
if (slot < 0)
```

[`public static Type GetType(string idMat)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/DNA.cs#L556-L561)
```cs:line-numbers=556
		"jelly" => Type.Default, 
		"gold" => Type.Superior, 
		"amethyst" => Type.Brain, 
		"lapis" => Type.Superimani,  // [!code ++]
		_ => Type.Inferior, 
	};
}
```

[`public string GetMaterialId(Type type)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/DNA.cs#L566-L571)
```cs:line-numbers=566
{
	Type.Default => "jelly", 
	Type.Superior => "gold", 
	Type.Superimani => "lapis",  // [!code ++]
	Type.Brain => "amethyst", 
	_ => "process", 
};
```

## Trait

[`public virtual int CostRerollShop`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/Trait.cs#L568-L573)
```cs:line-numbers=568
	}
}

public virtual CurrencyType CurrencyRerollShop => CurrencyType.Influence; // [!code ++]
 // [!code ++]
public virtual bool AllowCriminal => owner.isThing;

public virtual int RestockDay => 5;
```

[`public virtual void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/Trait.cs#L2338-L2346)
```cs:line-numbers=2338
	break;
}
ShopType shopType = ShopType;
if ((uint)(shopType - 24) <= 1u && EClass._zone is Zone_Yowyn) // [!code --]
if ((uint)(shopType - 24) <= 1u) // [!code ++]
{
	Add("milk_kumiromi", EClass.rndHalf(6), 0); // [!code --]
	if (EClass._zone is Zone_Yowyn) // [!code ++]
	{ // [!code ++]
		Add("milk_kumiromi", EClass.rndHalf(6), 0); // [!code ++]
	} // [!code ++]
	if (EClass._zone is Zone_Nefu) // [!code ++]
	{ // [!code ++]
		Add("milk_horome", EClass.rndHalf(6), 0); // [!code ++]
	} // [!code ++]
}
switch (owner.id)
{
```

## TraitGodStatue

[`public override void OnCreate(int lv)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/TraitGodStatue.cs#L13-L18)
```cs:line-numbers=13
	OnChangeMaterial();
}

public override void OnBarter(bool reroll = false) // [!code ++]
{ // [!code ++]
	if (Religion.id == "machine") // [!code ++]
	{ // [!code ++]
		DNA.GenerateManiGene(owner); // [!code ++]
	} // [!code ++]
} // [!code ++]
 // [!code ++]
public void OnChangeMaterial()
{
	owner.isOn = owner.material.alias == "gold";
```

## TraitPowerStatue

[`public override bool CanBeDestroyed`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/TraitPowerStatue.cs#L25-L29)
```cs:line-numbers=25
public override string IDInvStyle => "jewelbox";

public override int CostRerollShop => 0; // [!code --]
public override int CostRerollShop => 1; // [!code ++]
 // [!code ++]
public override CurrencyType CurrencyRerollShop => CurrencyType.BlueCapsule; // [!code ++]

public override bool CanStack => false;
```

[`public override bool OnUse(Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/TraitPowerStatue.cs#L60-L66)
```cs:line-numbers=60
	{
		DNA.GenerateManiGene(owner);
	}
	LayerInventory.CreateContainer<InvOwnerChoose>(owner, owner); // [!code --]
	LayerInventory.CreateContainer<InvOwnerChoose>(owner, owner).TryShowHint("h_invManiGene"); // [!code ++]
	SE.Play("pop_principal"); // [!code ++]
	return false;
}
Msg.Say("shrine_power", owner);
```

## UIInventory

[`public void SwitchTab(Tab tab)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/UIInventory.cs#L335-L341)
```cs:line-numbers=335
{
	tab.owner.BuildUICurrency(layer.uiCurrency, tab.owner.owner.trait.CostRerollShop != 0 || EMono.debug.enable);
}
layer.TryShowHint("h_inv" + tab.mode); // [!code --]
if ((bool)headerRow)
{
	UIHeader[] componentsInChildren = headerRow.GetComponentsInChildren<UIHeader>(includeInactive: true);
```

[`void RefreshDist2()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/c9331eee289b44d5cf50ab5e2b488c5cb270471b/Elin/UIInventory.cs#L909-L932)
```cs:line-numbers=909
	{
		break;
	}
	menuBottom.AddButton("rerollShop".lang(cost.ToString() ?? ""), delegate // [!code --]
	menuBottom.AddButton("rerollShop".lang(cost.ToString() ?? "", (_owner.trait.CurrencyRerollShop == CurrencyType.BlueCapsule) ? EMono.sources.things.map["drug_blue"].GetName() : "influence".lang()), delegate // [!code ++]
	{
		if (EMono._zone.influence < cost) // [!code --]
		if (_owner.trait.CurrencyRerollShop == CurrencyType.BlueCapsule) // [!code ++]
		{
			SE.Beep(); // [!code --]
			Msg.Say("notEnoughInfluence"); // [!code --]
			if (EMono.pc.GetCurrency("drug_blue") < cost) // [!code ++]
			{ // [!code ++]
				SE.Beep(); // [!code ++]
				Msg.Say("notEnoughSomething", EMono.sources.things.map["drug_blue"].GetName()); // [!code ++]
				return; // [!code ++]
			} // [!code ++]
			EMono.pc.ModCurrency(-1, "drug_blue"); // [!code ++]
		}
		else
		{
			SE.Dice(); // [!code --]
			if (EMono._zone.influence < cost) // [!code ++]
			{ // [!code ++]
				SE.Beep(); // [!code ++]
				Msg.Say("notEnoughInfluence"); // [!code ++]
				return; // [!code ++]
			} // [!code ++]
			EMono._zone.influence -= cost;
			_owner.c_dateStockExpire = 0; // [!code --]
			_owner.trait.OnBarter(reroll: true); // [!code --]
			RefreshGrid(); // [!code --]
			Sort(); // [!code --]
			SE.Play("shop_open"); // [!code --]
		}
	}); // [!code --]
		SE.Dice(); // [!code ++]
		_owner.c_dateStockExpire = 0; // [!code ++]
		_owner.trait.OnBarter(reroll: true); // [!code ++]
		RefreshGrid(); // [!code ++]
		Sort(); // [!code ++]
		SE.Play("shop_open"); // [!code ++]
		TooltipManager.Instance.HideTooltips(immediate: true); // [!code ++]
	}, null, "DefaultContentFit").RebuildLayout(); // [!code ++]
	break;
}
}
```
