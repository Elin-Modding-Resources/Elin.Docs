---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 17 files modified. 2 new files created.
version: EA 23.286 Nightly
changes: AI_Idle/AI_OpenGambleChest/ActEffect/Biography/Card/CardBlueprint/Chara/DramaManager/EffectId/FactionBranch/ID_Achievement/+InvOwnerChangeRarity/LayerDragGrid/Trait/+TraitGarokkHammer/TraitKeeperOfGarden/TraitScrollStatic/Zone/Zone_EternalGarden
---

# EA 23.286 Nightly

March 16, 2026

17 files modified. 2 new files created.

## Important Changes

Possible breaking changes. Click the filename to view the chunk.
### [Zone_EternalGarden (1)](#zone-eternalgarden)
```cs:no-line-numbers
public override void OnActivate() // [!code --]

```
## AI_Idle

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/AI_Idle.cs#L876-L882)
```cs:line-numbers=876
	}
	if (EClass.rnd(owner.isSynced ? 10 : 2000) == 0 && owner.ability.Has(5058))
	{
		if (!owner.UseAbility(5058)) // [!code --]
		if (!owner.UseAbility(5058) && !owner.IsPCFaction) // [!code ++]
		{
			owner.AddCondition<ConInsane>(10000);
			owner.SetHostility(Hostility.Enemy);
```

## AI_OpenGambleChest

[`public override IEnumerable<Status> Run()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/AI_OpenGambleChest.cs#L56-L62)
```cs:line-numbers=56
			{
				owner.PlaySound("money");
				owner.PlayAnime(AnimeID.Jump);
				Thing thing = ThingGen.Create("money").SetNum(EClass.rndHalf(50 * (100 + Mathf.Min(target.c_lockLv, 10000000) * 10))); // [!code --]
				Thing thing = ThingGen.Create("money").SetNum(EClass.rndHalf(50 * (100 + Mathf.Min(target.c_lockLv * 10, 10000000)))); // [!code ++]
				owner.Pick(thing, msg: false);
				owner.Say("gambleChest_win", thing);
			}
```

## ActEffect

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/ActEffect.cs#L1098-L1110)
```cs:line-numbers=1098
	{
		CC.PlaySound("clean_floor");
		Msg.Say("exterminate");
		List<Chara> list3 = EClass._map.charas.Where((Chara c) => c.isCopy && !c.IsPCFaction).ToList(); // [!code --]
		if (list3.Count == 0) // [!code --]
		List<Chara> list2 = EClass._map.charas.Where((Chara c) => c.isCopy && !c.IsPCFaction).ToList(); // [!code ++]
		if (list2.Count == 0) // [!code ++]
		{
			Msg.SayNothingHappen();
			return;
		}
		foreach (Chara item in list3) // [!code --]
		foreach (Chara item in list2) // [!code ++]
		{
			item.Say("split_fail", item);
			item.PlayEffect("vanish");
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/ActEffect.cs#L1118-L1173)
```cs:line-numbers=1118
		{
			return;
		}
		Thing thing2 = ThingGen.Create("mine"); // [!code --]
		thing2.c_idRefCard = "dog_mine"; // [!code --]
		Thing thing = ThingGen.Create("mine"); // [!code ++]
		thing.c_idRefCard = "dog_mine"; // [!code ++]
		Zone.ignoreSpawnAnime = true;
		EClass._zone.AddCard(thing2, CC.pos).Install(); // [!code --]
		EClass._zone.AddCard(thing, CC.pos).Install(); // [!code ++]
		break;
	}
	case EffectId.LittleSisterMigration:
	case EffectId.SilverCatMigration:
	{
		bool flag3 = id == EffectId.SilverCatMigration; // [!code --]
		if (!EClass.game.IsSurvival && ((flag3 && !(EClass._zone is Zone_EternalGarden)) || (!flag3 && !(EClass._zone is Zone_LittleGarden)))) // [!code --]
		bool flag6 = id == EffectId.SilverCatMigration; // [!code ++]
		if (!EClass.game.IsSurvival && ((flag6 && EClass._zone.id != "startVillage2") || (!flag6 && !(EClass._zone is Zone_LittleGarden)))) // [!code ++]
		{
			Msg.SayNothingHappen();
			return;
		}
		List<Chara> list2 = new List<Chara>(); // [!code --]
		bool flag4 = false; // [!code --]
		List<Chara> list3 = new List<Chara>(); // [!code ++]
		bool flag7 = false; // [!code ++]
		foreach (Chara chara2 in EClass._map.charas)
		{
			if (!chara2.IsPCFactionOrMinion && chara2.id == (flag3 ? "cat_silver" : "littleOne")) // [!code --]
			if (!chara2.IsPCFactionOrMinion && chara2.id == (flag6 ? "cat_silver" : "littleOne")) // [!code ++]
			{
				if (flag4) // [!code --]
				if (flag7) // [!code ++]
				{
					flag4 = false; // [!code --]
					flag7 = false; // [!code ++]
					continue;
				}
				list2.Add(chara2); // [!code --]
				flag4 = true; // [!code --]
				list3.Add(chara2); // [!code ++]
				flag7 = true; // [!code ++]
			}
		}
		if (list2.Count == 0) // [!code --]
		if (list3.Count == 0) // [!code ++]
		{
			Msg.SayNothingHappen();
			return;
		}
		EClass.pc.PlaySound("chime_angel");
		foreach (Chara item2 in list2) // [!code --]
		foreach (Chara item2 in list3) // [!code ++]
		{
			item2.PlayEffect("revive");
			item2.Destroy();
		}
		Msg.Say(flag3 ? "cat_migration" : "little_migration", list2.Count.ToString() ?? ""); // [!code --]
		EClass._zone.ModInfluence(list2.Count); // [!code --]
		if (flag3) // [!code --]
		Msg.Say(flag6 ? "cat_migration" : "little_migration", list3.Count.ToString() ?? ""); // [!code ++]
		EClass._zone.ModInfluence(list3.Count); // [!code ++]
		if (flag6) // [!code ++]
		{
			EClass.player.stats.catDepart += list2.Count; // [!code --]
			EClass.player.stats.catDepart += list3.Count; // [!code ++]
		}
		else
		{
			EClass.player.stats.sistersDepart += list2.Count; // [!code --]
			EClass.player.stats.sistersDepart += list3.Count; // [!code ++]
		}
		break;
	}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/ActEffect.cs#L1224-L1232)
```cs:line-numbers=1224
		TC.PlaySound("curse3");
		TC.PlayEffect("curse");
		TC.Say("forgetItems", TC);
		int num4 = power / 50 + 1 + EClass.rnd(3); // [!code --]
		int num3 = power / 50 + 1 + EClass.rnd(3); // [!code ++]
		List<Thing> source = TC.things.List((Thing t) => t.c_IDTState == 0);
		for (int j = 0; j < num4; j++) // [!code --]
		for (int j = 0; j < num3; j++) // [!code ++]
		{
			source.RandomItem().c_IDTState = 5;
		}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/ActEffect.cs#L1238-L1247)
```cs:line-numbers=1238
	case EffectId.EnchantArmorGreat:
	{
		bool armor = id == EffectId.EnchantArmor || id == EffectId.EnchantArmorGreat;
		bool flag7 = id == EffectId.EnchantWeaponGreat || id == EffectId.EnchantArmorGreat; // [!code --]
		bool flag4 = id == EffectId.EnchantWeaponGreat || id == EffectId.EnchantArmorGreat; // [!code ++]
		if (!tc.isThing)
		{
			LayerDragGrid.CreateEnchant(CC, armor, flag7, state); // [!code --]
			LayerDragGrid.CreateEnchant(CC, armor, flag4, state); // [!code ++]
			return;
		}
		cc.PlaySound("identify");
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/ActEffect.cs#L1252-L1259)
```cs:line-numbers=1252
			tc.ModEncLv(-1);
			break;
		}
		int num6 = (flag7 ? 4 : 2) + (blessed ? 1 : 0); // [!code --]
		if (tc.encLV >= num6) // [!code --]
		int num5 = (flag4 ? 4 : 2) + (blessed ? 1 : 0); // [!code ++]
		if (tc.encLV >= num5) // [!code ++]
		{
			cc.Say("enc_resist", tc);
			break;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/ActEffect.cs#L1265-L1285)
```cs:line-numbers=1265
	case EffectId.Identify:
	case EffectId.GreaterIdentify:
	{
		bool flag6 = id == EffectId.GreaterIdentify; // [!code --]
		bool flag2 = id == EffectId.GreaterIdentify; // [!code ++]
		if (flag)
		{
			Redirect(EffectId.ForgetItems, flag6 ? BlessedState.Cursed : BlessedState.Normal, default(ActRef)); // [!code --]
			Redirect(EffectId.ForgetItems, flag2 ? BlessedState.Cursed : BlessedState.Normal, default(ActRef)); // [!code ++]
			break;
		}
		if (!tc.isThing)
		{
			int count = ((!blessed) ? 1 : (flag6 ? (2 + EClass.rnd(2)) : (3 + EClass.rnd(3)))); // [!code --]
			LayerDragGrid.CreateIdentify(CC, flag6, state, 0, count); // [!code --]
			int count = ((!blessed) ? 1 : (flag2 ? (2 + EClass.rnd(2)) : (3 + EClass.rnd(3)))); // [!code ++]
			LayerDragGrid.CreateIdentify(CC, flag2, state, 0, count); // [!code ++]
			return;
		}
		cc.PlaySound("identify");
		cc.PlayEffect("identify");
		tc.Thing.Identify(cc.IsPCParty, (!flag6) ? IDTSource.Identify : IDTSource.SuperiorIdentify); // [!code --]
		tc.Thing.Identify(cc.IsPCParty, (!flag2) ? IDTSource.Identify : IDTSource.SuperiorIdentify); // [!code ++]
		break;
	}
	case EffectId.Uncurse:
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/ActEffect.cs#L1289-L1305)
```cs:line-numbers=1289
			LayerDragGrid.CreateUncurse(CC, state);
			return;
		}
		Thing thing = tc.Thing; // [!code --]
		if (thing.blessedState == BlessedState.Cursed) // [!code --]
		Thing thing3 = tc.Thing; // [!code ++]
		if (thing3.blessedState == BlessedState.Cursed) // [!code ++]
		{
			thing.SetBlessedState(BlessedState.Normal); // [!code --]
			thing3.SetBlessedState(BlessedState.Normal); // [!code ++]
		}
		else if (thing.blessedState == BlessedState.Doomed) // [!code --]
		else if (thing3.blessedState == BlessedState.Doomed) // [!code ++]
		{
			thing.SetBlessedState(BlessedState.Normal); // [!code --]
			thing3.SetBlessedState(BlessedState.Normal); // [!code ++]
		}
		thing.GetRootCard()?.TryStack(thing); // [!code --]
		LayerInventory.SetDirty(thing); // [!code --]
		thing3.GetRootCard()?.TryStack(thing3); // [!code ++]
		LayerInventory.SetDirty(thing3); // [!code ++]
		break;
	}
	case EffectId.Lighten:
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/ActEffect.cs#L1315-L1321)
```cs:line-numbers=1315
		}
		cc.PlaySound("offering");
		cc.PlayEffect("buff");
		int num3 = (tc.isWeightChanged ? tc.c_weight : tc.Thing.source.weight); // [!code --]
		int num6 = (tc.isWeightChanged ? tc.c_weight : tc.Thing.source.weight); // [!code ++]
		tc.isWeightChanged = true;
		Element orCreateElement = tc.elements.GetOrCreateElement(64);
		Element orCreateElement2 = tc.elements.GetOrCreateElement(65);
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/ActEffect.cs#L1324-L1333)
```cs:line-numbers=1324
		bool flag5 = tc.IsEquipmentOrRangedOrAmmo || tc.IsThrownWeapon;
		if (flag)
		{
			num3 = (int)(0.01f * (float)num3 * (float)power * 0.75f + 500f); // [!code --]
			if (num3 < 0 || num3 > 10000000) // [!code --]
			num6 = (int)(0.01f * (float)num6 * (float)power * 0.75f + 500f); // [!code ++]
			if (num6 < 0 || num6 > 10000000) // [!code ++]
			{
				num3 = 10000000; // [!code --]
				num6 = 10000000; // [!code ++]
				flag5 = false;
			}
			if (flag5)
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/ActEffect.cs#L1347-L1353)
```cs:line-numbers=1347
		}
		else
		{
			num3 = num3 * (100 - power / 10) / 100; // [!code --]
			num6 = num6 * (100 - power / 10) / 100; // [!code ++]
			if (blessed)
			{
				power /= 4;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/ActEffect.cs#L1367-L1373)
```cs:line-numbers=1367
			}
			cc.Say("lighten", tc);
		}
		tc.c_weight = num3; // [!code --]
		tc.c_weight = num6; // [!code ++]
		tc.SetDirtyWeight();
		if (tc.parent == null)
		{
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/ActEffect.cs#L1391-L1423)
```cs:line-numbers=1391
		cc.PlayEffect("identify");
		cc.Say("reconstruct", tc);
		EClass.game.cards.uidNext += EClass.rnd(30);
		int num5 = Mathf.Max(tc.genLv, tc.LV, EClass.player.stats.deepest); // [!code --]
		int num4 = Mathf.Max(tc.genLv, tc.LV, EClass.player.stats.deepest); // [!code ++]
		CardBlueprint.Set(new CardBlueprint
		{
			blesstedState = state
		});
		Thing thing3 = ThingGen.Create(tc.id, -1, (int)((long)num5 * (long)power / 400)); // [!code --]
		thing3.genLv = num5; // [!code --]
		Thing thing2 = ThingGen.Create(tc.id, -1, (int)((long)num4 * (long)power / 400)); // [!code ++]
		thing2.genLv = num4; // [!code ++]
		if (tc.c_uidAttune != 0)
		{
			thing3.c_uidAttune = tc.c_uidAttune; // [!code --]
			if (thing3.id == "amulet_engagement" || thing3.id == "ring_engagement") // [!code --]
			thing2.c_uidAttune = tc.c_uidAttune; // [!code ++]
			if (thing2.id == "amulet_engagement" || thing2.id == "ring_engagement") // [!code ++]
			{
				if (tc.c_uidAttune != EClass.pc.uid)
				{
					thing3.elements.ModBase(484, 3); // [!code --]
					thing2.elements.ModBase(484, 3); // [!code ++]
				}
				if (thing3.rarity < Rarity.Mythical) // [!code --]
				if (thing2.rarity < Rarity.Mythical) // [!code ++]
				{
					thing3.rarity = Rarity.Mythical; // [!code --]
					thing2.rarity = Rarity.Mythical; // [!code ++]
				}
			}
		}
		tc.Destroy();
		CC.Pick(thing3, msg: false); // [!code --]
		CC.Pick(thing2, msg: false); // [!code ++]
		if (!CC.IsPC)
		{
			CC.TryEquip(thing3); // [!code --]
			CC.TryEquip(thing2); // [!code ++]
		}
		break;
	}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/ActEffect.cs#L1439-L1445)
```cs:line-numbers=1439
		if (row == null)
		{
			bool num = id == EffectId.ChangeMaterialGreater;
			bool flag2 = id == EffectId.ChangeMaterialLesser; // [!code --]
			bool flag3 = id == EffectId.ChangeMaterialLesser; // [!code ++]
			string text2 = tc.Thing.source.tierGroup;
			Dictionary<string, SourceMaterial.TierList> tierMap = SourceMaterial.tierMap;
			int num2 = 1;
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/ActEffect.cs#L1455-L1461)
```cs:line-numbers=1455
			{
				num2++;
			}
			if (flag2) // [!code --]
			if (flag3) // [!code ++]
			{
				num2 -= 2;
			}
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/ActEffect.cs#L1503-L1508)
```cs:line-numbers=1503
		}
		break;
	}
	case EffectId.ChangeRarity: // [!code ++]
	{ // [!code ++]
		EClass.sources.materials.alias.TryGetValue(actRef.n1); // [!code ++]
		if (!tc.isThing) // [!code ++]
		{ // [!code ++]
			LayerDragGrid.CreateChangeRarity(CC, actRef.refThing, id, state); // [!code ++]
			return; // [!code ++]
		} // [!code ++]
		if (tc.Num > 1) // [!code ++]
		{ // [!code ++]
			tc = tc.Split(1); // [!code ++]
		} // [!code ++]
		string name2 = tc.Name; // [!code ++]
		cc.PlaySound("offering"); // [!code ++]
		cc.PlayEffect("buff"); // [!code ++]
		CardBlueprint.Set(new CardBlueprint // [!code ++]
		{ // [!code ++]
			rarity = ((tc.rarity < Rarity.Legendary) ? Rarity.Legendary : Rarity.Mythical), // [!code ++]
			generation = CardBlueprint.Generation.GarokkHammer // [!code ++]
		}); // [!code ++]
		tc.Destroy(); // [!code ++]
		Rand.SetBaseSeed(tc.uid); // [!code ++]
		Rand.SetSeed(tc.uid); // [!code ++]
		tc = ThingGen.Create(tc.id, tc.idMaterial, tc.genLv); // [!code ++]
		Rand.SetSeed(); // [!code ++]
		Rand.SetBaseSeed(); // [!code ++]
		EClass._zone.AddCard(tc, cc.pos); // [!code ++]
		EClass.pc.Say("reconstruct", name2); // [!code ++]
		EClass.pc.Pick(tc.Thing, msg: false); // [!code ++]
		break; // [!code ++]
	} // [!code ++]
	case EffectId.ChangeAlias: // [!code ++]
		EClass.ui.AddLayer<LayerList>().SetStringList(delegate // [!code ++]
		{ // [!code ++]
			List<string> list11 = new List<string>(); // [!code ++]
			for (int num15 = 0; num15 < 10; num15++) // [!code ++]
			{ // [!code ++]
				list11.Add(AliasGen.GetRandomAlias()); // [!code ++]
			} // [!code ++]
			return list11; // [!code ++]
		}, delegate(int a, string b) // [!code ++]
		{ // [!code ++]
			EClass.pc._alias = b; // [!code ++]
			EClass.pc.Say("reconstruct", EClass.pc); // [!code ++]
			SE.Change(); // [!code ++]
			EClass.pc.PlayEffect("mutation"); // [!code ++]
		}).SetSize() // [!code ++]
			.EnableReroll() // [!code ++]
			.SetTitles("wAlias"); // [!code ++]
		break; // [!code ++]
	case EffectId.ChangeBiography: // [!code ++]
		EClass.pc.bio.RerollParents(); // [!code ++]
		EClass.pc.Say("reconstruct", EClass.pc); // [!code ++]
		SE.Change(); // [!code ++]
		EClass.pc.PlayEffect("mutation"); // [!code ++]
		break; // [!code ++]
	case EffectId.ReturnVoid:
	{
		Zone_Void root = EClass.game.spatials.Find<Zone_Void>();
```

[`void AddCon<T>(int rate, int power) where T : Condition`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/ActEffect.cs#L1769-L1775)
```cs:line-numbers=1769
			int currency = TC.GetCurrency();
			if (currency > 0)
			{
				currency = Mathf.Clamp(EClass.rnd(currency / 10), 1, 100 + EClass.rndHalf(CC.LV * 200)); // [!code --]
				currency = Mathf.Clamp(EClass.rnd(currency / 10), 1, 100 + EClass.rndHalf(Mathf.Min(CC.LV, 1000000) * 200)); // [!code ++]
				thing6 = ThingGen.Create("money").SetNum(currency);
				TC.ModCurrency(-currency);
			}
```

## Biography

[`public void RerollBio(Chara c, int ageIndex = 0, bool keepParent = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Biography.cs#L395-L407)
```cs:line-numbers=395
		GenerateAppearance(c);
		if (!keepParent)
		{
			GenerateDad(); // [!code --]
			GenerateMom(); // [!code --]
			idHome = RndBio("home"); // [!code --]
			idLoc = RndBio("loc"); // [!code --]
			RerollParents(); // [!code ++]
		}
	}

	public void RerollParents() // [!code ++]
	{ // [!code ++]
		GenerateDad(); // [!code ++]
		GenerateMom(); // [!code ++]
		idHome = RndBio("home"); // [!code ++]
		idLoc = RndBio("loc"); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public void GenerateBirthday(Chara c, int ageIndex = 0)
	{
		SourceRace.Row race = c.race;
```

## Card

[`public virtual int GetPrice(CurrencyType currency = CurrencyType.Money, bool sel`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Card.cs#L7601-L7606)
```cs:line-numbers=7601
				return LV / 8;
			case "372":
				return 3;
			case "hammer_garokk": // [!code ++]
				return 100; // [!code ++]
			case "scroll_random":
				if (refVal == 8288)
				{
```

## CardBlueprint

[`public class CardBlueprint : EClass`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/CardBlueprint.cs#L1-L5)
```cs:line-numbers=1
public class CardBlueprint : EClass
{
	public enum Generation // [!code ++]
	{ // [!code ++]
		Default, // [!code ++]
		GarokkHammer // [!code ++]
	} // [!code ++]
 // [!code ++]
	public int lv = -999;

	public int qualityBonus;
```

[`public class CardBlueprint : EClass`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/CardBlueprint.cs#L22-L27)
```cs:line-numbers=22

	public bool isCraft;

	public Generation generation; // [!code ++]
 // [!code ++]
	public static CardBlueprint current;

	public static CardBlueprint _Default = new CardBlueprint();
```

## Chara

[`public void SetDirtySpeed()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Chara.cs#L1904-L1909)
```cs:line-numbers=1904

	public void RefreshSpeed(Element.BonusInfo info = null)
	{
		if (currentZone != EClass._zone) // [!code ++]
		{ // [!code ++]
			dirtySpeed = true; // [!code ++]
			return; // [!code ++]
		} // [!code ++]
		if (ride != null && !ride.IsDeadOrSleeping)
		{
			ride.RefreshSpeed();
```

[`public void RestockEquip(bool onCreate)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Chara.cs#L4967-L4973)
```cs:line-numbers=4967
	case "mad_rich":
		if (onCreate)
		{
			int a = EClass.rndHalf(100 + (EClass.debug.enable ? 10 : EClass._zone.influence) * 50 + (int)Mathf.Sqrt(EClass._zone.DangerLv) * 100 + (int)Mathf.Sqrt(EClass.pc.FameLv) * 100 + EClass._zone.development * 10); // [!code --]
			int a = (EClass._zone.IsUserZone ? 200 : EClass.rndHalf(100 + (EClass.debug.enable ? 10 : EClass._zone.influence) * 50 + (int)Mathf.Sqrt(EClass._zone.DangerLv) * 100 + (int)Mathf.Sqrt(EClass.pc.FameLv) * 100 + EClass._zone.development * 10)); // [!code ++]
			ModCurrency(a);
		}
		break;
```

[`public void Kiss(Chara c)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Chara.cs#L6416-L6421)
```cs:line-numbers=6416

	public void Slap(Chara c, bool slapToDeath = false)
	{
		if (id == "olderyoungersister") // [!code ++]
		{ // [!code ++]
			Steam.GetAchievement(ID_Achievement.OYS); // [!code ++]
		} // [!code ++]
		PlaySound("whip");
		Say("slap", this, c);
		c.PlayAnime(AnimeID.Shiver);
```

## DramaManager

[`public void ParseLine(Dictionary<string, string> item)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/DramaManager.cs#L816-L822)
```cs:line-numbers=816
	case "getAchievement":
		AddEvent(delegate
		{
			if (new string[1] { "ONEV" }.Contains(p2)) // [!code --]
			if (new string[2] { "ONEV", "NERUN2" }.Contains(p2)) // [!code ++]
			{
				Steam.GetAchievement(p2.ToEnum<ID_Achievement>());
			}
```

## EffectId

[`public enum EffectId`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/EffectId.cs#L135-L139)
```cs:line-numbers=135
	GravityGun = 311,
	TransBlood = 312,
	Field = 313,
	SilverCatMigration = 314 // [!code --]
	SilverCatMigration = 314, // [!code ++]
	ChangeRarity = 315, // [!code ++]
	ChangeAlias = 316, // [!code ++]
	ChangeBiography = 317 // [!code ++]
}
```

## FactionBranch

[`public int GetResidentTax()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/FactionBranch.cs#L1073-L1079)
```cs:line-numbers=1073
				num5 = num6;
			}
		}
		int num7 = (int)((isWealthy ? 50 : 10) + (long)member.LV * 2L) * num5 / 100 * num4 / 100; // [!code --]
		int num7 = (int)(((isWealthy ? 50 : 10) + (long)member.LV * 2L) * num5 / 100 * num4 / 100); // [!code ++]
		if (isWealthy && num2 > 0)
		{
			num7 = num7 * (150 + (int)Mathf.Sqrt(num2) * 5) / 100;
```

## ID_Achievement

[`public enum ID_Achievement`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/ID_Achievement.cs#L23-L27)
```cs:line-numbers=23
	ONEV,
	JURE,
	BELL,
	NERUN2 // [!code --]
	NERUN2, // [!code ++]
	OYS // [!code ++]
}
```

## +InvOwnerChangeRarity

::: details File Created
```cs
public class InvOwnerChangeRarity : InvOwnerEffect
{
	public Thing consume;

	public override bool CanTargetAlly => true;

	public override string langTransfer => "invChangeRarity";

	public override string langWhat => "changeMaterial_what";

	public override Thing CreateDefaultContainer()
	{
		return ThingGen.Create("hammer_garokk");
	}

	public override bool ShouldShowGuide(Thing t)
	{
		if (t.IsEquipment && !t.IsUnique && !t.HasTag(CTAG.godArtifact))
		{
			return !t.IsLightsource;
		}
		return false;
	}

	public override void _OnProcess(Thing t)
	{
		ActEffect.Proc(idEffect, 100, state, t.GetRootCard(), t);
		if (consume != null)
		{
			consume.ModNum(-1);
		}
	}
}
```

:::
## LayerDragGrid

[`public static LayerDragGrid CreateChangeMaterial(Chara cc, Thing consume, Source`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/LayerDragGrid.cs#L509-L514)
```cs:line-numbers=509
		});
	}

	public static LayerDragGrid CreateChangeRarity(Chara cc, Thing consume, EffectId idEffect, BlessedState state = BlessedState.Normal, int price = 0, int count = 1) // [!code ++]
	{ // [!code ++]
		return TryProc(cc, new InvOwnerChangeRarity // [!code ++]
		{ // [!code ++]
			consume = consume, // [!code ++]
			state = state, // [!code ++]
			price = price, // [!code ++]
			count = count, // [!code ++]
			idEffect = idEffect // [!code ++]
		}); // [!code ++]
	} // [!code ++]
 // [!code ++]
	public static LayerDragGrid CreateUncurse(Chara cc, BlessedState state = BlessedState.Normal, int price = 0, int count = 1)
	{
		return TryProc(cc, new InvOwnerUncurse
```

## Trait

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L1563-L1569)
```cs:line-numbers=1563
		{
			break;
		}
		int num4 = 0; // [!code --]
		int num7 = 0; // [!code ++]
		foreach (Thing thing10 in c_copyContainer.things)
		{
			if (!owner.trait.CanCopy(thing10))
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L1578-L1595)
```cs:line-numbers=1578
			{
				thing5.elements.Remove(item.id);
			}
			int num5 = 1; // [!code --]
			int num8 = 1; // [!code ++]
			switch (owner.trait.CopyShop)
			{
			case CopyShopType.Item:
			{
				num5 = (1000 + owner.c_invest * 100) / (thing5.GetPrice(CurrencyType.Money, sell: false, PriceType.CopyShop) + 50); // [!code --]
				int[] array = new int[3] { 704, 703, 702 }; // [!code --]
				foreach (int ele in array) // [!code --]
				num8 = (1000 + owner.c_invest * 100) / (thing5.GetPrice(CurrencyType.Money, sell: false, PriceType.CopyShop) + 50); // [!code ++]
				int[] array2 = new int[3] { 704, 703, 702 }; // [!code ++]
				foreach (int ele in array2) // [!code ++]
				{
					if (thing5.HasElement(ele))
					{
						num5 = 1; // [!code --]
						num8 = 1; // [!code ++]
					}
				}
				break;
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L1598-L1610)
```cs:line-numbers=1598
				thing5.c_charges = thing10.c_charges;
				break;
			}
			if (num5 > 1 && thing5.trait.CanStack) // [!code --]
			if (num8 > 1 && thing5.trait.CanStack) // [!code ++]
			{
				thing5.SetNum(num5); // [!code --]
				thing5.SetNum(num8); // [!code ++]
			}
			AddThing(thing5);
			num4++; // [!code --]
			if (num4 > owner.trait.NumCopyItem) // [!code --]
			num7++; // [!code ++]
			if (num7 > owner.trait.NumCopyItem) // [!code ++]
			{
				break;
			}
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L1659-L1670)
```cs:line-numbers=1659
		break;
	case ShopType.RedBook:
	{
		for (int num7 = 0; num7 < 30; num7++) // [!code --]
		for (int k = 0; k < 30; k++) // [!code ++]
		{
			AddThing(ThingGen.CreateFromFilter("shop_seeker"));
		}
		break;
	}
	case ShopType.KeeperOfGarden: // [!code ++]
	{ // [!code ++]
		string[] array = new string[11] // [!code ++]
		{ // [!code ++]
			"stone_defense", "1325", "1326", "1327", "1328", "1330", "1331", "1332", "1333", "1283", // [!code ++]
			"1268" // [!code ++]
		}; // [!code ++]
		foreach (string id2 in array) // [!code ++]
		{ // [!code ++]
			AddThing(ThingGen.Create(id2, MATERIAL.GetRandomMaterialFromCategory(50, "rock", EClass.sources.materials.alias["granite"]).id).SetNum(10)); // [!code ++]
		} // [!code ++]
		Add("scroll_alias", 1, 0); // [!code ++]
		Add("scroll_biography", 1, 0); // [!code ++]
		Add("1329", 1, 0); // [!code ++]
		break; // [!code ++]
	} // [!code ++]
	case ShopType.Seed:
	{
		AddThing(TraitSeed.MakeSeed("rice")).SetNum(4 + EClass.rnd(4));
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L1672-L1682)
```cs:line-numbers=1672
		AddThing(TraitSeed.MakeSeed("carrot")).SetNum(4 + EClass.rnd(4));
		AddThing(TraitSeed.MakeSeed("potato")).SetNum(4 + EClass.rnd(4));
		AddThing(TraitSeed.MakeSeed("corn")).SetNum(4 + EClass.rnd(4));
		for (int num8 = 0; num8 < EClass.rnd(3) + 1; num8++) // [!code --]
		for (int num9 = 0; num9 < EClass.rnd(3) + 1; num9++) // [!code ++]
		{
			Add("462", 1, 0);
		}
		for (int num9 = 0; num9 < EClass.rnd(3) + 1; num9++) // [!code --]
		for (int num10 = 0; num10 < EClass.rnd(3) + 1; num10++) // [!code ++]
		{
			Add("1167", 1, 0);
		}
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L1757-L1765)
```cs:line-numbers=1757
				break;
			case ShopType.Influence:
			{
				bool num3 = owner.id == "big_sister"; // [!code --]
				TraitTicketFurniture.SetZone(num3 ? EClass.game.spatials.Find("little_garden") : EClass._zone, Add("ticket_furniture", 1, 0).SetNum(99)); // [!code --]
				if (num3) // [!code --]
				bool num4 = owner.id == "big_sister"; // [!code ++]
				TraitTicketFurniture.SetZone(num4 ? EClass.game.spatials.Find("little_garden") : EClass._zone, Add("ticket_furniture", 1, 0).SetNum(99)); // [!code ++]
				if (num4) // [!code ++]
				{
					Add("littleball", 10, 0);
					if (!owner.Chara.affinity.CanGiveCard())
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L1772-L1778)
```cs:line-numbers=1772
					}
					if (!reroll)
					{
						for (int l = 0; l < 20; l++) // [!code --]
						for (int n = 0; n < 20; n++) // [!code ++]
						{
							owner.Chara.ModExp(287, 1000);
						}
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L1782-L1788)
```cs:line-numbers=1782
					AddThing(thing3);
					break;
				}
				for (int m = 0; m < 10; m++) // [!code --]
				for (int num5 = 0; num5 < 10; num5++) // [!code ++]
				{
					Thing thing4 = ThingGen.Create(EClass._zone.IsFestival ? "1123" : ((EClass.rnd(3) == 0) ? "1169" : "1160"));
					thing4.DyeRandom();
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L1790-L1796)
```cs:line-numbers=1790
				}
				if (EClass._zone is Zone_Exile)
				{
					for (int n = 0; n < 30; n++) // [!code --]
					for (int num6 = 0; num6 < 30; num6++) // [!code ++]
					{
						Add("1235", 1, -1);
						Add("1236", 1, -1);
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L1831-L1837)
```cs:line-numbers=1831
				AddThing(ThingGen.CreatePerfume(9501, 5));
				AddThing(ThingGen.CreatePerfume(9502, 5));
				AddThing(ThingGen.CreatePerfume(9503, 5));
				for (int k = 0; k < 5; k++) // [!code --]
				for (int l = 0; l < 5; l++) // [!code ++]
				{
					Thing thing2 = ThingGen.CreateFromCategory("seasoning").SetNum(10);
					thing2.elements.SetBase(2, 40);
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L1841-L1861)
```cs:line-numbers=1841
				break;
			}
			case ShopType.Medal:
				NoRestockId("sword_dragon"); // [!code --]
				NoRestockId("hammer_garokk", 3); // [!code ++]
				NoRestockId("sword_dragon", 1); // [!code ++]
				Add("sword_dragon", 1, 0).SetReplica(on: true);
				NoRestockId("point_stick"); // [!code --]
				NoRestockId("point_stick", 1); // [!code ++]
				Add("point_stick", 1, 0).SetReplica(on: true);
				NoRestockId("blunt_bonehammer"); // [!code --]
				NoRestockId("blunt_bonehammer", 1); // [!code ++]
				Add("blunt_bonehammer", 1, 0).SetReplica(on: true);
				NoRestockId("pole_gunlance"); // [!code --]
				NoRestockId("pole_gunlance", 1); // [!code ++]
				Add("pole_gunlance", 1, 0).SetReplica(on: true);
				NoRestockId("sword_muramasa"); // [!code --]
				NoRestockId("sword_muramasa", 1); // [!code ++]
				Add("sword_muramasa", 1, 0).SetReplica(on: true);
				NoRestockId("sword_forgetmenot"); // [!code --]
				NoRestockId("sword_forgetmenot", 1); // [!code ++]
				Add("sword_forgetmenot", 1, 0).SetReplica(on: true);
				NoRestockId("dagger_fish"); // [!code --]
				NoRestockId("dagger_fish", 1); // [!code ++]
				Add("dagger_fish", 1, 0).SetReplica(on: true);
				NoRestockId("sword_zephir"); // [!code --]
				NoRestockId("sword_zephir", 1); // [!code ++]
				Add("sword_zephir", 1, 0).SetReplica(on: true);
				Add("ribbon", 1, 0);
				Add("helm_sage", 1, 0);
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L1887-L1906)
```cs:line-numbers=1887
				break;
			default:
			{
				float num = (float)(3 + Mathf.Min(ShopLv / 5, 10)) + Mathf.Sqrt(ShopLv); // [!code --]
				int num2 = 300; // [!code --]
				float num2 = (float)(3 + Mathf.Min(ShopLv / 5, 10)) + Mathf.Sqrt(ShopLv); // [!code ++]
				int num3 = 300; // [!code ++]
				switch (ShopType)
				{
				case ShopType.Ecopo:
					num2 = 30; // [!code --]
					num3 = 30; // [!code ++]
					break;
				case ShopType.StrangeGirl:
					num2 = 50; // [!code --]
					num3 = 50; // [!code ++]
					break;
				}
				num = num * (float)(100 + EClass.pc.Evalue(1406) * 5) / 100f; // [!code --]
				num = Mathf.Min(num, num2); // [!code --]
				for (int j = 0; (float)j < num; j++) // [!code --]
				num2 = num2 * (float)(100 + EClass.pc.Evalue(1406) * 5) / 100f; // [!code ++]
				num2 = Mathf.Min(num2, num3); // [!code ++]
				for (int j = 0; (float)j < num2; j++) // [!code ++]
				{
					Thing thing = CreateStock();
					if ((!thing.trait.IsNoShop || (ShopType == ShopType.LoytelMart && (EClass.debug.enable || EClass.player.flags.loytelMartLv >= 2))) && (!(thing.trait is TraitRod) || thing.c_charges != 0) && thing.GetPrice() > 0)
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L1922-L1931)
```cs:line-numbers=1922
				{
					continue;
				}
				string[] recipeKey = item3.row.recipeKey; // [!code --]
				for (int num6 = 0; num6 < recipeKey.Length; num6++) // [!code --]
				string[] array = item3.row.recipeKey; // [!code ++]
				for (int m = 0; m < array.Length; m++) // [!code ++]
				{
					if (recipeKey[num6] == ShopType.ToString()) // [!code --]
					if (array[m] == ShopType.ToString()) // [!code ++]
					{
						NoRestock(ThingGen.CreateRecipe(item3.id));
						break;
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L1948-L1967)
```cs:line-numbers=1948
				}
				break;
			}
			case ShopType.KeeperOfGarden: // [!code --]
				Add("1329", 1, 0); // [!code --]
				break; // [!code --]
			case ShopType.StrangeGirl:
			{
				int num12 = (EClass.debug.enable ? 20 : (EClass._zone.development / 10)); // [!code --]
				if (num12 > 0) // [!code --]
				int num14 = (EClass.debug.enable ? 20 : (EClass._zone.development / 10)); // [!code ++]
				if (num14 > 0) // [!code ++]
				{
					Add("syringe_gene", num12, 0); // [!code --]
					Add("syringe_gene", num14, 0); // [!code ++]
					Add("diary_little", 1, 0);
				}
				if (num12 > 10) // [!code --]
				if (num14 > 10) // [!code ++]
				{
					Add("syringe_heaven", num12 / 5, 0); // [!code --]
					Add("syringe_heaven", num14 / 5, 0); // [!code ++]
					Add("1276", 1, 0);
				}
				Add("medal", 10, 0);
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L2023-L2034)
```cs:line-numbers=2023
					Add("ticket_armpillow", 1, 0);
					Add("ticket_champagne", 1, 0);
				}
				for (int num10 = 0; num10 < 3; num10++) // [!code --]
				for (int num11 = 0; num11 < 3; num11++) // [!code ++]
				{
					if (EClass.rnd(5) == 0)
					{
						TreasureType treasureType = ((EClass.rnd(10) == 0) ? TreasureType.BossNefia : ((EClass.rnd(10) == 0) ? TreasureType.Map : TreasureType.RandomChest));
						int num11 = EClass.rnd(EClass.rnd(ShopLv + (EClass.debug.enable ? 200 : 50)) + 1) + 1; // [!code --]
						int num12 = EClass.rnd(EClass.rnd(ShopLv + (EClass.debug.enable ? 200 : 50)) + 1) + 1; // [!code ++]
						Thing thing6 = ThingGen.Create(treasureType switch
						{
							TreasureType.Map => "chest_treasure", 
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L2036-L2045)
```cs:line-numbers=2036
							_ => "chest3", 
						});
						thing6.c_lockedHard = true;
						thing6.c_lockLv = num11; // [!code --]
						thing6.c_priceAdd = 2000 + num11 * 250 * ((treasureType == TreasureType.RandomChest) ? 1 : 5); // [!code --]
						thing6.c_lockLv = num12; // [!code ++]
						thing6.c_priceAdd = 2000 + num12 * 250 * ((treasureType == TreasureType.RandomChest) ? 1 : 5); // [!code ++]
						thing6.c_revealLock = true;
						ThingGen.CreateTreasureContent(thing6, num11, treasureType, clearContent: true); // [!code --]
						ThingGen.CreateTreasureContent(thing6, num12, treasureType, clearContent: true); // [!code ++]
						AddThing(thing6);
					}
				}
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L2051-L2057)
```cs:line-numbers=2051
			case ShopType.General:
			case ShopType.Food:
			{
				for (int num14 = 0; num14 < (EClass.debug.enable ? 3 : 3); num14++) // [!code --]
				for (int num15 = 0; num15 < (EClass.debug.enable ? 3 : 3); num15++) // [!code ++]
				{
					if (EClass.rnd(3) == 0)
					{
```

[`public void OnBarter(bool reroll = false)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L2151-L2161)
```cs:line-numbers=2151
			{
				return;
			}
			int num15 = t.things.width * 10; // [!code --]
			if (t.things.Count > num15) // [!code --]
			int num16 = t.things.width * 10; // [!code ++]
			if (t.things.Count > num16) // [!code ++]
			{
				int num16 = t.things.Count - num15; // [!code --]
				for (int num17 = 0; num17 < num16; num17++) // [!code --]
				int num17 = t.things.Count - num16; // [!code ++]
				for (int num18 = 0; num18 < num17; num18++) // [!code ++]
				{
					t.things.LastItem().Destroy();
				}
```

[`void NoRestock(Thing _t)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L2200-L2208)
```cs:line-numbers=2200
						AddThing(_t);
					}
				}
				void NoRestockId(string _id) // [!code --]
				void NoRestockId(string _id, int num) // [!code ++]
				{
					NoRestock(ThingGen.Create(_id)); // [!code --]
					NoRestock(ThingGen.Create(_id).SetNum(num)); // [!code ++]
				}
			}

```

[`public Thing CreateStock()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Trait.cs#L2229-L2235)
```cs:line-numbers=2229
				}
				return FromFilter("shop_drink");
			case ShopType.Furniture:
			case ShopType.KeeperOfGarden: // [!code --]
				return FromFilter("shop_furniture");
			case ShopType.Book:
				return FromFilter("shop_book");
```

## +TraitGarokkHammer

::: details File Created
```cs
public class TraitGarokkHammer : TraitItem
{
	public override bool OnUse(Chara c)
	{
		ActEffect.Proc(EffectId.ChangeRarity, EClass.pc, null, 100, new ActRef
		{
			n1 = owner.material.alias,
			refThing = owner.Thing
		});
		return true;
	}
}
```

:::
## TraitKeeperOfGarden

[`public override ShopType ShopType`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/TraitKeeperOfGarden.cs#L8-L14)
```cs:line-numbers=8
{
	get
	{
		if (!(EClass._zone is Zone_EternalGarden) && !EClass.game.IsSurvival) // [!code --]
		if (!(EClass._zone.id == "startVillage2") && !EClass.game.IsSurvival) // [!code ++]
		{
			return ShopType.None;
		}
```

## TraitScrollStatic

[`public override SourceElement.Row source`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/TraitScrollStatic.cs#L16-L22)
```cs:line-numbers=16

	public virtual EffectId idEffect => owner.sourceCard.vals[1].ToEnum<EffectId>();

	public virtual int Power => int.Parse(owner.sourceCard.vals[2]); // [!code --]
	public virtual int Power // [!code ++]
	{ // [!code ++]
		get // [!code ++]
		{ // [!code ++]
			if (owner.sourceCard.vals.Length <= 2) // [!code ++]
			{ // [!code ++]
				return 100; // [!code ++]
			} // [!code ++]
			return int.Parse(owner.sourceCard.vals[2]); // [!code ++]
		} // [!code ++]
	} // [!code ++]

	public virtual string AliasEle => owner.sourceCard.vals.TryGet(3, returnNull: true);

```

## Zone

[`public virtual void OnRegenerate()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Zone.cs#L1163-L1168)
```cs:line-numbers=1163

	public virtual void OnActivate()
	{
		if (!(id == "startVillage2")) // [!code ++]
		{ // [!code ++]
			return; // [!code ++]
		} // [!code ++]
		if (EClass._map.version.IsBelow(0, 23, 226)) // [!code ++]
		{ // [!code ++]
			SetBGM(121, refresh: false); // [!code ++]
		} // [!code ++]
		int num = 0; // [!code ++]
		foreach (Chara chara in EClass._map.charas) // [!code ++]
		{ // [!code ++]
			if (chara.id == "cat_silver" && !chara.IsPCFaction) // [!code ++]
			{ // [!code ++]
				num++; // [!code ++]
			} // [!code ++]
		} // [!code ++]
		if (num > 0) // [!code ++]
		{ // [!code ++]
			Msg.Say("num_silvercat", num.ToString() ?? ""); // [!code ++]
		} // [!code ++]
	}

	public virtual void OnBeforeDeactivate()
```

## Zone_EternalGarden

[`public class Zone_EternalGarden : Zone_Civilized`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/6af02d623baf511a0f08f24f0e08bbb1e6f23803/Elin/Zone_EternalGarden.cs#L1-L23)
```cs:line-numbers=1
public class Zone_EternalGarden : Zone_Civilized
{
	public override void OnActivate() // [!code --]
	{ // [!code --]
		base.OnActivate(); // [!code --]
		if (EClass._map.version.IsBelow(0, 23, 226)) // [!code --]
		{ // [!code --]
			SetBGM(121, refresh: false); // [!code --]
		} // [!code --]
		int num = 0; // [!code --]
		foreach (Chara chara in EClass._map.charas) // [!code --]
		{ // [!code --]
			if (chara.id == "cat_silver" && !chara.IsPCFaction) // [!code --]
			{ // [!code --]
				num++; // [!code --]
			} // [!code --]
		} // [!code --]
		if (num > 0) // [!code --]
		{ // [!code --]
			Msg.Say("num_silvercat", num.ToString() ?? ""); // [!code --]
		} // [!code --]
	} // [!code --]
}
```
