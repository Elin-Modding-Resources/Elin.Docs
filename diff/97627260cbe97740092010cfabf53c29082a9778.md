---
exclude: true
aside: false
pageClass: diff-single-page
footer: false
editLink: false
lastUpdated: false
description: 4 files modified.
version: EA 23.305 Nightly
changes: Chara/ConTransmuteHuman/ELEMENT/Net
---

# EA 23.305 Nightly

May 10, 2026

4 files modified.

## Important Changes

**None.**
## Chara

[`public Chara Duplicate()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/97627260cbe97740092010cfabf53c29082a9778/Elin/Chara.cs#L1837-L1842)
```cs:line-numbers=1837
	chara.isScaled = base.isScaled;
	chara.c_altName = base.c_altName;
	chara._alias = _alias;
	if (pccData != null) // [!code ++]
	{ // [!code ++]
		chara.pccData = IO.DeepCopy(pccData); // [!code ++]
	} // [!code ++]
	if (HaveFur())
	{
		chara.c_fur = -1;
```

## ConTransmuteHuman

[`public override void OnBeforeStart()`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/97627260cbe97740092010cfabf53c29082a9778/Elin/ConTransmuteHuman.cs#L66-L83)
```cs:line-numbers=66
			}
			else
			{
				List<Chara> list2 = owner.pos.ListCharasInRadius(owner, 6, delegate(Chara c) // [!code --]
				{ // [!code --]
					if (!c.IsMultisize && c.IsHumanSpeak) // [!code --]
					{ // [!code --]
						CardRenderer renderer = c.renderer; // [!code --]
						if (renderer != null && !renderer.hasActor) // [!code --]
						{ // [!code --]
							return !c.HasElement(1427); // [!code --]
						} // [!code --]
					} // [!code --]
					return false; // [!code --]
				}); // [!code --]
				List<Chara> list2 = owner.pos.ListCharasInRadius(owner, 6, (Chara c) => !c.IsMultisize && c.IsHumanSpeak && !c.HasElement(1427)); // [!code ++]
				if (list2.Count > 0)
				{
					chara = list2.RandomItem().Duplicate();
```

## ELEMENT

[`public void _WriteNote(UINote n, Chara c, Act act)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/97627260cbe97740092010cfabf53c29082a9778/Elin/ELEMENT.cs#L642-L648)
```cs:line-numbers=642
					condition.SetRefVal(79, (act.id == 8710) ? 222 : 221);
					break;
				}
				n.AddText("_bullet".lang() + (condition.HasDuration ? "hintCon" : "hintCon2").lang(condition.Name, condition.EvaluateTurn(p).ToString() ?? "")); // [!code --]
				n.AddText("_bullet".lang() + (condition.HasDuration ? "hintCon" : "hintCon2").lang(condition.Name, condition.EvaluateTurn(p).ToString() ?? "") + ((condition.HasDuration && condition.MaxDuration != 0 && condition.MaxDuration < 999) ? "hintCon3".lang(condition.MaxDuration.ToString() ?? "") : "")); // [!code ++]
				condition._WriteNote(n, asChild: true);
			}
			else
```

## Net

[`using System;`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/97627260cbe97740092010cfabf53c29082a9778/Elin/Net.cs#L1-L6)
```cs:line-numbers=1
using System;
using System.Collections.Generic;
using System.IO;
using System.Net; // [!code ++]
using Cysharp.Threading.Tasks;
using Newtonsoft.Json;
using Newtonsoft.Json.Linq;
```

[`public static async UniTask<bool> UploadFile(string id, string password, string`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/97627260cbe97740092010cfabf53c29082a9778/Elin/Net.cs#L133-L141)
```cs:line-numbers=133
	}
	WWWForm wWWForm = new WWWForm();
	wWWForm.AddField("mode", 1);
	wWWForm.AddField("id", id); // [!code --]
	wWWForm.AddField("name", name); // [!code --]
	wWWForm.AddField("title", title); // [!code --]
	wWWForm.AddField("id", id.RemoveNewline()); // [!code ++]
	wWWForm.AddField("name", name.RemoveNewline()); // [!code ++]
	wWWForm.AddField("title", title.RemoveNewline()); // [!code ++]
	wWWForm.AddField("cat", cat);
	wWWForm.AddField("tag", tag);
	wWWForm.AddField("idLang", idLang);
```

[`public static async UniTask<List<DownloadMeta>> GetFileList(string idLang)`](https://github.com/Elin-Modding-Resources/Elin-Decompiled/blob/97627260cbe97740092010cfabf53c29082a9778/Elin/Net.cs#L229-L250)
```cs:line-numbers=229
		}
		return null;
	}
	StringReader stringReader = new StringReader(www.downloadHandler.text); // [!code --]
	for (string text = stringReader.ReadLine(); text != null; text = stringReader.ReadLine()) // [!code --]
	string[] array = www.downloadHandler.text.SplitNewline(); // [!code ++]
	foreach (string obj in array) // [!code ++]
	{
		if (!string.IsNullOrEmpty(text)) // [!code --]
		string[] array2 = obj.Split(','); // [!code ++]
		if (obj.StartsWith("files") && array2.Length >= 7) // [!code ++]
		{
			string[] array = text.Split(','); // [!code --]
			list.Add(new DownloadMeta
			{
				path = array[0], // [!code --]
				id = Path.GetFileNameWithoutExtension(array[0]), // [!code --]
				name = array[2], // [!code --]
				title = array[3], // [!code --]
				cat = array[5], // [!code --]
				date = array[6].Replace("\"", ""), // [!code --]
				version = ((array.Length >= 9) ? array[8].ToInt() : 0), // [!code --]
				tag = ((array.Length >= 10) ? array[9] : "") // [!code --]
				path = array2[0], // [!code ++]
				id = WebUtility.HtmlDecode(array2[1]), // [!code ++]
				name = WebUtility.HtmlDecode(array2[2]), // [!code ++]
				title = WebUtility.HtmlDecode(array2[3]), // [!code ++]
				cat = array2[5], // [!code ++]
				date = array2[6].Replace("\"", ""), // [!code ++]
				version = ((array2.Length > 8) ? array2[8].ToInt() : 0), // [!code ++]
				tag = ((array2.Length > 9) ? array2[9] : "") // [!code ++]
			});
		}
	}
```
